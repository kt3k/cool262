// Generated from ecma262/spec.html — do not edit by hand.
const sections = {"":"<p><b>The strict mode restriction and exceptions</b></p>\n  <ul>\n    <li>\n      <code>implements</code>, <code>interface</code>, <code>let</code>, <code>package</code>, <code>private</code>, <code>protected</code>, <code>public</code>, <code>static</code>, and <code>yield</code> are reserved words within strict mode code. (12.7.2).\n    </li>\n    <li>\n      A conforming implementation, when processing strict mode code, must disallow instances of the productions <code class=\"emu-grammar\">NumericLiteral :: LegacyOctalIntegerLiteral</code> and <code class=\"emu-grammar\">DecimalIntegerLiteral :: NonOctalDecimalIntegerLiteral</code>.\n    </li>\n    <li>\n      A conforming implementation, when processing strict mode code, must disallow instances of the productions <code class=\"emu-grammar\">EscapeSequence :: LegacyOctalEscapeSequence</code> and <code class=\"emu-grammar\">EscapeSequence :: NonOctalDecimalEscapeSequence</code>.\n    </li>\n    <li>\n      Assignment to an undeclared identifier or otherwise unresolvable reference does not create a property in the global object. When a simple assignment occurs within strict mode code, its <emu-nt>LeftHandSideExpression</emu-nt> must not evaluate to an unresolvable Reference. If it does a <b>ReferenceError</b> exception is thrown (6.2.5.6). The <emu-nt>LeftHandSideExpression</emu-nt> also may not be a reference to a data property with the attribute value { [[Writable]]: <b>false</b> }, to an accessor property with the attribute value { [[Set]]: <b>undefined</b> }, nor to a non-existent property of an object whose [[Extensible]] internal slot is <b>false</b>. In these cases a <code>TypeError</code> exception is thrown (13.15).\n    </li>\n    <li>\n      An <emu-nt>IdentifierReference</emu-nt> with the StringValue <b>\"eval\"</b> or <b>\"arguments\"</b> may not appear as the <emu-nt>LeftHandSideExpression</emu-nt> of an Assignment operator (13.15) or of an <emu-nt>UpdateExpression</emu-nt> (13.4) or as the <emu-nt>UnaryExpression</emu-nt> operated upon by a Prefix Increment (13.4.4) or a Prefix Decrement (13.4.5) operator.\n    </li>\n    <li>\n      Arguments objects for strict functions define a non-configurable accessor property <b>\"callee\"</b> which throws a <b>TypeError</b> exception on access (10.4.4.6).\n    </li>\n    <li>\n      Arguments objects for strict functions do not dynamically share their <emu-xref href=\"#array-index\">array-indexed</emu-xref> property values with the corresponding formal parameter bindings of their functions. (10.4.4).\n    </li>\n    <li>\n      For strict functions, if an arguments object is created the binding of the local identifier <code>arguments</code> to the arguments object is immutable and hence may not be the target of an assignment expression. (10.2.11).\n    </li>\n    <li>\n      It is a <b>SyntaxError</b> if the StringValue of a <emu-nt>BindingIdentifier</emu-nt> is either <b>\"eval\"</b> or <b>\"arguments\"</b> within strict mode code (13.1.1).\n    </li>\n    <li>\n      Strict mode eval code cannot instantiate variables or functions in the variable environment of the caller to eval. Instead, a new variable environment is created and that environment is used for declaration binding instantiation for the eval code (19.2.1).\n    </li>\n    <li>\n      If <b>this</b> is evaluated within strict mode code, then the <b>this</b> value is not coerced to an object. A <b>this</b> value of either <b>undefined</b> or <b>null</b> is not converted to the global object and primitive values are not converted to wrapper objects. The <b>this</b> value passed via a function call (including calls made using <code>Function.prototype.apply</code> and <code>Function.prototype.call</code>) do not coerce the passed <b>this</b> value to an object (10.2.1.2, 20.2.3.1, 20.2.3.3).\n    </li>\n    <li>\n      When a <code>delete</code> operator occurs within strict mode code, a <b>SyntaxError</b> is thrown if its <emu-nt>UnaryExpression</emu-nt> is a direct reference to a variable, function argument, or function name (13.5.1.1).\n    </li>\n    <li>\n      When a <code>delete</code> operator occurs within strict mode code, a <b>TypeError</b> is thrown if the property to be deleted has the attribute { [[Configurable]]: <b>false</b> } or otherwise cannot be deleted (13.5.1.2).\n    </li>\n    <li>\n      Strict mode code may not include a <emu-nt>WithStatement</emu-nt>. The occurrence of a <emu-nt>WithStatement</emu-nt> in such a context is a <b>SyntaxError</b> (14.11.1).\n    </li>\n    <li>\n      It is a <b>SyntaxError</b> if a <emu-nt>CatchParameter</emu-nt> occurs within strict mode code and the BoundNames of <emu-nt>CatchParameter</emu-nt> contains either <code>eval</code> or <code>arguments</code> (14.15.1).\n    </li>\n    <li>\n      It is a <b>SyntaxError</b> if the same <emu-nt>BindingIdentifier</emu-nt> appears more than once in the <emu-nt>FormalParameters</emu-nt> of a strict function. An attempt to create such a function using a Function, Generator, or AsyncFunction constructor is a <b>SyntaxError</b> (15.2.1, 20.2.1.1.1).\n    </li>\n    <li>\n      An implementation may not extend, beyond that defined in this specification, the meanings within strict functions of properties named <b>\"caller\"</b> or <b>\"arguments\"</b> of function instances.\n    </li>\n  </ul>"};
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
