// Generated from ecma262/spec.html — do not edit by hand.
const _sections = {"1":"<h2>Syntax</h2>\n    <pre class=\"emu-grammar\"><span class=\"nt lhs\">SourceCharacter</span> <span class=\"geq\">::</span>\n  <span class=\"desc\">&gt; any Unicode code point</span></pre>\n    <p><dfn>ECMAScript source text</dfn> is a sequence of Unicode code points. All Unicode code point values from U+0000 to U+10FFFF, including surrogate code points, may occur in ECMAScript source text where permitted by the ECMAScript grammars. The actual encodings used to store and interchange ECMAScript source text is not relevant to this specification. Regardless of the external source text encoding, a conforming ECMAScript implementation processes the source text as if it was an equivalent sequence of <emu-nt>SourceCharacter</emu-nt> values, each <emu-nt>SourceCharacter</emu-nt> being a Unicode code point. Conforming ECMAScript implementations are not required to perform any normalization of source text, or behave as though they were performing normalization of source text.</p>\n    <p>The components of a combining character sequence are treated as individual Unicode code points even though a user might think of the whole sequence as a single character.</p>\n    <emu-note>\n      <p>In string literals, regular expression literals, template literals and identifiers, any Unicode code point may also be expressed using Unicode escape sequences that explicitly express a code point's numeric value. Within a comment, such an escape sequence is effectively ignored as part of the comment.</p>\n      <p>ECMAScript differs from the Java programming language in the behaviour of Unicode escape sequences. In a Java program, if the Unicode escape sequence <code>\\\\u000A</code>, for example, occurs within a single-line comment, it is interpreted as a line terminator (Unicode code point U+000A is LINE FEED (LF)) and therefore the next code point is not part of the comment. Similarly, if the Unicode escape sequence <code>\\\\u000A</code> occurs within a string literal in a Java program, it is likewise interpreted as a line terminator, which is not allowed within a string literal—one must write <code>\\\\n</code> instead of <code>\\\\u000A</code> to cause a LINE FEED (LF) to be part of the value of a string literal. In an ECMAScript program, a Unicode escape sequence occurring within a comment is never interpreted and therefore cannot contribute to termination of the comment. Similarly, a Unicode escape sequence occurring within a string literal in an ECMAScript program always contributes to the literal and is never interpreted as a line terminator or as a code point that might terminate the string literal.</p>\n    </emu-note>\n\n    ","2":"<p>There are four types of ECMAScript code:</p>\n    <ul>\n      <li>\n        <dfn>Global code</dfn> is source text that is treated as an ECMAScript <emu-nt>Script</emu-nt>. The global code of a particular <emu-nt>Script</emu-nt> does not include any source text that is parsed as part of a <emu-nt>FunctionDeclaration</emu-nt>, <emu-nt>FunctionExpression</emu-nt>, <emu-nt>GeneratorDeclaration</emu-nt>, <emu-nt>GeneratorExpression</emu-nt>, <emu-nt>AsyncFunctionDeclaration</emu-nt>, <emu-nt>AsyncFunctionExpression</emu-nt>, <emu-nt>AsyncGeneratorDeclaration</emu-nt>, <emu-nt>AsyncGeneratorExpression</emu-nt>, <emu-nt>MethodDefinition</emu-nt>, <emu-nt>ArrowFunction</emu-nt>, <emu-nt>AsyncArrowFunction</emu-nt>, <emu-nt>ClassDeclaration</emu-nt>, or <emu-nt>ClassExpression</emu-nt>.\n      </li>\n      <li>\n        <dfn>Eval code</dfn> is the source text supplied to the built-in <code>eval</code> function. More precisely, if the parameter to the built-in <code>eval</code> function is a String, it is treated as an ECMAScript <emu-nt>Script</emu-nt>. The eval code for a particular invocation of <code>eval</code> is the global code portion of that <emu-nt>Script</emu-nt>.\n      </li>\n      <li>\n        <p><dfn>Function code</dfn> is source text that is parsed to supply the value of the [[ECMAScriptCode]] and [[FormalParameters]] internal slots (see <a href=\"/ordinary-and-exotic-objects-behaviours#sec-ecmascript-function-objects\">10.2</a>) of an ECMAScript function object. The function code of a particular ECMAScript function does not include any source text that is parsed as the function code of a nested <emu-nt>FunctionDeclaration</emu-nt>, <emu-nt>FunctionExpression</emu-nt>, <emu-nt>GeneratorDeclaration</emu-nt>, <emu-nt>GeneratorExpression</emu-nt>, <emu-nt>AsyncFunctionDeclaration</emu-nt>, <emu-nt>AsyncFunctionExpression</emu-nt>, <emu-nt>AsyncGeneratorDeclaration</emu-nt>, <emu-nt>AsyncGeneratorExpression</emu-nt>, <emu-nt>MethodDefinition</emu-nt>, <emu-nt>ArrowFunction</emu-nt>, <emu-nt>AsyncArrowFunction</emu-nt>, <emu-nt>ClassDeclaration</emu-nt>, or <emu-nt>ClassExpression</emu-nt>.</p>\n        <p>In addition, if the source text referred to above is parsed as:</p>\n        <ul>\n          <li>the <emu-nt>FormalParameters</emu-nt> and <emu-nt>FunctionBody</emu-nt> of a <emu-nt>FunctionDeclaration</emu-nt> or <emu-nt>FunctionExpression</emu-nt>,</li>\n          <li>the <emu-nt>FormalParameters</emu-nt> and <emu-nt>GeneratorBody</emu-nt> of a <emu-nt>GeneratorDeclaration</emu-nt> or <emu-nt>GeneratorExpression</emu-nt>,</li>\n          <li>the <emu-nt>FormalParameters</emu-nt> and <emu-nt>AsyncFunctionBody</emu-nt> of an <emu-nt>AsyncFunctionDeclaration</emu-nt> or <emu-nt>AsyncFunctionExpression</emu-nt>, or</li>\n          <li>the <emu-nt>FormalParameters</emu-nt> and <emu-nt>AsyncGeneratorBody</emu-nt> of an <emu-nt>AsyncGeneratorDeclaration</emu-nt> or <emu-nt>AsyncGeneratorExpression</emu-nt>,</li>\n        </ul>\n        <p>then the source text matched by the <emu-nt>BindingIdentifier</emu-nt> (if any) of that declaration or expression is also included in the function code of the corresponding function.</p>\n      </li>\n      <li>\n        <dfn>Module code</dfn> is source text that is code that is provided as a <emu-nt>ModuleBody</emu-nt>. It is the code that is directly evaluated when a module is initialized. The module code of a particular module does not include any source text that is parsed as part of a nested <emu-nt>FunctionDeclaration</emu-nt>, <emu-nt>FunctionExpression</emu-nt>, <emu-nt>GeneratorDeclaration</emu-nt>, <emu-nt>GeneratorExpression</emu-nt>, <emu-nt>AsyncFunctionDeclaration</emu-nt>, <emu-nt>AsyncFunctionExpression</emu-nt>, <emu-nt>AsyncGeneratorDeclaration</emu-nt>, <emu-nt>AsyncGeneratorExpression</emu-nt>, <emu-nt>MethodDefinition</emu-nt>, <emu-nt>ArrowFunction</emu-nt>, <emu-nt>AsyncArrowFunction</emu-nt>, <emu-nt>ClassDeclaration</emu-nt>, or <emu-nt>ClassExpression</emu-nt>.\n      </li>\n    </ul>\n    <emu-note>\n      <p>Function code is generally provided as the bodies of Function Definitions (<a href=\"/ecmascript-language-functions-and-classes#sec-function-definitions\">15.2</a>), Arrow Function Definitions (<a href=\"/ecmascript-language-functions-and-classes#sec-arrow-function-definitions\">15.3</a>), Method Definitions (<a href=\"/ecmascript-language-functions-and-classes#sec-method-definitions\">15.4</a>), Generator Function Definitions (<a href=\"/ecmascript-language-functions-and-classes#sec-generator-function-definitions\">15.5</a>), Async Function Definitions (<a href=\"/ecmascript-language-functions-and-classes#sec-async-function-definitions\">15.8</a>), Async Generator Function Definitions (<a href=\"/ecmascript-language-functions-and-classes#sec-async-generator-function-definitions\">15.6</a>), and Async Arrow Functions (<a href=\"/ecmascript-language-functions-and-classes#sec-async-arrow-function-definitions\">15.9</a>). Function code is also derived from the arguments to the Function constructor (<a href=\"/fundamental-objects#sec-function-p1-p2-pn-body\">20.2.1.1</a>), the GeneratorFunction constructor (<a href=\"/control-abstraction-objects#sec-generatorfunction\">27.3.1.1</a>), the AsyncFunction constructor (<a href=\"/control-abstraction-objects#sec-async-function-constructor-arguments\">27.7.1.1</a>), and the AsyncGeneratorFunction constructor (<a href=\"/control-abstraction-objects#sec-asyncgeneratorfunction\">27.4.1.1</a>).</p>\n    </emu-note>\n    <emu-note>\n      <p>The practical effect of including the <emu-nt>BindingIdentifier</emu-nt> in function code is that the Early Errors for strict mode code are applied to a <emu-nt>BindingIdentifier</emu-nt> that is the name of a function whose body contains a \"use strict\" directive, even if the surrounding code is not strict mode code.</p>\n    </emu-note>\n\n    ","":"","1.1":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Assert: 0 ≤ <var>cp</var> ≤ 0x10FFFF.</li><li>If <var>cp</var> ≤ 0xFFFF, return the String value consisting of the code unit whose numeric value is <var>cp</var>.</li><li>Let <var>cu1</var> be the code unit whose numeric value is floor((<var>cp</var> - 0x10000) / 0x400) + 0xD800.</li><li>Let <var>cu2</var> be the code unit whose numeric value is ((<var>cp</var> - 0x10000) modulo 0x400) + 0xDC00.</li><li>Return the string-concatenation of <var>cu1</var> and <var>cu2</var>.</li></ol></emu-alg>\n    ","1.2":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>text</var> into a String value, as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>result</var> be the empty String.</li><li>For each code point <var>cp</var> of <var>text</var>, do<ol><li>Set <var>result</var> to the string-concatenation of <var>result</var> and UTF16EncodeCodePoint(<var>cp</var>).</li></ol></li><li>Return <var>result</var>.</li></ol></emu-alg>\n    ","1.3":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>Two code units that form a UTF-16 surrogate pair are converted to a code point.</dd>\n      </dl>\n      <emu-alg><ol><li>Assert: <var>lead</var> is a leading surrogate and <var>trail</var> is a trailing surrogate.</li><li>Let <var>cp</var> be (<var>lead</var> - 0xD800) × 0x400 + (<var>trail</var> - 0xDC00) + 0x10000.</li><li>Return the code point <var>cp</var>.</li></ol></emu-alg>\n    ","1.4":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It interprets <var>string</var> as a sequence of UTF-16 encoded code points, as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>, and reads from it a single code point starting with the code unit at index <var>position</var>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>size</var> be the length of <var>string</var>.</li><li>Assert: <var>position</var> ≥ 0 and <var>position</var> &lt; <var>size</var>.</li><li>Let <var>first</var> be the code unit at index <var>position</var> within <var>string</var>.</li><li>Let <var>cp</var> be the code point whose numeric value is the numeric value of <var>first</var>.</li><li>If <var>first</var> is neither a leading surrogate nor a trailing surrogate, then<ol><li>Return the Record { [[CodePoint]]: <var>cp</var>, [[CodeUnitCount]]: 1, [[IsUnpairedSurrogate]]: <b>false</b> }.</li></ol></li><li>If <var>first</var> is a trailing surrogate or <var>position</var> + 1 = <var>size</var>, then<ol><li>Return the Record { [[CodePoint]]: <var>cp</var>, [[CodeUnitCount]]: 1, [[IsUnpairedSurrogate]]: <b>true</b> }.</li></ol></li><li>Let <var>second</var> be the code unit at index <var>position</var> + 1 within <var>string</var>.</li><li>If <var>second</var> is not a trailing surrogate, then<ol><li>Return the Record { [[CodePoint]]: <var>cp</var>, [[CodeUnitCount]]: 1, [[IsUnpairedSurrogate]]: <b>true</b> }.</li></ol></li><li>Set <var>cp</var> to UTF16SurrogatePairToCodePoint(<var>first</var>, <var>second</var>).</li><li>Return the Record { [[CodePoint]]: <var>cp</var>, [[CodeUnitCount]]: 2, [[IsUnpairedSurrogate]]: <b>false</b> }.</li></ol></emu-alg>\n    ","1.5":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It returns the sequence of Unicode code points that results from interpreting <var>string</var> as UTF-16 encoded Unicode text as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>codePoints</var> be a new empty List.</li><li>Let <var>size</var> be the length of <var>string</var>.</li><li>Let <var>position</var> be 0.</li><li>Repeat, while <var>position</var> &lt; <var>size</var>,<ol><li>Let <var>cp</var> be CodePointAt(<var>string</var>, <var>position</var>).</li><li>Append <var>cp</var>.[[CodePoint]] to <var>codePoints</var>.</li><li>Set <var>position</var> to <var>position</var> + <var>cp</var>.[[CodeUnitCount]].</li></ol></li><li>Return <var>codePoints</var>.</li></ol></emu-alg>\n    ","1.6":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If <var>sourceText</var> is a String, set <var>sourceText</var> to StringToCodePoints(<var>sourceText</var>).</li><li>Attempt to parse <var>sourceText</var> using <var>goalSymbol</var> as the goal symbol, and analyse the parse result for any early error conditions. Parsing and early error detection may be interleaved in an implementation-defined manner.</li><li>If the parse succeeded and no early errors were found, return the Parse Node (an instance of <var>goalSymbol</var>) at the root of the parse tree resulting from the parse.</li><li>Return a List of one or more <b>SyntaxError</b> objects representing the parsing errors and/or early errors. If more than one parsing error or early error is present, the number and ordering of error objects in the list is implementation-defined, but at least one must be present.</li></ol></emu-alg>\n      <emu-note>\n        <p>Consider a text that has an early error at a particular point, and also a syntax error at a later point. An implementation that does a parse pass followed by an early errors pass might report the syntax error and not proceed to the early errors pass. An implementation that interleaves the two activities might report the early error and not proceed to find the syntax error. A third implementation might report both errors. All of these behaviours are conformant.</p>\n      </emu-note>\n      <emu-note>\n        <p>See also clause <a href=\"/error-handling-and-language-extensions#sec-error-handling-and-language-extensions\">17</a>.</p>\n      </emu-note>\n    ","2.1":"<p>A <dfn id=\"directive-prologue\" variants=\"Directive Prologues\">Directive Prologue</dfn> is the longest sequence of <emu-nt>ExpressionStatement</emu-nt>s occurring as the initial <emu-nt>StatementListItem</emu-nt>s or <emu-nt>ModuleItem</emu-nt>s of a <emu-nt>FunctionBody</emu-nt>, a <emu-nt>ScriptBody</emu-nt>, or a <emu-nt>ModuleBody</emu-nt> and where each <emu-nt>ExpressionStatement</emu-nt> in the sequence consists entirely of a <emu-nt>StringLiteral</emu-nt> token followed by a semicolon. The semicolon may appear explicitly or may be inserted by automatic semicolon insertion (<a href=\"/ecmascript-language-lexical-grammar#sec-automatic-semicolon-insertion\">12.10</a>). A Directive Prologue may be an empty sequence.</p>\n      <p>A <dfn id=\"use-strict-directive\" variants=\"Use Strict Directives\">Use Strict Directive</dfn> is an <emu-nt>ExpressionStatement</emu-nt> in a Directive Prologue whose <emu-nt>StringLiteral</emu-nt> is either of the exact code point sequences <code>\"use strict\"</code> or <code>'use strict'</code>. A Use Strict Directive may not contain an <emu-nt>EscapeSequence</emu-nt> or <emu-nt>LineContinuation</emu-nt>.</p>\n      <p>A Directive Prologue may contain more than one Use Strict Directive. However, an implementation may issue a warning if this occurs.</p>\n      <emu-note>\n        <p>The <emu-nt>ExpressionStatement</emu-nt>s of a Directive Prologue are evaluated normally during evaluation of the containing production. Implementations may define implementation specific meanings for <emu-nt>ExpressionStatement</emu-nt>s which are not a Use Strict Directive and which occur in a Directive Prologue. If an appropriate notification mechanism exists, an implementation should issue a warning if it encounters in a Directive Prologue an <emu-nt>ExpressionStatement</emu-nt> that is not a Use Strict Directive and which does not have a meaning defined by the implementation.</p>\n      </emu-note>\n    ","2.2":"<p>An ECMAScript syntactic unit may be processed using either unrestricted or strict mode syntax and semantics (<a href=\"/overview#sec-strict-variant-of-ecmascript\">4.3.2</a>). Code is interpreted as <dfn>strict mode code</dfn> in the following situations:</p>\n      <ul>\n        <li>\n          Global code is strict mode code if it begins with a Directive Prologue that contains a Use Strict Directive.\n        </li>\n        <li>\n          Module code is always strict mode code.\n        </li>\n        <li>\n          All parts of a <emu-nt>ClassDeclaration</emu-nt> or a <emu-nt>ClassExpression</emu-nt> are strict mode code.\n        </li>\n        <li>\n          Eval code is strict mode code if it begins with a Directive Prologue that contains a Use Strict Directive or if the call to <code>eval</code> is a direct eval that is contained in strict mode code.\n        </li>\n        <li>\n          Function code is strict mode code if the associated <emu-nt>FunctionDeclaration</emu-nt>, <emu-nt>FunctionExpression</emu-nt>, <emu-nt>GeneratorDeclaration</emu-nt>, <emu-nt>GeneratorExpression</emu-nt>, <emu-nt>AsyncFunctionDeclaration</emu-nt>, <emu-nt>AsyncFunctionExpression</emu-nt>, <emu-nt>AsyncGeneratorDeclaration</emu-nt>, <emu-nt>AsyncGeneratorExpression</emu-nt>, <emu-nt>MethodDefinition</emu-nt>, <emu-nt>ArrowFunction</emu-nt>, or <emu-nt>AsyncArrowFunction</emu-nt> is contained in strict mode code or if the code that produces the value of the function's [[ECMAScriptCode]] internal slot begins with a Directive Prologue that contains a Use Strict Directive.\n        </li>\n        <li>\n          Function code that is supplied as the arguments to the built-in Function, Generator, AsyncFunction, and AsyncGenerator constructors is strict mode code if the last argument is a String that when processed is a <emu-nt>FunctionBody</emu-nt> that begins with a Directive Prologue that contains a Use Strict Directive.\n        </li>\n      </ul>\n      <p>ECMAScript code that is not strict mode code is called <dfn id=\"non-strict-code\">non-strict code</dfn>.</p>\n\n      ","2.2.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If the source text matched by <var>node</var> is strict mode code, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","2.3":"<p>An ECMAScript implementation may support the evaluation of function exotic objects whose evaluative behaviour is expressed in some host-defined form of executable code other than ECMAScript source text. Whether a function object is defined within ECMAScript code or is a built-in function is not observable from the perspective of ECMAScript code that calls or is called by such a function object.</p>\n    "};
const _basePath = process.env.NEXT_PUBLIC_BASE_PATH || '';
const sections = _basePath
  ? Object.fromEntries(
      Object.entries(_sections).map(([k, v]) => [
        k,
        v.replaceAll('href="/', `href="${_basePath}/`),
      ])
    )
  : _sections;
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
