// Generated from ecma262/spec.html — do not edit by hand.
const sections = {"1":"","2":"<p>When the ECMAScript host is a web browser the following additional properties of the standard built-in objects are defined.</p>\n\n    ","3":"","":"<p>The ECMAScript language syntax and semantics defined in this annex are required when the ECMAScript host is a web browser. The content of this annex is normative but optional if the ECMAScript host is not a web browser.</p>\n  <p>Some features defined in this annex are specified in this annex, and some are specified in the main body of this document.</p>\n  <p>When a feature is specified in the main body, each point where it affects the document is marked with the words \"Normative Optional\" in a coloured box. Moreover, where the feature involves particular wording in an algorithm or early error rule, this is guarded by the condition that “<dfn variants=\"otherwise supports\">the host supports</dfn>” the relevant feature. Web browsers are required to support all such features.</p>\n  <emu-note>\n    <p>This annex describes various legacy features and other characteristics of web browser ECMAScript hosts. All of the language features and behaviours specified in this annex have one or more undesirable characteristics and in the absence of legacy usage would be removed from this specification. However, the usage of these features by large numbers of existing web pages means that web browsers must continue to support them. The specifications in this annex define the requirements for interoperable implementations of these legacy features.</p>\n    <p>These features are not considered part of the core ECMAScript language. Programmers should not use or assume the existence of these features and behaviours when writing new ECMAScript code. ECMAScript implementations are discouraged from implementing these features unless the implementation is part of a web browser or is required to run the same legacy ECMAScript code that web browsers encounter.</p>\n  </emu-note>\n\n  ","1.1":"<p>The syntax and semantics of 12.4 is extended as follows except that this extension is not allowed when parsing source text using the goal symbol |Module|:</p>\n      <h2>Syntax</h2>\n      <pre class=\"emu-grammar\">InputElementHashbangOrRegExp ::\n  WhiteSpace\n  LineTerminator\n  Comment\n  CommonToken\n  HashbangComment\n  RegularExpressionLiteral\n  HTMLCloseComment\n\nComment ::\n  MultiLineComment\n  SingleLineComment\n  SingleLineHTMLOpenComment\n  SingleLineHTMLCloseComment\n  SingleLineDelimitedComment\n\nMultiLineComment ::\n  `/*` FirstCommentLine? LineTerminator MultiLineCommentChars? `*/` HTMLCloseComment?\n\nFirstCommentLine ::\n  SingleLineDelimitedCommentChars\n\nSingleLineHTMLOpenComment ::\n  `&lt;!--` SingleLineCommentChars?\n\nSingleLineHTMLCloseComment ::\n  LineTerminatorSequence HTMLCloseComment\n\nSingleLineDelimitedComment ::\n  `/*` SingleLineDelimitedCommentChars? `*/`\n\nHTMLCloseComment ::\n  WhiteSpaceSequence? SingleLineDelimitedCommentSequence? `--&gt;` SingleLineCommentChars?\n\nSingleLineDelimitedCommentChars ::\n  SingleLineNotAsteriskChar SingleLineDelimitedCommentChars?\n  `*` SingleLinePostAsteriskCommentChars?\n\nSingleLineNotAsteriskChar ::\n  SourceCharacter but not one of `*` or LineTerminator\n\nSingleLinePostAsteriskCommentChars ::\n  SingleLineNotForwardSlashOrAsteriskChar SingleLineDelimitedCommentChars?\n  `*` SingleLinePostAsteriskCommentChars?\n\nSingleLineNotForwardSlashOrAsteriskChar ::\n  SourceCharacter but not one of `/` or `*` or LineTerminator\n\nWhiteSpaceSequence ::\n  WhiteSpace WhiteSpaceSequence?\n\nSingleLineDelimitedCommentSequence ::\n  SingleLineDelimitedComment WhiteSpaceSequence? SingleLineDelimitedCommentSequence?</pre>\n      <p>Similar to a |MultiLineComment| that contains a line terminator code point, a |SingleLineHTMLCloseComment| is considered to be a |LineTerminator| for purposes of parsing by the syntactic grammar.</p>\n    ","1.2":"<p>The syntax of 22.2.1 is modified and extended as follows. These changes introduce ambiguities that are broken by the ordering of grammar productions and by contextual information. When parsing using the following grammar, each alternative is considered only if previous production alternatives do not match.</p>\n      <p>This alternative pattern grammar and semantics only changes the syntax and semantics of BMP patterns. The following grammar extensions include productions parameterized with the [UnicodeMode] parameter. However, none of these extensions change the syntax of Unicode patterns recognized when parsing with the [UnicodeMode] parameter present on the goal symbol.</p>\n      <h2>Syntax</h2>\n      <pre class=\"emu-grammar\">Term[UnicodeMode, UnicodeSetsMode, NamedCaptureGroups] ::\n  [+UnicodeMode] Assertion[+UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]\n  [+UnicodeMode] Atom[+UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups] Quantifier\n  [+UnicodeMode] Atom[+UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]\n  [~UnicodeMode] QuantifiableAssertion[?NamedCaptureGroups] Quantifier\n  [~UnicodeMode] Assertion[~UnicodeMode, ~UnicodeSetsMode, ?NamedCaptureGroups]\n  [~UnicodeMode] ExtendedAtom[?NamedCaptureGroups] Quantifier\n  [~UnicodeMode] ExtendedAtom[?NamedCaptureGroups]\n\nAssertion[UnicodeMode, UnicodeSetsMode, NamedCaptureGroups] ::\n  `^`\n  `$`\n  `\\b`\n  `\\B`\n  [+UnicodeMode] `(?=` Disjunction[+UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups] `)`\n  [+UnicodeMode] `(?!` Disjunction[+UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups] `)`\n  [~UnicodeMode] QuantifiableAssertion[?NamedCaptureGroups]\n  `(?&lt;=` Disjunction[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups] `)`\n  `(?&lt;!` Disjunction[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups] `)`\n\nQuantifiableAssertion[NamedCaptureGroups] ::\n  `(?=` Disjunction[~UnicodeMode, ~UnicodeSetsMode, ?NamedCaptureGroups] `)`\n  `(?!` Disjunction[~UnicodeMode, ~UnicodeSetsMode, ?NamedCaptureGroups] `)`\n\nExtendedAtom[NamedCaptureGroups] ::\n  `.`\n  `\\` AtomEscape[~UnicodeMode, ?NamedCaptureGroups]\n  `\\` [lookahead == `c`]\n  CharacterClass[~UnicodeMode, ~UnicodeSetsMode]\n  `(` GroupSpecifier[~UnicodeMode]? Disjunction[~UnicodeMode, ~UnicodeSetsMode, ?NamedCaptureGroups] `)`\n  `(?` RegularExpressionModifiers `:` Disjunction[~UnicodeMode, ~UnicodeSetsMode, ?NamedCaptureGroups] `)`\n  `(?` RegularExpressionModifiers `-` RegularExpressionModifiers `:` Disjunction[~UnicodeMode, ~UnicodeSetsMode, ?NamedCaptureGroups] `)`\n  InvalidBracedQuantifier\n  ExtendedPatternCharacter\n\nInvalidBracedQuantifier ::\n  `{` DecimalDigits[~Sep] `}`\n  `{` DecimalDigits[~Sep] `,}`\n  `{` DecimalDigits[~Sep] `,` DecimalDigits[~Sep] `}`\n\nExtendedPatternCharacter ::\n  SourceCharacter but not one of `^` `$` `\\` `.` `*` `+` `?` `(` `)` `[` `|`\n\nAtomEscape[UnicodeMode, NamedCaptureGroups] ::\n  [+UnicodeMode] DecimalEscape\n  [~UnicodeMode] DecimalEscape [> but only if the CapturingGroupNumber of |DecimalEscape| is &le; CountLeftCapturingParensWithin(the |Pattern| containing |DecimalEscape|)]\n  CharacterClassEscape[?UnicodeMode]\n  CharacterEscape[?UnicodeMode, ?NamedCaptureGroups]\n  [+NamedCaptureGroups] `k` GroupName[?UnicodeMode]\n\nCharacterEscape[UnicodeMode, NamedCaptureGroups] ::\n  ControlEscape\n  `c` AsciiLetter\n  `0` [lookahead &notin; DecimalDigit]\n  HexEscapeSequence\n  RegExpUnicodeEscapeSequence[?UnicodeMode]\n  [~UnicodeMode] LegacyOctalEscapeSequence\n  IdentityEscape[?UnicodeMode, ?NamedCaptureGroups]\n\nIdentityEscape[UnicodeMode, NamedCaptureGroups] ::\n  [+UnicodeMode] SyntaxCharacter\n  [+UnicodeMode] `/`\n  [~UnicodeMode] SourceCharacterIdentityEscape[?NamedCaptureGroups]\n\nSourceCharacterIdentityEscape[NamedCaptureGroups] ::\n  [~NamedCaptureGroups] SourceCharacter but not `c`\n  [+NamedCaptureGroups] SourceCharacter but not one of `c` or `k`\n\nClassAtomNoDash[UnicodeMode, NamedCaptureGroups] ::\n  SourceCharacter but not one of `\\` or `]` or `-`\n  `\\` ClassEscape[?UnicodeMode, ?NamedCaptureGroups]\n  `\\` [lookahead == `c`]\n\nClassEscape[UnicodeMode, NamedCaptureGroups] ::\n  `b`\n  [+UnicodeMode] `-`\n  [~UnicodeMode] `c` ClassControlLetter\n  CharacterClassEscape[?UnicodeMode]\n  CharacterEscape[?UnicodeMode, ?NamedCaptureGroups]\n\nClassControlLetter ::\n  DecimalDigit\n  `_`</pre>\n      <emu-note>\n        <p>When the same left-hand sides occurs with both [+UnicodeMode] and [\\~UnicodeMode] guards it is to control the disambiguation priority.</p>\n      </emu-note>\n\n      ","1.2.1":"<p>The semantics of 22.2.1.1 is extended as follows:</p>\n        <pre class=\"emu-grammar\">ExtendedAtom :: InvalidBracedQuantifier</pre>\n        <ul>\n          <li>\n            It is a Syntax Error if any source text is matched by this production.\n          </li>\n        </ul>\n        <p>Additionally, the rules for the following productions are modified with the addition of the <ins>highlighted</ins> text:</p>\n        <pre class=\"emu-grammar\">NonemptyClassRanges :: ClassAtom `-` ClassAtom ClassContents</pre>\n        <ul>\n          <li>\n            It is a Syntax Error if IsCharacterClass of the first |ClassAtom| is *true* or IsCharacterClass of the second |ClassAtom| is *true* <ins>and this production has a <sub>[UnicodeMode]</sub> parameter</ins>.\n          </li>\n          <li>\n            It is a Syntax Error if IsCharacterClass of the first |ClassAtom| is *false*, IsCharacterClass of the second |ClassAtom| is *false*, and the CharacterValue of the first |ClassAtom| is strictly greater than the CharacterValue of the second |ClassAtom|.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\">NonemptyClassRangesNoDash :: ClassAtomNoDash `-` ClassAtom ClassContents</pre>\n        <ul>\n          <li>\n            It is a Syntax Error if IsCharacterClass of |ClassAtomNoDash| is *true* or IsCharacterClass of |ClassAtom| is *true* <ins>and this production has a <sub>[UnicodeMode]</sub> parameter</ins>.\n          </li>\n          <li>\n            It is a Syntax Error if IsCharacterClass of |ClassAtomNoDash| is *false*, IsCharacterClass of |ClassAtom| is *false*, and the CharacterValue of |ClassAtomNoDash| is strictly greater than the CharacterValue of |ClassAtom|.\n          </li>\n        </ul>\n      ","1.2.2":"<p>In the definitions of CountLeftCapturingParensWithin and CountLeftCapturingParensBefore, references to “<code class=\"emu-grammar\">Atom :: `(` GroupSpecifier? Disjunction `)`</code> ” are to be interpreted as meaning “<code class=\"emu-grammar\">Atom :: `(` GroupSpecifier? Disjunction `)`</code> ” or “<code class=\"emu-grammar\">ExtendedAtom :: `(` GroupSpecifier? Disjunction `)`</code> ”.</p>\n      ","1.2.3":"<p>The semantics of 22.2.1.6 is extended as follows:</p>\n        <pre class=\"emu-grammar\">ClassAtomNoDash :: `\\` [lookahead == `c`]</pre>\n        <emu-alg><ol><li>Return *false*.</li></ol></emu-alg>\n      ","1.2.4":"<p>The semantics of 22.2.1.7 is extended as follows:</p>\n        <pre class=\"emu-grammar\">ClassAtomNoDash :: `\\` [lookahead == `c`]</pre>\n        <emu-alg><ol><li>Return the numeric value of U+005C (REVERSE SOLIDUS).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\">ClassEscape :: `c` ClassControlLetter</pre>\n        <emu-alg><ol><li>Let _ch_ be the code point matched by |ClassControlLetter|.</li><li>Let _i_ be the numeric value of _ch_.</li><li>Return the remainder of dividing _i_ by 32.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\">CharacterEscape :: LegacyOctalEscapeSequence</pre>\n        <emu-alg><ol><li>Return the MV of |LegacyOctalEscapeSequence| (see 12.9.4.3).</li></ol></emu-alg>\n      ","1.2.5":"<p>The semantics of CompileSubpattern is extended as follows:</p>\n\n        <p>The rule for <code class=\"emu-grammar\">Term :: QuantifiableAssertion Quantifier</code> is the same as for <code class=\"emu-grammar\">Term :: Atom Quantifier</code> but with |QuantifiableAssertion| substituted for |Atom|.</p>\n        <p>The rule for <code class=\"emu-grammar\">Term :: ExtendedAtom Quantifier</code> is the same as for <code class=\"emu-grammar\">Term :: Atom Quantifier</code> but with |ExtendedAtom| substituted for |Atom|.</p>\n        <p>The rule for <code class=\"emu-grammar\">Term :: ExtendedAtom</code> is the same as for <code class=\"emu-grammar\">Term :: Atom</code> but with |ExtendedAtom| substituted for |Atom|.</p>\n      ","1.2.6":"<p>CompileAssertion rules for the <code class=\"emu-grammar\">Assertion :: `(?=` Disjunction `)`</code> and <code class=\"emu-grammar\">Assertion :: `(?!` Disjunction `)`</code> productions are also used for the |QuantifiableAssertion| productions, but with |QuantifiableAssertion| substituted for |Assertion|.</p>\n      ","1.2.7":"<p>CompileAtom rules for the |Atom| productions except for <code class=\"emu-grammar\">Atom :: PatternCharacter</code> are also used for the |ExtendedAtom| productions, but with |ExtendedAtom| substituted for |Atom|. The following rules, with parameter _direction_, are also added:</p>\n        <pre class=\"emu-grammar\">ExtendedAtom :: `\\` [lookahead == `c`]</pre>\n        <emu-alg><ol><li>Let _charSet_ be the CharSet containing the single character `\\\\` U+005C (REVERSE SOLIDUS).</li><li>Return CharacterSetMatcher(_regexpRecord_, _charSet_, *false*, _direction_).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\">ExtendedAtom :: ExtendedPatternCharacter</pre>\n        <emu-alg><ol><li>Let _ch_ be the character represented by |ExtendedPatternCharacter|.</li><li>Let _charSet_ be a one-element CharSet containing the character _ch_.</li><li>Return CharacterSetMatcher(_regexpRecord_, _charSet_, *false*, _direction_).</li></ol></emu-alg>\n      ","1.2.8":"<p>The semantics of 22.2.2.9 is extended as follows:</p>\n\n        <p>The following two rules replace the corresponding rules of CompileToCharSet.</p>\n        <pre class=\"emu-grammar\">NonemptyClassRanges :: ClassAtom `-` ClassAtom ClassContents</pre>\n        <emu-alg><ol><li>Let _charSet_ be CompileToCharSet of the first |ClassAtom| with argument _regexpRecord_.</li><li>Let _otherSet_ be CompileToCharSet of the second |ClassAtom| with argument _regexpRecord_.</li><li>Let _remainingSet_ be CompileToCharSet of |ClassContents| with argument _regexpRecord_.</li><li>Let _rangeSet_ be CharacterRangeOrUnion(_regexpRecord_, _charSet_, _otherSet_).</li><li>Return the union of _rangeSet_ and _remainingSet_.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\">NonemptyClassRangesNoDash :: ClassAtomNoDash `-` ClassAtom ClassContents</pre>\n        <emu-alg><ol><li>Let _charSet_ be CompileToCharSet of |ClassAtomNoDash| with argument _regexpRecord_.</li><li>Let _otherSet_ be CompileToCharSet of |ClassAtom| with argument _regexpRecord_.</li><li>Let _remainingSet_ be CompileToCharSet of |ClassContents| with argument _regexpRecord_.</li><li>Let _rangeSet_ be CharacterRangeOrUnion(_regexpRecord_, _charSet_, _otherSet_).</li><li>Return the union of _rangeSet_ and _remainingSet_.</li></ol></emu-alg>\n\n        <p>In addition, the following rules are added to CompileToCharSet.</p>\n        <pre class=\"emu-grammar\">ClassEscape :: `c` ClassControlLetter</pre>\n        <emu-alg><ol><li>Let _cv_ be the CharacterValue of this |ClassEscape|.</li><li>Let _c_ be the character whose character value is _cv_.</li><li>Return the CharSet containing the single character _c_.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\">ClassAtomNoDash :: `\\` [lookahead == `c`]</pre>\n        <emu-alg><ol><li>Return the CharSet containing the single character `\\\\` U+005C (REVERSE SOLIDUS).</li></ol></emu-alg>\n\n        <emu-note>This production can only be reached from the sequence `\\c` within a character class where it is not followed by an acceptable control character.</emu-note>\n\n        ","1.2.8.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If HasEitherUnicodeFlag(_regexpRecord_) is *false*, then<ol><li>If _charSet_ does not contain exactly one character or _otherSet_ does not contain exactly one character, then<ol><li>Let _remainingSet_ be the CharSet containing the single character `-` U+002D (HYPHEN-MINUS).</li><li>Return the union of CharSets _charSet_, _otherSet_ and _remainingSet_.</li></ol></li></ol></li><li>Return CharacterRange(_charSet_, _otherSet_).</li></ol></emu-alg>\n        ","1.2.9":"<p>The semantics of 22.2.3.4 is extended as follows:</p>\n        <p>The abstract operation ParsePattern takes arguments _patternText_ (a sequence of Unicode code points), _u_ (a Boolean), and _v_ (a Boolean). It performs the following steps when called:</p>\n        <emu-alg><ol><li>If _v_ is *true* and _u_ is *true*, then<ol><li>Let _parseResult_ be a List containing one or more *SyntaxError* objects.</li></ol></li><li>Else if _v_ is *true*, then<ol><li>Let _parseResult_ be ParseText(_patternText_, |Pattern[+UnicodeMode, +UnicodeSetsMode, +NamedCaptureGroups]|).</li></ol></li><li>Else if _u_ is *true*, then<ol><li>Let _parseResult_ be ParseText(_patternText_, |Pattern[+UnicodeMode, ~UnicodeSetsMode, +NamedCaptureGroups]|).</li></ol></li><li>Else,<ol><li>Let _parseResult_ be ParseText(_patternText_, |Pattern[~UnicodeMode, ~UnicodeSetsMode, ~NamedCaptureGroups]|).</li><li>If _parseResult_ is a Parse Node and _parseResult_ contains a |GroupName|, then<ol><li>Set _parseResult_ to ParseText(_patternText_, |Pattern[~UnicodeMode, ~UnicodeSetsMode, +NamedCaptureGroups]|).</li></ol></li></ol></li><li>Return _parseResult_.</li></ol></emu-alg>\n      ","2.1":"<p>The entries in table-additional-well-known-intrinsic-objects are added to table-well-known-intrinsic-objects.</p>\n      <emu-table id=\"table-additional-well-known-intrinsic-objects\" caption=\"Additional Well-known Intrinsic Objects\" oldids=\"table-60\">\n        <table>\n          <thead>\n            <tr>\n              <th>\n                Intrinsic Name\n              </th>\n              <th>\n                Global Name\n              </th>\n              <th>\n                ECMAScript Language Association\n              </th>\n            </tr>\n          </thead>\n          <tr>\n            <td>\n              %escape%\n            </td>\n            <td>\n              `escape`\n            </td>\n            <td>\n              The `escape` function (B.2.1.1)\n            </td>\n          </tr>\n          <tr>\n            <td>\n              %unescape%\n            </td>\n            <td>\n              `unescape`\n            </td>\n            <td>\n              The `unescape` function (B.2.1.2)\n            </td>\n          </tr>\n        </table>\n      </emu-table>\n\n      ","2.1.1":"<p>This function is a property of the global object. It computes a new version of a String value in which certain code units have been replaced by a hexadecimal escape sequence.</p>\n        <p>When replacing a code unit of numeric value less than or equal to 0x00FF, a two-digit escape sequence of the form <code>%<var>xx</var></code> is used. When replacing a code unit of numeric value strictly greater than 0x00FF, a four-digit escape sequence of the form <code>%u<var>xxxx</var></code> is used.</p>\n        <p>It is the <dfn>%escape%</dfn> intrinsic object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Set _string_ to ? ToString(_string_).</li><li>Let _len_ be the length of _string_.</li><li>Let _result_ be the empty String.</li><li>Let _unescapedSet_ be the string-concatenation of the ASCII word characters and *\"@\\*+-./\"*.</li><li>Let _k_ be 0.</li><li>Repeat, while _k_ &lt; _len_,<ol><li>Let _codeUnit_ be the code unit at index _k_ within _string_.</li><li>If _unescapedSet_ contains _codeUnit_, then<ol><li>Let _str_ be _codeUnit_.</li></ol></li><li>Else,<ol><li>Let _n_ be the numeric value of _codeUnit_.</li><li>If _n_ &lt; 256, then<ol><li>Let _hex_ be the String representation of _n_, formatted as an uppercase hexadecimal number.</li><li>Let _str_ be the string-concatenation of *\"%\"* and StringPad(_hex_, 2, *\"0\"*, ~start~).</li></ol></li><li>Else,<ol><li>Let _hex_ be the String representation of _n_, formatted as an uppercase hexadecimal number.</li><li>Let _str_ be the string-concatenation of *\"%u\"* and StringPad(_hex_, 4, *\"0\"*, ~start~).</li></ol></li></ol></li><li>Set _result_ to the string-concatenation of _result_ and _str_.</li><li>Set _k_ to _k_ + 1.</li></ol></li><li>Return _result_.</li></ol></emu-alg>\n        <emu-note>\n          <p>The encoding is partly based on the encoding described in RFC 1738, but the entire encoding specified in this standard is described above without regard to the contents of RFC 1738. This encoding does not reflect changes to RFC 1738 made by RFC 3986.</p>\n        </emu-note>\n      ","2.1.2":"<p>This function is a property of the global object. It computes a new version of a String value in which each escape sequence of the sort that might be introduced by the `escape` function is replaced with the code unit that it represents.</p>\n        <p>It is the <dfn>%unescape%</dfn> intrinsic object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Set _string_ to ? ToString(_string_).</li><li>Let _len_ be the length of _string_.</li><li>Let _result_ be the empty String.</li><li>Let _k_ be 0.</li><li>Repeat, while _k_ &lt; _len_,<ol><li>Let _codeUnit_ be the code unit at index _k_ within _string_.</li><li>If _codeUnit_ is the code unit 0x0025 (PERCENT SIGN), then<ol><li>Let _hexDigits_ be the empty String.</li><li>Let _optionalAdvance_ be 0.</li><li>If _k_ + 5 &lt; _len_ and the code unit at index _k_ + 1 within _string_ is the code unit 0x0075 (LATIN SMALL LETTER U), then<ol><li>Set _hexDigits_ to the substring of _string_ from _k_ + 2 to _k_ + 6.</li><li>Set _optionalAdvance_ to 5.</li></ol></li><li>Else if _k_ + 3 ≤ _len_, then<ol><li>Set _hexDigits_ to the substring of _string_ from _k_ + 1 to _k_ + 3.</li><li>Set _optionalAdvance_ to 2.</li></ol></li><li>Let _parseResult_ be ParseText(_hexDigits_, |HexDigits[~Sep]|).</li><li>If _parseResult_ is a Parse Node, then<ol><li>Let _n_ be the MV of _parseResult_.</li><li>Set _codeUnit_ to the code unit whose numeric value is _n_.</li><li>Set _k_ to _k_ + _optionalAdvance_.</li></ol></li></ol></li><li>Set _result_ to the string-concatenation of _result_ and _codeUnit_.</li><li>Set _k_ to _k_ + 1.</li></ol></li><li>Return _result_.</li></ol></emu-alg>\n      ","2.2":"","2.2.1":"<p>This method returns a <emu-not-ref>substring</emu-not-ref> of the result of converting the *this* value to a String, starting from index _start_ and running for _length_ code units (or through the end of the String if _length_ is *undefined*). If _start_ is negative, it is treated as <emu-eqn>_sourceLength_ + _start_</emu-eqn> where _sourceLength_ is the length of the String. The result is a String value, not a String object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireObjectCoercible(_obj_).</li><li>Let _str_ be ? ToString(_obj_).</li><li>Let _size_ be the length of _str_.</li><li>Let _intStart_ be ? ToIntegerOrInfinity(_start_).</li><li>If _intStart_ = -∞, set _intStart_ to 0.</li><li>Else if _intStart_ &lt; 0, set _intStart_ to max(_size_ + _intStart_, 0).</li><li>Else, set _intStart_ to min(_intStart_, _size_).</li><li>If _length_ is *undefined*, let _intLength_ be _size_; else let _intLength_ be ? ToIntegerOrInfinity(_length_).</li><li>Set _intLength_ to the result of clamping _intLength_ between 0 and _size_.</li><li>Let _intEnd_ be min(_intStart_ + _intLength_, _size_).</li><li>Return the substring of _str_ from _intStart_ to _intEnd_.</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its *this* value be a String object. Therefore it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","2.2.2":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _str_ be the *this* value.</li><li>Return ? CreateHTML(_str_, *\"a\"*, *\"name\"*, _name_).</li></ol></emu-alg>\n\n        ","2.2.2.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Perform ? RequireObjectCoercible(_contents_).</li><li>Let _contentsStr_ be ? ToString(_contents_).</li><li>Let _p1_ be the string-concatenation of *\"&lt;\"* and _tag_.</li><li>If _attribute_ is not the empty String, then<ol><li>Let _attrValueStr_ be ? ToString(_attrValue_).</li><li>Let _escapedAttrValue_ be the String value that is the same as _attrValueStr_ except that each occurrence of the code unit 0x0022 (QUOTATION MARK) in _attrValueStr_ has been replaced with the six code unit sequence *\"&amp;quot;\"*.</li><li>Set _p1_ to the string-concatenation of:<ul><li>_p1_</li><li>the code unit 0x0020 (SPACE)</li><li>_attribute_</li><li>the code unit 0x003D (EQUALS SIGN)</li><li>the code unit 0x0022 (QUOTATION MARK)</li><li>_escapedAttrValue_</li><li>the code unit 0x0022 (QUOTATION MARK)</li></ul></li></ol></li><li>Let _p2_ be the string-concatenation of _p1_ and *\">\"*.</li><li>Let _p3_ be the string-concatenation of _p2_ and _contentsStr_.</li><li>Let _p4_ be the string-concatenation of _p3_, *\"&lt;/\"*, _tag_, and *\">\"*.</li><li>Return _p4_.</li></ol></emu-alg>\n        ","2.2.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _str_ be the *this* value.</li><li>Return ? CreateHTML(_str_, *\"big\"*, *\"\"*, *\"\"*).</li></ol></emu-alg>\n      ","2.2.4":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _str_ be the *this* value.</li><li>Return ? CreateHTML(_str_, *\"blink\"*, *\"\"*, *\"\"*).</li></ol></emu-alg>\n      ","2.2.5":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _str_ be the *this* value.</li><li>Return ? CreateHTML(_str_, *\"b\"*, *\"\"*, *\"\"*).</li></ol></emu-alg>\n      ","2.2.6":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _str_ be the *this* value.</li><li>Return ? CreateHTML(_str_, *\"tt\"*, *\"\"*, *\"\"*).</li></ol></emu-alg>\n      ","2.2.7":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _str_ be the *this* value.</li><li>Return ? CreateHTML(_str_, *\"font\"*, *\"color\"*, _colour_).</li></ol></emu-alg>\n      ","2.2.8":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _str_ be the *this* value.</li><li>Return ? CreateHTML(_str_, *\"font\"*, *\"size\"*, _size_).</li></ol></emu-alg>\n      ","2.2.9":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _str_ be the *this* value.</li><li>Return ? CreateHTML(_str_, *\"i\"*, *\"\"*, *\"\"*).</li></ol></emu-alg>\n      ","2.2.10":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _str_ be the *this* value.</li><li>Return ? CreateHTML(_str_, *\"a\"*, *\"href\"*, _url_).</li></ol></emu-alg>\n      ","2.2.11":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _str_ be the *this* value.</li><li>Return ? CreateHTML(_str_, *\"small\"*, *\"\"*, *\"\"*).</li></ol></emu-alg>\n      ","2.2.12":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _str_ be the *this* value.</li><li>Return ? CreateHTML(_str_, *\"strike\"*, *\"\"*, *\"\"*).</li></ol></emu-alg>\n      ","2.2.13":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _str_ be the *this* value.</li><li>Return ? CreateHTML(_str_, *\"sub\"*, *\"\"*, *\"\"*).</li></ol></emu-alg>\n      ","2.2.14":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _str_ be the *this* value.</li><li>Return ? CreateHTML(_str_, *\"sup\"*, *\"\"*, *\"\"*).</li></ol></emu-alg>\n      ","2.2.15":"<emu-note>\n          <p>The property *\"trimStart\"* is preferred. The *\"trimLeft\"* property is provided principally for compatibility with old code. It is recommended that the *\"trimStart\"* property be used in new ECMAScript code.</p>\n        </emu-note>\n        <p>The initial value of the *\"trimLeft\"* property is %String.prototype.trimStart%, defined in 22.1.3.34.</p>\n      ","2.2.16":"<emu-note>\n          <p>The property *\"trimEnd\"* is preferred. The *\"trimRight\"* property is provided principally for compatibility with old code. It is recommended that the *\"trimEnd\"* property be used in new ECMAScript code.</p>\n        </emu-note>\n        <p>The initial value of the *\"trimRight\"* property is %String.prototype.trimEnd%, defined in 22.1.3.33.</p>\n      ","2.3":"","2.3.1":"<emu-note>\n          <p>The `getFullYear` method is preferred for nearly all purposes, because it avoids the “year 2000 problem.”</p>\n        </emu-note>\n        <p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _dateObject_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_dateObject_, [[DateValue]]).</li><li>Let _t_ be _dateObject_.[[DateValue]].</li><li>If _t_ is *NaN*, return *NaN*.</li><li>Return YearFromTime(LocalTime(_t_)) - *1900*<sub>𝔽</sub>.</li></ol></emu-alg>\n      ","2.3.2":"<emu-note>\n          <p>The `setFullYear` method is preferred for nearly all purposes, because it avoids the “year 2000 problem.”</p>\n        </emu-note>\n        <p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _dateObject_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_dateObject_, [[DateValue]]).</li><li>Let _time_ be _dateObject_.[[DateValue]].</li><li>Let _year_ be ? ToNumber(_year_).</li><li>If _time_ is *NaN*, set _time_ to *+0*<sub>𝔽</sub>; else set _time_ to LocalTime(_time_).</li><li>Let _fullYear_ be MakeFullYear(_year_).</li><li>Let _day_ be MakeDay(_fullYear_, MonthFromTime(_time_), DateFromTime(_time_)).</li><li>Let _date_ be MakeDate(_day_, TimeWithinDay(_time_)).</li><li>Let _utcTimestamp_ be TimeClip(UTC(_date_)).</li><li>Set _dateObject_.[[DateValue]] to _utcTimestamp_.</li><li>Return _utcTimestamp_.</li></ol></emu-alg>\n      ","2.3.3":"<emu-note>\n          <p>The `toUTCString` method is preferred. This method is provided principally for compatibility with old code.</p>\n        </emu-note>\n        <p>The initial value of the *\"toGMTString\"* property is %Date.prototype.toUTCString%, defined in 21.4.4.43.</p>\n      ","2.4":"","2.4.1":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[RegExpMatcher]]).</li><li>If _pattern_ is an Object and _pattern_ has a [[RegExpMatcher]] internal slot, then<ol><li>If _flags_ is not *undefined*, throw a *TypeError* exception.</li><li>Set _flags_ to _pattern_.[[OriginalFlags]].</li><li>Set _pattern_ to _pattern_.[[OriginalSource]].</li></ol></li><li>Return ? RegExpInitialize(_obj_, _pattern_, _flags_).</li></ol></emu-alg>\n        <emu-note>\n          <p>This method completely reinitializes the *this* value RegExp with a new pattern and flags. An implementation may interpret use of this method as an assertion that the resulting RegExp object will be used multiple times and hence is a candidate for extra optimization.</p>\n        </emu-note>\n      ","3.1":"<p>Prior to ECMAScript 2015, the specification of |LabelledStatement| did not allow for the association of a statement label with a |FunctionDeclaration|. However, a labelled |FunctionDeclaration| was an allowable extension for non-strict code and most browser-hosted ECMAScript implementations supported that extension. In ECMAScript 2015 and later, the grammar production for |LabelledStatement| permits use of |FunctionDeclaration| as a |LabelledItem| but 14.13.1 includes an Early Error rule that produces a Syntax Error if that occurs. That rule is then modified to suppress the Syntax Error in non-strict code if the host supports this feature.</p>\n      <emu-note>\n        <p>The early error rules for |WithStatement|, |IfStatement|, and |IterationStatement| prevent these statements from containing a labelled |FunctionDeclaration| in non-strict code.</p>\n      </emu-note>\n    ","3.2":"<p>Prior to ECMAScript 2015, the ECMAScript specification did not define the occurrence of a |FunctionDeclaration| as an element of a |Block| statement's |StatementList|. However, support for that form of |FunctionDeclaration| was an allowable extension and most browser-hosted ECMAScript implementations permitted them. Unfortunately, the semantics of such declarations differ among those implementations. Because of these semantic differences, existing web ECMAScript source text that uses |Block| level function declarations is only portable among browser implementations if the usage only depends upon the semantic intersection of all of the browser implementations for such declarations. The following are the use cases that fall within that intersection semantics:</p>\n      <ol>\n        <li>\n          <p>A function is declared and only referenced within a single block.</p>\n          <ul>\n            <li>\n              One or more |FunctionDeclaration|s whose |BindingIdentifier| is the name _f_ occur within the function code of an enclosing function _g_ and that declaration is nested within a |Block|.\n            </li>\n            <li>\n              No other declaration of _f_ that is not a `var` declaration occurs within the function code of _g_.\n            </li>\n            <li>\n              All occurrences of _f_ as an |IdentifierReference| are within the |StatementList| of the |Block| containing the declaration of _f_.\n            </li>\n          </ul>\n        </li>\n        <li>\n          <p>A function is declared and possibly used within a single |Block| but also referenced by an inner function definition that is not contained within that same |Block|.</p>\n          <ul>\n            <li>\n              One or more |FunctionDeclaration|s whose |BindingIdentifier| is the name _f_ occur within the function code of an enclosing function _g_ and that declaration is nested within a |Block|.\n            </li>\n            <li>\n              No other declaration of _f_ that is not a `var` declaration occurs within the function code of _g_.\n            </li>\n            <li>\n              There may be occurrences of _f_ as an |IdentifierReference| within the |StatementList| of the |Block| containing the declaration of _f_.\n            </li>\n            <li>\n              There is at least one occurrence of _f_ as an |IdentifierReference| within another function _h_ that is nested within _g_ and no other declaration of _f_ shadows the references to _f_ from within _h_.\n            </li>\n            <li>\n              All invocations of _h_ occur after the declaration of _f_ has been evaluated.\n            </li>\n          </ul>\n        </li>\n        <li>\n          <p>A function is declared and possibly used within a single block but also referenced within subsequent blocks.</p>\n          <ul>\n            <li>\n              One or more |FunctionDeclaration| whose |BindingIdentifier| is the name _f_ occur within the function code of an enclosing function _g_ and that declaration is nested within a |Block|.\n            </li>\n            <li>\n              No other declaration of _f_ that is not a `var` declaration occurs within the function code of _g_.\n            </li>\n            <li>\n              There may be occurrences of _f_ as an |IdentifierReference| within the |StatementList| of the |Block| containing the declaration of _f_.\n            </li>\n            <li>\n              There is at least one occurrence of _f_ as an |IdentifierReference| within the function code of _g_ that lexically follows the |Block| containing the declaration of _f_.\n            </li>\n          </ul>\n        </li>\n      </ol>\n      <p>The first use case is interoperable with the semantics of |Block| level function declarations provided by ECMAScript 2015. Any pre-existing ECMAScript source text that employs that use case will operate using the Block level function declarations semantics defined by clauses 10, 14, and 15.</p>\n      <p>ECMAScript 2015 interoperability for the second and third use cases requires the following extensions to the clause 10, clause 15, clause 19.2.1 and clause 16.1.7 semantics.</p>\n      <p>If an ECMAScript implementation has a mechanism for reporting diagnostic warning messages, a warning should be produced when code contains a |FunctionDeclaration| for which these compatibility semantics are applied and introduce observable differences from non-compatibility semantics. For example, if a var binding is not introduced because its introduction would create an early error, a warning message should not be produced.</p>\n      <p>This feature involves special semantics at the following points:</p>\n      <ul>\n        <li>one of the early error rules for <code class=\"emu-grammar\">Block : `{` StatementList `}`</code> in 14.2.1</li>\n        <li>one of the early error rules for <code class=\"emu-grammar\">SwitchStatement : `switch` `(` Expression `)` CaseBlock</code> in 14.12.1</li>\n        <li>step step-functiondeclarationinstantiation-web-compat-insertion-point in FunctionDeclarationInstantiation</li>\n        <li>step step-blockdeclarationinstantiation-createmutablebinding in BlockDeclarationInstantiation</li>\n        <li>step step-blockdeclarationinstantiation-initializebinding in BlockDeclarationInstantiation</li>\n        <li>step step-globaldeclarationinstantiation-web-compat-insertion-point in GlobalDeclarationInstantiation</li>\n        <li>step step-evaldeclarationinstantiation-web-compat-insertion-point in EvalDeclarationInstantiation</li>\n      </ul>\n    ","3.3":"<p>The following augments the |IfStatement| production in 14.6:</p>\n      <pre class=\"emu-grammar\">IfStatement[Yield, Await, Return] :\n  `if` `(` Expression[+In, ?Yield, ?Await] `)` FunctionDeclaration[?Yield, ?Await, ~Default] `else` Statement[?Yield, ?Await, ?Return]\n  `if` `(` Expression[+In, ?Yield, ?Await] `)` Statement[?Yield, ?Await, ?Return] `else` FunctionDeclaration[?Yield, ?Await, ~Default]\n  `if` `(` Expression[+In, ?Yield, ?Await] `)` FunctionDeclaration[?Yield, ?Await, ~Default] `else` FunctionDeclaration[?Yield, ?Await, ~Default]\n  `if` `(` Expression[+In, ?Yield, ?Await] `)` FunctionDeclaration[?Yield, ?Await, ~Default] [lookahead != `else`]</pre>\n      <p>This production only applies when parsing non-strict code. Source text matched by this production is processed as if each matching occurrence of |FunctionDeclaration[?Yield, ?Await, ~Default]| was the sole |StatementListItem| of a |BlockStatement| occupying that position in the source text. The semantics of such a synthetic |BlockStatement| includes the web legacy compatibility semantics specified in B.3.2.</p>\n    ","3.4":"<p>In this feature, the |Block| of a |Catch| clause may contain `var` declarations that bind a name that is also bound by the |CatchParameter|. This is accomplished by modifying an early error rule for <code class=\"emu-grammar\">Catch : `catch` `(` CatchParameter `)` Block</code> in 14.15.1.</p>\n      <emu-note>\n        <p>At runtime, such bindings are instantiated in the VariableDeclarationEnvironment. They do not shadow the same-named bindings introduced by the |CatchParameter| and hence the |Initializer| for such `var` declarations will assign to the corresponding catch parameter rather than the `var` binding.</p>\n      </emu-note>\n      <p>This modified behaviour also applies to `var` and `function` declarations introduced by direct eval calls contained within the |Block| of a |Catch| clause. This change is accomplished by modifying steps step-evaldeclarationinstantiation-throw-duplicate-binding and step-evaldeclarationinstantiation-web-compat-bindingexists in EvalDeclarationInstantiation.</p>\n    ","3.5":"<p>The following augments the |ForInOfStatement| production in 14.7.5:</p>\n      <pre class=\"emu-grammar\">ForInOfStatement[Yield, Await, Return] :\n  `for` `(` `var` BindingIdentifier[?Yield, ?Await] Initializer[~In, ?Yield, ?Await] `in` Expression[+In, ?Yield, ?Await] `)` Statement[?Yield, ?Await, ?Return]</pre>\n      <p>This production only applies when parsing non-strict code.</p>\n      <p>The static semantics of ContainsDuplicateLabels in 8.3.1 are augmented with the following:</p>\n      <pre class=\"emu-grammar\">ForInOfStatement : `for` `(` `var` BindingIdentifier Initializer `in` Expression `)` Statement</pre>\n      <emu-alg><ol><li>Return ContainsDuplicateLabels of |Statement| with argument _labelSet_.</li></ol></emu-alg>\n      <p>The static semantics of ContainsUndefinedBreakTarget in 8.3.2 are augmented with the following:</p>\n      <pre class=\"emu-grammar\">ForInOfStatement : `for` `(` `var` BindingIdentifier Initializer `in` Expression `)` Statement</pre>\n      <emu-alg><ol><li>Return ContainsUndefinedBreakTarget of |Statement| with argument _labelSet_.</li></ol></emu-alg>\n      <p>The static semantics of ContainsUndefinedContinueTarget in 8.3.3 are augmented with the following:</p>\n      <pre class=\"emu-grammar\">ForInOfStatement : `for` `(` `var` BindingIdentifier Initializer `in` Expression `)` Statement</pre>\n      <emu-alg><ol><li>Return ContainsUndefinedContinueTarget of |Statement| with arguments _iterationSet_ and « ».</li></ol></emu-alg>\n      <p>The static semantics of IsDestructuring in 14.7.5.2 are augmented with the following:</p>\n      <pre class=\"emu-grammar\">BindingIdentifier :\n  Identifier\n  `yield`\n  `await`</pre>\n      <emu-alg><ol><li>Return *false*.</li></ol></emu-alg>\n      <p>The static semantics of VarDeclaredNames in 8.2.6 are augmented with the following:</p>\n      <pre class=\"emu-grammar\">ForInOfStatement : `for` `(` `var` BindingIdentifier Initializer `in` Expression `)` Statement</pre>\n      <emu-alg><ol><li>Let _names1_ be the BoundNames of |BindingIdentifier|.</li><li>Let _names2_ be the VarDeclaredNames of |Statement|.</li><li>Return the list-concatenation of _names1_ and _names2_.</li></ol></emu-alg>\n      <p>The static semantics of VarScopedDeclarations in 8.2.7 are augmented with the following:</p>\n      <pre class=\"emu-grammar\">ForInOfStatement : `for` `(` `var` BindingIdentifier Initializer `in` Expression `)` Statement</pre>\n      <emu-alg><ol><li>Let _declarations1_ be « |BindingIdentifier| ».</li><li>Let _declarations2_ be the VarScopedDeclarations of |Statement|.</li><li>Return the list-concatenation of _declarations1_ and _declarations2_.</li></ol></emu-alg>\n      <p>The runtime semantics of ForInOfLoopEvaluation in 14.7.5.5 are augmented with the following:</p>\n      <pre class=\"emu-grammar\">ForInOfStatement : `for` `(` `var` BindingIdentifier Initializer `in` Expression `)` Statement</pre>\n      <emu-alg><ol><li>Let _bindingId_ be the StringValue of |BindingIdentifier|.</li><li>Let _lhs_ be ? ResolveBinding(_bindingId_).</li><li>If IsAnonymousFunctionDefinition(|Initializer|) is *true*, then<ol><li>Let _value_ be ? NamedEvaluation of |Initializer| with argument _bindingId_.</li></ol></li><li>Else,<ol><li>Let _rhs_ be ? Evaluation of |Initializer|.</li><li>Let _value_ be ? GetValue(_rhs_).</li></ol></li><li>Perform ? PutValue(_lhs_, _value_).</li><li>Let _keyResult_ be ? ForIn/OfHeadEvaluation(« », |Expression|, ~enumerate~).</li><li>Return ? ForIn/OfBodyEvaluation(|BindingIdentifier|, |Statement|, _keyResult_, ~enumerate~, ~var-binding~, _labelSet_).</li></ol></emu-alg>\n    ","3.6":"<p>An <dfn>[[IsHTMLDDA]] internal slot</dfn> may exist on host-defined objects. Objects with an [[IsHTMLDDA]] internal slot behave like *undefined* in the <emu-xref href=\"#sec-toboolean\">ToBoolean</emu-xref> and IsLooselyEqual abstract operations and when used as an operand for the <emu-xref href=\"#sec-typeof-operator\">`typeof` operator</emu-xref>.</p>\n      <emu-note>\n        <p>Objects with an [[IsHTMLDDA]] internal slot are never created by this specification. However, the <a href=\"https://html.spec.whatwg.org/multipage/obsolete.html#dom-document-all\"><code>document.all</code> object</a> in web browsers is a host-defined exotic object with this slot that exists for web compatibility purposes. There are no other known examples of this type of object and implementations should not create any with the exception of `document.all`.</p>\n      </emu-note>\n      <p>This feature involves special semantics at the following points:</p>\n      <ul>\n        <li>step step-to-boolean-web-compat-insertion-point in ToBoolean</li>\n        <li>step step-abstract-equality-comparison-web-compat-insertion-point in IsLooselyEqual</li>\n        <li>step step-typeof-web-compat-insertion-point in the evaluation semantics for `typeof`</li>\n      </ul>\n    ","3.7":"<p>The HostMakeJobCallback abstract operation allows hosts which are web browsers to specify non-default behaviour.</p>\n    ","3.8":"<p>The HostEnsureCanAddPrivateElement abstract operation allows hosts which are web browsers to specify non-default behaviour.</p>\n    ","3.9":"<p>When a function call (13.3.6) is used as an assignment target in non-strict code, instead of producing an early error, a *ReferenceError* exception is thrown during evaluation of the assignment.</p>\n      <emu-note>\n        <p>When the assignment target is the |LeftHandSideExpression| of an |AssignmentExpression|, the assignment operator must be `=` or an |AssignmentOperator|; in particular, the allowance here does not apply to the logical assignment operators (`??=`, `&&=`, `||=`).</p>\n      </emu-note>\n      <p>See step step-assignmenttargettype-web-compat of AssignmentTargetType for <code class=\"emu-grammar\">CallExpression : CoverCallExpressionAndAsyncArrowHead</code> and <code class=\"emu-grammar\">CallExpression : CallExpression Arguments</code>.</p>\n    "};
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
