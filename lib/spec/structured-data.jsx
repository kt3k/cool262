// Generated from ecma262/spec.html — do not edit by hand.
const sections = {"1":"","2":"","3":"","4":"<p>The Atomics object:</p>\n    <ul>\n      <li>is <dfn>%Atomics%</dfn>.</li>\n      <li>is the initial value of the *\"Atomics\"* property of the global object.</li>\n      <li>is an ordinary object.</li>\n      <li>has a [[Prototype]] internal slot whose value is %Object.prototype%.</li>\n      <li>does not have a [[Construct]] internal method; it cannot be used as a constructor with the `new` operator.</li>\n      <li>does not have a [[Call]] internal method; it cannot be invoked as a function.</li>\n    </ul>\n    <p>The Atomics object provides functions that operate indivisibly (atomically) on shared memory array cells as well as functions that let agents wait for and dispatch primitive events. When used with discipline, the Atomics functions allow multi-agent programs that communicate through shared memory to execute in a well-understood order even on parallel CPUs. The rules that govern shared-memory communication are provided by the memory model, defined below.</p>\n    <emu-note>\n      <p>For informative guidelines for programming and implementing shared memory in ECMAScript, please see the notes at the end of the memory model section.</p>\n    </emu-note>\n\n    ","5":"<p>The JSON object:</p>\n    <ul>\n      <li>is <dfn>%JSON%</dfn>.</li>\n      <li>is the initial value of the *\"JSON\"* property of the global object.</li>\n      <li>is an ordinary object.</li>\n      <li>contains two functions, `parse` and `stringify`, that are used to parse and construct JSON texts.</li>\n      <li>has a [[Prototype]] internal slot whose value is %Object.prototype%.</li>\n      <li>does not have a [[Construct]] internal method; it cannot be used as a constructor with the `new` operator.</li>\n      <li>does not have a [[Call]] internal method; it cannot be invoked as a function.</li>\n    </ul>\n    <p>The JSON Data Interchange Format is defined in ECMA-404. The JSON interchange format used in this specification is exactly that described by ECMA-404. Conforming implementations of `JSON.parse` and `JSON.stringify` must support the exact interchange format described in the ECMA-404 specification without any deletions or extensions to the format.</p>\n\n    ","":"","1.1":"<p>The descriptions below in this section, 25.4, and 29 use the read-modify-write modification function internal data structure.</p>\n      <p>A <dfn variants=\"read-modify-write modification functions\">read-modify-write modification function</dfn> is a mathematical function that is represented as an Abstract Closure that takes two Lists of byte values as arguments and returns a List of byte values. These Abstract Closures satisfy all of the following properties:</p>\n      <ul>\n        <li>They perform all their algorithm steps atomically.</li>\n        <li>Their individual algorithm steps are not observable.</li>\n      </ul>\n      <emu-note>\n        <p>To aid verifying that a read-modify-write modification function's algorithm steps constitute a pure, mathematical function, the following editorial conventions are recommended:</p>\n        <ul>\n          <li>They do not access, directly or transitively via invoked abstract operations and Abstract Closures, any language or specification values except their parameters and captured values.</li>\n          <li>They do not invoke, directly or transitively, abstract operations and Abstract Closures that return Completion Records.</li>\n          <li>They do not return Completion Records.</li>\n        </ul>\n      </emu-note>\n    ","1.2":"<p>A <dfn>fixed-length ArrayBuffer</dfn> is an ArrayBuffer whose byte length cannot change after creation.</p>\n      <p>A <dfn>resizable ArrayBuffer</dfn> is an ArrayBuffer whose byte length may change after creation via calls to 25.1.6.6.</p>\n      <p>The kind of ArrayBuffer object that is created depends on the arguments passed to 25.1.4.1.</p>\n    ","1.3":"","1.3.1":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It is used to create an ArrayBuffer.</dd>\n        </dl>\n        <emu-alg><ol><li>Let _slots_ be « [[ArrayBufferData]], [[ArrayBufferByteLength]], [[ArrayBufferDetachKey]] ».</li><li>If _maxByteLength_ is present and _maxByteLength_ is not ~empty~, let _allocatingResizableBuffer_ be *true*; else let _allocatingResizableBuffer_ be *false*.</li><li>If _allocatingResizableBuffer_ is *true*, then<ol><li>If _byteLength_ > _maxByteLength_, throw a *RangeError* exception.</li><li>Append [[ArrayBufferMaxByteLength]] to _slots_.</li></ol></li><li>Let _obj_ be ? OrdinaryCreateFromConstructor(_constructor_, *\"%ArrayBuffer.prototype%\"*, _slots_).</li><li>Let _block_ be ? CreateByteDataBlock(_byteLength_).</li><li>Set _obj_.[[ArrayBufferData]] to _block_.</li><li>Set _obj_.[[ArrayBufferByteLength]] to _byteLength_.</li><li>If _allocatingResizableBuffer_ is *true*, then<ol><li>If it is not possible to create a Data Block _block_ consisting of _maxByteLength_ bytes, throw a *RangeError* exception.</li><li>NOTE: Resizable ArrayBuffers are designed to be implementable with in-place growth. Implementations may throw if, for example, virtual memory cannot be reserved up front.</li><li>Set _obj_.[[ArrayBufferMaxByteLength]] to _maxByteLength_.</li></ol></li><li>Return _obj_.</li></ol></emu-alg>\n      ","1.3.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If IsGrowableSharedArrayBuffer(_arrayBuffer_) is *true*, then<ol><li>Let _bufferByteLengthBlock_ be _arrayBuffer_.[[ArrayBufferByteLengthData]].</li><li>Let _rawLength_ be GetRawBytesFromSharedBlock(_bufferByteLengthBlock_, 0, ~biguint64~, *true*, _order_).</li><li>Let _agentRecord_ be the Agent Record of the surrounding agent.</li><li>Let _isLittleEndian_ be _agentRecord_.[[LittleEndian]].</li><li>Return ℝ(RawBytesToNumeric(~biguint64~, _rawLength_, _isLittleEndian_)).</li></ol></li><li>Assert: IsDetachedBuffer(_arrayBuffer_) is *false*.</li><li>Return _arrayBuffer_.[[ArrayBufferByteLength]].</li></ol></emu-alg>\n      ","1.3.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Perform ? RequireInternalSlot(_arrayBuffer_, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(_arrayBuffer_) is *true*, throw a *TypeError* exception.</li><li>If _newLength_ is *undefined*, then<ol><li>Let _newByteLength_ be _arrayBuffer_.[[ArrayBufferByteLength]].</li></ol></li><li>Else,<ol><li>Let _newByteLength_ be ? ToIndex(_newLength_).</li></ol></li><li>If IsDetachedBuffer(_arrayBuffer_) is *true*, throw a *TypeError* exception.</li><li>If _preserveResizability_ is ~preserve-resizability~ and IsFixedLengthArrayBuffer(_arrayBuffer_) is *false*, then<ol><li>Let _newMaxByteLength_ be _arrayBuffer_.[[ArrayBufferMaxByteLength]].</li></ol></li><li>Else,<ol><li>Let _newMaxByteLength_ be ~empty~.</li></ol></li><li>If _arrayBuffer_.[[ArrayBufferDetachKey]] is not *undefined*, throw a *TypeError* exception.</li><li>Let _newBuffer_ be ? <emu-meta suppress-effects=\"user-code\">AllocateArrayBuffer(%ArrayBuffer%, _newByteLength_, _newMaxByteLength_)</emu-meta>.</li><li>Let _copyLength_ be min(_newByteLength_, _arrayBuffer_.[[ArrayBufferByteLength]]).</li><li>Let _fromBlock_ be _arrayBuffer_.[[ArrayBufferData]].</li><li>Let _toBlock_ be _newBuffer_.[[ArrayBufferData]].</li><li>Perform CopyDataBlockBytes(_toBlock_, 0, _fromBlock_, 0, _copyLength_).</li><li>NOTE: Neither creation of the new Data Block nor copying from the old Data Block are observable. Implementations may implement this method as a zero-copy move or a `realloc`.</li><li>Perform ! DetachArrayBuffer(_arrayBuffer_).</li><li>Return _newBuffer_.</li></ol></emu-alg>\n      ","1.3.4":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If _arrayBuffer_.[[ArrayBufferData]] is *null*, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      ","1.3.5":"<dl class=\"header\">\n          <dt>skip global checks</dt>\n          <dd>true</dd>\n        </dl>\n        <emu-alg><ol><li>Assert: IsSharedArrayBuffer(_arrayBuffer_) is *false*.</li><li>If _key_ is not present, set _key_ to *undefined*.</li><li>If _arrayBuffer_.[[ArrayBufferDetachKey]] is not _key_, throw a *TypeError* exception.</li><li>Set _arrayBuffer_.[[ArrayBufferData]] to *null*.</li><li>Set _arrayBuffer_.[[ArrayBufferByteLength]] to 0.</li><li>Return ~unused~.</li></ol></emu-alg>\n        <emu-note>\n          <p>Detaching an ArrayBuffer instance disassociates the Data Block used as its backing store from the instance and sets the byte length of the buffer to 0.</p>\n        </emu-note>\n      ","1.3.6":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It creates a new ArrayBuffer whose data is a copy of _srcBuffer_'s data over the range starting at _srcByteOffset_ and continuing for _srcLength_ bytes.</dd>\n        </dl>\n        <emu-alg><ol><li>Assert: IsDetachedBuffer(_srcBuffer_) is *false*.</li><li>Let _targetBuffer_ be ? <emu-meta suppress-effects=\"user-code\">AllocateArrayBuffer(%ArrayBuffer%, _srcLength_)</emu-meta>.</li><li>Let _srcBlock_ be _srcBuffer_.[[ArrayBufferData]].</li><li>Let _targetBlock_ be _targetBuffer_.[[ArrayBufferData]].</li><li>Perform CopyDataBlockBytes(_targetBlock_, 0, _srcBlock_, _srcByteOffset_, _srcLength_).</li><li>Return _targetBuffer_.</li></ol></emu-alg>\n      ","1.3.7":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If _options_ is not an Object, return ~empty~.</li><li>Let _maxByteLength_ be ? Get(_options_, *\"maxByteLength\"*).</li><li>If _maxByteLength_ is *undefined*, return ~empty~.</li><li>Return ? ToIndex(_maxByteLength_).</li></ol></emu-alg>\n      ","1.3.8":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It gives the host an opportunity to perform implementation-defined resizing of _buffer_. If the host chooses not to handle resizing of _buffer_, it may return ~unhandled~ for the default behaviour.</dd>\n        </dl>\n\n        <p>The implementation of HostResizeArrayBuffer must conform to the following requirements:</p>\n        <ul>\n          <li>The abstract operation does not detach _buffer_.</li>\n          <li>If the abstract operation completes normally with ~handled~, _buffer_.[[ArrayBufferByteLength]] is _newByteLength_.</li>\n        </ul>\n\n        <p>The default implementation of HostResizeArrayBuffer is to return NormalCompletion(~unhandled~).</p>\n      ","1.3.9":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If _arrayBuffer_ has an [[ArrayBufferMaxByteLength]] internal slot, return *false*.</li><li>Return *true*.</li></ol></emu-alg>\n      ","1.3.10":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It verifies if the argument _type_ is an unsigned TypedArray element type.</dd>\n        </dl>\n        <emu-alg><ol><li>If _type_ is one of ~uint8~, ~uint8clamped~, ~uint16~, ~uint32~, or ~biguint64~, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      ","1.3.11":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It verifies if the argument _type_ is an Integer TypedArray element type not including ~uint8clamped~.</dd>\n        </dl>\n        <emu-alg><ol><li>If _type_ is one of ~int8~, ~uint8~, ~int16~, ~uint16~, ~int32~, or ~uint32~, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      ","1.3.12":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It verifies if the argument _type_ is a BigInt TypedArray element type.</dd>\n        </dl>\n        <emu-alg><ol><li>If _type_ is either ~biguint64~ or ~bigint64~, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      ","1.3.13":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If IsUnclampedIntegerElementType(_type_) is *true*, return *true*.</li><li>If IsBigIntElementType(_type_) is *true* and _order_ is neither ~init~ nor ~unordered~, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      ","1.3.14":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _elementSize_ be the Element Size value specified in table-the-typedarray-constructors for Element Type _type_.</li><li>If _isLittleEndian_ is *false*, reverse the order of the elements of _rawBytes_.</li><li>If _type_ is ~float16~, then<ol><li>Let _value_ be the byte elements of _rawBytes_ concatenated and interpreted as a little-endian bit string encoding of an IEEE 754-2019 binary16 value.</li><li>If _value_ is a NaN, return *NaN*.</li><li>Return the Number value that corresponds to _value_.</li></ol></li><li>If _type_ is ~float32~, then<ol><li>Let _value_ be the byte elements of _rawBytes_ concatenated and interpreted as a little-endian bit string encoding of an IEEE 754-2019 binary32 value.</li><li>If _value_ is a NaN, return *NaN*.</li><li>Return the Number value that corresponds to _value_.</li></ol></li><li>If _type_ is ~float64~, then<ol><li>Let _value_ be the byte elements of _rawBytes_ concatenated and interpreted as a little-endian bit string encoding of an IEEE 754-2019 binary64 value.</li><li>If _value_ is a NaN, return *NaN*.</li><li>Return the Number value that corresponds to _value_.</li></ol></li><li>If IsUnsignedElementType(_type_) is *true*, then<ol><li>Let _intValue_ be the byte elements of _rawBytes_ concatenated and interpreted as a bit string encoding of an unsigned little-endian binary number.</li></ol></li><li>Else,<ol><li>Let _intValue_ be the byte elements of _rawBytes_ concatenated and interpreted as a bit string encoding of a binary little-endian two's complement number of bit length _elementSize_ × 8.</li></ol></li><li>If IsBigIntElementType(_type_) is *true*, return the BigInt value that corresponds to _intValue_.</li><li>Return the Number value that corresponds to _intValue_.</li></ol></emu-alg>\n      ","1.3.15":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _elementSize_ be the Element Size value specified in table-the-typedarray-constructors for Element Type _type_.</li><li>Let _agentRecord_ be the Agent Record of the surrounding agent.</li><li>Let _execution_ be _agentRecord_.[[CandidateExecution]].</li><li>Let _eventsRecord_ be the Agent Events Record of _execution_.[[EventsRecords]] whose [[AgentSignifier]] is AgentSignifier().</li><li>If _isTypedArray_ is *true* and IsNoTearConfiguration(_type_, _order_) is *true*, let _noTear_ be *true*; else let _noTear_ be *false*.</li><li>Let _rawValue_ be a List of length _elementSize_ whose elements are nondeterministically chosen byte values.</li><li>NOTE: In implementations, _rawValue_ is the result of a non-atomic or atomic read instruction on the underlying hardware. The nondeterminism is a semantic prescription of the memory model to describe observable behaviour of hardware with weak consistency.</li><li>Let _readEvent_ be ReadSharedMemory { [[Order]]: _order_, [[NoTear]]: _noTear_, [[Block]]: _block_, [[ByteIndex]]: _byteIndex_, [[ElementSize]]: _elementSize_ }.</li><li>Append _readEvent_ to _eventsRecord_.[[EventList]].</li><li>Append Chosen Value Record { [[Event]]: _readEvent_, [[ChosenValue]]: _rawValue_ } to _execution_.[[ChosenValues]].</li><li>Return _rawValue_.</li></ol></emu-alg>\n      ","1.3.16":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: IsDetachedBuffer(_arrayBuffer_) is *false*.</li><li>Assert: There are sufficient bytes in _arrayBuffer_ starting at _byteIndex_ to represent a value of _type_.</li><li>Let _block_ be _arrayBuffer_.[[ArrayBufferData]].</li><li>Let _elementSize_ be the Element Size value specified in table-the-typedarray-constructors for Element Type _type_.</li><li>If IsSharedArrayBuffer(_arrayBuffer_) is *true*, then<ol><li>Assert: _block_ is a Shared Data Block.</li><li>Let _rawValue_ be GetRawBytesFromSharedBlock(_block_, _byteIndex_, _type_, _isTypedArray_, _order_).</li></ol></li><li>Else,<ol><li>Let _rawValue_ be a List whose elements are bytes from _block_ at indices in the interval from _byteIndex_ (inclusive) to _byteIndex_ + _elementSize_ (exclusive).</li></ol></li><li>Assert: The number of elements in _rawValue_ is _elementSize_.</li><li>If _isLittleEndian_ is not present, then<ol><li>Let _agentRecord_ be the Agent Record of the surrounding agent.</li><li>Set _isLittleEndian_ to _agentRecord_.[[LittleEndian]].</li></ol></li><li>Return RawBytesToNumeric(_type_, _rawValue_, _isLittleEndian_).</li></ol></emu-alg>\n      ","1.3.17":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If _type_ is ~float16~, then<ol><li>Let _rawBytes_ be a List whose elements are the 2 bytes that are the result of converting _value_ to IEEE 754-2019 binary16 format using roundTiesToEven mode. The bytes are arranged in little endian order. If _value_ is *NaN*, _rawBytes_ may be set to any implementation chosen IEEE 754-2019 binary16 format NaN encoding. An implementation must always choose the same encoding for each implementation distinguishable *NaN* value.</li></ol></li><li>Else if _type_ is ~float32~, then<ol><li>Let _rawBytes_ be a List whose elements are the 4 bytes that are the result of converting _value_ to IEEE 754-2019 binary32 format using roundTiesToEven mode. The bytes are arranged in little endian order. If _value_ is *NaN*, _rawBytes_ may be set to any implementation chosen IEEE 754-2019 binary32 format NaN encoding. An implementation must always choose the same encoding for each implementation distinguishable *NaN* value.</li></ol></li><li>Else if _type_ is ~float64~, then<ol><li>Let _rawBytes_ be a List whose elements are the 8 bytes that are the IEEE 754-2019 binary64 format encoding of _value_. The bytes are arranged in little endian order. If _value_ is *NaN*, _rawBytes_ may be set to any implementation chosen IEEE 754-2019 binary64 format NaN encoding. An implementation must always choose the same encoding for each implementation distinguishable *NaN* value.</li></ol></li><li>Else,<ol><li>Let _n_ be the Element Size value specified in table-the-typedarray-constructors for Element Type _type_.</li><li>Let _conversionOperation_ be the abstract operation named in the “Conversion Operation” column of table-the-typedarray-constructors for Element Type _type_.</li><li>Let _intValue_ be ℝ(! _conversionOperation_(_value_)).</li><li>If _intValue_ ≥ 0, then<ol><li>Let _rawBytes_ be a List whose elements are the _n_-byte binary encoding of _intValue_. The bytes are ordered in little endian order.</li></ol></li><li>Else,<ol><li>Let _rawBytes_ be a List whose elements are the _n_-byte binary two's complement encoding of _intValue_. The bytes are ordered in little endian order.</li></ol></li></ol></li><li>If _isLittleEndian_ is *false*, reverse the order of the elements of _rawBytes_.</li><li>Return _rawBytes_.</li></ol></emu-alg>\n      ","1.3.18":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: IsDetachedBuffer(_arrayBuffer_) is *false*.</li><li>Assert: There are sufficient bytes in _arrayBuffer_ starting at _byteIndex_ to represent a value of _type_.</li><li>Assert: _value_ is a BigInt if IsBigIntElementType(_type_) is *true*; else, _value_ is a Number.</li><li>Let _block_ be _arrayBuffer_.[[ArrayBufferData]].</li><li>Let _elementSize_ be the Element Size value specified in table-the-typedarray-constructors for Element Type _type_.</li><li>Let _agentRecord_ be the Agent Record of the surrounding agent.</li><li>If _isLittleEndian_ is not present, then<ol><li>Set _isLittleEndian_ to _agentRecord_.[[LittleEndian]].</li></ol></li><li>Let _rawBytes_ be NumericToRawBytes(_type_, _value_, _isLittleEndian_).</li><li>If IsSharedArrayBuffer(_arrayBuffer_) is *true*, then<ol><li>Let _execution_ be _agentRecord_.[[CandidateExecution]].</li><li>Let _eventsRecord_ be the Agent Events Record of _execution_.[[EventsRecords]] whose [[AgentSignifier]] is AgentSignifier().</li><li>If _isTypedArray_ is *true* and IsNoTearConfiguration(_type_, _order_) is *true*, let _noTear_ be *true*; else let _noTear_ be *false*.</li><li>Append WriteSharedMemory { [[Order]]: _order_, [[NoTear]]: _noTear_, [[Block]]: _block_, [[ByteIndex]]: _byteIndex_, [[ElementSize]]: _elementSize_, [[Payload]]: _rawBytes_ } to _eventsRecord_.[[EventList]].</li></ol></li><li>Else,<ol><li>Store the individual bytes of _rawBytes_ into _block_, starting at _block_[_byteIndex_].</li></ol></li><li>Return ~unused~.</li></ol></emu-alg>\n      ","1.3.19":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: IsDetachedBuffer(_arrayBuffer_) is *false*.</li><li>Assert: There are sufficient bytes in _arrayBuffer_ starting at _byteIndex_ to represent a value of _type_.</li><li>Assert: _value_ is a BigInt if IsBigIntElementType(_type_) is *true*; else, _value_ is a Number.</li><li>Let _block_ be _arrayBuffer_.[[ArrayBufferData]].</li><li>Let _elementSize_ be the Element Size value specified in table-the-typedarray-constructors for Element Type _type_.</li><li>Let _agentRecord_ be the Agent Record of the surrounding agent.</li><li>Let _isLittleEndian_ be _agentRecord_.[[LittleEndian]].</li><li>Let _rawBytes_ be NumericToRawBytes(_type_, _value_, _isLittleEndian_).</li><li>If IsSharedArrayBuffer(_arrayBuffer_) is *true*, then<ol><li>Let _execution_ be _agentRecord_.[[CandidateExecution]].</li><li>Let _eventsRecord_ be the Agent Events Record of _execution_.[[EventsRecords]] whose [[AgentSignifier]] is AgentSignifier().</li><li>Let _rawBytesRead_ be a List of length _elementSize_ whose elements are nondeterministically chosen byte values.</li><li>NOTE: In implementations, _rawBytesRead_ is the result of a load-link, of a load-exclusive, or of an operand of a read-modify-write instruction on the underlying hardware. The nondeterminism is a semantic prescription of the memory model to describe observable behaviour of hardware with weak consistency.</li><li>Let _rmwEvent_ be ReadModifyWriteSharedMemory { [[Order]]: ~seq-cst~, [[NoTear]]: *true*, [[Block]]: _block_, [[ByteIndex]]: _byteIndex_, [[ElementSize]]: _elementSize_, [[Payload]]: _rawBytes_, [[ModifyOp]]: _op_ }.</li><li>Append _rmwEvent_ to _eventsRecord_.[[EventList]].</li><li>Append Chosen Value Record { [[Event]]: _rmwEvent_, [[ChosenValue]]: _rawBytesRead_ } to _execution_.[[ChosenValues]].</li></ol></li><li>Else,<ol><li>Let _rawBytesRead_ be a List of length _elementSize_ whose elements are the sequence of _elementSize_ bytes starting with _block_[_byteIndex_].</li><li>Let _rawBytesModified_ be _op_(_rawBytesRead_, _rawBytes_).</li><li>Store the individual bytes of _rawBytesModified_ into _block_, starting at _block_[_byteIndex_].</li></ol></li><li>Return RawBytesToNumeric(_type_, _rawBytesRead_, _isLittleEndian_).</li></ol></emu-alg>\n      ","1.4":"<p>The ArrayBuffer constructor:</p>\n      <ul>\n        <li>is <dfn>%ArrayBuffer%</dfn>.</li>\n        <li>is the initial value of the *\"ArrayBuffer\"* property of the global object.</li>\n        <li>creates and initializes a new ArrayBuffer when called as a constructor.</li>\n        <li>is not intended to be called as a function and will throw an exception when called in that manner.</li>\n        <li>may be used as the value of an `extends` clause of a class definition. Subclass constructors that intend to inherit the specified ArrayBuffer behaviour must include a `super` call to the ArrayBuffer constructor to create and initialize subclass instances with the internal state necessary to support the `ArrayBuffer.prototype` built-in methods.</li>\n      </ul>\n\n      ","1.4.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is *undefined*, throw a *TypeError* exception.</li><li>Let _byteLength_ be ? ToIndex(_length_).</li><li>Let _requestedMaxByteLength_ be ? GetArrayBufferMaxByteLengthOption(_options_).</li><li>Return ? AllocateArrayBuffer(NewTarget, _byteLength_, _requestedMaxByteLength_).</li></ol></emu-alg>\n      ","1.5":"<p>The ArrayBuffer constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is %Function.prototype%.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","1.5.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If _arg_ is not an Object, return *false*.</li><li>If _arg_ has a [[ViewedArrayBuffer]] internal slot, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      ","1.5.2":"<p>The initial value of `ArrayBuffer.prototype` is the ArrayBuffer prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *false* }.</p>\n      ","1.5.3":"<p>`ArrayBuffer[%Symbol.species%]` is an accessor property whose set accessor function is *undefined*. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Return the *this* value.</li></ol></emu-alg>\n        <p>The value of the *\"name\"* property of this function is *\"get [Symbol.species]\"*.</p>\n        <emu-note>\n          <p>25.1.6.7 normally uses its *this* value's constructor to create a derived object. However, a subclass constructor may over-ride that default behaviour for the 25.1.6.7 method by redefining its %Symbol.species% property.</p>\n        </emu-note>\n      ","1.6":"<p>The <dfn>ArrayBuffer prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn>%ArrayBuffer.prototype%</dfn>.</li>\n        <li>has a [[Prototype]] internal slot whose value is %Object.prototype%.</li>\n        <li>is an ordinary object.</li>\n        <li>does not have an [[ArrayBufferData]] or [[ArrayBufferByteLength]] internal slot.</li>\n      </ul>\n\n      ","1.6.1":"<p>`ArrayBuffer.prototype.byteLength` is an accessor property whose set accessor function is *undefined*. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(_obj_) is *true*, throw a *TypeError* exception.</li><li>If IsDetachedBuffer(_obj_) is *true*, return *+0*<sub>𝔽</sub>.</li><li>Let _length_ be _obj_.[[ArrayBufferByteLength]].</li><li>Return 𝔽(_length_).</li></ol></emu-alg>\n      ","1.6.2":"<p>The initial value of `ArrayBuffer.prototype.constructor` is %ArrayBuffer%.</p>\n      ","1.6.3":"<p>`ArrayBuffer.prototype.detached` is an accessor property whose set accessor function is *undefined*. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(_obj_) is *true*, throw a *TypeError* exception.</li><li>Return IsDetachedBuffer(_obj_).</li></ol></emu-alg>\n      ","1.6.4":"<p>`ArrayBuffer.prototype.maxByteLength` is an accessor property whose set accessor function is *undefined*. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(_obj_) is *true*, throw a *TypeError* exception.</li><li>If IsDetachedBuffer(_obj_) is *true*, return *+0*<sub>𝔽</sub>.</li><li>If IsFixedLengthArrayBuffer(_obj_) is *true*, then<ol><li>Let _length_ be _obj_.[[ArrayBufferByteLength]].</li></ol></li><li>Else,<ol><li>Let _length_ be _obj_.[[ArrayBufferMaxByteLength]].</li></ol></li><li>Return 𝔽(_length_).</li></ol></emu-alg>\n      ","1.6.5":"<p>`ArrayBuffer.prototype.resizable` is an accessor property whose set accessor function is *undefined*. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(_obj_) is *true*, throw a *TypeError* exception.</li><li>If IsFixedLengthArrayBuffer(_obj_) is *false*, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      ","1.6.6":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[ArrayBufferMaxByteLength]]).</li><li>If IsSharedArrayBuffer(_obj_) is *true*, throw a *TypeError* exception.</li><li>Let _newByteLength_ be ? ToIndex(_newLength_).</li><li>If IsDetachedBuffer(_obj_) is *true*, throw a *TypeError* exception.</li><li>If _newByteLength_ > _obj_.[[ArrayBufferMaxByteLength]], throw a *RangeError* exception.</li><li>Let _hostHandled_ be ? HostResizeArrayBuffer(_obj_, _newByteLength_).</li><li>If _hostHandled_ is ~handled~, return *undefined*.</li><li>Let _oldBlock_ be _obj_.[[ArrayBufferData]].</li><li>Let _newBlock_ be ? CreateByteDataBlock(_newByteLength_).</li><li>Let _copyLength_ be min(_newByteLength_, _obj_.[[ArrayBufferByteLength]]).</li><li>Perform CopyDataBlockBytes(_newBlock_, 0, _oldBlock_, 0, _copyLength_).</li><li>NOTE: Neither creation of the new Data Block nor copying from the old Data Block are observable. Implementations may implement this method as in-place growth or shrinkage.</li><li>Set _obj_.[[ArrayBufferData]] to _newBlock_.</li><li>Set _obj_.[[ArrayBufferByteLength]] to _newByteLength_.</li><li>Return *undefined*.</li></ol></emu-alg>\n      ","1.6.7":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(_obj_) is *true*, throw a *TypeError* exception.</li><li>If IsDetachedBuffer(_obj_) is *true*, throw a *TypeError* exception.</li><li>Let _len_ be _obj_.[[ArrayBufferByteLength]].</li><li>Let _relativeStart_ be ? ToIntegerOrInfinity(_start_).</li><li>If _relativeStart_ = -∞, let _first_ be 0.</li><li>Else if _relativeStart_ &lt; 0, let _first_ be max(_len_ + _relativeStart_, 0).</li><li>Else, let _first_ be min(_relativeStart_, _len_).</li><li>If _end_ is *undefined*, let _relativeEnd_ be _len_; else let _relativeEnd_ be ? ToIntegerOrInfinity(_end_).</li><li>If _relativeEnd_ = -∞, let _final_ be 0.</li><li>Else if _relativeEnd_ &lt; 0, let _final_ be max(_len_ + _relativeEnd_, 0).</li><li>Else, let _final_ be min(_relativeEnd_, _len_).</li><li>Let _newLen_ be max(_final_ - _first_, 0).</li><li>Let _ctor_ be ? SpeciesConstructor(_obj_, %ArrayBuffer%).</li><li>Let _new_ be ? Construct(_ctor_, « 𝔽(_newLen_) »).</li><li>Perform ? RequireInternalSlot(_new_, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(_new_) is *true*, throw a *TypeError* exception.</li><li>If IsDetachedBuffer(_new_) is *true*, throw a *TypeError* exception.</li><li>If SameValue(_new_, _obj_) is *true*, throw a *TypeError* exception.</li><li>If _new_.[[ArrayBufferByteLength]] &lt; _newLen_, throw a *TypeError* exception.</li><li>NOTE: Side-effects of the above steps may have detached or resized _obj_.</li><li>If IsDetachedBuffer(_obj_) is *true*, throw a *TypeError* exception.</li><li>Let _fromBuf_ be _obj_.[[ArrayBufferData]].</li><li>Let _toBuf_ be _new_.[[ArrayBufferData]].</li><li>Let _currentLen_ be _obj_.[[ArrayBufferByteLength]].</li><li>If _first_ &lt; _currentLen_, then<ol><li>Let _count_ be min(_newLen_, _currentLen_ - _first_).</li><li>Perform CopyDataBlockBytes(_toBuf_, 0, _fromBuf_, _first_, _count_).</li></ol></li><li>Return _new_.</li></ol></emu-alg>\n      ","1.6.8":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Return ? ArrayBufferCopyAndDetach(_obj_, _newLength_, ~preserve-resizability~).</li></ol></emu-alg>\n      ","1.6.9":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Return ? ArrayBufferCopyAndDetach(_obj_, _newLength_, ~fixed-length~).</li></ol></emu-alg>\n      ","1.6.10":"<p>The initial value of the %Symbol.toStringTag% property is the String value *\"ArrayBuffer\"*.</p>\n        <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *true* }.</p>\n      ","1.7":"<p>ArrayBuffer instances inherit properties from the ArrayBuffer prototype object. ArrayBuffer instances each have an [[ArrayBufferData]] internal slot, an [[ArrayBufferByteLength]] internal slot, and an [[ArrayBufferDetachKey]] internal slot. ArrayBuffer instances which are resizable each have an [[ArrayBufferMaxByteLength]] internal slot.</p>\n      <p>ArrayBuffer instances whose [[ArrayBufferData]] is *null* are considered to be detached and all operators to access or modify data contained in the ArrayBuffer instance will fail.</p>\n      <p>ArrayBuffer instances whose [[ArrayBufferDetachKey]] is set to a value other than *undefined* need to have all DetachArrayBuffer calls passing that same \"detach key\" as an argument, otherwise a TypeError will result. This internal slot is only ever set by certain embedding environments, not by algorithms in this specification.</p>\n    ","1.8":"<emu-note>\n        <p>The following are guidelines for ECMAScript programmers working with resizable ArrayBuffer.</p>\n        <p>We recommend that programs be tested in their deployment environments where possible. The amount of available physical memory differs greatly between hardware devices. Similarly, virtual memory subsystems also differ greatly between hardware devices as well as operating systems. An application that runs without out-of-memory errors on a 64-bit desktop web browser could run out of memory on a 32-bit mobile web browser.</p>\n        <p>When choosing a value for the *\"maxByteLength\"* option for resizable ArrayBuffer, we recommend that the smallest possible size for the application be chosen. We recommend that *\"maxByteLength\"* does not exceed 1,073,741,824 (2<sup>30</sup> bytes or 1GiB).</p>\n        <p>Please note that successfully constructing a resizable ArrayBuffer for a particular maximum size does not guarantee that future resizes will succeed.</p>\n      </emu-note>\n\n      <emu-note>\n        <p>The following are guidelines for ECMAScript implementers implementing resizable ArrayBuffer.</p>\n        <p>Resizable ArrayBuffer can be implemented as copying upon resize, as in-place growth via reserving virtual memory up front, or as a combination of both for different values of the constructor's *\"maxByteLength\"* option.</p>\n        <p>If a host is multi-tenanted (i.e. it runs many ECMAScript applications simultaneously), such as a web browser, and its implementations choose to implement in-place growth by reserving virtual memory, we recommend that both 32-bit and 64-bit implementations throw for values of *\"maxByteLength\"* ≥ 1GiB to 1.5GiB. This is to reduce the likelihood a single application can exhaust the virtual memory address space and to reduce interoperability risk.</p>\n        <p>If a host does not have virtual memory, such as those running on embedded devices without an MMU, or if a host only implements resizing by copying, it may accept any <emu-not-ref>Number value for</emu-not-ref> the *\"maxByteLength\"* option. However, we recommend a *RangeError* be thrown if a memory block of the requested size can never be allocated. For example, if the requested size is greater than the maximum amount of usable memory on the device.</p>\n      </emu-note>\n    ","2.1":"<p>A <dfn>fixed-length SharedArrayBuffer</dfn> is a SharedArrayBuffer whose byte length cannot change after creation.</p>\n      <p>A <dfn>growable SharedArrayBuffer</dfn> is a SharedArrayBuffer whose byte length may increase after creation via calls to 25.2.5.3.</p>\n      <p>The kind of SharedArrayBuffer object that is created depends on the arguments passed to 25.2.3.1.</p>\n    ","2.2":"","2.2.1":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It is used to create a SharedArrayBuffer.</dd>\n        </dl>\n        <emu-alg><ol><li>Let _slots_ be « [[ArrayBufferData]] ».</li><li>If _maxByteLength_ is present and _maxByteLength_ is not ~empty~, let _allocatingGrowableBuffer_ be *true*; else let _allocatingGrowableBuffer_ be *false*.</li><li>If _allocatingGrowableBuffer_ is *true*, then<ol><li>If _byteLength_ > _maxByteLength_, throw a *RangeError* exception.</li><li>Append [[ArrayBufferByteLengthData]] and [[ArrayBufferMaxByteLength]] to _slots_.</li></ol></li><li>Else,<ol><li>Append [[ArrayBufferByteLength]] to _slots_.</li></ol></li><li>Let _obj_ be ? OrdinaryCreateFromConstructor(_constructor_, *\"%SharedArrayBuffer.prototype%\"*, _slots_).</li><li>If _allocatingGrowableBuffer_ is *true*, let _allocLength_ be _maxByteLength_; else let _allocLength_ be _byteLength_.</li><li>Let _block_ be ? CreateSharedByteDataBlock(_allocLength_).</li><li>Set _obj_.[[ArrayBufferData]] to _block_.</li><li>If _allocatingGrowableBuffer_ is *true*, then<ol><li>Assert: _byteLength_ ≤ _maxByteLength_.</li><li>Let _byteLengthBlock_ be ? CreateSharedByteDataBlock(8).</li><li>Perform SetValueInBuffer(_byteLengthBlock_, 0, ~biguint64~, ℤ(_byteLength_), *true*, ~seq-cst~).</li><li>Set _obj_.[[ArrayBufferByteLengthData]] to _byteLengthBlock_.</li><li>Set _obj_.[[ArrayBufferMaxByteLength]] to _maxByteLength_.</li></ol></li><li>Else,<ol><li>Set _obj_.[[ArrayBufferByteLength]] to _byteLength_.</li></ol></li><li>Return _obj_.</li></ol></emu-alg>\n      ","2.2.2":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It tests whether an object is a SharedArrayBuffer.</dd>\n        </dl>\n        <emu-alg><ol><li>If _obj_.[[ArrayBufferData]] is a Shared Data Block, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      ","2.2.3":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It tests whether an object is a growable SharedArrayBuffer.</dd>\n        </dl>\n        <emu-alg><ol><li>If IsSharedArrayBuffer(_obj_) is *true* and _obj_ has an [[ArrayBufferByteLengthData]] internal slot, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      ","2.2.4":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It gives the host an opportunity to perform implementation-defined growing of _buffer_. If the host chooses not to handle growing of _buffer_, it may return ~unhandled~ for the default behaviour.</dd>\n        </dl>\n        <p>The implementation of HostGrowSharedArrayBuffer must conform to the following requirements:</p>\n        <ul>\n          <li>If the abstract operation does not complete normally with ~unhandled~, and _newByteLength_ &lt; the current byte length of the _buffer_ or _newByteLength_ > _buffer_.[[ArrayBufferMaxByteLength]], throw a *RangeError* exception.</li>\n          <li>Let _agentRecord_ be the Agent Record of the surrounding agent. Let _isLittleEndian_ be _agentRecord_.[[LittleEndian]]. If the abstract operation completes normally with ~handled~, a WriteSharedMemory or ReadModifyWriteSharedMemory event whose [[Order]] is ~seq-cst~, [[Payload]] is NumericToRawBytes(~biguint64~, _newByteLength_, _isLittleEndian_), [[Block]] is _buffer_.[[ArrayBufferByteLengthData]], [[ByteIndex]] is 0, and [[ElementSize]] is 8 is added to the surrounding agent's candidate execution such that racing calls to 25.2.5.3 are not \"lost\", i.e. silently do nothing.</li>\n        </ul>\n\n        <emu-note>\n          <p>The second requirement above is intentionally vague about how or when the current byte length of _buffer_ is read. Because the byte length must be updated via an atomic read-modify-write operation on the underlying hardware, architectures that use load-link/store-conditional or load-exclusive/store-exclusive instruction pairs may wish to keep the paired instructions close in the instruction stream. As such, 25.2.5.3 itself does not perform bounds checking on _newByteLength_ before calling HostGrowSharedArrayBuffer, nor is there a requirement on when the current byte length is read.</p>\n          <p>This is in contrast with HostResizeArrayBuffer, which is guaranteed that 0 ≤ _newByteLength_ ≤ _buffer_.[[ArrayBufferMaxByteLength]].</p>\n        </emu-note>\n\n        <p>The default implementation of HostGrowSharedArrayBuffer is to return NormalCompletion(~unhandled~).</p>\n      ","2.3":"<p>The SharedArrayBuffer constructor:</p>\n      <ul>\n        <li>is <dfn>%SharedArrayBuffer%</dfn>.</li>\n        <li>is the initial value of the *\"SharedArrayBuffer\"* property of the global object, if that property is present (see below).</li>\n        <li>creates and initializes a new SharedArrayBuffer when called as a constructor.</li>\n        <li>is not intended to be called as a function and will throw an exception when called in that manner.</li>\n        <li>may be used as the value of an `extends` clause of a class definition. Subclass constructors that intend to inherit the specified SharedArrayBuffer behaviour must include a `super` call to the SharedArrayBuffer constructor to create and initialize subclass instances with the internal state necessary to support the `SharedArrayBuffer.prototype` built-in methods.</li>\n      </ul>\n\n      <p>Whenever a host does not provide concurrent access to SharedArrayBuffers it may omit the *\"SharedArrayBuffer\"* property of the global object.</p>\n\n      <emu-note>\n        <p>Unlike an `ArrayBuffer`, a `SharedArrayBuffer` cannot become detached, and its internal [[ArrayBufferData]] slot is never *null*.</p>\n      </emu-note>\n\n      ","2.3.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is *undefined*, throw a *TypeError* exception.</li><li>Let _byteLength_ be ? ToIndex(_length_).</li><li>Let _requestedMaxByteLength_ be ? GetArrayBufferMaxByteLengthOption(_options_).</li><li>Return ? AllocateSharedArrayBuffer(NewTarget, _byteLength_, _requestedMaxByteLength_).</li></ol></emu-alg>\n      ","2.4":"<p>The SharedArrayBuffer constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is %Function.prototype%.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","2.4.1":"<p>The initial value of `SharedArrayBuffer.prototype` is the SharedArrayBuffer prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *false* }.</p>\n      ","2.4.2":"<p>`SharedArrayBuffer[%Symbol.species%]` is an accessor property whose set accessor function is *undefined*. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Return the *this* value.</li></ol></emu-alg>\n        <p>The value of the *\"name\"* property of this function is *\"get [Symbol.species]\"*.</p>\n      ","2.5":"<p>The <dfn>SharedArrayBuffer prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn>%SharedArrayBuffer.prototype%</dfn>.</li>\n        <li>has a [[Prototype]] internal slot whose value is %Object.prototype%.</li>\n        <li>is an ordinary object.</li>\n        <li>does not have an [[ArrayBufferData]] or [[ArrayBufferByteLength]] internal slot.</li>\n      </ul>\n\n      ","2.5.1":"<p>`SharedArrayBuffer.prototype.byteLength` is an accessor property whose set accessor function is *undefined*. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(_obj_) is *false*, throw a *TypeError* exception.</li><li>Let _length_ be ArrayBufferByteLength(_obj_, ~seq-cst~).</li><li>Return 𝔽(_length_).</li></ol></emu-alg>\n      ","2.5.2":"<p>The initial value of `SharedArrayBuffer.prototype.constructor` is %SharedArrayBuffer%.</p>\n      ","2.5.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[ArrayBufferMaxByteLength]]).</li><li>If IsSharedArrayBuffer(_obj_) is *false*, throw a *TypeError* exception.</li><li>Let _newByteLength_ be ? ToIndex(_newLength_).</li><li>Let _hostHandled_ be ? HostGrowSharedArrayBuffer(_obj_, _newByteLength_).</li><li>If _hostHandled_ is ~handled~, return *undefined*.</li><li>Let _agentRecord_ be the Agent Record of the surrounding agent.</li><li>Let _isLittleEndian_ be _agentRecord_.[[LittleEndian]].</li><li>Let _byteLengthBlock_ be _obj_.[[ArrayBufferByteLengthData]].</li><li>Let _currentByteLengthRawBytes_ be GetRawBytesFromSharedBlock(_byteLengthBlock_, 0, ~biguint64~, *true*, ~seq-cst~).</li><li>Let _newByteLengthRawBytes_ be NumericToRawBytes(~biguint64~, ℤ(_newByteLength_), _isLittleEndian_).</li><li>Repeat,<ol><li>NOTE: This is a compare-and-exchange loop to ensure that parallel, racing grows of the same buffer are totally ordered, are not lost, and do not silently do nothing. The loop exits if it was able to attempt to grow uncontended.</li><li>Let _currentByteLength_ be ℝ(RawBytesToNumeric(~biguint64~, _currentByteLengthRawBytes_, _isLittleEndian_)).</li><li>If _newByteLength_ = _currentByteLength_, return *undefined*.</li><li>If _newByteLength_ &lt; _currentByteLength_ or _newByteLength_ > _obj_.[[ArrayBufferMaxByteLength]], throw a *RangeError* exception.</li><li>Let _byteLengthDelta_ be _newByteLength_ - _currentByteLength_.</li><li>If it is impossible to create a new Shared Data Block value consisting of _byteLengthDelta_ bytes, throw a *RangeError* exception.</li><li>NOTE: No new Shared Data Block is constructed and used here. The observable behaviour of growable SharedArrayBuffers is specified by allocating a max-sized Shared Data Block at construction time, and this step captures the requirement that implementations that run out of memory must throw a *RangeError*.</li><li>Let _readByteLengthRawBytes_ be AtomicCompareExchangeInSharedBlock(_byteLengthBlock_, 0, 8, _currentByteLengthRawBytes_, _newByteLengthRawBytes_).</li><li>If ByteListEqual(_readByteLengthRawBytes_, _currentByteLengthRawBytes_) is *true*, return *undefined*.</li><li>Set _currentByteLengthRawBytes_ to _readByteLengthRawBytes_.</li></ol></li></ol></emu-alg>\n        <emu-note>\n          <p>Spurious failures of the compare-exchange to update the length are prohibited. If the bounds checking for the new length passes and the implementation is not out of memory, a ReadModifyWriteSharedMemory event (i.e. a successful compare-exchange) is always added into the candidate execution.</p>\n          <p>Parallel calls to SharedArrayBuffer.prototype.grow are totally ordered. For example, consider two racing calls: `sab.grow(10)` and `sab.grow(20)`. One of the two calls is guaranteed to win the race. The call to `sab.grow(10)` will never shrink `sab` even if `sab.grow(20)` happened first; in that case it will instead throw a RangeError.</p>\n        </emu-note>\n      ","2.5.4":"<p>`SharedArrayBuffer.prototype.growable` is an accessor property whose set accessor function is *undefined*. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(_obj_) is *false*, throw a *TypeError* exception.</li><li>If IsFixedLengthArrayBuffer(_obj_) is *false*, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      ","2.5.5":"<p>`SharedArrayBuffer.prototype.maxByteLength` is an accessor property whose set accessor function is *undefined*. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(_obj_) is *false*, throw a *TypeError* exception.</li><li>If IsFixedLengthArrayBuffer(_obj_) is *true*, then<ol><li>Let _length_ be _obj_.[[ArrayBufferByteLength]].</li></ol></li><li>Else,<ol><li>Let _length_ be _obj_.[[ArrayBufferMaxByteLength]].</li></ol></li><li>Return 𝔽(_length_).</li></ol></emu-alg>\n      ","2.5.6":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(_obj_) is *false*, throw a *TypeError* exception.</li><li>Let _len_ be ArrayBufferByteLength(_obj_, ~seq-cst~).</li><li>Let _relativeStart_ be ? ToIntegerOrInfinity(_start_).</li><li>If _relativeStart_ = -∞, let _first_ be 0.</li><li>Else if _relativeStart_ &lt; 0, let _first_ be max(_len_ + _relativeStart_, 0).</li><li>Else, let _first_ be min(_relativeStart_, _len_).</li><li>If _end_ is *undefined*, let _relativeEnd_ be _len_; else let _relativeEnd_ be ? ToIntegerOrInfinity(_end_).</li><li>If _relativeEnd_ = -∞, let _final_ be 0.</li><li>Else if _relativeEnd_ &lt; 0, let _final_ be max(_len_ + _relativeEnd_, 0).</li><li>Else, let _final_ be min(_relativeEnd_, _len_).</li><li>Let _newLen_ be max(_final_ - _first_, 0).</li><li>Let _ctor_ be ? SpeciesConstructor(_obj_, %SharedArrayBuffer%).</li><li>Let _new_ be ? Construct(_ctor_, « 𝔽(_newLen_) »).</li><li>Perform ? RequireInternalSlot(_new_, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(_new_) is *false*, throw a *TypeError* exception.</li><li>If _new_.[[ArrayBufferData]] is _obj_.[[ArrayBufferData]], throw a *TypeError* exception.</li><li>If ArrayBufferByteLength(_new_, ~seq-cst~) &lt; _newLen_, throw a *TypeError* exception.</li><li>Let _fromBuf_ be _obj_.[[ArrayBufferData]].</li><li>Let _toBuf_ be _new_.[[ArrayBufferData]].</li><li>Perform CopyDataBlockBytes(_toBuf_, 0, _fromBuf_, _first_, _newLen_).</li><li>Return _new_.</li></ol></emu-alg>\n      ","2.5.7":"<p>The initial value of the %Symbol.toStringTag% property is the String value *\"SharedArrayBuffer\"*.</p>\n        <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *true* }.</p>\n      ","2.6":"<p>SharedArrayBuffer instances inherit properties from the SharedArrayBuffer prototype object. SharedArrayBuffer instances each have an [[ArrayBufferData]] internal slot. SharedArrayBuffer instances which are not growable each have an [[ArrayBufferByteLength]] internal slot. SharedArrayBuffer instances which are growable each have an [[ArrayBufferByteLengthData]] internal slot and an [[ArrayBufferMaxByteLength]] internal slot.</p>\n\n      <emu-note>\n        <p>SharedArrayBuffer instances, unlike ArrayBuffer instances, are never detached.</p>\n      </emu-note>\n    ","2.7":"<emu-note>\n        <p>The following are guidelines for ECMAScript programmers working with growable SharedArrayBuffer.</p>\n        <p>We recommend that programs be tested in their deployment environments where possible. The amount of available physical memory differ greatly between hardware devices. Similarly, virtual memory subsystems also differ greatly between hardware devices as well as operating systems. An application that runs without out-of-memory errors on a 64-bit desktop web browser could run out of memory on a 32-bit mobile web browser.</p>\n        <p>When choosing a value for the *\"maxByteLength\"* option for growable SharedArrayBuffer, we recommend that the smallest possible size for the application be chosen. We recommend that *\"maxByteLength\"* does not exceed 1073741824, or 1GiB.</p>\n        <p>Please note that successfully constructing a growable SharedArrayBuffer for a particular maximum size does not guarantee that future grows will succeed.</p>\n        <p>Not all loads of a growable SharedArrayBuffer's length are synchronizing ~seq-cst~ loads. Loads of the length that are for bounds-checking of an integer-indexed property access, e.g. `u8[idx]`, are not synchronizing. In general, in the absence of explicit synchronization, one property access being in-bound does not imply a subsequent property access in the same agent is also in-bound. In contrast, explicit loads of the length via the `length` and `byteLength` getters on SharedArrayBuffer, %TypedArray%.prototype, and DataView.prototype are synchronizing. Loads of the length that are performed by built-in methods to check if a TypedArray is entirely out-of-bounds are also synchronizing.</p>\n      </emu-note>\n\n      <emu-note>\n        <p>The following are guidelines for ECMAScript implementers implementing growable SharedArrayBuffer.</p>\n        <p>We recommend growable SharedArrayBuffer be implemented as in-place growth via reserving virtual memory up front.</p>\n        <p>Because grow operations can happen in parallel with memory accesses on a growable SharedArrayBuffer, the constraints of the memory model require that even unordered accesses do not \"tear\" (bits of their values will not be mixed). In practice, this means the underlying data block of a growable SharedArrayBuffer cannot be grown by being copied without stopping the world. We do not recommend stopping the world as an implementation strategy because it introduces a serialization point and is slow.</p>\n        <p>Grown memory must appear zeroed from the moment of its creation, including to any racy accesses in parallel. This can be accomplished via zero-filled-on-demand virtual memory pages, or careful synchronization if manually zeroing memory.</p>\n        <p>Integer-indexed property access on TypedArray views of growable SharedArrayBuffers is intended to be optimizable similarly to access on TypedArray views of non-growable SharedArrayBuffers, because integer-indexed property loads on are not synchronizing on the underlying buffer's length (see programmer guidelines above). For example, bounds checks for property accesses may still be hoisted out of loops.</p>\n        <p>In practice it is difficult to implement growable SharedArrayBuffer by copying on hosts that do not have virtual memory, such as those running on embedded devices without an MMU. Memory usage behaviour of growable SharedArrayBuffers on such hosts may significantly differ from that of hosts with virtual memory. Such hosts should clearly communicate memory usage expectations to users.</p>\n      </emu-note>\n    ","3.1":"","3.1.1":"<p>A <dfn variants=\"DataView With Buffer Witness Records\">DataView With Buffer Witness Record</dfn> is a Record value used to encapsulate a DataView along with a cached byte length of the viewed buffer. It is used to help ensure there is a single ReadSharedMemory event of the byte length data block when the viewed buffer is a growable SharedArrayBuffer.</p>\n        <p>DataView With Buffer Witness Records have the fields listed in table-dataview-with-buffer-witness-record-fields.</p>\n        <emu-table id=\"table-dataview-with-buffer-witness-record-fields\" caption=\"DataView With Buffer Witness Record Fields\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Field Name\n                </th>\n                <th>\n                  Value\n                </th>\n                <th>\n                  Meaning\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                [[Object]]\n              </td>\n              <td>\n                a DataView\n              </td>\n              <td>\n                The DataView object whose buffer's byte length is loaded.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[CachedBufferByteLength]]\n              </td>\n              <td>\n                a non-negative integer or ~detached~\n              </td>\n              <td>\n                The byte length of the object's [[ViewedArrayBuffer]] when the Record was created.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n      ","3.1.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _buffer_ be _obj_.[[ViewedArrayBuffer]].</li><li>If IsDetachedBuffer(_buffer_) is *true*, then<ol><li>Let _byteLength_ be ~detached~.</li></ol></li><li>Else,<ol><li>Let _byteLength_ be ArrayBufferByteLength(_buffer_, _order_).</li></ol></li><li>Return the DataView With Buffer Witness Record { [[Object]]: _obj_, [[CachedBufferByteLength]]: _byteLength_ }.</li></ol></emu-alg>\n      ","3.1.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: IsViewOutOfBounds(_viewRecord_) is *false*.</li><li>Let _view_ be _viewRecord_.[[Object]].</li><li>If _view_.[[ByteLength]] is not ~auto~, return _view_.[[ByteLength]].</li><li>Assert: IsFixedLengthArrayBuffer(_view_.[[ViewedArrayBuffer]]) is *false*.</li><li>Let _byteOffset_ be _view_.[[ByteOffset]].</li><li>Let _byteLength_ be _viewRecord_.[[CachedBufferByteLength]].</li><li>Assert: _byteLength_ is not ~detached~.</li><li>Return _byteLength_ - _byteOffset_.</li></ol></emu-alg>\n      ","3.1.4":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _view_ be _viewRecord_.[[Object]].</li><li>Let _bufferByteLength_ be _viewRecord_.[[CachedBufferByteLength]].</li><li>If IsDetachedBuffer(_view_.[[ViewedArrayBuffer]]) is *true*, then<ol><li>Assert: _bufferByteLength_ is ~detached~.</li><li>Return *true*.</li></ol></li><li>Assert: _bufferByteLength_ is a non-negative integer.</li><li>Let _byteOffsetStart_ be _view_.[[ByteOffset]].</li><li>If _view_.[[ByteLength]] is ~auto~, then<ol><li>Let _byteOffsetEnd_ be _bufferByteLength_.</li></ol></li><li>Else,<ol><li>Let _byteOffsetEnd_ be _byteOffsetStart_ + _view_.[[ByteLength]].</li></ol></li><li>NOTE: A 0-length DataView whose [[ByteOffset]] is _bufferByteLength_ is not considered out-of-bounds.</li><li>If _byteOffsetStart_ > _bufferByteLength_ or _byteOffsetEnd_ > _bufferByteLength_, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      ","3.1.5":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It is used by functions on DataView instances to retrieve values from the view's buffer.</dd>\n        </dl>\n        <emu-alg><ol><li>Perform ? RequireInternalSlot(_view_, [[DataView]]).</li><li>Assert: _view_ has a [[ViewedArrayBuffer]] internal slot.</li><li>Let _getIndex_ be ? ToIndex(_requestIndex_).</li><li>Set _isLittleEndian_ to ToBoolean(_isLittleEndian_).</li><li>Let _viewOffset_ be _view_.[[ByteOffset]].</li><li>Let _viewRecord_ be MakeDataViewWithBufferWitnessRecord(_view_, ~unordered~).</li><li>NOTE: Bounds checking is not a synchronizing operation when _view_'s backing buffer is a growable SharedArrayBuffer.</li><li>If IsViewOutOfBounds(_viewRecord_) is *true*, throw a *TypeError* exception.</li><li>Let _viewSize_ be GetViewByteLength(_viewRecord_).</li><li>Let _elementSize_ be the Element Size value specified in table-the-typedarray-constructors for Element Type _type_.</li><li>If _getIndex_ + _elementSize_ > _viewSize_, throw a *RangeError* exception.</li><li>Let _bufferIndex_ be _getIndex_ + _viewOffset_.</li><li>Return GetValueFromBuffer(_view_.[[ViewedArrayBuffer]], _bufferIndex_, _type_, *false*, ~unordered~, _isLittleEndian_).</li></ol></emu-alg>\n      ","3.1.6":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It is used by functions on DataView instances to store values into the view's buffer.</dd>\n        </dl>\n        <emu-alg><ol><li>Perform ? RequireInternalSlot(_view_, [[DataView]]).</li><li>Assert: _view_ has a [[ViewedArrayBuffer]] internal slot.</li><li>Let _getIndex_ be ? ToIndex(_requestIndex_).</li><li>If IsBigIntElementType(_type_) is *true*, let _numberValue_ be ? ToBigInt(_value_).</li><li>Else, let _numberValue_ be ? ToNumber(_value_).</li><li>Set _isLittleEndian_ to ToBoolean(_isLittleEndian_).</li><li>Let _viewOffset_ be _view_.[[ByteOffset]].</li><li>Let _viewRecord_ be MakeDataViewWithBufferWitnessRecord(_view_, ~unordered~).</li><li>NOTE: Bounds checking is not a synchronizing operation when _view_'s backing buffer is a growable SharedArrayBuffer.</li><li>If IsViewOutOfBounds(_viewRecord_) is *true*, throw a *TypeError* exception.</li><li>Let _viewSize_ be GetViewByteLength(_viewRecord_).</li><li>Let _elementSize_ be the Element Size value specified in table-the-typedarray-constructors for Element Type _type_.</li><li>If _getIndex_ + _elementSize_ > _viewSize_, throw a *RangeError* exception.</li><li>Let _bufferIndex_ be _getIndex_ + _viewOffset_.</li><li>Perform SetValueInBuffer(_view_.[[ViewedArrayBuffer]], _bufferIndex_, _type_, _numberValue_, *false*, ~unordered~, _isLittleEndian_).</li><li>Return *undefined*.</li></ol></emu-alg>\n      ","3.2":"<p>The DataView constructor:</p>\n      <ul>\n        <li>is <dfn>%DataView%</dfn>.</li>\n        <li>is the initial value of the *\"DataView\"* property of the global object.</li>\n        <li>creates and initializes a new DataView when called as a constructor.</li>\n        <li>is not intended to be called as a function and will throw an exception when called in that manner.</li>\n        <li>may be used as the value of an `extends` clause of a class definition. Subclass constructors that intend to inherit the specified DataView behaviour must include a `super` call to the DataView constructor to create and initialize subclass instances with the internal state necessary to support the `DataView.prototype` built-in methods.</li>\n      </ul>\n\n      ","3.2.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is *undefined*, throw a *TypeError* exception.</li><li>Perform ? RequireInternalSlot(_buffer_, [[ArrayBufferData]]).</li><li>Let _offset_ be ? ToIndex(_byteOffset_).</li><li>If IsDetachedBuffer(_buffer_) is *true*, throw a *TypeError* exception.</li><li>Let _bufferByteLength_ be ArrayBufferByteLength(_buffer_, ~seq-cst~).</li><li>If _offset_ > _bufferByteLength_, throw a *RangeError* exception.</li><li>Let _bufferIsFixedLength_ be IsFixedLengthArrayBuffer(_buffer_).</li><li>If _byteLength_ is *undefined*, then<ol><li>If _bufferIsFixedLength_ is *true*, then<ol><li>Let _viewByteLength_ be _bufferByteLength_ - _offset_.</li></ol></li><li>Else,<ol><li>Let _viewByteLength_ be ~auto~.</li></ol></li></ol></li><li>Else,<ol><li>Let _viewByteLength_ be ? ToIndex(_byteLength_).</li><li>If _offset_ + _viewByteLength_ > _bufferByteLength_, throw a *RangeError* exception.</li></ol></li><li>Let _obj_ be ? OrdinaryCreateFromConstructor(NewTarget, *\"%DataView.prototype%\"*, « [[DataView]], [[ViewedArrayBuffer]], [[ByteLength]], [[ByteOffset]] »).</li><li>If IsDetachedBuffer(_buffer_) is *true*, throw a *TypeError* exception.</li><li>Set _bufferByteLength_ to ArrayBufferByteLength(_buffer_, ~seq-cst~).</li><li>If _offset_ > _bufferByteLength_, throw a *RangeError* exception.</li><li>If _byteLength_ is not *undefined*, then<ol><li>If _offset_ + _viewByteLength_ > _bufferByteLength_, throw a *RangeError* exception.</li></ol></li><li>Set _obj_.[[ViewedArrayBuffer]] to _buffer_.</li><li>Set _obj_.[[ByteLength]] to _viewByteLength_.</li><li>Set _obj_.[[ByteOffset]] to _offset_.</li><li>Return _obj_.</li></ol></emu-alg>\n      ","3.3":"<p>The DataView constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is %Function.prototype%.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","3.3.1":"<p>The initial value of `DataView.prototype` is the DataView prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *false* }.</p>\n      ","3.4":"<p>The <dfn>DataView prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn>%DataView.prototype%</dfn>.</li>\n        <li>has a [[Prototype]] internal slot whose value is %Object.prototype%.</li>\n        <li>is an ordinary object.</li>\n        <li>does not have a [[DataView]], [[ViewedArrayBuffer]], [[ByteLength]], or [[ByteOffset]] internal slot.</li>\n      </ul>\n\n      ","3.4.1":"<p>`DataView.prototype.buffer` is an accessor property whose set accessor function is *undefined*. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[DataView]]).</li><li>Assert: _obj_ has a [[ViewedArrayBuffer]] internal slot.</li><li>Let _buffer_ be _obj_.[[ViewedArrayBuffer]].</li><li>Return _buffer_.</li></ol></emu-alg>\n      ","3.4.2":"<p>`DataView.prototype.byteLength` is an accessor property whose set accessor function is *undefined*. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[DataView]]).</li><li>Assert: _obj_ has a [[ViewedArrayBuffer]] internal slot.</li><li>Let _viewRecord_ be MakeDataViewWithBufferWitnessRecord(_obj_, ~seq-cst~).</li><li>If IsViewOutOfBounds(_viewRecord_) is *true*, throw a *TypeError* exception.</li><li>Let _size_ be GetViewByteLength(_viewRecord_).</li><li>Return 𝔽(_size_).</li></ol></emu-alg>\n      ","3.4.3":"<p>`DataView.prototype.byteOffset` is an accessor property whose set accessor function is *undefined*. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _obj_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_obj_, [[DataView]]).</li><li>Assert: _obj_ has a [[ViewedArrayBuffer]] internal slot.</li><li>Let _viewRecord_ be MakeDataViewWithBufferWitnessRecord(_obj_, ~seq-cst~).</li><li>If IsViewOutOfBounds(_viewRecord_) is *true*, throw a *TypeError* exception.</li><li>Let _offset_ be _obj_.[[ByteOffset]].</li><li>Return 𝔽(_offset_).</li></ol></emu-alg>\n      ","3.4.4":"<p>The initial value of `DataView.prototype.constructor` is %DataView%.</p>\n      ","3.4.5":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>Return ? GetViewValue(_view_, _byteOffset_, _littleEndian_, ~bigint64~).</li></ol></emu-alg>\n      ","3.4.6":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>Return ? GetViewValue(_view_, _byteOffset_, _littleEndian_, ~biguint64~).</li></ol></emu-alg>\n      ","3.4.7":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? GetViewValue(_view_, _byteOffset_, _littleEndian_, ~float16~).</li></ol></emu-alg>\n      ","3.4.8":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? GetViewValue(_view_, _byteOffset_, _littleEndian_, ~float32~).</li></ol></emu-alg>\n      ","3.4.9":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? GetViewValue(_view_, _byteOffset_, _littleEndian_, ~float64~).</li></ol></emu-alg>\n      ","3.4.10":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>Return ? GetViewValue(_view_, _byteOffset_, *true*, ~int8~).</li></ol></emu-alg>\n      ","3.4.11":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? GetViewValue(_view_, _byteOffset_, _littleEndian_, ~int16~).</li></ol></emu-alg>\n      ","3.4.12":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? GetViewValue(_view_, _byteOffset_, _littleEndian_, ~int32~).</li></ol></emu-alg>\n      ","3.4.13":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>Return ? GetViewValue(_view_, _byteOffset_, *true*, ~uint8~).</li></ol></emu-alg>\n      ","3.4.14":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? GetViewValue(_view_, _byteOffset_, _littleEndian_, ~uint16~).</li></ol></emu-alg>\n      ","3.4.15":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? GetViewValue(_view_, _byteOffset_, _littleEndian_, ~uint32~).</li></ol></emu-alg>\n      ","3.4.16":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>Return ? SetViewValue(_view_, _byteOffset_, _littleEndian_, ~bigint64~, _value_).</li></ol></emu-alg>\n      ","3.4.17":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>Return ? SetViewValue(_view_, _byteOffset_, _littleEndian_, ~biguint64~, _value_).</li></ol></emu-alg>\n      ","3.4.18":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? SetViewValue(_view_, _byteOffset_, _littleEndian_, ~float16~, _value_).</li></ol></emu-alg>\n      ","3.4.19":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? SetViewValue(_view_, _byteOffset_, _littleEndian_, ~float32~, _value_).</li></ol></emu-alg>\n      ","3.4.20":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? SetViewValue(_view_, _byteOffset_, _littleEndian_, ~float64~, _value_).</li></ol></emu-alg>\n      ","3.4.21":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>Return ? SetViewValue(_view_, _byteOffset_, *true*, ~int8~, _value_).</li></ol></emu-alg>\n      ","3.4.22":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? SetViewValue(_view_, _byteOffset_, _littleEndian_, ~int16~, _value_).</li></ol></emu-alg>\n      ","3.4.23":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? SetViewValue(_view_, _byteOffset_, _littleEndian_, ~int32~, _value_).</li></ol></emu-alg>\n      ","3.4.24":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>Return ? SetViewValue(_view_, _byteOffset_, *true*, ~uint8~, _value_).</li></ol></emu-alg>\n      ","3.4.25":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? SetViewValue(_view_, _byteOffset_, _littleEndian_, ~uint16~, _value_).</li></ol></emu-alg>\n      ","3.4.26":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _view_ be the *this* value.</li><li>If _littleEndian_ is not present, set _littleEndian_ to *false*.</li><li>Return ? SetViewValue(_view_, _byteOffset_, _littleEndian_, ~uint32~, _value_).</li></ol></emu-alg>\n      ","3.4.27":"<p>The initial value of the %Symbol.toStringTag% property is the String value *\"DataView\"*.</p>\n        <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *true* }.</p>\n      ","3.5":"<p>DataView instances are ordinary objects that inherit properties from the DataView prototype object. DataView instances each have [[DataView]], [[ViewedArrayBuffer]], [[ByteLength]], and [[ByteOffset]] internal slots.</p>\n      <emu-note>\n        <p>The value of the [[DataView]] internal slot is not used within this specification. The simple presence of that internal slot is used within the specification to identify objects created using the DataView constructor.</p>\n      </emu-note>\n    ","4.1":"<p>A <dfn variants=\"Waiter Records\">Waiter Record</dfn> is a Record value used to denote a particular call to `Atomics.wait` or `Atomics.waitAsync`.</p>\n      <p>A Waiter Record has fields listed in table-waiterrecord.</p>\n      <emu-table id=\"table-waiterrecord\" caption=\"Waiter Record Fields\">\n        <table>\n          <thead>\n            <tr>\n              <th>\n                Field Name\n              </th>\n              <th>\n                Value\n              </th>\n              <th>\n                Meaning\n              </th>\n            </tr>\n          </thead>\n          <tr>\n            <td>\n              [[AgentSignifier]]\n            </td>\n            <td>\n              an agent signifier\n            </td>\n            <td>\n              The agent that called `Atomics.wait` or `Atomics.waitAsync`.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[PromiseCapability]]\n            </td>\n            <td>\n              a PromiseCapability Record or ~blocking~\n            </td>\n            <td>\n              If denoting a call to `Atomics.waitAsync`, the resulting promise, otherwise ~blocking~.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[TimeoutTime]]\n            </td>\n            <td>\n              a non-negative extended mathematical value\n            </td>\n            <td>\n              The earliest time by which timeout may be triggered; computed using time values.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[Result]]\n            </td>\n            <td>\n              *\"ok\"* or *\"timed-out\"*\n            </td>\n            <td>\n              The return value of the call.\n            </td>\n          </tr>\n        </table>\n      </emu-table>\n    ","4.2":"<p>A <dfn variants=\"WaiterList Records\">WaiterList Record</dfn> is used to explain waiting and notification of agents via `Atomics.wait`, `Atomics.waitAsync`, and `Atomics.notify`.</p>\n      <p>A WaiterList Record has fields listed in table-waiterlistrecord.</p>\n      <emu-table id=\"table-waiterlistrecord\" caption=\"WaiterList Record Fields\">\n        <table>\n          <thead>\n            <tr>\n              <th>\n                Field Name\n              </th>\n              <th>\n                Value\n              </th>\n              <th>\n                Meaning\n              </th>\n            </tr>\n          </thead>\n          <tr>\n            <td>\n              [[Waiters]]\n            </td>\n            <td>\n              a List of Waiter Records\n            </td>\n            <td>\n              The calls to `Atomics.wait` or `Atomics.waitAsync` that are waiting on the location with which this WaiterList is associated.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[MostRecentLeaveEvent]]\n            </td>\n            <td>\n              a Synchronize event or ~empty~\n            </td>\n            <td>\n              The event of the most recent leaving of its critical section, or ~empty~ if its critical section has never been entered.\n            </td>\n          </tr>\n        </table>\n      </emu-table>\n      <p>There can be multiple Waiter Records in a WaiterList with the same agent signifier.</p>\n      <p>The agent cluster has a store of WaiterList Records; the store is indexed by (_block_, _i_), where _block_ is a Shared Data Block and _i_ a byte offset into the memory of _block_. WaiterList Records are agent-independent: a lookup in the store of WaiterList Records by (_block_, _i_) will result in the same WaiterList Record in any agent in the agent cluster.</p>\n      <p>Each WaiterList Record has a <dfn variants=\"critical sections\">critical section</dfn> that controls exclusive access to that WaiterList Record during evaluation. Only a single agent may enter a WaiterList Record's critical section at one time. Entering and leaving a WaiterList Record's critical section is controlled by the abstract operations EnterCriticalSection and LeaveCriticalSection. Operations on a WaiterList Record—adding and removing waiting agents, traversing the list of agents, suspending and notifying agents on the list, setting and retrieving the Synchronize event—may only be performed by agents that have entered the WaiterList Record's critical section.</p>\n    ","4.3":"","4.3.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _taRecord_ be ? ValidateTypedArray(_typedArray_, ~unordered~).</li><li>NOTE: Bounds checking is not a synchronizing operation when _typedArray_'s backing buffer is a growable SharedArrayBuffer.</li><li>If _waitable_ is *true*, then<ol><li>If _typedArray_.[[TypedArrayName]] is neither *\"Int32Array\"* nor *\"BigInt64Array\"*, throw a *TypeError* exception.</li></ol></li><li>Else,<ol><li>Let _type_ be TypedArrayElementType(_typedArray_).</li><li>If IsUnclampedIntegerElementType(_type_) is *false* and IsBigIntElementType(_type_) is *false*, throw a *TypeError* exception.</li></ol></li><li>Return _taRecord_.</li></ol></emu-alg>\n      ","4.3.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _length_ be TypedArrayLength(_taRecord_).</li><li>Let _accessIndex_ be ? ToIndex(_requestIndex_).</li><li>Assert: _accessIndex_ ≥ 0.</li><li>If _accessIndex_ ≥ _length_, throw a *RangeError* exception.</li><li>Let _typedArray_ be _taRecord_.[[Object]].</li><li>Let _elementSize_ be TypedArrayElementSize(_typedArray_).</li><li>Let _offset_ be _typedArray_.[[ByteOffset]].</li><li>Return (_accessIndex_ × _elementSize_) + _offset_.</li></ol></emu-alg>\n      ","4.3.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _taRecord_ be ? ValidateIntegerTypedArray(_typedArray_, *false*).</li><li>Return ? ValidateAtomicAccess(_taRecord_, _requestIndex_).</li></ol></emu-alg>\n      ","4.3.4":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>This operation revalidates the index within the backing buffer for atomic operations after all argument coercions are performed in Atomics methods, as argument coercions can have arbitrary side effects, which could cause the buffer to become out of bounds. This operation does not throw when _typedArray_'s backing buffer is a SharedArrayBuffer.</dd>\n        </dl>\n        <emu-alg><ol><li>Let _taRecord_ be MakeTypedArrayWithBufferWitnessRecord(_typedArray_, ~unordered~).</li><li>NOTE: Bounds checking is not a synchronizing operation when _typedArray_'s backing buffer is a growable SharedArrayBuffer.</li><li>If IsTypedArrayOutOfBounds(_taRecord_) is *true*, throw a *TypeError* exception.</li><li>Assert: _byteIndexInBuffer_ ≥ _typedArray_.[[ByteOffset]].</li><li>If _byteIndexInBuffer_ ≥ _taRecord_.[[CachedBufferByteLength]], throw a *RangeError* exception.</li><li>Return ~unused~.</li></ol></emu-alg>\n      ","4.3.5":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: _i_ and _i_ + 3 are valid byte offsets within the memory of _block_.</li><li>Return the WaiterList Record that is referenced by the pair (_block_, _i_).</li></ol></emu-alg>\n      ","4.3.6":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is not in the critical section for any WaiterList Record.</li><li>Wait until no agent is in the critical section for _waiterList_, then enter the critical section for _waiterList_ (without allowing any other agent to enter).</li><li>If _waiterList_.[[MostRecentLeaveEvent]] is not ~empty~, then<ol><li>NOTE: A _waiterList_ whose critical section has been entered at least once has a Synchronize event set by LeaveCriticalSection.</li><li>Let _agentRecord_ be the Agent Record of the surrounding agent.</li><li>Let _execution_ be _agentRecord_.[[CandidateExecution]].</li><li>Let _eventsRecord_ be the Agent Events Record of _execution_.[[EventsRecords]] whose [[AgentSignifier]] is AgentSignifier().</li><li>Let _enterEvent_ be a new Synchronize event.</li><li>Append _enterEvent_ to _eventsRecord_.[[EventList]].</li><li>Append (_waiterList_.[[MostRecentLeaveEvent]], _enterEvent_) to _eventsRecord_.[[AgentSynchronizesWith]].</li></ol></li><li>Return ~unused~.</li></ol></emu-alg>\n        <p>EnterCriticalSection has <dfn>contention</dfn> when an agent attempting to enter the critical section must wait for another agent to leave it. When there is no contention, FIFO order of EnterCriticalSection calls is observable. When there is contention, an implementation may choose an arbitrary order but may not cause an agent to wait indefinitely.</p>\n      ","4.3.7":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is in the critical section for _waiterList_.</li><li>Let _agentRecord_ be the Agent Record of the surrounding agent.</li><li>Let _execution_ be _agentRecord_.[[CandidateExecution]].</li><li>Let _eventsRecord_ be the Agent Events Record of _execution_.[[EventsRecords]] whose [[AgentSignifier]] is AgentSignifier().</li><li>Let _leaveEvent_ be a new Synchronize event.</li><li>Append _leaveEvent_ to _eventsRecord_.[[EventList]].</li><li>Set _waiterList_.[[MostRecentLeaveEvent]] to _leaveEvent_.</li><li>Leave the critical section for _waiterList_.</li><li>Return ~unused~.</li></ol></emu-alg>\n      ","4.3.8":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is in the critical section for _waiterList_.</li><li>Assert: There is no Waiter Record in _waiterList_.[[Waiters]] whose [[PromiseCapability]] field is _waiterRecord_.[[PromiseCapability]] and whose [[AgentSignifier]] field is _waiterRecord_.[[AgentSignifier]].</li><li>Append _waiterRecord_ to _waiterList_.[[Waiters]].</li><li>Return ~unused~.</li></ol></emu-alg>\n      ","4.3.9":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is in the critical section for _waiterList_.</li><li>Assert: _waiterList_.[[Waiters]] contains _waiterRecord_.</li><li>Remove _waiterRecord_ from _waiterList_.[[Waiters]].</li><li>Return ~unused~.</li></ol></emu-alg>\n      ","4.3.10":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is in the critical section for _waiterList_.</li><li>Let _len_ be the number of elements in _waiterList_.[[Waiters]].</li><li>Set _count_ to min(_count_, _len_).</li><li>Let _waiters_ be a List whose elements are the first _count_ elements of _waiterList_.[[Waiters]].</li><li>Remove the first _count_ elements of _waiterList_.[[Waiters]].</li><li>Return _waiters_.</li></ol></emu-alg>\n      ","4.3.11":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is in the critical section for _waiterList_.</li><li>Assert: _waiterList_.[[Waiters]] contains _waiterRecord_.</li><li>Let _thisAgent_ be AgentSignifier().</li><li>Assert: _waiterRecord_.[[AgentSignifier]] is _thisAgent_.</li><li>Assert: _waiterRecord_.[[PromiseCapability]] is ~blocking~.</li><li>Assert: AgentCanSuspend() is *true*.</li><li>Perform LeaveCriticalSection(_waiterList_) and suspend the surrounding agent until the time is _waiterRecord_.[[TimeoutTime]], performing the combined operation in such a way that a notification that arrives after the critical section is exited but before the suspension takes effect is not lost. The surrounding agent can only wake from suspension due to a timeout or due to another agent calling NotifyWaiter with arguments _waiterList_ and _thisAgent_ (i.e. via a call to `Atomics.notify`).</li><li>Perform EnterCriticalSection(_waiterList_).</li><li>Return ~unused~.</li></ol></emu-alg>\n      ","4.3.12":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is in the critical section for _waiterList_.</li><li>If _waiterRecord_.[[PromiseCapability]] is ~blocking~, then<ol><li>Wake the agent whose signifier is _waiterRecord_.[[AgentSignifier]] from suspension.</li><li>NOTE: This causes the agent to resume execution in SuspendThisAgent.</li></ol></li><li>Else if AgentSignifier() is _waiterRecord_.[[AgentSignifier]], then<ol><li>Let _promiseCapability_ be _waiterRecord_.[[PromiseCapability]].</li><li>Perform ! Call(_promiseCapability_.[[Resolve]], *undefined*, « _waiterRecord_.[[Result]] »).</li></ol></li><li>Else,<ol><li>Perform EnqueueResolveInAgentJob(_waiterRecord_.[[AgentSignifier]], _waiterRecord_.[[PromiseCapability]], _waiterRecord_.[[Result]]).</li></ol></li><li>Return ~unused~.</li></ol></emu-alg>\n        <emu-note>\n          <p>An agent must not access another agent's promise capability in any capacity beyond passing it to the host.</p>\n        </emu-note>\n      ","4.3.13":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _resolveJob_ be a new Job Abstract Closure with no parameters that captures _agentSignifier_, _promiseCapability_, and _resolution_ and performs the following steps when called:<ol><li>Assert: AgentSignifier() is _agentSignifier_.</li><li>Perform ! Call(_promiseCapability_.[[Resolve]], *undefined*, « _resolution_ »).</li><li>Return ~unused~.</li></ol></li><li>Let _realmInTargetAgent_ be ! GetFunctionRealm(_promiseCapability_.[[Resolve]]).</li><li>Assert: _agentSignifier_ is _realmInTargetAgent_.[[AgentSignifier]].</li><li>Perform HostEnqueueGenericJob(_resolveJob_, _realmInTargetAgent_).</li><li>Return ~unused~.</li></ol></emu-alg>\n      ","4.3.14":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _taRecord_ be ? ValidateIntegerTypedArray(_typedArray_, *true*).</li><li>Let _buffer_ be _taRecord_.[[Object]].[[ViewedArrayBuffer]].</li><li>If IsSharedArrayBuffer(_buffer_) is *false*, throw a *TypeError* exception.</li><li>Let _byteIndexInBuffer_ be ? ValidateAtomicAccess(_taRecord_, _index_).</li><li>Let _arrayTypeName_ be _typedArray_.[[TypedArrayName]].</li><li>If _arrayTypeName_ is *\"BigInt64Array\"*, let _expected_ be ? ToBigInt64(_value_).</li><li>Else, let _expected_ be ? ToInt32(_value_).</li><li>Let _timeoutNumber_ be ? ToNumber(_timeout_).</li><li>If _timeoutNumber_ is either *NaN* or *+∞*<sub>𝔽</sub>, let _realTimeout_ be +∞.</li><li>Else if _timeoutNumber_ is *-∞*<sub>𝔽</sub>, let _realTimeout_ be 0.</li><li>Else, let _realTimeout_ be max(ℝ(_timeoutNumber_), 0).</li><li>If _mode_ is ~sync~ and AgentCanSuspend() is *false*, throw a *TypeError* exception.</li><li>Let _block_ be _buffer_.[[ArrayBufferData]].</li><li>Let _waiterList_ be GetWaiterList(_block_, _byteIndexInBuffer_).</li><li>If _mode_ is ~sync~, then<ol><li>Let _promiseCapability_ be ~blocking~.</li><li>Let _resultObject_ be *undefined*.</li></ol></li><li>Else,<ol><li>Let _promiseCapability_ be ! NewPromiseCapability(%Promise%).</li><li>Let _resultObject_ be OrdinaryObjectCreate(%Object.prototype%).</li></ol></li><li>Perform EnterCriticalSection(_waiterList_).</li><li>Let _elementType_ be TypedArrayElementType(_typedArray_).</li><li>Let _witness_ be GetValueFromBuffer(_buffer_, _byteIndexInBuffer_, _elementType_, *true*, ~seq-cst~).</li><li>If _expected_ ≠ _witness_, then<ol><li>Perform LeaveCriticalSection(_waiterList_).</li><li>If _mode_ is ~sync~, return *\"not-equal\"*.</li><li>Perform ! CreateDataPropertyOrThrow(_resultObject_, *\"async\"*, *false*).</li><li>Perform ! CreateDataPropertyOrThrow(_resultObject_, *\"value\"*, *\"not-equal\"*).</li><li>Return _resultObject_.</li></ol></li><li>If _realTimeout_ = 0 and _mode_ is ~async~, then<ol><li>NOTE: There is no special handling of synchronous immediate timeouts. Asynchronous immediate timeouts have special handling in order to fail fast and avoid unnecessary Promise jobs.</li><li>Perform LeaveCriticalSection(_waiterList_).</li><li>Perform ! CreateDataPropertyOrThrow(_resultObject_, *\"async\"*, *false*).</li><li>Perform ! CreateDataPropertyOrThrow(_resultObject_, *\"value\"*, *\"timed-out\"*).</li><li>Return _resultObject_.</li></ol></li><li>Let _thisAgent_ be AgentSignifier().</li><li>Let _now_ be the time value (UTC) identifying the current time.</li><li>Let _additionalTimeout_ be an implementation-defined non-negative mathematical value.</li><li>Let _timeoutTime_ be ℝ(_now_) + _realTimeout_ + _additionalTimeout_.</li><li>NOTE: When _realTimeout_ is +∞, _timeoutTime_ is also +∞.</li><li>Let _waiterRecord_ be a new Waiter Record { [[AgentSignifier]]: _thisAgent_, [[PromiseCapability]]: _promiseCapability_, [[TimeoutTime]]: _timeoutTime_, [[Result]]: *\"ok\"* }.</li><li>Perform AddWaiter(_waiterList_, _waiterRecord_).</li><li>If _mode_ is ~sync~, then<ol><li>Perform SuspendThisAgent(_waiterList_, _waiterRecord_).</li></ol></li><li>Else if _timeoutTime_ is finite, then<ol><li>Perform EnqueueAtomicsWaitAsyncTimeoutJob(_waiterList_, _waiterRecord_).</li></ol></li><li>Perform LeaveCriticalSection(_waiterList_).</li><li>If _mode_ is ~sync~, return _waiterRecord_.[[Result]].</li><li>Perform ! CreateDataPropertyOrThrow(_resultObject_, *\"async\"*, *true*).</li><li>Perform ! CreateDataPropertyOrThrow(_resultObject_, *\"value\"*, _promiseCapability_.[[Promise]]).</li><li>Return _resultObject_.</li></ol></emu-alg>\n        <emu-note>\n          <p>_additionalTimeout_ allows implementations to pad timeouts as necessary, such as for reducing power consumption or coarsening timer resolution to mitigate timing attacks. This value may differ from call to call of DoWait.</p>\n        </emu-note>\n      ","4.3.15":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _timeoutJob_ be a new Job Abstract Closure with no parameters that captures _waiterList_ and _waiterRecord_ and performs the following steps when called:<ol><li>Perform EnterCriticalSection(_waiterList_).</li><li>If _waiterList_.[[Waiters]] contains _waiterRecord_, then<ol><li>Let _timeOfJobExecution_ be the time value (UTC) identifying the current time.</li><li>Assert: ℝ(_timeOfJobExecution_) ≥ _waiterRecord_.[[TimeoutTime]] (ignoring potential non-monotonicity of time values).</li><li>Set _waiterRecord_.[[Result]] to *\"timed-out\"*.</li><li>Perform RemoveWaiter(_waiterList_, _waiterRecord_).</li><li>Perform NotifyWaiter(_waiterList_, _waiterRecord_).</li></ol></li><li>Perform LeaveCriticalSection(_waiterList_).</li><li>Return ~unused~.</li></ol></li><li>Let _now_ be the time value (UTC) identifying the current time.</li><li>Let _currentRealm_ be the current Realm Record.</li><li>Perform HostEnqueueTimeoutJob(_timeoutJob_, _currentRealm_, 𝔽(_waiterRecord_.[[TimeoutTime]]) - _now_).</li><li>Return ~unused~.</li></ol></emu-alg>\n      ","4.3.16":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _agentRecord_ be the Agent Record of the surrounding agent.</li><li>Let _execution_ be _agentRecord_.[[CandidateExecution]].</li><li>Let _eventsRecord_ be the Agent Events Record of _execution_.[[EventsRecords]] whose [[AgentSignifier]] is AgentSignifier().</li><li>Let _rawBytesRead_ be a List of length _elementSize_ whose elements are nondeterministically chosen byte values.</li><li>NOTE: In implementations, _rawBytesRead_ is the result of a load-link, of a load-exclusive, or of an operand of a read-modify-write instruction on the underlying hardware. The nondeterminism is a semantic prescription of the memory model to describe observable behaviour of hardware with weak consistency.</li><li>NOTE: The comparison of the expected value and the read value is performed outside of the read-modify-write modification function to avoid needlessly strong synchronization when the expected value is not equal to the read value.</li><li>If ByteListEqual(_rawBytesRead_, _expectedBytes_) is *true*, then<ol><li>Let _second_ be a new read-modify-write modification function with parameters (_oldBytes_, _newBytes_) that captures nothing and performs the following steps atomically when called:<ol><li>Return _newBytes_.</li></ol></li><li>Let _event_ be ReadModifyWriteSharedMemory { [[Order]]: ~seq-cst~, [[NoTear]]: *true*, [[Block]]: _block_, [[ByteIndex]]: _byteIndexInBuffer_, [[ElementSize]]: _elementSize_, [[Payload]]: _replacementBytes_, [[ModifyOp]]: _second_ }.</li></ol></li><li>Else,<ol><li>Let _event_ be ReadSharedMemory { [[Order]]: ~seq-cst~, [[NoTear]]: *true*, [[Block]]: _block_, [[ByteIndex]]: _byteIndexInBuffer_, [[ElementSize]]: _elementSize_ }.</li></ol></li><li>Append _event_ to _eventsRecord_.[[EventList]].</li><li>Append Chosen Value Record { [[Event]]: _event_, [[ChosenValue]]: _rawBytesRead_ } to _execution_.[[ChosenValues]].</li><li>Return _rawBytesRead_.</li></ol></emu-alg>\n      ","4.3.17":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>_op_ takes two List of byte values arguments and returns a List of byte values. This operation atomically loads a value, combines it with another value, and stores the combination. It returns the loaded value.</dd>\n        </dl>\n        <emu-alg><ol><li>Let _byteIndexInBuffer_ be ? ValidateAtomicAccessOnIntegerTypedArray(_typedArray_, _index_).</li><li>If _typedArray_.[[ContentType]] is ~bigint~, let _coerced_ be ? ToBigInt(_value_).</li><li>Else, let _coerced_ be 𝔽(? ToIntegerOrInfinity(_value_)).</li><li>Perform ? RevalidateAtomicAccess(_typedArray_, _byteIndexInBuffer_).</li><li>Let _buffer_ be _typedArray_.[[ViewedArrayBuffer]].</li><li>Let _elementType_ be TypedArrayElementType(_typedArray_).</li><li>Return GetModifySetValueInBuffer(_buffer_, _byteIndexInBuffer_, _elementType_, _coerced_, _op_).</li></ol></emu-alg>\n      ","4.3.18":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>The operation atomically performs a bitwise operation on all byte values of the arguments and returns a List of byte values.</dd>\n        </dl>\n        <emu-alg><ol><li>Assert: _xBytes_ and _yBytes_ have the same number of elements.</li><li>Let _result_ be a new empty List.</li><li>Let _i_ be 0.</li><li>For each element _xByte_ of _xBytes_, do<ol><li>Let _yByte_ be _yBytes_[_i_].</li><li>If _op_ is `&amp;`, then<ol><li>Let _resultByte_ be the result of applying the bitwise AND operation to _xByte_ and _yByte_.</li></ol></li><li>Else if _op_ is `^`, then<ol><li>Let _resultByte_ be the result of applying the bitwise exclusive OR (XOR) operation to _xByte_ and _yByte_.</li></ol></li><li>Else,<ol><li>Assert: _op_ is `|`.</li><li>Let _resultByte_ be the result of applying the bitwise inclusive OR operation to _xByte_ and _yByte_.</li></ol></li><li>Set _i_ to _i_ + 1.</li><li>Append _resultByte_ to _result_.</li></ol></li><li>Return _result_.</li></ol></emu-alg>\n      ","4.3.19":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If _xBytes_ and _yBytes_ do not have the same number of elements, return *false*.</li><li>Let _i_ be 0.</li><li>For each element _xByte_ of _xBytes_, do<ol><li>Let _yByte_ be _yBytes_[_i_].</li><li>If _xByte_ ≠ _yByte_, return *false*.</li><li>Set _i_ to _i_ + 1.</li></ol></li><li>Return *true*.</li></ol></emu-alg>\n      ","4.4":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _add_ be a new read-modify-write modification function with parameters (_xBytes_, _yBytes_) that captures _typedArray_ and performs the following steps atomically when called:<ol><li>Let _type_ be TypedArrayElementType(_typedArray_).</li><li>Let _agentRecord_ be the Agent Record of the surrounding agent.</li><li>Let _isLittleEndian_ be _agentRecord_.[[LittleEndian]].</li><li>Let _x_ be RawBytesToNumeric(_type_, _xBytes_, _isLittleEndian_).</li><li>Let _y_ be RawBytesToNumeric(_type_, _yBytes_, _isLittleEndian_).</li><li>If _x_ is a Number, then<ol><li>Let _sum_ be Number::add(_x_, _y_).</li></ol></li><li>Else,<ol><li>Assert: _x_ is a BigInt.</li><li>Let _sum_ be BigInt::add(_x_, _y_).</li></ol></li><li>Let _sumBytes_ be NumericToRawBytes(_type_, _sum_, _isLittleEndian_).</li><li>Assert: _sumBytes_, _xBytes_, and _yBytes_ have the same number of elements.</li><li>Return _sumBytes_.</li></ol></li><li>Return ? AtomicReadModifyWrite(_typedArray_, _index_, _value_, _add_).</li></ol></emu-alg>\n    ","4.5":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _and_ be a new read-modify-write modification function with parameters (_xBytes_, _yBytes_) that captures nothing and performs the following steps atomically when called:<ol><li>Return ByteListBitwiseOp(`&amp;`, _xBytes_, _yBytes_).</li></ol></li><li>Return ? AtomicReadModifyWrite(_typedArray_, _index_, _value_, _and_).</li></ol></emu-alg>\n    ","4.6":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _byteIndexInBuffer_ be ? ValidateAtomicAccessOnIntegerTypedArray(_typedArray_, _index_).</li><li>Let _buffer_ be _typedArray_.[[ViewedArrayBuffer]].</li><li>Let _block_ be _buffer_.[[ArrayBufferData]].</li><li>If _typedArray_.[[ContentType]] is ~bigint~, then<ol><li>Let _expected_ be ? ToBigInt(_expectedValue_).</li><li>Let _replacement_ be ? ToBigInt(_replacementValue_).</li></ol></li><li>Else,<ol><li>Let _expected_ be 𝔽(? ToIntegerOrInfinity(_expectedValue_)).</li><li>Let _replacement_ be 𝔽(? ToIntegerOrInfinity(_replacementValue_)).</li></ol></li><li>Perform ? RevalidateAtomicAccess(_typedArray_, _byteIndexInBuffer_).</li><li>Let _elementType_ be TypedArrayElementType(_typedArray_).</li><li>Let _elementSize_ be TypedArrayElementSize(_typedArray_).</li><li>Let _agentRecord_ be the Agent Record of the surrounding agent.</li><li>Let _isLittleEndian_ be _agentRecord_.[[LittleEndian]].</li><li>Let _expectedBytes_ be NumericToRawBytes(_elementType_, _expected_, _isLittleEndian_).</li><li>Let _replacementBytes_ be NumericToRawBytes(_elementType_, _replacement_, _isLittleEndian_).</li><li>If IsSharedArrayBuffer(_buffer_) is *true*, then<ol><li>Let _rawBytesRead_ be AtomicCompareExchangeInSharedBlock(_block_, _byteIndexInBuffer_, _elementSize_, _expectedBytes_, _replacementBytes_).</li></ol></li><li>Else,<ol><li>Let _rawBytesRead_ be a List of length _elementSize_ whose elements are the sequence of _elementSize_ bytes starting with _block_[_byteIndexInBuffer_].</li><li>If ByteListEqual(_rawBytesRead_, _expectedBytes_) is *true*, then<ol><li>Store the individual bytes of _replacementBytes_ into _block_, starting at _block_[_byteIndexInBuffer_].</li></ol></li></ol></li><li>Return RawBytesToNumeric(_elementType_, _rawBytesRead_, _isLittleEndian_).</li></ol></emu-alg>\n    ","4.7":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _second_ be a new read-modify-write modification function with parameters (_oldBytes_, _newBytes_) that captures nothing and performs the following steps atomically when called:<ol><li>Return _newBytes_.</li></ol></li><li>Return ? AtomicReadModifyWrite(_typedArray_, _index_, _value_, _second_).</li></ol></emu-alg>\n    ","4.8":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _n_ be ? ToIntegerOrInfinity(_size_).</li><li>Let _agentRecord_ be the Agent Record of the surrounding agent.</li><li>If _n_ = 1, return _agentRecord_.[[IsLockFree1]].</li><li>If _n_ = 2, return _agentRecord_.[[IsLockFree2]].</li><li>If _n_ = 4, return *true*.</li><li>If _n_ = 8, return _agentRecord_.[[IsLockFree8]].</li><li>Return *false*.</li></ol></emu-alg>\n      <emu-note>\n        <p>This function is an optimization primitive. The intuition is that if the atomic step of an atomic primitive (`compareExchange`, `load`, `store`, `add`, `sub`, `and`, `or`, `xor`, or `exchange`) on a datum of size _n_ bytes will be performed without the surrounding agent acquiring a lock outside the _n_ bytes comprising the datum, then `Atomics.isLockFree`(_n_) will return *true*. High-performance algorithms will use this function to determine whether to use locks or atomic operations in critical sections. If an atomic primitive is not lock-free then it is often more efficient for an algorithm to provide its own locking.</p>\n        <p>`Atomics.isLockFree`(4) always returns *true* as that can be supported on all known relevant hardware. Being able to assume this will generally simplify programs.</p>\n        <p>Regardless of the value returned by this function, all atomic operations are guaranteed to be atomic. For example, they will never have a visible operation take place in the middle of the operation (e.g., \"tearing\").</p>\n      </emu-note>\n    ","4.9":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _byteIndexInBuffer_ be ? ValidateAtomicAccessOnIntegerTypedArray(_typedArray_, _index_).</li><li>Perform ? RevalidateAtomicAccess(_typedArray_, _byteIndexInBuffer_).</li><li>Let _buffer_ be _typedArray_.[[ViewedArrayBuffer]].</li><li>Let _elementType_ be TypedArrayElementType(_typedArray_).</li><li>Return GetValueFromBuffer(_buffer_, _byteIndexInBuffer_, _elementType_, *true*, ~seq-cst~).</li></ol></emu-alg>\n    ","4.10":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _or_ be a new read-modify-write modification function with parameters (_xBytes_, _yBytes_) that captures nothing and performs the following steps atomically when called:<ol><li>Return ByteListBitwiseOp(`|`, _xBytes_, _yBytes_).</li></ol></li><li>Return ? AtomicReadModifyWrite(_typedArray_, _index_, _value_, _or_).</li></ol></emu-alg>\n    ","4.11":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _byteIndexInBuffer_ be ? ValidateAtomicAccessOnIntegerTypedArray(_typedArray_, _index_).</li><li>If _typedArray_.[[ContentType]] is ~bigint~, let _coerced_ be ? ToBigInt(_value_).</li><li>Else, let _coerced_ be 𝔽(? ToIntegerOrInfinity(_value_)).</li><li>Perform ? RevalidateAtomicAccess(_typedArray_, _byteIndexInBuffer_).</li><li>Let _buffer_ be _typedArray_.[[ViewedArrayBuffer]].</li><li>Let _elementType_ be TypedArrayElementType(_typedArray_).</li><li>Perform SetValueInBuffer(_buffer_, _byteIndexInBuffer_, _elementType_, _coerced_, *true*, ~seq-cst~).</li><li>Return _coerced_.</li></ol></emu-alg>\n    ","4.12":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _subtract_ be a new read-modify-write modification function with parameters (_xBytes_, _yBytes_) that captures _typedArray_ and performs the following steps atomically when called:<ol><li>Let _type_ be TypedArrayElementType(_typedArray_).</li><li>Let _agentRecord_ be the Agent Record of the surrounding agent.</li><li>Let _isLittleEndian_ be _agentRecord_.[[LittleEndian]].</li><li>Let _x_ be RawBytesToNumeric(_type_, _xBytes_, _isLittleEndian_).</li><li>Let _y_ be RawBytesToNumeric(_type_, _yBytes_, _isLittleEndian_).</li><li>If _x_ is a Number, then<ol><li>Let _difference_ be Number::subtract(_x_, _y_).</li></ol></li><li>Else,<ol><li>Assert: _x_ is a BigInt.</li><li>Let _difference_ be BigInt::subtract(_x_, _y_).</li></ol></li><li>Let _differenceBytes_ be NumericToRawBytes(_type_, _difference_, _isLittleEndian_).</li><li>Assert: _differenceBytes_, _xBytes_, and _yBytes_ have the same number of elements.</li><li>Return _differenceBytes_.</li></ol></li><li>Return ? AtomicReadModifyWrite(_typedArray_, _index_, _value_, _subtract_).</li></ol></emu-alg>\n    ","4.13":"<p>This function puts the surrounding agent in a wait queue and suspends it until notified or until the wait times out, returning a String differentiating those cases.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Return ? DoWait(~sync~, _typedArray_, _index_, _value_, _timeout_).</li></ol></emu-alg>\n    ","4.14":"<p>This function returns a Promise that is resolved when the calling agent is notified or the timeout is reached.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Return ? DoWait(~async~, _typedArray_, _index_, _value_, _timeout_).</li></ol></emu-alg>\n    ","4.15":"<p>This function notifies some agents that are sleeping in the wait queue.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _taRecord_ be ? ValidateIntegerTypedArray(_typedArray_, *true*).</li><li>Let _byteIndexInBuffer_ be ? ValidateAtomicAccess(_taRecord_, _index_).</li><li>If _count_ is *undefined*, then<ol><li>Set _count_ to +∞.</li></ol></li><li>Else,<ol><li>Let _intCount_ be ? ToIntegerOrInfinity(_count_).</li><li>Set _count_ to max(_intCount_, 0).</li></ol></li><li>Let _buffer_ be _typedArray_.[[ViewedArrayBuffer]].</li><li>Let _block_ be _buffer_.[[ArrayBufferData]].</li><li>If IsSharedArrayBuffer(_buffer_) is *false*, return *+0*<sub>𝔽</sub>.</li><li>Let _waiterList_ be GetWaiterList(_block_, _byteIndexInBuffer_).</li><li>Perform EnterCriticalSection(_waiterList_).</li><li>Let _waiters_ be RemoveWaiters(_waiterList_, _count_).</li><li>For each element _waiterRecord_ of _waiters_, do<ol><li>Perform NotifyWaiter(_waiterList_, _waiterRecord_).</li></ol></li><li>Perform LeaveCriticalSection(_waiterList_).</li><li>Let _n_ be the number of elements in _waiters_.</li><li>Return 𝔽(_n_).</li></ol></emu-alg>\n    ","4.16":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _xor_ be a new read-modify-write modification function with parameters (_xBytes_, _yBytes_) that captures nothing and performs the following steps atomically when called:<ol><li>Return ByteListBitwiseOp(`^`, _xBytes_, _yBytes_).</li></ol></li><li>Return ? AtomicReadModifyWrite(_typedArray_, _index_, _value_, _xor_).</li></ol></emu-alg>\n    ","4.17":"<p>The initial value of the %Symbol.toStringTag% property is the String value *\"Atomics\"*.</p>\n      <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *true* }.</p>\n    ","5.1":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If _obj_ is an Object and _obj_ has an [[IsRawJSON]] internal slot, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n    ","5.2":"<p>This function parses a JSON text (a JSON-formatted String) and produces an ECMAScript language value. The JSON format represents literals, arrays, and objects with a syntax similar to the syntax for ECMAScript literals, Array Initializers, and Object Initializers. After parsing, JSON objects are realized as ECMAScript objects. JSON arrays are realized as ECMAScript Array instances. JSON strings, numbers, booleans, and null are realized as ECMAScript Strings, Numbers, Booleans, and *null*.</p>\n      <p>The optional _reviver_ parameter is a function that can filter and transform the results. For each value produced by the parse, _reviver_ is called with three arguments (the associated property key, the value, and a context object). If the property is unmodified and its value is primitive, the provided context object has a *\"source\"* property containing source text of the corresponding Parse Node. If the call returns *undefined*, the property is deleted. Otherwise, the property is redefined to use the return value.</p>\n      <emu-alg><ol><li>Let _jsonString_ be ? ToString(_text_).</li><li>Let _parseResult_ be ? ParseJSON(_jsonString_).</li><li>Let _unfiltered_ be _parseResult_.[[Value]].</li><li>If IsCallable(_reviver_) is *false*, return _unfiltered_.</li><li>Let _root_ be OrdinaryObjectCreate(%Object.prototype%).</li><li>Let _rootName_ be the empty String.</li><li>Perform ! CreateDataPropertyOrThrow(_root_, _rootName_, _unfiltered_).</li><li>Let _snapshot_ be <emu-meta suppress-effects=\"user-code\">CreateJSONParseRecord(_parseResult_.[[ParseNode]], _rootName_, _unfiltered_)</emu-meta>.</li><li>Return ? InternalizeJSONProperty(_root_, _rootName_, _reviver_, _snapshot_).</li></ol></emu-alg>\n      <p>The *\"length\"* property of this function is *2*<sub>𝔽</sub>.</p>\n\n      ","5.2.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If StringToCodePoints(_text_) is not a valid JSON text as specified in ECMA-404, throw a *SyntaxError* exception.</li><li>Let _scriptString_ be the string-concatenation of *\"(\"*, _text_, and *\");\"*.</li><li>Let _script_ be ParseText(_scriptString_, |Script|).</li><li>NOTE: The early error rules defined in 13.2.5.1 have special handling for the above invocation of ParseText.</li><li>Assert: _script_ is a Parse Node.</li><li>Let _result_ be ! <emu-meta suppress-effects=\"user-code\">Evaluation of _script_</emu-meta>.</li><li>NOTE: The PropertyDefinitionEvaluation semantics defined in 13.2.5.6 have special handling for the above evaluation.</li><li>Assert: _result_ is either a String, a Number, a Boolean, an Object that is defined by either an |ArrayLiteral| or an |ObjectLiteral|, or *null*.</li><li>Return the Record { [[ParseNode]]: _script_, [[Value]]: _result_ }.</li></ol></emu-alg>\n        <p>It is not permitted for a conforming implementation of `JSON.parse` to extend the JSON grammars. If an implementation wishes to support a modified or extended JSON interchange format it must do so by defining a different parse function.</p>\n        <emu-note>\n          <p>Valid JSON text is a subset of the ECMAScript |PrimaryExpression| syntax. Step step-json-parse-validate verifies that _jsonString_ conforms to that subset, and step step-json-parse-assert-type asserts that evaluation returns a value of an appropriate type.</p>\n          <p>However, because 13.2.5.6 behaves differently during ParseJSON, the same source text can produce different results when evaluated as a |PrimaryExpression| rather than as JSON. Furthermore, the Early Error for duplicate *\"__proto__\"* properties in object literals, which likewise does not apply during ParseJSON, means that not all texts accepted by ParseJSON are valid as a |PrimaryExpression|, despite matching the grammar.</p>\n        </emu-note>\n        <emu-note>\n          <p>In the case where there are duplicate name Strings within an object, lexically preceding values for the same key shall be overwritten.</p>\n        </emu-note>\n      ","5.2.2":"<p>A <dfn variants=\"JSON Parse Records\">JSON Parse Record</dfn> is a Record value used to describe the initial state of a value parsed from JSON text.</p>\n        <p>JSON Parse Records have the fields listed in table-json-parse-record.</p>\n        <emu-table id=\"table-json-parse-record\" caption=\"JSON Parse Record Fields\">\n          <table>\n            <tr>\n              <th>Field Name</th>\n              <th>Value</th>\n              <th>Meaning</th>\n            </tr>\n            <tr>\n              <td>[[ParseNode]]</td>\n              <td>a Parse Node</td>\n              <td>The context Parse Node.</td>\n            </tr>\n            <tr>\n              <td>[[Key]]</td>\n              <td>a property name</td>\n              <td>The property name with which [[Value]] is associated.</td>\n            </tr>\n            <tr>\n              <td>[[Value]]</td>\n              <td>an ECMAScript language value</td>\n              <td>The value produced by evaluation of [[ParseNode]].</td>\n            </tr>\n            <tr>\n              <td>[[Elements]]</td>\n              <td>a List of JSON Parse Records</td>\n              <td>If [[Value]] is an Array, this contains the JSON Parse Records corresponding with the elements of [[Value]]. Otherwise, this is an empty List.</td>\n            </tr>\n            <tr>\n              <td>[[Entries]]</td>\n              <td>a List of JSON Parse Records</td>\n              <td>If [[Value]] is a non-Array Object, this contains the JSON Parse Records corresponding with the entries of [[Value]]. Otherwise, this is an empty List.</td>\n            </tr>\n          </table>\n        </emu-table>\n      ","5.2.3":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It recursively combines a _parseNode_ parsed from JSON text and the _val_ produced by its evaluation.</dd>\n        </dl>\n        <emu-alg><ol><li>Let _typedValNode_ be ShallowestContainedJSONValue(_parseNode_).</li><li>Assert: _typedValNode_ is not ~empty~.</li><li>Let _elements_ be a new empty List.</li><li>Let _entries_ be a new empty List.</li><li>If _val_ is an Object, then<ol><li>Let _isArray_ be ! <emu-meta suppress-effects=\"user-code\">IsArray(_val_)</emu-meta>.</li><li>If _isArray_ is *true*, then<ol><li>Assert: _typedValNode_ is an |ArrayLiteral| Parse Node.</li><li>Let _contentNodes_ be the JSONArrayLiteralContentNodes of _typedValNode_.</li><li>Let _len_ be the number of elements in _contentNodes_.</li><li>Let _valLen_ be ! <emu-meta suppress-effects=\"user-code\">LengthOfArrayLike(_val_)</emu-meta>.</li><li>Assert: _valLen_ is _len_.</li><li>Let _index_ be 0.</li><li>Repeat, while _index_ &lt; _len_,<ol><li>Let _propName_ be ! ToString(𝔽(_index_)).</li><li>Let _elementParseRecord_ be CreateJSONParseRecord(_contentNodes_[_index_], _propName_, ! <emu-meta suppress-effects=\"user-code\">Get(_val_, _propName_)</emu-meta>).</li><li>Append _elementParseRecord_ to _elements_.</li><li>Set _index_ to _index_ + 1.</li></ol></li></ol></li><li>Else,<ol><li>Assert: _typedValNode_ is an |ObjectLiteral| Parse Node.</li><li>Let _propertyNodes_ be the PropertyDefinitionNodes of _typedValNode_.</li><li>NOTE: Because _val_ was produced from JSON text and has not been modified, all of its property keys are Strings and will be exhaustively enumerated.</li><li>Let _keys_ be ! <emu-meta suppress-effects=\"user-code\">EnumerableOwnProperties(_val_, ~key~)</emu-meta>.</li><li>For each String _propertyKey_ of _keys_, do<ol><li>NOTE: In the case of JSON text specifying multiple name/value pairs with the same name for a single object (such as <code>{\"a\":\"lost\",\"a\":\"kept\"}</code>), the value for the corresponding property of the resulting ECMAScript object is specified by the last pair with that name.</li><li>Let _propertyDefinition_ be ~empty~.</li><li>For each Parse Node _propertyNode_ of _propertyNodes_, do<ol><li>Let _propName_ be the PropName of _propertyNode_.</li><li>If _propName_ is _propertyKey_, set _propertyDefinition_ to _propertyNode_.</li></ol></li><li>Assert: _propertyDefinition_ is <code class=\"emu-grammar\">PropertyDefinition : PropertyName `:` AssignmentExpression</code>.</li><li>Let _propertyValueNode_ be the |AssignmentExpression| of _propertyDefinition_.</li><li>Let _entryParseRecord_ be CreateJSONParseRecord(_propertyValueNode_, _propertyKey_, ! <emu-meta suppress-effects=\"user-code\">Get(_val_, _propertyKey_)</emu-meta>).</li><li>Append _entryParseRecord_ to _entries_.</li></ol></li></ol></li></ol></li><li>Else,<ol><li>Assert: _typedValNode_ is neither an |ArrayLiteral| Parse Node nor an |ObjectLiteral| Parse Node.</li></ol></li><li>Return the JSON Parse Record { [[ParseNode]]: _typedValNode_, [[Key]]: _key_, [[Value]]: _val_, [[Elements]]: _elements_, [[Entries]]: _entries_ }.</li></ol></emu-alg>\n      ","5.2.4":"<dl class=\"header\">\n        </dl>\n        <emu-note>\n          <p>This algorithm intentionally does not throw an exception if either [[Delete]] or CreateDataProperty return *false*.</p>\n        </emu-note>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _val_ be ? Get(_holder_, _name_).</li><li>Let _context_ be OrdinaryObjectCreate(%Object.prototype%).</li><li>If _parseRecord_ is a JSON Parse Record and SameValue(_parseRecord_.[[Value]], _val_) is *true*, then<ol><li>If _val_ is not an Object, then<ol><li>Let _parseNode_ be _parseRecord_.[[ParseNode]].</li><li>Assert: _parseNode_ is neither an |ArrayLiteral| Parse Node nor an |ObjectLiteral| Parse Node.</li><li>Let _sourceText_ be the source text matched by _parseNode_.</li><li>Perform ! CreateDataPropertyOrThrow(_context_, *\"source\"*, CodePointsToString(_sourceText_)).</li></ol></li><li>Let _elementRecords_ be _parseRecord_.[[Elements]].</li><li>Let _entryRecords_ be _parseRecord_.[[Entries]].</li></ol></li><li>Else,<ol><li>Let _elementRecords_ be a new empty List.</li><li>Let _entryRecords_ be a new empty List.</li></ol></li><li>If _val_ is an Object, then<ol><li>Let _isArray_ be ? IsArray(_val_).</li><li>If _isArray_ is *true*, then<ol><li>Let _elementRecordsLen_ be the number of elements in _elementRecords_.</li><li>Let _len_ be ? LengthOfArrayLike(_val_).</li><li>Let _index_ be 0.</li><li>Repeat, while _index_ &lt; _len_,<ol><li>Let _propertyKey_ be ! ToString(𝔽(_index_)).</li><li>If _index_ &lt; _elementRecordsLen_, let _elementRecord_ be _elementRecords_[_index_]; else let _elementRecord_ be ~empty~.</li><li>Let _newElement_ be ? InternalizeJSONProperty(_val_, _propertyKey_, _reviver_, _elementRecord_).</li><li>If _newElement_ is *undefined*, then<ol><li>Perform ? <emu-meta effects=\"user-code\">_val_.[[Delete]](_propertyKey_)</emu-meta>.</li></ol></li><li>Else,<ol><li>Perform ? CreateDataProperty(_val_, _propertyKey_, _newElement_).</li></ol></li><li>Set _index_ to _index_ + 1.</li></ol></li></ol></li><li>Else,<ol><li>Let _keys_ be ? EnumerableOwnProperties(_val_, ~key~).</li><li>For each String _propertyKey_ of _keys_, do<ol><li>If there exists an element _entry_ of _entryRecords_ such that _entry_.[[Key]] is _propertyKey_, let _entryRecord_ be _entry_; else let _entryRecord_ be ~empty~.</li><li>Let _newElement_ be ? InternalizeJSONProperty(_val_, _propertyKey_, _reviver_, _entryRecord_).</li><li>If _newElement_ is *undefined*, then<ol><li>Perform ? <emu-meta effects=\"user-code\">_val_.[[Delete]](_propertyKey_)</emu-meta>.</li></ol></li><li>Else,<ol><li>Perform ? CreateDataProperty(_val_, _propertyKey_, _newElement_).</li></ol></li></ol></li></ol></li></ol></li><li>Return ? Call(_reviver_, _holder_, « _name_, _val_, _context_ »).</li></ol></emu-alg>\n      ","5.2.5":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It performs a breadth-first search of the parse tree rooted at _root_, and returns the first node that is an instance of a nonterminal corresponding to a JSON value, or ~empty~ if there is no such node.</dd>\n        </dl>\n        <emu-alg><ol><li>Let _activeFunc_ be the active function object.</li><li>Assert: _activeFunc_ is a `JSON.parse` built-in function object (see <emu-xref href=\"#sec-json.parse\">JSON.parse</emu-xref>).</li><li>Let _types_ be « |NullLiteral|, |BooleanLiteral|, |NumericLiteral|, |StringLiteral|, |ArrayLiteral|, |ObjectLiteral|, |UnaryExpression| ».</li><li>Let _unaryExpression_ be ~empty~.</li><li>Let _queue_ be « _root_ ».</li><li>Repeat, while _queue_ is not empty,<ol><li>Let _candidate_ be the first element of _queue_.</li><li>Remove the first element from _queue_.</li><li>Let _queuedChildren_ be *false*.</li><li>For each nonterminal _type_ of _types_, do<ol><li>If _candidate_ is an instance of _type_, then<ol><li>NOTE: In the JSON grammar, a <code>number</code> token may represent a negative value. In ECMAScript, negation is represented as a unary operation in which a |UnaryExpression| parses to a `-` followed by a derived |UnaryExpression|.</li><li>If _type_ is |UnaryExpression|, then<ol><li>If the parent of _candidate_ is not a |UnaryExpression| Parse Node, set _unaryExpression_ to _candidate_.</li></ol></li><li>Else if _type_ is |NumericLiteral|, then<ol><li>Assert: _candidate_ is contained within _unaryExpression_.</li><li>Return _unaryExpression_.</li></ol></li><li>Else,<ol><li>Return _candidate_.</li></ol></li></ol></li><li>If _queuedChildren_ is *false*, _candidate_ is an instance of a nonterminal, and _candidate_ Contains _type_ is *true*, then<ol><li>Let _children_ be a List containing each child node of _candidate_, in order.</li><li>Set _queue_ to the list-concatenation of _queue_ and _children_.</li><li>Set _queuedChildren_ to *true*.</li></ol></li></ol></li></ol></li><li>Return ~empty~.</li></ol></emu-alg>\n      ","5.2.6":"<dl class=\"header\">\n        </dl>\n        <pre class=\"emu-grammar\">ArrayLiteral :\n  `[` Elision? `]`\n  `[` ElementList `]`\n  `[` ElementList `,` Elision? `]`</pre>\n        <emu-alg><ol><li>Assert: |Elision| is not present.</li><li>If |ElementList| is not present, return a new empty List.</li><li>Return the JSONArrayLiteralContentNodes of |ElementList|.</li></ol></emu-alg>\n\n        <pre class=\"emu-grammar\">ElementList : Elision? AssignmentExpression</pre>\n        <emu-alg><ol><li>Assert: |Elision| is not present.</li><li>Return « |AssignmentExpression| ».</li></ol></emu-alg>\n\n        <pre class=\"emu-grammar\">ElementList : ElementList `,` Elision? AssignmentExpression</pre>\n        <emu-alg><ol><li>Assert: |Elision| is not present.</li><li>Let _elements_ be the JSONArrayLiteralContentNodes of the derived |ElementList|.</li><li>Return the list-concatenation of _elements_ and « |AssignmentExpression| ».</li></ol></emu-alg>\n\n        <pre class=\"emu-grammar\">ElementList :\n  Elision? SpreadElement\n  ElementList `,` Elision? SpreadElement</pre>\n        <emu-alg><ol><li>NOTE: JSON text as specified in ECMA-404 does not include |SpreadElement|.</li><li>Assert: This step is never reached.</li></ol></emu-alg>\n      ","5.3":"<p>This function returns an object representing raw JSON text of a string, number, boolean, or null value.</p>\n      <emu-alg><ol><li>Let _jsonString_ be ? ToString(_text_).</li><li>If _jsonString_ is the empty String, throw a *SyntaxError* exception.</li><li>If the first code unit of _jsonString_ is not either an ASCII lowercase letter code unit (0x0061 through 0x007A, inclusive), an ASCII digit code unit (0x0030 through 0x0039, inclusive), 0x0022 (QUOTATION MARK), or 0x002D (HYPHEN-MINUS), throw a *SyntaxError* exception.</li><li>If the last code unit of _jsonString_ is not either an ASCII lowercase letter code unit (0x0061 through 0x007A, inclusive), an ASCII digit code unit (0x0030 through 0x0039, inclusive), or 0x0022 (QUOTATION MARK), throw a *SyntaxError* exception.</li><li>Let _parseResult_ be ? ParseJSON(_jsonString_).</li><li>Assert: _parseResult_.[[Value]] is either a String, a Number, a Boolean, or *null*.</li><li>Let _internalSlotsList_ be « [[IsRawJSON]] ».</li><li>Let _obj_ be OrdinaryObjectCreate(*null*, _internalSlotsList_).</li><li>Perform ! CreateDataPropertyOrThrow(_obj_, *\"rawJSON\"*, _jsonString_).</li><li>Perform ! SetIntegrityLevel(_obj_, ~frozen~).</li><li>Return _obj_.</li></ol></emu-alg>\n    ","5.4":"<p>This function returns a String in UTF-16 encoded JSON format representing an ECMAScript language value, or *undefined*. It can take three parameters. The _value_ parameter is an ECMAScript language value, which is usually an object or array, although it can also be a String, Boolean, Number or *null*. The optional _replacer_ parameter is either a function that alters the way objects and arrays are stringified, or an array of Strings and Numbers that acts as an inclusion list for selecting the object properties that will be stringified. The optional _space_ parameter is a String or Number that allows the result to have white space injected into it to improve human readability.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _stack_ be a new empty List.</li><li>Let _indent_ be the empty String.</li><li>Let _propertyList_ be *undefined*.</li><li>Let _replacerFunction_ be *undefined*.</li><li>If _replacer_ is an Object, then<ol><li>If IsCallable(_replacer_) is *true*, then<ol><li>Set _replacerFunction_ to _replacer_.</li></ol></li><li>Else,<ol><li>Let _isArray_ be ? IsArray(_replacer_).</li><li>If _isArray_ is *true*, then<ol><li>Set _propertyList_ to a new empty List.</li><li>Let _len_ be ? LengthOfArrayLike(_replacer_).</li><li>Let _k_ be 0.</li><li>Repeat, while _k_ &lt; _len_,<ol><li>Let _propertyKey_ be ! ToString(𝔽(_k_)).</li><li>Let _propertyValue_ be ? Get(_replacer_, _propertyKey_).</li><li>Let _item_ be *undefined*.</li><li>If _propertyValue_ is a String, then<ol><li>Set _item_ to _propertyValue_.</li></ol></li><li>Else if _propertyValue_ is a Number, then<ol><li>Set _item_ to ! ToString(_propertyValue_).</li></ol></li><li>Else if _propertyValue_ is an Object, then<ol><li>If _propertyValue_ has a [[StringData]] or [[NumberData]] internal slot, set _item_ to ? ToString(_propertyValue_).</li></ol></li><li>If _item_ is not *undefined* and _propertyList_ does not contain _item_, then<ol><li>Append _item_ to _propertyList_.</li></ol></li><li>Set _k_ to _k_ + 1.</li></ol></li></ol></li></ol></li></ol></li><li>If _space_ is an Object, then<ol><li>If _space_ has a [[NumberData]] internal slot, then<ol><li>Set _space_ to ? ToNumber(_space_).</li></ol></li><li>Else if _space_ has a [[StringData]] internal slot, then<ol><li>Set _space_ to ? ToString(_space_).</li></ol></li></ol></li><li>If _space_ is a Number, then<ol><li>Let _spaceMV_ be ! ToIntegerOrInfinity(_space_).</li><li>Set _spaceMV_ to min(10, _spaceMV_).</li><li>If _spaceMV_ &lt; 1, let _gap_ be the empty String; else let _gap_ be the String value containing _spaceMV_ occurrences of the code unit 0x0020 (SPACE).</li></ol></li><li>Else if _space_ is a String, then<ol><li>If the length of _space_ ≤ 10, let _gap_ be _space_; else let _gap_ be the substring of _space_ from 0 to 10.</li></ol></li><li>Else,<ol><li>Let _gap_ be the empty String.</li></ol></li><li>Let _wrapper_ be OrdinaryObjectCreate(%Object.prototype%).</li><li>Perform ! CreateDataPropertyOrThrow(_wrapper_, the empty String, _value_).</li><li>Let _state_ be the JSON Serialization Record { [[ReplacerFunction]]: _replacerFunction_, [[Stack]]: _stack_, [[Indent]]: _indent_, [[Gap]]: _gap_, [[PropertyList]]: _propertyList_ }.</li><li>Return ? SerializeJSONProperty(_state_, the empty String, _wrapper_).</li></ol></emu-alg>\n      <p>The *\"length\"* property of this function is *3*<sub>𝔽</sub>.</p>\n      <emu-note>\n        <p>JSON structures are allowed to be nested to any depth, but they must be acyclic. If _value_ is or contains a cyclic structure, then this function must throw a *TypeError* exception. This is an example of a value that cannot be stringified:</p>\n        <pre><code class=\"javascript\">\n          a = [];\n          a[0] = a;\n          my_text = JSON.stringify(a); // This must throw a TypeError.\n        </code></pre>\n      </emu-note>\n      <emu-note>\n        <p>Symbolic primitive values are rendered as follows:</p>\n        <ul>\n          <li>\n            The *null* value is rendered in JSON text as the String value *\"null\"*.\n          </li>\n          <li>\n            The *undefined* value is not rendered.\n          </li>\n          <li>\n            The *true* value is rendered in JSON text as the String value *\"true\"*.\n          </li>\n          <li>\n            The *false* value is rendered in JSON text as the String value *\"false\"*.\n          </li>\n        </ul>\n      </emu-note>\n      <emu-note>\n        <p>String values are wrapped in QUOTATION MARK (`\"`) code units. The code units `\"` and `\\\\` are escaped with `\\\\` prefixes. Control characters code units are replaced with escape sequences `\\\\u`HHHH, or with the shorter forms, `\\\\b` (BACKSPACE), `\\\\f` (FORM FEED), `\\\\n` (LINE FEED), `\\\\r` (CARRIAGE RETURN), `\\\\t` (CHARACTER TABULATION).</p>\n      </emu-note>\n      <emu-note>\n        <p>Finite numbers are stringified as if by calling ToString(_number_). *NaN* and *Infinity* regardless of sign are represented as the String value *\"null\"*.</p>\n      </emu-note>\n      <emu-note>\n        <p>Values that do not have a JSON representation (such as *undefined* and functions) do not produce a String. Instead they produce the *undefined* value. In arrays these values are represented as the String value *\"null\"*. In objects an unrepresentable value causes the property to be excluded from stringification.</p>\n      </emu-note>\n      <emu-note>\n        <p>An object is rendered as U+007B (LEFT CURLY BRACKET) followed by zero or more properties, separated with a U+002C (COMMA), closed with a U+007D (RIGHT CURLY BRACKET). A property is a quoted String representing the property name, a U+003A (COLON), and then the stringified property value. An array is rendered as an opening U+005B (LEFT SQUARE BRACKET) followed by zero or more values, separated with a U+002C (COMMA), closed with a U+005D (RIGHT SQUARE BRACKET).</p>\n      </emu-note>\n\n      ","5.4.1":"<p>A <dfn variants=\"JSON Serialization Records\">JSON Serialization Record</dfn> is a Record value used to enable serialization to the JSON format.</p>\n        <p>JSON Serialization Records have the fields listed in table-json-serialization-record.</p>\n        <emu-table id=\"table-json-serialization-record\" caption=\"JSON Serialization Record Fields\">\n          <table>\n            <thead>\n              <tr>\n                <th>Field Name</th>\n                <th>Value</th>\n                <th>Meaning</th>\n              </tr>\n            </thead>\n            <tr>\n              <td>[[ReplacerFunction]]</td>\n              <td>a function object or *undefined*</td>\n              <td>A function that can supply replacement values for object properties (from JSON.stringify's _replacer_ parameter).</td>\n            </tr>\n            <tr>\n              <td>[[PropertyList]]</td>\n              <td>either a List of Strings or *undefined*</td>\n              <td>The names of properties to include when serializing a non-array object (from JSON.stringify's _replacer_ parameter).</td>\n            </tr>\n            <tr>\n              <td>[[Gap]]</td>\n              <td>a String</td>\n              <td>The unit of indentation (from JSON.stringify's _space_ parameter).</td>\n            </tr>\n            <tr>\n              <td>[[Stack]]</td>\n              <td>a List of Objects</td>\n              <td>The set of nested objects that are in the process of being serialized. Used to detect cyclic structures.</td>\n            </tr>\n            <tr>\n              <td>[[Indent]]</td>\n              <td>a String</td>\n              <td>The current indentation.</td>\n            </tr>\n          </table>\n        </emu-table>\n      ","5.4.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _value_ be ? Get(_holder_, _key_).</li><li>If _value_ is an Object or _value_ is a BigInt, then<ol><li>Let _toJSON_ be ? GetV(_value_, *\"toJSON\"*).</li><li>If IsCallable(_toJSON_) is *true*, then<ol><li>Set _value_ to ? Call(_toJSON_, _value_, « _key_ »).</li></ol></li></ol></li><li>If _state_.[[ReplacerFunction]] is not *undefined*, then<ol><li>Set _value_ to ? Call(_state_.[[ReplacerFunction]], _holder_, « _key_, _value_ »).</li></ol></li><li>If _value_ is an Object, then<ol><li>If _value_ has an [[IsRawJSON]] internal slot, then<ol><li>Let _rawJSON_ be ! <emu-meta suppress-effects=\"user-code\">Get(_value_, *\"rawJSON\"*)</emu-meta>.</li><li>Assert: _rawJSON_ is a String.</li><li>Return _rawJSON_.</li></ol></li><li>If _value_ has a [[NumberData]] internal slot, then<ol><li>Set _value_ to ? ToNumber(_value_).</li></ol></li><li>Else if _value_ has a [[StringData]] internal slot, then<ol><li>Set _value_ to ? ToString(_value_).</li></ol></li><li>Else if _value_ has a [[BooleanData]] internal slot, then<ol><li>Set _value_ to _value_.[[BooleanData]].</li></ol></li><li>Else if _value_ has a [[BigIntData]] internal slot, then<ol><li>Set _value_ to _value_.[[BigIntData]].</li></ol></li></ol></li><li>If _value_ is *null*, return *\"null\"*.</li><li>If _value_ is *true*, return *\"true\"*.</li><li>If _value_ is *false*, return *\"false\"*.</li><li>If _value_ is a String, return QuoteJSONString(_value_).</li><li>If _value_ is a Number, then<ol><li>If _value_ is finite, return ! ToString(_value_).</li><li>Return *\"null\"*.</li></ol></li><li>If _value_ is a BigInt, throw a *TypeError* exception.</li><li>If _value_ is an Object and IsCallable(_value_) is *false*, then<ol><li>Let _isArray_ be ? IsArray(_value_).</li><li>If _isArray_ is *true*, then<ol><li>Return ? SerializeJSONArray(_state_, _value_).</li></ol></li><li>Return ? SerializeJSONObject(_state_, _value_).</li></ol></li><li>Return *undefined*.</li></ol></emu-alg>\n      ","5.4.3":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It wraps _value_ in 0x0022 (QUOTATION MARK) code units and escapes certain other code units within it. This operation interprets _value_ as a sequence of UTF-16 encoded code points, as described in 6.1.4.</dd>\n        </dl>\n        <emu-alg><ol><li>Let _product_ be the String value consisting solely of the code unit 0x0022 (QUOTATION MARK).</li><li>For each code point _cp_ of StringToCodePoints(_value_), do<ol><li>If _cp_ is listed in the “Code Point” column of table-json-single-character-escapes, then<ol><li>Set _product_ to the string-concatenation of _product_ and the escape sequence for _cp_ as specified in the “Escape Sequence” column of the corresponding row.</li></ol></li><li>Else if _cp_ has a numeric value less than 0x0020 (SPACE) or _cp_ has the same numeric value as a leading surrogate or trailing surrogate, then<ol><li>Let _unit_ be the code unit whose numeric value is the numeric value of _cp_.</li><li>Set _product_ to the string-concatenation of _product_ and UnicodeEscape(_unit_).</li></ol></li><li>Else,<ol><li>Set _product_ to the string-concatenation of _product_ and UTF16EncodeCodePoint(_cp_).</li></ol></li></ol></li><li>Set _product_ to the string-concatenation of _product_ and the code unit 0x0022 (QUOTATION MARK).</li><li>Return _product_.</li></ol></emu-alg>\n        <emu-table id=\"table-json-single-character-escapes\" caption=\"JSON Single Character Escape Sequences\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Code Point\n                </th>\n                <th>\n                  Unicode Character Name\n                </th>\n                <th>\n                  Escape Sequence\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                U+0008\n              </td>\n              <td>\n                BACKSPACE\n              </td>\n              <td>\n                `\\\\b`\n              </td>\n            </tr>\n            <tr>\n              <td>\n                U+0009\n              </td>\n              <td>\n                CHARACTER TABULATION\n              </td>\n              <td>\n                `\\\\t`\n              </td>\n            </tr>\n            <tr>\n              <td>\n                U+000A\n              </td>\n              <td>\n                LINE FEED (LF)\n              </td>\n              <td>\n                `\\\\n`\n              </td>\n            </tr>\n            <tr>\n              <td>\n                U+000C\n              </td>\n              <td>\n                FORM FEED (FF)\n              </td>\n              <td>\n                `\\\\f`\n              </td>\n            </tr>\n            <tr>\n              <td>\n                U+000D\n              </td>\n              <td>\n                CARRIAGE RETURN (CR)\n              </td>\n              <td>\n                `\\\\r`\n              </td>\n            </tr>\n            <tr>\n              <td>\n                U+0022\n              </td>\n              <td>\n                QUOTATION MARK\n              </td>\n              <td>\n                `\\\\\"`\n              </td>\n            </tr>\n            <tr>\n              <td>\n                U+005C\n              </td>\n              <td>\n                REVERSE SOLIDUS\n              </td>\n              <td>\n                `\\\\\\\\`\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n      ","5.4.4":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It represents _codeUnit_ as a Unicode escape sequence.</dd>\n        </dl>\n        <emu-alg><ol><li>Let _n_ be the numeric value of _codeUnit_.</li><li>Assert: _n_ ≤ 0xFFFF.</li><li>Let _hex_ be the String representation of _n_, formatted as a lowercase hexadecimal number.</li><li>Return the string-concatenation of the code unit 0x005C (REVERSE SOLIDUS), *\"u\"*, and StringPad(_hex_, 4, *\"0\"*, ~start~).</li></ol></emu-alg>\n      ","5.4.5":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It serializes an object.</dd>\n        </dl>\n        <emu-alg><ol><li>If _state_.[[Stack]] contains _value_, throw a *TypeError* exception because the structure is cyclical.</li><li>Append _value_ to _state_.[[Stack]].</li><li>Let _stepBack_ be _state_.[[Indent]].</li><li>Set _state_.[[Indent]] to the string-concatenation of _state_.[[Indent]] and _state_.[[Gap]].</li><li>If _state_.[[PropertyList]] is not *undefined*, then<ol><li>Let _keys_ be _state_.[[PropertyList]].</li></ol></li><li>Else,<ol><li>Let _keys_ be ? EnumerableOwnProperties(_value_, ~key~).</li></ol></li><li>Let _partial_ be a new empty List.</li><li>For each element _propertyKey_ of _keys_, do<ol><li>Let _strP_ be ? SerializeJSONProperty(_state_, _propertyKey_, _value_).</li><li>If _strP_ is not *undefined*, then<ol><li>Let _member_ be QuoteJSONString(_propertyKey_).</li><li>Set _member_ to the string-concatenation of _member_ and *\":\"*.</li><li>If _state_.[[Gap]] is not the empty String, then<ol><li>Set _member_ to the string-concatenation of _member_ and the code unit 0x0020 (SPACE).</li></ol></li><li>Set _member_ to the string-concatenation of _member_ and _strP_.</li><li>Append _member_ to _partial_.</li></ol></li></ol></li><li>If _partial_ is empty, then<ol><li>Let _final_ be *\"{}\"*.</li></ol></li><li>Else,<ol><li>If _state_.[[Gap]] is the empty String, then<ol><li>Let _properties_ be the String value formed by concatenating all the element Strings of _partial_ with each adjacent pair of Strings separated with the code unit 0x002C (COMMA). A comma is not inserted either before the first String or after the last String.</li><li>Let _final_ be the string-concatenation of *\"{\"*, _properties_, and *\"}\"*.</li></ol></li><li>Else,<ol><li>Let _separator_ be the string-concatenation of the code unit 0x002C (COMMA), the code unit 0x000A (LINE FEED), and _state_.[[Indent]].</li><li>Let _properties_ be the String value formed by concatenating all the element Strings of _partial_ with each adjacent pair of Strings separated with _separator_. The _separator_ String is not inserted either before the first String or after the last String.</li><li>Let _final_ be the string-concatenation of *\"{\"*, the code unit 0x000A (LINE FEED), _state_.[[Indent]], _properties_, the code unit 0x000A (LINE FEED), _stepBack_, and *\"}\"*.</li></ol></li></ol></li><li>Remove the last element of _state_.[[Stack]].</li><li>Set _state_.[[Indent]] to _stepBack_.</li><li>Return _final_.</li></ol></emu-alg>\n      ","5.4.6":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It serializes an array.</dd>\n        </dl>\n        <emu-alg><ol><li>If _state_.[[Stack]] contains _value_, throw a *TypeError* exception because the structure is cyclical.</li><li>Append _value_ to _state_.[[Stack]].</li><li>Let _stepBack_ be _state_.[[Indent]].</li><li>Set _state_.[[Indent]] to the string-concatenation of _state_.[[Indent]] and _state_.[[Gap]].</li><li>Let _partial_ be a new empty List.</li><li>Let _len_ be ? LengthOfArrayLike(_value_).</li><li>Let _index_ be 0.</li><li>Repeat, while _index_ &lt; _len_,<ol><li>Let _strP_ be ? SerializeJSONProperty(_state_, ! ToString(𝔽(_index_)), _value_).</li><li>If _strP_ is *undefined*, then<ol><li>Append *\"null\"* to _partial_.</li></ol></li><li>Else,<ol><li>Append _strP_ to _partial_.</li></ol></li><li>Set _index_ to _index_ + 1.</li></ol></li><li>If _partial_ is empty, then<ol><li>Let _final_ be *\"[]\"*.</li></ol></li><li>Else,<ol><li>If _state_.[[Gap]] is the empty String, then<ol><li>Let _properties_ be the String value formed by concatenating all the element Strings of _partial_ with each adjacent pair of Strings separated with the code unit 0x002C (COMMA). A comma is not inserted either before the first String or after the last String.</li><li>Let _final_ be the string-concatenation of *\"[\"*, _properties_, and *\"]\"*.</li></ol></li><li>Else,<ol><li>Let _separator_ be the string-concatenation of the code unit 0x002C (COMMA), the code unit 0x000A (LINE FEED), and _state_.[[Indent]].</li><li>Let _properties_ be the String value formed by concatenating all the element Strings of _partial_ with each adjacent pair of Strings separated with _separator_. The _separator_ String is not inserted either before the first String or after the last String.</li><li>Let _final_ be the string-concatenation of *\"[\"*, the code unit 0x000A (LINE FEED), _state_.[[Indent]], _properties_, the code unit 0x000A (LINE FEED), _stepBack_, and *\"]\"*.</li></ol></li></ol></li><li>Remove the last element of _state_.[[Stack]].</li><li>Set _state_.[[Indent]] to _stepBack_.</li><li>Return _final_.</li></ol></emu-alg>\n        <emu-note>\n          <p>The representation of arrays includes only the elements in the interval from *+0*<sub>𝔽</sub> (inclusive) to `array.length` (exclusive). Properties whose keys are not array indices are excluded from the stringification. An array is stringified as an opening LEFT SQUARE BRACKET, elements separated by COMMA, and a closing RIGHT SQUARE BRACKET.</p>\n        </emu-note>\n      ","5.5":"<p>The initial value of the %Symbol.toStringTag% property is the String value *\"JSON\"*.</p>\n      <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *true* }.</p>\n    "};
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
