// Generated from ecma262/spec.html — do not edit by hand.
const _sections = {"1":"","2":"","3":"","4":"<p>The Atomics object:</p>\n    <ul>\n      <li>is <dfn><code class=\"emu-intrinsic\">%Atomics%</code></dfn>.</li>\n      <li>is the initial value of the <b>\"Atomics\"</b> property of the global object.</li>\n      <li>is an ordinary object.</li>\n      <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n      <li>does not have a [[Construct]] internal method; it cannot be used as a constructor with the <code>new</code> operator.</li>\n      <li>does not have a [[Call]] internal method; it cannot be invoked as a function.</li>\n    </ul>\n    <p>The Atomics object provides functions that operate indivisibly (atomically) on shared memory array cells as well as functions that let agents wait for and dispatch primitive events. When used with discipline, the Atomics functions allow multi-agent programs that communicate through shared memory to execute in a well-understood order even on parallel CPUs. The rules that govern shared-memory communication are provided by the memory model, defined below.</p>\n    <emu-note>\n      <p>For informative guidelines for programming and implementing shared memory in ECMAScript, please see the notes at the end of the memory model section.</p>\n    </emu-note>\n\n    ","5":"<p>The JSON object:</p>\n    <ul>\n      <li>is <dfn><code class=\"emu-intrinsic\">%JSON%</code></dfn>.</li>\n      <li>is the initial value of the <b>\"JSON\"</b> property of the global object.</li>\n      <li>is an ordinary object.</li>\n      <li>contains two functions, <code>parse</code> and <code>stringify</code>, that are used to parse and construct JSON texts.</li>\n      <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n      <li>does not have a [[Construct]] internal method; it cannot be used as a constructor with the <code>new</code> operator.</li>\n      <li>does not have a [[Call]] internal method; it cannot be invoked as a function.</li>\n    </ul>\n    <p>The JSON Data Interchange Format is defined in ECMA-404. The JSON interchange format used in this specification is exactly that described by ECMA-404. Conforming implementations of <code>JSON.parse</code> and <code>JSON.stringify</code> must support the exact interchange format described in the ECMA-404 specification without any deletions or extensions to the format.</p>\n\n    ","":"","1.1":"<p>The descriptions below in this section, <a href=\"/structured-data#sec-atomics-object\">25.4</a>, and <a href=\"/memory-model#sec-memory-model\">29</a> use the read-modify-write modification function internal data structure.</p>\n      <p>A <dfn variants=\"read-modify-write modification functions\">read-modify-write modification function</dfn> is a mathematical function that is represented as an Abstract Closure that takes two Lists of byte values as arguments and returns a List of byte values. These Abstract Closures satisfy all of the following properties:</p>\n      <ul>\n        <li>They perform all their algorithm steps atomically.</li>\n        <li>Their individual algorithm steps are not observable.</li>\n      </ul>\n      <emu-note>\n        <p>To aid verifying that a read-modify-write modification function's algorithm steps constitute a pure, mathematical function, the following editorial conventions are recommended:</p>\n        <ul>\n          <li>They do not access, directly or transitively via invoked abstract operations and Abstract Closures, any language or specification values except their parameters and captured values.</li>\n          <li>They do not invoke, directly or transitively, abstract operations and Abstract Closures that return Completion Records.</li>\n          <li>They do not return Completion Records.</li>\n        </ul>\n      </emu-note>\n    ","1.2":"<p>A <dfn>fixed-length ArrayBuffer</dfn> is an ArrayBuffer whose byte length cannot change after creation.</p>\n      <p>A <dfn>resizable ArrayBuffer</dfn> is an ArrayBuffer whose byte length may change after creation via calls to <a href=\"/structured-data#sec-arraybuffer.prototype.resize\">25.1.6.6</a>.</p>\n      <p>The kind of ArrayBuffer object that is created depends on the arguments passed to <a href=\"/structured-data#sec-arraybuffer-length\">25.1.4.1</a>.</p>\n    ","1.3":"","1.3.1":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It is used to create an ArrayBuffer.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>slots</var> be « [[ArrayBufferData]], [[ArrayBufferByteLength]], [[ArrayBufferDetachKey]] ».</li><li>If <var>maxByteLength</var> is present and <var>maxByteLength</var> is not <emu-const>empty</emu-const>, let <var>allocatingResizableBuffer</var> be <b>true</b>; else let <var>allocatingResizableBuffer</var> be <b>false</b>.</li><li>If <var>allocatingResizableBuffer</var> is <b>true</b>, then<ol><li>If <var>byteLength</var> > <var>maxByteLength</var>, throw a <b>RangeError</b> exception.</li><li>Append [[ArrayBufferMaxByteLength]] to <var>slots</var>.</li></ol></li><li>Let <var>obj</var> be ? OrdinaryCreateFromConstructor(<var>constructor</var>, <b>\"<code class=\"emu-intrinsic\">%ArrayBuffer.prototype%</code>\"</b>, <var>slots</var>).</li><li>Let <var>block</var> be ? CreateByteDataBlock(<var>byteLength</var>).</li><li>Set <var>obj</var>.[[ArrayBufferData]] to <var>block</var>.</li><li>Set <var>obj</var>.[[ArrayBufferByteLength]] to <var>byteLength</var>.</li><li>If <var>allocatingResizableBuffer</var> is <b>true</b>, then<ol><li>If it is not possible to create a Data Block <var>block</var> consisting of <var>maxByteLength</var> bytes, throw a <b>RangeError</b> exception.</li><li>NOTE: Resizable ArrayBuffers are designed to be implementable with in-place growth. Implementations may throw if, for example, virtual memory cannot be reserved up front.</li><li>Set <var>obj</var>.[[ArrayBufferMaxByteLength]] to <var>maxByteLength</var>.</li></ol></li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","1.3.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If IsGrowableSharedArrayBuffer(<var>arrayBuffer</var>) is <b>true</b>, then<ol><li>Let <var>bufferByteLengthBlock</var> be <var>arrayBuffer</var>.[[ArrayBufferByteLengthData]].</li><li>Let <var>rawLength</var> be GetRawBytesFromSharedBlock(<var>bufferByteLengthBlock</var>, 0, <emu-const>biguint64</emu-const>, <b>true</b>, <var>order</var>).</li><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Let <var>isLittleEndian</var> be <var>agentRecord</var>.[[LittleEndian]].</li><li>Return ℝ(RawBytesToNumeric(<emu-const>biguint64</emu-const>, <var>rawLength</var>, <var>isLittleEndian</var>)).</li></ol></li><li>Assert: IsDetachedBuffer(<var>arrayBuffer</var>) is <b>false</b>.</li><li>Return <var>arrayBuffer</var>.[[ArrayBufferByteLength]].</li></ol></emu-alg>\n      ","1.3.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Perform ? RequireInternalSlot(<var>arrayBuffer</var>, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(<var>arrayBuffer</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>If <var>newLength</var> is <b>undefined</b>, then<ol><li>Let <var>newByteLength</var> be <var>arrayBuffer</var>.[[ArrayBufferByteLength]].</li></ol></li><li>Else,<ol><li>Let <var>newByteLength</var> be ? ToIndex(<var>newLength</var>).</li></ol></li><li>If IsDetachedBuffer(<var>arrayBuffer</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>If <var>preserveResizability</var> is <emu-const>preserve-resizability</emu-const> and IsFixedLengthArrayBuffer(<var>arrayBuffer</var>) is <b>false</b>, then<ol><li>Let <var>newMaxByteLength</var> be <var>arrayBuffer</var>.[[ArrayBufferMaxByteLength]].</li></ol></li><li>Else,<ol><li>Let <var>newMaxByteLength</var> be <emu-const>empty</emu-const>.</li></ol></li><li>If <var>arrayBuffer</var>.[[ArrayBufferDetachKey]] is not <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>newBuffer</var> be ? <emu-meta suppress-effects=\"user-code\">AllocateArrayBuffer(<code class=\"emu-intrinsic\">%ArrayBuffer%</code>, <var>newByteLength</var>, <var>newMaxByteLength</var>)</emu-meta>.</li><li>Let <var>copyLength</var> be min(<var>newByteLength</var>, <var>arrayBuffer</var>.[[ArrayBufferByteLength]]).</li><li>Let <var>fromBlock</var> be <var>arrayBuffer</var>.[[ArrayBufferData]].</li><li>Let <var>toBlock</var> be <var>newBuffer</var>.[[ArrayBufferData]].</li><li>Perform CopyDataBlockBytes(<var>toBlock</var>, 0, <var>fromBlock</var>, 0, <var>copyLength</var>).</li><li>NOTE: Neither creation of the new Data Block nor copying from the old Data Block are observable. Implementations may implement this method as a zero-copy move or a <code>realloc</code>.</li><li>Perform ! DetachArrayBuffer(<var>arrayBuffer</var>).</li><li>Return <var>newBuffer</var>.</li></ol></emu-alg>\n      ","1.3.4":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If <var>arrayBuffer</var>.[[ArrayBufferData]] is <b>null</b>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","1.3.5":"<dl class=\"header\">\n          <dt>skip global checks</dt>\n          <dd>true</dd>\n        </dl>\n        <emu-alg><ol><li>Assert: IsSharedArrayBuffer(<var>arrayBuffer</var>) is <b>false</b>.</li><li>If <var>key</var> is not present, set <var>key</var> to <b>undefined</b>.</li><li>If <var>arrayBuffer</var>.[[ArrayBufferDetachKey]] is not <var>key</var>, throw a <b>TypeError</b> exception.</li><li>Set <var>arrayBuffer</var>.[[ArrayBufferData]] to <b>null</b>.</li><li>Set <var>arrayBuffer</var>.[[ArrayBufferByteLength]] to 0.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n        <emu-note>\n          <p>Detaching an ArrayBuffer instance disassociates the Data Block used as its backing store from the instance and sets the byte length of the buffer to 0.</p>\n        </emu-note>\n      ","1.3.6":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It creates a new ArrayBuffer whose data is a copy of <var>srcBuffer</var>'s data over the range starting at <var>srcByteOffset</var> and continuing for <var>srcLength</var> bytes.</dd>\n        </dl>\n        <emu-alg><ol><li>Assert: IsDetachedBuffer(<var>srcBuffer</var>) is <b>false</b>.</li><li>Let <var>targetBuffer</var> be ? <emu-meta suppress-effects=\"user-code\">AllocateArrayBuffer(<code class=\"emu-intrinsic\">%ArrayBuffer%</code>, <var>srcLength</var>)</emu-meta>.</li><li>Let <var>srcBlock</var> be <var>srcBuffer</var>.[[ArrayBufferData]].</li><li>Let <var>targetBlock</var> be <var>targetBuffer</var>.[[ArrayBufferData]].</li><li>Perform CopyDataBlockBytes(<var>targetBlock</var>, 0, <var>srcBlock</var>, <var>srcByteOffset</var>, <var>srcLength</var>).</li><li>Return <var>targetBuffer</var>.</li></ol></emu-alg>\n      ","1.3.7":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If <var>options</var> is not an Object, return <emu-const>empty</emu-const>.</li><li>Let <var>maxByteLength</var> be ? Get(<var>options</var>, <b>\"maxByteLength\"</b>).</li><li>If <var>maxByteLength</var> is <b>undefined</b>, return <emu-const>empty</emu-const>.</li><li>Return ? ToIndex(<var>maxByteLength</var>).</li></ol></emu-alg>\n      ","1.3.8":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It gives the host an opportunity to perform implementation-defined resizing of <var>buffer</var>. If the host chooses not to handle resizing of <var>buffer</var>, it may return <emu-const>unhandled</emu-const> for the default behaviour.</dd>\n        </dl>\n\n        <p>The implementation of HostResizeArrayBuffer must conform to the following requirements:</p>\n        <ul>\n          <li>The abstract operation does not detach <var>buffer</var>.</li>\n          <li>If the abstract operation completes normally with <emu-const>handled</emu-const>, <var>buffer</var>.[[ArrayBufferByteLength]] is <var>newByteLength</var>.</li>\n        </ul>\n\n        <p>The default implementation of HostResizeArrayBuffer is to return NormalCompletion(<emu-const>unhandled</emu-const>).</p>\n      ","1.3.9":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If <var>arrayBuffer</var> has an [[ArrayBufferMaxByteLength]] internal slot, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n      ","1.3.10":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It verifies if the argument <var>type</var> is an unsigned TypedArray element type.</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>type</var> is one of <emu-const>uint8</emu-const>, <emu-const>uint8clamped</emu-const>, <emu-const>uint16</emu-const>, <emu-const>uint32</emu-const>, or <emu-const>biguint64</emu-const>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","1.3.11":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It verifies if the argument <var>type</var> is an Integer TypedArray element type not including <emu-const>uint8clamped</emu-const>.</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>type</var> is one of <emu-const>int8</emu-const>, <emu-const>uint8</emu-const>, <emu-const>int16</emu-const>, <emu-const>uint16</emu-const>, <emu-const>int32</emu-const>, or <emu-const>uint32</emu-const>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","1.3.12":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It verifies if the argument <var>type</var> is a BigInt TypedArray element type.</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>type</var> is either <emu-const>biguint64</emu-const> or <emu-const>bigint64</emu-const>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","1.3.13":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If IsUnclampedIntegerElementType(<var>type</var>) is <b>true</b>, return <b>true</b>.</li><li>If IsBigIntElementType(<var>type</var>) is <b>true</b> and <var>order</var> is neither <emu-const>init</emu-const> nor <emu-const>unordered</emu-const>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","1.3.14":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>elementSize</var> be the Element Size value specified in table-the-typedarray-constructors for Element Type <var>type</var>.</li><li>If <var>isLittleEndian</var> is <b>false</b>, reverse the order of the elements of <var>rawBytes</var>.</li><li>If <var>type</var> is <emu-const>float16</emu-const>, then<ol><li>Let <var>value</var> be the byte elements of <var>rawBytes</var> concatenated and interpreted as a little-endian bit string encoding of an IEEE 754-2019 binary16 value.</li><li>If <var>value</var> is a NaN, return <b>NaN</b>.</li><li>Return the Number value that corresponds to <var>value</var>.</li></ol></li><li>If <var>type</var> is <emu-const>float32</emu-const>, then<ol><li>Let <var>value</var> be the byte elements of <var>rawBytes</var> concatenated and interpreted as a little-endian bit string encoding of an IEEE 754-2019 binary32 value.</li><li>If <var>value</var> is a NaN, return <b>NaN</b>.</li><li>Return the Number value that corresponds to <var>value</var>.</li></ol></li><li>If <var>type</var> is <emu-const>float64</emu-const>, then<ol><li>Let <var>value</var> be the byte elements of <var>rawBytes</var> concatenated and interpreted as a little-endian bit string encoding of an IEEE 754-2019 binary64 value.</li><li>If <var>value</var> is a NaN, return <b>NaN</b>.</li><li>Return the Number value that corresponds to <var>value</var>.</li></ol></li><li>If IsUnsignedElementType(<var>type</var>) is <b>true</b>, then<ol><li>Let <var>intValue</var> be the byte elements of <var>rawBytes</var> concatenated and interpreted as a bit string encoding of an unsigned little-endian binary number.</li></ol></li><li>Else,<ol><li>Let <var>intValue</var> be the byte elements of <var>rawBytes</var> concatenated and interpreted as a bit string encoding of a binary little-endian two's complement number of bit length <var>elementSize</var> × 8.</li></ol></li><li>If IsBigIntElementType(<var>type</var>) is <b>true</b>, return the BigInt value that corresponds to <var>intValue</var>.</li><li>Return the Number value that corresponds to <var>intValue</var>.</li></ol></emu-alg>\n      ","1.3.15":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>elementSize</var> be the Element Size value specified in table-the-typedarray-constructors for Element Type <var>type</var>.</li><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Let <var>execution</var> be <var>agentRecord</var>.[[CandidateExecution]].</li><li>Let <var>eventsRecord</var> be the Agent Events Record of <var>execution</var>.[[EventsRecords]] whose [[AgentSignifier]] is AgentSignifier().</li><li>If <var>isTypedArray</var> is <b>true</b> and IsNoTearConfiguration(<var>type</var>, <var>order</var>) is <b>true</b>, let <var>noTear</var> be <b>true</b>; else let <var>noTear</var> be <b>false</b>.</li><li>Let <var>rawValue</var> be a List of length <var>elementSize</var> whose elements are nondeterministically chosen byte values.</li><li>NOTE: In implementations, <var>rawValue</var> is the result of a non-atomic or atomic read instruction on the underlying hardware. The nondeterminism is a semantic prescription of the memory model to describe observable behaviour of hardware with weak consistency.</li><li>Let <var>readEvent</var> be ReadSharedMemory { [[Order]]: <var>order</var>, [[NoTear]]: <var>noTear</var>, [[Block]]: <var>block</var>, [[ByteIndex]]: <var>byteIndex</var>, [[ElementSize]]: <var>elementSize</var> }.</li><li>Append <var>readEvent</var> to <var>eventsRecord</var>.[[EventList]].</li><li>Append Chosen Value Record { [[Event]]: <var>readEvent</var>, [[ChosenValue]]: <var>rawValue</var> } to <var>execution</var>.[[ChosenValues]].</li><li>Return <var>rawValue</var>.</li></ol></emu-alg>\n      ","1.3.16":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: IsDetachedBuffer(<var>arrayBuffer</var>) is <b>false</b>.</li><li>Assert: There are sufficient bytes in <var>arrayBuffer</var> starting at <var>byteIndex</var> to represent a value of <var>type</var>.</li><li>Let <var>block</var> be <var>arrayBuffer</var>.[[ArrayBufferData]].</li><li>Let <var>elementSize</var> be the Element Size value specified in table-the-typedarray-constructors for Element Type <var>type</var>.</li><li>If IsSharedArrayBuffer(<var>arrayBuffer</var>) is <b>true</b>, then<ol><li>Assert: <var>block</var> is a Shared Data Block.</li><li>Let <var>rawValue</var> be GetRawBytesFromSharedBlock(<var>block</var>, <var>byteIndex</var>, <var>type</var>, <var>isTypedArray</var>, <var>order</var>).</li></ol></li><li>Else,<ol><li>Let <var>rawValue</var> be a List whose elements are bytes from <var>block</var> at indices in the interval from <var>byteIndex</var> (inclusive) to <var>byteIndex</var> + <var>elementSize</var> (exclusive).</li></ol></li><li>Assert: The number of elements in <var>rawValue</var> is <var>elementSize</var>.</li><li>If <var>isLittleEndian</var> is not present, then<ol><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Set <var>isLittleEndian</var> to <var>agentRecord</var>.[[LittleEndian]].</li></ol></li><li>Return RawBytesToNumeric(<var>type</var>, <var>rawValue</var>, <var>isLittleEndian</var>).</li></ol></emu-alg>\n      ","1.3.17":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If <var>type</var> is <emu-const>float16</emu-const>, then<ol><li>Let <var>rawBytes</var> be a List whose elements are the 2 bytes that are the result of converting <var>value</var> to IEEE 754-2019 binary16 format using roundTiesToEven mode. The bytes are arranged in little endian order. If <var>value</var> is <b>NaN</b>, <var>rawBytes</var> may be set to any implementation chosen IEEE 754-2019 binary16 format NaN encoding. An implementation must always choose the same encoding for each implementation distinguishable <b>NaN</b> value.</li></ol></li><li>Else if <var>type</var> is <emu-const>float32</emu-const>, then<ol><li>Let <var>rawBytes</var> be a List whose elements are the 4 bytes that are the result of converting <var>value</var> to IEEE 754-2019 binary32 format using roundTiesToEven mode. The bytes are arranged in little endian order. If <var>value</var> is <b>NaN</b>, <var>rawBytes</var> may be set to any implementation chosen IEEE 754-2019 binary32 format NaN encoding. An implementation must always choose the same encoding for each implementation distinguishable <b>NaN</b> value.</li></ol></li><li>Else if <var>type</var> is <emu-const>float64</emu-const>, then<ol><li>Let <var>rawBytes</var> be a List whose elements are the 8 bytes that are the IEEE 754-2019 binary64 format encoding of <var>value</var>. The bytes are arranged in little endian order. If <var>value</var> is <b>NaN</b>, <var>rawBytes</var> may be set to any implementation chosen IEEE 754-2019 binary64 format NaN encoding. An implementation must always choose the same encoding for each implementation distinguishable <b>NaN</b> value.</li></ol></li><li>Else,<ol><li>Let <var>n</var> be the Element Size value specified in table-the-typedarray-constructors for Element Type <var>type</var>.</li><li>Let <var>conversionOperation</var> be the abstract operation named in the “Conversion Operation” column of table-the-typedarray-constructors for Element Type <var>type</var>.</li><li>Let <var>intValue</var> be ℝ(! <var>conversionOperation</var>(<var>value</var>)).</li><li>If <var>intValue</var> ≥ 0, then<ol><li>Let <var>rawBytes</var> be a List whose elements are the <var>n</var>-byte binary encoding of <var>intValue</var>. The bytes are ordered in little endian order.</li></ol></li><li>Else,<ol><li>Let <var>rawBytes</var> be a List whose elements are the <var>n</var>-byte binary two's complement encoding of <var>intValue</var>. The bytes are ordered in little endian order.</li></ol></li></ol></li><li>If <var>isLittleEndian</var> is <b>false</b>, reverse the order of the elements of <var>rawBytes</var>.</li><li>Return <var>rawBytes</var>.</li></ol></emu-alg>\n      ","1.3.18":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: IsDetachedBuffer(<var>arrayBuffer</var>) is <b>false</b>.</li><li>Assert: There are sufficient bytes in <var>arrayBuffer</var> starting at <var>byteIndex</var> to represent a value of <var>type</var>.</li><li>Assert: <var>value</var> is a BigInt if IsBigIntElementType(<var>type</var>) is <b>true</b>; else, <var>value</var> is a Number.</li><li>Let <var>block</var> be <var>arrayBuffer</var>.[[ArrayBufferData]].</li><li>Let <var>elementSize</var> be the Element Size value specified in table-the-typedarray-constructors for Element Type <var>type</var>.</li><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>If <var>isLittleEndian</var> is not present, then<ol><li>Set <var>isLittleEndian</var> to <var>agentRecord</var>.[[LittleEndian]].</li></ol></li><li>Let <var>rawBytes</var> be NumericToRawBytes(<var>type</var>, <var>value</var>, <var>isLittleEndian</var>).</li><li>If IsSharedArrayBuffer(<var>arrayBuffer</var>) is <b>true</b>, then<ol><li>Let <var>execution</var> be <var>agentRecord</var>.[[CandidateExecution]].</li><li>Let <var>eventsRecord</var> be the Agent Events Record of <var>execution</var>.[[EventsRecords]] whose [[AgentSignifier]] is AgentSignifier().</li><li>If <var>isTypedArray</var> is <b>true</b> and IsNoTearConfiguration(<var>type</var>, <var>order</var>) is <b>true</b>, let <var>noTear</var> be <b>true</b>; else let <var>noTear</var> be <b>false</b>.</li><li>Append WriteSharedMemory { [[Order]]: <var>order</var>, [[NoTear]]: <var>noTear</var>, [[Block]]: <var>block</var>, [[ByteIndex]]: <var>byteIndex</var>, [[ElementSize]]: <var>elementSize</var>, [[Payload]]: <var>rawBytes</var> } to <var>eventsRecord</var>.[[EventList]].</li></ol></li><li>Else,<ol><li>Store the individual bytes of <var>rawBytes</var> into <var>block</var>, starting at <var>block</var>[<var>byteIndex</var>].</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","1.3.19":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: IsDetachedBuffer(<var>arrayBuffer</var>) is <b>false</b>.</li><li>Assert: There are sufficient bytes in <var>arrayBuffer</var> starting at <var>byteIndex</var> to represent a value of <var>type</var>.</li><li>Assert: <var>value</var> is a BigInt if IsBigIntElementType(<var>type</var>) is <b>true</b>; else, <var>value</var> is a Number.</li><li>Let <var>block</var> be <var>arrayBuffer</var>.[[ArrayBufferData]].</li><li>Let <var>elementSize</var> be the Element Size value specified in table-the-typedarray-constructors for Element Type <var>type</var>.</li><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Let <var>isLittleEndian</var> be <var>agentRecord</var>.[[LittleEndian]].</li><li>Let <var>rawBytes</var> be NumericToRawBytes(<var>type</var>, <var>value</var>, <var>isLittleEndian</var>).</li><li>If IsSharedArrayBuffer(<var>arrayBuffer</var>) is <b>true</b>, then<ol><li>Let <var>execution</var> be <var>agentRecord</var>.[[CandidateExecution]].</li><li>Let <var>eventsRecord</var> be the Agent Events Record of <var>execution</var>.[[EventsRecords]] whose [[AgentSignifier]] is AgentSignifier().</li><li>Let <var>rawBytesRead</var> be a List of length <var>elementSize</var> whose elements are nondeterministically chosen byte values.</li><li>NOTE: In implementations, <var>rawBytesRead</var> is the result of a load-link, of a load-exclusive, or of an operand of a read-modify-write instruction on the underlying hardware. The nondeterminism is a semantic prescription of the memory model to describe observable behaviour of hardware with weak consistency.</li><li>Let <var>rmwEvent</var> be ReadModifyWriteSharedMemory { [[Order]]: <emu-const>seq-cst</emu-const>, [[NoTear]]: <b>true</b>, [[Block]]: <var>block</var>, [[ByteIndex]]: <var>byteIndex</var>, [[ElementSize]]: <var>elementSize</var>, [[Payload]]: <var>rawBytes</var>, [[ModifyOp]]: <var>op</var> }.</li><li>Append <var>rmwEvent</var> to <var>eventsRecord</var>.[[EventList]].</li><li>Append Chosen Value Record { [[Event]]: <var>rmwEvent</var>, [[ChosenValue]]: <var>rawBytesRead</var> } to <var>execution</var>.[[ChosenValues]].</li></ol></li><li>Else,<ol><li>Let <var>rawBytesRead</var> be a List of length <var>elementSize</var> whose elements are the sequence of <var>elementSize</var> bytes starting with <var>block</var>[<var>byteIndex</var>].</li><li>Let <var>rawBytesModified</var> be <var>op</var>(<var>rawBytesRead</var>, <var>rawBytes</var>).</li><li>Store the individual bytes of <var>rawBytesModified</var> into <var>block</var>, starting at <var>block</var>[<var>byteIndex</var>].</li></ol></li><li>Return RawBytesToNumeric(<var>type</var>, <var>rawBytesRead</var>, <var>isLittleEndian</var>).</li></ol></emu-alg>\n      ","1.4":"<p>The ArrayBuffer constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%ArrayBuffer%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"ArrayBuffer\"</b> property of the global object.</li>\n        <li>creates and initializes a new ArrayBuffer when called as a constructor.</li>\n        <li>is not intended to be called as a function and will throw an exception when called in that manner.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified ArrayBuffer behaviour must include a <code>super</code> call to the ArrayBuffer constructor to create and initialize subclass instances with the internal state necessary to support the <code>ArrayBuffer.prototype</code> built-in methods.</li>\n      </ul>\n\n      ","1.4.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>byteLength</var> be ? ToIndex(<var>length</var>).</li><li>Let <var>requestedMaxByteLength</var> be ? GetArrayBufferMaxByteLengthOption(<var>options</var>).</li><li>Return ? AllocateArrayBuffer(NewTarget, <var>byteLength</var>, <var>requestedMaxByteLength</var>).</li></ol></emu-alg>\n      ","1.5":"<p>The ArrayBuffer constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","1.5.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>arg</var> is not an Object, return <b>false</b>.</li><li>If <var>arg</var> has a [[ViewedArrayBuffer]] internal slot, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","1.5.2":"<p>The initial value of <code>ArrayBuffer.prototype</code> is the ArrayBuffer prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.5.3":"<p><code>ArrayBuffer[%Symbol.species%]</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Return the <b>this</b> value.</li></ol></emu-alg>\n        <p>The value of the <b>\"name\"</b> property of this function is <b>\"get [Symbol.species]\"</b>.</p>\n        <emu-note>\n          <p><a href=\"/structured-data#sec-arraybuffer.prototype.slice\">25.1.6.7</a> normally uses its <b>this</b> value's constructor to create a derived object. However, a subclass constructor may over-ride that default behaviour for the <a href=\"/structured-data#sec-arraybuffer.prototype.slice\">25.1.6.7</a> method by redefining its <code class=\"emu-intrinsic\">%Symbol.species%</code> property.</p>\n        </emu-note>\n      ","1.6":"<p>The <dfn>ArrayBuffer prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%ArrayBuffer.prototype%</code></dfn>.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n        <li>is an ordinary object.</li>\n        <li>does not have an [[ArrayBufferData]] or [[ArrayBufferByteLength]] internal slot.</li>\n      </ul>\n\n      ","1.6.1":"<p><code>ArrayBuffer.prototype.byteLength</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(<var>obj</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>If IsDetachedBuffer(<var>obj</var>) is <b>true</b>, return <b>+0</b><sub>𝔽</sub>.</li><li>Let <var>length</var> be <var>obj</var>.[[ArrayBufferByteLength]].</li><li>Return 𝔽(<var>length</var>).</li></ol></emu-alg>\n      ","1.6.2":"<p>The initial value of <code>ArrayBuffer.prototype.constructor</code> is <code class=\"emu-intrinsic\">%ArrayBuffer%</code>.</p>\n      ","1.6.3":"<p><code>ArrayBuffer.prototype.detached</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(<var>obj</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Return IsDetachedBuffer(<var>obj</var>).</li></ol></emu-alg>\n      ","1.6.4":"<p><code>ArrayBuffer.prototype.maxByteLength</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(<var>obj</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>If IsDetachedBuffer(<var>obj</var>) is <b>true</b>, return <b>+0</b><sub>𝔽</sub>.</li><li>If IsFixedLengthArrayBuffer(<var>obj</var>) is <b>true</b>, then<ol><li>Let <var>length</var> be <var>obj</var>.[[ArrayBufferByteLength]].</li></ol></li><li>Else,<ol><li>Let <var>length</var> be <var>obj</var>.[[ArrayBufferMaxByteLength]].</li></ol></li><li>Return 𝔽(<var>length</var>).</li></ol></emu-alg>\n      ","1.6.5":"<p><code>ArrayBuffer.prototype.resizable</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(<var>obj</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>If IsFixedLengthArrayBuffer(<var>obj</var>) is <b>false</b>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","1.6.6":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[ArrayBufferMaxByteLength]]).</li><li>If IsSharedArrayBuffer(<var>obj</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>newByteLength</var> be ? ToIndex(<var>newLength</var>).</li><li>If IsDetachedBuffer(<var>obj</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>If <var>newByteLength</var> > <var>obj</var>.[[ArrayBufferMaxByteLength]], throw a <b>RangeError</b> exception.</li><li>Let <var>hostHandled</var> be ? HostResizeArrayBuffer(<var>obj</var>, <var>newByteLength</var>).</li><li>If <var>hostHandled</var> is <emu-const>handled</emu-const>, return <b>undefined</b>.</li><li>Let <var>oldBlock</var> be <var>obj</var>.[[ArrayBufferData]].</li><li>Let <var>newBlock</var> be ? CreateByteDataBlock(<var>newByteLength</var>).</li><li>Let <var>copyLength</var> be min(<var>newByteLength</var>, <var>obj</var>.[[ArrayBufferByteLength]]).</li><li>Perform CopyDataBlockBytes(<var>newBlock</var>, 0, <var>oldBlock</var>, 0, <var>copyLength</var>).</li><li>NOTE: Neither creation of the new Data Block nor copying from the old Data Block are observable. Implementations may implement this method as in-place growth or shrinkage.</li><li>Set <var>obj</var>.[[ArrayBufferData]] to <var>newBlock</var>.</li><li>Set <var>obj</var>.[[ArrayBufferByteLength]] to <var>newByteLength</var>.</li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n      ","1.6.7":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(<var>obj</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>If IsDetachedBuffer(<var>obj</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>len</var> be <var>obj</var>.[[ArrayBufferByteLength]].</li><li>Let <var>relativeStart</var> be ? ToIntegerOrInfinity(<var>start</var>).</li><li>If <var>relativeStart</var> = -∞, let <var>first</var> be 0.</li><li>Else if <var>relativeStart</var> &lt; 0, let <var>first</var> be max(<var>len</var> + <var>relativeStart</var>, 0).</li><li>Else, let <var>first</var> be min(<var>relativeStart</var>, <var>len</var>).</li><li>If <var>end</var> is <b>undefined</b>, let <var>relativeEnd</var> be <var>len</var>; else let <var>relativeEnd</var> be ? ToIntegerOrInfinity(<var>end</var>).</li><li>If <var>relativeEnd</var> = -∞, let <var>final</var> be 0.</li><li>Else if <var>relativeEnd</var> &lt; 0, let <var>final</var> be max(<var>len</var> + <var>relativeEnd</var>, 0).</li><li>Else, let <var>final</var> be min(<var>relativeEnd</var>, <var>len</var>).</li><li>Let <var>newLen</var> be max(<var>final</var> - <var>first</var>, 0).</li><li>Let <var>ctor</var> be ? SpeciesConstructor(<var>obj</var>, <code class=\"emu-intrinsic\">%ArrayBuffer%</code>).</li><li>Let <var>new</var> be ? Construct(<var>ctor</var>, « 𝔽(<var>newLen</var>) »).</li><li>Perform ? RequireInternalSlot(<var>new</var>, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(<var>new</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>If IsDetachedBuffer(<var>new</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>If SameValue(<var>new</var>, <var>obj</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>If <var>new</var>.[[ArrayBufferByteLength]] &lt; <var>newLen</var>, throw a <b>TypeError</b> exception.</li><li>NOTE: Side-effects of the above steps may have detached or resized <var>obj</var>.</li><li>If IsDetachedBuffer(<var>obj</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>fromBuf</var> be <var>obj</var>.[[ArrayBufferData]].</li><li>Let <var>toBuf</var> be <var>new</var>.[[ArrayBufferData]].</li><li>Let <var>currentLen</var> be <var>obj</var>.[[ArrayBufferByteLength]].</li><li>If <var>first</var> &lt; <var>currentLen</var>, then<ol><li>Let <var>count</var> be min(<var>newLen</var>, <var>currentLen</var> - <var>first</var>).</li><li>Perform CopyDataBlockBytes(<var>toBuf</var>, 0, <var>fromBuf</var>, <var>first</var>, <var>count</var>).</li></ol></li><li>Return <var>new</var>.</li></ol></emu-alg>\n      ","1.6.8":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Return ? ArrayBufferCopyAndDetach(<var>obj</var>, <var>newLength</var>, <emu-const>preserve-resizability</emu-const>).</li></ol></emu-alg>\n      ","1.6.9":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Return ? ArrayBufferCopyAndDetach(<var>obj</var>, <var>newLength</var>, <emu-const>fixed-length</emu-const>).</li></ol></emu-alg>\n      ","1.6.10":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"ArrayBuffer\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","1.7":"<p>ArrayBuffer instances inherit properties from the ArrayBuffer prototype object. ArrayBuffer instances each have an [[ArrayBufferData]] internal slot, an [[ArrayBufferByteLength]] internal slot, and an [[ArrayBufferDetachKey]] internal slot. ArrayBuffer instances which are resizable each have an [[ArrayBufferMaxByteLength]] internal slot.</p>\n      <p>ArrayBuffer instances whose [[ArrayBufferData]] is <b>null</b> are considered to be detached and all operators to access or modify data contained in the ArrayBuffer instance will fail.</p>\n      <p>ArrayBuffer instances whose [[ArrayBufferDetachKey]] is set to a value other than <b>undefined</b> need to have all DetachArrayBuffer calls passing that same \"detach key\" as an argument, otherwise a TypeError will result. This internal slot is only ever set by certain embedding environments, not by algorithms in this specification.</p>\n    ","1.8":"<emu-note>\n        <p>The following are guidelines for ECMAScript programmers working with resizable ArrayBuffer.</p>\n        <p>We recommend that programs be tested in their deployment environments where possible. The amount of available physical memory differs greatly between hardware devices. Similarly, virtual memory subsystems also differ greatly between hardware devices as well as operating systems. An application that runs without out-of-memory errors on a 64-bit desktop web browser could run out of memory on a 32-bit mobile web browser.</p>\n        <p>When choosing a value for the <b>\"maxByteLength\"</b> option for resizable ArrayBuffer, we recommend that the smallest possible size for the application be chosen. We recommend that <b>\"maxByteLength\"</b> does not exceed 1,073,741,824 (2<sup>30</sup> bytes or 1GiB).</p>\n        <p>Please note that successfully constructing a resizable ArrayBuffer for a particular maximum size does not guarantee that future resizes will succeed.</p>\n      </emu-note>\n\n      <emu-note>\n        <p>The following are guidelines for ECMAScript implementers implementing resizable ArrayBuffer.</p>\n        <p>Resizable ArrayBuffer can be implemented as copying upon resize, as in-place growth via reserving virtual memory up front, or as a combination of both for different values of the constructor's <b>\"maxByteLength\"</b> option.</p>\n        <p>If a host is multi-tenanted (i.e. it runs many ECMAScript applications simultaneously), such as a web browser, and its implementations choose to implement in-place growth by reserving virtual memory, we recommend that both 32-bit and 64-bit implementations throw for values of <b>\"maxByteLength\"</b> ≥ 1GiB to 1.5GiB. This is to reduce the likelihood a single application can exhaust the virtual memory address space and to reduce interoperability risk.</p>\n        <p>If a host does not have virtual memory, such as those running on embedded devices without an MMU, or if a host only implements resizing by copying, it may accept any <emu-not-ref>Number value for</emu-not-ref> the <b>\"maxByteLength\"</b> option. However, we recommend a <b>RangeError</b> be thrown if a memory block of the requested size can never be allocated. For example, if the requested size is greater than the maximum amount of usable memory on the device.</p>\n      </emu-note>\n    ","2.1":"<p>A <dfn>fixed-length SharedArrayBuffer</dfn> is a SharedArrayBuffer whose byte length cannot change after creation.</p>\n      <p>A <dfn>growable SharedArrayBuffer</dfn> is a SharedArrayBuffer whose byte length may increase after creation via calls to <a href=\"/structured-data#sec-sharedarraybuffer.prototype.grow\">25.2.5.3</a>.</p>\n      <p>The kind of SharedArrayBuffer object that is created depends on the arguments passed to <a href=\"/structured-data#sec-sharedarraybuffer-length\">25.2.3.1</a>.</p>\n    ","2.2":"","2.2.1":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It is used to create a SharedArrayBuffer.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>slots</var> be « [[ArrayBufferData]] ».</li><li>If <var>maxByteLength</var> is present and <var>maxByteLength</var> is not <emu-const>empty</emu-const>, let <var>allocatingGrowableBuffer</var> be <b>true</b>; else let <var>allocatingGrowableBuffer</var> be <b>false</b>.</li><li>If <var>allocatingGrowableBuffer</var> is <b>true</b>, then<ol><li>If <var>byteLength</var> > <var>maxByteLength</var>, throw a <b>RangeError</b> exception.</li><li>Append [[ArrayBufferByteLengthData]] and [[ArrayBufferMaxByteLength]] to <var>slots</var>.</li></ol></li><li>Else,<ol><li>Append [[ArrayBufferByteLength]] to <var>slots</var>.</li></ol></li><li>Let <var>obj</var> be ? OrdinaryCreateFromConstructor(<var>constructor</var>, <b>\"<code class=\"emu-intrinsic\">%SharedArrayBuffer.prototype%</code>\"</b>, <var>slots</var>).</li><li>If <var>allocatingGrowableBuffer</var> is <b>true</b>, let <var>allocLength</var> be <var>maxByteLength</var>; else let <var>allocLength</var> be <var>byteLength</var>.</li><li>Let <var>block</var> be ? CreateSharedByteDataBlock(<var>allocLength</var>).</li><li>Set <var>obj</var>.[[ArrayBufferData]] to <var>block</var>.</li><li>If <var>allocatingGrowableBuffer</var> is <b>true</b>, then<ol><li>Assert: <var>byteLength</var> ≤ <var>maxByteLength</var>.</li><li>Let <var>byteLengthBlock</var> be ? CreateSharedByteDataBlock(8).</li><li>Perform SetValueInBuffer(<var>byteLengthBlock</var>, 0, <emu-const>biguint64</emu-const>, ℤ(<var>byteLength</var>), <b>true</b>, <emu-const>seq-cst</emu-const>).</li><li>Set <var>obj</var>.[[ArrayBufferByteLengthData]] to <var>byteLengthBlock</var>.</li><li>Set <var>obj</var>.[[ArrayBufferMaxByteLength]] to <var>maxByteLength</var>.</li></ol></li><li>Else,<ol><li>Set <var>obj</var>.[[ArrayBufferByteLength]] to <var>byteLength</var>.</li></ol></li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","2.2.2":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It tests whether an object is a SharedArrayBuffer.</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>obj</var>.[[ArrayBufferData]] is a Shared Data Block, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","2.2.3":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It tests whether an object is a growable SharedArrayBuffer.</dd>\n        </dl>\n        <emu-alg><ol><li>If IsSharedArrayBuffer(<var>obj</var>) is <b>true</b> and <var>obj</var> has an [[ArrayBufferByteLengthData]] internal slot, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","2.2.4":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It gives the host an opportunity to perform implementation-defined growing of <var>buffer</var>. If the host chooses not to handle growing of <var>buffer</var>, it may return <emu-const>unhandled</emu-const> for the default behaviour.</dd>\n        </dl>\n        <p>The implementation of HostGrowSharedArrayBuffer must conform to the following requirements:</p>\n        <ul>\n          <li>If the abstract operation does not complete normally with <emu-const>unhandled</emu-const>, and <var>newByteLength</var> &lt; the current byte length of the <var>buffer</var> or <var>newByteLength</var> > <var>buffer</var>.[[ArrayBufferMaxByteLength]], throw a <b>RangeError</b> exception.</li>\n          <li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent. Let <var>isLittleEndian</var> be <var>agentRecord</var>.[[LittleEndian]]. If the abstract operation completes normally with <emu-const>handled</emu-const>, a WriteSharedMemory or ReadModifyWriteSharedMemory event whose [[Order]] is <emu-const>seq-cst</emu-const>, [[Payload]] is NumericToRawBytes(<emu-const>biguint64</emu-const>, <var>newByteLength</var>, <var>isLittleEndian</var>), [[Block]] is <var>buffer</var>.[[ArrayBufferByteLengthData]], [[ByteIndex]] is 0, and [[ElementSize]] is 8 is added to the surrounding agent's candidate execution such that racing calls to <a href=\"/structured-data#sec-sharedarraybuffer.prototype.grow\">25.2.5.3</a> are not \"lost\", i.e. silently do nothing.</li>\n        </ul>\n\n        <emu-note>\n          <p>The second requirement above is intentionally vague about how or when the current byte length of <var>buffer</var> is read. Because the byte length must be updated via an atomic read-modify-write operation on the underlying hardware, architectures that use load-link/store-conditional or load-exclusive/store-exclusive instruction pairs may wish to keep the paired instructions close in the instruction stream. As such, <a href=\"/structured-data#sec-sharedarraybuffer.prototype.grow\">25.2.5.3</a> itself does not perform bounds checking on <var>newByteLength</var> before calling HostGrowSharedArrayBuffer, nor is there a requirement on when the current byte length is read.</p>\n          <p>This is in contrast with HostResizeArrayBuffer, which is guaranteed that 0 ≤ <var>newByteLength</var> ≤ <var>buffer</var>.[[ArrayBufferMaxByteLength]].</p>\n        </emu-note>\n\n        <p>The default implementation of HostGrowSharedArrayBuffer is to return NormalCompletion(<emu-const>unhandled</emu-const>).</p>\n      ","2.3":"<p>The SharedArrayBuffer constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%SharedArrayBuffer%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"SharedArrayBuffer\"</b> property of the global object, if that property is present (see below).</li>\n        <li>creates and initializes a new SharedArrayBuffer when called as a constructor.</li>\n        <li>is not intended to be called as a function and will throw an exception when called in that manner.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified SharedArrayBuffer behaviour must include a <code>super</code> call to the SharedArrayBuffer constructor to create and initialize subclass instances with the internal state necessary to support the <code>SharedArrayBuffer.prototype</code> built-in methods.</li>\n      </ul>\n\n      <p>Whenever a host does not provide concurrent access to SharedArrayBuffers it may omit the <b>\"SharedArrayBuffer\"</b> property of the global object.</p>\n\n      <emu-note>\n        <p>Unlike an <code>ArrayBuffer</code>, a <code>SharedArrayBuffer</code> cannot become detached, and its internal [[ArrayBufferData]] slot is never <b>null</b>.</p>\n      </emu-note>\n\n      ","2.3.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>byteLength</var> be ? ToIndex(<var>length</var>).</li><li>Let <var>requestedMaxByteLength</var> be ? GetArrayBufferMaxByteLengthOption(<var>options</var>).</li><li>Return ? AllocateSharedArrayBuffer(NewTarget, <var>byteLength</var>, <var>requestedMaxByteLength</var>).</li></ol></emu-alg>\n      ","2.4":"<p>The SharedArrayBuffer constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","2.4.1":"<p>The initial value of <code>SharedArrayBuffer.prototype</code> is the SharedArrayBuffer prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","2.4.2":"<p><code>SharedArrayBuffer[%Symbol.species%]</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Return the <b>this</b> value.</li></ol></emu-alg>\n        <p>The value of the <b>\"name\"</b> property of this function is <b>\"get [Symbol.species]\"</b>.</p>\n      ","2.5":"<p>The <dfn>SharedArrayBuffer prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%SharedArrayBuffer.prototype%</code></dfn>.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n        <li>is an ordinary object.</li>\n        <li>does not have an [[ArrayBufferData]] or [[ArrayBufferByteLength]] internal slot.</li>\n      </ul>\n\n      ","2.5.1":"<p><code>SharedArrayBuffer.prototype.byteLength</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(<var>obj</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>length</var> be ArrayBufferByteLength(<var>obj</var>, <emu-const>seq-cst</emu-const>).</li><li>Return 𝔽(<var>length</var>).</li></ol></emu-alg>\n      ","2.5.2":"<p>The initial value of <code>SharedArrayBuffer.prototype.constructor</code> is <code class=\"emu-intrinsic\">%SharedArrayBuffer%</code>.</p>\n      ","2.5.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[ArrayBufferMaxByteLength]]).</li><li>If IsSharedArrayBuffer(<var>obj</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>newByteLength</var> be ? ToIndex(<var>newLength</var>).</li><li>Let <var>hostHandled</var> be ? HostGrowSharedArrayBuffer(<var>obj</var>, <var>newByteLength</var>).</li><li>If <var>hostHandled</var> is <emu-const>handled</emu-const>, return <b>undefined</b>.</li><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Let <var>isLittleEndian</var> be <var>agentRecord</var>.[[LittleEndian]].</li><li>Let <var>byteLengthBlock</var> be <var>obj</var>.[[ArrayBufferByteLengthData]].</li><li>Let <var>currentByteLengthRawBytes</var> be GetRawBytesFromSharedBlock(<var>byteLengthBlock</var>, 0, <emu-const>biguint64</emu-const>, <b>true</b>, <emu-const>seq-cst</emu-const>).</li><li>Let <var>newByteLengthRawBytes</var> be NumericToRawBytes(<emu-const>biguint64</emu-const>, ℤ(<var>newByteLength</var>), <var>isLittleEndian</var>).</li><li>Repeat,<ol><li>NOTE: This is a compare-and-exchange loop to ensure that parallel, racing grows of the same buffer are totally ordered, are not lost, and do not silently do nothing. The loop exits if it was able to attempt to grow uncontended.</li><li>Let <var>currentByteLength</var> be ℝ(RawBytesToNumeric(<emu-const>biguint64</emu-const>, <var>currentByteLengthRawBytes</var>, <var>isLittleEndian</var>)).</li><li>If <var>newByteLength</var> = <var>currentByteLength</var>, return <b>undefined</b>.</li><li>If <var>newByteLength</var> &lt; <var>currentByteLength</var> or <var>newByteLength</var> > <var>obj</var>.[[ArrayBufferMaxByteLength]], throw a <b>RangeError</b> exception.</li><li>Let <var>byteLengthDelta</var> be <var>newByteLength</var> - <var>currentByteLength</var>.</li><li>If it is impossible to create a new Shared Data Block value consisting of <var>byteLengthDelta</var> bytes, throw a <b>RangeError</b> exception.</li><li>NOTE: No new Shared Data Block is constructed and used here. The observable behaviour of growable SharedArrayBuffers is specified by allocating a max-sized Shared Data Block at construction time, and this step captures the requirement that implementations that run out of memory must throw a <b>RangeError</b>.</li><li>Let <var>readByteLengthRawBytes</var> be AtomicCompareExchangeInSharedBlock(<var>byteLengthBlock</var>, 0, 8, <var>currentByteLengthRawBytes</var>, <var>newByteLengthRawBytes</var>).</li><li>If ByteListEqual(<var>readByteLengthRawBytes</var>, <var>currentByteLengthRawBytes</var>) is <b>true</b>, return <b>undefined</b>.</li><li>Set <var>currentByteLengthRawBytes</var> to <var>readByteLengthRawBytes</var>.</li></ol></li></ol></emu-alg>\n        <emu-note>\n          <p>Spurious failures of the compare-exchange to update the length are prohibited. If the bounds checking for the new length passes and the implementation is not out of memory, a ReadModifyWriteSharedMemory event (i.e. a successful compare-exchange) is always added into the candidate execution.</p>\n          <p>Parallel calls to SharedArrayBuffer.prototype.grow are totally ordered. For example, consider two racing calls: <code>sab.grow(10)</code> and <code>sab.grow(20)</code>. One of the two calls is guaranteed to win the race. The call to <code>sab.grow(10)</code> will never shrink <code>sab</code> even if <code>sab.grow(20)</code> happened first; in that case it will instead throw a RangeError.</p>\n        </emu-note>\n      ","2.5.4":"<p><code>SharedArrayBuffer.prototype.growable</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(<var>obj</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>If IsFixedLengthArrayBuffer(<var>obj</var>) is <b>false</b>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","2.5.5":"<p><code>SharedArrayBuffer.prototype.maxByteLength</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(<var>obj</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>If IsFixedLengthArrayBuffer(<var>obj</var>) is <b>true</b>, then<ol><li>Let <var>length</var> be <var>obj</var>.[[ArrayBufferByteLength]].</li></ol></li><li>Else,<ol><li>Let <var>length</var> be <var>obj</var>.[[ArrayBufferMaxByteLength]].</li></ol></li><li>Return 𝔽(<var>length</var>).</li></ol></emu-alg>\n      ","2.5.6":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(<var>obj</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>len</var> be ArrayBufferByteLength(<var>obj</var>, <emu-const>seq-cst</emu-const>).</li><li>Let <var>relativeStart</var> be ? ToIntegerOrInfinity(<var>start</var>).</li><li>If <var>relativeStart</var> = -∞, let <var>first</var> be 0.</li><li>Else if <var>relativeStart</var> &lt; 0, let <var>first</var> be max(<var>len</var> + <var>relativeStart</var>, 0).</li><li>Else, let <var>first</var> be min(<var>relativeStart</var>, <var>len</var>).</li><li>If <var>end</var> is <b>undefined</b>, let <var>relativeEnd</var> be <var>len</var>; else let <var>relativeEnd</var> be ? ToIntegerOrInfinity(<var>end</var>).</li><li>If <var>relativeEnd</var> = -∞, let <var>final</var> be 0.</li><li>Else if <var>relativeEnd</var> &lt; 0, let <var>final</var> be max(<var>len</var> + <var>relativeEnd</var>, 0).</li><li>Else, let <var>final</var> be min(<var>relativeEnd</var>, <var>len</var>).</li><li>Let <var>newLen</var> be max(<var>final</var> - <var>first</var>, 0).</li><li>Let <var>ctor</var> be ? SpeciesConstructor(<var>obj</var>, <code class=\"emu-intrinsic\">%SharedArrayBuffer%</code>).</li><li>Let <var>new</var> be ? Construct(<var>ctor</var>, « 𝔽(<var>newLen</var>) »).</li><li>Perform ? RequireInternalSlot(<var>new</var>, [[ArrayBufferData]]).</li><li>If IsSharedArrayBuffer(<var>new</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>If <var>new</var>.[[ArrayBufferData]] is <var>obj</var>.[[ArrayBufferData]], throw a <b>TypeError</b> exception.</li><li>If ArrayBufferByteLength(<var>new</var>, <emu-const>seq-cst</emu-const>) &lt; <var>newLen</var>, throw a <b>TypeError</b> exception.</li><li>Let <var>fromBuf</var> be <var>obj</var>.[[ArrayBufferData]].</li><li>Let <var>toBuf</var> be <var>new</var>.[[ArrayBufferData]].</li><li>Perform CopyDataBlockBytes(<var>toBuf</var>, 0, <var>fromBuf</var>, <var>first</var>, <var>newLen</var>).</li><li>Return <var>new</var>.</li></ol></emu-alg>\n      ","2.5.7":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"SharedArrayBuffer\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","2.6":"<p>SharedArrayBuffer instances inherit properties from the SharedArrayBuffer prototype object. SharedArrayBuffer instances each have an [[ArrayBufferData]] internal slot. SharedArrayBuffer instances which are not growable each have an [[ArrayBufferByteLength]] internal slot. SharedArrayBuffer instances which are growable each have an [[ArrayBufferByteLengthData]] internal slot and an [[ArrayBufferMaxByteLength]] internal slot.</p>\n\n      <emu-note>\n        <p>SharedArrayBuffer instances, unlike ArrayBuffer instances, are never detached.</p>\n      </emu-note>\n    ","2.7":"<emu-note>\n        <p>The following are guidelines for ECMAScript programmers working with growable SharedArrayBuffer.</p>\n        <p>We recommend that programs be tested in their deployment environments where possible. The amount of available physical memory differ greatly between hardware devices. Similarly, virtual memory subsystems also differ greatly between hardware devices as well as operating systems. An application that runs without out-of-memory errors on a 64-bit desktop web browser could run out of memory on a 32-bit mobile web browser.</p>\n        <p>When choosing a value for the <b>\"maxByteLength\"</b> option for growable SharedArrayBuffer, we recommend that the smallest possible size for the application be chosen. We recommend that <b>\"maxByteLength\"</b> does not exceed 1073741824, or 1GiB.</p>\n        <p>Please note that successfully constructing a growable SharedArrayBuffer for a particular maximum size does not guarantee that future grows will succeed.</p>\n        <p>Not all loads of a growable SharedArrayBuffer's length are synchronizing <emu-const>seq-cst</emu-const> loads. Loads of the length that are for bounds-checking of an integer-indexed property access, e.g. <code>u8[idx]</code>, are not synchronizing. In general, in the absence of explicit synchronization, one property access being in-bound does not imply a subsequent property access in the same agent is also in-bound. In contrast, explicit loads of the length via the <code>length</code> and <code>byteLength</code> getters on SharedArrayBuffer, <code class=\"emu-intrinsic\">%TypedArray%</code>.prototype, and DataView.prototype are synchronizing. Loads of the length that are performed by built-in methods to check if a TypedArray is entirely out-of-bounds are also synchronizing.</p>\n      </emu-note>\n\n      <emu-note>\n        <p>The following are guidelines for ECMAScript implementers implementing growable SharedArrayBuffer.</p>\n        <p>We recommend growable SharedArrayBuffer be implemented as in-place growth via reserving virtual memory up front.</p>\n        <p>Because grow operations can happen in parallel with memory accesses on a growable SharedArrayBuffer, the constraints of the memory model require that even unordered accesses do not \"tear\" (bits of their values will not be mixed). In practice, this means the underlying data block of a growable SharedArrayBuffer cannot be grown by being copied without stopping the world. We do not recommend stopping the world as an implementation strategy because it introduces a serialization point and is slow.</p>\n        <p>Grown memory must appear zeroed from the moment of its creation, including to any racy accesses in parallel. This can be accomplished via zero-filled-on-demand virtual memory pages, or careful synchronization if manually zeroing memory.</p>\n        <p>Integer-indexed property access on TypedArray views of growable SharedArrayBuffers is intended to be optimizable similarly to access on TypedArray views of non-growable SharedArrayBuffers, because integer-indexed property loads on are not synchronizing on the underlying buffer's length (see programmer guidelines above). For example, bounds checks for property accesses may still be hoisted out of loops.</p>\n        <p>In practice it is difficult to implement growable SharedArrayBuffer by copying on hosts that do not have virtual memory, such as those running on embedded devices without an MMU. Memory usage behaviour of growable SharedArrayBuffers on such hosts may significantly differ from that of hosts with virtual memory. Such hosts should clearly communicate memory usage expectations to users.</p>\n      </emu-note>\n    ","3.1":"","3.1.1":"<p>A <dfn variants=\"DataView With Buffer Witness Records\">DataView With Buffer Witness Record</dfn> is a Record value used to encapsulate a DataView along with a cached byte length of the viewed buffer. It is used to help ensure there is a single ReadSharedMemory event of the byte length data block when the viewed buffer is a growable SharedArrayBuffer.</p>\n        <p>DataView With Buffer Witness Records have the fields listed in table-dataview-with-buffer-witness-record-fields.</p>\n        <emu-table id=\"table-dataview-with-buffer-witness-record-fields\" caption=\"DataView With Buffer Witness Record Fields\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Field Name\n                </th>\n                <th>\n                  Value\n                </th>\n                <th>\n                  Meaning\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                [[Object]]\n              </td>\n              <td>\n                a DataView\n              </td>\n              <td>\n                The DataView object whose buffer's byte length is loaded.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[CachedBufferByteLength]]\n              </td>\n              <td>\n                a non-negative integer or <emu-const>detached</emu-const>\n              </td>\n              <td>\n                The byte length of the object's [[ViewedArrayBuffer]] when the Record was created.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n      ","3.1.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>buffer</var> be <var>obj</var>.[[ViewedArrayBuffer]].</li><li>If IsDetachedBuffer(<var>buffer</var>) is <b>true</b>, then<ol><li>Let <var>byteLength</var> be <emu-const>detached</emu-const>.</li></ol></li><li>Else,<ol><li>Let <var>byteLength</var> be ArrayBufferByteLength(<var>buffer</var>, <var>order</var>).</li></ol></li><li>Return the DataView With Buffer Witness Record { [[Object]]: <var>obj</var>, [[CachedBufferByteLength]]: <var>byteLength</var> }.</li></ol></emu-alg>\n      ","3.1.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: IsViewOutOfBounds(<var>viewRecord</var>) is <b>false</b>.</li><li>Let <var>view</var> be <var>viewRecord</var>.[[Object]].</li><li>If <var>view</var>.[[ByteLength]] is not <emu-const>auto</emu-const>, return <var>view</var>.[[ByteLength]].</li><li>Assert: IsFixedLengthArrayBuffer(<var>view</var>.[[ViewedArrayBuffer]]) is <b>false</b>.</li><li>Let <var>byteOffset</var> be <var>view</var>.[[ByteOffset]].</li><li>Let <var>byteLength</var> be <var>viewRecord</var>.[[CachedBufferByteLength]].</li><li>Assert: <var>byteLength</var> is not <emu-const>detached</emu-const>.</li><li>Return <var>byteLength</var> - <var>byteOffset</var>.</li></ol></emu-alg>\n      ","3.1.4":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>view</var> be <var>viewRecord</var>.[[Object]].</li><li>Let <var>bufferByteLength</var> be <var>viewRecord</var>.[[CachedBufferByteLength]].</li><li>If IsDetachedBuffer(<var>view</var>.[[ViewedArrayBuffer]]) is <b>true</b>, then<ol><li>Assert: <var>bufferByteLength</var> is <emu-const>detached</emu-const>.</li><li>Return <b>true</b>.</li></ol></li><li>Assert: <var>bufferByteLength</var> is a non-negative integer.</li><li>Let <var>byteOffsetStart</var> be <var>view</var>.[[ByteOffset]].</li><li>If <var>view</var>.[[ByteLength]] is <emu-const>auto</emu-const>, then<ol><li>Let <var>byteOffsetEnd</var> be <var>bufferByteLength</var>.</li></ol></li><li>Else,<ol><li>Let <var>byteOffsetEnd</var> be <var>byteOffsetStart</var> + <var>view</var>.[[ByteLength]].</li></ol></li><li>NOTE: A 0-length DataView whose [[ByteOffset]] is <var>bufferByteLength</var> is not considered out-of-bounds.</li><li>If <var>byteOffsetStart</var> > <var>bufferByteLength</var> or <var>byteOffsetEnd</var> > <var>bufferByteLength</var>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","3.1.5":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It is used by functions on DataView instances to retrieve values from the view's buffer.</dd>\n        </dl>\n        <emu-alg><ol><li>Perform ? RequireInternalSlot(<var>view</var>, [[DataView]]).</li><li>Assert: <var>view</var> has a [[ViewedArrayBuffer]] internal slot.</li><li>Let <var>getIndex</var> be ? ToIndex(<var>requestIndex</var>).</li><li>Set <var>isLittleEndian</var> to ToBoolean(<var>isLittleEndian</var>).</li><li>Let <var>viewOffset</var> be <var>view</var>.[[ByteOffset]].</li><li>Let <var>viewRecord</var> be MakeDataViewWithBufferWitnessRecord(<var>view</var>, <emu-const>unordered</emu-const>).</li><li>NOTE: Bounds checking is not a synchronizing operation when <var>view</var>'s backing buffer is a growable SharedArrayBuffer.</li><li>If IsViewOutOfBounds(<var>viewRecord</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>viewSize</var> be GetViewByteLength(<var>viewRecord</var>).</li><li>Let <var>elementSize</var> be the Element Size value specified in table-the-typedarray-constructors for Element Type <var>type</var>.</li><li>If <var>getIndex</var> + <var>elementSize</var> > <var>viewSize</var>, throw a <b>RangeError</b> exception.</li><li>Let <var>bufferIndex</var> be <var>getIndex</var> + <var>viewOffset</var>.</li><li>Return GetValueFromBuffer(<var>view</var>.[[ViewedArrayBuffer]], <var>bufferIndex</var>, <var>type</var>, <b>false</b>, <emu-const>unordered</emu-const>, <var>isLittleEndian</var>).</li></ol></emu-alg>\n      ","3.1.6":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It is used by functions on DataView instances to store values into the view's buffer.</dd>\n        </dl>\n        <emu-alg><ol><li>Perform ? RequireInternalSlot(<var>view</var>, [[DataView]]).</li><li>Assert: <var>view</var> has a [[ViewedArrayBuffer]] internal slot.</li><li>Let <var>getIndex</var> be ? ToIndex(<var>requestIndex</var>).</li><li>If IsBigIntElementType(<var>type</var>) is <b>true</b>, let <var>numberValue</var> be ? ToBigInt(<var>value</var>).</li><li>Else, let <var>numberValue</var> be ? ToNumber(<var>value</var>).</li><li>Set <var>isLittleEndian</var> to ToBoolean(<var>isLittleEndian</var>).</li><li>Let <var>viewOffset</var> be <var>view</var>.[[ByteOffset]].</li><li>Let <var>viewRecord</var> be MakeDataViewWithBufferWitnessRecord(<var>view</var>, <emu-const>unordered</emu-const>).</li><li>NOTE: Bounds checking is not a synchronizing operation when <var>view</var>'s backing buffer is a growable SharedArrayBuffer.</li><li>If IsViewOutOfBounds(<var>viewRecord</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>viewSize</var> be GetViewByteLength(<var>viewRecord</var>).</li><li>Let <var>elementSize</var> be the Element Size value specified in table-the-typedarray-constructors for Element Type <var>type</var>.</li><li>If <var>getIndex</var> + <var>elementSize</var> > <var>viewSize</var>, throw a <b>RangeError</b> exception.</li><li>Let <var>bufferIndex</var> be <var>getIndex</var> + <var>viewOffset</var>.</li><li>Perform SetValueInBuffer(<var>view</var>.[[ViewedArrayBuffer]], <var>bufferIndex</var>, <var>type</var>, <var>numberValue</var>, <b>false</b>, <emu-const>unordered</emu-const>, <var>isLittleEndian</var>).</li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n      ","3.2":"<p>The DataView constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%DataView%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"DataView\"</b> property of the global object.</li>\n        <li>creates and initializes a new DataView when called as a constructor.</li>\n        <li>is not intended to be called as a function and will throw an exception when called in that manner.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified DataView behaviour must include a <code>super</code> call to the DataView constructor to create and initialize subclass instances with the internal state necessary to support the <code>DataView.prototype</code> built-in methods.</li>\n      </ul>\n\n      ","3.2.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Perform ? RequireInternalSlot(<var>buffer</var>, [[ArrayBufferData]]).</li><li>Let <var>offset</var> be ? ToIndex(<var>byteOffset</var>).</li><li>If IsDetachedBuffer(<var>buffer</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>bufferByteLength</var> be ArrayBufferByteLength(<var>buffer</var>, <emu-const>seq-cst</emu-const>).</li><li>If <var>offset</var> > <var>bufferByteLength</var>, throw a <b>RangeError</b> exception.</li><li>Let <var>bufferIsFixedLength</var> be IsFixedLengthArrayBuffer(<var>buffer</var>).</li><li>If <var>byteLength</var> is <b>undefined</b>, then<ol><li>If <var>bufferIsFixedLength</var> is <b>true</b>, then<ol><li>Let <var>viewByteLength</var> be <var>bufferByteLength</var> - <var>offset</var>.</li></ol></li><li>Else,<ol><li>Let <var>viewByteLength</var> be <emu-const>auto</emu-const>.</li></ol></li></ol></li><li>Else,<ol><li>Let <var>viewByteLength</var> be ? ToIndex(<var>byteLength</var>).</li><li>If <var>offset</var> + <var>viewByteLength</var> > <var>bufferByteLength</var>, throw a <b>RangeError</b> exception.</li></ol></li><li>Let <var>obj</var> be ? OrdinaryCreateFromConstructor(NewTarget, <b>\"<code class=\"emu-intrinsic\">%DataView.prototype%</code>\"</b>, « [[DataView]], [[ViewedArrayBuffer]], [[ByteLength]], [[ByteOffset]] »).</li><li>If IsDetachedBuffer(<var>buffer</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Set <var>bufferByteLength</var> to ArrayBufferByteLength(<var>buffer</var>, <emu-const>seq-cst</emu-const>).</li><li>If <var>offset</var> > <var>bufferByteLength</var>, throw a <b>RangeError</b> exception.</li><li>If <var>byteLength</var> is not <b>undefined</b>, then<ol><li>If <var>offset</var> + <var>viewByteLength</var> > <var>bufferByteLength</var>, throw a <b>RangeError</b> exception.</li></ol></li><li>Set <var>obj</var>.[[ViewedArrayBuffer]] to <var>buffer</var>.</li><li>Set <var>obj</var>.[[ByteLength]] to <var>viewByteLength</var>.</li><li>Set <var>obj</var>.[[ByteOffset]] to <var>offset</var>.</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","3.3":"<p>The DataView constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","3.3.1":"<p>The initial value of <code>DataView.prototype</code> is the DataView prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","3.4":"<p>The <dfn>DataView prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%DataView.prototype%</code></dfn>.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n        <li>is an ordinary object.</li>\n        <li>does not have a [[DataView]], [[ViewedArrayBuffer]], [[ByteLength]], or [[ByteOffset]] internal slot.</li>\n      </ul>\n\n      ","3.4.1":"<p><code>DataView.prototype.buffer</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[DataView]]).</li><li>Assert: <var>obj</var> has a [[ViewedArrayBuffer]] internal slot.</li><li>Let <var>buffer</var> be <var>obj</var>.[[ViewedArrayBuffer]].</li><li>Return <var>buffer</var>.</li></ol></emu-alg>\n      ","3.4.2":"<p><code>DataView.prototype.byteLength</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[DataView]]).</li><li>Assert: <var>obj</var> has a [[ViewedArrayBuffer]] internal slot.</li><li>Let <var>viewRecord</var> be MakeDataViewWithBufferWitnessRecord(<var>obj</var>, <emu-const>seq-cst</emu-const>).</li><li>If IsViewOutOfBounds(<var>viewRecord</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>size</var> be GetViewByteLength(<var>viewRecord</var>).</li><li>Return 𝔽(<var>size</var>).</li></ol></emu-alg>\n      ","3.4.3":"<p><code>DataView.prototype.byteOffset</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[DataView]]).</li><li>Assert: <var>obj</var> has a [[ViewedArrayBuffer]] internal slot.</li><li>Let <var>viewRecord</var> be MakeDataViewWithBufferWitnessRecord(<var>obj</var>, <emu-const>seq-cst</emu-const>).</li><li>If IsViewOutOfBounds(<var>viewRecord</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>offset</var> be <var>obj</var>.[[ByteOffset]].</li><li>Return 𝔽(<var>offset</var>).</li></ol></emu-alg>\n      ","3.4.4":"<p>The initial value of <code>DataView.prototype.constructor</code> is <code class=\"emu-intrinsic\">%DataView%</code>.</p>\n      ","3.4.5":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>Return ? GetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>bigint64</emu-const>).</li></ol></emu-alg>\n      ","3.4.6":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>Return ? GetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>biguint64</emu-const>).</li></ol></emu-alg>\n      ","3.4.7":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? GetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>float16</emu-const>).</li></ol></emu-alg>\n      ","3.4.8":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? GetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>float32</emu-const>).</li></ol></emu-alg>\n      ","3.4.9":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? GetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>float64</emu-const>).</li></ol></emu-alg>\n      ","3.4.10":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>Return ? GetViewValue(<var>view</var>, <var>byteOffset</var>, <b>true</b>, <emu-const>int8</emu-const>).</li></ol></emu-alg>\n      ","3.4.11":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? GetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>int16</emu-const>).</li></ol></emu-alg>\n      ","3.4.12":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? GetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>int32</emu-const>).</li></ol></emu-alg>\n      ","3.4.13":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>Return ? GetViewValue(<var>view</var>, <var>byteOffset</var>, <b>true</b>, <emu-const>uint8</emu-const>).</li></ol></emu-alg>\n      ","3.4.14":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? GetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>uint16</emu-const>).</li></ol></emu-alg>\n      ","3.4.15":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? GetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>uint32</emu-const>).</li></ol></emu-alg>\n      ","3.4.16":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>Return ? SetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>bigint64</emu-const>, <var>value</var>).</li></ol></emu-alg>\n      ","3.4.17":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>Return ? SetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>biguint64</emu-const>, <var>value</var>).</li></ol></emu-alg>\n      ","3.4.18":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? SetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>float16</emu-const>, <var>value</var>).</li></ol></emu-alg>\n      ","3.4.19":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? SetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>float32</emu-const>, <var>value</var>).</li></ol></emu-alg>\n      ","3.4.20":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? SetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>float64</emu-const>, <var>value</var>).</li></ol></emu-alg>\n      ","3.4.21":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>Return ? SetViewValue(<var>view</var>, <var>byteOffset</var>, <b>true</b>, <emu-const>int8</emu-const>, <var>value</var>).</li></ol></emu-alg>\n      ","3.4.22":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? SetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>int16</emu-const>, <var>value</var>).</li></ol></emu-alg>\n      ","3.4.23":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? SetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>int32</emu-const>, <var>value</var>).</li></ol></emu-alg>\n      ","3.4.24":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>Return ? SetViewValue(<var>view</var>, <var>byteOffset</var>, <b>true</b>, <emu-const>uint8</emu-const>, <var>value</var>).</li></ol></emu-alg>\n      ","3.4.25":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? SetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>uint16</emu-const>, <var>value</var>).</li></ol></emu-alg>\n      ","3.4.26":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>view</var> be the <b>this</b> value.</li><li>If <var>littleEndian</var> is not present, set <var>littleEndian</var> to <b>false</b>.</li><li>Return ? SetViewValue(<var>view</var>, <var>byteOffset</var>, <var>littleEndian</var>, <emu-const>uint32</emu-const>, <var>value</var>).</li></ol></emu-alg>\n      ","3.4.27":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"DataView\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","3.5":"<p>DataView instances are ordinary objects that inherit properties from the DataView prototype object. DataView instances each have [[DataView]], [[ViewedArrayBuffer]], [[ByteLength]], and [[ByteOffset]] internal slots.</p>\n      <emu-note>\n        <p>The value of the [[DataView]] internal slot is not used within this specification. The simple presence of that internal slot is used within the specification to identify objects created using the DataView constructor.</p>\n      </emu-note>\n    ","4.1":"<p>A <dfn variants=\"Waiter Records\">Waiter Record</dfn> is a Record value used to denote a particular call to <code>Atomics.wait</code> or <code>Atomics.waitAsync</code>.</p>\n      <p>A Waiter Record has fields listed in table-waiterrecord.</p>\n      <emu-table id=\"table-waiterrecord\" caption=\"Waiter Record Fields\">\n        <table>\n          <thead>\n            <tr>\n              <th>\n                Field Name\n              </th>\n              <th>\n                Value\n              </th>\n              <th>\n                Meaning\n              </th>\n            </tr>\n          </thead>\n          <tr>\n            <td>\n              [[AgentSignifier]]\n            </td>\n            <td>\n              an agent signifier\n            </td>\n            <td>\n              The agent that called <code>Atomics.wait</code> or <code>Atomics.waitAsync</code>.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[PromiseCapability]]\n            </td>\n            <td>\n              a PromiseCapability Record or <emu-const>blocking</emu-const>\n            </td>\n            <td>\n              If denoting a call to <code>Atomics.waitAsync</code>, the resulting promise, otherwise <emu-const>blocking</emu-const>.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[TimeoutTime]]\n            </td>\n            <td>\n              a non-negative extended mathematical value\n            </td>\n            <td>\n              The earliest time by which timeout may be triggered; computed using time values.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[Result]]\n            </td>\n            <td>\n              <b>\"ok\"</b> or <b>\"timed-out\"</b>\n            </td>\n            <td>\n              The return value of the call.\n            </td>\n          </tr>\n        </table>\n      </emu-table>\n    ","4.2":"<p>A <dfn variants=\"WaiterList Records\">WaiterList Record</dfn> is used to explain waiting and notification of agents via <code>Atomics.wait</code>, <code>Atomics.waitAsync</code>, and <code>Atomics.notify</code>.</p>\n      <p>A WaiterList Record has fields listed in table-waiterlistrecord.</p>\n      <emu-table id=\"table-waiterlistrecord\" caption=\"WaiterList Record Fields\">\n        <table>\n          <thead>\n            <tr>\n              <th>\n                Field Name\n              </th>\n              <th>\n                Value\n              </th>\n              <th>\n                Meaning\n              </th>\n            </tr>\n          </thead>\n          <tr>\n            <td>\n              [[Waiters]]\n            </td>\n            <td>\n              a List of Waiter Records\n            </td>\n            <td>\n              The calls to <code>Atomics.wait</code> or <code>Atomics.waitAsync</code> that are waiting on the location with which this WaiterList is associated.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[MostRecentLeaveEvent]]\n            </td>\n            <td>\n              a Synchronize event or <emu-const>empty</emu-const>\n            </td>\n            <td>\n              The event of the most recent leaving of its critical section, or <emu-const>empty</emu-const> if its critical section has never been entered.\n            </td>\n          </tr>\n        </table>\n      </emu-table>\n      <p>There can be multiple Waiter Records in a WaiterList with the same agent signifier.</p>\n      <p>The agent cluster has a store of WaiterList Records; the store is indexed by (<var>block</var>, <var>i</var>), where <var>block</var> is a Shared Data Block and <var>i</var> a byte offset into the memory of <var>block</var>. WaiterList Records are agent-independent: a lookup in the store of WaiterList Records by (<var>block</var>, <var>i</var>) will result in the same WaiterList Record in any agent in the agent cluster.</p>\n      <p>Each WaiterList Record has a <dfn variants=\"critical sections\">critical section</dfn> that controls exclusive access to that WaiterList Record during evaluation. Only a single agent may enter a WaiterList Record's critical section at one time. Entering and leaving a WaiterList Record's critical section is controlled by the abstract operations EnterCriticalSection and LeaveCriticalSection. Operations on a WaiterList Record—adding and removing waiting agents, traversing the list of agents, suspending and notifying agents on the list, setting and retrieving the Synchronize event—may only be performed by agents that have entered the WaiterList Record's critical section.</p>\n    ","4.3":"","4.3.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>taRecord</var> be ? ValidateTypedArray(<var>typedArray</var>, <emu-const>unordered</emu-const>).</li><li>NOTE: Bounds checking is not a synchronizing operation when <var>typedArray</var>'s backing buffer is a growable SharedArrayBuffer.</li><li>If <var>waitable</var> is <b>true</b>, then<ol><li>If <var>typedArray</var>.[[TypedArrayName]] is neither <b>\"Int32Array\"</b> nor <b>\"BigInt64Array\"</b>, throw a <b>TypeError</b> exception.</li></ol></li><li>Else,<ol><li>Let <var>type</var> be TypedArrayElementType(<var>typedArray</var>).</li><li>If IsUnclampedIntegerElementType(<var>type</var>) is <b>false</b> and IsBigIntElementType(<var>type</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li></ol></li><li>Return <var>taRecord</var>.</li></ol></emu-alg>\n      ","4.3.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>length</var> be TypedArrayLength(<var>taRecord</var>).</li><li>Let <var>accessIndex</var> be ? ToIndex(<var>requestIndex</var>).</li><li>Assert: <var>accessIndex</var> ≥ 0.</li><li>If <var>accessIndex</var> ≥ <var>length</var>, throw a <b>RangeError</b> exception.</li><li>Let <var>typedArray</var> be <var>taRecord</var>.[[Object]].</li><li>Let <var>elementSize</var> be TypedArrayElementSize(<var>typedArray</var>).</li><li>Let <var>offset</var> be <var>typedArray</var>.[[ByteOffset]].</li><li>Return (<var>accessIndex</var> × <var>elementSize</var>) + <var>offset</var>.</li></ol></emu-alg>\n      ","4.3.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>taRecord</var> be ? ValidateIntegerTypedArray(<var>typedArray</var>, <b>false</b>).</li><li>Return ? ValidateAtomicAccess(<var>taRecord</var>, <var>requestIndex</var>).</li></ol></emu-alg>\n      ","4.3.4":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>This operation revalidates the index within the backing buffer for atomic operations after all argument coercions are performed in Atomics methods, as argument coercions can have arbitrary side effects, which could cause the buffer to become out of bounds. This operation does not throw when <var>typedArray</var>'s backing buffer is a SharedArrayBuffer.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>taRecord</var> be MakeTypedArrayWithBufferWitnessRecord(<var>typedArray</var>, <emu-const>unordered</emu-const>).</li><li>NOTE: Bounds checking is not a synchronizing operation when <var>typedArray</var>'s backing buffer is a growable SharedArrayBuffer.</li><li>If IsTypedArrayOutOfBounds(<var>taRecord</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Assert: <var>byteIndexInBuffer</var> ≥ <var>typedArray</var>.[[ByteOffset]].</li><li>If <var>byteIndexInBuffer</var> ≥ <var>taRecord</var>.[[CachedBufferByteLength]], throw a <b>RangeError</b> exception.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","4.3.5":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: <var>i</var> and <var>i</var> + 3 are valid byte offsets within the memory of <var>block</var>.</li><li>Return the WaiterList Record that is referenced by the pair (<var>block</var>, <var>i</var>).</li></ol></emu-alg>\n      ","4.3.6":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is not in the critical section for any WaiterList Record.</li><li>Wait until no agent is in the critical section for <var>waiterList</var>, then enter the critical section for <var>waiterList</var> (without allowing any other agent to enter).</li><li>If <var>waiterList</var>.[[MostRecentLeaveEvent]] is not <emu-const>empty</emu-const>, then<ol><li>NOTE: A <var>waiterList</var> whose critical section has been entered at least once has a Synchronize event set by LeaveCriticalSection.</li><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Let <var>execution</var> be <var>agentRecord</var>.[[CandidateExecution]].</li><li>Let <var>eventsRecord</var> be the Agent Events Record of <var>execution</var>.[[EventsRecords]] whose [[AgentSignifier]] is AgentSignifier().</li><li>Let <var>enterEvent</var> be a new Synchronize event.</li><li>Append <var>enterEvent</var> to <var>eventsRecord</var>.[[EventList]].</li><li>Append (<var>waiterList</var>.[[MostRecentLeaveEvent]], <var>enterEvent</var>) to <var>eventsRecord</var>.[[AgentSynchronizesWith]].</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n        <p>EnterCriticalSection has <dfn>contention</dfn> when an agent attempting to enter the critical section must wait for another agent to leave it. When there is no contention, FIFO order of EnterCriticalSection calls is observable. When there is contention, an implementation may choose an arbitrary order but may not cause an agent to wait indefinitely.</p>\n      ","4.3.7":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is in the critical section for <var>waiterList</var>.</li><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Let <var>execution</var> be <var>agentRecord</var>.[[CandidateExecution]].</li><li>Let <var>eventsRecord</var> be the Agent Events Record of <var>execution</var>.[[EventsRecords]] whose [[AgentSignifier]] is AgentSignifier().</li><li>Let <var>leaveEvent</var> be a new Synchronize event.</li><li>Append <var>leaveEvent</var> to <var>eventsRecord</var>.[[EventList]].</li><li>Set <var>waiterList</var>.[[MostRecentLeaveEvent]] to <var>leaveEvent</var>.</li><li>Leave the critical section for <var>waiterList</var>.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","4.3.8":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is in the critical section for <var>waiterList</var>.</li><li>Assert: There is no Waiter Record in <var>waiterList</var>.[[Waiters]] whose [[PromiseCapability]] field is <var>waiterRecord</var>.[[PromiseCapability]] and whose [[AgentSignifier]] field is <var>waiterRecord</var>.[[AgentSignifier]].</li><li>Append <var>waiterRecord</var> to <var>waiterList</var>.[[Waiters]].</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","4.3.9":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is in the critical section for <var>waiterList</var>.</li><li>Assert: <var>waiterList</var>.[[Waiters]] contains <var>waiterRecord</var>.</li><li>Remove <var>waiterRecord</var> from <var>waiterList</var>.[[Waiters]].</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","4.3.10":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is in the critical section for <var>waiterList</var>.</li><li>Let <var>len</var> be the number of elements in <var>waiterList</var>.[[Waiters]].</li><li>Set <var>count</var> to min(<var>count</var>, <var>len</var>).</li><li>Let <var>waiters</var> be a List whose elements are the first <var>count</var> elements of <var>waiterList</var>.[[Waiters]].</li><li>Remove the first <var>count</var> elements of <var>waiterList</var>.[[Waiters]].</li><li>Return <var>waiters</var>.</li></ol></emu-alg>\n      ","4.3.11":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is in the critical section for <var>waiterList</var>.</li><li>Assert: <var>waiterList</var>.[[Waiters]] contains <var>waiterRecord</var>.</li><li>Let <var>thisAgent</var> be AgentSignifier().</li><li>Assert: <var>waiterRecord</var>.[[AgentSignifier]] is <var>thisAgent</var>.</li><li>Assert: <var>waiterRecord</var>.[[PromiseCapability]] is <emu-const>blocking</emu-const>.</li><li>Assert: AgentCanSuspend() is <b>true</b>.</li><li>Perform LeaveCriticalSection(<var>waiterList</var>) and suspend the surrounding agent until the time is <var>waiterRecord</var>.[[TimeoutTime]], performing the combined operation in such a way that a notification that arrives after the critical section is exited but before the suspension takes effect is not lost. The surrounding agent can only wake from suspension due to a timeout or due to another agent calling NotifyWaiter with arguments <var>waiterList</var> and <var>thisAgent</var> (i.e. via a call to <code>Atomics.notify</code>).</li><li>Perform EnterCriticalSection(<var>waiterList</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","4.3.12":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: The surrounding agent is in the critical section for <var>waiterList</var>.</li><li>If <var>waiterRecord</var>.[[PromiseCapability]] is <emu-const>blocking</emu-const>, then<ol><li>Wake the agent whose signifier is <var>waiterRecord</var>.[[AgentSignifier]] from suspension.</li><li>NOTE: This causes the agent to resume execution in SuspendThisAgent.</li></ol></li><li>Else if AgentSignifier() is <var>waiterRecord</var>.[[AgentSignifier]], then<ol><li>Let <var>promiseCapability</var> be <var>waiterRecord</var>.[[PromiseCapability]].</li><li>Perform ! Call(<var>promiseCapability</var>.[[Resolve]], <b>undefined</b>, « <var>waiterRecord</var>.[[Result]] »).</li></ol></li><li>Else,<ol><li>Perform EnqueueResolveInAgentJob(<var>waiterRecord</var>.[[AgentSignifier]], <var>waiterRecord</var>.[[PromiseCapability]], <var>waiterRecord</var>.[[Result]]).</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n        <emu-note>\n          <p>An agent must not access another agent's promise capability in any capacity beyond passing it to the host.</p>\n        </emu-note>\n      ","4.3.13":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>resolveJob</var> be a new Job Abstract Closure with no parameters that captures <var>agentSignifier</var>, <var>promiseCapability</var>, and <var>resolution</var> and performs the following steps when called:<ol><li>Assert: AgentSignifier() is <var>agentSignifier</var>.</li><li>Perform ! Call(<var>promiseCapability</var>.[[Resolve]], <b>undefined</b>, « <var>resolution</var> »).</li><li>Return <emu-const>unused</emu-const>.</li></ol></li><li>Let <var>realmInTargetAgent</var> be ! GetFunctionRealm(<var>promiseCapability</var>.[[Resolve]]).</li><li>Assert: <var>agentSignifier</var> is <var>realmInTargetAgent</var>.[[AgentSignifier]].</li><li>Perform HostEnqueueGenericJob(<var>resolveJob</var>, <var>realmInTargetAgent</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","4.3.14":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>taRecord</var> be ? ValidateIntegerTypedArray(<var>typedArray</var>, <b>true</b>).</li><li>Let <var>buffer</var> be <var>taRecord</var>.[[Object]].[[ViewedArrayBuffer]].</li><li>If IsSharedArrayBuffer(<var>buffer</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>byteIndexInBuffer</var> be ? ValidateAtomicAccess(<var>taRecord</var>, <var>index</var>).</li><li>Let <var>arrayTypeName</var> be <var>typedArray</var>.[[TypedArrayName]].</li><li>If <var>arrayTypeName</var> is <b>\"BigInt64Array\"</b>, let <var>expected</var> be ? ToBigInt64(<var>value</var>).</li><li>Else, let <var>expected</var> be ? ToInt32(<var>value</var>).</li><li>Let <var>timeoutNumber</var> be ? ToNumber(<var>timeout</var>).</li><li>If <var>timeoutNumber</var> is either <b>NaN</b> or <b>+∞</b><sub>𝔽</sub>, let <var>realTimeout</var> be +∞.</li><li>Else if <var>timeoutNumber</var> is <b>-∞</b><sub>𝔽</sub>, let <var>realTimeout</var> be 0.</li><li>Else, let <var>realTimeout</var> be max(ℝ(<var>timeoutNumber</var>), 0).</li><li>If <var>mode</var> is <emu-const>sync</emu-const> and AgentCanSuspend() is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>block</var> be <var>buffer</var>.[[ArrayBufferData]].</li><li>Let <var>waiterList</var> be GetWaiterList(<var>block</var>, <var>byteIndexInBuffer</var>).</li><li>If <var>mode</var> is <emu-const>sync</emu-const>, then<ol><li>Let <var>promiseCapability</var> be <emu-const>blocking</emu-const>.</li><li>Let <var>resultObject</var> be <b>undefined</b>.</li></ol></li><li>Else,<ol><li>Let <var>promiseCapability</var> be ! NewPromiseCapability(<code class=\"emu-intrinsic\">%Promise%</code>).</li><li>Let <var>resultObject</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Object.prototype%</code>).</li></ol></li><li>Perform EnterCriticalSection(<var>waiterList</var>).</li><li>Let <var>elementType</var> be TypedArrayElementType(<var>typedArray</var>).</li><li>Let <var>witness</var> be GetValueFromBuffer(<var>buffer</var>, <var>byteIndexInBuffer</var>, <var>elementType</var>, <b>true</b>, <emu-const>seq-cst</emu-const>).</li><li>If <var>expected</var> ≠ <var>witness</var>, then<ol><li>Perform LeaveCriticalSection(<var>waiterList</var>).</li><li>If <var>mode</var> is <emu-const>sync</emu-const>, return <b>\"not-equal\"</b>.</li><li>Perform ! CreateDataPropertyOrThrow(<var>resultObject</var>, <b>\"async\"</b>, <b>false</b>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>resultObject</var>, <b>\"value\"</b>, <b>\"not-equal\"</b>).</li><li>Return <var>resultObject</var>.</li></ol></li><li>If <var>realTimeout</var> = 0 and <var>mode</var> is <emu-const>async</emu-const>, then<ol><li>NOTE: There is no special handling of synchronous immediate timeouts. Asynchronous immediate timeouts have special handling in order to fail fast and avoid unnecessary Promise jobs.</li><li>Perform LeaveCriticalSection(<var>waiterList</var>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>resultObject</var>, <b>\"async\"</b>, <b>false</b>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>resultObject</var>, <b>\"value\"</b>, <b>\"timed-out\"</b>).</li><li>Return <var>resultObject</var>.</li></ol></li><li>Let <var>thisAgent</var> be AgentSignifier().</li><li>Let <var>now</var> be the time value (UTC) identifying the current time.</li><li>Let <var>additionalTimeout</var> be an implementation-defined non-negative mathematical value.</li><li>Let <var>timeoutTime</var> be ℝ(<var>now</var>) + <var>realTimeout</var> + <var>additionalTimeout</var>.</li><li>NOTE: When <var>realTimeout</var> is +∞, <var>timeoutTime</var> is also +∞.</li><li>Let <var>waiterRecord</var> be a new Waiter Record { [[AgentSignifier]]: <var>thisAgent</var>, [[PromiseCapability]]: <var>promiseCapability</var>, [[TimeoutTime]]: <var>timeoutTime</var>, [[Result]]: <b>\"ok\"</b> }.</li><li>Perform AddWaiter(<var>waiterList</var>, <var>waiterRecord</var>).</li><li>If <var>mode</var> is <emu-const>sync</emu-const>, then<ol><li>Perform SuspendThisAgent(<var>waiterList</var>, <var>waiterRecord</var>).</li></ol></li><li>Else if <var>timeoutTime</var> is finite, then<ol><li>Perform EnqueueAtomicsWaitAsyncTimeoutJob(<var>waiterList</var>, <var>waiterRecord</var>).</li></ol></li><li>Perform LeaveCriticalSection(<var>waiterList</var>).</li><li>If <var>mode</var> is <emu-const>sync</emu-const>, return <var>waiterRecord</var>.[[Result]].</li><li>Perform ! CreateDataPropertyOrThrow(<var>resultObject</var>, <b>\"async\"</b>, <b>true</b>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>resultObject</var>, <b>\"value\"</b>, <var>promiseCapability</var>.[[Promise]]).</li><li>Return <var>resultObject</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p><var>additionalTimeout</var> allows implementations to pad timeouts as necessary, such as for reducing power consumption or coarsening timer resolution to mitigate timing attacks. This value may differ from call to call of DoWait.</p>\n        </emu-note>\n      ","4.3.15":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>timeoutJob</var> be a new Job Abstract Closure with no parameters that captures <var>waiterList</var> and <var>waiterRecord</var> and performs the following steps when called:<ol><li>Perform EnterCriticalSection(<var>waiterList</var>).</li><li>If <var>waiterList</var>.[[Waiters]] contains <var>waiterRecord</var>, then<ol><li>Let <var>timeOfJobExecution</var> be the time value (UTC) identifying the current time.</li><li>Assert: ℝ(<var>timeOfJobExecution</var>) ≥ <var>waiterRecord</var>.[[TimeoutTime]] (ignoring potential non-monotonicity of time values).</li><li>Set <var>waiterRecord</var>.[[Result]] to <b>\"timed-out\"</b>.</li><li>Perform RemoveWaiter(<var>waiterList</var>, <var>waiterRecord</var>).</li><li>Perform NotifyWaiter(<var>waiterList</var>, <var>waiterRecord</var>).</li></ol></li><li>Perform LeaveCriticalSection(<var>waiterList</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></li><li>Let <var>now</var> be the time value (UTC) identifying the current time.</li><li>Let <var>currentRealm</var> be the current Realm Record.</li><li>Perform HostEnqueueTimeoutJob(<var>timeoutJob</var>, <var>currentRealm</var>, 𝔽(<var>waiterRecord</var>.[[TimeoutTime]]) - <var>now</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","4.3.16":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Let <var>execution</var> be <var>agentRecord</var>.[[CandidateExecution]].</li><li>Let <var>eventsRecord</var> be the Agent Events Record of <var>execution</var>.[[EventsRecords]] whose [[AgentSignifier]] is AgentSignifier().</li><li>Let <var>rawBytesRead</var> be a List of length <var>elementSize</var> whose elements are nondeterministically chosen byte values.</li><li>NOTE: In implementations, <var>rawBytesRead</var> is the result of a load-link, of a load-exclusive, or of an operand of a read-modify-write instruction on the underlying hardware. The nondeterminism is a semantic prescription of the memory model to describe observable behaviour of hardware with weak consistency.</li><li>NOTE: The comparison of the expected value and the read value is performed outside of the read-modify-write modification function to avoid needlessly strong synchronization when the expected value is not equal to the read value.</li><li>If ByteListEqual(<var>rawBytesRead</var>, <var>expectedBytes</var>) is <b>true</b>, then<ol><li>Let <var>second</var> be a new read-modify-write modification function with parameters (<var>oldBytes</var>, <var>newBytes</var>) that captures nothing and performs the following steps atomically when called:<ol><li>Return <var>newBytes</var>.</li></ol></li><li>Let <var>event</var> be ReadModifyWriteSharedMemory { [[Order]]: <emu-const>seq-cst</emu-const>, [[NoTear]]: <b>true</b>, [[Block]]: <var>block</var>, [[ByteIndex]]: <var>byteIndexInBuffer</var>, [[ElementSize]]: <var>elementSize</var>, [[Payload]]: <var>replacementBytes</var>, [[ModifyOp]]: <var>second</var> }.</li></ol></li><li>Else,<ol><li>Let <var>event</var> be ReadSharedMemory { [[Order]]: <emu-const>seq-cst</emu-const>, [[NoTear]]: <b>true</b>, [[Block]]: <var>block</var>, [[ByteIndex]]: <var>byteIndexInBuffer</var>, [[ElementSize]]: <var>elementSize</var> }.</li></ol></li><li>Append <var>event</var> to <var>eventsRecord</var>.[[EventList]].</li><li>Append Chosen Value Record { [[Event]]: <var>event</var>, [[ChosenValue]]: <var>rawBytesRead</var> } to <var>execution</var>.[[ChosenValues]].</li><li>Return <var>rawBytesRead</var>.</li></ol></emu-alg>\n      ","4.3.17":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd><var>op</var> takes two List of byte values arguments and returns a List of byte values. This operation atomically loads a value, combines it with another value, and stores the combination. It returns the loaded value.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>byteIndexInBuffer</var> be ? ValidateAtomicAccessOnIntegerTypedArray(<var>typedArray</var>, <var>index</var>).</li><li>If <var>typedArray</var>.[[ContentType]] is <emu-const>bigint</emu-const>, let <var>coerced</var> be ? ToBigInt(<var>value</var>).</li><li>Else, let <var>coerced</var> be 𝔽(? ToIntegerOrInfinity(<var>value</var>)).</li><li>Perform ? RevalidateAtomicAccess(<var>typedArray</var>, <var>byteIndexInBuffer</var>).</li><li>Let <var>buffer</var> be <var>typedArray</var>.[[ViewedArrayBuffer]].</li><li>Let <var>elementType</var> be TypedArrayElementType(<var>typedArray</var>).</li><li>Return GetModifySetValueInBuffer(<var>buffer</var>, <var>byteIndexInBuffer</var>, <var>elementType</var>, <var>coerced</var>, <var>op</var>).</li></ol></emu-alg>\n      ","4.3.18":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>The operation atomically performs a bitwise operation on all byte values of the arguments and returns a List of byte values.</dd>\n        </dl>\n        <emu-alg><ol><li>Assert: <var>xBytes</var> and <var>yBytes</var> have the same number of elements.</li><li>Let <var>result</var> be a new empty List.</li><li>Let <var>i</var> be 0.</li><li>For each element <var>xByte</var> of <var>xBytes</var>, do<ol><li>Let <var>yByte</var> be <var>yBytes</var>[<var>i</var>].</li><li>If <var>op</var> is <code>&amp;</code>, then<ol><li>Let <var>resultByte</var> be the result of applying the bitwise AND operation to <var>xByte</var> and <var>yByte</var>.</li></ol></li><li>Else if <var>op</var> is <code>^</code>, then<ol><li>Let <var>resultByte</var> be the result of applying the bitwise exclusive OR (XOR) operation to <var>xByte</var> and <var>yByte</var>.</li></ol></li><li>Else,<ol><li>Assert: <var>op</var> is <code>|</code>.</li><li>Let <var>resultByte</var> be the result of applying the bitwise inclusive OR operation to <var>xByte</var> and <var>yByte</var>.</li></ol></li><li>Set <var>i</var> to <var>i</var> + 1.</li><li>Append <var>resultByte</var> to <var>result</var>.</li></ol></li><li>Return <var>result</var>.</li></ol></emu-alg>\n      ","4.3.19":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If <var>xBytes</var> and <var>yBytes</var> do not have the same number of elements, return <b>false</b>.</li><li>Let <var>i</var> be 0.</li><li>For each element <var>xByte</var> of <var>xBytes</var>, do<ol><li>Let <var>yByte</var> be <var>yBytes</var>[<var>i</var>].</li><li>If <var>xByte</var> ≠ <var>yByte</var>, return <b>false</b>.</li><li>Set <var>i</var> to <var>i</var> + 1.</li></ol></li><li>Return <b>true</b>.</li></ol></emu-alg>\n      ","4.4":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>add</var> be a new read-modify-write modification function with parameters (<var>xBytes</var>, <var>yBytes</var>) that captures <var>typedArray</var> and performs the following steps atomically when called:<ol><li>Let <var>type</var> be TypedArrayElementType(<var>typedArray</var>).</li><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Let <var>isLittleEndian</var> be <var>agentRecord</var>.[[LittleEndian]].</li><li>Let <var>x</var> be RawBytesToNumeric(<var>type</var>, <var>xBytes</var>, <var>isLittleEndian</var>).</li><li>Let <var>y</var> be RawBytesToNumeric(<var>type</var>, <var>yBytes</var>, <var>isLittleEndian</var>).</li><li>If <var>x</var> is a Number, then<ol><li>Let <var>sum</var> be Number::add(<var>x</var>, <var>y</var>).</li></ol></li><li>Else,<ol><li>Assert: <var>x</var> is a BigInt.</li><li>Let <var>sum</var> be BigInt::add(<var>x</var>, <var>y</var>).</li></ol></li><li>Let <var>sumBytes</var> be NumericToRawBytes(<var>type</var>, <var>sum</var>, <var>isLittleEndian</var>).</li><li>Assert: <var>sumBytes</var>, <var>xBytes</var>, and <var>yBytes</var> have the same number of elements.</li><li>Return <var>sumBytes</var>.</li></ol></li><li>Return ? AtomicReadModifyWrite(<var>typedArray</var>, <var>index</var>, <var>value</var>, <var>add</var>).</li></ol></emu-alg>\n    ","4.5":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>and</var> be a new read-modify-write modification function with parameters (<var>xBytes</var>, <var>yBytes</var>) that captures nothing and performs the following steps atomically when called:<ol><li>Return ByteListBitwiseOp(<code>&amp;</code>, <var>xBytes</var>, <var>yBytes</var>).</li></ol></li><li>Return ? AtomicReadModifyWrite(<var>typedArray</var>, <var>index</var>, <var>value</var>, <var>and</var>).</li></ol></emu-alg>\n    ","4.6":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>byteIndexInBuffer</var> be ? ValidateAtomicAccessOnIntegerTypedArray(<var>typedArray</var>, <var>index</var>).</li><li>Let <var>buffer</var> be <var>typedArray</var>.[[ViewedArrayBuffer]].</li><li>Let <var>block</var> be <var>buffer</var>.[[ArrayBufferData]].</li><li>If <var>typedArray</var>.[[ContentType]] is <emu-const>bigint</emu-const>, then<ol><li>Let <var>expected</var> be ? ToBigInt(<var>expectedValue</var>).</li><li>Let <var>replacement</var> be ? ToBigInt(<var>replacementValue</var>).</li></ol></li><li>Else,<ol><li>Let <var>expected</var> be 𝔽(? ToIntegerOrInfinity(<var>expectedValue</var>)).</li><li>Let <var>replacement</var> be 𝔽(? ToIntegerOrInfinity(<var>replacementValue</var>)).</li></ol></li><li>Perform ? RevalidateAtomicAccess(<var>typedArray</var>, <var>byteIndexInBuffer</var>).</li><li>Let <var>elementType</var> be TypedArrayElementType(<var>typedArray</var>).</li><li>Let <var>elementSize</var> be TypedArrayElementSize(<var>typedArray</var>).</li><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Let <var>isLittleEndian</var> be <var>agentRecord</var>.[[LittleEndian]].</li><li>Let <var>expectedBytes</var> be NumericToRawBytes(<var>elementType</var>, <var>expected</var>, <var>isLittleEndian</var>).</li><li>Let <var>replacementBytes</var> be NumericToRawBytes(<var>elementType</var>, <var>replacement</var>, <var>isLittleEndian</var>).</li><li>If IsSharedArrayBuffer(<var>buffer</var>) is <b>true</b>, then<ol><li>Let <var>rawBytesRead</var> be AtomicCompareExchangeInSharedBlock(<var>block</var>, <var>byteIndexInBuffer</var>, <var>elementSize</var>, <var>expectedBytes</var>, <var>replacementBytes</var>).</li></ol></li><li>Else,<ol><li>Let <var>rawBytesRead</var> be a List of length <var>elementSize</var> whose elements are the sequence of <var>elementSize</var> bytes starting with <var>block</var>[<var>byteIndexInBuffer</var>].</li><li>If ByteListEqual(<var>rawBytesRead</var>, <var>expectedBytes</var>) is <b>true</b>, then<ol><li>Store the individual bytes of <var>replacementBytes</var> into <var>block</var>, starting at <var>block</var>[<var>byteIndexInBuffer</var>].</li></ol></li></ol></li><li>Return RawBytesToNumeric(<var>elementType</var>, <var>rawBytesRead</var>, <var>isLittleEndian</var>).</li></ol></emu-alg>\n    ","4.7":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>second</var> be a new read-modify-write modification function with parameters (<var>oldBytes</var>, <var>newBytes</var>) that captures nothing and performs the following steps atomically when called:<ol><li>Return <var>newBytes</var>.</li></ol></li><li>Return ? AtomicReadModifyWrite(<var>typedArray</var>, <var>index</var>, <var>value</var>, <var>second</var>).</li></ol></emu-alg>\n    ","4.8":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>n</var> be ? ToIntegerOrInfinity(<var>size</var>).</li><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>If <var>n</var> = 1, return <var>agentRecord</var>.[[IsLockFree1]].</li><li>If <var>n</var> = 2, return <var>agentRecord</var>.[[IsLockFree2]].</li><li>If <var>n</var> = 4, return <b>true</b>.</li><li>If <var>n</var> = 8, return <var>agentRecord</var>.[[IsLockFree8]].</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      <emu-note>\n        <p>This function is an optimization primitive. The intuition is that if the atomic step of an atomic primitive (<code>compareExchange</code>, <code>load</code>, <code>store</code>, <code>add</code>, <code>sub</code>, <code>and</code>, <code>or</code>, <code>xor</code>, or <code>exchange</code>) on a datum of size <var>n</var> bytes will be performed without the surrounding agent acquiring a lock outside the <var>n</var> bytes comprising the datum, then <code>Atomics.isLockFree</code>(<var>n</var>) will return <b>true</b>. High-performance algorithms will use this function to determine whether to use locks or atomic operations in critical sections. If an atomic primitive is not lock-free then it is often more efficient for an algorithm to provide its own locking.</p>\n        <p><code>Atomics.isLockFree</code>(4) always returns <b>true</b> as that can be supported on all known relevant hardware. Being able to assume this will generally simplify programs.</p>\n        <p>Regardless of the value returned by this function, all atomic operations are guaranteed to be atomic. For example, they will never have a visible operation take place in the middle of the operation (e.g., \"tearing\").</p>\n      </emu-note>\n    ","4.9":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>byteIndexInBuffer</var> be ? ValidateAtomicAccessOnIntegerTypedArray(<var>typedArray</var>, <var>index</var>).</li><li>Perform ? RevalidateAtomicAccess(<var>typedArray</var>, <var>byteIndexInBuffer</var>).</li><li>Let <var>buffer</var> be <var>typedArray</var>.[[ViewedArrayBuffer]].</li><li>Let <var>elementType</var> be TypedArrayElementType(<var>typedArray</var>).</li><li>Return GetValueFromBuffer(<var>buffer</var>, <var>byteIndexInBuffer</var>, <var>elementType</var>, <b>true</b>, <emu-const>seq-cst</emu-const>).</li></ol></emu-alg>\n    ","4.10":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>or</var> be a new read-modify-write modification function with parameters (<var>xBytes</var>, <var>yBytes</var>) that captures nothing and performs the following steps atomically when called:<ol><li>Return ByteListBitwiseOp(<code>|</code>, <var>xBytes</var>, <var>yBytes</var>).</li></ol></li><li>Return ? AtomicReadModifyWrite(<var>typedArray</var>, <var>index</var>, <var>value</var>, <var>or</var>).</li></ol></emu-alg>\n    ","4.11":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>byteIndexInBuffer</var> be ? ValidateAtomicAccessOnIntegerTypedArray(<var>typedArray</var>, <var>index</var>).</li><li>If <var>typedArray</var>.[[ContentType]] is <emu-const>bigint</emu-const>, let <var>coerced</var> be ? ToBigInt(<var>value</var>).</li><li>Else, let <var>coerced</var> be 𝔽(? ToIntegerOrInfinity(<var>value</var>)).</li><li>Perform ? RevalidateAtomicAccess(<var>typedArray</var>, <var>byteIndexInBuffer</var>).</li><li>Let <var>buffer</var> be <var>typedArray</var>.[[ViewedArrayBuffer]].</li><li>Let <var>elementType</var> be TypedArrayElementType(<var>typedArray</var>).</li><li>Perform SetValueInBuffer(<var>buffer</var>, <var>byteIndexInBuffer</var>, <var>elementType</var>, <var>coerced</var>, <b>true</b>, <emu-const>seq-cst</emu-const>).</li><li>Return <var>coerced</var>.</li></ol></emu-alg>\n    ","4.12":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>subtract</var> be a new read-modify-write modification function with parameters (<var>xBytes</var>, <var>yBytes</var>) that captures <var>typedArray</var> and performs the following steps atomically when called:<ol><li>Let <var>type</var> be TypedArrayElementType(<var>typedArray</var>).</li><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Let <var>isLittleEndian</var> be <var>agentRecord</var>.[[LittleEndian]].</li><li>Let <var>x</var> be RawBytesToNumeric(<var>type</var>, <var>xBytes</var>, <var>isLittleEndian</var>).</li><li>Let <var>y</var> be RawBytesToNumeric(<var>type</var>, <var>yBytes</var>, <var>isLittleEndian</var>).</li><li>If <var>x</var> is a Number, then<ol><li>Let <var>difference</var> be Number::subtract(<var>x</var>, <var>y</var>).</li></ol></li><li>Else,<ol><li>Assert: <var>x</var> is a BigInt.</li><li>Let <var>difference</var> be BigInt::subtract(<var>x</var>, <var>y</var>).</li></ol></li><li>Let <var>differenceBytes</var> be NumericToRawBytes(<var>type</var>, <var>difference</var>, <var>isLittleEndian</var>).</li><li>Assert: <var>differenceBytes</var>, <var>xBytes</var>, and <var>yBytes</var> have the same number of elements.</li><li>Return <var>differenceBytes</var>.</li></ol></li><li>Return ? AtomicReadModifyWrite(<var>typedArray</var>, <var>index</var>, <var>value</var>, <var>subtract</var>).</li></ol></emu-alg>\n    ","4.13":"<p>This function puts the surrounding agent in a wait queue and suspends it until notified or until the wait times out, returning a String differentiating those cases.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Return ? DoWait(<emu-const>sync</emu-const>, <var>typedArray</var>, <var>index</var>, <var>value</var>, <var>timeout</var>).</li></ol></emu-alg>\n    ","4.14":"<p>This function returns a Promise that is resolved when the calling agent is notified or the timeout is reached.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Return ? DoWait(<emu-const>async</emu-const>, <var>typedArray</var>, <var>index</var>, <var>value</var>, <var>timeout</var>).</li></ol></emu-alg>\n    ","4.15":"<p>This function notifies some agents that are sleeping in the wait queue.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>taRecord</var> be ? ValidateIntegerTypedArray(<var>typedArray</var>, <b>true</b>).</li><li>Let <var>byteIndexInBuffer</var> be ? ValidateAtomicAccess(<var>taRecord</var>, <var>index</var>).</li><li>If <var>count</var> is <b>undefined</b>, then<ol><li>Set <var>count</var> to +∞.</li></ol></li><li>Else,<ol><li>Let <var>intCount</var> be ? ToIntegerOrInfinity(<var>count</var>).</li><li>Set <var>count</var> to max(<var>intCount</var>, 0).</li></ol></li><li>Let <var>buffer</var> be <var>typedArray</var>.[[ViewedArrayBuffer]].</li><li>Let <var>block</var> be <var>buffer</var>.[[ArrayBufferData]].</li><li>If IsSharedArrayBuffer(<var>buffer</var>) is <b>false</b>, return <b>+0</b><sub>𝔽</sub>.</li><li>Let <var>waiterList</var> be GetWaiterList(<var>block</var>, <var>byteIndexInBuffer</var>).</li><li>Perform EnterCriticalSection(<var>waiterList</var>).</li><li>Let <var>waiters</var> be RemoveWaiters(<var>waiterList</var>, <var>count</var>).</li><li>For each element <var>waiterRecord</var> of <var>waiters</var>, do<ol><li>Perform NotifyWaiter(<var>waiterList</var>, <var>waiterRecord</var>).</li></ol></li><li>Perform LeaveCriticalSection(<var>waiterList</var>).</li><li>Let <var>n</var> be the number of elements in <var>waiters</var>.</li><li>Return 𝔽(<var>n</var>).</li></ol></emu-alg>\n    ","4.16":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>xor</var> be a new read-modify-write modification function with parameters (<var>xBytes</var>, <var>yBytes</var>) that captures nothing and performs the following steps atomically when called:<ol><li>Return ByteListBitwiseOp(<code>^</code>, <var>xBytes</var>, <var>yBytes</var>).</li></ol></li><li>Return ? AtomicReadModifyWrite(<var>typedArray</var>, <var>index</var>, <var>value</var>, <var>xor</var>).</li></ol></emu-alg>\n    ","4.17":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"Atomics\"</b>.</p>\n      <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n    ","5.1":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If <var>obj</var> is an Object and <var>obj</var> has an [[IsRawJSON]] internal slot, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n    ","5.2":"<p>This function parses a JSON text (a JSON-formatted String) and produces an ECMAScript language value. The JSON format represents literals, arrays, and objects with a syntax similar to the syntax for ECMAScript literals, Array Initializers, and Object Initializers. After parsing, JSON objects are realized as ECMAScript objects. JSON arrays are realized as ECMAScript Array instances. JSON strings, numbers, booleans, and null are realized as ECMAScript Strings, Numbers, Booleans, and <b>null</b>.</p>\n      <p>The optional <var>reviver</var> parameter is a function that can filter and transform the results. For each value produced by the parse, <var>reviver</var> is called with three arguments (the associated property key, the value, and a context object). If the property is unmodified and its value is primitive, the provided context object has a <b>\"source\"</b> property containing source text of the corresponding Parse Node. If the call returns <b>undefined</b>, the property is deleted. Otherwise, the property is redefined to use the return value.</p>\n      <emu-alg><ol><li>Let <var>jsonString</var> be ? ToString(<var>text</var>).</li><li>Let <var>parseResult</var> be ? ParseJSON(<var>jsonString</var>).</li><li>Let <var>unfiltered</var> be <var>parseResult</var>.[[Value]].</li><li>If IsCallable(<var>reviver</var>) is <b>false</b>, return <var>unfiltered</var>.</li><li>Let <var>root</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Object.prototype%</code>).</li><li>Let <var>rootName</var> be the empty String.</li><li>Perform ! CreateDataPropertyOrThrow(<var>root</var>, <var>rootName</var>, <var>unfiltered</var>).</li><li>Let <var>snapshot</var> be <emu-meta suppress-effects=\"user-code\">CreateJSONParseRecord(<var>parseResult</var>.[[ParseNode]], <var>rootName</var>, <var>unfiltered</var>)</emu-meta>.</li><li>Return ? InternalizeJSONProperty(<var>root</var>, <var>rootName</var>, <var>reviver</var>, <var>snapshot</var>).</li></ol></emu-alg>\n      <p>The <b>\"length\"</b> property of this function is <b>2</b><sub>𝔽</sub>.</p>\n\n      ","5.2.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If StringToCodePoints(<var>text</var>) is not a valid JSON text as specified in ECMA-404, throw a <b>SyntaxError</b> exception.</li><li>Let <var>scriptString</var> be the string-concatenation of <b>\"(\"</b>, <var>text</var>, and <b>\");\"</b>.</li><li>Let <var>script</var> be ParseText(<var>scriptString</var>, <emu-nt>Script</emu-nt>).</li><li>NOTE: The early error rules defined in <a href=\"/ecmascript-language-expressions#sec-object-initializer-static-semantics-early-errors\">13.2.5.1</a> have special handling for the above invocation of ParseText.</li><li>Assert: <var>script</var> is a Parse Node.</li><li>Let <var>result</var> be ! <emu-meta suppress-effects=\"user-code\">Evaluation of <var>script</var></emu-meta>.</li><li>NOTE: The PropertyDefinitionEvaluation semantics defined in <a href=\"/ecmascript-language-expressions#sec-runtime-semantics-propertydefinitionevaluation\">13.2.5.6</a> have special handling for the above evaluation.</li><li>Assert: <var>result</var> is either a String, a Number, a Boolean, an Object that is defined by either an <emu-nt>ArrayLiteral</emu-nt> or an <emu-nt>ObjectLiteral</emu-nt>, or <b>null</b>.</li><li>Return the Record { [[ParseNode]]: <var>script</var>, [[Value]]: <var>result</var> }.</li></ol></emu-alg>\n        <p>It is not permitted for a conforming implementation of <code>JSON.parse</code> to extend the JSON grammars. If an implementation wishes to support a modified or extended JSON interchange format it must do so by defining a different parse function.</p>\n        <emu-note>\n          <p>Valid JSON text is a subset of the ECMAScript <emu-nt>PrimaryExpression</emu-nt> syntax. Step step-json-parse-validate verifies that <var>jsonString</var> conforms to that subset, and step step-json-parse-assert-type asserts that evaluation returns a value of an appropriate type.</p>\n          <p>However, because <a href=\"/ecmascript-language-expressions#sec-runtime-semantics-propertydefinitionevaluation\">13.2.5.6</a> behaves differently during ParseJSON, the same source text can produce different results when evaluated as a <emu-nt>PrimaryExpression</emu-nt> rather than as JSON. Furthermore, the Early Error for duplicate <b>\"__proto__\"</b> properties in object literals, which likewise does not apply during ParseJSON, means that not all texts accepted by ParseJSON are valid as a <emu-nt>PrimaryExpression</emu-nt>, despite matching the grammar.</p>\n        </emu-note>\n        <emu-note>\n          <p>In the case where there are duplicate name Strings within an object, lexically preceding values for the same key shall be overwritten.</p>\n        </emu-note>\n      ","5.2.2":"<p>A <dfn variants=\"JSON Parse Records\">JSON Parse Record</dfn> is a Record value used to describe the initial state of a value parsed from JSON text.</p>\n        <p>JSON Parse Records have the fields listed in table-json-parse-record.</p>\n        <emu-table id=\"table-json-parse-record\" caption=\"JSON Parse Record Fields\">\n          <table>\n            <tr>\n              <th>Field Name</th>\n              <th>Value</th>\n              <th>Meaning</th>\n            </tr>\n            <tr>\n              <td>[[ParseNode]]</td>\n              <td>a Parse Node</td>\n              <td>The context Parse Node.</td>\n            </tr>\n            <tr>\n              <td>[[Key]]</td>\n              <td>a property name</td>\n              <td>The property name with which [[Value]] is associated.</td>\n            </tr>\n            <tr>\n              <td>[[Value]]</td>\n              <td>an ECMAScript language value</td>\n              <td>The value produced by evaluation of [[ParseNode]].</td>\n            </tr>\n            <tr>\n              <td>[[Elements]]</td>\n              <td>a List of JSON Parse Records</td>\n              <td>If [[Value]] is an Array, this contains the JSON Parse Records corresponding with the elements of [[Value]]. Otherwise, this is an empty List.</td>\n            </tr>\n            <tr>\n              <td>[[Entries]]</td>\n              <td>a List of JSON Parse Records</td>\n              <td>If [[Value]] is a non-Array Object, this contains the JSON Parse Records corresponding with the entries of [[Value]]. Otherwise, this is an empty List.</td>\n            </tr>\n          </table>\n        </emu-table>\n      ","5.2.3":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It recursively combines a <var>parseNode</var> parsed from JSON text and the <var>val</var> produced by its evaluation.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>typedValNode</var> be ShallowestContainedJSONValue(<var>parseNode</var>).</li><li>Assert: <var>typedValNode</var> is not <emu-const>empty</emu-const>.</li><li>Let <var>elements</var> be a new empty List.</li><li>Let <var>entries</var> be a new empty List.</li><li>If <var>val</var> is an Object, then<ol><li>Let <var>isArray</var> be ! <emu-meta suppress-effects=\"user-code\">IsArray(<var>val</var>)</emu-meta>.</li><li>If <var>isArray</var> is <b>true</b>, then<ol><li>Assert: <var>typedValNode</var> is an <emu-nt>ArrayLiteral</emu-nt> Parse Node.</li><li>Let <var>contentNodes</var> be the JSONArrayLiteralContentNodes of <var>typedValNode</var>.</li><li>Let <var>len</var> be the number of elements in <var>contentNodes</var>.</li><li>Let <var>valLen</var> be ! <emu-meta suppress-effects=\"user-code\">LengthOfArrayLike(<var>val</var>)</emu-meta>.</li><li>Assert: <var>valLen</var> is <var>len</var>.</li><li>Let <var>index</var> be 0.</li><li>Repeat, while <var>index</var> &lt; <var>len</var>,<ol><li>Let <var>propName</var> be ! ToString(𝔽(<var>index</var>)).</li><li>Let <var>elementParseRecord</var> be CreateJSONParseRecord(<var>contentNodes</var>[<var>index</var>], <var>propName</var>, ! <emu-meta suppress-effects=\"user-code\">Get(<var>val</var>, <var>propName</var>)</emu-meta>).</li><li>Append <var>elementParseRecord</var> to <var>elements</var>.</li><li>Set <var>index</var> to <var>index</var> + 1.</li></ol></li></ol></li><li>Else,<ol><li>Assert: <var>typedValNode</var> is an <emu-nt>ObjectLiteral</emu-nt> Parse Node.</li><li>Let <var>propertyNodes</var> be the PropertyDefinitionNodes of <var>typedValNode</var>.</li><li>NOTE: Because <var>val</var> was produced from JSON text and has not been modified, all of its property keys are Strings and will be exhaustively enumerated.</li><li>Let <var>keys</var> be ! <emu-meta suppress-effects=\"user-code\">EnumerableOwnProperties(<var>val</var>, <emu-const>key</emu-const>)</emu-meta>.</li><li>For each String <var>propertyKey</var> of <var>keys</var>, do<ol><li>NOTE: In the case of JSON text specifying multiple name/value pairs with the same name for a single object (such as <code>{\"a\":\"lost\",\"a\":\"kept\"}</code>), the value for the corresponding property of the resulting ECMAScript object is specified by the last pair with that name.</li><li>Let <var>propertyDefinition</var> be <emu-const>empty</emu-const>.</li><li>For each Parse Node <var>propertyNode</var> of <var>propertyNodes</var>, do<ol><li>Let <var>propName</var> be the PropName of <var>propertyNode</var>.</li><li>If <var>propName</var> is <var>propertyKey</var>, set <var>propertyDefinition</var> to <var>propertyNode</var>.</li></ol></li><li>Assert: <var>propertyDefinition</var> is <code class=\"emu-grammar\">PropertyDefinition : PropertyName `:` AssignmentExpression</code>.</li><li>Let <var>propertyValueNode</var> be the <emu-nt>AssignmentExpression</emu-nt> of <var>propertyDefinition</var>.</li><li>Let <var>entryParseRecord</var> be CreateJSONParseRecord(<var>propertyValueNode</var>, <var>propertyKey</var>, ! <emu-meta suppress-effects=\"user-code\">Get(<var>val</var>, <var>propertyKey</var>)</emu-meta>).</li><li>Append <var>entryParseRecord</var> to <var>entries</var>.</li></ol></li></ol></li></ol></li><li>Else,<ol><li>Assert: <var>typedValNode</var> is neither an <emu-nt>ArrayLiteral</emu-nt> Parse Node nor an <emu-nt>ObjectLiteral</emu-nt> Parse Node.</li></ol></li><li>Return the JSON Parse Record { [[ParseNode]]: <var>typedValNode</var>, [[Key]]: <var>key</var>, [[Value]]: <var>val</var>, [[Elements]]: <var>elements</var>, [[Entries]]: <var>entries</var> }.</li></ol></emu-alg>\n      ","5.2.4":"<dl class=\"header\">\n        </dl>\n        <emu-note>\n          <p>This algorithm intentionally does not throw an exception if either [[Delete]] or CreateDataProperty return <b>false</b>.</p>\n        </emu-note>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>val</var> be ? Get(<var>holder</var>, <var>name</var>).</li><li>Let <var>context</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Object.prototype%</code>).</li><li>If <var>parseRecord</var> is a JSON Parse Record and SameValue(<var>parseRecord</var>.[[Value]], <var>val</var>) is <b>true</b>, then<ol><li>If <var>val</var> is not an Object, then<ol><li>Let <var>parseNode</var> be <var>parseRecord</var>.[[ParseNode]].</li><li>Assert: <var>parseNode</var> is neither an <emu-nt>ArrayLiteral</emu-nt> Parse Node nor an <emu-nt>ObjectLiteral</emu-nt> Parse Node.</li><li>Let <var>sourceText</var> be the source text matched by <var>parseNode</var>.</li><li>Perform ! CreateDataPropertyOrThrow(<var>context</var>, <b>\"source\"</b>, CodePointsToString(<var>sourceText</var>)).</li></ol></li><li>Let <var>elementRecords</var> be <var>parseRecord</var>.[[Elements]].</li><li>Let <var>entryRecords</var> be <var>parseRecord</var>.[[Entries]].</li></ol></li><li>Else,<ol><li>Let <var>elementRecords</var> be a new empty List.</li><li>Let <var>entryRecords</var> be a new empty List.</li></ol></li><li>If <var>val</var> is an Object, then<ol><li>Let <var>isArray</var> be ? IsArray(<var>val</var>).</li><li>If <var>isArray</var> is <b>true</b>, then<ol><li>Let <var>elementRecordsLen</var> be the number of elements in <var>elementRecords</var>.</li><li>Let <var>len</var> be ? LengthOfArrayLike(<var>val</var>).</li><li>Let <var>index</var> be 0.</li><li>Repeat, while <var>index</var> &lt; <var>len</var>,<ol><li>Let <var>propertyKey</var> be ! ToString(𝔽(<var>index</var>)).</li><li>If <var>index</var> &lt; <var>elementRecordsLen</var>, let <var>elementRecord</var> be <var>elementRecords</var>[<var>index</var>]; else let <var>elementRecord</var> be <emu-const>empty</emu-const>.</li><li>Let <var>newElement</var> be ? InternalizeJSONProperty(<var>val</var>, <var>propertyKey</var>, <var>reviver</var>, <var>elementRecord</var>).</li><li>If <var>newElement</var> is <b>undefined</b>, then<ol><li>Perform ? <emu-meta effects=\"user-code\"><var>val</var>.[[Delete]](<var>propertyKey</var>)</emu-meta>.</li></ol></li><li>Else,<ol><li>Perform ? CreateDataProperty(<var>val</var>, <var>propertyKey</var>, <var>newElement</var>).</li></ol></li><li>Set <var>index</var> to <var>index</var> + 1.</li></ol></li></ol></li><li>Else,<ol><li>Let <var>keys</var> be ? EnumerableOwnProperties(<var>val</var>, <emu-const>key</emu-const>).</li><li>For each String <var>propertyKey</var> of <var>keys</var>, do<ol><li>If there exists an element <var>entry</var> of <var>entryRecords</var> such that <var>entry</var>.[[Key]] is <var>propertyKey</var>, let <var>entryRecord</var> be <var>entry</var>; else let <var>entryRecord</var> be <emu-const>empty</emu-const>.</li><li>Let <var>newElement</var> be ? InternalizeJSONProperty(<var>val</var>, <var>propertyKey</var>, <var>reviver</var>, <var>entryRecord</var>).</li><li>If <var>newElement</var> is <b>undefined</b>, then<ol><li>Perform ? <emu-meta effects=\"user-code\"><var>val</var>.[[Delete]](<var>propertyKey</var>)</emu-meta>.</li></ol></li><li>Else,<ol><li>Perform ? CreateDataProperty(<var>val</var>, <var>propertyKey</var>, <var>newElement</var>).</li></ol></li></ol></li></ol></li></ol></li><li>Return ? Call(<var>reviver</var>, <var>holder</var>, « <var>name</var>, <var>val</var>, <var>context</var> »).</li></ol></emu-alg>\n      ","5.2.5":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It performs a breadth-first search of the parse tree rooted at <var>root</var>, and returns the first node that is an instance of a nonterminal corresponding to a JSON value, or <emu-const>empty</emu-const> if there is no such node.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>activeFunc</var> be the active function object.</li><li>Assert: <var>activeFunc</var> is a <code>JSON.parse</code> built-in function object (see <a href=\"/structured-data#sec-json.parse\">JSON.parse</a>).</li><li>Let <var>types</var> be « <emu-nt>NullLiteral</emu-nt>, <emu-nt>BooleanLiteral</emu-nt>, <emu-nt>NumericLiteral</emu-nt>, <emu-nt>StringLiteral</emu-nt>, <emu-nt>ArrayLiteral</emu-nt>, <emu-nt>ObjectLiteral</emu-nt>, <emu-nt>UnaryExpression</emu-nt> ».</li><li>Let <var>unaryExpression</var> be <emu-const>empty</emu-const>.</li><li>Let <var>queue</var> be « <var>root</var> ».</li><li>Repeat, while <var>queue</var> is not empty,<ol><li>Let <var>candidate</var> be the first element of <var>queue</var>.</li><li>Remove the first element from <var>queue</var>.</li><li>Let <var>queuedChildren</var> be <b>false</b>.</li><li>For each nonterminal <var>type</var> of <var>types</var>, do<ol><li>If <var>candidate</var> is an instance of <var>type</var>, then<ol><li>NOTE: In the JSON grammar, a <code>number</code> token may represent a negative value. In ECMAScript, negation is represented as a unary operation in which a <emu-nt>UnaryExpression</emu-nt> parses to a <code>-</code> followed by a derived <emu-nt>UnaryExpression</emu-nt>.</li><li>If <var>type</var> is <emu-nt>UnaryExpression</emu-nt>, then<ol><li>If the parent of <var>candidate</var> is not a <emu-nt>UnaryExpression</emu-nt> Parse Node, set <var>unaryExpression</var> to <var>candidate</var>.</li></ol></li><li>Else if <var>type</var> is <emu-nt>NumericLiteral</emu-nt>, then<ol><li>Assert: <var>candidate</var> is contained within <var>unaryExpression</var>.</li><li>Return <var>unaryExpression</var>.</li></ol></li><li>Else,<ol><li>Return <var>candidate</var>.</li></ol></li></ol></li><li>If <var>queuedChildren</var> is <b>false</b>, <var>candidate</var> is an instance of a nonterminal, and <var>candidate</var> Contains <var>type</var> is <b>true</b>, then<ol><li>Let <var>children</var> be a List containing each child node of <var>candidate</var>, in order.</li><li>Set <var>queue</var> to the list-concatenation of <var>queue</var> and <var>children</var>.</li><li>Set <var>queuedChildren</var> to <b>true</b>.</li></ol></li></ol></li></ol></li><li>Return <emu-const>empty</emu-const>.</li></ol></emu-alg>\n      ","5.2.6":"<dl class=\"header\">\n        </dl>\n        <pre class=\"emu-grammar\">ArrayLiteral :\n  `[` Elision? `]`\n  `[` ElementList `]`\n  `[` ElementList `,` Elision? `]`</pre>\n        <emu-alg><ol><li>Assert: <emu-nt>Elision</emu-nt> is not present.</li><li>If <emu-nt>ElementList</emu-nt> is not present, return a new empty List.</li><li>Return the JSONArrayLiteralContentNodes of <emu-nt>ElementList</emu-nt>.</li></ol></emu-alg>\n\n        <pre class=\"emu-grammar\">ElementList : Elision? AssignmentExpression</pre>\n        <emu-alg><ol><li>Assert: <emu-nt>Elision</emu-nt> is not present.</li><li>Return « <emu-nt>AssignmentExpression</emu-nt> ».</li></ol></emu-alg>\n\n        <pre class=\"emu-grammar\">ElementList : ElementList `,` Elision? AssignmentExpression</pre>\n        <emu-alg><ol><li>Assert: <emu-nt>Elision</emu-nt> is not present.</li><li>Let <var>elements</var> be the JSONArrayLiteralContentNodes of the derived <emu-nt>ElementList</emu-nt>.</li><li>Return the list-concatenation of <var>elements</var> and « <emu-nt>AssignmentExpression</emu-nt> ».</li></ol></emu-alg>\n\n        <pre class=\"emu-grammar\">ElementList :\n  Elision? SpreadElement\n  ElementList `,` Elision? SpreadElement</pre>\n        <emu-alg><ol><li>NOTE: JSON text as specified in ECMA-404 does not include <emu-nt>SpreadElement</emu-nt>.</li><li>Assert: This step is never reached.</li></ol></emu-alg>\n      ","5.3":"<p>This function returns an object representing raw JSON text of a string, number, boolean, or null value.</p>\n      <emu-alg><ol><li>Let <var>jsonString</var> be ? ToString(<var>text</var>).</li><li>If <var>jsonString</var> is the empty String, throw a <b>SyntaxError</b> exception.</li><li>If the first code unit of <var>jsonString</var> is not either an ASCII lowercase letter code unit (0x0061 through 0x007A, inclusive), an ASCII digit code unit (0x0030 through 0x0039, inclusive), 0x0022 (QUOTATION MARK), or 0x002D (HYPHEN-MINUS), throw a <b>SyntaxError</b> exception.</li><li>If the last code unit of <var>jsonString</var> is not either an ASCII lowercase letter code unit (0x0061 through 0x007A, inclusive), an ASCII digit code unit (0x0030 through 0x0039, inclusive), or 0x0022 (QUOTATION MARK), throw a <b>SyntaxError</b> exception.</li><li>Let <var>parseResult</var> be ? ParseJSON(<var>jsonString</var>).</li><li>Assert: <var>parseResult</var>.[[Value]] is either a String, a Number, a Boolean, or <b>null</b>.</li><li>Let <var>internalSlotsList</var> be « [[IsRawJSON]] ».</li><li>Let <var>obj</var> be OrdinaryObjectCreate(<b>null</b>, <var>internalSlotsList</var>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>obj</var>, <b>\"rawJSON\"</b>, <var>jsonString</var>).</li><li>Perform ! SetIntegrityLevel(<var>obj</var>, <emu-const>frozen</emu-const>).</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n    ","5.4":"<p>This function returns a String in UTF-16 encoded JSON format representing an ECMAScript language value, or <b>undefined</b>. It can take three parameters. The <var>value</var> parameter is an ECMAScript language value, which is usually an object or array, although it can also be a String, Boolean, Number or <b>null</b>. The optional <var>replacer</var> parameter is either a function that alters the way objects and arrays are stringified, or an array of Strings and Numbers that acts as an inclusion list for selecting the object properties that will be stringified. The optional <var>space</var> parameter is a String or Number that allows the result to have white space injected into it to improve human readability.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>stack</var> be a new empty List.</li><li>Let <var>indent</var> be the empty String.</li><li>Let <var>propertyList</var> be <b>undefined</b>.</li><li>Let <var>replacerFunction</var> be <b>undefined</b>.</li><li>If <var>replacer</var> is an Object, then<ol><li>If IsCallable(<var>replacer</var>) is <b>true</b>, then<ol><li>Set <var>replacerFunction</var> to <var>replacer</var>.</li></ol></li><li>Else,<ol><li>Let <var>isArray</var> be ? IsArray(<var>replacer</var>).</li><li>If <var>isArray</var> is <b>true</b>, then<ol><li>Set <var>propertyList</var> to a new empty List.</li><li>Let <var>len</var> be ? LengthOfArrayLike(<var>replacer</var>).</li><li>Let <var>k</var> be 0.</li><li>Repeat, while <var>k</var> &lt; <var>len</var>,<ol><li>Let <var>propertyKey</var> be ! ToString(𝔽(<var>k</var>)).</li><li>Let <var>propertyValue</var> be ? Get(<var>replacer</var>, <var>propertyKey</var>).</li><li>Let <var>item</var> be <b>undefined</b>.</li><li>If <var>propertyValue</var> is a String, then<ol><li>Set <var>item</var> to <var>propertyValue</var>.</li></ol></li><li>Else if <var>propertyValue</var> is a Number, then<ol><li>Set <var>item</var> to ! ToString(<var>propertyValue</var>).</li></ol></li><li>Else if <var>propertyValue</var> is an Object, then<ol><li>If <var>propertyValue</var> has a [[StringData]] or [[NumberData]] internal slot, set <var>item</var> to ? ToString(<var>propertyValue</var>).</li></ol></li><li>If <var>item</var> is not <b>undefined</b> and <var>propertyList</var> does not contain <var>item</var>, then<ol><li>Append <var>item</var> to <var>propertyList</var>.</li></ol></li><li>Set <var>k</var> to <var>k</var> + 1.</li></ol></li></ol></li></ol></li></ol></li><li>If <var>space</var> is an Object, then<ol><li>If <var>space</var> has a [[NumberData]] internal slot, then<ol><li>Set <var>space</var> to ? ToNumber(<var>space</var>).</li></ol></li><li>Else if <var>space</var> has a [[StringData]] internal slot, then<ol><li>Set <var>space</var> to ? ToString(<var>space</var>).</li></ol></li></ol></li><li>If <var>space</var> is a Number, then<ol><li>Let <var>spaceMV</var> be ! ToIntegerOrInfinity(<var>space</var>).</li><li>Set <var>spaceMV</var> to min(10, <var>spaceMV</var>).</li><li>If <var>spaceMV</var> &lt; 1, let <var>gap</var> be the empty String; else let <var>gap</var> be the String value containing <var>spaceMV</var> occurrences of the code unit 0x0020 (SPACE).</li></ol></li><li>Else if <var>space</var> is a String, then<ol><li>If the length of <var>space</var> ≤ 10, let <var>gap</var> be <var>space</var>; else let <var>gap</var> be the substring of <var>space</var> from 0 to 10.</li></ol></li><li>Else,<ol><li>Let <var>gap</var> be the empty String.</li></ol></li><li>Let <var>wrapper</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Object.prototype%</code>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>wrapper</var>, the empty String, <var>value</var>).</li><li>Let <var>state</var> be the JSON Serialization Record { [[ReplacerFunction]]: <var>replacerFunction</var>, [[Stack]]: <var>stack</var>, [[Indent]]: <var>indent</var>, [[Gap]]: <var>gap</var>, [[PropertyList]]: <var>propertyList</var> }.</li><li>Return ? SerializeJSONProperty(<var>state</var>, the empty String, <var>wrapper</var>).</li></ol></emu-alg>\n      <p>The <b>\"length\"</b> property of this function is <b>3</b><sub>𝔽</sub>.</p>\n      <emu-note>\n        <p>JSON structures are allowed to be nested to any depth, but they must be acyclic. If <var>value</var> is or contains a cyclic structure, then this function must throw a <b>TypeError</b> exception. This is an example of a value that cannot be stringified:</p>\n        <pre><code class=\"javascript\">\n          a = [];\n          a[0] = a;\n          my_text = JSON.stringify(a); // This must throw a TypeError.\n        </code></pre>\n      </emu-note>\n      <emu-note>\n        <p>Symbolic primitive values are rendered as follows:</p>\n        <ul>\n          <li>\n            The <b>null</b> value is rendered in JSON text as the String value <b>\"null\"</b>.\n          </li>\n          <li>\n            The <b>undefined</b> value is not rendered.\n          </li>\n          <li>\n            The <b>true</b> value is rendered in JSON text as the String value <b>\"true\"</b>.\n          </li>\n          <li>\n            The <b>false</b> value is rendered in JSON text as the String value <b>\"false\"</b>.\n          </li>\n        </ul>\n      </emu-note>\n      <emu-note>\n        <p>String values are wrapped in QUOTATION MARK (<code>\"</code>) code units. The code units <code>\"</code> and <code>\\\\</code> are escaped with <code>\\\\</code> prefixes. Control characters code units are replaced with escape sequences <code>\\\\u</code>HHHH, or with the shorter forms, <code>\\\\b</code> (BACKSPACE), <code>\\\\f</code> (FORM FEED), <code>\\\\n</code> (LINE FEED), <code>\\\\r</code> (CARRIAGE RETURN), <code>\\\\t</code> (CHARACTER TABULATION).</p>\n      </emu-note>\n      <emu-note>\n        <p>Finite numbers are stringified as if by calling ToString(<var>number</var>). <b>NaN</b> and <b>Infinity</b> regardless of sign are represented as the String value <b>\"null\"</b>.</p>\n      </emu-note>\n      <emu-note>\n        <p>Values that do not have a JSON representation (such as <b>undefined</b> and functions) do not produce a String. Instead they produce the <b>undefined</b> value. In arrays these values are represented as the String value <b>\"null\"</b>. In objects an unrepresentable value causes the property to be excluded from stringification.</p>\n      </emu-note>\n      <emu-note>\n        <p>An object is rendered as U+007B (LEFT CURLY BRACKET) followed by zero or more properties, separated with a U+002C (COMMA), closed with a U+007D (RIGHT CURLY BRACKET). A property is a quoted String representing the property name, a U+003A (COLON), and then the stringified property value. An array is rendered as an opening U+005B (LEFT SQUARE BRACKET) followed by zero or more values, separated with a U+002C (COMMA), closed with a U+005D (RIGHT SQUARE BRACKET).</p>\n      </emu-note>\n\n      ","5.4.1":"<p>A <dfn variants=\"JSON Serialization Records\">JSON Serialization Record</dfn> is a Record value used to enable serialization to the JSON format.</p>\n        <p>JSON Serialization Records have the fields listed in table-json-serialization-record.</p>\n        <emu-table id=\"table-json-serialization-record\" caption=\"JSON Serialization Record Fields\">\n          <table>\n            <thead>\n              <tr>\n                <th>Field Name</th>\n                <th>Value</th>\n                <th>Meaning</th>\n              </tr>\n            </thead>\n            <tr>\n              <td>[[ReplacerFunction]]</td>\n              <td>a function object or <b>undefined</b></td>\n              <td>A function that can supply replacement values for object properties (from JSON.stringify's <var>replacer</var> parameter).</td>\n            </tr>\n            <tr>\n              <td>[[PropertyList]]</td>\n              <td>either a List of Strings or <b>undefined</b></td>\n              <td>The names of properties to include when serializing a non-array object (from JSON.stringify's <var>replacer</var> parameter).</td>\n            </tr>\n            <tr>\n              <td>[[Gap]]</td>\n              <td>a String</td>\n              <td>The unit of indentation (from JSON.stringify's <var>space</var> parameter).</td>\n            </tr>\n            <tr>\n              <td>[[Stack]]</td>\n              <td>a List of Objects</td>\n              <td>The set of nested objects that are in the process of being serialized. Used to detect cyclic structures.</td>\n            </tr>\n            <tr>\n              <td>[[Indent]]</td>\n              <td>a String</td>\n              <td>The current indentation.</td>\n            </tr>\n          </table>\n        </emu-table>\n      ","5.4.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>value</var> be ? Get(<var>holder</var>, <var>key</var>).</li><li>If <var>value</var> is an Object or <var>value</var> is a BigInt, then<ol><li>Let <var>toJSON</var> be ? GetV(<var>value</var>, <b>\"toJSON\"</b>).</li><li>If IsCallable(<var>toJSON</var>) is <b>true</b>, then<ol><li>Set <var>value</var> to ? Call(<var>toJSON</var>, <var>value</var>, « <var>key</var> »).</li></ol></li></ol></li><li>If <var>state</var>.[[ReplacerFunction]] is not <b>undefined</b>, then<ol><li>Set <var>value</var> to ? Call(<var>state</var>.[[ReplacerFunction]], <var>holder</var>, « <var>key</var>, <var>value</var> »).</li></ol></li><li>If <var>value</var> is an Object, then<ol><li>If <var>value</var> has an [[IsRawJSON]] internal slot, then<ol><li>Let <var>rawJSON</var> be ! <emu-meta suppress-effects=\"user-code\">Get(<var>value</var>, <b>\"rawJSON\"</b>)</emu-meta>.</li><li>Assert: <var>rawJSON</var> is a String.</li><li>Return <var>rawJSON</var>.</li></ol></li><li>If <var>value</var> has a [[NumberData]] internal slot, then<ol><li>Set <var>value</var> to ? ToNumber(<var>value</var>).</li></ol></li><li>Else if <var>value</var> has a [[StringData]] internal slot, then<ol><li>Set <var>value</var> to ? ToString(<var>value</var>).</li></ol></li><li>Else if <var>value</var> has a [[BooleanData]] internal slot, then<ol><li>Set <var>value</var> to <var>value</var>.[[BooleanData]].</li></ol></li><li>Else if <var>value</var> has a [[BigIntData]] internal slot, then<ol><li>Set <var>value</var> to <var>value</var>.[[BigIntData]].</li></ol></li></ol></li><li>If <var>value</var> is <b>null</b>, return <b>\"null\"</b>.</li><li>If <var>value</var> is <b>true</b>, return <b>\"true\"</b>.</li><li>If <var>value</var> is <b>false</b>, return <b>\"false\"</b>.</li><li>If <var>value</var> is a String, return QuoteJSONString(<var>value</var>).</li><li>If <var>value</var> is a Number, then<ol><li>If <var>value</var> is finite, return ! ToString(<var>value</var>).</li><li>Return <b>\"null\"</b>.</li></ol></li><li>If <var>value</var> is a BigInt, throw a <b>TypeError</b> exception.</li><li>If <var>value</var> is an Object and IsCallable(<var>value</var>) is <b>false</b>, then<ol><li>Let <var>isArray</var> be ? IsArray(<var>value</var>).</li><li>If <var>isArray</var> is <b>true</b>, then<ol><li>Return ? SerializeJSONArray(<var>state</var>, <var>value</var>).</li></ol></li><li>Return ? SerializeJSONObject(<var>state</var>, <var>value</var>).</li></ol></li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n      ","5.4.3":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It wraps <var>value</var> in 0x0022 (QUOTATION MARK) code units and escapes certain other code units within it. This operation interprets <var>value</var> as a sequence of UTF-16 encoded code points, as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>product</var> be the String value consisting solely of the code unit 0x0022 (QUOTATION MARK).</li><li>For each code point <var>cp</var> of StringToCodePoints(<var>value</var>), do<ol><li>If <var>cp</var> is listed in the “Code Point” column of table-json-single-character-escapes, then<ol><li>Set <var>product</var> to the string-concatenation of <var>product</var> and the escape sequence for <var>cp</var> as specified in the “Escape Sequence” column of the corresponding row.</li></ol></li><li>Else if <var>cp</var> has a numeric value less than 0x0020 (SPACE) or <var>cp</var> has the same numeric value as a leading surrogate or trailing surrogate, then<ol><li>Let <var>unit</var> be the code unit whose numeric value is the numeric value of <var>cp</var>.</li><li>Set <var>product</var> to the string-concatenation of <var>product</var> and UnicodeEscape(<var>unit</var>).</li></ol></li><li>Else,<ol><li>Set <var>product</var> to the string-concatenation of <var>product</var> and UTF16EncodeCodePoint(<var>cp</var>).</li></ol></li></ol></li><li>Set <var>product</var> to the string-concatenation of <var>product</var> and the code unit 0x0022 (QUOTATION MARK).</li><li>Return <var>product</var>.</li></ol></emu-alg>\n        <emu-table id=\"table-json-single-character-escapes\" caption=\"JSON Single Character Escape Sequences\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Code Point\n                </th>\n                <th>\n                  Unicode Character Name\n                </th>\n                <th>\n                  Escape Sequence\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                U+0008\n              </td>\n              <td>\n                BACKSPACE\n              </td>\n              <td>\n                <code>\\\\b</code>\n              </td>\n            </tr>\n            <tr>\n              <td>\n                U+0009\n              </td>\n              <td>\n                CHARACTER TABULATION\n              </td>\n              <td>\n                <code>\\\\t</code>\n              </td>\n            </tr>\n            <tr>\n              <td>\n                U+000A\n              </td>\n              <td>\n                LINE FEED (LF)\n              </td>\n              <td>\n                <code>\\\\n</code>\n              </td>\n            </tr>\n            <tr>\n              <td>\n                U+000C\n              </td>\n              <td>\n                FORM FEED (FF)\n              </td>\n              <td>\n                <code>\\\\f</code>\n              </td>\n            </tr>\n            <tr>\n              <td>\n                U+000D\n              </td>\n              <td>\n                CARRIAGE RETURN (CR)\n              </td>\n              <td>\n                <code>\\\\r</code>\n              </td>\n            </tr>\n            <tr>\n              <td>\n                U+0022\n              </td>\n              <td>\n                QUOTATION MARK\n              </td>\n              <td>\n                <code>\\\\\"</code>\n              </td>\n            </tr>\n            <tr>\n              <td>\n                U+005C\n              </td>\n              <td>\n                REVERSE SOLIDUS\n              </td>\n              <td>\n                <code>\\\\\\\\</code>\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n      ","5.4.4":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It represents <var>codeUnit</var> as a Unicode escape sequence.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>n</var> be the numeric value of <var>codeUnit</var>.</li><li>Assert: <var>n</var> ≤ 0xFFFF.</li><li>Let <var>hex</var> be the String representation of <var>n</var>, formatted as a lowercase hexadecimal number.</li><li>Return the string-concatenation of the code unit 0x005C (REVERSE SOLIDUS), <b>\"u\"</b>, and StringPad(<var>hex</var>, 4, <b>\"0\"</b>, <emu-const>start</emu-const>).</li></ol></emu-alg>\n      ","5.4.5":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It serializes an object.</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>state</var>.[[Stack]] contains <var>value</var>, throw a <b>TypeError</b> exception because the structure is cyclical.</li><li>Append <var>value</var> to <var>state</var>.[[Stack]].</li><li>Let <var>stepBack</var> be <var>state</var>.[[Indent]].</li><li>Set <var>state</var>.[[Indent]] to the string-concatenation of <var>state</var>.[[Indent]] and <var>state</var>.[[Gap]].</li><li>If <var>state</var>.[[PropertyList]] is not <b>undefined</b>, then<ol><li>Let <var>keys</var> be <var>state</var>.[[PropertyList]].</li></ol></li><li>Else,<ol><li>Let <var>keys</var> be ? EnumerableOwnProperties(<var>value</var>, <emu-const>key</emu-const>).</li></ol></li><li>Let <var>partial</var> be a new empty List.</li><li>For each element <var>propertyKey</var> of <var>keys</var>, do<ol><li>Let <var>strP</var> be ? SerializeJSONProperty(<var>state</var>, <var>propertyKey</var>, <var>value</var>).</li><li>If <var>strP</var> is not <b>undefined</b>, then<ol><li>Let <var>member</var> be QuoteJSONString(<var>propertyKey</var>).</li><li>Set <var>member</var> to the string-concatenation of <var>member</var> and <b>\":\"</b>.</li><li>If <var>state</var>.[[Gap]] is not the empty String, then<ol><li>Set <var>member</var> to the string-concatenation of <var>member</var> and the code unit 0x0020 (SPACE).</li></ol></li><li>Set <var>member</var> to the string-concatenation of <var>member</var> and <var>strP</var>.</li><li>Append <var>member</var> to <var>partial</var>.</li></ol></li></ol></li><li>If <var>partial</var> is empty, then<ol><li>Let <var>final</var> be <b>\"{}\"</b>.</li></ol></li><li>Else,<ol><li>If <var>state</var>.[[Gap]] is the empty String, then<ol><li>Let <var>properties</var> be the String value formed by concatenating all the element Strings of <var>partial</var> with each adjacent pair of Strings separated with the code unit 0x002C (COMMA). A comma is not inserted either before the first String or after the last String.</li><li>Let <var>final</var> be the string-concatenation of <b>\"{\"</b>, <var>properties</var>, and <b>\"}\"</b>.</li></ol></li><li>Else,<ol><li>Let <var>separator</var> be the string-concatenation of the code unit 0x002C (COMMA), the code unit 0x000A (LINE FEED), and <var>state</var>.[[Indent]].</li><li>Let <var>properties</var> be the String value formed by concatenating all the element Strings of <var>partial</var> with each adjacent pair of Strings separated with <var>separator</var>. The <var>separator</var> String is not inserted either before the first String or after the last String.</li><li>Let <var>final</var> be the string-concatenation of <b>\"{\"</b>, the code unit 0x000A (LINE FEED), <var>state</var>.[[Indent]], <var>properties</var>, the code unit 0x000A (LINE FEED), <var>stepBack</var>, and <b>\"}\"</b>.</li></ol></li></ol></li><li>Remove the last element of <var>state</var>.[[Stack]].</li><li>Set <var>state</var>.[[Indent]] to <var>stepBack</var>.</li><li>Return <var>final</var>.</li></ol></emu-alg>\n      ","5.4.6":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It serializes an array.</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>state</var>.[[Stack]] contains <var>value</var>, throw a <b>TypeError</b> exception because the structure is cyclical.</li><li>Append <var>value</var> to <var>state</var>.[[Stack]].</li><li>Let <var>stepBack</var> be <var>state</var>.[[Indent]].</li><li>Set <var>state</var>.[[Indent]] to the string-concatenation of <var>state</var>.[[Indent]] and <var>state</var>.[[Gap]].</li><li>Let <var>partial</var> be a new empty List.</li><li>Let <var>len</var> be ? LengthOfArrayLike(<var>value</var>).</li><li>Let <var>index</var> be 0.</li><li>Repeat, while <var>index</var> &lt; <var>len</var>,<ol><li>Let <var>strP</var> be ? SerializeJSONProperty(<var>state</var>, ! ToString(𝔽(<var>index</var>)), <var>value</var>).</li><li>If <var>strP</var> is <b>undefined</b>, then<ol><li>Append <b>\"null\"</b> to <var>partial</var>.</li></ol></li><li>Else,<ol><li>Append <var>strP</var> to <var>partial</var>.</li></ol></li><li>Set <var>index</var> to <var>index</var> + 1.</li></ol></li><li>If <var>partial</var> is empty, then<ol><li>Let <var>final</var> be <b>\"[]\"</b>.</li></ol></li><li>Else,<ol><li>If <var>state</var>.[[Gap]] is the empty String, then<ol><li>Let <var>properties</var> be the String value formed by concatenating all the element Strings of <var>partial</var> with each adjacent pair of Strings separated with the code unit 0x002C (COMMA). A comma is not inserted either before the first String or after the last String.</li><li>Let <var>final</var> be the string-concatenation of <b>\"[\"</b>, <var>properties</var>, and <b>\"]\"</b>.</li></ol></li><li>Else,<ol><li>Let <var>separator</var> be the string-concatenation of the code unit 0x002C (COMMA), the code unit 0x000A (LINE FEED), and <var>state</var>.[[Indent]].</li><li>Let <var>properties</var> be the String value formed by concatenating all the element Strings of <var>partial</var> with each adjacent pair of Strings separated with <var>separator</var>. The <var>separator</var> String is not inserted either before the first String or after the last String.</li><li>Let <var>final</var> be the string-concatenation of <b>\"[\"</b>, the code unit 0x000A (LINE FEED), <var>state</var>.[[Indent]], <var>properties</var>, the code unit 0x000A (LINE FEED), <var>stepBack</var>, and <b>\"]\"</b>.</li></ol></li></ol></li><li>Remove the last element of <var>state</var>.[[Stack]].</li><li>Set <var>state</var>.[[Indent]] to <var>stepBack</var>.</li><li>Return <var>final</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The representation of arrays includes only the elements in the interval from <b>+0</b><sub>𝔽</sub> (inclusive) to <code>array.length</code> (exclusive). Properties whose keys are not array indices are excluded from the stringification. An array is stringified as an opening LEFT SQUARE BRACKET, elements separated by COMMA, and a closing RIGHT SQUARE BRACKET.</p>\n        </emu-note>\n      ","5.5":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"JSON\"</b>.</p>\n      <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n    "};
const _basePath = process.env.NEXT_PUBLIC_BASE_PATH || '';
const sections = _basePath
  ? Object.fromEntries(
      Object.entries(_sections).map(([k, v]) => [
        k,
        v.replaceAll('href="/', `href="${_basePath}/`),
      ])
    )
  : _sections;
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
