// Generated from ecma262/spec.html — do not edit by hand.
const _sections = {"1":"<p>An implementation must not extend this specification in the following ways:</p>\n    <ul>\n      <li>\n        ECMAScript function objects defined using syntactic constructors in strict mode code must not be created with own properties named <b>\"caller\"</b> or <b>\"arguments\"</b>. Such own properties also must not be created for function objects defined using an <emu-nt>ArrowFunction</emu-nt>, <emu-nt>MethodDefinition</emu-nt>, <emu-nt>GeneratorDeclaration</emu-nt>, <emu-nt>GeneratorExpression</emu-nt>, <emu-nt>AsyncGeneratorDeclaration</emu-nt>, <emu-nt>AsyncGeneratorExpression</emu-nt>, <emu-nt>ClassDeclaration</emu-nt>, <emu-nt>ClassExpression</emu-nt>, <emu-nt>AsyncFunctionDeclaration</emu-nt>, <emu-nt>AsyncFunctionExpression</emu-nt>, or <emu-nt>AsyncArrowFunction</emu-nt> regardless of whether the definition is contained in strict mode code. Built-in functions, strict functions created using the Function constructor, generator functions created using the Generator constructor, async functions created using the AsyncFunction constructor, and functions created using the <code>bind</code> method also must not be created with such own properties.\n      </li>\n      <li>\n        If an implementation extends any function object with an own property named <b>\"caller\"</b> the value of that property, as observed using [[Get]] or [[GetOwnProperty]], must not be a strict function object. If it is an accessor property, the function that is the value of the property's [[Get]] attribute must never return a strict function when called.\n      </li>\n      <li>\n        Neither mapped nor unmapped arguments objects may be created with an own property named <b>\"caller\"</b>.\n      </li>\n      <li>\n        The behaviour of built-in methods which are specified in ECMA-402, such as those named <code>toLocaleString</code>, must not be extended except as specified in ECMA-402.\n      </li>\n      <li>\n        The RegExp pattern grammars in <a href=\"/text-processing#sec-patterns\">22.2.1</a> and <a href=\"/additional-ecmascript-features-for-web-browsers#sec-regular-expressions-patterns\">B.1.2</a> must not be extended to recognize any of the source characters A-Z or a-z as <emu-nt>IdentityEscape[+UnicodeMode]</emu-nt> when the <sub>[UnicodeMode]</sub> grammar parameter is present.\n      </li>\n      <li>\n        The Syntactic Grammar must not be extended in any manner that allows the token <code>:</code> to immediately follow source text that is matched by the <emu-nt>BindingIdentifier</emu-nt> nonterminal symbol.\n      </li>\n      <li>\n        When processing strict mode code, an implementation must not relax the early error rules of <a href=\"/ecmascript-language-lexical-grammar#sec-numeric-literals-early-errors\">12.9.3.1</a>.\n      </li>\n      <li>\n        <emu-nt>TemplateEscapeSequence</emu-nt> must not be extended to include <emu-nt>LegacyOctalEscapeSequence</emu-nt> or <emu-nt>NonOctalDecimalEscapeSequence</emu-nt> as defined in <a href=\"/ecmascript-language-lexical-grammar#sec-literals-string-literals\">12.9.4</a>.\n      </li>\n      <li>\n        When processing strict mode code, the extensions defined in <a href=\"/additional-ecmascript-features-for-web-browsers#sec-labelled-function-declarations\">B.3.1</a>, <a href=\"/additional-ecmascript-features-for-web-browsers#sec-block-level-function-declarations-web-legacy-compatibility-semantics\">B.3.2</a>, <a href=\"/additional-ecmascript-features-for-web-browsers#sec-functiondeclarations-in-ifstatement-statement-clauses\">B.3.3</a>, and <a href=\"/additional-ecmascript-features-for-web-browsers#sec-initializers-in-forin-statement-heads\">B.3.5</a> must not be supported.\n      </li>\n      <li>\n        When parsing for the <emu-nt>Module</emu-nt> goal symbol, the lexical grammar extensions defined in <a href=\"/additional-ecmascript-features-for-web-browsers#sec-html-like-comments\">B.1.1</a> must not be supported.\n      </li>\n      <!-- The following is so that in the future we can potentially add new arguments or support ArgumentList. -->\n      <li>\n        <emu-nt>ImportCall</emu-nt> must not be extended.\n      </li>\n    </ul>\n  ","":"<p>An implementation must report most errors at the time the relevant ECMAScript language construct is evaluated. An <dfn id=\"early-error\" variants=\"early errors\">early error</dfn> is an error that can be detected and reported prior to the evaluation of any construct in the <emu-nt>Script</emu-nt> containing the error. The presence of an early error prevents the evaluation of the construct. An implementation must report early errors in a <emu-nt>Script</emu-nt> as part of parsing that <emu-nt>Script</emu-nt> in ParseScript. Early errors in a <emu-nt>Module</emu-nt> are reported at the point when the <emu-nt>Module</emu-nt> would be evaluated and the <emu-nt>Module</emu-nt> is never initialized. Early errors in <b>eval</b> code are reported at the time <code>eval</code> is called and prevent evaluation of the <b>eval</b> code. All errors that are not early errors are runtime errors.</p>\n  <p>An implementation must report as an early error any occurrence of a condition that is listed in a “Static Semantics: Early Errors” subclause of this specification.</p>\n  <p>An implementation shall not treat other kinds of errors as early errors even if the compiler can prove that a construct cannot execute without error under any circumstances. An implementation may issue an early warning in such a case, but it should not report the error until the relevant construct is actually executed.</p>\n  <p>An implementation shall report all errors as specified, except for the following:</p>\n  <ul>\n    <li>\n      Except as restricted in <a href=\"/error-handling-and-language-extensions#sec-forbidden-extensions\">17.1</a>, a host or implementation may extend <emu-nt>Script</emu-nt> syntax, <emu-nt>Module</emu-nt> syntax, and regular expression pattern or flag syntax. To permit this, all operations (such as calling <code>eval</code>, using a regular expression literal, or using the Function or RegExp constructor) that are allowed to throw <b>SyntaxError</b> are permitted to exhibit host-defined behaviour instead of throwing <b>SyntaxError</b> when they encounter a host-defined extension to the script syntax or regular expression pattern or flag syntax.\n    </li>\n    <li>\n      Except as restricted in <a href=\"/error-handling-and-language-extensions#sec-forbidden-extensions\">17.1</a>, a host or implementation may provide additional types, values, objects, properties, and functions beyond those described in this specification. This may cause constructs (such as looking up a variable in the global scope) to have host-defined behaviour instead of throwing an error (such as <b>ReferenceError</b>).\n    </li>\n  </ul>\n\n  "};
const _basePath = process.env.NEXT_PUBLIC_BASE_PATH || '';
const sections = _basePath
  ? Object.fromEntries(
      Object.entries(_sections).map(([k, v]) => [
        k,
        v.replaceAll('href="/', `href="${_basePath}/`),
      ])
    )
  : _sections;
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
