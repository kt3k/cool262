// Generated from ecma262/spec.html — do not edit by hand.
const sections = {"1":"<p>The ECMAScript language implicitly performs automatic type conversion as needed. To clarify the semantics of certain constructs it is useful to define a set of conversion abstract operations. The conversion abstract operations are polymorphic; they can accept a value of any ECMAScript language type. But no other specification types are used with these operations.</p>\n    <p>The BigInt type has no implicit conversions in the ECMAScript language; programmers must call BigInt explicitly to convert values from other types.</p>\n\n    ","2":"","3":"","4":"<p>See Common Iteration Interfaces (27.1).</p>\n\n    ","":"<p>These operations are not a part of the ECMAScript language; they are defined here solely to aid the specification of the semantics of the ECMAScript language. Other, more specialized abstract operations are defined throughout this specification.</p>\n\n  ","1.1":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts its _input_ argument to a non-Object type. If an object is capable of converting to more than one primitive type, it may use the optional hint _preferredType_ to favour that type.</dd>\n      </dl>\n      <emu-alg><ol><li>If _input_ is an Object, then<ol><li>Let _exoticToPrim_ be ? GetMethod(_input_, %Symbol.toPrimitive%).</li><li>If _exoticToPrim_ is not *undefined*, then<ol><li>If _preferredType_ is not present, then<ol><li>Let _hint_ be *\"default\"*.</li></ol></li><li>Else if _preferredType_ is ~string~, then<ol><li>Let _hint_ be *\"string\"*.</li></ol></li><li>Else,<ol><li>Assert: _preferredType_ is ~number~.</li><li>Let _hint_ be *\"number\"*.</li></ol></li><li>Let _result_ be ? Call(_exoticToPrim_, _input_, « _hint_ »).</li><li>If _result_ is not an Object, return _result_.</li><li>Throw a *TypeError* exception.</li></ol></li><li>If _preferredType_ is not present, set _preferredType_ to ~number~.</li><li>Return ? OrdinaryToPrimitive(_input_, _preferredType_).</li></ol></li><li>Return _input_.</li></ol></emu-alg>\n      <emu-note>\n        <p>When ToPrimitive is called without a hint, then it generally behaves as if the hint were ~number~. However, objects may over-ride this behaviour by defining a %Symbol.toPrimitive% method. Of the objects defined in this specification only Dates (see 21.4.4.45) and Symbol objects (see 20.4.3.5) over-ride the default ToPrimitive behaviour. Dates treat the absence of a hint as if the hint were ~string~.</p>\n      </emu-note>\n\n      ","1.1.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If _hint_ is ~string~, then<ol><li>Let _methodNames_ be « *\"toString\"*, *\"valueOf\"* ».</li></ol></li><li>Else,<ol><li>Let _methodNames_ be « *\"valueOf\"*, *\"toString\"* ».</li></ol></li><li>For each element _name_ of _methodNames_, do<ol><li>Let _method_ be ? Get(_obj_, _name_).</li><li>If IsCallable(_method_) is *true*, then<ol><li>Let _result_ be ? Call(_method_, _obj_).</li><li>If _result_ is not an Object, return _result_.</li></ol></li></ol></li><li>Throw a *TypeError* exception.</li></ol></emu-alg>\n      ","1.2":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to a value of type Boolean.</dd>\n      </dl>\n      <emu-alg><ol><li>If _argument_ is a Boolean, return _argument_.</li><li>If _argument_ is one of *undefined*, *null*, *+0*<sub>𝔽</sub>, *-0*<sub>𝔽</sub>, *NaN*, *0*<sub>ℤ</sub>, or the empty String, return *false*.</li><li>If the host is a web browser or otherwise supports B.3.6, then<ol><li>If _argument_ is an Object and _argument_ has an [[IsHTMLDDA]] internal slot, return *false*.</li></ol></li><li>Return *true*.</li></ol></emu-alg>\n    ","1.3":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It returns _value_ converted to a Number or a BigInt.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _primValue_ be ? ToPrimitive(_value_, ~number~).</li><li>If _primValue_ is a BigInt, return _primValue_.</li><li>Return ? <emu-meta suppress-effects=\"user-code\">ToNumber(_primValue_)</emu-meta>.</li></ol></emu-alg>\n    ","1.4":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to a value of type Number.</dd>\n      </dl>\n      <emu-alg><ol><li>If _argument_ is a Number, return _argument_.</li><li>If _argument_ is either a Symbol or a BigInt, throw a *TypeError* exception.</li><li>If _argument_ is *undefined*, return *NaN*.</li><li>If _argument_ is either *null* or *false*, return *+0*<sub>𝔽</sub>.</li><li>If _argument_ is *true*, return *1*<sub>𝔽</sub>.</li><li>If _argument_ is a String, return StringToNumber(_argument_).</li><li>Assert: _argument_ is an Object.</li><li>Let _primValue_ be ? ToPrimitive(_argument_, ~number~).</li><li>Assert: _primValue_ is not an Object.</li><li>Return ? ToNumber(_primValue_).</li></ol></emu-alg>\n\n      ","1.4.1":"<p>The abstract operation StringToNumber specifies how to convert a String value to a Number value, using the following grammar.</p>\n        <h2>Syntax</h2>\n        <pre class=\"emu-grammar\">StringNumericLiteral :::\n  StrWhiteSpace?\n  StrWhiteSpace? StrNumericLiteral StrWhiteSpace?\n\nStrWhiteSpace :::\n  StrWhiteSpaceChar StrWhiteSpace?\n\nStrWhiteSpaceChar :::\n  WhiteSpace\n  LineTerminator\n\nStrNumericLiteral :::\n  StrDecimalLiteral\n  NonDecimalIntegerLiteral[~Sep]\n\nStrDecimalLiteral :::\n  StrUnsignedDecimalLiteral\n  `+` StrUnsignedDecimalLiteral\n  `-` StrUnsignedDecimalLiteral\n\nStrUnsignedDecimalLiteral :::\n  `Infinity`\n  DecimalDigits[~Sep] `.` DecimalDigits[~Sep]? ExponentPart[~Sep]?\n  `.` DecimalDigits[~Sep] ExponentPart[~Sep]?\n  DecimalDigits[~Sep] ExponentPart[~Sep]?</pre>\n        <p>All grammar symbols not explicitly defined above have the definitions used in the Lexical Grammar for numeric literals (12.9.3)</p>\n        <emu-note>\n          <p>Some differences should be noted between the syntax of a |StringNumericLiteral| and a |NumericLiteral|:</p>\n          <ul>\n            <li>\n              A |StringNumericLiteral| may include leading and/or trailing white space and/or line terminators.\n            </li>\n            <li>\n              A |StringNumericLiteral| that is decimal may have any number of leading `0` digits.\n            </li>\n            <li>\n              A |StringNumericLiteral| that is decimal may include a `+` or `-` to indicate its sign.\n            </li>\n            <li>\n              A |StringNumericLiteral| that is empty or contains only white space is converted to *+0*<sub>𝔽</sub>.\n            </li>\n            <li>\n              `Infinity` and `-Infinity` are recognized as a |StringNumericLiteral| but not as a |NumericLiteral|.\n            </li>\n            <li>\n              A |StringNumericLiteral| cannot include a |BigIntLiteralSuffix|.\n            </li>\n            <li>\n              A |StringNumericLiteral| cannot include a |NumericLiteralSeparator|.\n            </li>\n          </ul>\n        </emu-note>\n\n        ","1.4.1.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let _literal_ be ParseText(_str_, |StringNumericLiteral|).</li><li>If _literal_ is a List of errors, return *NaN*.</li><li>Return the StringNumericValue of _literal_.</li></ol></emu-alg>\n        ","1.4.1.2":"<dl class=\"header\">\n          </dl>\n          <emu-note>\n            <p>The conversion of a |StringNumericLiteral| to a Number value is similar overall to the determination of the NumericValue of a |NumericLiteral| (see 12.9.3), but some of the details are different.</p>\n          </emu-note>\n          <pre class=\"emu-grammar\">StringNumericLiteral ::: StrWhiteSpace?</pre>\n          <emu-alg><ol><li>Return *+0*<sub>𝔽</sub>.</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StringNumericLiteral ::: StrWhiteSpace? StrNumericLiteral StrWhiteSpace?</pre>\n          <emu-alg><ol><li>Return the StringNumericValue of |StrNumericLiteral|.</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StrNumericLiteral ::: NonDecimalIntegerLiteral</pre>\n          <emu-alg><ol><li>Return 𝔽(MV of |NonDecimalIntegerLiteral|).</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StrDecimalLiteral ::: `-` StrUnsignedDecimalLiteral</pre>\n          <emu-alg><ol><li>Let _a_ be the StringNumericValue of |StrUnsignedDecimalLiteral|.</li><li>If _a_ is *+0*<sub>𝔽</sub>, return *-0*<sub>𝔽</sub>.</li><li>Return -_a_.</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StrUnsignedDecimalLiteral ::: `Infinity`</pre>\n          <emu-alg><ol><li>Return *+∞*<sub>𝔽</sub>.</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StrUnsignedDecimalLiteral ::: DecimalDigits `.` DecimalDigits? ExponentPart?</pre>\n          <emu-alg><ol><li>Let _a_ be the MV of the first |DecimalDigits|.</li><li>If the second |DecimalDigits| is present, then<ol><li>Let _b_ be the MV of the second |DecimalDigits|.</li><li>Let _n_ be the number of code points in the second |DecimalDigits|.</li></ol></li><li>Else,<ol><li>Let _b_ be 0.</li><li>Let _n_ be 0.</li></ol></li><li>If |ExponentPart| is present, let _e_ be the MV of |ExponentPart|; else let _e_ be 0.</li><li>Return RoundMVResult((_a_ + (_b_ × 10<sup>-_n_</sup>)) × 10<sup>_e_</sup>).</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StrUnsignedDecimalLiteral ::: `.` DecimalDigits ExponentPart?</pre>\n          <emu-alg><ol><li>Let _b_ be the MV of |DecimalDigits|.</li><li>If |ExponentPart| is present, let _e_ be the MV of |ExponentPart|; else let _e_ be 0.</li><li>Let _n_ be the number of code points in |DecimalDigits|.</li><li>Return RoundMVResult(_b_ × 10<sup>_e_ - _n_</sup>).</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StrUnsignedDecimalLiteral ::: DecimalDigits ExponentPart?</pre>\n          <emu-alg><ol><li>Let _a_ be the MV of |DecimalDigits|.</li><li>If |ExponentPart| is present, let _e_ be the MV of |ExponentPart|; else let _e_ be 0.</li><li>Return RoundMVResult(_a_ × 10<sup>_e_</sup>).</li></ol></emu-alg>\n        ","1.4.1.3":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It converts _n_ to a Number in an implementation-defined manner. For the purposes of this abstract operation, a digit is significant if it is not zero or there is a non-zero digit to its left and there is a non-zero digit to its right. For the purposes of this abstract operation, \"the mathematical value denoted by\" a representation of a mathematical value is the inverse of \"the decimal representation of\" a mathematical value.</dd>\n          </dl>\n          <emu-alg><ol><li>If the decimal representation of _n_ has 20 or fewer significant digits, return 𝔽(_n_).</li><li>Let _option1_ be the mathematical value denoted by the result of replacing each significant digit in the decimal representation of _n_ after the 20th with a 0 digit.</li><li>Let _option2_ be the mathematical value denoted by the result of replacing each significant digit in the decimal representation of _n_ after the 20th with a 0 digit and then incrementing it at the 20th position (with carrying as necessary).</li><li>Let _chosen_ be an implementation-defined choice of either _option1_ or _option2_.</li><li>Return 𝔽(_chosen_).</li></ol></emu-alg>\n        ","1.5":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to an integer representing its Number value with fractional part truncated, or to +∞ or -∞ when that Number value is infinite.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _number_ be ? ToNumber(_argument_).</li><li>If _number_ is one of *NaN*, *+0*<sub>𝔽</sub>, or *-0*<sub>𝔽</sub>, return 0.</li><li>If _number_ is *+∞*<sub>𝔽</sub>, return +∞.</li><li>If _number_ is *-∞*<sub>𝔽</sub>, return -∞.</li><li>Return truncate(ℝ(_number_)).</li></ol></emu-alg>\n      <emu-note>\n        𝔽(ToIntegerOrInfinity(_x_)) never returns *-0*<sub>𝔽</sub> for any value of _x_. The truncation of the fractional part is performed after converting _x_ to a mathematical value.\n      </emu-note>\n    ","1.6":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It maps _int_ to one of 2<sup>_bitWidth_</sup> integers in the inclusive interval from 0 to 2<sup>_bitWidth_</sup> - 1 (if _signed_ is ~unsigned~) or -2<sup>_bitWidth_ - 1</sup> to 2<sup>_bitWidth_ - 1</sup> - 1 (if _signed_ is ~signed~).</dd>\n      </dl>\n      <emu-alg><ol><li>If _int_ = +∞ or _int_ = -∞, return 0.</li><li>Let _fixedInt_ be _int_ modulo 2<sup>_bitWidth_</sup>.</li><li>NOTE: The following step does not change the two's complement representation of _fixedInt_.</li><li>If _signed_ is ~signed~ and _fixedInt_ ≥ 2<sup>_bitWidth_ - 1</sup>, set _fixedInt_ to _fixedInt_ - 2<sup>_bitWidth_</sup>.</li><li>Return _fixedInt_.</li></ol></emu-alg>\n      <emu-note>\n        <p>ToFixedSizeInteger is idempotent: for any ECMAScript language value _x_, ToFixedSizeInteger(ToFixedSizeInteger(_x_, _signed_, _bitWidth_), _signed_, _bitWidth_) is the same as ToFixedSizeInteger(_x_, _signed_, _bitWidth_). In fact, because +∞ and -∞ are mapped to 0, this invariant even survives inversion of _signed_ (e.g., ToFixedSizeInteger(ToFixedSizeInteger(_x_, ~signed~, 32), ~unsigned~, 32) is the same as ToFixedSizeInteger(_x_, ~unsigned~, 32)).</p>\n      </emu-note>\n    ","1.7":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to one of 2<sup>32</sup> integral Number values in the inclusive interval from 𝔽(-2<sup>31</sup>) to 𝔽(2<sup>31</sup> - 1), excluding *-0*<sub>𝔽</sub>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _int_ be ? ToIntegerOrInfinity(_argument_).</li><li>Return 𝔽(ToFixedSizeInteger(_int_, ~signed~, 32)).</li></ol></emu-alg>\n    ","1.8":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to one of 2<sup>32</sup> integral Number values in the inclusive interval from *+0*<sub>𝔽</sub> to 𝔽(2<sup>32</sup> - 1).</dd>\n      </dl>\n      <emu-alg><ol><li>Let _int_ be ? ToIntegerOrInfinity(_argument_).</li><li>Return 𝔽(ToFixedSizeInteger(_int_, ~unsigned~, 32)).</li></ol></emu-alg>\n    ","1.9":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to one of 2<sup>16</sup> integral Number values in the inclusive interval from 𝔽(-2<sup>15</sup>) to 𝔽(2<sup>15</sup> - 1), excluding *-0*<sub>𝔽</sub>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _int_ be ? ToIntegerOrInfinity(_argument_).</li><li>Return 𝔽(ToFixedSizeInteger(_int_, ~signed~, 16)).</li></ol></emu-alg>\n    ","1.10":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to one of 2<sup>16</sup> integral Number values in the inclusive interval from *+0*<sub>𝔽</sub> to 𝔽(2<sup>16</sup> - 1).</dd>\n      </dl>\n      <emu-alg><ol><li>Let _int_ be ? ToIntegerOrInfinity(_argument_).</li><li>Return 𝔽(ToFixedSizeInteger(_int_, ~unsigned~, 16)).</li></ol></emu-alg>\n    ","1.11":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to one of 2<sup>8</sup> integral Number values in the inclusive interval from *-128*<sub>𝔽</sub> to *127*<sub>𝔽</sub>, excluding *-0*<sub>𝔽</sub>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _int_ be ? ToIntegerOrInfinity(_argument_).</li><li>Return 𝔽(ToFixedSizeInteger(_int_, ~signed~, 8)).</li></ol></emu-alg>\n    ","1.12":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to one of 2<sup>8</sup> integral Number values in the inclusive interval from *+0*<sub>𝔽</sub> to *255*<sub>𝔽</sub>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _int_ be ? ToIntegerOrInfinity(_argument_).</li><li>Return 𝔽(ToFixedSizeInteger(_int_, ~unsigned~, 8)).</li></ol></emu-alg>\n    ","1.13":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It clamps and rounds _argument_ to one of 2<sup>8</sup> integral Number values in the inclusive interval from *+0*<sub>𝔽</sub> to *255*<sub>𝔽</sub>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _number_ be ? ToNumber(_argument_).</li><li>If _number_ is *NaN*, return *+0*<sub>𝔽</sub>.</li><li>Let _mv_ be the extended mathematical value of _number_.</li><li>Let _clamped_ be the result of clamping _mv_ between 0 and 255.</li><li>Let _f_ be floor(_clamped_).</li><li>If _clamped_ &lt; _f_ + 0.5, return 𝔽(_f_).</li><li>If _clamped_ > _f_ + 0.5, return 𝔽(_f_ + 1).</li><li>If _f_ is even, return 𝔽(_f_).</li><li>Return 𝔽(_f_ + 1).</li></ol></emu-alg>\n      <emu-note>\n        <p>Unlike most other ECMAScript integer conversion operations, ToUint8Clamp rounds rather than truncates non-integral values. It also uses “round half to even” tie-breaking, which differs from the “round half up” tie-breaking of <emu-xref href=\"#sec-math.round\">`Math.round`</emu-xref>.</p>\n      </emu-note>\n    ","1.14":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to a BigInt value, or throws if an implicit conversion from Number would be required.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _prim_ be ? ToPrimitive(_argument_, ~number~).</li><li>Return the value that _prim_ corresponds to in table-tobigint.</li></ol></emu-alg>\n      <emu-table id=\"table-tobigint\" caption=\"BigInt Conversions\">\n        <table>\n          <thead>\n            <tr>\n              <th>\n                Argument Type\n              </th>\n              <th>\n                Result\n              </th>\n            </tr>\n          </thead>\n          <tr>\n            <td>\n              Undefined\n            </td>\n            <td>\n              Throw a *TypeError* exception.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              Null\n            </td>\n            <td>\n              Throw a *TypeError* exception.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              Boolean\n            </td>\n            <td>\n              Return `1n` if _prim_ is *true* and `0n` if _prim_ is *false*.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              BigInt\n            </td>\n            <td>\n              Return _prim_.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              Number\n            </td>\n            <td>\n              Throw a *TypeError* exception.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              String\n            </td>\n            <td>\n              <emu-alg><ol><li>Let _n_ be StringToBigInt(_prim_).</li><li>If _n_ is *undefined*, throw a *SyntaxError* exception.</li><li>Return _n_.</li></ol></emu-alg>\n            </td>\n          </tr>\n          <tr>\n            <td>\n              Symbol\n            </td>\n            <td>\n              Throw a *TypeError* exception.\n            </td>\n          </tr>\n        </table>\n      </emu-table>\n    ","1.15":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let _literal_ be ParseText(_str_, |StringIntegerLiteral|).</li><li>If _literal_ is a List of errors, return *undefined*.</li><li>Let _mv_ be the MV of _literal_.</li><li>Assert: _mv_ is an integer.</li><li>Return ℤ(_mv_).</li></ol></emu-alg>\n\n      ","1.15.1":"<p>StringToBigInt uses the following grammar.</p>\n        <h2>Syntax</h2>\n        <pre class=\"emu-grammar\">StringIntegerLiteral :::\n  StrWhiteSpace?\n  StrWhiteSpace? StrIntegerLiteral StrWhiteSpace?\n\nStrIntegerLiteral :::\n  SignedInteger[~Sep]\n  NonDecimalIntegerLiteral[~Sep]</pre>\n      ","1.15.2":"<ul>\n          <li>\n            The MV of <code class=\"emu-grammar\">StringIntegerLiteral ::: StrWhiteSpace?</code> is 0.\n          </li>\n          <li>\n            The MV of <code class=\"emu-grammar\">StringIntegerLiteral ::: StrWhiteSpace? StrIntegerLiteral StrWhiteSpace?</code> is the MV of |StrIntegerLiteral|.\n          </li>\n        </ul>\n      ","1.16":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to one of 2<sup>64</sup> BigInt values in the inclusive interval from ℤ(-2<sup>63</sup>) to ℤ(2<sup>63</sup> - 1).</dd>\n      </dl>\n      <emu-alg><ol><li>Let _int_ be ℝ(? ToBigInt(_argument_)).</li><li>Return ℤ(ToFixedSizeInteger(_int_, ~signed~, 64)).</li></ol></emu-alg>\n    ","1.17":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to one of 2<sup>64</sup> BigInt values in the inclusive interval from *0*<sub>ℤ</sub> to ℤ(2<sup>64</sup> - 1).</dd>\n      </dl>\n      <emu-alg><ol><li>Let _int_ be ℝ(? ToBigInt(_argument_)).</li><li>Return ℤ(ToFixedSizeInteger(_int_, ~unsigned~, 64)).</li></ol></emu-alg>\n    ","1.18":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to a value of type String.</dd>\n      </dl>\n      <emu-alg><ol><li>If _argument_ is a String, return _argument_.</li><li>If _argument_ is a Symbol, throw a *TypeError* exception.</li><li>If _argument_ is *undefined*, return *\"undefined\"*.</li><li>If _argument_ is *null*, return *\"null\"*.</li><li>If _argument_ is *true*, return *\"true\"*.</li><li>If _argument_ is *false*, return *\"false\"*.</li><li>If _argument_ is a Number, return Number::toString(_argument_, 10).</li><li>If _argument_ is a BigInt, return BigInt::toString(_argument_, 10).</li><li>Assert: _argument_ is an Object.</li><li>Let _primValue_ be ? ToPrimitive(_argument_, ~string~).</li><li>Assert: _primValue_ is not an Object.</li><li>Return ? ToString(_primValue_).</li></ol></emu-alg>\n    ","1.19":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to a value of type Object.</dd>\n      </dl>\n      <emu-alg><ol><li>If _argument_ is either *undefined* or *null*, throw a *TypeError* exception.</li><li>If _argument_ is a Boolean, return a new Boolean object whose [[BooleanData]] internal slot is set to _argument_. See 20.3 for a description of Boolean objects.</li><li>If _argument_ is a Number, return a new Number object whose [[NumberData]] internal slot is set to _argument_. See 21.1 for a description of Number objects.</li><li>If _argument_ is a String, return a new String object whose [[StringData]] internal slot is set to _argument_. See 22.1 for a description of String objects.</li><li>If _argument_ is a Symbol, return a new Symbol object whose [[SymbolData]] internal slot is set to _argument_. See 20.4 for a description of Symbol objects.</li><li>If _argument_ is a BigInt, return a new BigInt object whose [[BigIntData]] internal slot is set to _argument_. See 21.2 for a description of BigInt objects.</li><li>Assert: _argument_ is an Object.</li><li>Return _argument_.</li></ol></emu-alg>\n    ","1.20":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _argument_ to a value that can be used as a property key.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _key_ be ? ToPrimitive(_argument_, ~string~).</li><li>If _key_ is a Symbol, then<ol><li>Return _key_.</li></ol></li><li>Return ! ToString(_key_).</li></ol></emu-alg>\n    ","1.21":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It clamps and truncates _argument_ to a non-negative integral Number suitable for use as the length of an array-like object.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _len_ be ? ToIntegerOrInfinity(_argument_).</li><li>If _len_ ≤ 0, return *+0*<sub>𝔽</sub>.</li><li>Return 𝔽(min(_len_, 2<sup>53</sup> - 1)).</li></ol></emu-alg>\n    ","1.22":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>If _argument_ is either *\"-0\"* or exactly matches ToString(_n_) for some Number value _n_, it returns the respective Number value. Otherwise, it returns *undefined*.</dd>\n      </dl>\n      <emu-alg><ol><li>If _argument_ is *\"-0\"*, return *-0*<sub>𝔽</sub>.</li><li>Let _n_ be ! ToNumber(_argument_).</li><li>If ! ToString(_n_) is _argument_, return _n_.</li><li>Return *undefined*.</li></ol></emu-alg>\n      <p>A <dfn variants=\"canonical numeric strings\">canonical numeric string</dfn> is any String for which the CanonicalNumericIndexString abstract operation does not return *undefined*.</p>\n    ","1.23":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts _value_ to an integer and returns that integer if it is non-negative and corresponds with an integer index. Otherwise, it throws an exception.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _integer_ be ? ToIntegerOrInfinity(_value_).</li><li>If _integer_ is not in the inclusive interval from 0 to 2<sup>53</sup> - 1, throw a *RangeError* exception.</li><li>Return _integer_.</li></ol></emu-alg>\n    ","2.1":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It throws an error if _argument_ is a value that cannot be converted to an Object using ToObject.</dd>\n      </dl>\n      <emu-alg><ol><li>If _argument_ is either *undefined* or *null*, throw a *TypeError* exception.</li><li>Return ~unused~.</li></ol></emu-alg>\n    ","2.2":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If _argument_ is not an Object, return *false*.</li><li>If _argument_ is an Array exotic object, return *true*.</li><li>If _argument_ is a Proxy exotic object, then<ol><li>Perform ? ValidateNonRevokedProxy(_argument_).</li><li>Let _proxyTarget_ be _argument_.[[ProxyTarget]].</li><li>Return ? IsArray(_proxyTarget_).</li></ol></li><li>Return *false*.</li></ol></emu-alg>\n    ","2.3":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It determines if _argument_ is a callable function with a [[Call]] internal method.</dd>\n      </dl>\n      <emu-alg><ol><li>If _argument_ is not an Object, return *false*.</li><li>If _argument_ has a [[Call]] internal method, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n    ","2.4":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It determines if _argument_ is a function object with a [[Construct]] internal method.</dd>\n      </dl>\n      <emu-alg><ol><li>If _argument_ is not an Object, return *false*.</li><li>If _argument_ has a [[Construct]] internal method, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n    ","2.5":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to determine whether additional properties can be added to _obj_.</dd>\n      </dl>\n      <emu-alg><ol><li>Return ? <emu-meta effects=\"user-code\">_obj_.[[IsExtensible]]()</emu-meta>.</li></ol></emu-alg>\n    ","2.6":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If _argument_ is not an Object, return *false*.</li><li>Let _matcher_ be ? Get(_argument_, %Symbol.match%).</li><li>If _matcher_ is not *undefined*, return ToBoolean(_matcher_).</li><li>If _argument_ has a [[RegExpMatcher]] internal slot, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n    ","2.7":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It interprets _string_ as a sequence of UTF-16 encoded code points, as described in 6.1.4, and determines whether it is a <a href=\"http://www.unicode.org/glossary/#well_formed_code_unit_sequence\">well formed</a> UTF-16 sequence.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _len_ be the length of _string_.</li><li>Let _k_ be 0.</li><li>Repeat, while _k_ &lt; _len_,<ol><li>Let _cp_ be CodePointAt(_string_, _k_).</li><li>If _cp_.[[IsUnpairedSurrogate]] is *true*, return *false*.</li><li>Set _k_ to _k_ + _cp_.[[CodeUnitCount]].</li></ol></li><li>Return *true*.</li></ol></emu-alg>\n    ","2.8":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It determines whether or not the two arguments are the same type.</dd>\n      </dl>\n      <emu-alg><ol><li>If _x_ is *undefined* and _y_ is *undefined*, return *true*.</li><li>If _x_ is *null* and _y_ is *null*, return *true*.</li><li>If _x_ is a Boolean and _y_ is a Boolean, return *true*.</li><li>If _x_ is a Number and _y_ is a Number, return *true*.</li><li>If _x_ is a BigInt and _y_ is a BigInt, return *true*.</li><li>If _x_ is a Symbol and _y_ is a Symbol, return *true*.</li><li>If _x_ is a String and _y_ is a String, return *true*.</li><li>If _x_ is an Object and _y_ is an Object, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n    ","2.9":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It determines whether or not the two arguments are the same value.</dd>\n      </dl>\n      <emu-alg><ol><li>If SameType(_x_, _y_) is *false*, return *false*.</li><li>If _x_ is a Number, then<ol><li>Return Number::sameValue(_x_, _y_).</li></ol></li><li>Return SameValueNonNumber(_x_, _y_).</li></ol></emu-alg>\n      <emu-note>\n        <p>This algorithm differs from the IsStrictlyEqual Algorithm by treating all *NaN* values as equivalent and by differentiating *+0*<sub>𝔽</sub> from *-0*<sub>𝔽</sub>.</p>\n      </emu-note>\n    ","2.10":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It determines whether or not the two arguments are the same value (ignoring the difference between *+0*<sub>𝔽</sub> and *-0*<sub>𝔽</sub>).</dd>\n      </dl>\n      <emu-alg><ol><li>If SameType(_x_, _y_) is *false*, return *false*.</li><li>If _x_ is a Number, then<ol><li>Return Number::sameValueZero(_x_, _y_).</li></ol></li><li>Return SameValueNonNumber(_x_, _y_).</li></ol></emu-alg>\n      <emu-note>\n        <p>SameValueZero differs from SameValue only in that it treats *+0*<sub>𝔽</sub> and *-0*<sub>𝔽</sub> as equivalent.</p>\n      </emu-note>\n    ","2.11":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Assert: SameType(_x_, _y_) is *true*.</li><li>If _x_ is either *undefined* or *null*, return *true*.</li><li>If _x_ is a BigInt, then<ol><li>Return BigInt::equal(_x_, _y_).</li></ol></li><li>If _x_ is a String, then<ol><li>If _x_ and _y_ have the same length and the same code units in the same positions, return *true*.</li><li>Return *false*.</li></ol></li><li>If _x_ is a Boolean, then<ol><li>If _x_ is *true* and _y_ is *true*, return *true*.</li><li>If _x_ is *false* and _y_ is *false*, return *true*.</li><li>Return *false*.</li></ol></li><li>NOTE: All other ECMAScript language values are compared by identity.</li><li>If _x_ is _y_, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      <emu-note>\n        For expository purposes, some cases are handled separately within this algorithm even if it is unnecessary to do so.\n      </emu-note>\n      <emu-note>\n        The specifics of what \"_x_ is _y_\" means are detailed in 5.2.8.\n      </emu-note>\n    ","2.12":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It provides the semantics for the comparison _x_ &lt; _y_, returning *true*, *false*, or *undefined* (which indicates that the operands could not be coerced to comparable values of the same numeric type). The _leftFirst_ flag is used to control the order in which operations with potentially visible side-effects are performed upon _x_ and _y_. It is necessary because ECMAScript specifies left to right evaluation of expressions. If _leftFirst_ is *true*, the _x_ parameter corresponds to an expression that occurs to the left of the _y_ parameter's corresponding expression. If _leftFirst_ is *false*, the reverse is the case and operations must be performed upon _y_ before _x_.</dd>\n      </dl>\n      <emu-alg><ol><li>If _leftFirst_ is *true*, then<ol><li>Let _px_ be ? ToPrimitive(_x_, ~number~).</li><li>Let _py_ be ? ToPrimitive(_y_, ~number~).</li></ol></li><li>Else,<ol><li>NOTE: The order of evaluation needs to be reversed to preserve left to right evaluation.</li><li>Let _py_ be ? ToPrimitive(_y_, ~number~).</li><li>Let _px_ be ? ToPrimitive(_x_, ~number~).</li></ol></li><li>If _px_ is a String and _py_ is a String, then<ol><li>Let _lx_ be the length of _px_.</li><li>Let _ly_ be the length of _py_.</li><li>For each integer _i_ such that 0 ≤ _i_ &lt; min(_lx_, _ly_), in ascending order, do<ol><li>Let _cx_ be the numeric value of the code unit at index _i_ within _px_.</li><li>Let _cy_ be the numeric value of the code unit at index _i_ within _py_.</li><li>If _cx_ &lt; _cy_, return *true*.</li><li>If _cx_ > _cy_, return *false*.</li></ol></li><li>If _lx_ &lt; _ly_, return *true*.</li><li>Return *false*.</li></ol></li><li>If _px_ is a BigInt and _py_ is a String, then<ol><li>Let _ny_ be StringToBigInt(_py_).</li><li>If _ny_ is *undefined*, return *undefined*.</li><li>Return BigInt::lessThan(_px_, _ny_).</li></ol></li><li>If _px_ is a String and _py_ is a BigInt, then<ol><li>Let _nx_ be StringToBigInt(_px_).</li><li>If _nx_ is *undefined*, return *undefined*.</li><li>Return BigInt::lessThan(_nx_, _py_).</li></ol></li><li>NOTE: Because _px_ and _py_ are primitive values, evaluation order is not important.</li><li>Let _nx_ be ? <emu-meta suppress-effects=\"user-code\">ToNumeric(_px_)</emu-meta>.</li><li>Let _ny_ be ? <emu-meta suppress-effects=\"user-code\">ToNumeric(_py_)</emu-meta>.</li><li>If SameType(_nx_, _ny_) is *true*, then<ol><li>If _nx_ is a Number, return Number::lessThan(_nx_, _ny_).</li><li>Assert: _nx_ is a BigInt.</li><li>Return BigInt::lessThan(_nx_, _ny_).</li></ol></li><li>Assert: _nx_ is a BigInt and _ny_ is a Number, or _nx_ is a Number and _ny_ is a BigInt.</li><li>If _nx_ is *NaN* or _ny_ is *NaN*, return *undefined*.</li><li>If _nx_ is *-∞*<sub>𝔽</sub> or _ny_ is *+∞*<sub>𝔽</sub>, return *true*.</li><li>If _nx_ is *+∞*<sub>𝔽</sub> or _ny_ is *-∞*<sub>𝔽</sub>, return *false*.</li><li>If ℝ(_nx_) &lt; ℝ(_ny_), return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      <emu-note>\n        <p>Step step-arc-string-check differs from step step-binary-op-string-check in the algorithm that handles the addition operator `+` (13.15.3) by using the logical-and operation instead of the logical-or operation.</p>\n      </emu-note>\n      <emu-note>\n        <p>The comparison of Strings uses a simple lexicographic ordering on sequences of UTF-16 code unit values. There is no attempt to use the more complex, semantically oriented definitions of character or string equality and collating order defined in the Unicode specification. Therefore String values that are canonically equal according to the Unicode Standard but not in the same normalization form could test as unequal. Also note that lexicographic ordering by <em>code unit</em> differs from ordering by <em>code point</em> for Strings containing surrogate pairs.</p>\n      </emu-note>\n    ","2.13":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It provides the semantics for the `==` operator.</dd>\n      </dl>\n      <emu-alg><ol><li>If SameType(_x_, _y_) is *true*, then<ol><li>Return IsStrictlyEqual(_x_, _y_).</li></ol></li><li>If _x_ is *null* and _y_ is *undefined*, return *true*.</li><li>If _x_ is *undefined* and _y_ is *null*, return *true*.</li><li>If the host is a web browser or otherwise supports B.3.6, then<ol><li>If _x_ is an Object, _x_ has an [[IsHTMLDDA]] internal slot, and _y_ is either *undefined* or *null*, return *true*.</li><li>If _x_ is either *undefined* or *null*, _y_ is an Object, and _y_ has an [[IsHTMLDDA]] internal slot, return *true*.</li></ol></li><li>If _x_ is a Number and _y_ is a String, return ! IsLooselyEqual(_x_, ! ToNumber(_y_)).</li><li>If _x_ is a String and _y_ is a Number, return ! IsLooselyEqual(! ToNumber(_x_), _y_).</li><li>If _x_ is a BigInt and _y_ is a String, then<ol><li>Let _n_ be StringToBigInt(_y_).</li><li>If _n_ is *undefined*, return *false*.</li><li>Return ! IsLooselyEqual(_x_, _n_).</li></ol></li><li>If _x_ is a String and _y_ is a BigInt, return ! IsLooselyEqual(_y_, _x_).</li><li>If _x_ is a Boolean, return ! IsLooselyEqual(! ToNumber(_x_), _y_).</li><li>If _y_ is a Boolean, return ! IsLooselyEqual(_x_, ! ToNumber(_y_)).</li><li>If _x_ is either a String, a Number, a BigInt, or a Symbol and _y_ is an Object, return ! IsLooselyEqual(_x_, ? ToPrimitive(_y_)).</li><li>If _x_ is an Object and _y_ is either a String, a Number, a BigInt, or a Symbol, return ! IsLooselyEqual(? ToPrimitive(_x_), _y_).</li><li>If _x_ is a BigInt and _y_ is a Number, or if _x_ is a Number and _y_ is a BigInt, then<ol><li>If _x_ is not finite or _y_ is not finite, return *false*.</li><li>If ℝ(_x_) = ℝ(_y_), return *true*.</li><li>Return *false*.</li></ol></li><li>Return *false*.</li></ol></emu-alg>\n    ","2.14":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It provides the semantics for the `===` operator.</dd>\n      </dl>\n      <emu-alg><ol><li>If SameType(_x_, _y_) is *false*, return *false*.</li><li>If _x_ is a Number, then<ol><li>Return Number::equal(_x_, _y_).</li></ol></li><li>Return SameValueNonNumber(_x_, _y_).</li></ol></emu-alg>\n      <emu-note>\n        <p>This algorithm differs from the SameValue Algorithm in its treatment of signed zeroes and NaNs.</p>\n      </emu-note>\n    ","3.1":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is the source of all ECMAScript objects that are created algorithmically, including both ordinary objects and exotic objects. It factors out common steps used in creating all objects, and centralizes object creation.</dd>\n      </dl>\n\n      <emu-alg><ol><li>Set _internalSlotsList_ to the list-concatenation of _internalSlotsList_ and « [[PrivateElements]] ».</li><li>Let _obj_ be a newly created object with an internal slot for each name in _internalSlotsList_.</li><li>NOTE: As described in 6.1.7.2, the initial value of each such internal slot is *undefined* unless specified otherwise.</li><li>Set _obj_.[[PrivateElements]] to a new empty List.</li><li>Set _obj_'s essential internal methods to the default ordinary object definitions specified in 10.1.</li><li>Assert: If the caller will not be overriding both _obj_'s [[GetPrototypeOf]] and [[SetPrototypeOf]] essential internal methods, then _internalSlotsList_ contains [[Prototype]].</li><li>Assert: If the caller will not be overriding all of _obj_'s [[SetPrototypeOf]], [[IsExtensible]], and [[PreventExtensions]] essential internal methods, then _internalSlotsList_ contains [[Extensible]].</li><li>If _internalSlotsList_ contains [[Extensible]], set _obj_.[[Extensible]] to *true*.</li><li>Return _obj_.</li></ol></emu-alg>\n\n      <emu-note>\n        <p>Within this specification, exotic objects are created in abstract operations such as ArrayCreate and BoundFunctionCreate by first calling MakeBasicObject to obtain a basic, foundational object, and then overriding some or all of that object's internal methods. In order to encapsulate exotic object creation, the object's essential internal methods are never modified outside those operations.</p>\n      </emu-note>\n    ","3.2":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to retrieve the value of a specific property of an object.</dd>\n      </dl>\n      <emu-alg><ol><li>Return ? <emu-meta effects=\"user-code\">_obj_.[[Get]](_propertyKey_, _obj_)</emu-meta>.</li></ol></emu-alg>\n    ","3.3":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to retrieve the value of a specific property of an ECMAScript language value. If the value is not an object, the property lookup is performed using a wrapper object appropriate for the type of the value.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _obj_ be ? ToObject(_value_).</li><li>Return ? <emu-meta effects=\"user-code\">_obj_.[[Get]](_propertyKey_, _value_)</emu-meta>.</li></ol></emu-alg>\n    ","3.4":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to set the value of a specific property of an object. _value_ is the new value for the property.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _success_ be ? <emu-meta effects=\"user-code\">_obj_.[[Set]](_propertyKey_, _value_, _obj_)</emu-meta>.</li><li>If _success_ is *false* and _throw_ is *true*, throw a *TypeError* exception.</li><li>Return ~unused~.</li></ol></emu-alg>\n    ","3.5":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to create a new own property of an object.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _newDesc_ be the PropertyDescriptor { [[Value]]: _value_, [[Writable]]: *true*, [[Enumerable]]: *true*, [[Configurable]]: *true* }.</li><li>Return ? <emu-meta effects=\"user-code\">_obj_.[[DefineOwnProperty]](_propertyKey_, _newDesc_)</emu-meta>.</li></ol></emu-alg>\n      <emu-note>\n        <p>This abstract operation creates a property whose attributes are set to the same defaults used for properties created by the ECMAScript language assignment operator. Normally, the property will not already exist. If it does exist and is not configurable or if _obj_ is not extensible, [[DefineOwnProperty]] will return *false*.</p>\n      </emu-note>\n    ","3.6":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to create a new own property of an object. It throws a *TypeError* exception if the requested property update cannot be performed.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _success_ be ? CreateDataProperty(_obj_, _propertyKey_, _value_).</li><li>If _success_ is *false*, throw a *TypeError* exception.</li><li>Return ~unused~.</li></ol></emu-alg>\n      <emu-note>\n        <p>This abstract operation creates a property whose attributes are set to the same defaults used for properties created by the ECMAScript language assignment operator. Normally, the property will not already exist. If it does exist and is not configurable or if _obj_ is not extensible, [[DefineOwnProperty]] will return *false* causing this operation to throw a *TypeError* exception.</p>\n      </emu-note>\n    ","3.7":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to create a new non-enumerable own property of an ordinary object.</dd>\n      </dl>\n      <emu-alg><ol><li>Assert: _obj_ is an ordinary, extensible object with no non-configurable properties.</li><li>Let _newDesc_ be the PropertyDescriptor { [[Value]]: _value_, [[Writable]]: *true*, [[Enumerable]]: *false*, [[Configurable]]: *true* }.</li><li>Perform ! DefinePropertyOrThrow(_obj_, _propertyKey_, _newDesc_).</li><li>Return ~unused~.</li></ol></emu-alg>\n      <emu-note>\n        <p>This abstract operation creates a property whose attributes are set to the same defaults used for properties created by the ECMAScript language assignment operator except it is not enumerable. Normally, the property will not already exist. If it does exist, DefinePropertyOrThrow is guaranteed to complete normally.</p>\n      </emu-note>\n    ","3.8":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to call the [[DefineOwnProperty]] internal method of an object in a manner that will throw a *TypeError* exception if the requested property update cannot be performed.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _success_ be ? <emu-meta effects=\"user-code\">_obj_.[[DefineOwnProperty]](_propertyKey_, _desc_)</emu-meta>.</li><li>If _success_ is *false*, throw a *TypeError* exception.</li><li>Return ~unused~.</li></ol></emu-alg>\n    ","3.9":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to remove a specific own property of an object. It throws an exception if the property is not configurable.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _success_ be ? <emu-meta effects=\"user-code\">_obj_.[[Delete]](_propertyKey_)</emu-meta>.</li><li>If _success_ is *false*, throw a *TypeError* exception.</li><li>Return ~unused~.</li></ol></emu-alg>\n    ","3.10":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to get the value of a specific property of an ECMAScript language value when the value of the property is expected to be a function.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _func_ be ? GetV(_value_, _propertyKey_).</li><li>If _func_ is either *undefined* or *null*, return *undefined*.</li><li>If IsCallable(_func_) is *false*, throw a *TypeError* exception.</li><li>Return _func_.</li></ol></emu-alg>\n    ","3.11":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to determine whether an object has a property with the specified property key. The property may be either own or inherited.</dd>\n      </dl>\n      <emu-alg><ol><li>Return ? <emu-meta effects=\"user-code\">_obj_.[[HasProperty]](_propertyKey_)</emu-meta>.</li></ol></emu-alg>\n    ","3.12":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to determine whether an object has an own property with the specified property key.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _desc_ be ? <emu-meta effects=\"user-code\">_obj_.[[GetOwnProperty]](_propertyKey_)</emu-meta>.</li><li>If _desc_ is *undefined*, return *false*.</li><li>Return *true*.</li></ol></emu-alg>\n    ","3.13":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to call the [[Call]] internal method of a function object. _func_ is the function object, _thisValue_ is an ECMAScript language value that is the *this* value of the [[Call]], and _argumentsList_ is the value passed to the corresponding argument of the internal method. If _argumentsList_ is not present, a new empty List is used as its value.</dd>\n      </dl>\n      <emu-alg><ol><li>If _argumentsList_ is not present, set _argumentsList_ to a new empty List.</li><li>If IsCallable(_func_) is *false*, throw a *TypeError* exception.</li><li>Return ? <emu-meta effects=\"user-code\">_func_.[[Call]](_thisValue_, _argumentsList_)</emu-meta>.</li></ol></emu-alg>\n    ","3.14":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to call the [[Construct]] internal method of a function object. _argumentsList_ and _newTarget_ are the values to be passed as the corresponding arguments of the internal method. If _argumentsList_ is not present, a new empty List is used as its value. If _newTarget_ is not present, _constructor_ is used as its value.</dd>\n      </dl>\n      <emu-alg><ol><li>If _newTarget_ is not present, set _newTarget_ to _constructor_.</li><li>If _argumentsList_ is not present, set _argumentsList_ to a new empty List.</li><li>Return ? <emu-meta effects=\"user-code\">_constructor_.[[Construct]](_argumentsList_, _newTarget_)</emu-meta>.</li></ol></emu-alg>\n      <emu-note>\n        <p>If _newTarget_ is not present, this operation is equivalent to: `new F(...argumentsList)`</p>\n      </emu-note>\n    ","3.15":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to fix the set of own properties of an object.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _status_ be ? _obj_.[[PreventExtensions]]().</li><li>If _status_ is *false*, return *false*.</li><li>Let _keys_ be ? _obj_.[[OwnPropertyKeys]]().</li><li>If _level_ is ~sealed~, then<ol><li>For each element _key_ of _keys_, do<ol><li>Perform ? DefinePropertyOrThrow(_obj_, _key_, PropertyDescriptor { [[Configurable]]: *false* }).</li></ol></li></ol></li><li>Else,<ol><li>Assert: _level_ is ~frozen~.</li><li>For each element _key_ of _keys_, do<ol><li>Let _currentDesc_ be ? <emu-meta effects=\"user-code\">_obj_.[[GetOwnProperty]](_key_)</emu-meta>.</li><li>If _currentDesc_ is not *undefined*, then<ol><li>If IsAccessorDescriptor(_currentDesc_) is *true*, then<ol><li>Let _desc_ be the PropertyDescriptor { [[Configurable]]: *false* }.</li></ol></li><li>Else,<ol><li>Let _desc_ be the PropertyDescriptor { [[Configurable]]: *false*, [[Writable]]: *false* }.</li></ol></li><li>Perform ? DefinePropertyOrThrow(_obj_, _key_, _desc_).</li></ol></li></ol></li></ol></li><li>Return *true*.</li></ol></emu-alg>\n    ","3.16":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to determine if the set of own properties of an object are fixed.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _extensible_ be ? IsExtensible(_obj_).</li><li>If _extensible_ is *true*, return *false*.</li><li>NOTE: If the object is extensible, none of its properties are examined.</li><li>Let _keys_ be ? _obj_.[[OwnPropertyKeys]]().</li><li>For each element _key_ of _keys_, do<ol><li>Let _currentDesc_ be ? <emu-meta effects=\"user-code\">_obj_.[[GetOwnProperty]](_key_)</emu-meta>.</li><li>If _currentDesc_ is not *undefined*, then<ol><li>If _currentDesc_.[[Configurable]] is *true*, return *false*.</li><li>If _level_ is ~frozen~ and IsDataDescriptor(_currentDesc_) is *true*, then<ol><li>If _currentDesc_.[[Writable]] is *true*, return *false*.</li></ol></li></ol></li></ol></li><li>Return *true*.</li></ol></emu-alg>\n    ","3.17":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to create an Array whose elements are provided by _elements_.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _array_ be ! ArrayCreate(0).</li><li>Let _n_ be 0.</li><li>For each element _e_ of _elements_, do<ol><li>Perform ! CreateDataPropertyOrThrow(_array_, ! ToString(𝔽(_n_)), _e_).</li><li>Set _n_ to _n_ + 1.</li></ol></li><li>Return _array_.</li></ol></emu-alg>\n    ","3.18":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It returns the value of the *\"length\"* property of an array-like object.</dd>\n      </dl>\n      <emu-alg><ol><li>Return ℝ(? ToLength(? Get(_obj_, *\"length\"*))).</li></ol></emu-alg>\n      <p>An <dfn variants=\"array-like objects\">array-like object</dfn> is any object for which this operation returns a normal completion.</p>\n      <emu-note>\n        Typically, an array-like object would also have some properties with integer index names. However, that is not a requirement of this definition.\n      </emu-note>\n      <emu-note>\n        Arrays and String objects are examples of array-like objects.\n      </emu-note>\n    ","3.19":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to create a List value whose elements are provided by the indexed properties of _obj_. _validElementTypes_ indicates the types of values that are allowed as elements.</dd>\n      </dl>\n      <emu-alg><ol><li>If _validElementTypes_ is not present, set _validElementTypes_ to ~all~.</li><li>If _obj_ is not an Object, throw a *TypeError* exception.</li><li>Let _len_ be ? LengthOfArrayLike(_obj_).</li><li>Let _list_ be a new empty List.</li><li>Let _index_ be 0.</li><li>Repeat, while _index_ &lt; _len_,<ol><li>Let _indexName_ be ! ToString(𝔽(_index_)).</li><li>Let _next_ be ? Get(_obj_, _indexName_).</li><li>If _validElementTypes_ is ~property-key~ and _next_ is not a property key, throw a *TypeError* exception.</li><li>Append _next_ to _list_.</li><li>Set _index_ to _index_ + 1.</li></ol></li><li>Return _list_.</li></ol></emu-alg>\n    ","3.20":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to call a method property of an ECMAScript language value. _value_ serves as both the lookup point for the property and the *this* value of the call. _argumentsList_ is the list of arguments values passed to the method. If _argumentsList_ is not present, a new empty List is used as its value.</dd>\n      </dl>\n\n      <emu-alg><ol><li>If _argumentsList_ is not present, set _argumentsList_ to a new empty List.</li><li>Let _func_ be ? GetV(_value_, _propertyKey_).</li><li>Return ? Call(_func_, _value_, _argumentsList_).</li></ol></emu-alg>\n    ","3.21":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It implements the default algorithm for determining if _instance_ inherits from the instance object inheritance path provided by _constructor_.</dd>\n      </dl>\n      <emu-alg><ol><li>If IsCallable(_constructor_) is *false*, return *false*.</li><li>If _constructor_ has a [[BoundTargetFunction]] internal slot, then<ol><li>Let _boundConstructor_ be _constructor_.[[BoundTargetFunction]].</li><li>Return ? InstanceofOperator(_instance_, _boundConstructor_).</li></ol></li><li>If _instance_ is not an Object, return *false*.</li><li>Let _proto_ be ? Get(_constructor_, *\"prototype\"*).</li><li>If _proto_ is not an Object, throw a *TypeError* exception.</li><li>Repeat,<ol><li>Set _instance_ to ? <emu-meta effects=\"user-code\">_instance_.[[GetPrototypeOf]]()</emu-meta>.</li><li>If _instance_ is *null*, return *false*.</li><li>If SameValue(_proto_, _instance_) is *true*, return *true*.</li></ol></li></ol></emu-alg>\n    ","3.22":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to retrieve the constructor that should be used to create new objects that are derived from _obj_. _defaultConstructor_ is the constructor to use if a constructor %Symbol.species% property cannot be found starting from _obj_.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _constructor_ be ? Get(_obj_, *\"constructor\"*).</li><li>If _constructor_ is *undefined*, return _defaultConstructor_.</li><li>If _constructor_ is not an Object, throw a *TypeError* exception.</li><li>Let _species_ be ? Get(_constructor_, %Symbol.species%).</li><li>If _species_ is either *undefined* or *null*, return _defaultConstructor_.</li><li>If IsConstructor(_species_) is *true*, return _species_.</li><li>Throw a *TypeError* exception.</li></ol></emu-alg>\n    ","3.23":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let _ownKeys_ be ? <emu-meta effects=\"user-code\">_obj_.[[OwnPropertyKeys]]()</emu-meta>.</li><li>Let _results_ be a new empty List.</li><li>For each element _key_ of _ownKeys_, do<ol><li>If _key_ is a String, then<ol><li>Let _desc_ be ? <emu-meta effects=\"user-code\">_obj_.[[GetOwnProperty]](_key_)</emu-meta>.</li><li>If _desc_ is not *undefined* and _desc_.[[Enumerable]] is *true*, then<ol><li>If _kind_ is ~key~, then<ol><li>Append _key_ to _results_.</li></ol></li><li>Else,<ol><li>Let _value_ be ? Get(_obj_, _key_).</li><li>If _kind_ is ~value~, then<ol><li>Append _value_ to _results_.</li></ol></li><li>Else,<ol><li>Assert: _kind_ is ~key+value~.</li><li>Let _entry_ be CreateArrayFromList(« _key_, _value_ »).</li><li>Append _entry_ to _results_.</li></ol></li></ol></li></ol></li></ol></li></ol></li><li>Return _results_.</li></ol></emu-alg>\n    ","3.24":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If _func_ has a [[Realm]] internal slot, then<ol><li>Return _func_.[[Realm]].</li></ol></li><li>If _func_ is a bound function exotic object, then<ol><li>Let _boundTargetFunction_ be _func_.[[BoundTargetFunction]].</li><li>Return ? GetFunctionRealm(_boundTargetFunction_).</li></ol></li><li>If _func_ is a Proxy exotic object, then<ol><li>Perform ? ValidateNonRevokedProxy(_func_).</li><li>Let _proxyTarget_ be _func_.[[ProxyTarget]].</li><li>Assert: _proxyTarget_ is a function object.</li><li>Return ? GetFunctionRealm(_proxyTarget_).</li></ol></li><li>Return the current Realm Record.</li></ol></emu-alg>\n      <emu-note>\n        <p>Step step-getfunctionrealm-default-return will only be reached if _func_ is a non-standard function exotic object that does not have a [[Realm]] internal slot.</p>\n      </emu-note>\n    ","3.25":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If _source_ is either *undefined* or *null*, return ~unused~.</li><li>Let _from_ be ! ToObject(_source_).</li><li>Let _keys_ be ? <emu-meta effects=\"user-code\">_from_.[[OwnPropertyKeys]]()</emu-meta>.</li><li>For each element _nextKey_ of _keys_, do<ol><li>Let _excluded_ be *false*.</li><li>For each element _e_ of _excludedItems_, do<ol><li>If SameValue(_e_, _nextKey_) is *true*, then<ol><li>Set _excluded_ to *true*.</li></ol></li></ol></li><li>If _excluded_ is *false*, then<ol><li>Let _desc_ be ? <emu-meta effects=\"user-code\">_from_.[[GetOwnProperty]](_nextKey_)</emu-meta>.</li><li>If _desc_ is not *undefined* and _desc_.[[Enumerable]] is *true*, then<ol><li>Let _propValue_ be ? Get(_from_, _nextKey_).</li><li>Perform ! CreateDataPropertyOrThrow(_target_, _nextKey_, _propValue_).</li></ol></li></ol></li></ol></li><li>Return ~unused~.</li></ol></emu-alg>\n      <emu-note>\n        <p>The target passed in here is always a newly created object which is not directly accessible in case of an error being thrown.</p>\n      </emu-note>\n    ","3.26":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If _obj_.[[PrivateElements]] contains a PrivateElement _pe_ such that _pe_.[[Key]] is _privateName_, then<ol><li>Return _pe_.</li></ol></li><li>Return ~empty~.</li></ol></emu-alg>\n    ","3.27":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If the host is a web browser, then<ol><li>Perform ? HostEnsureCanAddPrivateElement(_obj_).</li></ol></li><li>Let _entry_ be PrivateElementFind(_obj_, _privateName_).</li><li>If _entry_ is not ~empty~, throw a *TypeError* exception.</li><li>Append PrivateElement { [[Key]]: _privateName_, [[Kind]]: ~field~, [[Value]]: _value_ } to _obj_.[[PrivateElements]].</li><li>Return ~unused~.</li></ol></emu-alg>\n    ","3.28":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Assert: _method_.[[Kind]] is either ~method~ or ~accessor~.</li><li>If the host is a web browser, then<ol><li>Perform ? HostEnsureCanAddPrivateElement(_obj_).</li></ol></li><li>Let _entry_ be PrivateElementFind(_obj_, _method_.[[Key]]).</li><li>If _entry_ is not ~empty~, throw a *TypeError* exception.</li><li>Append _method_ to _obj_.[[PrivateElements]].</li><li>Return ~unused~.</li></ol></emu-alg>\n      <emu-note>\n        <p>The values for private methods and accessors are shared across instances. This operation does not create a new copy of the method or accessor.</p>\n      </emu-note>\n    ","3.29":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It allows host environments to prevent the addition of private elements to particular host-defined exotic objects.</dd>\n      </dl>\n      <p>An implementation of HostEnsureCanAddPrivateElement must conform to the following requirements:</p>\n      <ul>\n        <li>If _obj_ is not a host-defined exotic object, this abstract operation must return NormalCompletion(~unused~) and perform no other steps.</li>\n        <li>Any two calls of this abstract operation with the same argument must return the same kind of Completion Record.</li>\n      </ul>\n      <p>The default implementation of HostEnsureCanAddPrivateElement is to return NormalCompletion(~unused~).</p>\n      <p>This abstract operation is only invoked by ECMAScript hosts that are web browsers.</p>\n    ","3.30":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let _entry_ be PrivateElementFind(_obj_, _privateName_).</li><li>If _entry_ is ~empty~, throw a *TypeError* exception.</li><li>If _entry_.[[Kind]] is either ~field~ or ~method~, then<ol><li>Return _entry_.[[Value]].</li></ol></li><li>Assert: _entry_.[[Kind]] is ~accessor~.</li><li>If _entry_.[[Get]] is *undefined*, throw a *TypeError* exception.</li><li>Let _getter_ be _entry_.[[Get]].</li><li>Return ? Call(_getter_, _obj_).</li></ol></emu-alg>\n    ","3.31":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let _entry_ be PrivateElementFind(_obj_, _privateName_).</li><li>If _entry_ is ~empty~, throw a *TypeError* exception.</li><li>If _entry_.[[Kind]] is ~method~, throw a *TypeError* exception.</li><li>If _entry_.[[Kind]] is ~field~, then<ol><li>Set _entry_.[[Value]] to _value_.</li></ol></li><li>Else,<ol><li>Assert: _entry_.[[Kind]] is ~accessor~.</li><li>If _entry_.[[Set]] is *undefined*, throw a *TypeError* exception.</li><li>Let _setter_ be _entry_.[[Set]].</li><li>Perform ? Call(_setter_, _obj_, « _value_ »).</li></ol></li><li>Return ~unused~.</li></ol></emu-alg>\n    ","3.32":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let _fieldName_ be _fieldRecord_.[[Name]].</li><li>Let _initializer_ be _fieldRecord_.[[Initializer]].</li><li>If _initializer_ is not ~empty~, then<ol><li>Let _initValue_ be ? Call(_initializer_, _receiver_).</li></ol></li><li>Else,<ol><li>Let _initValue_ be *undefined*.</li></ol></li><li>If _fieldName_ is a Private Name, then<ol><li>Perform ? PrivateFieldAdd(_receiver_, _fieldName_, _initValue_).</li></ol></li><li>Else,<ol><li>Assert: _fieldName_ is a property key.</li><li>Perform ? CreateDataPropertyOrThrow(_receiver_, _fieldName_, _initValue_).</li></ol></li><li>Return ~unused~.</li></ol></emu-alg>\n    ","3.33":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let _methods_ be _constructor_.[[PrivateMethods]].</li><li>For each PrivateElement _method_ of _methods_, do<ol><li>Perform ? PrivateMethodOrAccessorAdd(_obj_, _method_).</li></ol></li><li>Let _fields_ be _constructor_.[[Fields]].</li><li>For each element _fieldRecord_ of _fields_, do<ol><li>Perform ? DefineField(_obj_, _fieldRecord_).</li></ol></li><li>Return ~unused~.</li></ol></emu-alg>\n    ","3.34":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>For each Record { [[Key]], [[Elements]] } _group_ of _groups_, do<ol><li>If SameValue(_group_.[[Key]], _key_) is *true*, then<ol><li>Assert: Exactly one element of _groups_ meets this criterion.</li><li>Append _value_ to _group_.[[Elements]].</li><li>Return ~unused~.</li></ol></li></ol></li><li>Let _group_ be the Record { [[Key]]: _key_, [[Elements]]: « _value_ » }.</li><li>Append _group_ to _groups_.</li><li>Return ~unused~.</li></ol></emu-alg>\n    ","3.35":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Perform ? RequireObjectCoercible(_items_).</li><li>If IsCallable(_callback_) is *false*, throw a *TypeError* exception.</li><li>Let _groups_ be a new empty List.</li><li>Let _iteratorRecord_ be ? GetIterator(_items_, ~sync~).</li><li>Let _k_ be 0.</li><li>Repeat,<ol><li>If _k_ ≥ 2<sup>53</sup> - 1, then<ol><li>Let _error_ be ThrowCompletion(a newly created *TypeError* object).</li><li>Return ? IteratorClose(_iteratorRecord_, _error_).</li></ol></li><li>Let _next_ be ? IteratorStepValue(_iteratorRecord_).</li><li>If _next_ is ~done~, then<ol><li>Return _groups_.</li></ol></li><li>Let _value_ be _next_.</li><li>Let _key_ be Completion(Call(_callback_, *undefined*, « _value_, 𝔽(_k_) »)).</li><li>IfAbruptCloseIterator(_key_, _iteratorRecord_).</li><li>If _keyCoercion_ is ~property~, then<ol><li>Set _key_ to Completion(ToPropertyKey(_key_)).</li><li>IfAbruptCloseIterator(_key_, _iteratorRecord_).</li></ol></li><li>Else,<ol><li>Assert: _keyCoercion_ is ~collection~.</li><li>Set _key_ to CanonicalizeKeyedCollectionKey(_key_).</li></ol></li><li>Perform AddValueToKeyedGroup(_groups_, _key_, _value_).</li><li>Set _k_ to _k_ + 1.</li></ol></li></ol></emu-alg>\n    ","3.36":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If _options_ is *undefined*, then<ol><li>Return OrdinaryObjectCreate(*null*).</li></ol></li><li>If _options_ is an Object, then<ol><li>Return _options_.</li></ol></li><li>Throw a *TypeError* exception.</li></ol></emu-alg>\n    ","3.37":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If _thisValue_ is not an Object, then<ol><li>Throw a *TypeError* exception.</li></ol></li><li>If SameValue(_thisValue_, _home_) is *true*, then<ol><li>NOTE: Throwing here emulates assignment to a non-writable data property on the _home_ object in strict mode code.</li><li>Throw a *TypeError* exception.</li></ol></li><li>Let _desc_ be ? _thisValue_.[[GetOwnProperty]](_propertyKey_).</li><li>If _desc_ is *undefined*, then<ol><li>Perform ? CreateDataPropertyOrThrow(_thisValue_, _propertyKey_, _value_).</li></ol></li><li>Else,<ol><li>Perform ? Set(_thisValue_, _propertyKey_, _value_, *true*).</li></ol></li><li>Return ~unused~.</li></ol></emu-alg>\n    ","4.1":"<p>An <dfn variants=\"Iterator Records\">Iterator Record</dfn> is a Record value used to encapsulate an iterator or async iterator along with the `next` method.</p>\n      <p>Iterator Records have the fields listed in table-iterator-record-fields.</p>\n      <emu-table id=\"table-iterator-record-fields\" caption=\"Iterator Record Fields\">\n        <table>\n          <thead>\n            <tr>\n              <th>\n                Field Name\n              </th>\n              <th>\n                Value\n              </th>\n              <th>\n                Meaning\n              </th>\n            </tr>\n          </thead>\n          <tr>\n            <td>\n              [[Iterator]]\n            </td>\n            <td>\n              an Object\n            </td>\n            <td>\n              An object that conforms to the iterator interface or the async iterator interface.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[NextMethod]]\n            </td>\n            <td>\n              an ECMAScript language value\n            </td>\n            <td>\n              The `next` method of the [[Iterator]] object.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[Done]]\n            </td>\n            <td>\n              a Boolean\n            </td>\n            <td>\n              Whether the iterator has completed or been closed.\n            </td>\n          </tr>\n        </table>\n      </emu-table>\n    ","4.2":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let _nextMethod_ be ? Get(_obj_, *\"next\"*).</li><li>Let _iteratorRecord_ be the Iterator Record { [[Iterator]]: _obj_, [[NextMethod]]: _nextMethod_, [[Done]]: *false* }.</li><li>Return _iteratorRecord_.</li></ol></emu-alg>\n    ","4.3":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let _iterator_ be ? Call(_method_, _obj_).</li><li>If _iterator_ is not an Object, throw a *TypeError* exception.</li><li>Return ? GetIteratorDirect(_iterator_).</li></ol></emu-alg>\n    ","4.4":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If _kind_ is ~async~, then<ol><li>Let _method_ be ? GetMethod(_obj_, %Symbol.asyncIterator%).</li><li>If _method_ is *undefined*, then<ol><li>Let _syncMethod_ be ? GetMethod(_obj_, %Symbol.iterator%).</li><li>If _syncMethod_ is *undefined*, throw a *TypeError* exception.</li><li>Let _syncIteratorRecord_ be ? GetIteratorFromMethod(_obj_, _syncMethod_).</li><li>Return CreateAsyncFromSyncIterator(_syncIteratorRecord_).</li></ol></li></ol></li><li>Else,<ol><li>Let _method_ be ? GetMethod(_obj_, %Symbol.iterator%).</li></ol></li><li>If _method_ is *undefined*, throw a *TypeError* exception.</li><li>Return ? GetIteratorFromMethod(_obj_, _method_).</li></ol></emu-alg>\n    ","4.5":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If _obj_ is not an Object, then<ol><li>If _primitiveHandling_ is ~reject-primitives~, throw a *TypeError* exception.</li><li>Assert: _primitiveHandling_ is ~iterate-string-primitives~.</li><li>If _obj_ is not a String, throw a *TypeError* exception.</li></ol></li><li>Let _method_ be ? GetMethod(_obj_, %Symbol.iterator%).</li><li>If _method_ is *undefined*, then<ol><li>Let _iterator_ be _obj_.</li></ol></li><li>Else,<ol><li>Let _iterator_ be ? Call(_method_, _obj_).</li></ol></li><li>If _iterator_ is not an Object, throw a *TypeError* exception.</li><li>Return ? GetIteratorDirect(_iterator_).</li></ol></emu-alg>\n    ","4.6":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If _value_ is not present, then<ol><li>Let _result_ be Completion(Call(_iteratorRecord_.[[NextMethod]], _iteratorRecord_.[[Iterator]])).</li></ol></li><li>Else,<ol><li>Let _result_ be Completion(Call(_iteratorRecord_.[[NextMethod]], _iteratorRecord_.[[Iterator]], « _value_ »)).</li></ol></li><li>If _result_ is a throw completion, then<ol><li>Set _iteratorRecord_.[[Done]] to *true*.</li><li>Return ? _result_.</li></ol></li><li>Set _result_ to ! _result_.</li><li>If _result_ is not an Object, then<ol><li>Set _iteratorRecord_.[[Done]] to *true*.</li><li>Throw a *TypeError* exception.</li></ol></li><li>Return _result_.</li></ol></emu-alg>\n    ","4.7":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Return ToBoolean(? Get(_iteratorResult_, *\"done\"*)).</li></ol></emu-alg>\n    ","4.8":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Return ? Get(_iteratorResult_, *\"value\"*).</li></ol></emu-alg>\n    ","4.9":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It requests the next value from _iteratorRecord_.[[Iterator]] by calling _iteratorRecord_.[[NextMethod]] and returns either ~done~ indicating that the iterator has reached its end or the IteratorResult object if a next value is available.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _result_ be ? IteratorNext(_iteratorRecord_).</li><li>Let _done_ be Completion(IteratorComplete(_result_)).</li><li>If _done_ is a throw completion, then<ol><li>Set _iteratorRecord_.[[Done]] to *true*.</li><li>Return ? _done_.</li></ol></li><li>Set _done_ to ! _done_.</li><li>If _done_ is *true*, then<ol><li>Set _iteratorRecord_.[[Done]] to *true*.</li><li>Return ~done~.</li></ol></li><li>Return _result_.</li></ol></emu-alg>\n    ","4.10":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It requests the next value from _iteratorRecord_.[[Iterator]] by calling _iteratorRecord_.[[NextMethod]] and returns either ~done~ indicating that the iterator has reached its end or the value from the IteratorResult object if a next value is available.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _result_ be ? IteratorStep(_iteratorRecord_).</li><li>If _result_ is ~done~, then<ol><li>Return ~done~.</li></ol></li><li>Let _value_ be Completion(IteratorValue(_result_)).</li><li>If _value_ is a throw completion, then<ol><li>Set _iteratorRecord_.[[Done]] to *true*.</li></ol></li><li>Return ? _value_.</li></ol></emu-alg>\n    ","4.11":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to notify an iterator that it should perform any actions it would normally perform when it has reached its completed state.</dd>\n      </dl>\n      <emu-alg><ol><li>Assert: _iteratorRecord_.[[Iterator]] is an Object.</li><li>Let _iterator_ be _iteratorRecord_.[[Iterator]].</li><li>Let _innerResult_ be Completion(GetMethod(_iterator_, *\"return\"*)).</li><li>If _innerResult_ is a normal completion, then<ol><li>Let _return_ be _innerResult_.[[Value]].</li><li>If _return_ is *undefined*, return ? _completion_.</li><li>Set _innerResult_ to Completion(Call(_return_, _iterator_)).</li></ol></li><li>If _completion_ is a throw completion, return ? _completion_.</li><li>If _innerResult_ is a throw completion, return ? _innerResult_.</li><li>If _innerResult_.[[Value]] is not an Object, throw a *TypeError* exception.</li><li>Return ? _completion_.</li></ol></emu-alg>\n    ","4.12":"<dl class=\"header\">\n        <dt>skip global checks</dt>\n        <dd>true</dd>\n      </dl>\n      <emu-alg><ol><li>For each element _iter_ of _iters_, in reverse List order, do<ol><li>Set _completion_ to Completion(IteratorClose(_iter_, _completion_)).</li></ol></li><li>Return ? _completion_.</li></ol></emu-alg>\n    ","4.13":"<p>IfAbruptCloseIterator is a shorthand for a sequence of algorithm steps that use an Iterator Record. An algorithm step of the form:</p>\n      <emu-alg><ol><li>IfAbruptCloseIterator(_value_, _iteratorRecord_).</li></ol></emu-alg>\n      <p>means the same thing as:</p>\n      <emu-alg><ol><li>Assert: _value_ is a Completion Record.</li><li>If _value_ is an abrupt completion, return ? IteratorClose(_iteratorRecord_, _value_).</li><li>Set _value_ to ! _value_.</li></ol></emu-alg>\n    ","4.14":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to notify an async iterator that it should perform any actions it would normally perform when it has reached its completed state.</dd>\n      </dl>\n      <emu-alg><ol><li>Assert: _iteratorRecord_.[[Iterator]] is an Object.</li><li>Let _iterator_ be _iteratorRecord_.[[Iterator]].</li><li>Let _innerResult_ be Completion(GetMethod(_iterator_, *\"return\"*)).</li><li>If _innerResult_ is a normal completion, then<ol><li>Let _return_ be _innerResult_.[[Value]].</li><li>If _return_ is *undefined*, return ? _completion_.</li><li>Set _innerResult_ to Completion(Call(_return_, _iterator_)).</li><li>If _innerResult_ is a normal completion, set _innerResult_ to Completion(Await(_innerResult_.[[Value]])).</li></ol></li><li>If _completion_ is a throw completion, return ? _completion_.</li><li>If _innerResult_ is a throw completion, return ? _innerResult_.</li><li>If _innerResult_.[[Value]] is not an Object, throw a *TypeError* exception.</li><li>Return ? _completion_.</li></ol></emu-alg>\n    ","4.15":"<p>IfAbruptCloseAsyncIterator is a shorthand for a sequence of algorithm steps that use an Iterator Record. An algorithm step of the form:</p>\n      <emu-alg><ol><li>IfAbruptCloseAsyncIterator(_value_, _iteratorRecord_).</li></ol></emu-alg>\n      <p>means the same thing as:</p>\n      <emu-alg><ol><li>Assert: _value_ is a Completion Record.</li><li>If _value_ is an abrupt completion, return ? AsyncIteratorClose(_iteratorRecord_, _value_).</li><li>Set _value_ to ! _value_.</li></ol></emu-alg>\n    ","4.16":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It creates an object that conforms to the IteratorResult interface.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _obj_ be OrdinaryObjectCreate(%Object.prototype%).</li><li>Perform ! CreateDataPropertyOrThrow(_obj_, *\"value\"*, _value_).</li><li>Perform ! CreateDataPropertyOrThrow(_obj_, *\"done\"*, _done_).</li><li>Return _obj_.</li></ol></emu-alg>\n    ","4.17":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It creates an Iterator Record whose [[NextMethod]] returns the successive elements of _list_.</dd>\n      </dl>\n      <emu-alg><ol><li>Let _closure_ be a new Abstract Closure with no parameters that captures _list_ and performs the following steps when called:<ol><li>For each element _value_ of _list_, do<ol><li>Perform ? GeneratorYield(CreateIteratorResultObject(_value_, *false*)).</li></ol></li><li>Return NormalCompletion(*undefined*).</li></ol></li><li>Let _iterator_ be CreateIteratorFromClosure(_closure_, ~empty~, %Iterator.prototype%).</li><li>Return the Iterator Record { [[Iterator]]: _iterator_, [[NextMethod]]: %GeneratorPrototype.next%, [[Done]]: *false* }.</li></ol></emu-alg>\n      <emu-note>\n        <p>The list iterator object is never directly accessible to ECMAScript code.</p>\n      </emu-note>\n    ","4.18":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let _values_ be a new empty List.</li><li>Repeat,<ol><li>Let _next_ be ? IteratorStepValue(_iteratorRecord_).</li><li>If _next_ is ~done~, then<ol><li>Return _values_.</li></ol></li><li>Append _next_ to _values_.</li></ol></li></ol></emu-alg>\n    "};
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
