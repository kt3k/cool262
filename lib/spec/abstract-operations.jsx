// Generated from ecma262/spec.html — do not edit by hand.
const _sections = {"1":"<p>The ECMAScript language implicitly performs automatic type conversion as needed. To clarify the semantics of certain constructs it is useful to define a set of conversion abstract operations. The conversion abstract operations are polymorphic; they can accept a value of any ECMAScript language type. But no other specification types are used with these operations.</p>\n    <p>The BigInt type has no implicit conversions in the ECMAScript language; programmers must call BigInt explicitly to convert values from other types.</p>\n\n    ","2":"","3":"","4":"<p>See Common Iteration Interfaces (<a href=\"/control-abstraction-objects#sec-iteration\">27.1</a>).</p>\n\n    ","":"<p>These operations are not a part of the ECMAScript language; they are defined here solely to aid the specification of the semantics of the ECMAScript language. Other, more specialized abstract operations are defined throughout this specification.</p>\n\n  ","1.1":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts its <var>input</var> argument to a non-Object type. If an object is capable of converting to more than one primitive type, it may use the optional hint <var>preferredType</var> to favour that type.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>input</var> is an Object, then<ol><li>Let <var>exoticToPrim</var> be ? GetMethod(<var>input</var>, <code class=\"emu-intrinsic\">%Symbol.toPrimitive%</code>).</li><li>If <var>exoticToPrim</var> is not <b>undefined</b>, then<ol><li>If <var>preferredType</var> is not present, then<ol><li>Let <var>hint</var> be <b>\"default\"</b>.</li></ol></li><li>Else if <var>preferredType</var> is <emu-const>string</emu-const>, then<ol><li>Let <var>hint</var> be <b>\"string\"</b>.</li></ol></li><li>Else,<ol><li>Assert: <var>preferredType</var> is <emu-const>number</emu-const>.</li><li>Let <var>hint</var> be <b>\"number\"</b>.</li></ol></li><li>Let <var>result</var> be ? Call(<var>exoticToPrim</var>, <var>input</var>, « <var>hint</var> »).</li><li>If <var>result</var> is not an Object, return <var>result</var>.</li><li>Throw a <b>TypeError</b> exception.</li></ol></li><li>If <var>preferredType</var> is not present, set <var>preferredType</var> to <emu-const>number</emu-const>.</li><li>Return ? OrdinaryToPrimitive(<var>input</var>, <var>preferredType</var>).</li></ol></li><li>Return <var>input</var>.</li></ol></emu-alg>\n      <emu-note>\n        <p>When ToPrimitive is called without a hint, then it generally behaves as if the hint were <emu-const>number</emu-const>. However, objects may over-ride this behaviour by defining a <code class=\"emu-intrinsic\">%Symbol.toPrimitive%</code> method. Of the objects defined in this specification only Dates (see <a href=\"/numbers-and-dates#sec-date.prototype-%symbol.toprimitive%\">21.4.4.45</a>) and Symbol objects (see <a href=\"/fundamental-objects#sec-symbol.prototype-%symbol.toprimitive%\">20.4.3.5</a>) over-ride the default ToPrimitive behaviour. Dates treat the absence of a hint as if the hint were <emu-const>string</emu-const>.</p>\n      </emu-note>\n\n      ","1.1.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If <var>hint</var> is <emu-const>string</emu-const>, then<ol><li>Let <var>methodNames</var> be « <b>\"toString\"</b>, <b>\"valueOf\"</b> ».</li></ol></li><li>Else,<ol><li>Let <var>methodNames</var> be « <b>\"valueOf\"</b>, <b>\"toString\"</b> ».</li></ol></li><li>For each element <var>name</var> of <var>methodNames</var>, do<ol><li>Let <var>method</var> be ? Get(<var>obj</var>, <var>name</var>).</li><li>If IsCallable(<var>method</var>) is <b>true</b>, then<ol><li>Let <var>result</var> be ? Call(<var>method</var>, <var>obj</var>).</li><li>If <var>result</var> is not an Object, return <var>result</var>.</li></ol></li></ol></li><li>Throw a <b>TypeError</b> exception.</li></ol></emu-alg>\n      ","1.2":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to a value of type Boolean.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>argument</var> is a Boolean, return <var>argument</var>.</li><li>If <var>argument</var> is one of <b>undefined</b>, <b>null</b>, <b>+0</b><sub>𝔽</sub>, <b>-0</b><sub>𝔽</sub>, <b>NaN</b>, <b>0</b><sub>ℤ</sub>, or the empty String, return <b>false</b>.</li><li>If the host is a web browser or otherwise supports <a href=\"/additional-ecmascript-features-for-web-browsers#sec-IsHTMLDDA-internal-slot\">B.3.6</a>, then<ol><li>If <var>argument</var> is an Object and <var>argument</var> has an [[IsHTMLDDA]] internal slot, return <b>false</b>.</li></ol></li><li>Return <b>true</b>.</li></ol></emu-alg>\n    ","1.3":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It returns <var>value</var> converted to a Number or a BigInt.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>primValue</var> be ? ToPrimitive(<var>value</var>, <emu-const>number</emu-const>).</li><li>If <var>primValue</var> is a BigInt, return <var>primValue</var>.</li><li>Return ? <emu-meta suppress-effects=\"user-code\">ToNumber(<var>primValue</var>)</emu-meta>.</li></ol></emu-alg>\n    ","1.4":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to a value of type Number.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>argument</var> is a Number, return <var>argument</var>.</li><li>If <var>argument</var> is either a Symbol or a BigInt, throw a <b>TypeError</b> exception.</li><li>If <var>argument</var> is <b>undefined</b>, return <b>NaN</b>.</li><li>If <var>argument</var> is either <b>null</b> or <b>false</b>, return <b>+0</b><sub>𝔽</sub>.</li><li>If <var>argument</var> is <b>true</b>, return <b>1</b><sub>𝔽</sub>.</li><li>If <var>argument</var> is a String, return StringToNumber(<var>argument</var>).</li><li>Assert: <var>argument</var> is an Object.</li><li>Let <var>primValue</var> be ? ToPrimitive(<var>argument</var>, <emu-const>number</emu-const>).</li><li>Assert: <var>primValue</var> is not an Object.</li><li>Return ? ToNumber(<var>primValue</var>).</li></ol></emu-alg>\n\n      ","1.4.1":"<p>The abstract operation StringToNumber specifies how to convert a String value to a Number value, using the following grammar.</p>\n        <h2>Syntax</h2>\n        <pre class=\"emu-grammar\">StringNumericLiteral :::\n  StrWhiteSpace?\n  StrWhiteSpace? StrNumericLiteral StrWhiteSpace?\n\nStrWhiteSpace :::\n  StrWhiteSpaceChar StrWhiteSpace?\n\nStrWhiteSpaceChar :::\n  WhiteSpace\n  LineTerminator\n\nStrNumericLiteral :::\n  StrDecimalLiteral\n  NonDecimalIntegerLiteral[~Sep]\n\nStrDecimalLiteral :::\n  StrUnsignedDecimalLiteral\n  `+` StrUnsignedDecimalLiteral\n  `-` StrUnsignedDecimalLiteral\n\nStrUnsignedDecimalLiteral :::\n  `Infinity`\n  DecimalDigits[~Sep] `.` DecimalDigits[~Sep]? ExponentPart[~Sep]?\n  `.` DecimalDigits[~Sep] ExponentPart[~Sep]?\n  DecimalDigits[~Sep] ExponentPart[~Sep]?</pre>\n        <p>All grammar symbols not explicitly defined above have the definitions used in the Lexical Grammar for numeric literals (<a href=\"/ecmascript-language-lexical-grammar#sec-literals-numeric-literals\">12.9.3</a>)</p>\n        <emu-note>\n          <p>Some differences should be noted between the syntax of a <emu-nt>StringNumericLiteral</emu-nt> and a <emu-nt>NumericLiteral</emu-nt>:</p>\n          <ul>\n            <li>\n              A <emu-nt>StringNumericLiteral</emu-nt> may include leading and/or trailing white space and/or line terminators.\n            </li>\n            <li>\n              A <emu-nt>StringNumericLiteral</emu-nt> that is decimal may have any number of leading <code>0</code> digits.\n            </li>\n            <li>\n              A <emu-nt>StringNumericLiteral</emu-nt> that is decimal may include a <code>+</code> or <code>-</code> to indicate its sign.\n            </li>\n            <li>\n              A <emu-nt>StringNumericLiteral</emu-nt> that is empty or contains only white space is converted to <b>+0</b><sub>𝔽</sub>.\n            </li>\n            <li>\n              <code>Infinity</code> and <code>-Infinity</code> are recognized as a <emu-nt>StringNumericLiteral</emu-nt> but not as a <emu-nt>NumericLiteral</emu-nt>.\n            </li>\n            <li>\n              A <emu-nt>StringNumericLiteral</emu-nt> cannot include a <emu-nt>BigIntLiteralSuffix</emu-nt>.\n            </li>\n            <li>\n              A <emu-nt>StringNumericLiteral</emu-nt> cannot include a <emu-nt>NumericLiteralSeparator</emu-nt>.\n            </li>\n          </ul>\n        </emu-note>\n\n        ","1.4.1.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>literal</var> be ParseText(<var>str</var>, <emu-nt>StringNumericLiteral</emu-nt>).</li><li>If <var>literal</var> is a List of errors, return <b>NaN</b>.</li><li>Return the StringNumericValue of <var>literal</var>.</li></ol></emu-alg>\n        ","1.4.1.2":"<dl class=\"header\">\n          </dl>\n          <emu-note>\n            <p>The conversion of a <emu-nt>StringNumericLiteral</emu-nt> to a Number value is similar overall to the determination of the NumericValue of a <emu-nt>NumericLiteral</emu-nt> (see <a href=\"/ecmascript-language-lexical-grammar#sec-literals-numeric-literals\">12.9.3</a>), but some of the details are different.</p>\n          </emu-note>\n          <pre class=\"emu-grammar\">StringNumericLiteral ::: StrWhiteSpace?</pre>\n          <emu-alg><ol><li>Return <b>+0</b><sub>𝔽</sub>.</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StringNumericLiteral ::: StrWhiteSpace? StrNumericLiteral StrWhiteSpace?</pre>\n          <emu-alg><ol><li>Return the StringNumericValue of <emu-nt>StrNumericLiteral</emu-nt>.</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StrNumericLiteral ::: NonDecimalIntegerLiteral</pre>\n          <emu-alg><ol><li>Return 𝔽(MV of <emu-nt>NonDecimalIntegerLiteral</emu-nt>).</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StrDecimalLiteral ::: `-` StrUnsignedDecimalLiteral</pre>\n          <emu-alg><ol><li>Let <var>a</var> be the StringNumericValue of <emu-nt>StrUnsignedDecimalLiteral</emu-nt>.</li><li>If <var>a</var> is <b>+0</b><sub>𝔽</sub>, return <b>-0</b><sub>𝔽</sub>.</li><li>Return -<var>a</var>.</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StrUnsignedDecimalLiteral ::: `Infinity`</pre>\n          <emu-alg><ol><li>Return <b>+∞</b><sub>𝔽</sub>.</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StrUnsignedDecimalLiteral ::: DecimalDigits `.` DecimalDigits? ExponentPart?</pre>\n          <emu-alg><ol><li>Let <var>a</var> be the MV of the first <emu-nt>DecimalDigits</emu-nt>.</li><li>If the second <emu-nt>DecimalDigits</emu-nt> is present, then<ol><li>Let <var>b</var> be the MV of the second <emu-nt>DecimalDigits</emu-nt>.</li><li>Let <var>n</var> be the number of code points in the second <emu-nt>DecimalDigits</emu-nt>.</li></ol></li><li>Else,<ol><li>Let <var>b</var> be 0.</li><li>Let <var>n</var> be 0.</li></ol></li><li>If <emu-nt>ExponentPart</emu-nt> is present, let <var>e</var> be the MV of <emu-nt>ExponentPart</emu-nt>; else let <var>e</var> be 0.</li><li>Return RoundMVResult((<var>a</var> + (<var>b</var> × 10<sup>-<var>n</var></sup>)) × 10<sup><var>e</var></sup>).</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StrUnsignedDecimalLiteral ::: `.` DecimalDigits ExponentPart?</pre>\n          <emu-alg><ol><li>Let <var>b</var> be the MV of <emu-nt>DecimalDigits</emu-nt>.</li><li>If <emu-nt>ExponentPart</emu-nt> is present, let <var>e</var> be the MV of <emu-nt>ExponentPart</emu-nt>; else let <var>e</var> be 0.</li><li>Let <var>n</var> be the number of code points in <emu-nt>DecimalDigits</emu-nt>.</li><li>Return RoundMVResult(<var>b</var> × 10<sup><var>e</var> - <var>n</var></sup>).</li></ol></emu-alg>\n          <pre class=\"emu-grammar\">StrUnsignedDecimalLiteral ::: DecimalDigits ExponentPart?</pre>\n          <emu-alg><ol><li>Let <var>a</var> be the MV of <emu-nt>DecimalDigits</emu-nt>.</li><li>If <emu-nt>ExponentPart</emu-nt> is present, let <var>e</var> be the MV of <emu-nt>ExponentPart</emu-nt>; else let <var>e</var> be 0.</li><li>Return RoundMVResult(<var>a</var> × 10<sup><var>e</var></sup>).</li></ol></emu-alg>\n        ","1.4.1.3":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It converts <var>n</var> to a Number in an implementation-defined manner. For the purposes of this abstract operation, a digit is significant if it is not zero or there is a non-zero digit to its left and there is a non-zero digit to its right. For the purposes of this abstract operation, \"the mathematical value denoted by\" a representation of a mathematical value is the inverse of \"the decimal representation of\" a mathematical value.</dd>\n          </dl>\n          <emu-alg><ol><li>If the decimal representation of <var>n</var> has 20 or fewer significant digits, return 𝔽(<var>n</var>).</li><li>Let <var>option1</var> be the mathematical value denoted by the result of replacing each significant digit in the decimal representation of <var>n</var> after the 20th with a 0 digit.</li><li>Let <var>option2</var> be the mathematical value denoted by the result of replacing each significant digit in the decimal representation of <var>n</var> after the 20th with a 0 digit and then incrementing it at the 20th position (with carrying as necessary).</li><li>Let <var>chosen</var> be an implementation-defined choice of either <var>option1</var> or <var>option2</var>.</li><li>Return 𝔽(<var>chosen</var>).</li></ol></emu-alg>\n        ","1.5":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to an integer representing its Number value with fractional part truncated, or to +∞ or -∞ when that Number value is infinite.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>number</var> be ? ToNumber(<var>argument</var>).</li><li>If <var>number</var> is one of <b>NaN</b>, <b>+0</b><sub>𝔽</sub>, or <b>-0</b><sub>𝔽</sub>, return 0.</li><li>If <var>number</var> is <b>+∞</b><sub>𝔽</sub>, return +∞.</li><li>If <var>number</var> is <b>-∞</b><sub>𝔽</sub>, return -∞.</li><li>Return truncate(ℝ(<var>number</var>)).</li></ol></emu-alg>\n      <emu-note>\n        𝔽(ToIntegerOrInfinity(<var>x</var>)) never returns <b>-0</b><sub>𝔽</sub> for any value of <var>x</var>. The truncation of the fractional part is performed after converting <var>x</var> to a mathematical value.\n      </emu-note>\n    ","1.6":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It maps <var>int</var> to one of 2<sup><var>bitWidth</var></sup> integers in the inclusive interval from 0 to 2<sup><var>bitWidth</var></sup> - 1 (if <var>signed</var> is <emu-const>unsigned</emu-const>) or -2<sup><var>bitWidth</var> - 1</sup> to 2<sup><var>bitWidth</var> - 1</sup> - 1 (if <var>signed</var> is <emu-const>signed</emu-const>).</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>int</var> = +∞ or <var>int</var> = -∞, return 0.</li><li>Let <var>fixedInt</var> be <var>int</var> modulo 2<sup><var>bitWidth</var></sup>.</li><li>NOTE: The following step does not change the two's complement representation of <var>fixedInt</var>.</li><li>If <var>signed</var> is <emu-const>signed</emu-const> and <var>fixedInt</var> ≥ 2<sup><var>bitWidth</var> - 1</sup>, set <var>fixedInt</var> to <var>fixedInt</var> - 2<sup><var>bitWidth</var></sup>.</li><li>Return <var>fixedInt</var>.</li></ol></emu-alg>\n      <emu-note>\n        <p>ToFixedSizeInteger is idempotent: for any ECMAScript language value <var>x</var>, ToFixedSizeInteger(ToFixedSizeInteger(<var>x</var>, <var>signed</var>, <var>bitWidth</var>), <var>signed</var>, <var>bitWidth</var>) is the same as ToFixedSizeInteger(<var>x</var>, <var>signed</var>, <var>bitWidth</var>). In fact, because +∞ and -∞ are mapped to 0, this invariant even survives inversion of <var>signed</var> (e.g., ToFixedSizeInteger(ToFixedSizeInteger(<var>x</var>, <emu-const>signed</emu-const>, 32), <emu-const>unsigned</emu-const>, 32) is the same as ToFixedSizeInteger(<var>x</var>, <emu-const>unsigned</emu-const>, 32)).</p>\n      </emu-note>\n    ","1.7":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to one of 2<sup>32</sup> integral Number values in the inclusive interval from 𝔽(-2<sup>31</sup>) to 𝔽(2<sup>31</sup> - 1), excluding <b>-0</b><sub>𝔽</sub>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>int</var> be ? ToIntegerOrInfinity(<var>argument</var>).</li><li>Return 𝔽(ToFixedSizeInteger(<var>int</var>, <emu-const>signed</emu-const>, 32)).</li></ol></emu-alg>\n    ","1.8":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to one of 2<sup>32</sup> integral Number values in the inclusive interval from <b>+0</b><sub>𝔽</sub> to 𝔽(2<sup>32</sup> - 1).</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>int</var> be ? ToIntegerOrInfinity(<var>argument</var>).</li><li>Return 𝔽(ToFixedSizeInteger(<var>int</var>, <emu-const>unsigned</emu-const>, 32)).</li></ol></emu-alg>\n    ","1.9":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to one of 2<sup>16</sup> integral Number values in the inclusive interval from 𝔽(-2<sup>15</sup>) to 𝔽(2<sup>15</sup> - 1), excluding <b>-0</b><sub>𝔽</sub>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>int</var> be ? ToIntegerOrInfinity(<var>argument</var>).</li><li>Return 𝔽(ToFixedSizeInteger(<var>int</var>, <emu-const>signed</emu-const>, 16)).</li></ol></emu-alg>\n    ","1.10":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to one of 2<sup>16</sup> integral Number values in the inclusive interval from <b>+0</b><sub>𝔽</sub> to 𝔽(2<sup>16</sup> - 1).</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>int</var> be ? ToIntegerOrInfinity(<var>argument</var>).</li><li>Return 𝔽(ToFixedSizeInteger(<var>int</var>, <emu-const>unsigned</emu-const>, 16)).</li></ol></emu-alg>\n    ","1.11":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to one of 2<sup>8</sup> integral Number values in the inclusive interval from <b>-128</b><sub>𝔽</sub> to <b>127</b><sub>𝔽</sub>, excluding <b>-0</b><sub>𝔽</sub>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>int</var> be ? ToIntegerOrInfinity(<var>argument</var>).</li><li>Return 𝔽(ToFixedSizeInteger(<var>int</var>, <emu-const>signed</emu-const>, 8)).</li></ol></emu-alg>\n    ","1.12":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to one of 2<sup>8</sup> integral Number values in the inclusive interval from <b>+0</b><sub>𝔽</sub> to <b>255</b><sub>𝔽</sub>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>int</var> be ? ToIntegerOrInfinity(<var>argument</var>).</li><li>Return 𝔽(ToFixedSizeInteger(<var>int</var>, <emu-const>unsigned</emu-const>, 8)).</li></ol></emu-alg>\n    ","1.13":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It clamps and rounds <var>argument</var> to one of 2<sup>8</sup> integral Number values in the inclusive interval from <b>+0</b><sub>𝔽</sub> to <b>255</b><sub>𝔽</sub>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>number</var> be ? ToNumber(<var>argument</var>).</li><li>If <var>number</var> is <b>NaN</b>, return <b>+0</b><sub>𝔽</sub>.</li><li>Let <var>mv</var> be the extended mathematical value of <var>number</var>.</li><li>Let <var>clamped</var> be the result of clamping <var>mv</var> between 0 and 255.</li><li>Let <var>f</var> be floor(<var>clamped</var>).</li><li>If <var>clamped</var> &lt; <var>f</var> + 0.5, return 𝔽(<var>f</var>).</li><li>If <var>clamped</var> > <var>f</var> + 0.5, return 𝔽(<var>f</var> + 1).</li><li>If <var>f</var> is even, return 𝔽(<var>f</var>).</li><li>Return 𝔽(<var>f</var> + 1).</li></ol></emu-alg>\n      <emu-note>\n        <p>Unlike most other ECMAScript integer conversion operations, ToUint8Clamp rounds rather than truncates non-integral values. It also uses “round half to even” tie-breaking, which differs from the “round half up” tie-breaking of <a href=\"/numbers-and-dates#sec-math.round\"><code>Math.round</code></a>.</p>\n      </emu-note>\n    ","1.14":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to a BigInt value, or throws if an implicit conversion from Number would be required.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>prim</var> be ? ToPrimitive(<var>argument</var>, <emu-const>number</emu-const>).</li><li>Return the value that <var>prim</var> corresponds to in table-tobigint.</li></ol></emu-alg>\n      <emu-table id=\"table-tobigint\" caption=\"BigInt Conversions\">\n        <table>\n          <thead>\n            <tr>\n              <th>\n                Argument Type\n              </th>\n              <th>\n                Result\n              </th>\n            </tr>\n          </thead>\n          <tr>\n            <td>\n              Undefined\n            </td>\n            <td>\n              Throw a <b>TypeError</b> exception.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              Null\n            </td>\n            <td>\n              Throw a <b>TypeError</b> exception.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              Boolean\n            </td>\n            <td>\n              Return <code>1n</code> if <var>prim</var> is <b>true</b> and <code>0n</code> if <var>prim</var> is <b>false</b>.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              BigInt\n            </td>\n            <td>\n              Return <var>prim</var>.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              Number\n            </td>\n            <td>\n              Throw a <b>TypeError</b> exception.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              String\n            </td>\n            <td>\n              <emu-alg><ol><li>Let <var>n</var> be StringToBigInt(<var>prim</var>).</li><li>If <var>n</var> is <b>undefined</b>, throw a <b>SyntaxError</b> exception.</li><li>Return <var>n</var>.</li></ol></emu-alg>\n            </td>\n          </tr>\n          <tr>\n            <td>\n              Symbol\n            </td>\n            <td>\n              Throw a <b>TypeError</b> exception.\n            </td>\n          </tr>\n        </table>\n      </emu-table>\n    ","1.15":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>literal</var> be ParseText(<var>str</var>, <emu-nt>StringIntegerLiteral</emu-nt>).</li><li>If <var>literal</var> is a List of errors, return <b>undefined</b>.</li><li>Let <var>mv</var> be the MV of <var>literal</var>.</li><li>Assert: <var>mv</var> is an integer.</li><li>Return ℤ(<var>mv</var>).</li></ol></emu-alg>\n\n      ","1.15.1":"<p>StringToBigInt uses the following grammar.</p>\n        <h2>Syntax</h2>\n        <pre class=\"emu-grammar\">StringIntegerLiteral :::\n  StrWhiteSpace?\n  StrWhiteSpace? StrIntegerLiteral StrWhiteSpace?\n\nStrIntegerLiteral :::\n  SignedInteger[~Sep]\n  NonDecimalIntegerLiteral[~Sep]</pre>\n      ","1.15.2":"<ul>\n          <li>\n            The MV of <code class=\"emu-grammar\">StringIntegerLiteral ::: StrWhiteSpace?</code> is 0.\n          </li>\n          <li>\n            The MV of <code class=\"emu-grammar\">StringIntegerLiteral ::: StrWhiteSpace? StrIntegerLiteral StrWhiteSpace?</code> is the MV of <emu-nt>StrIntegerLiteral</emu-nt>.\n          </li>\n        </ul>\n      ","1.16":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to one of 2<sup>64</sup> BigInt values in the inclusive interval from ℤ(-2<sup>63</sup>) to ℤ(2<sup>63</sup> - 1).</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>int</var> be ℝ(? ToBigInt(<var>argument</var>)).</li><li>Return ℤ(ToFixedSizeInteger(<var>int</var>, <emu-const>signed</emu-const>, 64)).</li></ol></emu-alg>\n    ","1.17":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to one of 2<sup>64</sup> BigInt values in the inclusive interval from <b>0</b><sub>ℤ</sub> to ℤ(2<sup>64</sup> - 1).</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>int</var> be ℝ(? ToBigInt(<var>argument</var>)).</li><li>Return ℤ(ToFixedSizeInteger(<var>int</var>, <emu-const>unsigned</emu-const>, 64)).</li></ol></emu-alg>\n    ","1.18":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to a value of type String.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>argument</var> is a String, return <var>argument</var>.</li><li>If <var>argument</var> is a Symbol, throw a <b>TypeError</b> exception.</li><li>If <var>argument</var> is <b>undefined</b>, return <b>\"undefined\"</b>.</li><li>If <var>argument</var> is <b>null</b>, return <b>\"null\"</b>.</li><li>If <var>argument</var> is <b>true</b>, return <b>\"true\"</b>.</li><li>If <var>argument</var> is <b>false</b>, return <b>\"false\"</b>.</li><li>If <var>argument</var> is a Number, return Number::toString(<var>argument</var>, 10).</li><li>If <var>argument</var> is a BigInt, return BigInt::toString(<var>argument</var>, 10).</li><li>Assert: <var>argument</var> is an Object.</li><li>Let <var>primValue</var> be ? ToPrimitive(<var>argument</var>, <emu-const>string</emu-const>).</li><li>Assert: <var>primValue</var> is not an Object.</li><li>Return ? ToString(<var>primValue</var>).</li></ol></emu-alg>\n    ","1.19":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to a value of type Object.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>argument</var> is either <b>undefined</b> or <b>null</b>, throw a <b>TypeError</b> exception.</li><li>If <var>argument</var> is a Boolean, return a new Boolean object whose [[BooleanData]] internal slot is set to <var>argument</var>. See <a href=\"/fundamental-objects#sec-boolean-objects\">20.3</a> for a description of Boolean objects.</li><li>If <var>argument</var> is a Number, return a new Number object whose [[NumberData]] internal slot is set to <var>argument</var>. See <a href=\"/numbers-and-dates#sec-number-objects\">21.1</a> for a description of Number objects.</li><li>If <var>argument</var> is a String, return a new String object whose [[StringData]] internal slot is set to <var>argument</var>. See <a href=\"/text-processing#sec-string-objects\">22.1</a> for a description of String objects.</li><li>If <var>argument</var> is a Symbol, return a new Symbol object whose [[SymbolData]] internal slot is set to <var>argument</var>. See <a href=\"/fundamental-objects#sec-symbol-objects\">20.4</a> for a description of Symbol objects.</li><li>If <var>argument</var> is a BigInt, return a new BigInt object whose [[BigIntData]] internal slot is set to <var>argument</var>. See <a href=\"/numbers-and-dates#sec-bigint-objects\">21.2</a> for a description of BigInt objects.</li><li>Assert: <var>argument</var> is an Object.</li><li>Return <var>argument</var>.</li></ol></emu-alg>\n    ","1.20":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>argument</var> to a value that can be used as a property key.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>key</var> be ? ToPrimitive(<var>argument</var>, <emu-const>string</emu-const>).</li><li>If <var>key</var> is a Symbol, then<ol><li>Return <var>key</var>.</li></ol></li><li>Return ! ToString(<var>key</var>).</li></ol></emu-alg>\n    ","1.21":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It clamps and truncates <var>argument</var> to a non-negative integral Number suitable for use as the length of an array-like object.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>len</var> be ? ToIntegerOrInfinity(<var>argument</var>).</li><li>If <var>len</var> ≤ 0, return <b>+0</b><sub>𝔽</sub>.</li><li>Return 𝔽(min(<var>len</var>, 2<sup>53</sup> - 1)).</li></ol></emu-alg>\n    ","1.22":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>If <var>argument</var> is either <b>\"-0\"</b> or exactly matches ToString(<var>n</var>) for some Number value <var>n</var>, it returns the respective Number value. Otherwise, it returns <b>undefined</b>.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>argument</var> is <b>\"-0\"</b>, return <b>-0</b><sub>𝔽</sub>.</li><li>Let <var>n</var> be ! ToNumber(<var>argument</var>).</li><li>If ! ToString(<var>n</var>) is <var>argument</var>, return <var>n</var>.</li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n      <p>A <dfn variants=\"canonical numeric strings\">canonical numeric string</dfn> is any String for which the CanonicalNumericIndexString abstract operation does not return <b>undefined</b>.</p>\n    ","1.23":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It converts <var>value</var> to an integer and returns that integer if it is non-negative and corresponds with an integer index. Otherwise, it throws an exception.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>integer</var> be ? ToIntegerOrInfinity(<var>value</var>).</li><li>If <var>integer</var> is not in the inclusive interval from 0 to 2<sup>53</sup> - 1, throw a <b>RangeError</b> exception.</li><li>Return <var>integer</var>.</li></ol></emu-alg>\n    ","2.1":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It throws an error if <var>argument</var> is a value that cannot be converted to an Object using ToObject.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>argument</var> is either <b>undefined</b> or <b>null</b>, throw a <b>TypeError</b> exception.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    ","2.2":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If <var>argument</var> is not an Object, return <b>false</b>.</li><li>If <var>argument</var> is an Array exotic object, return <b>true</b>.</li><li>If <var>argument</var> is a Proxy exotic object, then<ol><li>Perform ? ValidateNonRevokedProxy(<var>argument</var>).</li><li>Let <var>proxyTarget</var> be <var>argument</var>.[[ProxyTarget]].</li><li>Return ? IsArray(<var>proxyTarget</var>).</li></ol></li><li>Return <b>false</b>.</li></ol></emu-alg>\n    ","2.3":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It determines if <var>argument</var> is a callable function with a [[Call]] internal method.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>argument</var> is not an Object, return <b>false</b>.</li><li>If <var>argument</var> has a [[Call]] internal method, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n    ","2.4":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It determines if <var>argument</var> is a function object with a [[Construct]] internal method.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>argument</var> is not an Object, return <b>false</b>.</li><li>If <var>argument</var> has a [[Construct]] internal method, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n    ","2.5":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to determine whether additional properties can be added to <var>obj</var>.</dd>\n      </dl>\n      <emu-alg><ol><li>Return ? <emu-meta effects=\"user-code\"><var>obj</var>.[[IsExtensible]]()</emu-meta>.</li></ol></emu-alg>\n    ","2.6":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If <var>argument</var> is not an Object, return <b>false</b>.</li><li>Let <var>matcher</var> be ? Get(<var>argument</var>, <code class=\"emu-intrinsic\">%Symbol.match%</code>).</li><li>If <var>matcher</var> is not <b>undefined</b>, return ToBoolean(<var>matcher</var>).</li><li>If <var>argument</var> has a [[RegExpMatcher]] internal slot, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n    ","2.7":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It interprets <var>string</var> as a sequence of UTF-16 encoded code points, as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>, and determines whether it is a <a href=\"http://www.unicode.org/glossary/#well_formed_code_unit_sequence\">well formed</a> UTF-16 sequence.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>len</var> be the length of <var>string</var>.</li><li>Let <var>k</var> be 0.</li><li>Repeat, while <var>k</var> &lt; <var>len</var>,<ol><li>Let <var>cp</var> be CodePointAt(<var>string</var>, <var>k</var>).</li><li>If <var>cp</var>.[[IsUnpairedSurrogate]] is <b>true</b>, return <b>false</b>.</li><li>Set <var>k</var> to <var>k</var> + <var>cp</var>.[[CodeUnitCount]].</li></ol></li><li>Return <b>true</b>.</li></ol></emu-alg>\n    ","2.8":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It determines whether or not the two arguments are the same type.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>x</var> is <b>undefined</b> and <var>y</var> is <b>undefined</b>, return <b>true</b>.</li><li>If <var>x</var> is <b>null</b> and <var>y</var> is <b>null</b>, return <b>true</b>.</li><li>If <var>x</var> is a Boolean and <var>y</var> is a Boolean, return <b>true</b>.</li><li>If <var>x</var> is a Number and <var>y</var> is a Number, return <b>true</b>.</li><li>If <var>x</var> is a BigInt and <var>y</var> is a BigInt, return <b>true</b>.</li><li>If <var>x</var> is a Symbol and <var>y</var> is a Symbol, return <b>true</b>.</li><li>If <var>x</var> is a String and <var>y</var> is a String, return <b>true</b>.</li><li>If <var>x</var> is an Object and <var>y</var> is an Object, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n    ","2.9":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It determines whether or not the two arguments are the same value.</dd>\n      </dl>\n      <emu-alg><ol><li>If SameType(<var>x</var>, <var>y</var>) is <b>false</b>, return <b>false</b>.</li><li>If <var>x</var> is a Number, then<ol><li>Return Number::sameValue(<var>x</var>, <var>y</var>).</li></ol></li><li>Return SameValueNonNumber(<var>x</var>, <var>y</var>).</li></ol></emu-alg>\n      <emu-note>\n        <p>This algorithm differs from the IsStrictlyEqual Algorithm by treating all <b>NaN</b> values as equivalent and by differentiating <b>+0</b><sub>𝔽</sub> from <b>-0</b><sub>𝔽</sub>.</p>\n      </emu-note>\n    ","2.10":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It determines whether or not the two arguments are the same value (ignoring the difference between <b>+0</b><sub>𝔽</sub> and <b>-0</b><sub>𝔽</sub>).</dd>\n      </dl>\n      <emu-alg><ol><li>If SameType(<var>x</var>, <var>y</var>) is <b>false</b>, return <b>false</b>.</li><li>If <var>x</var> is a Number, then<ol><li>Return Number::sameValueZero(<var>x</var>, <var>y</var>).</li></ol></li><li>Return SameValueNonNumber(<var>x</var>, <var>y</var>).</li></ol></emu-alg>\n      <emu-note>\n        <p>SameValueZero differs from SameValue only in that it treats <b>+0</b><sub>𝔽</sub> and <b>-0</b><sub>𝔽</sub> as equivalent.</p>\n      </emu-note>\n    ","2.11":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Assert: SameType(<var>x</var>, <var>y</var>) is <b>true</b>.</li><li>If <var>x</var> is either <b>undefined</b> or <b>null</b>, return <b>true</b>.</li><li>If <var>x</var> is a BigInt, then<ol><li>Return BigInt::equal(<var>x</var>, <var>y</var>).</li></ol></li><li>If <var>x</var> is a String, then<ol><li>If <var>x</var> and <var>y</var> have the same length and the same code units in the same positions, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></li><li>If <var>x</var> is a Boolean, then<ol><li>If <var>x</var> is <b>true</b> and <var>y</var> is <b>true</b>, return <b>true</b>.</li><li>If <var>x</var> is <b>false</b> and <var>y</var> is <b>false</b>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></li><li>NOTE: All other ECMAScript language values are compared by identity.</li><li>If <var>x</var> is <var>y</var>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      <emu-note>\n        For expository purposes, some cases are handled separately within this algorithm even if it is unnecessary to do so.\n      </emu-note>\n      <emu-note>\n        The specifics of what \"<var>x</var> is <var>y</var>\" means are detailed in <a href=\"/notational-conventions#sec-identity\">5.2.8</a>.\n      </emu-note>\n    ","2.12":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It provides the semantics for the comparison <var>x</var> &lt; <var>y</var>, returning <b>true</b>, <b>false</b>, or <b>undefined</b> (which indicates that the operands could not be coerced to comparable values of the same numeric type). The <var>leftFirst</var> flag is used to control the order in which operations with potentially visible side-effects are performed upon <var>x</var> and <var>y</var>. It is necessary because ECMAScript specifies left to right evaluation of expressions. If <var>leftFirst</var> is <b>true</b>, the <var>x</var> parameter corresponds to an expression that occurs to the left of the <var>y</var> parameter's corresponding expression. If <var>leftFirst</var> is <b>false</b>, the reverse is the case and operations must be performed upon <var>y</var> before <var>x</var>.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>leftFirst</var> is <b>true</b>, then<ol><li>Let <var>px</var> be ? ToPrimitive(<var>x</var>, <emu-const>number</emu-const>).</li><li>Let <var>py</var> be ? ToPrimitive(<var>y</var>, <emu-const>number</emu-const>).</li></ol></li><li>Else,<ol><li>NOTE: The order of evaluation needs to be reversed to preserve left to right evaluation.</li><li>Let <var>py</var> be ? ToPrimitive(<var>y</var>, <emu-const>number</emu-const>).</li><li>Let <var>px</var> be ? ToPrimitive(<var>x</var>, <emu-const>number</emu-const>).</li></ol></li><li>If <var>px</var> is a String and <var>py</var> is a String, then<ol><li>Let <var>lx</var> be the length of <var>px</var>.</li><li>Let <var>ly</var> be the length of <var>py</var>.</li><li>For each integer <var>i</var> such that 0 ≤ <var>i</var> &lt; min(<var>lx</var>, <var>ly</var>), in ascending order, do<ol><li>Let <var>cx</var> be the numeric value of the code unit at index <var>i</var> within <var>px</var>.</li><li>Let <var>cy</var> be the numeric value of the code unit at index <var>i</var> within <var>py</var>.</li><li>If <var>cx</var> &lt; <var>cy</var>, return <b>true</b>.</li><li>If <var>cx</var> > <var>cy</var>, return <b>false</b>.</li></ol></li><li>If <var>lx</var> &lt; <var>ly</var>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></li><li>If <var>px</var> is a BigInt and <var>py</var> is a String, then<ol><li>Let <var>ny</var> be StringToBigInt(<var>py</var>).</li><li>If <var>ny</var> is <b>undefined</b>, return <b>undefined</b>.</li><li>Return BigInt::lessThan(<var>px</var>, <var>ny</var>).</li></ol></li><li>If <var>px</var> is a String and <var>py</var> is a BigInt, then<ol><li>Let <var>nx</var> be StringToBigInt(<var>px</var>).</li><li>If <var>nx</var> is <b>undefined</b>, return <b>undefined</b>.</li><li>Return BigInt::lessThan(<var>nx</var>, <var>py</var>).</li></ol></li><li>NOTE: Because <var>px</var> and <var>py</var> are primitive values, evaluation order is not important.</li><li>Let <var>nx</var> be ? <emu-meta suppress-effects=\"user-code\">ToNumeric(<var>px</var>)</emu-meta>.</li><li>Let <var>ny</var> be ? <emu-meta suppress-effects=\"user-code\">ToNumeric(<var>py</var>)</emu-meta>.</li><li>If SameType(<var>nx</var>, <var>ny</var>) is <b>true</b>, then<ol><li>If <var>nx</var> is a Number, return Number::lessThan(<var>nx</var>, <var>ny</var>).</li><li>Assert: <var>nx</var> is a BigInt.</li><li>Return BigInt::lessThan(<var>nx</var>, <var>ny</var>).</li></ol></li><li>Assert: <var>nx</var> is a BigInt and <var>ny</var> is a Number, or <var>nx</var> is a Number and <var>ny</var> is a BigInt.</li><li>If <var>nx</var> is <b>NaN</b> or <var>ny</var> is <b>NaN</b>, return <b>undefined</b>.</li><li>If <var>nx</var> is <b>-∞</b><sub>𝔽</sub> or <var>ny</var> is <b>+∞</b><sub>𝔽</sub>, return <b>true</b>.</li><li>If <var>nx</var> is <b>+∞</b><sub>𝔽</sub> or <var>ny</var> is <b>-∞</b><sub>𝔽</sub>, return <b>false</b>.</li><li>If ℝ(<var>nx</var>) &lt; ℝ(<var>ny</var>), return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      <emu-note>\n        <p>Step step-arc-string-check differs from step step-binary-op-string-check in the algorithm that handles the addition operator <code>+</code> (<a href=\"/ecmascript-language-expressions#sec-applystringornumericbinaryoperator\">13.15.3</a>) by using the logical-and operation instead of the logical-or operation.</p>\n      </emu-note>\n      <emu-note>\n        <p>The comparison of Strings uses a simple lexicographic ordering on sequences of UTF-16 code unit values. There is no attempt to use the more complex, semantically oriented definitions of character or string equality and collating order defined in the Unicode specification. Therefore String values that are canonically equal according to the Unicode Standard but not in the same normalization form could test as unequal. Also note that lexicographic ordering by <em>code unit</em> differs from ordering by <em>code point</em> for Strings containing surrogate pairs.</p>\n      </emu-note>\n    ","2.13":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It provides the semantics for the <code>==</code> operator.</dd>\n      </dl>\n      <emu-alg><ol><li>If SameType(<var>x</var>, <var>y</var>) is <b>true</b>, then<ol><li>Return IsStrictlyEqual(<var>x</var>, <var>y</var>).</li></ol></li><li>If <var>x</var> is <b>null</b> and <var>y</var> is <b>undefined</b>, return <b>true</b>.</li><li>If <var>x</var> is <b>undefined</b> and <var>y</var> is <b>null</b>, return <b>true</b>.</li><li>If the host is a web browser or otherwise supports <a href=\"/additional-ecmascript-features-for-web-browsers#sec-IsHTMLDDA-internal-slot\">B.3.6</a>, then<ol><li>If <var>x</var> is an Object, <var>x</var> has an [[IsHTMLDDA]] internal slot, and <var>y</var> is either <b>undefined</b> or <b>null</b>, return <b>true</b>.</li><li>If <var>x</var> is either <b>undefined</b> or <b>null</b>, <var>y</var> is an Object, and <var>y</var> has an [[IsHTMLDDA]] internal slot, return <b>true</b>.</li></ol></li><li>If <var>x</var> is a Number and <var>y</var> is a String, return ! IsLooselyEqual(<var>x</var>, ! ToNumber(<var>y</var>)).</li><li>If <var>x</var> is a String and <var>y</var> is a Number, return ! IsLooselyEqual(! ToNumber(<var>x</var>), <var>y</var>).</li><li>If <var>x</var> is a BigInt and <var>y</var> is a String, then<ol><li>Let <var>n</var> be StringToBigInt(<var>y</var>).</li><li>If <var>n</var> is <b>undefined</b>, return <b>false</b>.</li><li>Return ! IsLooselyEqual(<var>x</var>, <var>n</var>).</li></ol></li><li>If <var>x</var> is a String and <var>y</var> is a BigInt, return ! IsLooselyEqual(<var>y</var>, <var>x</var>).</li><li>If <var>x</var> is a Boolean, return ! IsLooselyEqual(! ToNumber(<var>x</var>), <var>y</var>).</li><li>If <var>y</var> is a Boolean, return ! IsLooselyEqual(<var>x</var>, ! ToNumber(<var>y</var>)).</li><li>If <var>x</var> is either a String, a Number, a BigInt, or a Symbol and <var>y</var> is an Object, return ! IsLooselyEqual(<var>x</var>, ? ToPrimitive(<var>y</var>)).</li><li>If <var>x</var> is an Object and <var>y</var> is either a String, a Number, a BigInt, or a Symbol, return ! IsLooselyEqual(? ToPrimitive(<var>x</var>), <var>y</var>).</li><li>If <var>x</var> is a BigInt and <var>y</var> is a Number, or if <var>x</var> is a Number and <var>y</var> is a BigInt, then<ol><li>If <var>x</var> is not finite or <var>y</var> is not finite, return <b>false</b>.</li><li>If ℝ(<var>x</var>) = ℝ(<var>y</var>), return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></li><li>Return <b>false</b>.</li></ol></emu-alg>\n    ","2.14":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It provides the semantics for the <code>===</code> operator.</dd>\n      </dl>\n      <emu-alg><ol><li>If SameType(<var>x</var>, <var>y</var>) is <b>false</b>, return <b>false</b>.</li><li>If <var>x</var> is a Number, then<ol><li>Return Number::equal(<var>x</var>, <var>y</var>).</li></ol></li><li>Return SameValueNonNumber(<var>x</var>, <var>y</var>).</li></ol></emu-alg>\n      <emu-note>\n        <p>This algorithm differs from the SameValue Algorithm in its treatment of signed zeroes and NaNs.</p>\n      </emu-note>\n    ","3.1":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is the source of all ECMAScript objects that are created algorithmically, including both ordinary objects and exotic objects. It factors out common steps used in creating all objects, and centralizes object creation.</dd>\n      </dl>\n\n      <emu-alg><ol><li>Set <var>internalSlotsList</var> to the list-concatenation of <var>internalSlotsList</var> and « [[PrivateElements]] ».</li><li>Let <var>obj</var> be a newly created object with an internal slot for each name in <var>internalSlotsList</var>.</li><li>NOTE: As described in <a href=\"/ecmascript-data-types-and-values#sec-object-internal-methods-and-internal-slots\">6.1.7.2</a>, the initial value of each such internal slot is <b>undefined</b> unless specified otherwise.</li><li>Set <var>obj</var>.[[PrivateElements]] to a new empty List.</li><li>Set <var>obj</var>'s essential internal methods to the default ordinary object definitions specified in <a href=\"/ordinary-and-exotic-objects-behaviours#sec-ordinary-object-internal-methods-and-internal-slots\">10.1</a>.</li><li>Assert: If the caller will not be overriding both <var>obj</var>'s [[GetPrototypeOf]] and [[SetPrototypeOf]] essential internal methods, then <var>internalSlotsList</var> contains [[Prototype]].</li><li>Assert: If the caller will not be overriding all of <var>obj</var>'s [[SetPrototypeOf]], [[IsExtensible]], and [[PreventExtensions]] essential internal methods, then <var>internalSlotsList</var> contains [[Extensible]].</li><li>If <var>internalSlotsList</var> contains [[Extensible]], set <var>obj</var>.[[Extensible]] to <b>true</b>.</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n\n      <emu-note>\n        <p>Within this specification, exotic objects are created in abstract operations such as ArrayCreate and BoundFunctionCreate by first calling MakeBasicObject to obtain a basic, foundational object, and then overriding some or all of that object's internal methods. In order to encapsulate exotic object creation, the object's essential internal methods are never modified outside those operations.</p>\n      </emu-note>\n    ","3.2":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to retrieve the value of a specific property of an object.</dd>\n      </dl>\n      <emu-alg><ol><li>Return ? <emu-meta effects=\"user-code\"><var>obj</var>.[[Get]](<var>propertyKey</var>, <var>obj</var>)</emu-meta>.</li></ol></emu-alg>\n    ","3.3":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to retrieve the value of a specific property of an ECMAScript language value. If the value is not an object, the property lookup is performed using a wrapper object appropriate for the type of the value.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>obj</var> be ? ToObject(<var>value</var>).</li><li>Return ? <emu-meta effects=\"user-code\"><var>obj</var>.[[Get]](<var>propertyKey</var>, <var>value</var>)</emu-meta>.</li></ol></emu-alg>\n    ","3.4":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to set the value of a specific property of an object. <var>value</var> is the new value for the property.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>success</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[Set]](<var>propertyKey</var>, <var>value</var>, <var>obj</var>)</emu-meta>.</li><li>If <var>success</var> is <b>false</b> and <var>throw</var> is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    ","3.5":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to create a new own property of an object.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>newDesc</var> be the PropertyDescriptor { [[Value]]: <var>value</var>, [[Writable]]: <b>true</b>, [[Enumerable]]: <b>true</b>, [[Configurable]]: <b>true</b> }.</li><li>Return ? <emu-meta effects=\"user-code\"><var>obj</var>.[[DefineOwnProperty]](<var>propertyKey</var>, <var>newDesc</var>)</emu-meta>.</li></ol></emu-alg>\n      <emu-note>\n        <p>This abstract operation creates a property whose attributes are set to the same defaults used for properties created by the ECMAScript language assignment operator. Normally, the property will not already exist. If it does exist and is not configurable or if <var>obj</var> is not extensible, [[DefineOwnProperty]] will return <b>false</b>.</p>\n      </emu-note>\n    ","3.6":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to create a new own property of an object. It throws a <b>TypeError</b> exception if the requested property update cannot be performed.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>success</var> be ? CreateDataProperty(<var>obj</var>, <var>propertyKey</var>, <var>value</var>).</li><li>If <var>success</var> is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      <emu-note>\n        <p>This abstract operation creates a property whose attributes are set to the same defaults used for properties created by the ECMAScript language assignment operator. Normally, the property will not already exist. If it does exist and is not configurable or if <var>obj</var> is not extensible, [[DefineOwnProperty]] will return <b>false</b> causing this operation to throw a <b>TypeError</b> exception.</p>\n      </emu-note>\n    ","3.7":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to create a new non-enumerable own property of an ordinary object.</dd>\n      </dl>\n      <emu-alg><ol><li>Assert: <var>obj</var> is an ordinary, extensible object with no non-configurable properties.</li><li>Let <var>newDesc</var> be the PropertyDescriptor { [[Value]]: <var>value</var>, [[Writable]]: <b>true</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</li><li>Perform ! DefinePropertyOrThrow(<var>obj</var>, <var>propertyKey</var>, <var>newDesc</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      <emu-note>\n        <p>This abstract operation creates a property whose attributes are set to the same defaults used for properties created by the ECMAScript language assignment operator except it is not enumerable. Normally, the property will not already exist. If it does exist, DefinePropertyOrThrow is guaranteed to complete normally.</p>\n      </emu-note>\n    ","3.8":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to call the [[DefineOwnProperty]] internal method of an object in a manner that will throw a <b>TypeError</b> exception if the requested property update cannot be performed.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>success</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[DefineOwnProperty]](<var>propertyKey</var>, <var>desc</var>)</emu-meta>.</li><li>If <var>success</var> is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    ","3.9":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to remove a specific own property of an object. It throws an exception if the property is not configurable.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>success</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[Delete]](<var>propertyKey</var>)</emu-meta>.</li><li>If <var>success</var> is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    ","3.10":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to get the value of a specific property of an ECMAScript language value when the value of the property is expected to be a function.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>func</var> be ? GetV(<var>value</var>, <var>propertyKey</var>).</li><li>If <var>func</var> is either <b>undefined</b> or <b>null</b>, return <b>undefined</b>.</li><li>If IsCallable(<var>func</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return <var>func</var>.</li></ol></emu-alg>\n    ","3.11":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to determine whether an object has a property with the specified property key. The property may be either own or inherited.</dd>\n      </dl>\n      <emu-alg><ol><li>Return ? <emu-meta effects=\"user-code\"><var>obj</var>.[[HasProperty]](<var>propertyKey</var>)</emu-meta>.</li></ol></emu-alg>\n    ","3.12":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to determine whether an object has an own property with the specified property key.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>desc</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[GetOwnProperty]](<var>propertyKey</var>)</emu-meta>.</li><li>If <var>desc</var> is <b>undefined</b>, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n    ","3.13":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to call the [[Call]] internal method of a function object. <var>func</var> is the function object, <var>thisValue</var> is an ECMAScript language value that is the <b>this</b> value of the [[Call]], and <var>argumentsList</var> is the value passed to the corresponding argument of the internal method. If <var>argumentsList</var> is not present, a new empty List is used as its value.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>argumentsList</var> is not present, set <var>argumentsList</var> to a new empty List.</li><li>If IsCallable(<var>func</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return ? <emu-meta effects=\"user-code\"><var>func</var>.[[Call]](<var>thisValue</var>, <var>argumentsList</var>)</emu-meta>.</li></ol></emu-alg>\n    ","3.14":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to call the [[Construct]] internal method of a function object. <var>argumentsList</var> and <var>newTarget</var> are the values to be passed as the corresponding arguments of the internal method. If <var>argumentsList</var> is not present, a new empty List is used as its value. If <var>newTarget</var> is not present, <var>constructor</var> is used as its value.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>newTarget</var> is not present, set <var>newTarget</var> to <var>constructor</var>.</li><li>If <var>argumentsList</var> is not present, set <var>argumentsList</var> to a new empty List.</li><li>Return ? <emu-meta effects=\"user-code\"><var>constructor</var>.[[Construct]](<var>argumentsList</var>, <var>newTarget</var>)</emu-meta>.</li></ol></emu-alg>\n      <emu-note>\n        <p>If <var>newTarget</var> is not present, this operation is equivalent to: <code>new F(...argumentsList)</code></p>\n      </emu-note>\n    ","3.15":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to fix the set of own properties of an object.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>status</var> be ? <var>obj</var>.[[PreventExtensions]]().</li><li>If <var>status</var> is <b>false</b>, return <b>false</b>.</li><li>Let <var>keys</var> be ? <var>obj</var>.[[OwnPropertyKeys]]().</li><li>If <var>level</var> is <emu-const>sealed</emu-const>, then<ol><li>For each element <var>key</var> of <var>keys</var>, do<ol><li>Perform ? DefinePropertyOrThrow(<var>obj</var>, <var>key</var>, PropertyDescriptor { [[Configurable]]: <b>false</b> }).</li></ol></li></ol></li><li>Else,<ol><li>Assert: <var>level</var> is <emu-const>frozen</emu-const>.</li><li>For each element <var>key</var> of <var>keys</var>, do<ol><li>Let <var>currentDesc</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[GetOwnProperty]](<var>key</var>)</emu-meta>.</li><li>If <var>currentDesc</var> is not <b>undefined</b>, then<ol><li>If IsAccessorDescriptor(<var>currentDesc</var>) is <b>true</b>, then<ol><li>Let <var>desc</var> be the PropertyDescriptor { [[Configurable]]: <b>false</b> }.</li></ol></li><li>Else,<ol><li>Let <var>desc</var> be the PropertyDescriptor { [[Configurable]]: <b>false</b>, [[Writable]]: <b>false</b> }.</li></ol></li><li>Perform ? DefinePropertyOrThrow(<var>obj</var>, <var>key</var>, <var>desc</var>).</li></ol></li></ol></li></ol></li><li>Return <b>true</b>.</li></ol></emu-alg>\n    ","3.16":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to determine if the set of own properties of an object are fixed.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>extensible</var> be ? IsExtensible(<var>obj</var>).</li><li>If <var>extensible</var> is <b>true</b>, return <b>false</b>.</li><li>NOTE: If the object is extensible, none of its properties are examined.</li><li>Let <var>keys</var> be ? <var>obj</var>.[[OwnPropertyKeys]]().</li><li>For each element <var>key</var> of <var>keys</var>, do<ol><li>Let <var>currentDesc</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[GetOwnProperty]](<var>key</var>)</emu-meta>.</li><li>If <var>currentDesc</var> is not <b>undefined</b>, then<ol><li>If <var>currentDesc</var>.[[Configurable]] is <b>true</b>, return <b>false</b>.</li><li>If <var>level</var> is <emu-const>frozen</emu-const> and IsDataDescriptor(<var>currentDesc</var>) is <b>true</b>, then<ol><li>If <var>currentDesc</var>.[[Writable]] is <b>true</b>, return <b>false</b>.</li></ol></li></ol></li></ol></li><li>Return <b>true</b>.</li></ol></emu-alg>\n    ","3.17":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to create an Array whose elements are provided by <var>elements</var>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>array</var> be ! ArrayCreate(0).</li><li>Let <var>n</var> be 0.</li><li>For each element <var>e</var> of <var>elements</var>, do<ol><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, ! ToString(𝔽(<var>n</var>)), <var>e</var>).</li><li>Set <var>n</var> to <var>n</var> + 1.</li></ol></li><li>Return <var>array</var>.</li></ol></emu-alg>\n    ","3.18":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It returns the value of the <b>\"length\"</b> property of an array-like object.</dd>\n      </dl>\n      <emu-alg><ol><li>Return ℝ(? ToLength(? Get(<var>obj</var>, <b>\"length\"</b>))).</li></ol></emu-alg>\n      <p>An <dfn variants=\"array-like objects\">array-like object</dfn> is any object for which this operation returns a normal completion.</p>\n      <emu-note>\n        Typically, an array-like object would also have some properties with integer index names. However, that is not a requirement of this definition.\n      </emu-note>\n      <emu-note>\n        Arrays and String objects are examples of array-like objects.\n      </emu-note>\n    ","3.19":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to create a List value whose elements are provided by the indexed properties of <var>obj</var>. <var>validElementTypes</var> indicates the types of values that are allowed as elements.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>validElementTypes</var> is not present, set <var>validElementTypes</var> to <emu-const>all</emu-const>.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>len</var> be ? LengthOfArrayLike(<var>obj</var>).</li><li>Let <var>list</var> be a new empty List.</li><li>Let <var>index</var> be 0.</li><li>Repeat, while <var>index</var> &lt; <var>len</var>,<ol><li>Let <var>indexName</var> be ! ToString(𝔽(<var>index</var>)).</li><li>Let <var>next</var> be ? Get(<var>obj</var>, <var>indexName</var>).</li><li>If <var>validElementTypes</var> is <emu-const>property-key</emu-const> and <var>next</var> is not a property key, throw a <b>TypeError</b> exception.</li><li>Append <var>next</var> to <var>list</var>.</li><li>Set <var>index</var> to <var>index</var> + 1.</li></ol></li><li>Return <var>list</var>.</li></ol></emu-alg>\n    ","3.20":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to call a method property of an ECMAScript language value. <var>value</var> serves as both the lookup point for the property and the <b>this</b> value of the call. <var>argumentsList</var> is the list of arguments values passed to the method. If <var>argumentsList</var> is not present, a new empty List is used as its value.</dd>\n      </dl>\n\n      <emu-alg><ol><li>If <var>argumentsList</var> is not present, set <var>argumentsList</var> to a new empty List.</li><li>Let <var>func</var> be ? GetV(<var>value</var>, <var>propertyKey</var>).</li><li>Return ? Call(<var>func</var>, <var>value</var>, <var>argumentsList</var>).</li></ol></emu-alg>\n    ","3.21":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It implements the default algorithm for determining if <var>instance</var> inherits from the instance object inheritance path provided by <var>constructor</var>.</dd>\n      </dl>\n      <emu-alg><ol><li>If IsCallable(<var>constructor</var>) is <b>false</b>, return <b>false</b>.</li><li>If <var>constructor</var> has a [[BoundTargetFunction]] internal slot, then<ol><li>Let <var>boundConstructor</var> be <var>constructor</var>.[[BoundTargetFunction]].</li><li>Return ? InstanceofOperator(<var>instance</var>, <var>boundConstructor</var>).</li></ol></li><li>If <var>instance</var> is not an Object, return <b>false</b>.</li><li>Let <var>proto</var> be ? Get(<var>constructor</var>, <b>\"prototype\"</b>).</li><li>If <var>proto</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Repeat,<ol><li>Set <var>instance</var> to ? <emu-meta effects=\"user-code\"><var>instance</var>.[[GetPrototypeOf]]()</emu-meta>.</li><li>If <var>instance</var> is <b>null</b>, return <b>false</b>.</li><li>If SameValue(<var>proto</var>, <var>instance</var>) is <b>true</b>, return <b>true</b>.</li></ol></li></ol></emu-alg>\n    ","3.22":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to retrieve the constructor that should be used to create new objects that are derived from <var>obj</var>. <var>defaultConstructor</var> is the constructor to use if a constructor <code class=\"emu-intrinsic\">%Symbol.species%</code> property cannot be found starting from <var>obj</var>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>constructor</var> be ? Get(<var>obj</var>, <b>\"constructor\"</b>).</li><li>If <var>constructor</var> is <b>undefined</b>, return <var>defaultConstructor</var>.</li><li>If <var>constructor</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>species</var> be ? Get(<var>constructor</var>, <code class=\"emu-intrinsic\">%Symbol.species%</code>).</li><li>If <var>species</var> is either <b>undefined</b> or <b>null</b>, return <var>defaultConstructor</var>.</li><li>If IsConstructor(<var>species</var>) is <b>true</b>, return <var>species</var>.</li><li>Throw a <b>TypeError</b> exception.</li></ol></emu-alg>\n    ","3.23":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>ownKeys</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[OwnPropertyKeys]]()</emu-meta>.</li><li>Let <var>results</var> be a new empty List.</li><li>For each element <var>key</var> of <var>ownKeys</var>, do<ol><li>If <var>key</var> is a String, then<ol><li>Let <var>desc</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[GetOwnProperty]](<var>key</var>)</emu-meta>.</li><li>If <var>desc</var> is not <b>undefined</b> and <var>desc</var>.[[Enumerable]] is <b>true</b>, then<ol><li>If <var>kind</var> is <emu-const>key</emu-const>, then<ol><li>Append <var>key</var> to <var>results</var>.</li></ol></li><li>Else,<ol><li>Let <var>value</var> be ? Get(<var>obj</var>, <var>key</var>).</li><li>If <var>kind</var> is <emu-const>value</emu-const>, then<ol><li>Append <var>value</var> to <var>results</var>.</li></ol></li><li>Else,<ol><li>Assert: <var>kind</var> is <emu-const>key+value</emu-const>.</li><li>Let <var>entry</var> be CreateArrayFromList(« <var>key</var>, <var>value</var> »).</li><li>Append <var>entry</var> to <var>results</var>.</li></ol></li></ol></li></ol></li></ol></li></ol></li><li>Return <var>results</var>.</li></ol></emu-alg>\n    ","3.24":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If <var>func</var> has a [[Realm]] internal slot, then<ol><li>Return <var>func</var>.[[Realm]].</li></ol></li><li>If <var>func</var> is a bound function exotic object, then<ol><li>Let <var>boundTargetFunction</var> be <var>func</var>.[[BoundTargetFunction]].</li><li>Return ? GetFunctionRealm(<var>boundTargetFunction</var>).</li></ol></li><li>If <var>func</var> is a Proxy exotic object, then<ol><li>Perform ? ValidateNonRevokedProxy(<var>func</var>).</li><li>Let <var>proxyTarget</var> be <var>func</var>.[[ProxyTarget]].</li><li>Assert: <var>proxyTarget</var> is a function object.</li><li>Return ? GetFunctionRealm(<var>proxyTarget</var>).</li></ol></li><li>Return the current Realm Record.</li></ol></emu-alg>\n      <emu-note>\n        <p>Step step-getfunctionrealm-default-return will only be reached if <var>func</var> is a non-standard function exotic object that does not have a [[Realm]] internal slot.</p>\n      </emu-note>\n    ","3.25":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If <var>source</var> is either <b>undefined</b> or <b>null</b>, return <emu-const>unused</emu-const>.</li><li>Let <var>from</var> be ! ToObject(<var>source</var>).</li><li>Let <var>keys</var> be ? <emu-meta effects=\"user-code\"><var>from</var>.[[OwnPropertyKeys]]()</emu-meta>.</li><li>For each element <var>nextKey</var> of <var>keys</var>, do<ol><li>Let <var>excluded</var> be <b>false</b>.</li><li>For each element <var>e</var> of <var>excludedItems</var>, do<ol><li>If SameValue(<var>e</var>, <var>nextKey</var>) is <b>true</b>, then<ol><li>Set <var>excluded</var> to <b>true</b>.</li></ol></li></ol></li><li>If <var>excluded</var> is <b>false</b>, then<ol><li>Let <var>desc</var> be ? <emu-meta effects=\"user-code\"><var>from</var>.[[GetOwnProperty]](<var>nextKey</var>)</emu-meta>.</li><li>If <var>desc</var> is not <b>undefined</b> and <var>desc</var>.[[Enumerable]] is <b>true</b>, then<ol><li>Let <var>propValue</var> be ? Get(<var>from</var>, <var>nextKey</var>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>target</var>, <var>nextKey</var>, <var>propValue</var>).</li></ol></li></ol></li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      <emu-note>\n        <p>The target passed in here is always a newly created object which is not directly accessible in case of an error being thrown.</p>\n      </emu-note>\n    ","3.26":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If <var>obj</var>.[[PrivateElements]] contains a PrivateElement <var>pe</var> such that <var>pe</var>.[[Key]] is <var>privateName</var>, then<ol><li>Return <var>pe</var>.</li></ol></li><li>Return <emu-const>empty</emu-const>.</li></ol></emu-alg>\n    ","3.27":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If the host is a web browser, then<ol><li>Perform ? HostEnsureCanAddPrivateElement(<var>obj</var>).</li></ol></li><li>Let <var>entry</var> be PrivateElementFind(<var>obj</var>, <var>privateName</var>).</li><li>If <var>entry</var> is not <emu-const>empty</emu-const>, throw a <b>TypeError</b> exception.</li><li>Append PrivateElement { [[Key]]: <var>privateName</var>, [[Kind]]: <emu-const>field</emu-const>, [[Value]]: <var>value</var> } to <var>obj</var>.[[PrivateElements]].</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    ","3.28":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Assert: <var>method</var>.[[Kind]] is either <emu-const>method</emu-const> or <emu-const>accessor</emu-const>.</li><li>If the host is a web browser, then<ol><li>Perform ? HostEnsureCanAddPrivateElement(<var>obj</var>).</li></ol></li><li>Let <var>entry</var> be PrivateElementFind(<var>obj</var>, <var>method</var>.[[Key]]).</li><li>If <var>entry</var> is not <emu-const>empty</emu-const>, throw a <b>TypeError</b> exception.</li><li>Append <var>method</var> to <var>obj</var>.[[PrivateElements]].</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      <emu-note>\n        <p>The values for private methods and accessors are shared across instances. This operation does not create a new copy of the method or accessor.</p>\n      </emu-note>\n    ","3.29":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It allows host environments to prevent the addition of private elements to particular host-defined exotic objects.</dd>\n      </dl>\n      <p>An implementation of HostEnsureCanAddPrivateElement must conform to the following requirements:</p>\n      <ul>\n        <li>If <var>obj</var> is not a host-defined exotic object, this abstract operation must return NormalCompletion(<emu-const>unused</emu-const>) and perform no other steps.</li>\n        <li>Any two calls of this abstract operation with the same argument must return the same kind of Completion Record.</li>\n      </ul>\n      <p>The default implementation of HostEnsureCanAddPrivateElement is to return NormalCompletion(<emu-const>unused</emu-const>).</p>\n      <p>This abstract operation is only invoked by ECMAScript hosts that are web browsers.</p>\n    ","3.30":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>entry</var> be PrivateElementFind(<var>obj</var>, <var>privateName</var>).</li><li>If <var>entry</var> is <emu-const>empty</emu-const>, throw a <b>TypeError</b> exception.</li><li>If <var>entry</var>.[[Kind]] is either <emu-const>field</emu-const> or <emu-const>method</emu-const>, then<ol><li>Return <var>entry</var>.[[Value]].</li></ol></li><li>Assert: <var>entry</var>.[[Kind]] is <emu-const>accessor</emu-const>.</li><li>If <var>entry</var>.[[Get]] is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>getter</var> be <var>entry</var>.[[Get]].</li><li>Return ? Call(<var>getter</var>, <var>obj</var>).</li></ol></emu-alg>\n    ","3.31":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>entry</var> be PrivateElementFind(<var>obj</var>, <var>privateName</var>).</li><li>If <var>entry</var> is <emu-const>empty</emu-const>, throw a <b>TypeError</b> exception.</li><li>If <var>entry</var>.[[Kind]] is <emu-const>method</emu-const>, throw a <b>TypeError</b> exception.</li><li>If <var>entry</var>.[[Kind]] is <emu-const>field</emu-const>, then<ol><li>Set <var>entry</var>.[[Value]] to <var>value</var>.</li></ol></li><li>Else,<ol><li>Assert: <var>entry</var>.[[Kind]] is <emu-const>accessor</emu-const>.</li><li>If <var>entry</var>.[[Set]] is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>setter</var> be <var>entry</var>.[[Set]].</li><li>Perform ? Call(<var>setter</var>, <var>obj</var>, « <var>value</var> »).</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    ","3.32":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>fieldName</var> be <var>fieldRecord</var>.[[Name]].</li><li>Let <var>initializer</var> be <var>fieldRecord</var>.[[Initializer]].</li><li>If <var>initializer</var> is not <emu-const>empty</emu-const>, then<ol><li>Let <var>initValue</var> be ? Call(<var>initializer</var>, <var>receiver</var>).</li></ol></li><li>Else,<ol><li>Let <var>initValue</var> be <b>undefined</b>.</li></ol></li><li>If <var>fieldName</var> is a Private Name, then<ol><li>Perform ? PrivateFieldAdd(<var>receiver</var>, <var>fieldName</var>, <var>initValue</var>).</li></ol></li><li>Else,<ol><li>Assert: <var>fieldName</var> is a property key.</li><li>Perform ? CreateDataPropertyOrThrow(<var>receiver</var>, <var>fieldName</var>, <var>initValue</var>).</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    ","3.33":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>methods</var> be <var>constructor</var>.[[PrivateMethods]].</li><li>For each PrivateElement <var>method</var> of <var>methods</var>, do<ol><li>Perform ? PrivateMethodOrAccessorAdd(<var>obj</var>, <var>method</var>).</li></ol></li><li>Let <var>fields</var> be <var>constructor</var>.[[Fields]].</li><li>For each element <var>fieldRecord</var> of <var>fields</var>, do<ol><li>Perform ? DefineField(<var>obj</var>, <var>fieldRecord</var>).</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    ","3.34":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>For each Record { [[Key]], [[Elements]] } <var>group</var> of <var>groups</var>, do<ol><li>If SameValue(<var>group</var>.[[Key]], <var>key</var>) is <b>true</b>, then<ol><li>Assert: Exactly one element of <var>groups</var> meets this criterion.</li><li>Append <var>value</var> to <var>group</var>.[[Elements]].</li><li>Return <emu-const>unused</emu-const>.</li></ol></li></ol></li><li>Let <var>group</var> be the Record { [[Key]]: <var>key</var>, [[Elements]]: « <var>value</var> » }.</li><li>Append <var>group</var> to <var>groups</var>.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    ","3.35":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Perform ? RequireObjectCoercible(<var>items</var>).</li><li>If IsCallable(<var>callback</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>groups</var> be a new empty List.</li><li>Let <var>iteratorRecord</var> be ? GetIterator(<var>items</var>, <emu-const>sync</emu-const>).</li><li>Let <var>k</var> be 0.</li><li>Repeat,<ol><li>If <var>k</var> ≥ 2<sup>53</sup> - 1, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>TypeError</b> object).</li><li>Return ? IteratorClose(<var>iteratorRecord</var>, <var>error</var>).</li></ol></li><li>Let <var>next</var> be ? IteratorStepValue(<var>iteratorRecord</var>).</li><li>If <var>next</var> is <emu-const>done</emu-const>, then<ol><li>Return <var>groups</var>.</li></ol></li><li>Let <var>value</var> be <var>next</var>.</li><li>Let <var>key</var> be Completion(Call(<var>callback</var>, <b>undefined</b>, « <var>value</var>, 𝔽(<var>k</var>) »)).</li><li>IfAbruptCloseIterator(<var>key</var>, <var>iteratorRecord</var>).</li><li>If <var>keyCoercion</var> is <emu-const>property</emu-const>, then<ol><li>Set <var>key</var> to Completion(ToPropertyKey(<var>key</var>)).</li><li>IfAbruptCloseIterator(<var>key</var>, <var>iteratorRecord</var>).</li></ol></li><li>Else,<ol><li>Assert: <var>keyCoercion</var> is <emu-const>collection</emu-const>.</li><li>Set <var>key</var> to CanonicalizeKeyedCollectionKey(<var>key</var>).</li></ol></li><li>Perform AddValueToKeyedGroup(<var>groups</var>, <var>key</var>, <var>value</var>).</li><li>Set <var>k</var> to <var>k</var> + 1.</li></ol></li></ol></emu-alg>\n    ","3.36":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If <var>options</var> is <b>undefined</b>, then<ol><li>Return OrdinaryObjectCreate(<b>null</b>).</li></ol></li><li>If <var>options</var> is an Object, then<ol><li>Return <var>options</var>.</li></ol></li><li>Throw a <b>TypeError</b> exception.</li></ol></emu-alg>\n    ","3.37":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If <var>thisValue</var> is not an Object, then<ol><li>Throw a <b>TypeError</b> exception.</li></ol></li><li>If SameValue(<var>thisValue</var>, <var>home</var>) is <b>true</b>, then<ol><li>NOTE: Throwing here emulates assignment to a non-writable data property on the <var>home</var> object in strict mode code.</li><li>Throw a <b>TypeError</b> exception.</li></ol></li><li>Let <var>desc</var> be ? <var>thisValue</var>.[[GetOwnProperty]](<var>propertyKey</var>).</li><li>If <var>desc</var> is <b>undefined</b>, then<ol><li>Perform ? CreateDataPropertyOrThrow(<var>thisValue</var>, <var>propertyKey</var>, <var>value</var>).</li></ol></li><li>Else,<ol><li>Perform ? Set(<var>thisValue</var>, <var>propertyKey</var>, <var>value</var>, <b>true</b>).</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    ","4.1":"<p>An <dfn variants=\"Iterator Records\">Iterator Record</dfn> is a Record value used to encapsulate an iterator or async iterator along with the <code>next</code> method.</p>\n      <p>Iterator Records have the fields listed in table-iterator-record-fields.</p>\n      <emu-table id=\"table-iterator-record-fields\" caption=\"Iterator Record Fields\">\n        <table>\n          <thead>\n            <tr>\n              <th>\n                Field Name\n              </th>\n              <th>\n                Value\n              </th>\n              <th>\n                Meaning\n              </th>\n            </tr>\n          </thead>\n          <tr>\n            <td>\n              [[Iterator]]\n            </td>\n            <td>\n              an Object\n            </td>\n            <td>\n              An object that conforms to the iterator interface or the async iterator interface.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[NextMethod]]\n            </td>\n            <td>\n              an ECMAScript language value\n            </td>\n            <td>\n              The <code>next</code> method of the [[Iterator]] object.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[Done]]\n            </td>\n            <td>\n              a Boolean\n            </td>\n            <td>\n              Whether the iterator has completed or been closed.\n            </td>\n          </tr>\n        </table>\n      </emu-table>\n    ","4.2":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>nextMethod</var> be ? Get(<var>obj</var>, <b>\"next\"</b>).</li><li>Let <var>iteratorRecord</var> be the Iterator Record { [[Iterator]]: <var>obj</var>, [[NextMethod]]: <var>nextMethod</var>, [[Done]]: <b>false</b> }.</li><li>Return <var>iteratorRecord</var>.</li></ol></emu-alg>\n    ","4.3":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>iterator</var> be ? Call(<var>method</var>, <var>obj</var>).</li><li>If <var>iterator</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Return ? GetIteratorDirect(<var>iterator</var>).</li></ol></emu-alg>\n    ","4.4":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If <var>kind</var> is <emu-const>async</emu-const>, then<ol><li>Let <var>method</var> be ? GetMethod(<var>obj</var>, <code class=\"emu-intrinsic\">%Symbol.asyncIterator%</code>).</li><li>If <var>method</var> is <b>undefined</b>, then<ol><li>Let <var>syncMethod</var> be ? GetMethod(<var>obj</var>, <code class=\"emu-intrinsic\">%Symbol.iterator%</code>).</li><li>If <var>syncMethod</var> is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>syncIteratorRecord</var> be ? GetIteratorFromMethod(<var>obj</var>, <var>syncMethod</var>).</li><li>Return CreateAsyncFromSyncIterator(<var>syncIteratorRecord</var>).</li></ol></li></ol></li><li>Else,<ol><li>Let <var>method</var> be ? GetMethod(<var>obj</var>, <code class=\"emu-intrinsic\">%Symbol.iterator%</code>).</li></ol></li><li>If <var>method</var> is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Return ? GetIteratorFromMethod(<var>obj</var>, <var>method</var>).</li></ol></emu-alg>\n    ","4.5":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If <var>obj</var> is not an Object, then<ol><li>If <var>primitiveHandling</var> is <emu-const>reject-primitives</emu-const>, throw a <b>TypeError</b> exception.</li><li>Assert: <var>primitiveHandling</var> is <emu-const>iterate-string-primitives</emu-const>.</li><li>If <var>obj</var> is not a String, throw a <b>TypeError</b> exception.</li></ol></li><li>Let <var>method</var> be ? GetMethod(<var>obj</var>, <code class=\"emu-intrinsic\">%Symbol.iterator%</code>).</li><li>If <var>method</var> is <b>undefined</b>, then<ol><li>Let <var>iterator</var> be <var>obj</var>.</li></ol></li><li>Else,<ol><li>Let <var>iterator</var> be ? Call(<var>method</var>, <var>obj</var>).</li></ol></li><li>If <var>iterator</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Return ? GetIteratorDirect(<var>iterator</var>).</li></ol></emu-alg>\n    ","4.6":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If <var>value</var> is not present, then<ol><li>Let <var>result</var> be Completion(Call(<var>iteratorRecord</var>.[[NextMethod]], <var>iteratorRecord</var>.[[Iterator]])).</li></ol></li><li>Else,<ol><li>Let <var>result</var> be Completion(Call(<var>iteratorRecord</var>.[[NextMethod]], <var>iteratorRecord</var>.[[Iterator]], « <var>value</var> »)).</li></ol></li><li>If <var>result</var> is a throw completion, then<ol><li>Set <var>iteratorRecord</var>.[[Done]] to <b>true</b>.</li><li>Return ? <var>result</var>.</li></ol></li><li>Set <var>result</var> to ! <var>result</var>.</li><li>If <var>result</var> is not an Object, then<ol><li>Set <var>iteratorRecord</var>.[[Done]] to <b>true</b>.</li><li>Throw a <b>TypeError</b> exception.</li></ol></li><li>Return <var>result</var>.</li></ol></emu-alg>\n    ","4.7":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Return ToBoolean(? Get(<var>iteratorResult</var>, <b>\"done\"</b>)).</li></ol></emu-alg>\n    ","4.8":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Return ? Get(<var>iteratorResult</var>, <b>\"value\"</b>).</li></ol></emu-alg>\n    ","4.9":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It requests the next value from <var>iteratorRecord</var>.[[Iterator]] by calling <var>iteratorRecord</var>.[[NextMethod]] and returns either <emu-const>done</emu-const> indicating that the iterator has reached its end or the IteratorResult object if a next value is available.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>result</var> be ? IteratorNext(<var>iteratorRecord</var>).</li><li>Let <var>done</var> be Completion(IteratorComplete(<var>result</var>)).</li><li>If <var>done</var> is a throw completion, then<ol><li>Set <var>iteratorRecord</var>.[[Done]] to <b>true</b>.</li><li>Return ? <var>done</var>.</li></ol></li><li>Set <var>done</var> to ! <var>done</var>.</li><li>If <var>done</var> is <b>true</b>, then<ol><li>Set <var>iteratorRecord</var>.[[Done]] to <b>true</b>.</li><li>Return <emu-const>done</emu-const>.</li></ol></li><li>Return <var>result</var>.</li></ol></emu-alg>\n    ","4.10":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It requests the next value from <var>iteratorRecord</var>.[[Iterator]] by calling <var>iteratorRecord</var>.[[NextMethod]] and returns either <emu-const>done</emu-const> indicating that the iterator has reached its end or the value from the IteratorResult object if a next value is available.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>result</var> be ? IteratorStep(<var>iteratorRecord</var>).</li><li>If <var>result</var> is <emu-const>done</emu-const>, then<ol><li>Return <emu-const>done</emu-const>.</li></ol></li><li>Let <var>value</var> be Completion(IteratorValue(<var>result</var>)).</li><li>If <var>value</var> is a throw completion, then<ol><li>Set <var>iteratorRecord</var>.[[Done]] to <b>true</b>.</li></ol></li><li>Return ? <var>value</var>.</li></ol></emu-alg>\n    ","4.11":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to notify an iterator that it should perform any actions it would normally perform when it has reached its completed state.</dd>\n      </dl>\n      <emu-alg><ol><li>Assert: <var>iteratorRecord</var>.[[Iterator]] is an Object.</li><li>Let <var>iterator</var> be <var>iteratorRecord</var>.[[Iterator]].</li><li>Let <var>innerResult</var> be Completion(GetMethod(<var>iterator</var>, <b>\"return\"</b>)).</li><li>If <var>innerResult</var> is a normal completion, then<ol><li>Let <var>return</var> be <var>innerResult</var>.[[Value]].</li><li>If <var>return</var> is <b>undefined</b>, return ? <var>completion</var>.</li><li>Set <var>innerResult</var> to Completion(Call(<var>return</var>, <var>iterator</var>)).</li></ol></li><li>If <var>completion</var> is a throw completion, return ? <var>completion</var>.</li><li>If <var>innerResult</var> is a throw completion, return ? <var>innerResult</var>.</li><li>If <var>innerResult</var>.[[Value]] is not an Object, throw a <b>TypeError</b> exception.</li><li>Return ? <var>completion</var>.</li></ol></emu-alg>\n    ","4.12":"<dl class=\"header\">\n        <dt>skip global checks</dt>\n        <dd>true</dd>\n      </dl>\n      <emu-alg><ol><li>For each element <var>iter</var> of <var>iters</var>, in reverse List order, do<ol><li>Set <var>completion</var> to Completion(IteratorClose(<var>iter</var>, <var>completion</var>)).</li></ol></li><li>Return ? <var>completion</var>.</li></ol></emu-alg>\n    ","4.13":"<p>IfAbruptCloseIterator is a shorthand for a sequence of algorithm steps that use an Iterator Record. An algorithm step of the form:</p>\n      <emu-alg><ol><li>IfAbruptCloseIterator(<var>value</var>, <var>iteratorRecord</var>).</li></ol></emu-alg>\n      <p>means the same thing as:</p>\n      <emu-alg><ol><li>Assert: <var>value</var> is a Completion Record.</li><li>If <var>value</var> is an abrupt completion, return ? IteratorClose(<var>iteratorRecord</var>, <var>value</var>).</li><li>Set <var>value</var> to ! <var>value</var>.</li></ol></emu-alg>\n    ","4.14":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to notify an async iterator that it should perform any actions it would normally perform when it has reached its completed state.</dd>\n      </dl>\n      <emu-alg><ol><li>Assert: <var>iteratorRecord</var>.[[Iterator]] is an Object.</li><li>Let <var>iterator</var> be <var>iteratorRecord</var>.[[Iterator]].</li><li>Let <var>innerResult</var> be Completion(GetMethod(<var>iterator</var>, <b>\"return\"</b>)).</li><li>If <var>innerResult</var> is a normal completion, then<ol><li>Let <var>return</var> be <var>innerResult</var>.[[Value]].</li><li>If <var>return</var> is <b>undefined</b>, return ? <var>completion</var>.</li><li>Set <var>innerResult</var> to Completion(Call(<var>return</var>, <var>iterator</var>)).</li><li>If <var>innerResult</var> is a normal completion, set <var>innerResult</var> to Completion(Await(<var>innerResult</var>.[[Value]])).</li></ol></li><li>If <var>completion</var> is a throw completion, return ? <var>completion</var>.</li><li>If <var>innerResult</var> is a throw completion, return ? <var>innerResult</var>.</li><li>If <var>innerResult</var>.[[Value]] is not an Object, throw a <b>TypeError</b> exception.</li><li>Return ? <var>completion</var>.</li></ol></emu-alg>\n    ","4.15":"<p>IfAbruptCloseAsyncIterator is a shorthand for a sequence of algorithm steps that use an Iterator Record. An algorithm step of the form:</p>\n      <emu-alg><ol><li>IfAbruptCloseAsyncIterator(<var>value</var>, <var>iteratorRecord</var>).</li></ol></emu-alg>\n      <p>means the same thing as:</p>\n      <emu-alg><ol><li>Assert: <var>value</var> is a Completion Record.</li><li>If <var>value</var> is an abrupt completion, return ? AsyncIteratorClose(<var>iteratorRecord</var>, <var>value</var>).</li><li>Set <var>value</var> to ! <var>value</var>.</li></ol></emu-alg>\n    ","4.16":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It creates an object that conforms to the IteratorResult interface.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>obj</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Object.prototype%</code>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>obj</var>, <b>\"value\"</b>, <var>value</var>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>obj</var>, <b>\"done\"</b>, <var>done</var>).</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n    ","4.17":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It creates an Iterator Record whose [[NextMethod]] returns the successive elements of <var>list</var>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>closure</var> be a new Abstract Closure with no parameters that captures <var>list</var> and performs the following steps when called:<ol><li>For each element <var>value</var> of <var>list</var>, do<ol><li>Perform ? GeneratorYield(CreateIteratorResultObject(<var>value</var>, <b>false</b>)).</li></ol></li><li>Return NormalCompletion(<b>undefined</b>).</li></ol></li><li>Let <var>iterator</var> be CreateIteratorFromClosure(<var>closure</var>, <emu-const>empty</emu-const>, <code class=\"emu-intrinsic\">%Iterator.prototype%</code>).</li><li>Return the Iterator Record { [[Iterator]]: <var>iterator</var>, [[NextMethod]]: <code class=\"emu-intrinsic\">%GeneratorPrototype.next%</code>, [[Done]]: <b>false</b> }.</li></ol></emu-alg>\n      <emu-note>\n        <p>The list iterator object is never directly accessible to ECMAScript code.</p>\n      </emu-note>\n    ","4.18":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>values</var> be a new empty List.</li><li>Repeat,<ol><li>Let <var>next</var> be ? IteratorStepValue(<var>iteratorRecord</var>).</li><li>If <var>next</var> is <emu-const>done</emu-const>, then<ol><li>Return <var>values</var>.</li></ol></li><li>Append <var>next</var> to <var>values</var>.</li></ol></li></ol></emu-alg>\n    "};
const _basePath = process.env.NEXT_PUBLIC_BASE_PATH || '';
const sections = _basePath
  ? Object.fromEntries(
      Object.entries(_sections).map(([k, v]) => [
        k,
        v.replaceAll('href="/', `href="${_basePath}/`),
      ])
    )
  : _sections;
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
