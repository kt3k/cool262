// Generated from ecma262/spec.html — do not edit by hand.
const _sections = {"1":"","2":"<p>A RegExp object contains a regular expression and the associated flags.</p>\n    <emu-note>\n      <p>The form and functionality of regular expressions is modelled after the regular expression facility in the Perl 5 programming language.</p>\n    </emu-note>\n\n    ","":"","1.1":"<p>The String constructor:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%String%</emu-intrinsic></dfn>.</li>\n        <li>is the initial value of the <b>\"String\"</b> property of the global object.</li>\n        <li>creates and initializes a new String object when called as a constructor.</li>\n        <li>performs a type conversion when called as a function rather than as a constructor.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified String behaviour must include a <code>super</code> call to the String constructor to create and initialize the subclass instance with a [[StringData]] internal slot.</li>\n      </ul>\n\n      ","1.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>value</var> is not present, then<ol><li>Let <var>str</var> be the empty String.</li></ol></li><li>Else,<ol><li>If NewTarget is <b>undefined</b> and <var>value</var> is a Symbol, return SymbolDescriptiveString(<var>value</var>).</li><li>Let <var>str</var> be ? ToString(<var>value</var>).</li></ol></li><li>If NewTarget is <b>undefined</b>, return <var>str</var>.</li><li>Return StringCreate(<var>str</var>, ? GetPrototypeFromConstructor(NewTarget, <b>\"<emu-intrinsic>%String.prototype%</emu-intrinsic>\"</b>)).</li></ol></emu-alg>\n      ","1.2":"<p>The String constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Function.prototype%</emu-intrinsic>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","1.2.1":"<p>This function may be called with any number of arguments which form the rest parameter <var>codeUnits</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>result</var> be the empty String.</li><li>For each element <var>next</var> of <var>codeUnits</var>, do<ol><li>Let <var>nextCU</var> be the code unit whose numeric value is ℝ(? ToUint16(<var>next</var>)).</li><li>Set <var>result</var> to the string-concatenation of <var>result</var> and <var>nextCU</var>.</li></ol></li><li>Return <var>result</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this function is <b>1</b><sub>𝔽</sub>.</p>\n      ","1.2.2":"<p>This function may be called with any number of arguments which form the rest parameter <var>codePoints</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>result</var> be the empty String.</li><li>For each element <var>next</var> of <var>codePoints</var>, do<ol><li>Let <var>nextCP</var> be ? ToNumber(<var>next</var>).</li><li>If <var>nextCP</var> is not an integral Number, throw a <b>RangeError</b> exception.</li><li>If ℝ(<var>nextCP</var>) &lt; 0 or ℝ(<var>nextCP</var>) > 0x10FFFF, throw a <b>RangeError</b> exception.</li><li>Set <var>result</var> to the string-concatenation of <var>result</var> and UTF16EncodeCodePoint(ℝ(<var>nextCP</var>)).</li></ol></li><li>Assert: If <var>codePoints</var> is empty, then <var>result</var> is the empty String.</li><li>Return <var>result</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this function is <b>1</b><sub>𝔽</sub>.</p>\n      ","1.2.3":"<p>The initial value of <code>String.prototype</code> is the String prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.2.4":"<p>This function may be called with a variable number of arguments. The first argument is <var>template</var> and the remainder of the arguments form the List <var>substitutions</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>substitutionCount</var> be the number of elements in <var>substitutions</var>.</li><li>Let <var>cooked</var> be ? ToObject(<var>template</var>).</li><li>Let <var>literals</var> be ? ToObject(? Get(<var>cooked</var>, <b>\"raw\"</b>)).</li><li>Let <var>literalCount</var> be ? LengthOfArrayLike(<var>literals</var>).</li><li>If <var>literalCount</var> ≤ 0, return the empty String.</li><li>Let <var>result</var> be the empty String.</li><li>Let <var>nextIndex</var> be 0.</li><li>Repeat,<ol><li>Let <var>nextLiteralVal</var> be ? Get(<var>literals</var>, ! ToString(𝔽(<var>nextIndex</var>))).</li><li>Let <var>nextLiteral</var> be ? ToString(<var>nextLiteralVal</var>).</li><li>Set <var>result</var> to the string-concatenation of <var>result</var> and <var>nextLiteral</var>.</li><li>If <var>nextIndex</var> + 1 = <var>literalCount</var>, return <var>result</var>.</li><li>If <var>nextIndex</var> &lt; <var>substitutionCount</var>, then<ol><li>Let <var>nextSubVal</var> be <var>substitutions</var>[<var>nextIndex</var>].</li><li>Let <var>nextSub</var> be ? ToString(<var>nextSubVal</var>).</li><li>Set <var>result</var> to the string-concatenation of <var>result</var> and <var>nextSub</var>.</li></ol></li><li>Set <var>nextIndex</var> to <var>nextIndex</var> + 1.</li></ol></li></ol></emu-alg>\n        <emu-note>\n          <p>This function is intended for use as a tag function of a Tagged Template (<a href=\"/ecmascript-language-expressions#sec-tagged-templates\">13.3.11</a>). When called as such, the first argument will be a well formed template object and the rest parameter will contain the substitution values.</p>\n        </emu-note>\n      ","1.3":"<p>The <dfn>String prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%String.prototype%</emu-intrinsic></dfn>.</li>\n        <li>is a String exotic object and has the internal methods specified for such objects.</li>\n        <li>has a [[StringData]] internal slot whose value is the empty String.</li>\n        <li>has a <b>\"length\"</b> property whose initial value is <b>+0</b><sub>𝔽</sub> and whose attributes are { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</li>\n        <li>has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Object.prototype%</emu-intrinsic>.</li>\n      </ul>\n      <p>Unless explicitly stated otherwise, the methods of the String prototype object defined below are not generic and the <b>this</b> value passed to them must be either a String value or an object that has a [[StringData]] internal slot that has been initialized to a String value.</p>\n\n      ","1.3.1":"<emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>len</var> be the length of <var>str</var>.</li><li>Let <var>relativeIndex</var> be ? ToIntegerOrInfinity(<var>index</var>).</li><li>If <var>relativeIndex</var> ≥ 0, then<ol><li>Let <var>k</var> be <var>relativeIndex</var>.</li></ol></li><li>Else,<ol><li>Let <var>k</var> be <var>len</var> + <var>relativeIndex</var>.</li></ol></li><li>If <var>k</var> &lt; 0 or <var>k</var> ≥ <var>len</var>, return <b>undefined</b>.</li><li>Return the substring of <var>str</var> from <var>k</var> to <var>k</var> + 1.</li></ol></emu-alg>\n      ","1.3.2":"<emu-note>\n          <p>This method returns a single element String containing the code unit at index <var>pos</var> within the String value resulting from converting this object to a String. If there is no element at that index, the result is the empty String. The result is a String value, not a String object.</p>\n          <p>If <code>pos</code> is an integral Number, then the result of <code>x.charAt(pos)</code> is equivalent to the result of <code>x.substring(pos, pos + 1)</code>.</p>\n        </emu-note>\n        <p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>position</var> be ? ToIntegerOrInfinity(<var>pos</var>).</li><li>Let <var>size</var> be the length of <var>str</var>.</li><li>If <var>position</var> &lt; 0 or <var>position</var> ≥ <var>size</var>, return the empty String.</li><li>Return the substring of <var>str</var> from <var>position</var> to <var>position</var> + 1.</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.3":"<emu-note>\n          <p>This method returns a Number (a non-negative integral Number less than 2<sup>16</sup>) that is the numeric value of the code unit at index <var>pos</var> within the String resulting from converting this object to a String. If there is no element at that index, the result is <b>NaN</b>.</p>\n        </emu-note>\n        <p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>position</var> be ? ToIntegerOrInfinity(<var>pos</var>).</li><li>Let <var>size</var> be the length of <var>str</var>.</li><li>If <var>position</var> &lt; 0 or <var>position</var> ≥ <var>size</var>, return <b>NaN</b>.</li><li>Return the Number value for the numeric value of the code unit at index <var>position</var> within the String <var>str</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.4":"<emu-note>\n          <p>This method returns a non-negative integral Number less than or equal to <b>0x10FFFF</b><sub>𝔽</sub> that is the numeric value of the UTF-16 encoded code point (<a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>) starting at the string element at index <var>pos</var> within the String resulting from converting this object to a String. If there is no element at that index, the result is <b>undefined</b>. If a valid UTF-16 surrogate pair does not begin at <var>pos</var>, the result is the code unit at <var>pos</var>.</p>\n        </emu-note>\n        <p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>position</var> be ? ToIntegerOrInfinity(<var>pos</var>).</li><li>Let <var>size</var> be the length of <var>str</var>.</li><li>If <var>position</var> &lt; 0 or <var>position</var> ≥ <var>size</var>, return <b>undefined</b>.</li><li>Let <var>cp</var> be CodePointAt(<var>str</var>, <var>position</var>).</li><li>Return 𝔽(<var>cp</var>.[[CodePoint]]).</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.5":"<emu-note>\n          <p>When this method is called it returns the String value consisting of the code units of the <b>this</b> value (converted to a String) followed by the code units of each of the arguments converted to a String. The result is a String value, not a String object.</p>\n        </emu-note>\n        <p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>result</var> be <var>str</var>.</li><li>For each element <var>next</var> of <var>args</var>, do<ol><li>Let <var>nextString</var> be ? ToString(<var>next</var>).</li><li>Set <var>result</var> to the string-concatenation of <var>result</var> and <var>nextString</var>.</li></ol></li><li>Return <var>result</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this method is <b>1</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.6":"<p>The initial value of <code>String.prototype.constructor</code> is <emu-intrinsic>%String%</emu-intrinsic>.</p>\n      ","1.3.7":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>isRegExp</var> be ? IsRegExp(<var>searchString</var>).</li><li>If <var>isRegExp</var> is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>searchStr</var> be ? ToString(<var>searchString</var>).</li><li>Let <var>len</var> be the length of <var>str</var>.</li><li>If <var>endPosition</var> is <b>undefined</b>, let <var>pos</var> be <var>len</var>; else let <var>pos</var> be ? ToIntegerOrInfinity(<var>endPosition</var>).</li><li>Let <var>end</var> be the result of clamping <var>pos</var> between 0 and <var>len</var>.</li><li>Let <var>searchLength</var> be the length of <var>searchStr</var>.</li><li>If <var>searchLength</var> = 0, return <b>true</b>.</li><li>Let <var>start</var> be <var>end</var> - <var>searchLength</var>.</li><li>If <var>start</var> &lt; 0, return <b>false</b>.</li><li>Let <var>substring</var> be the substring of <var>str</var> from <var>start</var> to <var>end</var>.</li><li>If <var>substring</var> is <var>searchStr</var>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p>Throwing an exception if the first argument is a RegExp is specified in order to allow future editions to define extensions that allow such argument values.</p>\n        </emu-note>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.8":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>isRegExp</var> be ? IsRegExp(<var>searchString</var>).</li><li>If <var>isRegExp</var> is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>searchStr</var> be ? ToString(<var>searchString</var>).</li><li>Let <var>pos</var> be ? ToIntegerOrInfinity(<var>position</var>).</li><li>Assert: If <var>position</var> is <b>undefined</b>, then <var>pos</var> is 0.</li><li>Let <var>len</var> be the length of <var>str</var>.</li><li>Let <var>start</var> be the result of clamping <var>pos</var> between 0 and <var>len</var>.</li><li>Let <var>index</var> be StringIndexOf(<var>str</var>, <var>searchStr</var>, <var>start</var>).</li><li>If <var>index</var> is <emu-const>not-found</emu-const>, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p>If <var>searchString</var> appears as a <emu-not-ref>substring</emu-not-ref> of the result of converting this object to a String, at one or more indices that are greater than or equal to <var>position</var>, this function returns <b>true</b>; otherwise, it returns <b>false</b>. If <var>position</var> is <b>undefined</b>, 0 is assumed, so as to search all of the String.</p>\n        </emu-note>\n        <emu-note>\n          <p>Throwing an exception if the first argument is a RegExp is specified in order to allow future editions to define extensions that allow such argument values.</p>\n        </emu-note>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.9":"<emu-note>\n          <p>If <var>searchString</var> appears as a <emu-not-ref>substring</emu-not-ref> of the result of converting this object to a String, at one or more indices that are greater than or equal to <var>position</var>, then the smallest such index is returned; otherwise, <b>-1</b><sub>𝔽</sub> is returned. If <var>position</var> is <b>undefined</b>, <b>+0</b><sub>𝔽</sub> is assumed, so as to search all of the String.</p>\n        </emu-note>\n        <p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>searchStr</var> be ? ToString(<var>searchString</var>).</li><li>Let <var>pos</var> be ? ToIntegerOrInfinity(<var>position</var>).</li><li>Assert: If <var>position</var> is <b>undefined</b>, then <var>pos</var> is 0.</li><li>Let <var>len</var> be the length of <var>str</var>.</li><li>Let <var>start</var> be the result of clamping <var>pos</var> between 0 and <var>len</var>.</li><li>Let <var>result</var> be StringIndexOf(<var>str</var>, <var>searchStr</var>, <var>start</var>).</li><li>If <var>result</var> is <emu-const>not-found</emu-const>, return <b>-1</b><sub>𝔽</sub>.</li><li>Return 𝔽(<var>result</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.10":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Return IsStringWellFormedUnicode(<var>str</var>).</li></ol></emu-alg>\n      ","1.3.11":"<emu-note>\n          <p>If <var>searchString</var> appears as a <emu-not-ref>substring</emu-not-ref> of the result of converting this object to a String at one or more indices that are smaller than or equal to <var>position</var>, then the greatest such index is returned; otherwise, <b>-1</b><sub>𝔽</sub> is returned. If <var>position</var> is <b>undefined</b>, the length of the String value is assumed, so as to search all of the String.</p>\n        </emu-note>\n        <p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>searchStr</var> be ? ToString(<var>searchString</var>).</li><li>Let <var>numPos</var> be ? ToNumber(<var>position</var>).</li><li>Assert: If <var>position</var> is <b>undefined</b>, then <var>numPos</var> is <b>NaN</b>.</li><li>If <var>numPos</var> is <b>NaN</b>, let <var>pos</var> be +∞; else let <var>pos</var> be ! ToIntegerOrInfinity(<var>numPos</var>).</li><li>Let <var>len</var> be the length of <var>str</var>.</li><li>Let <var>searchLen</var> be the length of <var>searchStr</var>.</li><li>If <var>len</var> &lt; <var>searchLen</var>, return <b>-1</b><sub>𝔽</sub>.</li><li>Let <var>start</var> be the result of clamping <var>pos</var> between 0 and <var>len</var> - <var>searchLen</var>.</li><li>Let <var>result</var> be StringLastIndexOf(<var>str</var>, <var>searchStr</var>, <var>start</var>).</li><li>If <var>result</var> is <emu-const>not-found</emu-const>, return <b>-1</b><sub>𝔽</sub>.</li><li>Return 𝔽(<var>result</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.12":"<p>An ECMAScript implementation that includes the ECMA-402 Internationalization API must implement this method as specified in the ECMA-402 specification. If an ECMAScript implementation does not include the ECMA-402 API the following specification of this method is used:</p>\n        <p>This method returns a Number other than <b>NaN</b> representing the result of an implementation-defined locale-sensitive String comparison of the <b>this</b> value (converted to a String <var>str</var>) with <var>that</var> (converted to a String <var>thatValue</var>). The result is intended to correspond with a sort order of String values according to conventions of the host environment's current locale, and will be negative when <var>str</var> is ordered before <var>thatValue</var>, positive when <var>str</var> is ordered after <var>thatValue</var>, and zero in all other cases (representing no relative ordering between <var>str</var> and <var>thatValue</var>).</p>\n        <p>Before performing the comparisons, this method performs the following steps to prepare the Strings:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>thatValue</var> be ? ToString(<var>that</var>).</li></ol></emu-alg>\n        <p>The meaning of the optional second and third parameters to this method are defined in the ECMA-402 specification; implementations that do not include ECMA-402 support must not assign any other interpretation to those parameter positions.</p>\n        <p>The actual return values are implementation-defined to permit encoding additional information in them, but this method, when considered as a method of two arguments, is required to be a consistent comparator defining a total ordering on the set of all Strings. This method is also required to recognize and honour canonical equivalence according to the Unicode Standard, including returning <b>+0</b><sub>𝔽</sub> when comparing distinguishable Strings that are canonically equivalent.</p>\n        <emu-note>\n          <p>This method itself is not directly suitable as an argument to <code>Array.prototype.sort</code> because the latter requires a function of two arguments.</p>\n        </emu-note>\n        <emu-note>\n          <p>This method may rely on whatever language- and/or locale-sensitive comparison functionality is available to the ECMAScript environment from the host environment, and is intended to compare according to the conventions of the host environment's current locale. However, regardless of comparison capabilities, this method must recognize and honour canonical equivalence according to the Unicode Standard—for example, the following comparisons must all return <b>+0</b><sub>𝔽</sub>:</p>\n          <pre><code class=\"javascript\">\n            // &#x212B; ANGSTROM SIGN vs.\n            // A&#x030A; LATIN CAPITAL LETTER A + COMBINING RING ABOVE\n            \"\\u212B\".localeCompare(\"A\\u030A\")\n\n            // &#x2126; OHM SIGN vs.\n            // &#x03A9; GREEK CAPITAL LETTER OMEGA\n            \"\\u2126\".localeCompare(\"\\u03A9\")\n\n            // &#x1E69; LATIN SMALL LETTER S WITH DOT BELOW AND DOT ABOVE vs.\n            // s&#x0307;&#x0323; LATIN SMALL LETTER S + COMBINING DOT ABOVE + COMBINING DOT BELOW\n            \"\\u1E69\".localeCompare(\"s\\u0307\\u0323\")\n\n            // &#x1E0B;&#x0323; LATIN SMALL LETTER D WITH DOT ABOVE + COMBINING DOT BELOW vs.\n            // &#x1E0D;&#x0307; LATIN SMALL LETTER D WITH DOT BELOW + COMBINING DOT ABOVE\n            \"\\u1E0B\\u0323\".localeCompare(\"\\u1E0D\\u0307\")\n\n            // &#x1100;&#x1161; HANGUL CHOSEONG KIYEOK + HANGUL JUNGSEONG A vs.\n            // &#xAC00; HANGUL SYLLABLE GA\n            \"\\u1100\\u1161\".localeCompare(\"\\uAC00\")\n          </code></pre>\n          <p>For a definition and discussion of canonical equivalence see the Unicode Standard, chapters 2 and 3, as well as <a href=\"https://unicode.org/reports/tr15/\">Unicode Standard Annex #15, Unicode Normalization Forms</a> and <a href=\"https://unicode.org/notes/tn5/\">Unicode Technical Note #5, Canonical Equivalence in Applications</a>. Also see <a href=\"https://unicode.org/reports/tr10/\">Unicode Technical Standard #10, Unicode Collation Algorithm</a>.</p>\n          <p>It is recommended that this method should not honour Unicode compatibility equivalents or compatibility decompositions as defined in the Unicode Standard, chapter 3, section 3.7.</p>\n        </emu-note>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.13":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>If <var>regexpOrPattern</var> is an Object, then<ol><li>Let <var>matcher</var> be ? GetMethod(<var>regexpOrPattern</var>, <emu-intrinsic>%Symbol.match%</emu-intrinsic>).</li><li>If <var>matcher</var> is not <b>undefined</b>, then<ol><li>Return ? Call(<var>matcher</var>, <var>regexpOrPattern</var>, « <var>thisValue</var> »).</li></ol></li></ol></li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>regexp</var> be ? RegExpCreate(<var>regexpOrPattern</var>, <b>undefined</b>).</li><li>Return ? Invoke(<var>regexp</var>, <emu-intrinsic>%Symbol.match%</emu-intrinsic>, « <var>str</var> »).</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.14":"<p>This method performs a regular expression match of the String representing the <b>this</b> value against <var>regexpOrPattern</var> and returns an iterator that yields match results. Each match result is an Array containing the matched portion of the String as the first element, followed by the portions matched by any capturing groups. If the regular expression never matches, the returned iterator does not yield any match results.</p>\n        <p>It performs the following steps when called:</p>\n\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>If <var>regexpOrPattern</var> is an Object, then<ol><li>Let <var>isRegExp</var> be ? IsRegExp(<var>regexpOrPattern</var>).</li><li>If <var>isRegExp</var> is <b>true</b>, then<ol><li>Let <var>flags</var> be ? Get(<var>regexpOrPattern</var>, <b>\"flags\"</b>).</li><li>Perform ? RequireObjectCoercible(<var>flags</var>).</li><li>If ? ToString(<var>flags</var>) does not contain <b>\"g\"</b>, throw a <b>TypeError</b> exception.</li></ol></li><li>Let <var>matcher</var> be ? GetMethod(<var>regexpOrPattern</var>, <emu-intrinsic>%Symbol.matchAll%</emu-intrinsic>).</li><li>If <var>matcher</var> is not <b>undefined</b>, then<ol><li>Return ? Call(<var>matcher</var>, <var>regexpOrPattern</var>, « <var>thisValue</var> »).</li></ol></li></ol></li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>regexp</var> be ? RegExpCreate(<var>regexpOrPattern</var>, <b>\"g\"</b>).</li><li>Return ? Invoke(<var>regexp</var>, <emu-intrinsic>%Symbol.matchAll%</emu-intrinsic>, « <var>str</var> »).</li></ol></emu-alg>\n        <emu-note>This method is intentionally generic, it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</emu-note>\n        <emu-note>Similarly to <code>String.prototype.split</code>, <code>String.prototype.matchAll</code> is designed to typically act without mutating its inputs.</emu-note>\n      ","1.3.15":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>If <var>form</var> is <b>undefined</b>, let <var>f</var> be <b>\"NFC\"</b>.</li><li>Else, let <var>f</var> be ? ToString(<var>form</var>).</li><li>If <var>f</var> is not one of <b>\"NFC\"</b>, <b>\"NFD\"</b>, <b>\"NFKC\"</b>, or <b>\"NFKD\"</b>, throw a <b>RangeError</b> exception.</li><li>Let <var>ns</var> be the String value that is the result of normalizing <var>str</var> into the normalization form named by <var>f</var> as specified in <a href=\"https://www.unicode.org/versions/latest/ch03.pdf\">the latest Unicode Standard, Normalization Forms</a>.</li><li>Return <var>ns</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.16":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Return ? StringPaddingBuiltinsImpl(<var>thisValue</var>, <var>maxLength</var>, <var>fillString</var>, <emu-const>end</emu-const>).</li></ol></emu-alg>\n      ","1.3.17":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Return ? StringPaddingBuiltinsImpl(<var>thisValue</var>, <var>maxLength</var>, <var>fillString</var>, <emu-const>start</emu-const>).</li></ol></emu-alg>\n\n        ","1.3.17.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>intMaxLength</var> be ℝ(? ToLength(<var>maxLength</var>)).</li><li>Let <var>stringLength</var> be the length of <var>str</var>.</li><li>If <var>intMaxLength</var> ≤ <var>stringLength</var>, return <var>str</var>.</li><li>If <var>fillString</var> is <b>undefined</b>, set <var>fillString</var> to the String value consisting solely of the code unit 0x0020 (SPACE).</li><li>Else, set <var>fillString</var> to ? ToString(<var>fillString</var>).</li><li>Return StringPad(<var>str</var>, <var>intMaxLength</var>, <var>fillString</var>, <var>placement</var>).</li></ol></emu-alg>\n        ","1.3.17.2":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>stringLength</var> be the length of <var>str</var>.</li><li>If <var>maxLength</var> ≤ <var>stringLength</var>, return <var>str</var>.</li><li>If <var>fillString</var> is the empty String, return <var>str</var>.</li><li>Let <var>fillLen</var> be <var>maxLength</var> - <var>stringLength</var>.</li><li>Let <var>truncatedStringFiller</var> be the String value consisting of repeated concatenations of <var>fillString</var> truncated to length <var>fillLen</var>.</li><li>If <var>placement</var> is <emu-const>start</emu-const>, return the string-concatenation of <var>truncatedStringFiller</var> and <var>str</var>.</li><li>Return the string-concatenation of <var>str</var> and <var>truncatedStringFiller</var>.</li></ol></emu-alg>\n          <emu-note>\n            <p>The argument <var>maxLength</var> will be clamped such that it can be no smaller than the length of <var>str</var>.</p>\n          </emu-note>\n          <emu-note>\n            <p>The argument <var>fillString</var> defaults to <b>\" \"</b> (the String value consisting of the code unit 0x0020 SPACE).</p>\n          </emu-note>\n        ","1.3.17.3":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>str</var> be the String representation of <var>n</var>, formatted as a decimal number.</li><li>Return StringPad(<var>str</var>, <var>minLength</var>, <b>\"0\"</b>, <emu-const>start</emu-const>).</li></ol></emu-alg>\n        ","1.3.18":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>n</var> be ? ToIntegerOrInfinity(<var>count</var>).</li><li>If <var>n</var> &lt; 0 or <var>n</var> = +∞, throw a <b>RangeError</b> exception.</li><li>If <var>n</var> = 0, return the empty String.</li><li>Return the String value that is made from <var>n</var> copies of <var>str</var> appended together.</li></ol></emu-alg>\n        <emu-note>\n          <p>This method creates the String value consisting of the code units of the <b>this</b> value (converted to String) repeated <var>count</var> times.</p>\n        </emu-note>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.19":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>If <var>searchValue</var> is an Object, then<ol><li>Let <var>replacer</var> be ? GetMethod(<var>searchValue</var>, <emu-intrinsic>%Symbol.replace%</emu-intrinsic>).</li><li>If <var>replacer</var> is not <b>undefined</b>, then<ol><li>Return ? Call(<var>replacer</var>, <var>searchValue</var>, « <var>thisValue</var>, <var>replaceValue</var> »).</li></ol></li></ol></li><li>Let <var>string</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>searchString</var> be ? ToString(<var>searchValue</var>).</li><li>Let <var>functionalReplace</var> be IsCallable(<var>replaceValue</var>).</li><li>If <var>functionalReplace</var> is <b>false</b>, then<ol><li>Set <var>replaceValue</var> to ? ToString(<var>replaceValue</var>).</li></ol></li><li>Let <var>searchLength</var> be the length of <var>searchString</var>.</li><li>Let <var>position</var> be StringIndexOf(<var>string</var>, <var>searchString</var>, 0).</li><li>If <var>position</var> is <emu-const>not-found</emu-const>, return <var>string</var>.</li><li>Let <var>preceding</var> be the substring of <var>string</var> from 0 to <var>position</var>.</li><li>Let <var>following</var> be the substring of <var>string</var> from <var>position</var> + <var>searchLength</var>.</li><li>If <var>functionalReplace</var> is <b>true</b>, then<ol><li>Let <var>replacement</var> be ? ToString(? Call(<var>replaceValue</var>, <b>undefined</b>, « <var>searchString</var>, 𝔽(<var>position</var>), <var>string</var> »)).</li></ol></li><li>Else,<ol><li>Assert: <var>replaceValue</var> is a String.</li><li>Let <var>captures</var> be a new empty List.</li><li>Let <var>replacement</var> be ! GetSubstitution(<var>searchString</var>, <var>string</var>, <var>position</var>, <var>captures</var>, <b>undefined</b>, <var>replaceValue</var>).</li></ol></li><li>Return the string-concatenation of <var>preceding</var>, <var>replacement</var>, and <var>following</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n\n        ","1.3.19.1":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>For the purposes of this abstract operation, a <em>decimal digit</em> is a code unit in the inclusive interval from 0x0030 (DIGIT ZERO) to 0x0039 (DIGIT NINE).</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>stringLength</var> be the length of <var>str</var>.</li><li>Assert: <var>position</var> ≤ <var>stringLength</var>.</li><li>Let <var>result</var> be the empty String.</li><li>Let <var>templateRemainder</var> be <var>replacementTemplate</var>.</li><li>Repeat, while <var>templateRemainder</var> is not the empty String,<ol><li>NOTE: The following steps isolate <var>ref</var> (a prefix of <var>templateRemainder</var>), determine <var>refReplacement</var> (its replacement), and then append that replacement to <var>result</var>.</li><li>If <var>templateRemainder</var> starts with <b>\"$$\"</b>, then<ol><li>Let <var>ref</var> be <b>\"$$\"</b>.</li><li>Let <var>refReplacement</var> be <b>\"$\"</b>.</li></ol></li><li>Else if <var>templateRemainder</var> starts with <b>\"$`\"</b>, then<ol><li>Let <var>ref</var> be <b>\"$`\"</b>.</li><li>Let <var>refReplacement</var> be the substring of <var>str</var> from 0 to <var>position</var>.</li></ol></li><li>Else if <var>templateRemainder</var> starts with <b>\"$&amp;\"</b>, then<ol><li>Let <var>ref</var> be <b>\"$&amp;\"</b>.</li><li>Let <var>refReplacement</var> be <var>matched</var>.</li></ol></li><li>Else if <var>templateRemainder</var> starts with <b>\"$'\"</b> (0x0024 (DOLLAR SIGN) followed by 0x0027 (APOSTROPHE)), then<ol><li>Let <var>ref</var> be <b>\"$'\"</b>.</li><li>Let <var>matchLength</var> be the length of <var>matched</var>.</li><li>Let <var>tailPos</var> be <var>position</var> + <var>matchLength</var>.</li><li>Let <var>refReplacement</var> be the substring of <var>str</var> from min(<var>tailPos</var>, <var>stringLength</var>).</li><li>NOTE: <var>tailPos</var> can exceed <var>stringLength</var> only if this abstract operation was invoked by a call to the intrinsic <emu-intrinsic>%Symbol.replace%</emu-intrinsic> method of <emu-intrinsic>%RegExp.prototype%</emu-intrinsic> on an object whose <b>\"exec\"</b> property is not the intrinsic <emu-intrinsic>%RegExp.prototype.exec%</emu-intrinsic>.</li></ol></li><li>Else if <var>templateRemainder</var> starts with <b>\"$\"</b> followed by 1 or more decimal digits, then<ol><li>If <var>templateRemainder</var> starts with <b>\"$\"</b> followed by 2 or more decimal digits, let <var>digitCount</var> be 2; else let <var>digitCount</var> be 1.</li><li>Let <var>digits</var> be the substring of <var>templateRemainder</var> from 1 to 1 + <var>digitCount</var>.</li><li>Let <var>index</var> be ℝ(StringToNumber(<var>digits</var>)).</li><li>Assert: 0 ≤ <var>index</var> ≤ 99.</li><li>Let <var>captureLen</var> be the number of elements in <var>captures</var>.</li><li>If <var>index</var> > <var>captureLen</var> and <var>digitCount</var> = 2, then<ol><li>NOTE: When a two-digit replacement pattern specifies an index exceeding the count of capturing groups, it is treated as a one-digit replacement pattern followed by a literal digit.</li><li>Set <var>digitCount</var> to 1.</li><li>Set <var>digits</var> to the substring of <var>digits</var> from 0 to 1.</li><li>Set <var>index</var> to ℝ(StringToNumber(<var>digits</var>)).</li></ol></li><li>Let <var>ref</var> be the substring of <var>templateRemainder</var> from 0 to 1 + <var>digitCount</var>.</li><li>If 1 ≤ <var>index</var> ≤ <var>captureLen</var>, then<ol><li>Let <var>capture</var> be <var>captures</var>[<var>index</var> - 1].</li><li>If <var>capture</var> is <b>undefined</b>, then<ol><li>Let <var>refReplacement</var> be the empty String.</li></ol></li><li>Else,<ol><li>Let <var>refReplacement</var> be <var>capture</var>.</li></ol></li></ol></li><li>Else,<ol><li>Let <var>refReplacement</var> be <var>ref</var>.</li></ol></li></ol></li><li>Else if <var>templateRemainder</var> starts with <b>\"$&lt;\"</b>, then<ol><li>Let <var>gtPos</var> be StringIndexOf(<var>templateRemainder</var>, <b>\">\"</b>, 0).</li><li>If <var>gtPos</var> is <emu-const>not-found</emu-const> or <var>namedCaptures</var> is <b>undefined</b>, then<ol><li>Let <var>ref</var> be <b>\"$&lt;\"</b>.</li><li>Let <var>refReplacement</var> be <var>ref</var>.</li></ol></li><li>Else,<ol><li>Let <var>ref</var> be the substring of <var>templateRemainder</var> from 0 to <var>gtPos</var> + 1.</li><li>Let <var>groupName</var> be the substring of <var>templateRemainder</var> from 2 to <var>gtPos</var>.</li><li>Assert: <var>namedCaptures</var> is an Object.</li><li>Let <var>capture</var> be ? Get(<var>namedCaptures</var>, <var>groupName</var>).</li><li>If <var>capture</var> is <b>undefined</b>, then<ol><li>Let <var>refReplacement</var> be the empty String.</li></ol></li><li>Else,<ol><li>Let <var>refReplacement</var> be ? ToString(<var>capture</var>).</li></ol></li></ol></li></ol></li><li>Else,<ol><li>Let <var>ref</var> be the substring of <var>templateRemainder</var> from 0 to 1.</li><li>Let <var>refReplacement</var> be <var>ref</var>.</li></ol></li><li>Let <var>refLength</var> be the length of <var>ref</var>.</li><li>Set <var>templateRemainder</var> to the substring of <var>templateRemainder</var> from <var>refLength</var>.</li><li>Set <var>result</var> to the string-concatenation of <var>result</var> and <var>refReplacement</var>.</li></ol></li><li>Return <var>result</var>.</li></ol></emu-alg>\n        ","1.3.20":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>If <var>searchValue</var> is an Object, then<ol><li>Let <var>isRegExp</var> be ? IsRegExp(<var>searchValue</var>).</li><li>If <var>isRegExp</var> is <b>true</b>, then<ol><li>Let <var>flags</var> be ? Get(<var>searchValue</var>, <b>\"flags\"</b>).</li><li>Perform ? RequireObjectCoercible(<var>flags</var>).</li><li>If ? ToString(<var>flags</var>) does not contain <b>\"g\"</b>, throw a <b>TypeError</b> exception.</li></ol></li><li>Let <var>replacer</var> be ? GetMethod(<var>searchValue</var>, <emu-intrinsic>%Symbol.replace%</emu-intrinsic>).</li><li>If <var>replacer</var> is not <b>undefined</b>, then<ol><li>Return ? Call(<var>replacer</var>, <var>searchValue</var>, « <var>thisValue</var>, <var>replaceValue</var> »).</li></ol></li></ol></li><li>Let <var>string</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>searchString</var> be ? ToString(<var>searchValue</var>).</li><li>Let <var>functionalReplace</var> be IsCallable(<var>replaceValue</var>).</li><li>If <var>functionalReplace</var> is <b>false</b>, then<ol><li>Set <var>replaceValue</var> to ? ToString(<var>replaceValue</var>).</li></ol></li><li>Let <var>searchLength</var> be the length of <var>searchString</var>.</li><li>Let <var>advanceBy</var> be max(1, <var>searchLength</var>).</li><li>Let <var>matchPositions</var> be a new empty List.</li><li>Let <var>position</var> be StringIndexOf(<var>string</var>, <var>searchString</var>, 0).</li><li>Repeat, while <var>position</var> is not <emu-const>not-found</emu-const>,<ol><li>Append <var>position</var> to <var>matchPositions</var>.</li><li>Set <var>position</var> to StringIndexOf(<var>string</var>, <var>searchString</var>, <var>position</var> + <var>advanceBy</var>).</li></ol></li><li>Let <var>endOfLastMatch</var> be 0.</li><li>Let <var>result</var> be the empty String.</li><li>For each element <var>matchPosition</var> of <var>matchPositions</var>, do<ol><li>Let <var>preserved</var> be the substring of <var>string</var> from <var>endOfLastMatch</var> to <var>matchPosition</var>.</li><li>If <var>functionalReplace</var> is <b>true</b>, then<ol><li>Let <var>replacement</var> be ? ToString(? Call(<var>replaceValue</var>, <b>undefined</b>, « <var>searchString</var>, 𝔽(<var>matchPosition</var>), <var>string</var> »)).</li></ol></li><li>Else,<ol><li>Assert: <var>replaceValue</var> is a String.</li><li>Let <var>captures</var> be a new empty List.</li><li>Let <var>replacement</var> be ! GetSubstitution(<var>searchString</var>, <var>string</var>, <var>matchPosition</var>, <var>captures</var>, <b>undefined</b>, <var>replaceValue</var>).</li></ol></li><li>Set <var>result</var> to the string-concatenation of <var>result</var>, <var>preserved</var>, and <var>replacement</var>.</li><li>Set <var>endOfLastMatch</var> to <var>matchPosition</var> + <var>searchLength</var>.</li></ol></li><li>If <var>endOfLastMatch</var> &lt; the length of <var>string</var>, then<ol><li>Set <var>result</var> to the string-concatenation of <var>result</var> and the substring of <var>string</var> from <var>endOfLastMatch</var>.</li></ol></li><li>Return <var>result</var>.</li></ol></emu-alg>\n      ","1.3.21":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>If <var>regexpOrPattern</var> is an Object, then<ol><li>Let <var>searcher</var> be ? GetMethod(<var>regexpOrPattern</var>, <emu-intrinsic>%Symbol.search%</emu-intrinsic>).</li><li>If <var>searcher</var> is not <b>undefined</b>, then<ol><li>Return ? Call(<var>searcher</var>, <var>regexpOrPattern</var>, « <var>thisValue</var> »).</li></ol></li></ol></li><li>Let <var>string</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>regexp</var> be ? RegExpCreate(<var>regexpOrPattern</var>, <b>undefined</b>).</li><li>Return ? Invoke(<var>regexp</var>, <emu-intrinsic>%Symbol.search%</emu-intrinsic>, « <var>string</var> »).</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.22":"<p>This method returns a <emu-not-ref>substring</emu-not-ref> of the result of converting this object to a String, starting from index <var>start</var> and running to, but not including, index <var>end</var> (or through the end of the String if <var>end</var> is <b>undefined</b>). If <var>start</var> is negative, it is treated as <emu-eqn><var>sourceLength</var> + <var>start</var></emu-eqn> where <var>sourceLength</var> is the length of the String. If <var>end</var> is negative, it is treated as <emu-eqn><var>sourceLength</var> + <var>end</var></emu-eqn> where <var>sourceLength</var> is the length of the String. The result is a String value, not a String object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>len</var> be the length of <var>str</var>.</li><li>Let <var>intStart</var> be ? ToIntegerOrInfinity(<var>start</var>).</li><li>If <var>intStart</var> = -∞, let <var>from</var> be 0.</li><li>Else if <var>intStart</var> &lt; 0, let <var>from</var> be max(<var>len</var> + <var>intStart</var>, 0).</li><li>Else, let <var>from</var> be min(<var>intStart</var>, <var>len</var>).</li><li>If <var>end</var> is <b>undefined</b>, let <var>intEnd</var> be <var>len</var>; else let <var>intEnd</var> be ? ToIntegerOrInfinity(<var>end</var>).</li><li>If <var>intEnd</var> = -∞, let <var>to</var> be 0.</li><li>Else if <var>intEnd</var> &lt; 0, let <var>to</var> be max(<var>len</var> + <var>intEnd</var>, 0).</li><li>Else, let <var>to</var> be min(<var>intEnd</var>, <var>len</var>).</li><li>If <var>from</var> ≥ <var>to</var>, return the empty String.</li><li>Return the substring of <var>str</var> from <var>from</var> to <var>to</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.23":"<p>This method returns an Array into which substrings of the result of converting this object to a String have been stored. The substrings are determined by searching from left to right for occurrences of <var>separator</var>; these occurrences are not part of any String in the returned array, but serve to divide up the String value. The value of <var>separator</var> may be a String of any length or it may be an object, such as a RegExp, that has a <emu-intrinsic>%Symbol.split%</emu-intrinsic> method.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>If <var>separator</var> is an Object, then<ol><li>Let <var>splitter</var> be ? GetMethod(<var>separator</var>, <emu-intrinsic>%Symbol.split%</emu-intrinsic>).</li><li>If <var>splitter</var> is not <b>undefined</b>, then<ol><li>Return ? Call(<var>splitter</var>, <var>separator</var>, « <var>thisValue</var>, <var>limit</var> »).</li></ol></li></ol></li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>If <var>limit</var> is <b>undefined</b>, let <var>lim</var> be 2<sup>32</sup> - 1; else let <var>lim</var> be ℝ(? ToUint32(<var>limit</var>)).</li><li>Let <var>separatorStr</var> be ? ToString(<var>separator</var>).</li><li>If <var>lim</var> = 0, then<ol><li>Return CreateArrayFromList(« »).</li></ol></li><li>If <var>separator</var> is <b>undefined</b>, then<ol><li>Return CreateArrayFromList(« <var>str</var> »).</li></ol></li><li>Let <var>separatorLength</var> be the length of <var>separatorStr</var>.</li><li>If <var>separatorLength</var> = 0, then<ol><li>Let <var>strLen</var> be the length of <var>str</var>.</li><li>Let <var>outLen</var> be the result of clamping <var>lim</var> between 0 and <var>strLen</var>.</li><li>Let <var>head</var> be the substring of <var>str</var> from 0 to <var>outLen</var>.</li><li>Let <var>codeUnits</var> be a List consisting of the sequence of code units that are the elements of <var>head</var>.</li><li>Return CreateArrayFromList(<var>codeUnits</var>).</li></ol></li><li>If <var>str</var> is the empty String, return CreateArrayFromList(« <var>str</var> »).</li><li>Let <var>substrings</var> be a new empty List.</li><li>Let <var>searchStart</var> be 0.</li><li>Let <var>matchIndex</var> be StringIndexOf(<var>str</var>, <var>separatorStr</var>, 0).</li><li>Repeat, while <var>matchIndex</var> is not <emu-const>not-found</emu-const>,<ol><li>Let <var>substring</var> be the substring of <var>str</var> from <var>searchStart</var> to <var>matchIndex</var>.</li><li>Append <var>substring</var> to <var>substrings</var>.</li><li>If the number of elements in <var>substrings</var> is <var>lim</var>, return CreateArrayFromList(<var>substrings</var>).</li><li>Set <var>searchStart</var> to <var>matchIndex</var> + <var>separatorLength</var>.</li><li>Set <var>matchIndex</var> to StringIndexOf(<var>str</var>, <var>separatorStr</var>, <var>searchStart</var>).</li></ol></li><li>Let <var>substring</var> be the substring of <var>str</var> from <var>searchStart</var>.</li><li>Append <var>substring</var> to <var>substrings</var>.</li><li>Return CreateArrayFromList(<var>substrings</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>The value of <var>separator</var> may be an empty String. In this case, <var>separator</var> does not match the empty <emu-not-ref>substring</emu-not-ref> at the beginning or end of the input String, nor does it match the empty <emu-not-ref>substring</emu-not-ref> at the end of the previous separator match. If <var>separator</var> is the empty String, the String is split up into individual code unit elements; the length of the result array equals the length of the String, and each <emu-not-ref>substring</emu-not-ref> contains one code unit.</p>\n          <p>If the <b>this</b> value is (or converts to) the empty String, the result depends on whether <var>separator</var> can match the empty String. If it can, the result array contains no elements. Otherwise, the result array contains one element, which is the empty String.</p>\n          <p>If <var>separator</var> is <b>undefined</b>, then the result array contains just one String, which is the <b>this</b> value (converted to a String). If <var>limit</var> is not <b>undefined</b>, then the output array is truncated so that it contains no more than <var>limit</var> elements.</p>\n        </emu-note>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.24":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>isRegExp</var> be ? IsRegExp(<var>searchString</var>).</li><li>If <var>isRegExp</var> is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>searchStr</var> be ? ToString(<var>searchString</var>).</li><li>Let <var>len</var> be the length of <var>str</var>.</li><li>If <var>position</var> is <b>undefined</b>, let <var>pos</var> be 0; else let <var>pos</var> be ? ToIntegerOrInfinity(<var>position</var>).</li><li>Let <var>start</var> be the result of clamping <var>pos</var> between 0 and <var>len</var>.</li><li>Let <var>searchLength</var> be the length of <var>searchStr</var>.</li><li>If <var>searchLength</var> = 0, return <b>true</b>.</li><li>Let <var>end</var> be <var>start</var> + <var>searchLength</var>.</li><li>If <var>end</var> > <var>len</var>, return <b>false</b>.</li><li>Let <var>substring</var> be the substring of <var>str</var> from <var>start</var> to <var>end</var>.</li><li>If <var>substring</var> is <var>searchStr</var>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p>Throwing an exception if the first argument is a RegExp is specified in order to allow future editions to define extensions that allow such argument values.</p>\n        </emu-note>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.25":"<p>This method returns a <emu-not-ref>substring</emu-not-ref> of the result of converting this object to a String, starting from index <var>start</var> and running to, but not including, index <var>end</var> of the String (or through the end of the String if <var>end</var> is <b>undefined</b>). The result is a String value, not a String object.</p>\n        <p>If either argument is <b>NaN</b> or negative, it is replaced with zero; if either argument is strictly greater than the length of the String, it is replaced with the length of the String.</p>\n        <p>If <var>start</var> is strictly greater than <var>end</var>, they are swapped.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>len</var> be the length of <var>str</var>.</li><li>Let <var>intStart</var> be ? ToIntegerOrInfinity(<var>start</var>).</li><li>If <var>end</var> is <b>undefined</b>, let <var>intEnd</var> be <var>len</var>; else let <var>intEnd</var> be ? ToIntegerOrInfinity(<var>end</var>).</li><li>Let <var>finalStart</var> be the result of clamping <var>intStart</var> between 0 and <var>len</var>.</li><li>Let <var>finalEnd</var> be the result of clamping <var>intEnd</var> between 0 and <var>len</var>.</li><li>Let <var>from</var> be min(<var>finalStart</var>, <var>finalEnd</var>).</li><li>Let <var>to</var> be max(<var>finalStart</var>, <var>finalEnd</var>).</li><li>Return the substring of <var>str</var> from <var>from</var> to <var>to</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.26":"<p>An ECMAScript implementation that includes the ECMA-402 Internationalization API must implement this method as specified in the ECMA-402 specification. If an ECMAScript implementation does not include the ECMA-402 API the following specification of this method is used:</p>\n        <p>This method interprets a String value as a sequence of UTF-16 encoded code points, as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>.</p>\n        <p>It works exactly the same as <code>toLowerCase</code> except that it is intended to yield a locale-sensitive result corresponding with conventions of the host environment's current locale. There will only be a difference in the few cases (such as Turkish) where the rules for that language conflict with the regular Unicode case mappings.</p>\n        <p>The meaning of the optional parameters to this method are defined in the ECMA-402 specification; implementations that do not include ECMA-402 support must not use those parameter positions for anything else.</p>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.27":"<p>An ECMAScript implementation that includes the ECMA-402 Internationalization API must implement this method as specified in the ECMA-402 specification. If an ECMAScript implementation does not include the ECMA-402 API the following specification of this method is used:</p>\n        <p>This method interprets a String value as a sequence of UTF-16 encoded code points, as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>.</p>\n        <p>It works exactly the same as <code>toUpperCase</code> except that it is intended to yield a locale-sensitive result corresponding with conventions of the host environment's current locale. There will only be a difference in the few cases (such as Turkish) where the rules for that language conflict with the regular Unicode case mappings.</p>\n        <p>The meaning of the optional parameters to this method are defined in the ECMA-402 specification; implementations that do not include ECMA-402 support must not use those parameter positions for anything else.</p>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.28":"<p>This method interprets a String value as a sequence of UTF-16 encoded code points, as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>sText</var> be StringToCodePoints(<var>str</var>).</li><li>Let <var>lowerText</var> be toLowercase(<var>sText</var>), according to the Unicode Default Case Conversion algorithm.</li><li>Let <var>lowercaseString</var> be CodePointsToString(<var>lowerText</var>).</li><li>Return <var>lowercaseString</var>.</li></ol></emu-alg>\n        <p>The result must be derived according to the locale-insensitive case mappings in the Unicode Character Database (this explicitly includes not only the file <a href=\"https://unicode.org/Public/UCD/latest/ucd/UnicodeData.txt\"><code>UnicodeData.txt</code></a>, but also all locale-insensitive mappings in the file <a href=\"https://unicode.org/Public/UCD/latest/ucd/SpecialCasing.txt\"><code>SpecialCasing.txt</code></a> that accompanies it).</p>\n        <emu-note>\n          <p>The case mapping of some code points may produce multiple code points. In this case the result String may not be the same length as the source String. Because both <code>toUpperCase</code> and <code>toLowerCase</code> have context-sensitive behaviour, the methods are not symmetrical. In other words, <code>s.toUpperCase().toLowerCase()</code> is not necessarily equal to <code>s.toLowerCase()</code>.</p>\n        </emu-note>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.29":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Return ? ThisStringValue(<b>this</b> value).</li></ol></emu-alg>\n        <emu-note>\n          <p>For a String object, this method happens to return the same thing as the <code>valueOf</code> method.</p>\n        </emu-note>\n      ","1.3.30":"<p>This method interprets a String value as a sequence of UTF-16 encoded code points, as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>.</p>\n        <p>It behaves in exactly the same way as <code>String.prototype.toLowerCase</code>, except that the String is mapped using the toUppercase algorithm of the Unicode Default Case Conversion.</p>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.31":"<p>This method returns a String representation of this object with all leading surrogates and trailing surrogates that are not part of a surrogate pair replaced with U+FFFD (REPLACEMENT CHARACTER).</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>Let <var>str</var> be ? ToString(<var>thisValue</var>).</li><li>Let <var>strLen</var> be the length of <var>str</var>.</li><li>Let <var>k</var> be 0.</li><li>Let <var>result</var> be the empty String.</li><li>Repeat, while <var>k</var> &lt; <var>strLen</var>,<ol><li>Let <var>cp</var> be CodePointAt(<var>str</var>, <var>k</var>).</li><li>If <var>cp</var>.[[IsUnpairedSurrogate]] is <b>true</b>, then<ol><li>Set <var>result</var> to the string-concatenation of <var>result</var> and 0xFFFD (REPLACEMENT CHARACTER).</li></ol></li><li>Else,<ol><li>Set <var>result</var> to the string-concatenation of <var>result</var> and UTF16EncodeCodePoint(<var>cp</var>.[[CodePoint]]).</li></ol></li><li>Set <var>k</var> to <var>k</var> + <var>cp</var>.[[CodeUnitCount]].</li></ol></li><li>Return <var>result</var>.</li></ol></emu-alg>\n      ","1.3.32":"<p>This method interprets a String value as a sequence of UTF-16 encoded code points, as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Return ? TrimString(<var>thisValue</var>, <emu-const>start+end</emu-const>).</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n\n        ","1.3.32.1":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It interprets <var>string</var> as a sequence of UTF-16 encoded code points, as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>.</dd>\n          </dl>\n          <emu-alg><ol><li>Perform ? RequireObjectCoercible(<var>string</var>).</li><li>Let <var>str</var> be ? ToString(<var>string</var>).</li><li>If <var>where</var> is <emu-const>start</emu-const>, then<ol><li>Let <var>trimmedString</var> be the String value that is a copy of <var>str</var> with leading white space removed.</li></ol></li><li>Else if <var>where</var> is <emu-const>end</emu-const>, then<ol><li>Let <var>trimmedString</var> be the String value that is a copy of <var>str</var> with trailing white space removed.</li></ol></li><li>Else,<ol><li>Assert: <var>where</var> is <emu-const>start+end</emu-const>.</li><li>Let <var>trimmedString</var> be the String value that is a copy of <var>str</var> with both leading and trailing white space removed.</li></ol></li><li>Return <var>trimmedString</var>.</li></ol></emu-alg>\n          <p>The definition of white space is the union of <emu-nt>WhiteSpace</emu-nt> and <emu-nt>LineTerminator</emu-nt>. When determining whether a Unicode code point is in Unicode general category “Space_Separator” (“Zs”), code unit sequences are interpreted as UTF-16 encoded code point sequences as specified in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>.</p>\n        ","1.3.33":"<p>This method interprets a String value as a sequence of UTF-16 encoded code points, as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>str</var> be the <b>this</b> value.</li><li>Return ? TrimString(<var>str</var>, <emu-const>end</emu-const>).</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.34":"<p>This method interprets a String value as a sequence of UTF-16 encoded code points, as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>str</var> be the <b>this</b> value.</li><li>Return ? TrimString(<var>str</var>, <emu-const>start</emu-const>).</li></ol></emu-alg>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a String object. Therefore, it can be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n      ","1.3.35":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Return ? ThisStringValue(<b>this</b> value).</li></ol></emu-alg>\n\n        ","1.3.35.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>value</var> is a String, return <var>value</var>.</li><li>If <var>value</var> is an Object and <var>value</var> has a [[StringData]] internal slot, then<ol><li>Let <var>str</var> be <var>value</var>.[[StringData]].</li><li>Assert: <var>str</var> is a String.</li><li>Return <var>str</var>.</li></ol></li><li>Throw a <b>TypeError</b> exception.</li></ol></emu-alg>\n        ","1.3.36":"<p>This method returns an iterator object that iterates over the code points of a String value, returning each code point as a String value.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>str</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>str</var>).</li><li>Set <var>str</var> to ? ToString(<var>str</var>).</li><li>Let <var>closure</var> be a new Abstract Closure with no parameters that captures <var>str</var> and performs the following steps when called:<ol><li>Let <var>len</var> be the length of <var>str</var>.</li><li>Let <var>position</var> be 0.</li><li>Repeat, while <var>position</var> &lt; <var>len</var>,<ol><li>Let <var>cp</var> be CodePointAt(<var>str</var>, <var>position</var>).</li><li>Let <var>nextIndex</var> be <var>position</var> + <var>cp</var>.[[CodeUnitCount]].</li><li>Let <var>resultString</var> be the substring of <var>str</var> from <var>position</var> to <var>nextIndex</var>.</li><li>Set <var>position</var> to <var>nextIndex</var>.</li><li>Perform ? GeneratorYield(CreateIteratorResultObject(<var>resultString</var>, <b>false</b>)).</li></ol></li><li>Return NormalCompletion(<emu-const>unused</emu-const>).</li></ol></li><li>Return CreateIteratorFromClosure(<var>closure</var>, <b>\"<emu-intrinsic>%StringIteratorPrototype%</emu-intrinsic>\"</b>, <emu-intrinsic>%StringIteratorPrototype%</emu-intrinsic>).</li></ol></emu-alg>\n        <p>The value of the <b>\"name\"</b> property of this method is <b>\"[Symbol.iterator]\"</b>.</p>\n      ","1.4":"<p>String instances are String exotic objects and have the internal methods specified for such objects. String instances inherit properties from the String prototype object. String instances also have a [[StringData]] internal slot. The [[StringData]] internal slot is the String value represented by this String object.</p>\n      <p>String instances have a <b>\"length\"</b> property, and a set of enumerable properties with integer-indexed names.</p>\n\n      ","1.4.1":"<p>The number of elements in the String value represented by this String object.</p>\n        <p>Once a String object is initialized, this property is unchanging. It has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.5":"<p>A <dfn variants=\"String Iterators,String Iterator object,String Iterator objects\">String Iterator</dfn> is an object that represents a specific iteration over some specific String instance object. There is not a named constructor for String Iterator objects. Instead, String Iterator objects are created by calling certain methods of String instance objects.</p>\n\n      ","1.5.1":"<p>The <dfn><emu-intrinsic>%StringIteratorPrototype%</emu-intrinsic></dfn> object:</p>\n        <ul>\n          <li>has properties that are inherited by all String Iterator objects.</li>\n          <li>is an ordinary object.</li>\n          <li>has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Iterator.prototype%</emu-intrinsic>.</li>\n          <li>has the following properties:</li>\n        </ul>\n\n        ","1.5.1.1":"<emu-alg><ol><li>Return ? <emu-meta suppress-effects=\"user-code\">GeneratorResume(<b>this</b> value, <emu-const>empty</emu-const>, <b>\"<emu-intrinsic>%StringIteratorPrototype%</emu-intrinsic>\"</b>)</emu-meta>.</li></ol></emu-alg>\n        ","1.5.1.2":"<p>The initial value of the <emu-intrinsic>%Symbol.toStringTag%</emu-intrinsic> property is the String value <b>\"String Iterator\"</b>.</p>\n          <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n        ","2.1":"<p>The RegExp constructor applies the following grammar to the input pattern String. An error occurs if the grammar cannot interpret the String as an expansion of <emu-nt>Pattern</emu-nt>.</p>\n      <h2>Syntax</h2>\n      <pre class=\"emu-grammar\"><span class=\"nt lhs\">Pattern</span><span class=\"p\">[UnicodeMode, UnicodeSetsMode, NamedCaptureGroups]</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">Disjunction</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span>\n\n<span class=\"nt lhs\">Disjunction</span><span class=\"p\">[UnicodeMode, UnicodeSetsMode, NamedCaptureGroups]</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">Alternative</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span>\n  <span class=\"nt\">Alternative</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span> <span class=\"t\">|</span> <span class=\"nt\">Disjunction</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span>\n\n<span class=\"nt lhs\">Alternative</span><span class=\"p\">[UnicodeMode, UnicodeSetsMode, NamedCaptureGroups]</span> <span class=\"geq\">::</span>\n  <span class=\"p\">[empty]</span>\n  <span class=\"nt\">Alternative</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span> <span class=\"nt\">Term</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span>\n\n<span class=\"nt lhs\">Term</span><span class=\"p\">[UnicodeMode, UnicodeSetsMode, NamedCaptureGroups]</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">Assertion</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span>\n  <span class=\"nt\">Atom</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span>\n  <span class=\"nt\">Atom</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span> <span class=\"nt\">Quantifier</span>\n\n<span class=\"nt lhs\">Assertion</span><span class=\"p\">[UnicodeMode, UnicodeSetsMode, NamedCaptureGroups]</span> <span class=\"geq\">::</span>\n  <span class=\"t\">^</span>\n  <span class=\"t\">$</span>\n  <span class=\"t\">\\b</span>\n  <span class=\"t\">\\B</span>\n  <span class=\"t\">(?=</span> <span class=\"nt\">Disjunction</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span> <span class=\"t\">)</span>\n  <span class=\"t\">(?!</span> <span class=\"nt\">Disjunction</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span> <span class=\"t\">)</span>\n  <span class=\"t\">(?&lt;=</span> <span class=\"nt\">Disjunction</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span> <span class=\"t\">)</span>\n  <span class=\"t\">(?&lt;!</span> <span class=\"nt\">Disjunction</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span> <span class=\"t\">)</span>\n\n<span class=\"nt lhs\">Quantifier</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">QuantifierPrefix</span>\n  <span class=\"nt\">QuantifierPrefix</span> <span class=\"t\">?</span>\n\n<span class=\"nt lhs\">QuantifierPrefix</span> <span class=\"geq\">::</span>\n  <span class=\"t\">*</span>\n  <span class=\"t\">+</span>\n  <span class=\"t\">?</span>\n  <span class=\"t\">{</span> <span class=\"nt\">DecimalDigits</span><span class=\"p\">[~Sep]</span> <span class=\"t\">}</span>\n  <span class=\"t\">{</span> <span class=\"nt\">DecimalDigits</span><span class=\"p\">[~Sep]</span> <span class=\"t\">,}</span>\n  <span class=\"t\">{</span> <span class=\"nt\">DecimalDigits</span><span class=\"p\">[~Sep]</span> <span class=\"t\">,</span> <span class=\"nt\">DecimalDigits</span><span class=\"p\">[~Sep]</span> <span class=\"t\">}</span>\n\n<span class=\"nt lhs\">Atom</span><span class=\"p\">[UnicodeMode, UnicodeSetsMode, NamedCaptureGroups]</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">PatternCharacter</span>\n  <span class=\"t\">.</span>\n  <span class=\"t\">\\</span> <span class=\"nt\">AtomEscape</span><span class=\"p\">[?UnicodeMode, ?NamedCaptureGroups]</span>\n  <span class=\"nt\">CharacterClass</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode]</span>\n  <span class=\"t\">(</span> <span class=\"nt\">GroupSpecifier</span><span class=\"p\">[?UnicodeMode]</span><span class=\"mod\">?</span> <span class=\"nt\">Disjunction</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span> <span class=\"t\">)</span>\n  <span class=\"t\">(?</span> <span class=\"nt\">RegularExpressionModifiers</span> <span class=\"t\">:</span> <span class=\"nt\">Disjunction</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span> <span class=\"t\">)</span>\n  <span class=\"t\">(?</span> <span class=\"nt\">RegularExpressionModifiers</span> <span class=\"t\">-</span> <span class=\"nt\">RegularExpressionModifiers</span> <span class=\"t\">:</span> <span class=\"nt\">Disjunction</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode, ?NamedCaptureGroups]</span> <span class=\"t\">)</span>\n\n<span class=\"nt lhs\">RegularExpressionModifiers</span> <span class=\"geq\">::</span>\n  <span class=\"p\">[empty]</span>\n  <span class=\"nt\">RegularExpressionModifiers</span> <span class=\"nt\">RegularExpressionModifier</span>\n\n<span class=\"nt lhs\">RegularExpressionModifier</span> <span class=\"geq\">::</span> <span class=\"oneof\">one of</span>\n  <span class=\"t\">i</span> <span class=\"t\">m</span> <span class=\"t\">s</span>\n\n<span class=\"nt lhs\">SyntaxCharacter</span> <span class=\"geq\">::</span> <span class=\"oneof\">one of</span>\n  <span class=\"t\">^</span> <span class=\"t\">$</span> <span class=\"t\">\\</span> <span class=\"t\">.</span> <span class=\"t\">*</span> <span class=\"t\">+</span> <span class=\"t\">?</span> <span class=\"t\">(</span> <span class=\"t\">)</span> <span class=\"t\">[</span> <span class=\"t\">]</span> <span class=\"t\">{</span> <span class=\"t\">}</span> <span class=\"t\">|</span>\n\n<span class=\"nt lhs\">PatternCharacter</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">SourceCharacter</span> but not <span class=\"nt\">SyntaxCharacter</span>\n\n<span class=\"nt lhs\">AtomEscape</span><span class=\"p\">[UnicodeMode, NamedCaptureGroups]</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">DecimalEscape</span>\n  <span class=\"nt\">CharacterClassEscape</span><span class=\"p\">[?UnicodeMode]</span>\n  <span class=\"nt\">CharacterEscape</span><span class=\"p\">[?UnicodeMode]</span>\n  <span class=\"p\">[+NamedCaptureGroups]</span> <span class=\"t\">k</span> <span class=\"nt\">GroupName</span><span class=\"p\">[?UnicodeMode]</span>\n\n<span class=\"nt lhs\">CharacterEscape</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">ControlEscape</span>\n  <span class=\"t\">c</span> <span class=\"nt\">AsciiLetter</span>\n  <span class=\"t\">0</span> <span class=\"p\">[lookahead &notin; DecimalDigit]</span>\n  <span class=\"nt\">HexEscapeSequence</span>\n  <span class=\"nt\">RegExpUnicodeEscapeSequence</span><span class=\"p\">[?UnicodeMode]</span>\n  <span class=\"nt\">IdentityEscape</span><span class=\"p\">[?UnicodeMode]</span>\n\n<span class=\"nt lhs\">ControlEscape</span> <span class=\"geq\">::</span> <span class=\"oneof\">one of</span>\n  <span class=\"t\">f</span> <span class=\"t\">n</span> <span class=\"t\">r</span> <span class=\"t\">t</span> <span class=\"t\">v</span>\n\n<span class=\"nt lhs\">GroupSpecifier</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"t\">?</span> <span class=\"nt\">GroupName</span><span class=\"p\">[?UnicodeMode]</span>\n\n<span class=\"nt lhs\">GroupName</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"t\">&lt;</span> <span class=\"nt\">RegExpIdentifierName</span><span class=\"p\">[?UnicodeMode]</span> <span class=\"t\">&gt;</span>\n\n<span class=\"nt lhs\">RegExpIdentifierName</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">RegExpIdentifierStart</span><span class=\"p\">[?UnicodeMode]</span>\n  <span class=\"nt\">RegExpIdentifierName</span><span class=\"p\">[?UnicodeMode]</span> <span class=\"nt\">RegExpIdentifierPart</span><span class=\"p\">[?UnicodeMode]</span>\n\n<span class=\"nt lhs\">RegExpIdentifierStart</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">IdentifierStartChar</span>\n  <span class=\"t\">\\</span> <span class=\"nt\">RegExpUnicodeEscapeSequence</span><span class=\"p\">[+UnicodeMode]</span>\n  <span class=\"p\">[~UnicodeMode]</span> <span class=\"nt\">UnicodeLeadSurrogate</span> <span class=\"nt\">UnicodeTrailSurrogate</span>\n\n<span class=\"nt lhs\">RegExpIdentifierPart</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">IdentifierPartChar</span>\n  <span class=\"t\">\\</span> <span class=\"nt\">RegExpUnicodeEscapeSequence</span><span class=\"p\">[+UnicodeMode]</span>\n  <span class=\"p\">[~UnicodeMode]</span> <span class=\"nt\">UnicodeLeadSurrogate</span> <span class=\"nt\">UnicodeTrailSurrogate</span>\n\n<span class=\"nt lhs\">RegExpUnicodeEscapeSequence</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"p\">[+UnicodeMode]</span> <span class=\"t\">u</span> <span class=\"nt\">HexLeadSurrogate</span> <span class=\"t\">\\u</span> <span class=\"nt\">HexTrailSurrogate</span>\n  <span class=\"p\">[+UnicodeMode]</span> <span class=\"t\">u</span> <span class=\"nt\">HexLeadSurrogate</span>\n  <span class=\"p\">[+UnicodeMode]</span> <span class=\"t\">u</span> <span class=\"nt\">HexTrailSurrogate</span>\n  <span class=\"p\">[+UnicodeMode]</span> <span class=\"t\">u</span> <span class=\"nt\">HexNonSurrogate</span>\n  <span class=\"p\">[~UnicodeMode]</span> <span class=\"t\">u</span> <span class=\"nt\">Hex4Digits</span>\n  <span class=\"p\">[+UnicodeMode]</span> <span class=\"t\">u{</span> <span class=\"nt\">CodePoint</span> <span class=\"t\">}</span>\n\n<span class=\"nt lhs\">UnicodeLeadSurrogate</span> <span class=\"geq\">::</span>\n  <span class=\"desc\">&gt; any Unicode code point in the inclusive interval from U+D800 to U+DBFF</span>\n\n<span class=\"nt lhs\">UnicodeTrailSurrogate</span> <span class=\"geq\">::</span>\n  <span class=\"desc\">&gt; any Unicode code point in the inclusive interval from U+DC00 to U+DFFF</span></pre>\n      <p>Each <code>\\\\u</code> <emu-nt>HexTrailSurrogate</emu-nt> for which the choice of associated <code>u</code> <emu-nt>HexLeadSurrogate</emu-nt> is ambiguous shall be associated with the nearest possible <code>u</code> <emu-nt>HexLeadSurrogate</emu-nt> that would otherwise have no corresponding <code>\\\\u</code> <emu-nt>HexTrailSurrogate</emu-nt>.</p>\n      <pre class=\"emu-grammar\"><span class=\"nt lhs\">HexLeadSurrogate</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">Hex4Digits</span> <span class=\"p\">[> but only if the MV of |Hex4Digits| is in the inclusive interval from 0xD800 to 0xDBFF]</span>\n\n<span class=\"nt lhs\">HexTrailSurrogate</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">Hex4Digits</span> <span class=\"p\">[> but only if the MV of |Hex4Digits| is in the inclusive interval from 0xDC00 to 0xDFFF]</span>\n\n<span class=\"nt lhs\">HexNonSurrogate</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">Hex4Digits</span> <span class=\"p\">[> but only if the MV of |Hex4Digits| is not in the inclusive interval from 0xD800 to 0xDFFF]</span>\n\n<span class=\"nt lhs\">IdentityEscape</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"p\">[+UnicodeMode]</span> <span class=\"nt\">SyntaxCharacter</span>\n  <span class=\"p\">[+UnicodeMode]</span> <span class=\"t\">/</span>\n  <span class=\"p\">[~UnicodeMode]</span> <span class=\"nt\">SourceCharacter</span> but not <span class=\"nt\">UnicodeIDContinue</span>\n\n<span class=\"nt lhs\">DecimalEscape</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">NonZeroDigit</span> <span class=\"nt\">DecimalDigits</span><span class=\"p\">[~Sep]</span><span class=\"mod\">?</span> <span class=\"p\">[lookahead &notin; DecimalDigit]</span>\n\n<span class=\"nt lhs\">CharacterClassEscape</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"t\">d</span>\n  <span class=\"t\">D</span>\n  <span class=\"t\">s</span>\n  <span class=\"t\">S</span>\n  <span class=\"t\">w</span>\n  <span class=\"t\">W</span>\n  <span class=\"p\">[+UnicodeMode]</span> <span class=\"t\">p{</span> <span class=\"nt\">UnicodePropertyValueExpression</span> <span class=\"t\">}</span>\n  <span class=\"p\">[+UnicodeMode]</span> <span class=\"t\">P{</span> <span class=\"nt\">UnicodePropertyValueExpression</span> <span class=\"t\">}</span>\n\n<span class=\"nt lhs\">UnicodePropertyValueExpression</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">UnicodePropertyName</span> <span class=\"t\">=</span> <span class=\"nt\">UnicodePropertyValue</span>\n  <span class=\"nt\">LoneUnicodePropertyNameOrValue</span>\n\n<span class=\"nt lhs\">UnicodePropertyName</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">UnicodePropertyNameCharacters</span>\n\n<span class=\"nt lhs\">UnicodePropertyNameCharacters</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">UnicodePropertyNameCharacter</span> <span class=\"nt\">UnicodePropertyNameCharacters</span><span class=\"mod\">?</span>\n\n<span class=\"nt lhs\">UnicodePropertyValue</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">UnicodePropertyValueCharacters</span>\n\n<span class=\"nt lhs\">LoneUnicodePropertyNameOrValue</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">UnicodePropertyValueCharacters</span>\n\n<span class=\"nt lhs\">UnicodePropertyValueCharacters</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">UnicodePropertyValueCharacter</span> <span class=\"nt\">UnicodePropertyValueCharacters</span><span class=\"mod\">?</span>\n\n<span class=\"nt lhs\">UnicodePropertyValueCharacter</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">UnicodePropertyNameCharacter</span>\n  <span class=\"nt\">DecimalDigit</span>\n\n<span class=\"nt lhs\">UnicodePropertyNameCharacter</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">AsciiLetter</span>\n  <span class=\"t\">_</span>\n\n<span class=\"nt lhs\">CharacterClass</span><span class=\"p\">[UnicodeMode, UnicodeSetsMode]</span> <span class=\"geq\">::</span>\n  <span class=\"t\">[</span> <span class=\"p\">[lookahead != `^`]</span> <span class=\"nt\">ClassContents</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode]</span> <span class=\"t\">]</span>\n  <span class=\"t\">[^</span> <span class=\"nt\">ClassContents</span><span class=\"p\">[?UnicodeMode, ?UnicodeSetsMode]</span> <span class=\"t\">]</span>\n\n<span class=\"nt lhs\">ClassContents</span><span class=\"p\">[UnicodeMode, UnicodeSetsMode]</span> <span class=\"geq\">::</span>\n  <span class=\"p\">[empty]</span>\n  <span class=\"p\">[~UnicodeSetsMode]</span> <span class=\"nt\">NonemptyClassRanges</span><span class=\"p\">[?UnicodeMode]</span>\n  <span class=\"p\">[+UnicodeSetsMode]</span> <span class=\"nt\">ClassSetExpression</span>\n\n<span class=\"nt lhs\">NonemptyClassRanges</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">ClassAtom</span><span class=\"p\">[?UnicodeMode]</span>\n  <span class=\"nt\">ClassAtom</span><span class=\"p\">[?UnicodeMode]</span> <span class=\"nt\">NonemptyClassRangesNoDash</span><span class=\"p\">[?UnicodeMode]</span>\n  <span class=\"nt\">ClassAtom</span><span class=\"p\">[?UnicodeMode]</span> <span class=\"t\">-</span> <span class=\"nt\">ClassAtom</span><span class=\"p\">[?UnicodeMode]</span> <span class=\"nt\">ClassContents</span><span class=\"p\">[?UnicodeMode, ~UnicodeSetsMode]</span>\n\n<span class=\"nt lhs\">NonemptyClassRangesNoDash</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">ClassAtom</span><span class=\"p\">[?UnicodeMode]</span>\n  <span class=\"nt\">ClassAtomNoDash</span><span class=\"p\">[?UnicodeMode]</span> <span class=\"nt\">NonemptyClassRangesNoDash</span><span class=\"p\">[?UnicodeMode]</span>\n  <span class=\"nt\">ClassAtomNoDash</span><span class=\"p\">[?UnicodeMode]</span> <span class=\"t\">-</span> <span class=\"nt\">ClassAtom</span><span class=\"p\">[?UnicodeMode]</span> <span class=\"nt\">ClassContents</span><span class=\"p\">[?UnicodeMode, ~UnicodeSetsMode]</span>\n\n<span class=\"nt lhs\">ClassAtom</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"t\">-</span>\n  <span class=\"nt\">ClassAtomNoDash</span><span class=\"p\">[?UnicodeMode]</span>\n\n<span class=\"nt lhs\">ClassAtomNoDash</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">SourceCharacter</span> but not <span class=\"oneof\">one of</span> <span class=\"t\">\\</span> or <span class=\"t\">]</span> or <span class=\"t\">-</span>\n  <span class=\"t\">\\</span> <span class=\"nt\">ClassEscape</span><span class=\"p\">[?UnicodeMode]</span>\n\n<span class=\"nt lhs\">ClassEscape</span><span class=\"p\">[UnicodeMode]</span> <span class=\"geq\">::</span>\n  <span class=\"t\">b</span>\n  <span class=\"p\">[+UnicodeMode]</span> <span class=\"t\">-</span>\n  <span class=\"nt\">CharacterClassEscape</span><span class=\"p\">[?UnicodeMode]</span>\n  <span class=\"nt\">CharacterEscape</span><span class=\"p\">[?UnicodeMode]</span>\n\n<span class=\"nt lhs\">ClassSetExpression</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">ClassUnion</span>\n  <span class=\"nt\">ClassIntersection</span>\n  <span class=\"nt\">ClassSubtraction</span>\n\n<span class=\"nt lhs\">ClassUnion</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">ClassSetRange</span> <span class=\"nt\">ClassUnion</span><span class=\"mod\">?</span>\n  <span class=\"nt\">ClassSetOperand</span> <span class=\"nt\">ClassUnion</span><span class=\"mod\">?</span>\n\n<span class=\"nt lhs\">ClassIntersection</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">ClassSetOperand</span> <span class=\"t\">&amp;&amp;</span> <span class=\"p\">[lookahead != `&amp;`]</span> <span class=\"nt\">ClassSetOperand</span>\n  <span class=\"nt\">ClassIntersection</span> <span class=\"t\">&amp;&amp;</span> <span class=\"p\">[lookahead != `&amp;`]</span> <span class=\"nt\">ClassSetOperand</span>\n\n<span class=\"nt lhs\">ClassSubtraction</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">ClassSetOperand</span> <span class=\"t\">--</span> <span class=\"nt\">ClassSetOperand</span>\n  <span class=\"nt\">ClassSubtraction</span> <span class=\"t\">--</span> <span class=\"nt\">ClassSetOperand</span>\n\n<span class=\"nt lhs\">ClassSetRange</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">ClassSetCharacter</span> <span class=\"t\">-</span> <span class=\"nt\">ClassSetCharacter</span>\n\n<span class=\"nt lhs\">ClassSetOperand</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">NestedClass</span>\n  <span class=\"nt\">ClassStringDisjunction</span>\n  <span class=\"nt\">ClassSetCharacter</span>\n\n<span class=\"nt lhs\">NestedClass</span> <span class=\"geq\">::</span>\n  <span class=\"t\">[</span> <span class=\"p\">[lookahead != `^`]</span> <span class=\"nt\">ClassContents</span><span class=\"p\">[+UnicodeMode, +UnicodeSetsMode]</span> <span class=\"t\">]</span>\n  <span class=\"t\">[^</span> <span class=\"nt\">ClassContents</span><span class=\"p\">[+UnicodeMode, +UnicodeSetsMode]</span> <span class=\"t\">]</span>\n  <span class=\"t\">\\</span> <span class=\"nt\">CharacterClassEscape</span><span class=\"p\">[+UnicodeMode]</span></pre>\n      <emu-note>\n        <p>The first two lines here are equivalent to CharacterClass.</p>\n      </emu-note>\n      <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassStringDisjunction</span> <span class=\"geq\">::</span>\n  <span class=\"t\">\\q{</span> <span class=\"nt\">ClassStringDisjunctionContents</span> <span class=\"t\">}</span>\n\n<span class=\"nt lhs\">ClassStringDisjunctionContents</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">ClassString</span>\n  <span class=\"nt\">ClassString</span> <span class=\"t\">|</span> <span class=\"nt\">ClassStringDisjunctionContents</span>\n\n<span class=\"nt lhs\">ClassString</span> <span class=\"geq\">::</span>\n  <span class=\"p\">[empty]</span>\n  <span class=\"nt\">NonEmptyClassString</span>\n\n<span class=\"nt lhs\">NonEmptyClassString</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">ClassSetCharacter</span> <span class=\"nt\">NonEmptyClassString</span><span class=\"mod\">?</span>\n\n<span class=\"nt lhs\">ClassSetCharacter</span> <span class=\"geq\">::</span>\n  <span class=\"p\">[lookahead &notin; ClassSetReservedDoublePunctuator]</span> <span class=\"nt\">SourceCharacter</span> but not <span class=\"nt\">ClassSetSyntaxCharacter</span>\n  <span class=\"t\">\\</span> <span class=\"nt\">CharacterEscape</span><span class=\"p\">[+UnicodeMode]</span>\n  <span class=\"t\">\\</span> <span class=\"nt\">ClassSetReservedPunctuator</span>\n  <span class=\"t\">\\b</span>\n\n<span class=\"nt lhs\">ClassSetReservedDoublePunctuator</span> <span class=\"geq\">::</span> <span class=\"oneof\">one of</span>\n  <span class=\"t\">&amp;&amp;</span> <span class=\"t\">!!</span> <span class=\"t\">##</span>\n  <span class=\"t\">$$</span> <span class=\"t\">%%</span> <span class=\"t\">**</span>\n  <span class=\"t\">++</span> <span class=\"t\">,,</span> <span class=\"t\">..</span>\n  <span class=\"t\">::</span> <span class=\"t\">;;</span> <span class=\"t\">&lt;&lt;</span>\n  <span class=\"t\">==</span> <span class=\"t\">&gt;&gt;</span> <span class=\"t\">??</span>\n  <span class=\"t\">@@</span> <span class=\"t\">^^</span> <span class=\"t\">&grave;&grave;</span>\n  <span class=\"t\">~~</span></pre>\n      <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSetSyntaxCharacter</span> <span class=\"geq\">::</span> <span class=\"oneof\">one of</span>\n  <span class=\"t\">(</span> <span class=\"t\">)</span> <span class=\"t\">[</span> <span class=\"t\">]</span> <span class=\"t\">{</span> <span class=\"t\">}</span> <span class=\"t\">/</span> <span class=\"t\">-</span> <span class=\"t\">\\</span> <span class=\"t\">|</span></pre>\n      <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSetReservedPunctuator</span> <span class=\"geq\">::</span> <span class=\"oneof\">one of</span>\n  <span class=\"t\">&amp;</span> <span class=\"t\">-</span> <span class=\"t\">!</span> <span class=\"t\">#</span> <span class=\"t\">%</span> <span class=\"t\">,</span>\n  <span class=\"t\">:</span> <span class=\"t\">;</span> <span class=\"t\">&lt;</span> <span class=\"t\">=</span> <span class=\"t\">&gt;</span> <span class=\"t\">@</span>\n  <span class=\"t\">&grave;</span> <span class=\"t\">~</span></pre>\n      <emu-note>\n        <p>A number of productions in this section are given alternative definitions in section <a href=\"/additional-ecmascript-features-for-web-browsers#sec-regular-expressions-patterns\">B.1.2</a>.</p>\n      </emu-note>\n\n      ","2.1.1":"<emu-note>\n          <p>This section is amended in <a href=\"/additional-ecmascript-features-for-web-browsers#sec-patterns-static-semantics-early-errors-annexb\">B.1.2.1</a>.</p>\n        </emu-note>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Pattern</span> <span class=\"geq\">::</span> <span class=\"nt\">Disjunction</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if CountLeftCapturingParensWithin(<emu-nt>Pattern</emu-nt>) ≥ 2<sup>32</sup> - 1.\n          </li>\n          <li>\n            It is a Syntax Error if <emu-nt>Pattern</emu-nt> contains two distinct <emu-nt>GroupSpecifier</emu-nt>s <var>x</var> and <var>y</var> such that the CapturingGroupName of <var>x</var> is the CapturingGroupName of <var>y</var> and such that MightBothParticipate(<var>x</var>, <var>y</var>) is <b>true</b>.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">QuantifierPrefix</span> <span class=\"geq\">::</span> <span class=\"t\">{</span> <span class=\"nt\">DecimalDigits</span> <span class=\"t\">,</span> <span class=\"nt\">DecimalDigits</span> <span class=\"t\">}</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if the MV of the first <emu-nt>DecimalDigits</emu-nt> is strictly greater than the MV of the second <emu-nt>DecimalDigits</emu-nt>.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Atom</span> <span class=\"geq\">::</span> <span class=\"t\">(?</span> <span class=\"nt\">RegularExpressionModifiers</span> <span class=\"t\">:</span> <span class=\"nt\">Disjunction</span> <span class=\"t\">)</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if the source text matched by <emu-nt>RegularExpressionModifiers</emu-nt> contains the same code point more than once.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Atom</span> <span class=\"geq\">::</span> <span class=\"t\">(?</span> <span class=\"nt\">RegularExpressionModifiers</span> <span class=\"t\">-</span> <span class=\"nt\">RegularExpressionModifiers</span> <span class=\"t\">:</span> <span class=\"nt\">Disjunction</span> <span class=\"t\">)</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if the source text matched by the first <emu-nt>RegularExpressionModifiers</emu-nt> and the source text matched by the second <emu-nt>RegularExpressionModifiers</emu-nt> are both empty.\n          </li>\n          <li>\n            It is a Syntax Error if the source text matched by the first <emu-nt>RegularExpressionModifiers</emu-nt> contains the same code point more than once.\n          </li>\n          <li>\n            It is a Syntax Error if the source text matched by the second <emu-nt>RegularExpressionModifiers</emu-nt> contains the same code point more than once.\n          </li>\n          <li>\n            It is a Syntax Error if any code point in the source text matched by the first <emu-nt>RegularExpressionModifiers</emu-nt> is also contained in the source text matched by the second <emu-nt>RegularExpressionModifiers</emu-nt>.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">AtomEscape</span> <span class=\"geq\">::</span> <span class=\"t\">k</span> <span class=\"nt\">GroupName</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if GroupSpecifiersThatMatch(<emu-nt>GroupName</emu-nt>) is empty.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">AtomEscape</span> <span class=\"geq\">::</span> <span class=\"nt\">DecimalEscape</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if the CapturingGroupNumber of <emu-nt>DecimalEscape</emu-nt> is strictly greater than CountLeftCapturingParensWithin(the <emu-nt>Pattern</emu-nt> containing <emu-nt>AtomEscape</emu-nt>).\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">NonemptyClassRanges</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassAtom</span> <span class=\"t\">-</span> <span class=\"nt\">ClassAtom</span> <span class=\"nt\">ClassContents</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if IsCharacterClass of the first <emu-nt>ClassAtom</emu-nt> is <b>true</b> or IsCharacterClass of the second <emu-nt>ClassAtom</emu-nt> is <b>true</b>.\n          </li>\n          <li>\n            It is a Syntax Error if IsCharacterClass of the first <emu-nt>ClassAtom</emu-nt> is <b>false</b>, IsCharacterClass of the second <emu-nt>ClassAtom</emu-nt> is <b>false</b>, and the CharacterValue of the first <emu-nt>ClassAtom</emu-nt> is strictly greater than the CharacterValue of the second <emu-nt>ClassAtom</emu-nt>.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">NonemptyClassRangesNoDash</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassAtomNoDash</span> <span class=\"t\">-</span> <span class=\"nt\">ClassAtom</span> <span class=\"nt\">ClassContents</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if IsCharacterClass of <emu-nt>ClassAtomNoDash</emu-nt> is <b>true</b> or IsCharacterClass of <emu-nt>ClassAtom</emu-nt> is <b>true</b>.\n          </li>\n          <li>\n            It is a Syntax Error if IsCharacterClass of <emu-nt>ClassAtomNoDash</emu-nt> is <b>false</b>, IsCharacterClass of <emu-nt>ClassAtom</emu-nt> is <b>false</b>, and the CharacterValue of <emu-nt>ClassAtomNoDash</emu-nt> is strictly greater than the CharacterValue of <emu-nt>ClassAtom</emu-nt>.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">RegExpIdentifierStart</span> <span class=\"geq\">::</span> <span class=\"t\">\\</span> <span class=\"nt\">RegExpUnicodeEscapeSequence</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if the CharacterValue of <emu-nt>RegExpUnicodeEscapeSequence</emu-nt> is not the numeric value of some code point matched by the <emu-nt>IdentifierStartChar</emu-nt> lexical grammar production.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">RegExpIdentifierStart</span> <span class=\"geq\">::</span> <span class=\"nt\">UnicodeLeadSurrogate</span> <span class=\"nt\">UnicodeTrailSurrogate</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if the RegExpIdentifierCodePoint of <emu-nt>RegExpIdentifierStart</emu-nt> is not matched by the <emu-nt>UnicodeIDStart</emu-nt> lexical grammar production.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">RegExpIdentifierPart</span> <span class=\"geq\">::</span> <span class=\"t\">\\</span> <span class=\"nt\">RegExpUnicodeEscapeSequence</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if the CharacterValue of <emu-nt>RegExpUnicodeEscapeSequence</emu-nt> is not the numeric value of some code point matched by the <emu-nt>IdentifierPartChar</emu-nt> lexical grammar production.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">RegExpIdentifierPart</span> <span class=\"geq\">::</span> <span class=\"nt\">UnicodeLeadSurrogate</span> <span class=\"nt\">UnicodeTrailSurrogate</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if the RegExpIdentifierCodePoint of <emu-nt>RegExpIdentifierPart</emu-nt> is not matched by the <emu-nt>UnicodeIDContinue</emu-nt> lexical grammar production.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">UnicodePropertyValueExpression</span> <span class=\"geq\">::</span> <span class=\"nt\">UnicodePropertyName</span> <span class=\"t\">=</span> <span class=\"nt\">UnicodePropertyValue</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if the source text matched by <emu-nt>UnicodePropertyName</emu-nt> is not a <emu-not-ref>Unicode property name</emu-not-ref> or property alias listed in the “<emu-not-ref>Property name and aliases</emu-not-ref>” column of table-nonbinary-unicode-properties.\n          </li>\n          <li>\n            It is a Syntax Error if the source text matched by <emu-nt>UnicodePropertyValue</emu-nt> is not a property value or property value alias for the Unicode property or property alias given by the source text matched by <emu-nt>UnicodePropertyName</emu-nt> listed in <a href=\"https://unicode.org/Public/UCD/latest/ucd/PropertyValueAliases.txt\"><code>PropertyValueAliases.txt</code></a>.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">UnicodePropertyValueExpression</span> <span class=\"geq\">::</span> <span class=\"nt\">LoneUnicodePropertyNameOrValue</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if the source text matched by <emu-nt>LoneUnicodePropertyNameOrValue</emu-nt> is not a Unicode property value or property value alias for the General_Category (gc) property listed in <a href=\"https://unicode.org/Public/UCD/latest/ucd/PropertyValueAliases.txt\"><code>PropertyValueAliases.txt</code></a>, nor a binary property or binary property alias listed in the “<emu-not-ref>Property name and aliases</emu-not-ref>” column of table-binary-unicode-properties, nor a binary property of strings listed in the “<emu-not-ref>Property name</emu-not-ref>” column of table-binary-unicode-properties-of-strings.\n          </li>\n          <li>\n            It is a Syntax Error if the enclosing <emu-nt>Pattern</emu-nt> does not have a <sub>[UnicodeSetsMode]</sub> parameter and the source text matched by <emu-nt>LoneUnicodePropertyNameOrValue</emu-nt> is a binary property of strings listed in the “<emu-not-ref>Property name</emu-not-ref>” column of table-binary-unicode-properties-of-strings.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">P{</span> <span class=\"nt\">UnicodePropertyValueExpression</span> <span class=\"t\">}</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if MayContainStrings of the <emu-nt>UnicodePropertyValueExpression</emu-nt> is <b>true</b>.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClass</span> <span class=\"geq\">::</span> <span class=\"t\">[^</span> <span class=\"nt\">ClassContents</span> <span class=\"t\">]</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if MayContainStrings of the <emu-nt>ClassContents</emu-nt> is <b>true</b>.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">NestedClass</span> <span class=\"geq\">::</span> <span class=\"t\">[^</span> <span class=\"nt\">ClassContents</span> <span class=\"t\">]</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if MayContainStrings of the <emu-nt>ClassContents</emu-nt> is <b>true</b>.\n          </li>\n        </ul>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSetRange</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSetCharacter</span> <span class=\"t\">-</span> <span class=\"nt\">ClassSetCharacter</span></pre>\n        <ul>\n          <li>\n            It is a Syntax Error if the CharacterValue of the first <emu-nt>ClassSetCharacter</emu-nt> is strictly greater than the CharacterValue of the second <emu-nt>ClassSetCharacter</emu-nt>.\n          </li>\n        </ul>\n      ","2.1.2":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the number of left-capturing parentheses in <var>node</var>. A <dfn variants=\"left-capturing parentheses\">left-capturing parenthesis</dfn> is any <code>(</code> pattern character that is matched by the <code>(</code> terminal of the <code class=\"emu-grammar\"><span class=\"nt lhs\">Atom</span> <span class=\"geq\">::</span> <span class=\"t\">(</span> <span class=\"nt\">GroupSpecifier</span><span class=\"mod\">?</span> <span class=\"nt\">Disjunction</span> <span class=\"t\">)</span></code> production.</dd>\n        </dl>\n        <emu-note>\n          <p>This section is amended in <a href=\"/additional-ecmascript-features-for-web-browsers#sec-countleftcapturingparens-annexb\">B.1.2.2</a>.</p>\n        </emu-note>\n        <emu-alg><ol><li>Assert: <var>node</var> is an instance of a production in <a href=\"/text-processing#sec-patterns\">the RegExp Pattern grammar</a>.</li><li>Return the number of <code class=\"emu-grammar\"><span class=\"nt lhs\">Atom</span> <span class=\"geq\">::</span> <span class=\"t\">(</span> <span class=\"nt\">GroupSpecifier</span><span class=\"mod\">?</span> <span class=\"nt\">Disjunction</span> <span class=\"t\">)</span></code> Parse Nodes contained within <var>node</var>.</li></ol></emu-alg>\n      ","2.1.3":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the number of left-capturing parentheses within the enclosing pattern that occur to the left of <var>node</var>.</dd>\n        </dl>\n        <emu-note>\n          <p>This section is amended in <a href=\"/additional-ecmascript-features-for-web-browsers#sec-countleftcapturingparens-annexb\">B.1.2.2</a>.</p>\n        </emu-note>\n        <emu-alg><ol><li>Assert: <var>node</var> is an instance of a production in <a href=\"/text-processing#sec-patterns\">the RegExp Pattern grammar</a>.</li><li>Let <var>pattern</var> be the <emu-nt>Pattern</emu-nt> containing <var>node</var>.</li><li>Return the number of <code class=\"emu-grammar\"><span class=\"nt lhs\">Atom</span> <span class=\"geq\">::</span> <span class=\"t\">(</span> <span class=\"nt\">GroupSpecifier</span><span class=\"mod\">?</span> <span class=\"nt\">Disjunction</span> <span class=\"t\">)</span></code> Parse Nodes contained within <var>pattern</var> that either occur before <var>node</var> or contain <var>node</var>.</li></ol></emu-alg>\n      ","2.1.4":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: <var>x</var> and <var>y</var> have the same enclosing <emu-nt>Pattern</emu-nt>.</li><li>If the enclosing <emu-nt>Pattern</emu-nt> contains a <code class=\"emu-grammar\"><span class=\"nt lhs\">Disjunction</span> <span class=\"geq\">::</span> <span class=\"nt\">Alternative</span> <span class=\"t\">|</span> <span class=\"nt\">Disjunction</span></code> Parse Node such that either <var>x</var> is contained within the <emu-nt>Alternative</emu-nt> and <var>y</var> is contained within the derived <emu-nt>Disjunction</emu-nt>, or <var>x</var> is contained within the derived <emu-nt>Disjunction</emu-nt> and <var>y</var> is contained within the <emu-nt>Alternative</emu-nt>, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n      ","2.1.5":"<dl class=\"header\">\n        </dl>\n        <emu-note>\n          <p>This section is amended in <a href=\"/additional-ecmascript-features-for-web-browsers#sec-patterns-static-semantics-early-errors-annexb\">B.1.2.1</a>.</p>\n        </emu-note>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">DecimalEscape</span> <span class=\"geq\">::</span> <span class=\"nt\">NonZeroDigit</span></pre>\n        <emu-alg><ol><li>Return the MV of <emu-nt>NonZeroDigit</emu-nt>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">DecimalEscape</span> <span class=\"geq\">::</span> <span class=\"nt\">NonZeroDigit</span> <span class=\"nt\">DecimalDigits</span></pre>\n        <emu-alg><ol><li>Let <var>n</var> be the number of code points in <emu-nt>DecimalDigits</emu-nt>.</li><li>Return (the MV of <emu-nt>NonZeroDigit</emu-nt> × 10<sup><var>n</var></sup> plus the MV of <emu-nt>DecimalDigits</emu-nt>).</li></ol></emu-alg>\n        <p>The definitions of “the MV of <emu-nt>NonZeroDigit</emu-nt>” and “the MV of <emu-nt>DecimalDigits</emu-nt>” are in <a href=\"/ecmascript-language-lexical-grammar#sec-literals-numeric-literals\">12.9.3</a>.</p>\n      ","2.1.6":"<dl class=\"header\">\n        </dl>\n        <emu-note>\n          <p>This section is amended in <a href=\"/additional-ecmascript-features-for-web-browsers#sec-patterns-static-semantics-is-character-class-annexb\">B.1.2.3</a>.</p>\n        </emu-note>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassAtom</span> <span class=\"geq\">::</span>\n  <span class=\"t\">-</span>\n\n<span class=\"nt lhs\">ClassAtomNoDash</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">SourceCharacter</span> but not <span class=\"oneof\">one of</span> <span class=\"t\">\\</span> or <span class=\"t\">]</span> or <span class=\"t\">-</span>\n\n<span class=\"nt lhs\">ClassEscape</span> <span class=\"geq\">::</span>\n  <span class=\"t\">b</span>\n  <span class=\"t\">-</span>\n  <span class=\"nt\">CharacterEscape</span></pre>\n        <emu-alg><ol><li>Return <b>false</b>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassEscape</span> <span class=\"geq\">::</span> <span class=\"nt\">CharacterClassEscape</span></pre>\n        <emu-alg><ol><li>Return <b>true</b>.</li></ol></emu-alg>\n      ","2.1.7":"<dl class=\"header\">\n        </dl>\n        <emu-note>\n          <p>This section is amended in <a href=\"/additional-ecmascript-features-for-web-browsers#sec-patterns-static-semantics-character-value-annexb\">B.1.2.4</a>.</p>\n        </emu-note>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassAtom</span> <span class=\"geq\">::</span> <span class=\"t\">-</span></pre>\n        <emu-alg><ol><li>Return the numeric value of U+002D (HYPHEN-MINUS).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassAtomNoDash</span> <span class=\"geq\">::</span> <span class=\"nt\">SourceCharacter</span> but not <span class=\"oneof\">one of</span> <span class=\"t\">\\</span> or <span class=\"t\">]</span> or <span class=\"t\">-</span></pre>\n        <emu-alg><ol><li>Let <var>ch</var> be the code point matched by <emu-nt>SourceCharacter</emu-nt>.</li><li>Return the numeric value of <var>ch</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">b</span></pre>\n        <emu-alg><ol><li>Return the numeric value of U+0008 (BACKSPACE).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">-</span></pre>\n        <emu-alg><ol><li>Return the numeric value of U+002D (HYPHEN-MINUS).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterEscape</span> <span class=\"geq\">::</span> <span class=\"nt\">ControlEscape</span></pre>\n        <emu-alg><ol><li>Return the numeric value according to table-controlescape-code-point-values.</li></ol></emu-alg>\n        <emu-table id=\"table-controlescape-code-point-values\" caption=\"ControlEscape Code Point Values\" oldids=\"table-47\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  ControlEscape\n                </th>\n                <th>\n                  Numeric Value\n                </th>\n                <th>\n                  Code Point\n                </th>\n                <th>\n                  Unicode Name\n                </th>\n                <th>\n                  Symbol\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                <code>t</code>\n              </td>\n              <td>\n                9\n              </td>\n              <td>\n                <code>U+0009</code>\n              </td>\n              <td>\n                CHARACTER TABULATION\n              </td>\n              <td>\n                &lt;HT>\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>n</code>\n              </td>\n              <td>\n                10\n              </td>\n              <td>\n                <code>U+000A</code>\n              </td>\n              <td>\n                LINE FEED (LF)\n              </td>\n              <td>\n                &lt;LF>\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>v</code>\n              </td>\n              <td>\n                11\n              </td>\n              <td>\n                <code>U+000B</code>\n              </td>\n              <td>\n                LINE TABULATION\n              </td>\n              <td>\n                &lt;VT>\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>f</code>\n              </td>\n              <td>\n                12\n              </td>\n              <td>\n                <code>U+000C</code>\n              </td>\n              <td>\n                FORM FEED (FF)\n              </td>\n              <td>\n                &lt;FF>\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>r</code>\n              </td>\n              <td>\n                13\n              </td>\n              <td>\n                <code>U+000D</code>\n              </td>\n              <td>\n                CARRIAGE RETURN (CR)\n              </td>\n              <td>\n                &lt;CR>\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterEscape</span> <span class=\"geq\">::</span> <span class=\"t\">c</span> <span class=\"nt\">AsciiLetter</span></pre>\n        <emu-alg><ol><li>Let <var>ch</var> be the code point matched by <emu-nt>AsciiLetter</emu-nt>.</li><li>Let <var>i</var> be the numeric value of <var>ch</var>.</li><li>Return the remainder of dividing <var>i</var> by 32.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterEscape</span> <span class=\"geq\">::</span> <span class=\"t\">0</span> <span class=\"p\">[lookahead &notin; DecimalDigit]</span></pre>\n        <emu-alg><ol><li>Return the numeric value of U+0000 (NULL).</li></ol></emu-alg>\n        <emu-note>\n          <p><code>\\\\0</code> represents the &lt;NUL> character and cannot be followed by a decimal digit.</p>\n        </emu-note>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterEscape</span> <span class=\"geq\">::</span> <span class=\"nt\">HexEscapeSequence</span></pre>\n        <emu-alg><ol><li>Return the MV of <emu-nt>HexEscapeSequence</emu-nt>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">RegExpUnicodeEscapeSequence</span> <span class=\"geq\">::</span> <span class=\"t\">u</span> <span class=\"nt\">HexLeadSurrogate</span> <span class=\"t\">\\u</span> <span class=\"nt\">HexTrailSurrogate</span></pre>\n        <emu-alg><ol><li>Let <var>lead</var> be the CharacterValue of <emu-nt>HexLeadSurrogate</emu-nt>.</li><li>Let <var>trail</var> be the CharacterValue of <emu-nt>HexTrailSurrogate</emu-nt>.</li><li>Let <var>cp</var> be UTF16SurrogatePairToCodePoint(<var>lead</var>, <var>trail</var>).</li><li>Return the numeric value of <var>cp</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">RegExpUnicodeEscapeSequence</span> <span class=\"geq\">::</span> <span class=\"t\">u</span> <span class=\"nt\">Hex4Digits</span></pre>\n        <emu-alg><ol><li>Return the MV of <emu-nt>Hex4Digits</emu-nt>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">RegExpUnicodeEscapeSequence</span> <span class=\"geq\">::</span> <span class=\"t\">u{</span> <span class=\"nt\">CodePoint</span> <span class=\"t\">}</span></pre>\n        <emu-alg><ol><li>Return the MV of <emu-nt>CodePoint</emu-nt>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">HexLeadSurrogate</span> <span class=\"geq\">::</span> <span class=\"nt\">Hex4Digits</span>\n\n<span class=\"nt lhs\">HexTrailSurrogate</span> <span class=\"geq\">::</span> <span class=\"nt\">Hex4Digits</span>\n\n<span class=\"nt lhs\">HexNonSurrogate</span> <span class=\"geq\">::</span> <span class=\"nt\">Hex4Digits</span></pre>\n        <emu-alg><ol><li>Return the MV of <emu-nt>Hex4Digits</emu-nt>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterEscape</span> <span class=\"geq\">::</span> <span class=\"nt\">IdentityEscape</span></pre>\n        <emu-alg><ol><li>Let <var>ch</var> be the code point matched by <emu-nt>IdentityEscape</emu-nt>.</li><li>Return the numeric value of <var>ch</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSetCharacter</span> <span class=\"geq\">::</span> <span class=\"nt\">SourceCharacter</span> but not <span class=\"nt\">ClassSetSyntaxCharacter</span></pre>\n        <emu-alg><ol><li>Let <var>ch</var> be the code point matched by <emu-nt>SourceCharacter</emu-nt>.</li><li>Return the numeric value of <var>ch</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSetCharacter</span> <span class=\"geq\">::</span> <span class=\"t\">\\</span> <span class=\"nt\">ClassSetReservedPunctuator</span></pre>\n        <emu-alg><ol><li>Let <var>ch</var> be the code point matched by <emu-nt>ClassSetReservedPunctuator</emu-nt>.</li><li>Return the numeric value of <var>ch</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSetCharacter</span> <span class=\"geq\">::</span> <span class=\"t\">\\b</span></pre>\n        <emu-alg><ol><li>Return the numeric value of U+0008 (BACKSPACE).</li></ol></emu-alg>\n      ","2.1.8":"<dl class=\"header\">\n        </dl>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClassEscape</span> <span class=\"geq\">::</span>\n  <span class=\"t\">d</span>\n  <span class=\"t\">D</span>\n  <span class=\"t\">s</span>\n  <span class=\"t\">S</span>\n  <span class=\"t\">w</span>\n  <span class=\"t\">W</span>\n  <span class=\"t\">P{</span> <span class=\"nt\">UnicodePropertyValueExpression</span> <span class=\"t\">}</span>\n\n<span class=\"nt lhs\">UnicodePropertyValueExpression</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">UnicodePropertyName</span> <span class=\"t\">=</span> <span class=\"nt\">UnicodePropertyValue</span>\n\n<span class=\"nt lhs\">NestedClass</span> <span class=\"geq\">::</span>\n  <span class=\"t\">[^</span> <span class=\"nt\">ClassContents</span> <span class=\"t\">]</span>\n\n<span class=\"nt lhs\">ClassContents</span> <span class=\"geq\">::</span>\n  <span class=\"p\">[empty]</span>\n  <span class=\"nt\">NonemptyClassRanges</span>\n\n<span class=\"nt lhs\">ClassSetOperand</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">ClassSetCharacter</span></pre>\n        <emu-alg><ol><li>Return <b>false</b>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">UnicodePropertyValueExpression</span> <span class=\"geq\">::</span> <span class=\"nt\">LoneUnicodePropertyNameOrValue</span></pre>\n        <emu-alg><ol><li>If the source text matched by <emu-nt>LoneUnicodePropertyNameOrValue</emu-nt> is a binary property of strings listed in the “<emu-not-ref>Property name</emu-not-ref>” column of table-binary-unicode-properties-of-strings, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassUnion</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSetRange</span> <span class=\"nt\">ClassUnion</span><span class=\"mod\">?</span></pre>\n        <emu-alg><ol><li>If the <emu-nt>ClassUnion</emu-nt> is present, return MayContainStrings of the <emu-nt>ClassUnion</emu-nt>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassUnion</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSetOperand</span> <span class=\"nt\">ClassUnion</span><span class=\"mod\">?</span></pre>\n        <emu-alg><ol><li>If MayContainStrings of the <emu-nt>ClassSetOperand</emu-nt> is <b>true</b>, return <b>true</b>.</li><li>If <emu-nt>ClassUnion</emu-nt> is present, return MayContainStrings of the <emu-nt>ClassUnion</emu-nt>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassIntersection</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSetOperand</span> <span class=\"t\">&amp;&amp;</span> <span class=\"nt\">ClassSetOperand</span></pre>\n        <emu-alg><ol><li>If MayContainStrings of the first <emu-nt>ClassSetOperand</emu-nt> is <b>false</b>, return <b>false</b>.</li><li>If MayContainStrings of the second <emu-nt>ClassSetOperand</emu-nt> is <b>false</b>, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassIntersection</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassIntersection</span> <span class=\"t\">&amp;&amp;</span> <span class=\"nt\">ClassSetOperand</span></pre>\n        <emu-alg><ol><li>If MayContainStrings of the <emu-nt>ClassIntersection</emu-nt> is <b>false</b>, return <b>false</b>.</li><li>If MayContainStrings of the <emu-nt>ClassSetOperand</emu-nt> is <b>false</b>, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSubtraction</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSetOperand</span> <span class=\"t\">--</span> <span class=\"nt\">ClassSetOperand</span></pre>\n        <emu-alg><ol><li>Return MayContainStrings of the first <emu-nt>ClassSetOperand</emu-nt>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSubtraction</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSubtraction</span> <span class=\"t\">--</span> <span class=\"nt\">ClassSetOperand</span></pre>\n        <emu-alg><ol><li>Return MayContainStrings of the <emu-nt>ClassSubtraction</emu-nt>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassStringDisjunctionContents</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassString</span> <span class=\"t\">|</span> <span class=\"nt\">ClassStringDisjunctionContents</span></pre>\n        <emu-alg><ol><li>If MayContainStrings of the <emu-nt>ClassString</emu-nt> is <b>true</b>, return <b>true</b>.</li><li>Return MayContainStrings of the <emu-nt>ClassStringDisjunctionContents</emu-nt>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassString</span> <span class=\"geq\">::</span> <span class=\"p\">[empty]</span></pre>\n        <emu-alg><ol><li>Return <b>true</b>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassString</span> <span class=\"geq\">::</span> <span class=\"nt\">NonEmptyClassString</span></pre>\n        <emu-alg><ol><li>Return MayContainStrings of the <emu-nt>NonEmptyClassString</emu-nt>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">NonEmptyClassString</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSetCharacter</span> <span class=\"nt\">NonEmptyClassString</span><span class=\"mod\">?</span></pre>\n        <emu-alg><ol><li>If <emu-nt>NonEmptyClassString</emu-nt> is present, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","2.1.9":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>name</var> be the CapturingGroupName of <var>thisGroupName</var>.</li><li>Let <var>pattern</var> be the <emu-nt>Pattern</emu-nt> containing <var>thisGroupName</var>.</li><li>Let <var>result</var> be a new empty List.</li><li>For each <emu-nt>GroupSpecifier</emu-nt> <var>gs</var> that <var>pattern</var> contains, do<ol><li>If the CapturingGroupName of <var>gs</var> is <var>name</var>, then<ol><li>Append <var>gs</var> to <var>result</var>.</li></ol></li></ol></li><li>Return <var>result</var>.</li></ol></emu-alg>\n      ","2.1.10":"<dl class=\"header\">\n        </dl>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">GroupName</span> <span class=\"geq\">::</span> <span class=\"t\">&lt;</span> <span class=\"nt\">RegExpIdentifierName</span> <span class=\"t\">&gt;</span></pre>\n        <emu-alg><ol><li>Let <var>idTextUnescaped</var> be the RegExpIdentifierCodePoints of <emu-nt>RegExpIdentifierName</emu-nt>.</li><li>Return CodePointsToString(<var>idTextUnescaped</var>).</li></ol></emu-alg>\n      ","2.1.11":"<dl class=\"header\">\n        </dl>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">RegExpIdentifierName</span> <span class=\"geq\">::</span> <span class=\"nt\">RegExpIdentifierStart</span></pre>\n        <emu-alg><ol><li>Let <var>cp</var> be the RegExpIdentifierCodePoint of <emu-nt>RegExpIdentifierStart</emu-nt>.</li><li>Return « <var>cp</var> ».</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">RegExpIdentifierName</span> <span class=\"geq\">::</span> <span class=\"nt\">RegExpIdentifierName</span> <span class=\"nt\">RegExpIdentifierPart</span></pre>\n        <emu-alg><ol><li>Let <var>cps</var> be the RegExpIdentifierCodePoints of the derived <emu-nt>RegExpIdentifierName</emu-nt>.</li><li>Let <var>cp</var> be the RegExpIdentifierCodePoint of <emu-nt>RegExpIdentifierPart</emu-nt>.</li><li>Return the list-concatenation of <var>cps</var> and « <var>cp</var> ».</li></ol></emu-alg>\n      ","2.1.12":"<dl class=\"header\">\n        </dl>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">RegExpIdentifierStart</span> <span class=\"geq\">::</span> <span class=\"nt\">IdentifierStartChar</span></pre>\n        <emu-alg><ol><li>Return the code point matched by <emu-nt>IdentifierStartChar</emu-nt>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">RegExpIdentifierPart</span> <span class=\"geq\">::</span> <span class=\"nt\">IdentifierPartChar</span></pre>\n        <emu-alg><ol><li>Return the code point matched by <emu-nt>IdentifierPartChar</emu-nt>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">RegExpIdentifierStart</span> <span class=\"geq\">::</span> <span class=\"t\">\\</span> <span class=\"nt\">RegExpUnicodeEscapeSequence</span>\n\n<span class=\"nt lhs\">RegExpIdentifierPart</span> <span class=\"geq\">::</span> <span class=\"t\">\\</span> <span class=\"nt\">RegExpUnicodeEscapeSequence</span></pre>\n        <emu-alg><ol><li>Return the code point whose numeric value is the CharacterValue of <emu-nt>RegExpUnicodeEscapeSequence</emu-nt>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">RegExpIdentifierStart</span> <span class=\"geq\">::</span> <span class=\"nt\">UnicodeLeadSurrogate</span> <span class=\"nt\">UnicodeTrailSurrogate</span>\n\n<span class=\"nt lhs\">RegExpIdentifierPart</span> <span class=\"geq\">::</span> <span class=\"nt\">UnicodeLeadSurrogate</span> <span class=\"nt\">UnicodeTrailSurrogate</span></pre>\n        <emu-alg><ol><li>Let <var>lead</var> be the code unit whose numeric value is the numeric value of the code point matched by <emu-nt>UnicodeLeadSurrogate</emu-nt>.</li><li>Let <var>trail</var> be the code unit whose numeric value is the numeric value of the code point matched by <emu-nt>UnicodeTrailSurrogate</emu-nt>.</li><li>Return UTF16SurrogatePairToCodePoint(<var>lead</var>, <var>trail</var>).</li></ol></emu-alg>\n      ","2.2":"<p>A regular expression pattern is converted into an Abstract Closure using the process described below. An implementation is encouraged to use more efficient algorithms than the ones listed below, as long as the results are the same. The Abstract Closure is used as the value of a RegExp object's [[RegExpMatcher]] internal slot.</p>\n      <p>A <emu-nt>Pattern</emu-nt> is a BMP pattern if its associated flags contain neither a <code>u</code> nor a <code>v</code>. Otherwise, it is a Unicode pattern. A BMP pattern matches against a String interpreted as consisting of a sequence of 16-bit values that are Unicode code points in the range of the Basic Multilingual Plane. A Unicode pattern matches against a String interpreted as consisting of Unicode code points encoded using UTF-16. In the context of describing the behaviour of a BMP pattern “character” means a single 16-bit Unicode BMP code point. In the context of describing the behaviour of a Unicode pattern “character” means a UTF-16 encoded code point (<a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>). In either context, “character value” means the numeric value of the corresponding non-encoded code point.</p>\n      <p>The syntax and semantics of <emu-nt>Pattern</emu-nt> is defined as if the source text for the <emu-nt>Pattern</emu-nt> was a List of <emu-nt>SourceCharacter</emu-nt> values where each <emu-nt>SourceCharacter</emu-nt> corresponds to a Unicode code point. If a BMP pattern contains a non-BMP <emu-nt>SourceCharacter</emu-nt> the entire pattern is encoded using UTF-16 and the individual code units of that encoding are used as the elements of the List.</p>\n      <emu-note>\n        <p>For example, consider a pattern expressed in source text as the single non-BMP character U+1D11E (MUSICAL SYMBOL G CLEF). Interpreted as a Unicode pattern, it would be a single element (character) List consisting of the single code point U+1D11E. However, interpreted as a BMP pattern, it is first UTF-16 encoded to produce a two element List consisting of the code units 0xD834 and 0xDD1E.</p>\n        <p>Patterns are passed to the RegExp constructor as ECMAScript String values in which non-BMP characters are UTF-16 encoded. For example, the single character MUSICAL SYMBOL G CLEF pattern, expressed as a String value, is a String of length 2 whose elements were the code units 0xD834 and 0xDD1E. So no further translation of the string would be necessary to process it as a BMP pattern consisting of two pattern characters. However, to process it as a Unicode pattern UTF16SurrogatePairToCodePoint must be used in producing a List whose sole element is a single pattern character, the code point U+1D11E.</p>\n        <p>An implementation may not actually perform such translations to or from UTF-16, but the semantics of this specification requires that the result of pattern matching be as if such translations were performed.</p>\n      </emu-note>\n\n      ","2.2.1":"<p>The descriptions below use the following internal data structures:</p>\n        <ul>\n          <li>\n            A <dfn>CharSetElement</dfn> is one of the two following entities:\n            <ul>\n              <li>\n                If <var>regexpRecord</var>.[[UnicodeSets]] is <b>false</b>, then a CharSetElement is a character in the sense of the Pattern Semantics above.\n              </li>\n              <li>\n                If <var>regexpRecord</var>.[[UnicodeSets]] is <b>true</b>, then a CharSetElement is a sequence whose elements are characters in the sense of the Pattern Semantics above. This includes the empty sequence, sequences of one character, and sequences of more than one character. For convenience, when working with CharSetElements of this kind, an individual character is treated interchangeably with a sequence of one character.\n              </li>\n            </ul>\n          </li>\n          <li>\n            A <dfn id=\"pattern-charset\" variants=\"CharSets\">CharSet</dfn> is a mathematical set of CharSetElements.\n          </li>\n          <li>\n            A <dfn id=\"pattern-capturerange\" variants=\"CaptureRanges\">CaptureRange</dfn> is a Record { [[StartIndex]], [[EndIndex]] } that represents the range of characters included in a capture, where [[StartIndex]] is an integer representing the start index (inclusive) of the range within <var>input</var>, and [[EndIndex]] is an integer representing the end index (exclusive) of the range within <var>input</var>. For any CaptureRange, these indices must satisfy the invariant that [[StartIndex]] ≤ [[EndIndex]].\n          </li>\n          <li>\n            A <dfn id=\"pattern-matchstate\" variants=\"MatchStates\" oldids=\"pattern-matchresult\">MatchState</dfn> is a Record { [[Input]], [[EndIndex]], [[Captures]] } where [[Input]] is a List of characters representing the String being matched, [[EndIndex]] is an integer, and [[Captures]] is a List of values, one for each left-capturing parenthesis in the pattern. MatchStates are used to represent partial match states in the regular expression matching algorithms. The [[EndIndex]] is one plus the index of the last input character matched so far by the pattern, while [[Captures]] holds the results of capturing parentheses. The <var>n</var><sup>th</sup> element of [[Captures]] is either a CaptureRange representing the range of characters captured by the <var>n</var><sup>th</sup> set of capturing parentheses, or <b>undefined</b> if the <var>n</var><sup>th</sup> set of capturing parentheses hasn't been reached yet. Due to backtracking, many MatchStates may be in use at any time during the matching process.\n          </li>\n          <li>\n            A <dfn id=\"pattern-matchercontinuation\" variants=\"MatcherContinuations\">MatcherContinuation</dfn> is an Abstract Closure that takes one MatchState argument and returns either a MatchState or <emu-const>failure</emu-const>. The MatcherContinuation attempts to match the remaining portion (specified by the closure's captured values) of the pattern against <var>input</var>, starting at the intermediate state given by its MatchState argument. If the match succeeds, the MatcherContinuation returns the final MatchState that it reached; if the match fails, the MatcherContinuation returns <emu-const>failure</emu-const>.\n          </li>\n          <li>\n            A <dfn id=\"pattern-matcher\" variants=\"Matchers\">Matcher</dfn> is an Abstract Closure that takes two arguments—a MatchState and a MatcherContinuation—and returns either a MatchState or <emu-const>failure</emu-const>. A Matcher attempts to match a middle subpattern (specified by the closure's captured values) of the pattern against the MatchState's [[Input]], starting at the intermediate state given by its MatchState argument. The MatcherContinuation argument should be a closure that matches the rest of the pattern. After matching the subpattern of a pattern to obtain a new MatchState, the Matcher then calls MatcherContinuation on that new MatchState to test if the rest of the pattern can match as well. If it can, the Matcher returns the MatchState returned by MatcherContinuation; if not, the Matcher may try different choices at its choice points, repeatedly calling MatcherContinuation until it either succeeds or all possibilities have been exhausted.\n          </li>\n        </ul>\n\n        ","2.2.1.1":"<p>A <dfn variants=\"RegExp Records\">RegExp Record</dfn> is a Record value used to store information about a RegExp that is needed during compilation and possibly during matching.</p>\n          <p>It has the following fields:</p>\n          <emu-table id=\"table-regexp-record-fields\" caption=\"RegExp Record Fields\">\n            <table>\n              <thead>\n                <tr>\n                  <th>Field Name</th>\n                  <th>Value</th>\n                  <th>Meaning</th>\n                </tr>\n              </thead>\n              <tr>\n                <td>[[IgnoreCase]]</td>\n                <td>a Boolean</td>\n                <td>indicates whether <b>\"i\"</b> appears in the RegExp's flags</td>\n              </tr>\n              <tr>\n                <td>[[Multiline]]</td>\n                <td>a Boolean</td>\n                <td>indicates whether <b>\"m\"</b> appears in the RegExp's flags</td>\n              </tr>\n              <tr>\n                <td>[[DotAll]]</td>\n                <td>a Boolean</td>\n                <td>indicates whether <b>\"s\"</b> appears in the RegExp's flags</td>\n              </tr>\n              <tr>\n                <td>[[Unicode]]</td>\n                <td>a Boolean</td>\n                <td>indicates whether <b>\"u\"</b> appears in the RegExp's flags</td>\n              </tr>\n              <tr>\n                <td>[[UnicodeSets]]</td>\n                <td>a Boolean</td>\n                <td>indicates whether <b>\"v\"</b> appears in the RegExp's flags</td>\n              </tr>\n              <tr>\n                <td>[[CapturingGroupsCount]]</td>\n                <td>a non-negative integer</td>\n                <td>the number of left-capturing parentheses in the RegExp's pattern</td>\n              </tr>\n            </table>\n          </emu-table>\n        ","2.2.2":"<dl class=\"header\">\n        </dl>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Pattern</span> <span class=\"geq\">::</span> <span class=\"nt\">Disjunction</span></pre>\n        <emu-alg><ol><li>Let <var>m</var> be CompileSubpattern of <emu-nt>Disjunction</emu-nt> with arguments <var>regexpRecord</var> and <emu-const>forward</emu-const>.</li><li>Return a new Abstract Closure with parameters (<var>input</var>, <var>index</var>) that captures <var>regexpRecord</var> and <var>m</var> and performs the following steps when called:<ol><li>Assert: <var>input</var> is a List of characters.</li><li>Assert: 0 ≤ <var>index</var> ≤ the number of elements in <var>input</var>.</li><li>Let <var>c</var> be a new MatcherContinuation with parameters (<var>y</var>) that captures nothing and performs the following steps when called:<ol><li>Assert: <var>y</var> is a MatchState.</li><li>Return <var>y</var>.</li></ol></li><li>Let <var>cap</var> be a List of <var>regexpRecord</var>.[[CapturingGroupsCount]] <b>undefined</b> values, indexed 1 through <var>regexpRecord</var>.[[CapturingGroupsCount]].</li><li>Let <var>x</var> be the MatchState { [[Input]]: <var>input</var>, [[EndIndex]]: <var>index</var>, [[Captures]]: <var>cap</var> }.</li><li>Return <var>m</var>(<var>x</var>, <var>c</var>).</li></ol></li></ol></emu-alg>\n        <emu-note>\n          <p>A Pattern compiles to an Abstract Closure value. RegExpBuiltinExec can then apply this procedure to a List of characters and an offset within that List to determine whether the pattern would match starting at exactly that offset within the List, and, if it does match, what the values of the capturing parentheses would be. The algorithms in <a href=\"/text-processing#sec-pattern-semantics\">22.2.2</a> are designed so that compiling a pattern may throw a <b>SyntaxError</b> exception; on the other hand, once the pattern is successfully compiled, applying the resulting Abstract Closure to find a match in a List of characters cannot throw an exception (except for any implementation-defined exceptions that can occur anywhere such as out-of-memory).</p>\n        </emu-note>\n      ","2.2.3":"<dl class=\"header\">\n        </dl>\n        <emu-note>\n          <p>This section is amended in <a href=\"/additional-ecmascript-features-for-web-browsers#sec-compilesubpattern-annexb\">B.1.2.5</a>.</p>\n        </emu-note>\n\n        <!-- Disjunction -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Disjunction</span> <span class=\"geq\">::</span> <span class=\"nt\">Alternative</span> <span class=\"t\">|</span> <span class=\"nt\">Disjunction</span></pre>\n        <emu-alg><ol><li>Let <var>m1</var> be CompileSubpattern of <emu-nt>Alternative</emu-nt> with arguments <var>regexpRecord</var> and <var>direction</var>.</li><li>Let <var>m2</var> be CompileSubpattern of <emu-nt>Disjunction</emu-nt> with arguments <var>regexpRecord</var> and <var>direction</var>.</li><li>Return MatchTwoAlternatives(<var>m1</var>, <var>m2</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>The <code>|</code> regular expression operator separates two alternatives. The pattern first tries to match the left <emu-nt>Alternative</emu-nt> (followed by the sequel of the regular expression); if it fails, it tries to match the right <emu-nt>Disjunction</emu-nt> (followed by the sequel of the regular expression). If the left <emu-nt>Alternative</emu-nt>, the right <emu-nt>Disjunction</emu-nt>, and the sequel all have choice points, all choices in the sequel are tried before moving on to the next choice in the left <emu-nt>Alternative</emu-nt>. If choices in the left <emu-nt>Alternative</emu-nt> are exhausted, the right <emu-nt>Disjunction</emu-nt> is tried instead of the left <emu-nt>Alternative</emu-nt>. Any capturing parentheses inside a portion of the pattern skipped by <code>|</code> produce <b>undefined</b> values instead of Strings. Thus, for example,</p>\n          <pre><code class=\"javascript\">/a|ab/.exec(\"abc\")</code></pre>\n          <p>returns the result <b>\"a\"</b> and not <b>\"ab\"</b>. Moreover,</p>\n          <pre><code class=\"javascript\">/((a)|(ab))((c)|(bc))/.exec(\"abc\")</code></pre>\n          <p>returns the array</p>\n          <pre><code class=\"javascript\">[\"abc\", \"a\", \"a\", undefined, \"bc\", undefined, \"bc\"]</code></pre>\n          <p>and not</p>\n          <pre><code class=\"javascript\">[\"abc\", \"ab\", undefined, \"ab\", \"c\", \"c\", undefined]</code></pre>\n          <p>The order in which the two alternatives are tried is independent of the value of <var>direction</var>.</p>\n        </emu-note>\n\n        <!-- Alternative -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Alternative</span> <span class=\"geq\">::</span> <span class=\"p\">[empty]</span></pre>\n        <emu-alg><ol><li>Return EmptyMatcher().</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Alternative</span> <span class=\"geq\">::</span> <span class=\"nt\">Alternative</span> <span class=\"nt\">Term</span></pre>\n        <emu-alg><ol><li>Let <var>m1</var> be CompileSubpattern of <emu-nt>Alternative</emu-nt> with arguments <var>regexpRecord</var> and <var>direction</var>.</li><li>Let <var>m2</var> be CompileSubpattern of <emu-nt>Term</emu-nt> with arguments <var>regexpRecord</var> and <var>direction</var>.</li><li>Return MatchSequence(<var>m1</var>, <var>m2</var>, <var>direction</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>Consecutive <emu-nt>Term</emu-nt>s try to simultaneously match consecutive portions of <var>input</var>. When <var>direction</var> is <emu-const>forward</emu-const>, if the left <emu-nt>Alternative</emu-nt>, the right <emu-nt>Term</emu-nt>, and the sequel of the regular expression all have choice points, all choices in the sequel are tried before moving on to the next choice in the right <emu-nt>Term</emu-nt>, and all choices in the right <emu-nt>Term</emu-nt> are tried before moving on to the next choice in the left <emu-nt>Alternative</emu-nt>. When <var>direction</var> is <emu-const>backward</emu-const>, the evaluation order of <emu-nt>Alternative</emu-nt> and <emu-nt>Term</emu-nt> are reversed.</p>\n        </emu-note>\n\n        <!-- Term -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Term</span> <span class=\"geq\">::</span> <span class=\"nt\">Assertion</span></pre>\n        <emu-alg><ol><li>Return CompileAssertion of <emu-nt>Assertion</emu-nt> with argument <var>regexpRecord</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The resulting Matcher is independent of <var>direction</var>.</p>\n        </emu-note>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Term</span> <span class=\"geq\">::</span> <span class=\"nt\">Atom</span></pre>\n        <emu-alg><ol><li>Return CompileAtom of <emu-nt>Atom</emu-nt> with arguments <var>regexpRecord</var> and <var>direction</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Term</span> <span class=\"geq\">::</span> <span class=\"nt\">Atom</span> <span class=\"nt\">Quantifier</span></pre>\n        <emu-alg><ol><li>Let <var>m</var> be CompileAtom of <emu-nt>Atom</emu-nt> with arguments <var>regexpRecord</var> and <var>direction</var>.</li><li>Let <var>q</var> be CompileQuantifier of <emu-nt>Quantifier</emu-nt>.</li><li>Assert: <var>q</var>.[[Min]] ≤ <var>q</var>.[[Max]].</li><li>Let <var>parenIndex</var> be CountLeftCapturingParensBefore(<emu-nt>Term</emu-nt>).</li><li>Let <var>parenCount</var> be CountLeftCapturingParensWithin(<emu-nt>Atom</emu-nt>).</li><li>Return a new Matcher with parameters (<var>x</var>, <var>c</var>) that captures <var>m</var>, <var>q</var>, <var>parenIndex</var>, and <var>parenCount</var> and performs the following steps when called:<ol><li>Assert: <var>x</var> is a MatchState.</li><li>Assert: <var>c</var> is a MatcherContinuation.</li><li>Return RepeatMatcher(<var>m</var>, <var>q</var>.[[Min]], <var>q</var>.[[Max]], <var>q</var>.[[Greedy]], <var>x</var>, <var>c</var>, <var>parenIndex</var>, <var>parenCount</var>).</li></ol></li></ol></emu-alg>\n\n        ","2.2.3.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>max</var> = 0, return <var>continue</var>(<var>matchState</var>).</li><li>Let <var>d</var> be a new MatcherContinuation with parameters (<var>y</var>) that captures <var>m</var>, <var>min</var>, <var>max</var>, <var>greedy</var>, <var>matchState</var>, <var>continue</var>, <var>parenIndex</var>, and <var>parenCount</var> and performs the following steps when called:<ol><li>Assert: <var>y</var> is a MatchState.</li><li>If <var>min</var> = 0 and <var>y</var>.[[EndIndex]] = <var>matchState</var>.[[EndIndex]], return <emu-const>failure</emu-const>.</li><li>If <var>min</var> = 0, let <var>min2</var> be 0; else let <var>min2</var> be <var>min</var> - 1.</li><li>If <var>max</var> = +∞, let <var>max2</var> be +∞; else let <var>max2</var> be <var>max</var> - 1.</li><li>Return RepeatMatcher(<var>m</var>, <var>min2</var>, <var>max2</var>, <var>greedy</var>, <var>y</var>, <var>continue</var>, <var>parenIndex</var>, <var>parenCount</var>).</li></ol></li><li>Let <var>cap</var> be a copy of <var>matchState</var>.[[Captures]].</li><li>For each integer <var>k</var> in the inclusive interval from <var>parenIndex</var> + 1 to <var>parenIndex</var> + <var>parenCount</var>, set <var>cap</var>[<var>k</var>] to <b>undefined</b>.</li><li>Let <var>input</var> be <var>matchState</var>.[[Input]].</li><li>Let <var>e</var> be <var>matchState</var>.[[EndIndex]].</li><li>Let <var>xr</var> be the MatchState { [[Input]]: <var>input</var>, [[EndIndex]]: <var>e</var>, [[Captures]]: <var>cap</var> }.</li><li>If <var>min</var> ≠ 0, return <var>m</var>(<var>xr</var>, <var>d</var>).</li><li>If <var>greedy</var> is <b>false</b>, then<ol><li>Let <var>z</var> be <var>continue</var>(<var>matchState</var>).</li><li>If <var>z</var> is not <emu-const>failure</emu-const>, return <var>z</var>.</li><li>Return <var>m</var>(<var>xr</var>, <var>d</var>).</li></ol></li><li>Let <var>z</var> be <var>m</var>(<var>xr</var>, <var>d</var>).</li><li>If <var>z</var> is not <emu-const>failure</emu-const>, return <var>z</var>.</li><li>Return <var>continue</var>(<var>matchState</var>).</li></ol></emu-alg>\n          <emu-note>\n            <p>An <emu-nt>Atom</emu-nt> followed by a <emu-nt>Quantifier</emu-nt> is repeated the number of times specified by the <emu-nt>Quantifier</emu-nt>. A <emu-nt>Quantifier</emu-nt> can be non-greedy, in which case the <emu-nt>Atom</emu-nt> pattern is repeated as few times as possible while still matching the sequel, or it can be greedy, in which case the <emu-nt>Atom</emu-nt> pattern is repeated as many times as possible while still matching the sequel. The <emu-nt>Atom</emu-nt> pattern is repeated rather than the input character sequence that it matches, so different repetitions of the <emu-nt>Atom</emu-nt> can match different input substrings.</p>\n          </emu-note>\n          <emu-note>\n            <p>If the <emu-nt>Atom</emu-nt> and the sequel of the regular expression all have choice points, the <emu-nt>Atom</emu-nt> is first matched as many (or as few, if non-greedy) times as possible. All choices in the sequel are tried before moving on to the next choice in the last repetition of <emu-nt>Atom</emu-nt>. All choices in the last (n<sup>th</sup>) repetition of <emu-nt>Atom</emu-nt> are tried before moving on to the next choice in the next-to-last (n - 1)<sup>st</sup> repetition of <emu-nt>Atom</emu-nt>; at which point it may turn out that more or fewer repetitions of <emu-nt>Atom</emu-nt> are now possible; these are exhausted (again, starting with either as few or as many as possible) before moving on to the next choice in the (n - 1)<sup>st</sup> repetition of <emu-nt>Atom</emu-nt> and so on.</p>\n            <p>Compare</p>\n            <pre><code class=\"javascript\">/a[a-z]{2,4}/.exec(\"abcdefghi\")</code></pre>\n            <p>which returns <b>\"abcde\"</b> with</p>\n            <pre><code class=\"javascript\">/a[a-z]{2,4}?/.exec(\"abcdefghi\")</code></pre>\n            <p>which returns <b>\"abc\"</b>.</p>\n            <p>Consider also</p>\n            <pre><code class=\"javascript\">/(aa|aabaac|ba|b|c)*/.exec(\"aabaac\")</code></pre>\n            <p>which, by the choice point ordering above, returns the array</p>\n            <pre><code class=\"javascript\">[\"aaba\", \"ba\"]</code></pre>\n            <p>and not any of:</p>\n            <pre><code class=\"javascript\">\n              [\"aabaac\", \"aabaac\"]\n              [\"aabaac\", \"c\"]\n            </code></pre>\n            <p>The above ordering of choice points can be used to write a regular expression that calculates the greatest common divisor of two numbers (represented in unary notation). The following example calculates the gcd of 10 and 15:</p>\n            <pre><code class=\"javascript\">\"aaaaaaaaaa,aaaaaaaaaaaaaaa\".replace(/^(a+)\\1*,\\1+$/, \"$1\")</code></pre>\n            <p>which returns the gcd in unary notation <b>\"aaaaa\"</b>.</p>\n          </emu-note>\n          <emu-note>\n            <p>Step step-repeatmatcher-clear-captures of the RepeatMatcher clears <emu-nt>Atom</emu-nt>'s captures each time <emu-nt>Atom</emu-nt> is repeated. We can see its behaviour in the regular expression</p>\n            <pre><code class=\"javascript\">/(z)((a+)?(b+)?(c))*/.exec(\"zaacbbbcac\")</code></pre>\n            <p>which returns the array</p>\n            <pre><code class=\"javascript\">[\"zaacbbbcac\", \"z\", \"ac\", \"a\", undefined, \"c\"]</code></pre>\n            <p>and not</p>\n            <pre><code class=\"javascript\">[\"zaacbbbcac\", \"z\", \"ac\", \"a\", \"bbb\", \"c\"]</code></pre>\n            <p>because each iteration of the outermost <code>*</code> clears all captured Strings contained in the quantified <emu-nt>Atom</emu-nt>, which in this case includes capture Strings numbered 2, 3, 4, and 5.</p>\n          </emu-note>\n          <emu-note>\n            <p>Step step-repeatmatcher-done of the RepeatMatcher states that once the minimum number of repetitions has been satisfied, any more expansions of <emu-nt>Atom</emu-nt> that match the empty character sequence are not considered for further repetitions. This prevents the regular expression engine from falling into an infinite loop on patterns such as:</p>\n            <pre><code class=\"javascript\">/(a*)*/.exec(\"b\")</code></pre>\n            <p>or the slightly more complicated:</p>\n            <pre><code class=\"javascript\">/(a*)b\\1+/.exec(\"baaaac\")</code></pre>\n            <p>which returns the array</p>\n            <pre><code class=\"javascript\">[\"b\", \"\"]</code></pre>\n          </emu-note>\n        ","2.2.3.2":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Return a new Matcher with parameters (<var>matchState</var>, <var>continue</var>) that captures nothing and performs the following steps when called:<ol><li>Assert: <var>matchState</var> is a MatchState.</li><li>Assert: <var>continue</var> is a MatcherContinuation.</li><li>Return <var>continue</var>(<var>matchState</var>).</li></ol></li></ol></emu-alg>\n        ","2.2.3.3":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Return a new Matcher with parameters (<var>matchState</var>, <var>continue</var>) that captures <var>m1</var> and <var>m2</var> and performs the following steps when called:<ol><li>Assert: <var>matchState</var> is a MatchState.</li><li>Assert: <var>continue</var> is a MatcherContinuation.</li><li>Let <var>r</var> be <var>m1</var>(<var>matchState</var>, <var>continue</var>).</li><li>If <var>r</var> is not <emu-const>failure</emu-const>, return <var>r</var>.</li><li>Return <var>m2</var>(<var>matchState</var>, <var>continue</var>).</li></ol></li></ol></emu-alg>\n        ","2.2.3.4":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>direction</var> is <emu-const>forward</emu-const>, then<ol><li>Return a new Matcher with parameters (<var>matchState</var>, <var>continue</var>) that captures <var>m1</var> and <var>m2</var> and performs the following steps when called:<ol><li>Assert: <var>matchState</var> is a MatchState.</li><li>Assert: <var>continue</var> is a MatcherContinuation.</li><li>Let <var>d</var> be a new MatcherContinuation with parameters (<var>y</var>) that captures <var>continue</var> and <var>m2</var> and performs the following steps when called:<ol><li>Assert: <var>y</var> is a MatchState.</li><li>Return <var>m2</var>(<var>y</var>, <var>continue</var>).</li></ol></li><li>Return <var>m1</var>(<var>matchState</var>, <var>d</var>).</li></ol></li></ol></li><li>Assert: <var>direction</var> is <emu-const>backward</emu-const>.</li><li>Return a new Matcher with parameters (<var>matchState</var>, <var>continue</var>) that captures <var>m1</var> and <var>m2</var> and performs the following steps when called:<ol><li>Assert: <var>matchState</var> is a MatchState.</li><li>Assert: <var>continue</var> is a MatcherContinuation.</li><li>Let <var>d</var> be a new MatcherContinuation with parameters (<var>y</var>) that captures <var>continue</var> and <var>m1</var> and performs the following steps when called:<ol><li>Assert: <var>y</var> is a MatchState.</li><li>Return <var>m1</var>(<var>y</var>, <var>continue</var>).</li></ol></li><li>Return <var>m2</var>(<var>matchState</var>, <var>d</var>).</li></ol></li></ol></emu-alg>\n        ","2.2.4":"<dl class=\"header\">\n        </dl>\n        <emu-note>\n          <p>This section is amended in <a href=\"/additional-ecmascript-features-for-web-browsers#sec-compileassertion-annexb\">B.1.2.6</a>.</p>\n        </emu-note>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Assertion</span> <span class=\"geq\">::</span> <span class=\"t\">^</span></pre>\n        <emu-alg><ol><li>Return a new Matcher with parameters (<var>matchState</var>, <var>continue</var>) that captures <var>regexpRecord</var> and performs the following steps when called:<ol><li>Assert: <var>matchState</var> is a MatchState.</li><li>Assert: <var>continue</var> is a MatcherContinuation.</li><li>Let <var>input</var> be <var>matchState</var>.[[Input]].</li><li>Let <var>e</var> be <var>matchState</var>.[[EndIndex]].</li><li>If <var>e</var> = 0, or if <var>regexpRecord</var>.[[Multiline]] is <b>true</b> and the character <var>input</var>[<var>e</var> - 1] is matched by <emu-nt>LineTerminator</emu-nt>, then<ol><li>Return <var>continue</var>(<var>matchState</var>).</li></ol></li><li>Return <emu-const>failure</emu-const>.</li></ol></li></ol></emu-alg>\n        <emu-note>\n          <p>Even when the <code>y</code> flag is used with a pattern, <code>^</code> always matches only at the beginning of <var>input</var>, or (if <var>regexpRecord</var>.[[Multiline]] is <b>true</b>) at the beginning of a line.</p>\n        </emu-note>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Assertion</span> <span class=\"geq\">::</span> <span class=\"t\">$</span></pre>\n        <emu-alg><ol><li>Return a new Matcher with parameters (<var>matchState</var>, <var>continue</var>) that captures <var>regexpRecord</var> and performs the following steps when called:<ol><li>Assert: <var>matchState</var> is a MatchState.</li><li>Assert: <var>continue</var> is a MatcherContinuation.</li><li>Let <var>input</var> be <var>matchState</var>.[[Input]].</li><li>Let <var>e</var> be <var>matchState</var>.[[EndIndex]].</li><li>Let <var>inputLength</var> be the number of elements in <var>input</var>.</li><li>If <var>e</var> = <var>inputLength</var>, or if <var>regexpRecord</var>.[[Multiline]] is <b>true</b> and the character <var>input</var>[<var>e</var>] is matched by <emu-nt>LineTerminator</emu-nt>, then<ol><li>Return <var>continue</var>(<var>matchState</var>).</li></ol></li><li>Return <emu-const>failure</emu-const>.</li></ol></li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Assertion</span> <span class=\"geq\">::</span> <span class=\"t\">\\b</span></pre>\n        <emu-alg><ol><li>Return a new Matcher with parameters (<var>matchState</var>, <var>continue</var>) that captures <var>regexpRecord</var> and performs the following steps when called:<ol><li>Assert: <var>matchState</var> is a MatchState.</li><li>Assert: <var>continue</var> is a MatcherContinuation.</li><li>Let <var>input</var> be <var>matchState</var>.[[Input]].</li><li>Let <var>e</var> be <var>matchState</var>.[[EndIndex]].</li><li>Let <var>a</var> be IsWordChar(<var>regexpRecord</var>, <var>input</var>, <var>e</var> - 1).</li><li>Let <var>b</var> be IsWordChar(<var>regexpRecord</var>, <var>input</var>, <var>e</var>).</li><li>If <var>a</var> is <b>true</b> and <var>b</var> is <b>false</b>, or if <var>a</var> is <b>false</b> and <var>b</var> is <b>true</b>, return <var>continue</var>(<var>matchState</var>).</li><li>Return <emu-const>failure</emu-const>.</li></ol></li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Assertion</span> <span class=\"geq\">::</span> <span class=\"t\">\\B</span></pre>\n        <emu-alg><ol><li>Return a new Matcher with parameters (<var>matchState</var>, <var>continue</var>) that captures <var>regexpRecord</var> and performs the following steps when called:<ol><li>Assert: <var>matchState</var> is a MatchState.</li><li>Assert: <var>continue</var> is a MatcherContinuation.</li><li>Let <var>input</var> be <var>matchState</var>.[[Input]].</li><li>Let <var>e</var> be <var>matchState</var>.[[EndIndex]].</li><li>Let <var>a</var> be IsWordChar(<var>regexpRecord</var>, <var>input</var>, <var>e</var> - 1).</li><li>Let <var>b</var> be IsWordChar(<var>regexpRecord</var>, <var>input</var>, <var>e</var>).</li><li>If <var>a</var> is <b>true</b> and <var>b</var> is <b>true</b>, or if <var>a</var> is <b>false</b> and <var>b</var> is <b>false</b>, return <var>continue</var>(<var>matchState</var>).</li><li>Return <emu-const>failure</emu-const>.</li></ol></li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Assertion</span> <span class=\"geq\">::</span> <span class=\"t\">(?=</span> <span class=\"nt\">Disjunction</span> <span class=\"t\">)</span></pre>\n        <emu-alg><ol><li>Let <var>m</var> be CompileSubpattern of <emu-nt>Disjunction</emu-nt> with arguments <var>regexpRecord</var> and <emu-const>forward</emu-const>.</li><li>Return a new Matcher with parameters (<var>matchState</var>, <var>continue</var>) that captures <var>m</var> and performs the following steps when called:<ol><li>Assert: <var>matchState</var> is a MatchState.</li><li>Assert: <var>continue</var> is a MatcherContinuation.</li><li>Let <var>d</var> be a new MatcherContinuation with parameters (<var>y</var>) that captures nothing and performs the following steps when called:<ol><li>Assert: <var>y</var> is a MatchState.</li><li>Return <var>y</var>.</li></ol></li><li>Let <var>r</var> be <var>m</var>(<var>matchState</var>, <var>d</var>).</li><li>If <var>r</var> is <emu-const>failure</emu-const>, return <emu-const>failure</emu-const>.</li><li>Assert: <var>r</var> is a MatchState.</li><li>Let <var>cap</var> be <var>r</var>.[[Captures]].</li><li>Let <var>input</var> be <var>matchState</var>.[[Input]].</li><li>Let <var>xe</var> be <var>matchState</var>.[[EndIndex]].</li><li>Let <var>z</var> be the MatchState { [[Input]]: <var>input</var>, [[EndIndex]]: <var>xe</var>, [[Captures]]: <var>cap</var> }.</li><li>Return <var>continue</var>(<var>z</var>).</li></ol></li></ol></emu-alg>\n        <emu-note>\n          <p>The form <code>(?=</code> <emu-nt>Disjunction</emu-nt> <code>)</code> specifies a zero-width positive lookahead. In order for it to succeed, the pattern inside <emu-nt>Disjunction</emu-nt> must match at the current position, but the current position is not advanced before matching the sequel. If <emu-nt>Disjunction</emu-nt> can match at the current position in several ways, only the first one is tried. Unlike other regular expression operators, there is no backtracking into a <code>(?=</code> form (this unusual behaviour is inherited from Perl). This only matters when the <emu-nt>Disjunction</emu-nt> contains capturing parentheses and the sequel of the pattern contains backreferences to those captures.</p>\n          <p>For example,</p>\n          <pre><code class=\"javascript\">/(?=(a+))/.exec(\"baaabac\")</code></pre>\n          <p>matches the empty String immediately after the first <code>b</code> and therefore returns the array:</p>\n          <pre><code class=\"javascript\">[\"\", \"aaa\"]</code></pre>\n          <p>To illustrate the lack of backtracking into the lookahead, consider:</p>\n          <pre><code class=\"javascript\">/(?=(a+))a*b\\1/.exec(\"baaabac\")</code></pre>\n          <p>This expression returns</p>\n          <pre><code class=\"javascript\">[\"aba\", \"a\"]</code></pre>\n          <p>and not:</p>\n          <pre><code class=\"javascript\">[\"aaaba\", \"a\"]</code></pre>\n        </emu-note>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Assertion</span> <span class=\"geq\">::</span> <span class=\"t\">(?!</span> <span class=\"nt\">Disjunction</span> <span class=\"t\">)</span></pre>\n        <emu-alg><ol><li>Let <var>m</var> be CompileSubpattern of <emu-nt>Disjunction</emu-nt> with arguments <var>regexpRecord</var> and <emu-const>forward</emu-const>.</li><li>Return a new Matcher with parameters (<var>matchState</var>, <var>continue</var>) that captures <var>m</var> and performs the following steps when called:<ol><li>Assert: <var>matchState</var> is a MatchState.</li><li>Assert: <var>continue</var> is a MatcherContinuation.</li><li>Let <var>d</var> be a new MatcherContinuation with parameters (<var>y</var>) that captures nothing and performs the following steps when called:<ol><li>Assert: <var>y</var> is a MatchState.</li><li>Return <var>y</var>.</li></ol></li><li>Let <var>r</var> be <var>m</var>(<var>matchState</var>, <var>d</var>).</li><li>If <var>r</var> is not <emu-const>failure</emu-const>, return <emu-const>failure</emu-const>.</li><li>Return <var>continue</var>(<var>matchState</var>).</li></ol></li></ol></emu-alg>\n        <emu-note>\n          <p>The form <code>(?!</code> <emu-nt>Disjunction</emu-nt> <code>)</code> specifies a zero-width negative lookahead. In order for it to succeed, the pattern inside <emu-nt>Disjunction</emu-nt> must fail to match at the current position. The current position is not advanced before matching the sequel. <emu-nt>Disjunction</emu-nt> can contain capturing parentheses, but backreferences to them only make sense from within <emu-nt>Disjunction</emu-nt> itself. Backreferences to these capturing parentheses from elsewhere in the pattern always return <b>undefined</b> because the negative lookahead must fail for the pattern to succeed. For example,</p>\n          <pre><code class=\"javascript\">/(.*?)a(?!(a+)b\\2c)\\2(.*)/.exec(\"baaabaac\")</code></pre>\n          <p>looks for an <code>a</code> not immediately followed by some positive number n of <code>a</code>'s, a <code>b</code>, another n <code>a</code>'s (specified by the first <code>\\\\2</code>) and a <code>c</code>. The second <code>\\\\2</code> is outside the negative lookahead, so it matches against <b>undefined</b> and therefore always succeeds. The whole expression returns the array:</p>\n          <pre><code class=\"javascript\">[\"baaabaac\", \"ba\", undefined, \"abaac\"]</code></pre>\n        </emu-note>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Assertion</span> <span class=\"geq\">::</span> <span class=\"t\">(?&lt;=</span> <span class=\"nt\">Disjunction</span> <span class=\"t\">)</span></pre>\n        <emu-alg><ol><li>Let <var>m</var> be CompileSubpattern of <emu-nt>Disjunction</emu-nt> with arguments <var>regexpRecord</var> and <emu-const>backward</emu-const>.</li><li>Return a new Matcher with parameters (<var>matchState</var>, <var>continue</var>) that captures <var>m</var> and performs the following steps when called:<ol><li>Assert: <var>matchState</var> is a MatchState.</li><li>Assert: <var>continue</var> is a MatcherContinuation.</li><li>Let <var>d</var> be a new MatcherContinuation with parameters (<var>y</var>) that captures nothing and performs the following steps when called:<ol><li>Assert: <var>y</var> is a MatchState.</li><li>Return <var>y</var>.</li></ol></li><li>Let <var>r</var> be <var>m</var>(<var>matchState</var>, <var>d</var>).</li><li>If <var>r</var> is <emu-const>failure</emu-const>, return <emu-const>failure</emu-const>.</li><li>Assert: <var>r</var> is a MatchState.</li><li>Let <var>cap</var> be <var>r</var>.[[Captures]].</li><li>Let <var>input</var> be <var>matchState</var>.[[Input]].</li><li>Let <var>xe</var> be <var>matchState</var>.[[EndIndex]].</li><li>Let <var>z</var> be the MatchState { [[Input]]: <var>input</var>, [[EndIndex]]: <var>xe</var>, [[Captures]]: <var>cap</var> }.</li><li>Return <var>continue</var>(<var>z</var>).</li></ol></li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Assertion</span> <span class=\"geq\">::</span> <span class=\"t\">(?&lt;!</span> <span class=\"nt\">Disjunction</span> <span class=\"t\">)</span></pre>\n        <emu-alg><ol><li>Let <var>m</var> be CompileSubpattern of <emu-nt>Disjunction</emu-nt> with arguments <var>regexpRecord</var> and <emu-const>backward</emu-const>.</li><li>Return a new Matcher with parameters (<var>matchState</var>, <var>continue</var>) that captures <var>m</var> and performs the following steps when called:<ol><li>Assert: <var>matchState</var> is a MatchState.</li><li>Assert: <var>continue</var> is a MatcherContinuation.</li><li>Let <var>d</var> be a new MatcherContinuation with parameters (<var>y</var>) that captures nothing and performs the following steps when called:<ol><li>Assert: <var>y</var> is a MatchState.</li><li>Return <var>y</var>.</li></ol></li><li>Let <var>r</var> be <var>m</var>(<var>matchState</var>, <var>d</var>).</li><li>If <var>r</var> is not <emu-const>failure</emu-const>, return <emu-const>failure</emu-const>.</li><li>Return <var>continue</var>(<var>matchState</var>).</li></ol></li></ol></emu-alg>\n\n        ","2.2.4.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>inputLength</var> be the number of elements in <var>input</var>.</li><li>If <var>e</var> = -1 or <var>e</var> = <var>inputLength</var>, return <b>false</b>.</li><li>Let <var>c</var> be the character <var>input</var>[<var>e</var>].</li><li>If WordCharacters(<var>regexpRecord</var>) contains <var>c</var>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n        ","2.2.5":"<dl class=\"header\">\n        </dl>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Quantifier</span> <span class=\"geq\">::</span> <span class=\"nt\">QuantifierPrefix</span></pre>\n        <emu-alg><ol><li>Let <var>qp</var> be CompileQuantifierPrefix of <emu-nt>QuantifierPrefix</emu-nt>.</li><li>Return the Record { [[Min]]: <var>qp</var>.[[Min]], [[Max]]: <var>qp</var>.[[Max]], [[Greedy]]: <b>true</b> }.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Quantifier</span> <span class=\"geq\">::</span> <span class=\"nt\">QuantifierPrefix</span> <span class=\"t\">?</span></pre>\n        <emu-alg><ol><li>Let <var>qp</var> be CompileQuantifierPrefix of <emu-nt>QuantifierPrefix</emu-nt>.</li><li>Return the Record { [[Min]]: <var>qp</var>.[[Min]], [[Max]]: <var>qp</var>.[[Max]], [[Greedy]]: <b>false</b> }.</li></ol></emu-alg>\n      ","2.2.6":"<dl class=\"header\">\n        </dl>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">QuantifierPrefix</span> <span class=\"geq\">::</span> <span class=\"t\">*</span></pre>\n        <emu-alg><ol><li>Return the Record { [[Min]]: 0, [[Max]]: +∞ }.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">QuantifierPrefix</span> <span class=\"geq\">::</span> <span class=\"t\">+</span></pre>\n        <emu-alg><ol><li>Return the Record { [[Min]]: 1, [[Max]]: +∞ }.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">QuantifierPrefix</span> <span class=\"geq\">::</span> <span class=\"t\">?</span></pre>\n        <emu-alg><ol><li>Return the Record { [[Min]]: 0, [[Max]]: 1 }.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">QuantifierPrefix</span> <span class=\"geq\">::</span> <span class=\"t\">{</span> <span class=\"nt\">DecimalDigits</span> <span class=\"t\">}</span></pre>\n        <emu-alg><ol><li>Let <var>i</var> be the MV of <emu-nt>DecimalDigits</emu-nt> (see <a href=\"/ecmascript-language-lexical-grammar#sec-literals-numeric-literals\">12.9.3</a>).</li><li>Return the Record { [[Min]]: <var>i</var>, [[Max]]: <var>i</var> }.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">QuantifierPrefix</span> <span class=\"geq\">::</span> <span class=\"t\">{</span> <span class=\"nt\">DecimalDigits</span> <span class=\"t\">,}</span></pre>\n        <emu-alg><ol><li>Let <var>i</var> be the MV of <emu-nt>DecimalDigits</emu-nt>.</li><li>Return the Record { [[Min]]: <var>i</var>, [[Max]]: +∞ }.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">QuantifierPrefix</span> <span class=\"geq\">::</span> <span class=\"t\">{</span> <span class=\"nt\">DecimalDigits</span> <span class=\"t\">,</span> <span class=\"nt\">DecimalDigits</span> <span class=\"t\">}</span></pre>\n        <emu-alg><ol><li>Let <var>i</var> be the MV of the first <emu-nt>DecimalDigits</emu-nt>.</li><li>Let <var>j</var> be the MV of the second <emu-nt>DecimalDigits</emu-nt>.</li><li>Return the Record { [[Min]]: <var>i</var>, [[Max]]: <var>j</var> }.</li></ol></emu-alg>\n      ","2.2.7":"<dl class=\"header\">\n        </dl>\n        <emu-note>\n          <p>This section is amended in <a href=\"/additional-ecmascript-features-for-web-browsers#sec-compileatom-annexb\">B.1.2.7</a>.</p>\n        </emu-note>\n\n        <!-- Atom -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Atom</span> <span class=\"geq\">::</span> <span class=\"nt\">PatternCharacter</span></pre>\n        <emu-alg><ol><li>Let <var>ch</var> be the character matched by <emu-nt>PatternCharacter</emu-nt>.</li><li>Let <var>charSet</var> be a one-element CharSet containing the character <var>ch</var>.</li><li>Return CharacterSetMatcher(<var>regexpRecord</var>, <var>charSet</var>, <b>false</b>, <var>direction</var>).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Atom</span> <span class=\"geq\">::</span> <span class=\"t\">.</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be AllCharacters(<var>regexpRecord</var>).</li><li>If <var>regexpRecord</var>.[[DotAll]] is not <b>true</b>, then<ol><li>Remove from <var>charSet</var> all characters corresponding to a code point on the right-hand side of the <emu-nt>LineTerminator</emu-nt> production.</li></ol></li><li>Return CharacterSetMatcher(<var>regexpRecord</var>, <var>charSet</var>, <b>false</b>, <var>direction</var>).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Atom</span> <span class=\"geq\">::</span> <span class=\"nt\">CharacterClass</span></pre>\n        <emu-alg><ol><li>Let <var>cc</var> be CompileCharacterClass of <emu-nt>CharacterClass</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>cs</var> be <var>cc</var>.[[CharSet]].</li><li>If <var>regexpRecord</var>.[[UnicodeSets]] is <b>false</b> or every CharSetElement of <var>cs</var> consists of a single character (including if <var>cs</var> is empty), return CharacterSetMatcher(<var>regexpRecord</var>, <var>cs</var>, <var>cc</var>.[[Invert]], <var>direction</var>).</li><li>Assert: <var>cc</var>.[[Invert]] is <b>false</b>.</li><li>Let <var>lm</var> be an empty List of Matchers.</li><li>For each CharSetElement <var>s</var> in <var>cs</var> containing more than 1 character, iterating in descending order of length, do<ol><li>Let <var>cs2</var> be a one-element CharSet containing the last code point of <var>s</var>.</li><li>Let <var>m2</var> be CharacterSetMatcher(<var>regexpRecord</var>, <var>cs2</var>, <b>false</b>, <var>direction</var>).</li><li>For each code point <var>c1</var> in <var>s</var>, iterating backwards from its second-to-last code point, do<ol><li>Let <var>cs1</var> be a one-element CharSet containing <var>c1</var>.</li><li>Let <var>m1</var> be CharacterSetMatcher(<var>regexpRecord</var>, <var>cs1</var>, <b>false</b>, <var>direction</var>).</li><li>Set <var>m2</var> to MatchSequence(<var>m1</var>, <var>m2</var>, <var>direction</var>).</li></ol></li><li>Append <var>m2</var> to <var>lm</var>.</li></ol></li><li>Let <var>singles</var> be the CharSet containing every CharSetElement of <var>cs</var> that consists of a single character.</li><li>Append CharacterSetMatcher(<var>regexpRecord</var>, <var>singles</var>, <b>false</b>, <var>direction</var>) to <var>lm</var>.</li><li>If <var>cs</var> contains the empty sequence of characters, append EmptyMatcher() to <var>lm</var>.</li><li>Let <var>m2</var> be the last Matcher in <var>lm</var>.</li><li>For each Matcher <var>m1</var> of <var>lm</var>, iterating backwards from its second-to-last element, do<ol><li>Set <var>m2</var> to MatchTwoAlternatives(<var>m1</var>, <var>m2</var>).</li></ol></li><li>Return <var>m2</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Atom</span> <span class=\"geq\">::</span> <span class=\"t\">(</span> <span class=\"nt\">GroupSpecifier</span><span class=\"mod\">?</span> <span class=\"nt\">Disjunction</span> <span class=\"t\">)</span></pre>\n        <emu-alg><ol><li>Let <var>m</var> be CompileSubpattern of <emu-nt>Disjunction</emu-nt> with arguments <var>regexpRecord</var> and <var>direction</var>.</li><li>Let <var>parenIndex</var> be CountLeftCapturingParensBefore(<emu-nt>Atom</emu-nt>).</li><li>Return a new Matcher with parameters (<var>x</var>, <var>c</var>) that captures <var>direction</var>, <var>m</var>, and <var>parenIndex</var> and performs the following steps when called:<ol><li>Assert: <var>x</var> is a MatchState.</li><li>Assert: <var>c</var> is a MatcherContinuation.</li><li>Let <var>d</var> be a new MatcherContinuation with parameters (<var>y</var>) that captures <var>x</var>, <var>c</var>, <var>direction</var>, and <var>parenIndex</var> and performs the following steps when called:<ol><li>Assert: <var>y</var> is a MatchState.</li><li>Let <var>cap</var> be a copy of <var>y</var>.[[Captures]].</li><li>Let <var>input</var> be <var>x</var>.[[Input]].</li><li>Let <var>xe</var> be <var>x</var>.[[EndIndex]].</li><li>Let <var>ye</var> be <var>y</var>.[[EndIndex]].</li><li>If <var>direction</var> is <emu-const>forward</emu-const>, then<ol><li>Assert: <var>xe</var> ≤ <var>ye</var>.</li><li>Let <var>r</var> be the CaptureRange { [[StartIndex]]: <var>xe</var>, [[EndIndex]]: <var>ye</var> }.</li></ol></li><li>Else,<ol><li>Assert: <var>direction</var> is <emu-const>backward</emu-const>.</li><li>Assert: <var>ye</var> ≤ <var>xe</var>.</li><li>Let <var>r</var> be the CaptureRange { [[StartIndex]]: <var>ye</var>, [[EndIndex]]: <var>xe</var> }.</li></ol></li><li>Set <var>cap</var>[<var>parenIndex</var> + 1] to <var>r</var>.</li><li>Let <var>z</var> be the MatchState { [[Input]]: <var>input</var>, [[EndIndex]]: <var>ye</var>, [[Captures]]: <var>cap</var> }.</li><li>Return <var>c</var>(<var>z</var>).</li></ol></li><li>Return <var>m</var>(<var>x</var>, <var>d</var>).</li></ol></li></ol></emu-alg>\n        <emu-note>\n          <p>Parentheses of the form <code>(</code> <emu-nt>Disjunction</emu-nt> <code>)</code> serve both to group the components of the <emu-nt>Disjunction</emu-nt> pattern together and to save the result of the match. The result can be used either in a backreference (<code>\\\\</code> followed by a non-zero decimal number), referenced in a replace String, or returned as part of an array from the regular expression matching Abstract Closure. To inhibit the capturing behaviour of parentheses, use the form <code>(?:</code> <emu-nt>Disjunction</emu-nt> <code>)</code> instead.</p>\n        </emu-note>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Atom</span> <span class=\"geq\">::</span> <span class=\"t\">(?</span> <span class=\"nt\">RegularExpressionModifiers</span> <span class=\"t\">:</span> <span class=\"nt\">Disjunction</span> <span class=\"t\">)</span></pre>\n        <emu-alg><ol><li>Let <var>addModifiers</var> be the source text matched by <emu-nt>RegularExpressionModifiers</emu-nt>.</li><li>Let <var>removeModifiers</var> be the empty String.</li><li>Let <var>modifiedRer</var> be UpdateModifiers(<var>regexpRecord</var>, CodePointsToString(<var>addModifiers</var>), <var>removeModifiers</var>).</li><li>Return CompileSubpattern of <emu-nt>Disjunction</emu-nt> with arguments <var>modifiedRer</var> and <var>direction</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">Atom</span> <span class=\"geq\">::</span> <span class=\"t\">(?</span> <span class=\"nt\">RegularExpressionModifiers</span> <span class=\"t\">-</span> <span class=\"nt\">RegularExpressionModifiers</span> <span class=\"t\">:</span> <span class=\"nt\">Disjunction</span> <span class=\"t\">)</span></pre>\n        <emu-alg><ol><li>Let <var>addModifiers</var> be the source text matched by the first <emu-nt>RegularExpressionModifiers</emu-nt>.</li><li>Let <var>removeModifiers</var> be the source text matched by the second <emu-nt>RegularExpressionModifiers</emu-nt>.</li><li>Let <var>modifiedRer</var> be UpdateModifiers(<var>regexpRecord</var>, CodePointsToString(<var>addModifiers</var>), CodePointsToString(<var>removeModifiers</var>)).</li><li>Return CompileSubpattern of <emu-nt>Disjunction</emu-nt> with arguments <var>modifiedRer</var> and <var>direction</var>.</li></ol></emu-alg>\n\n        <!-- AtomEscape -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">AtomEscape</span> <span class=\"geq\">::</span> <span class=\"nt\">DecimalEscape</span></pre>\n        <emu-alg><ol><li>Let <var>n</var> be the CapturingGroupNumber of <emu-nt>DecimalEscape</emu-nt>.</li><li>Assert: <var>n</var> ≤ <var>regexpRecord</var>.[[CapturingGroupsCount]].</li><li>Return BackreferenceMatcher(<var>regexpRecord</var>, « <var>n</var> », <var>direction</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>An escape sequence of the form <code>\\\\</code> followed by a non-zero decimal number <var>n</var> matches the result of the <var>n</var><sup>th</sup> set of capturing parentheses (<a href=\"/text-processing#sec-pattern-notation\">22.2.2.1</a>). It is an error if the regular expression has fewer than <var>n</var> capturing parentheses. If the regular expression has <var>n</var> or more capturing parentheses but the <var>n</var><sup>th</sup> one is <b>undefined</b> because it has not captured anything, then the backreference always succeeds.</p>\n        </emu-note>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">AtomEscape</span> <span class=\"geq\">::</span> <span class=\"nt\">CharacterEscape</span></pre>\n        <emu-alg><ol><li>Let <var>cv</var> be the CharacterValue of <emu-nt>CharacterEscape</emu-nt>.</li><li>Let <var>ch</var> be the character whose character value is <var>cv</var>.</li><li>Let <var>charSet</var> be a one-element CharSet containing the character <var>ch</var>.</li><li>Return CharacterSetMatcher(<var>regexpRecord</var>, <var>charSet</var>, <b>false</b>, <var>direction</var>).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">AtomEscape</span> <span class=\"geq\">::</span> <span class=\"nt\">CharacterClassEscape</span></pre>\n        <emu-alg><ol><li>Let <var>cs</var> be CompileToCharSet of <emu-nt>CharacterClassEscape</emu-nt> with argument <var>regexpRecord</var>.</li><li>If <var>regexpRecord</var>.[[UnicodeSets]] is <b>false</b> or every CharSetElement of <var>cs</var> consists of a single character (including if <var>cs</var> is empty), return CharacterSetMatcher(<var>regexpRecord</var>, <var>cs</var>, <b>false</b>, <var>direction</var>).</li><li>Let <var>lm</var> be an empty List of Matchers.</li><li>For each CharSetElement <var>s</var> in <var>cs</var> containing more than 1 character, iterating in descending order of length, do<ol><li>Let <var>cs2</var> be a one-element CharSet containing the last code point of <var>s</var>.</li><li>Let <var>m2</var> be CharacterSetMatcher(<var>regexpRecord</var>, <var>cs2</var>, <b>false</b>, <var>direction</var>).</li><li>For each code point <var>c1</var> in <var>s</var>, iterating backwards from its second-to-last code point, do<ol><li>Let <var>cs1</var> be a one-element CharSet containing <var>c1</var>.</li><li>Let <var>m1</var> be CharacterSetMatcher(<var>regexpRecord</var>, <var>cs1</var>, <b>false</b>, <var>direction</var>).</li><li>Set <var>m2</var> to MatchSequence(<var>m1</var>, <var>m2</var>, <var>direction</var>).</li></ol></li><li>Append <var>m2</var> to <var>lm</var>.</li></ol></li><li>Let <var>singles</var> be the CharSet containing every CharSetElement of <var>cs</var> that consists of a single character.</li><li>Append CharacterSetMatcher(<var>regexpRecord</var>, <var>singles</var>, <b>false</b>, <var>direction</var>) to <var>lm</var>.</li><li>If <var>cs</var> contains the empty sequence of characters, append EmptyMatcher() to <var>lm</var>.</li><li>Let <var>m2</var> be the last Matcher in <var>lm</var>.</li><li>For each Matcher <var>m1</var> of <var>lm</var>, iterating backwards from its second-to-last element, do<ol><li>Set <var>m2</var> to MatchTwoAlternatives(<var>m1</var>, <var>m2</var>).</li></ol></li><li>Return <var>m2</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">AtomEscape</span> <span class=\"geq\">::</span> <span class=\"t\">k</span> <span class=\"nt\">GroupName</span></pre>\n        <emu-alg><ol><li>Let <var>matchingGroupSpecifiers</var> be GroupSpecifiersThatMatch(<emu-nt>GroupName</emu-nt>).</li><li>Let <var>parenIndices</var> be a new empty List.</li><li>For each <emu-nt>GroupSpecifier</emu-nt> <var>groupSpecifier</var> of <var>matchingGroupSpecifiers</var>, do<ol><li>Let <var>parenIndex</var> be CountLeftCapturingParensBefore(<var>groupSpecifier</var>).</li><li>Append <var>parenIndex</var> to <var>parenIndices</var>.</li></ol></li><li>Return BackreferenceMatcher(<var>regexpRecord</var>, <var>parenIndices</var>, <var>direction</var>).</li></ol></emu-alg>\n\n        ","2.2.7.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>regexpRecord</var>.[[UnicodeSets]] is <b>true</b>, then<ol><li>Assert: <var>invert</var> is <b>false</b>.</li><li>Assert: Every CharSetElement of <var>charSet</var> consists of a single character.</li></ol></li><li>Return a new Matcher with parameters (<var>x</var>, <var>c</var>) that captures <var>regexpRecord</var>, <var>charSet</var>, <var>invert</var>, and <var>direction</var> and performs the following steps when called:<ol><li>Assert: <var>x</var> is a MatchState.</li><li>Assert: <var>c</var> is a MatcherContinuation.</li><li>Let <var>input</var> be <var>x</var>.[[Input]].</li><li>Let <var>endIndex</var> be <var>x</var>.[[EndIndex]].</li><li>If <var>direction</var> is <emu-const>forward</emu-const>, let <var>f</var> be <var>endIndex</var> + 1.</li><li>Else, let <var>f</var> be <var>endIndex</var> - 1.</li><li>Let <var>inputLength</var> be the number of elements in <var>input</var>.</li><li>If <var>f</var> &lt; 0 or <var>f</var> > <var>inputLength</var>, return <emu-const>failure</emu-const>.</li><li>Let <var>index</var> be min(<var>endIndex</var>, <var>f</var>).</li><li>Let <var>ch</var> be the character <var>input</var>[<var>index</var>].</li><li>Let <var>cc</var> be Canonicalize(<var>regexpRecord</var>, <var>ch</var>).</li><li>If there exists a CharSetElement in <var>charSet</var> containing exactly one character <var>a</var> such that Canonicalize(<var>regexpRecord</var>, <var>a</var>) is <var>cc</var>, let <var>found</var> be <b>true</b>; else let <var>found</var> be <b>false</b>.</li><li>If <var>invert</var> is <b>false</b> and <var>found</var> is <b>false</b>, return <emu-const>failure</emu-const>.</li><li>If <var>invert</var> is <b>true</b> and <var>found</var> is <b>true</b>, return <emu-const>failure</emu-const>.</li><li>Let <var>cap</var> be <var>x</var>.[[Captures]].</li><li>Let <var>y</var> be the MatchState { [[Input]]: <var>input</var>, [[EndIndex]]: <var>f</var>, [[Captures]]: <var>cap</var> }.</li><li>Return <var>c</var>(<var>y</var>).</li></ol></li></ol></emu-alg>\n        ","2.2.7.2":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Return a new Matcher with parameters (<var>x</var>, <var>c</var>) that captures <var>regexpRecord</var>, <var>ns</var>, and <var>direction</var> and performs the following steps when called:<ol><li>Assert: <var>x</var> is a MatchState.</li><li>Assert: <var>c</var> is a MatcherContinuation.</li><li>Let <var>input</var> be <var>x</var>.[[Input]].</li><li>Let <var>cap</var> be <var>x</var>.[[Captures]].</li><li>Let <var>r</var> be <b>undefined</b>.</li><li>For each integer <var>n</var> of <var>ns</var>, do<ol><li>If <var>cap</var>[<var>n</var>] is not <b>undefined</b>, then<ol><li>Assert: <var>r</var> is <b>undefined</b>.</li><li>Set <var>r</var> to <var>cap</var>[<var>n</var>].</li></ol></li></ol></li><li>If <var>r</var> is <b>undefined</b>, return <var>c</var>(<var>x</var>).</li><li>Let <var>endIndex</var> be <var>x</var>.[[EndIndex]].</li><li>Let <var>rs</var> be <var>r</var>.[[StartIndex]].</li><li>Let <var>re</var> be <var>r</var>.[[EndIndex]].</li><li>Let <var>len</var> be <var>re</var> - <var>rs</var>.</li><li>If <var>direction</var> is <emu-const>forward</emu-const>, let <var>f</var> be <var>endIndex</var> + <var>len</var>.</li><li>Else, let <var>f</var> be <var>endIndex</var> - <var>len</var>.</li><li>Let <var>inputLength</var> be the number of elements in <var>input</var>.</li><li>If <var>f</var> &lt; 0 or <var>f</var> > <var>inputLength</var>, return <emu-const>failure</emu-const>.</li><li>Let <var>g</var> be min(<var>endIndex</var>, <var>f</var>).</li><li>If there exists an integer <var>i</var> in the interval from 0 (inclusive) to <var>len</var> (exclusive) such that Canonicalize(<var>regexpRecord</var>, <var>input</var>[<var>rs</var> + <var>i</var>]) is not Canonicalize(<var>regexpRecord</var>, <var>input</var>[<var>g</var> + <var>i</var>]), return <emu-const>failure</emu-const>.</li><li>Let <var>y</var> be the MatchState { [[Input]]: <var>input</var>, [[EndIndex]]: <var>f</var>, [[Captures]]: <var>cap</var> }.</li><li>Return <var>c</var>(<var>y</var>).</li></ol></li></ol></emu-alg>\n        ","2.2.7.3":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If HasEitherUnicodeFlag(<var>regexpRecord</var>) is <b>true</b> and <var>regexpRecord</var>.[[IgnoreCase]] is <b>true</b>, then<ol><li>If the file <a href=\"https://unicode.org/Public/UCD/latest/ucd/CaseFolding.txt\"><code>CaseFolding.txt</code></a> of the Unicode Character Database provides a simple or common case folding mapping for <var>ch</var>, return the result of applying that mapping to <var>ch</var>.</li><li>Return <var>ch</var>.</li></ol></li><li>If <var>regexpRecord</var>.[[IgnoreCase]] is <b>false</b>, return <var>ch</var>.</li><li>Assert: <var>ch</var> is a UTF-16 code unit.</li><li>Let <var>cp</var> be the code point whose numeric value is the numeric value of <var>ch</var>.</li><li>Let <var>u</var> be toUppercase(« <var>cp</var> »), according to the Unicode Default Case Conversion algorithm.</li><li>Let <var>uStr</var> be CodePointsToString(<var>u</var>).</li><li>If the length of <var>uStr</var> ≠ 1, return <var>ch</var>.</li><li>Let <var>cu</var> be <var>uStr</var>'s single code unit element.</li><li>If the numeric value of <var>ch</var> ≥ 128 and the numeric value of <var>cu</var> &lt; 128, return <var>ch</var>.</li><li>Return <var>cu</var>.</li></ol></emu-alg>\n          <emu-note>\n            <p>In case-insignificant matches when HasEitherUnicodeFlag(<var>regexpRecord</var>) is <b>true</b>, all characters are implicitly case-folded using the simple mapping provided by the Unicode Standard immediately before they are compared. The simple mapping always maps to a single code point, so it does not map, for example, <code>ß</code> (U+00DF LATIN SMALL LETTER SHARP S) to <code>ss</code> or <code>SS</code>. It may however map code points outside the Basic Latin block to code points within it—for example, <code>ſ</code> (U+017F LATIN SMALL LETTER LONG S) case-folds to <code>s</code> (U+0073 LATIN SMALL LETTER S) and <code>K</code> (U+212A KELVIN SIGN) case-folds to <code>k</code> (U+006B LATIN SMALL LETTER K). Strings containing those code points are matched by regular expressions such as <code>/[a-z]/ui</code>.</p>\n            <p>In case-insignificant matches when HasEitherUnicodeFlag(<var>regexpRecord</var>) is <b>false</b>, the mapping is based on Unicode Default Case Conversion algorithm toUppercase rather than toCasefold, which results in some subtle differences. For example, <code>Ω</code> (U+2126 OHM SIGN) is mapped by toUppercase to itself but by toCasefold to <code>ω</code> (U+03C9 GREEK SMALL LETTER OMEGA) along with <code>Ω</code> (U+03A9 GREEK CAPITAL LETTER OMEGA), so <b>\"\\u2126\"</b> is matched by <code>/[ω]/ui</code> and <code>/[\\u03A9]/ui</code> but not by <code>/[ω]/i</code> or <code>/[\\u03A9]/i</code>. Also, no code point outside the Basic Latin block is mapped to a code point within it, so strings such as <b>\"\\u017F ſ\"</b> and <b>\"\\u212A K\"</b> are not matched by <code>/[a-z]/i</code>.</p>\n          </emu-note>\n        ","2.2.7.4":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Assert: <var>add</var> and <var>remove</var> have no elements in common.</li><li>Let <var>ignoreCase</var> be <var>regexpRecord</var>.[[IgnoreCase]].</li><li>Let <var>multiline</var> be <var>regexpRecord</var>.[[Multiline]].</li><li>Let <var>dotAll</var> be <var>regexpRecord</var>.[[DotAll]].</li><li>Let <var>unicode</var> be <var>regexpRecord</var>.[[Unicode]].</li><li>Let <var>unicodeSets</var> be <var>regexpRecord</var>.[[UnicodeSets]].</li><li>Let <var>capturingGroupsCount</var> be <var>regexpRecord</var>.[[CapturingGroupsCount]].</li><li>If <var>remove</var> contains <b>\"i\"</b>, set <var>ignoreCase</var> to <b>false</b>.</li><li>Else if <var>add</var> contains <b>\"i\"</b>, set <var>ignoreCase</var> to <b>true</b>.</li><li>If <var>remove</var> contains <b>\"m\"</b>, set <var>multiline</var> to <b>false</b>.</li><li>Else if <var>add</var> contains <b>\"m\"</b>, set <var>multiline</var> to <b>true</b>.</li><li>If <var>remove</var> contains <b>\"s\"</b>, set <var>dotAll</var> to <b>false</b>.</li><li>Else if <var>add</var> contains <b>\"s\"</b>, set <var>dotAll</var> to <b>true</b>.</li><li>Return the RegExp Record { [[IgnoreCase]]: <var>ignoreCase</var>, [[Multiline]]: <var>multiline</var>, [[DotAll]]: <var>dotAll</var>, [[Unicode]]: <var>unicode</var>, [[UnicodeSets]]: <var>unicodeSets</var>, [[CapturingGroupsCount]]: <var>capturingGroupsCount</var> }.</li></ol></emu-alg>\n        ","2.2.8":"<dl class=\"header\">\n        </dl>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClass</span> <span class=\"geq\">::</span> <span class=\"t\">[</span> <span class=\"nt\">ClassContents</span> <span class=\"t\">]</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of <emu-nt>ClassContents</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return the Record { [[CharSet]]: <var>charSet</var>, [[Invert]]: <b>false</b> }.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClass</span> <span class=\"geq\">::</span> <span class=\"t\">[^</span> <span class=\"nt\">ClassContents</span> <span class=\"t\">]</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of <emu-nt>ClassContents</emu-nt> with argument <var>regexpRecord</var>.</li><li>If <var>regexpRecord</var>.[[UnicodeSets]] is <b>true</b>, then<ol><li>Return the Record { [[CharSet]]: CharacterComplement(<var>regexpRecord</var>, <var>charSet</var>), [[Invert]]: <b>false</b> }.</li></ol></li><li>Return the Record { [[CharSet]]: <var>charSet</var>, [[Invert]]: <b>true</b> }.</li></ol></emu-alg>\n      ","2.2.9":"<dl class=\"header\">\n        </dl>\n        <emu-note>\n          <p>This section is amended in <a href=\"/additional-ecmascript-features-for-web-browsers#sec-compiletocharset-annexb\">B.1.2.8</a>.</p>\n        </emu-note>\n\n        <!-- ClassContents -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassContents</span> <span class=\"geq\">::</span> <span class=\"p\">[empty]</span></pre>\n        <emu-alg><ol><li>Return the empty CharSet.</li></ol></emu-alg>\n\n        <!-- NonemptyClassRanges -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">NonemptyClassRanges</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassAtom</span> <span class=\"nt\">NonemptyClassRangesNoDash</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of <emu-nt>ClassAtom</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>otherSet</var> be CompileToCharSet of <emu-nt>NonemptyClassRangesNoDash</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return the union of CharSets <var>charSet</var> and <var>otherSet</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">NonemptyClassRanges</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassAtom</span> <span class=\"t\">-</span> <span class=\"nt\">ClassAtom</span> <span class=\"nt\">ClassContents</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of the first <emu-nt>ClassAtom</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>otherSet</var> be CompileToCharSet of the second <emu-nt>ClassAtom</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>remainingSet</var> be CompileToCharSet of <emu-nt>ClassContents</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>rangeSet</var> be CharacterRange(<var>charSet</var>, <var>otherSet</var>).</li><li>Return the union of <var>rangeSet</var> and <var>remainingSet</var>.</li></ol></emu-alg>\n\n        <!-- NonemptyClassRangesNoDash -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">NonemptyClassRangesNoDash</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassAtomNoDash</span> <span class=\"nt\">NonemptyClassRangesNoDash</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of <emu-nt>ClassAtomNoDash</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>otherSet</var> be CompileToCharSet of <emu-nt>NonemptyClassRangesNoDash</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return the union of CharSets <var>charSet</var> and <var>otherSet</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">NonemptyClassRangesNoDash</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassAtomNoDash</span> <span class=\"t\">-</span> <span class=\"nt\">ClassAtom</span> <span class=\"nt\">ClassContents</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of <emu-nt>ClassAtomNoDash</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>otherSet</var> be CompileToCharSet of <emu-nt>ClassAtom</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>remainingSet</var> be CompileToCharSet of <emu-nt>ClassContents</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>rangeSet</var> be CharacterRange(<var>charSet</var>, <var>otherSet</var>).</li><li>Return the union of <var>rangeSet</var> and <var>remainingSet</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p><emu-nt>ClassContents</emu-nt> can expand into a single <emu-nt>ClassAtom</emu-nt> and/or ranges of two <emu-nt>ClassAtom</emu-nt> separated by dashes. In the latter case the <emu-nt>ClassContents</emu-nt> includes all characters between the first <emu-nt>ClassAtom</emu-nt> and the second <emu-nt>ClassAtom</emu-nt>, inclusive; an error occurs if either <emu-nt>ClassAtom</emu-nt> does not represent a single character (for example, if one is \\w) or if the first <emu-nt>ClassAtom</emu-nt>'s character value is strictly greater than the second <emu-nt>ClassAtom</emu-nt>'s character value.</p>\n        </emu-note>\n        <emu-note>\n          <p>Even if the pattern ignores case, the case of the two ends of a range is significant in determining which characters belong to the range. Thus, for example, the pattern <code>/[E-F]/i</code> matches only the letters <code>E</code>, <code>F</code>, <code>e</code>, and <code>f</code>, while the pattern <code>/[E-f]/i</code> matches all uppercase and lowercase letters in the Unicode Basic Latin block as well as the symbols <code>[</code>, <code>\\\\</code>, <code>]</code>, <code>^</code>, <code>_</code>, and <code>`</code>.</p>\n        </emu-note>\n        <emu-note>\n          <p>A <code>-</code> character can be treated literally or it can denote a range. It is treated literally if it is the first or last character of <emu-nt>ClassContents</emu-nt>, the beginning or end limit of a range specification, or immediately follows a range specification.</p>\n        </emu-note>\n\n        <!-- ClassAtom -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassAtom</span> <span class=\"geq\">::</span> <span class=\"t\">-</span></pre>\n        <emu-alg><ol><li>Return the CharSet containing the single character <code>-</code> U+002D (HYPHEN-MINUS).</li></ol></emu-alg>\n\n        <!-- ClassAtomNoDash -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassAtomNoDash</span> <span class=\"geq\">::</span> <span class=\"nt\">SourceCharacter</span> but not <span class=\"oneof\">one of</span> <span class=\"t\">\\</span> or <span class=\"t\">]</span> or <span class=\"t\">-</span></pre>\n        <emu-alg><ol><li>Return the CharSet containing the character matched by <emu-nt>SourceCharacter</emu-nt>.</li></ol></emu-alg>\n\n        <!-- ClassEscape -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassEscape</span> <span class=\"geq\">::</span>\n  <span class=\"t\">b</span>\n  <span class=\"t\">-</span>\n  <span class=\"nt\">CharacterEscape</span></pre>\n        <emu-alg><ol><li>Let <var>cv</var> be the CharacterValue of this <emu-nt>ClassEscape</emu-nt>.</li><li>Let <var>c</var> be the character whose character value is <var>cv</var>.</li><li>Return the CharSet containing the single character <var>c</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>A <emu-nt>ClassAtom</emu-nt> can use any of the escape sequences that are allowed in the rest of the regular expression except for <code>\\\\b</code>, <code>\\\\B</code>, and backreferences. Inside a <emu-nt>CharacterClass</emu-nt>, <code>\\\\b</code> means the backspace character, while <code>\\\\B</code> and backreferences raise errors. Using a backreference inside a <emu-nt>ClassAtom</emu-nt> causes an error.</p>\n        </emu-note>\n\n        <!-- CharacterClassEscape -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">d</span></pre>\n        <emu-alg><ol><li>Return the ten-element CharSet containing the characters <code>0</code>, <code>1</code>, <code>2</code>, <code>3</code>, <code>4</code>, <code>5</code>, <code>6</code>, <code>7</code>, <code>8</code>, and <code>9</code>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">D</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be the CharSet returned by <code class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">d</span></code>.</li><li>Return CharacterComplement(<var>regexpRecord</var>, <var>charSet</var>).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">s</span></pre>\n        <emu-alg><ol><li>Return the CharSet containing all characters corresponding to a code point on the right-hand side of the <emu-nt>WhiteSpace</emu-nt> or <emu-nt>LineTerminator</emu-nt> productions.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">S</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be the CharSet returned by <code class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">s</span></code>.</li><li>Return CharacterComplement(<var>regexpRecord</var>, <var>charSet</var>).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">w</span></pre>\n        <emu-alg><ol><li>Return MaybeSimpleCaseFolding(<var>regexpRecord</var>, WordCharacters(<var>regexpRecord</var>)).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">W</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be the CharSet returned by <code class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">w</span></code>.</li><li>Return CharacterComplement(<var>regexpRecord</var>, <var>charSet</var>).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">p{</span> <span class=\"nt\">UnicodePropertyValueExpression</span> <span class=\"t\">}</span></pre>\n        <emu-alg><ol><li>Return CompileToCharSet of <emu-nt>UnicodePropertyValueExpression</emu-nt> with argument <var>regexpRecord</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">CharacterClassEscape</span> <span class=\"geq\">::</span> <span class=\"t\">P{</span> <span class=\"nt\">UnicodePropertyValueExpression</span> <span class=\"t\">}</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of <emu-nt>UnicodePropertyValueExpression</emu-nt> with argument <var>regexpRecord</var>.</li><li>Assert: <var>charSet</var> contains only single code points.</li><li>Return CharacterComplement(<var>regexpRecord</var>, <var>charSet</var>).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">UnicodePropertyValueExpression</span> <span class=\"geq\">::</span> <span class=\"nt\">UnicodePropertyName</span> <span class=\"t\">=</span> <span class=\"nt\">UnicodePropertyValue</span></pre>\n        <emu-alg><ol><li>Let <var>ps</var> be the source text matched by <emu-nt>UnicodePropertyName</emu-nt>.</li><li>Let <var>p</var> be UnicodeMatchProperty(<var>regexpRecord</var>, <var>ps</var>).</li><li>Assert: <var>p</var> is a <emu-not-ref>Unicode property name</emu-not-ref> or property alias listed in the “<emu-not-ref>Property name and aliases</emu-not-ref>” column of table-nonbinary-unicode-properties.</li><li>Let <var>vs</var> be the source text matched by <emu-nt>UnicodePropertyValue</emu-nt>.</li><li>Let <var>v</var> be UnicodeMatchPropertyValue(<var>p</var>, <var>vs</var>).</li><li>Let <var>charSet</var> be the CharSet containing all Unicode code points whose character database definition includes the property <var>p</var> with value <var>v</var>.</li><li>Return MaybeSimpleCaseFolding(<var>regexpRecord</var>, <var>charSet</var>).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">UnicodePropertyValueExpression</span> <span class=\"geq\">::</span> <span class=\"nt\">LoneUnicodePropertyNameOrValue</span></pre>\n        <emu-alg><ol><li>Let <var>s</var> be the source text matched by <emu-nt>LoneUnicodePropertyNameOrValue</emu-nt>.</li><li>If UnicodeMatchPropertyValue(<code>General_Category</code>, <var>s</var>) is a Unicode property value or property value alias for the General_Category (gc) property listed in <a href=\"https://unicode.org/Public/UCD/latest/ucd/PropertyValueAliases.txt\"><code>PropertyValueAliases.txt</code></a>, then<ol><li>Return the CharSet containing all Unicode code points whose character database definition includes the property “General_Category” with value <var>s</var>.</li></ol></li><li>Let <var>p</var> be UnicodeMatchProperty(<var>regexpRecord</var>, <var>s</var>).</li><li>Assert: <var>p</var> is a binary Unicode property or binary property alias listed in the “<emu-not-ref>Property name and aliases</emu-not-ref>” column of table-binary-unicode-properties, or a binary Unicode property of strings listed in the “<emu-not-ref>Property name</emu-not-ref>” column of table-binary-unicode-properties-of-strings.</li><li>Let <var>charSet</var> be the CharSet containing all CharSetElements whose character database definition includes the property <var>p</var> with value “True”.</li><li>Return MaybeSimpleCaseFolding(<var>regexpRecord</var>, <var>charSet</var>).</li></ol></emu-alg>\n\n        <!-- ClassUnion -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassUnion</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSetRange</span> <span class=\"nt\">ClassUnion</span><span class=\"mod\">?</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of <emu-nt>ClassSetRange</emu-nt> with argument <var>regexpRecord</var>.</li><li>If <emu-nt>ClassUnion</emu-nt> is present, then<ol><li>Let <var>otherSet</var> be CompileToCharSet of <emu-nt>ClassUnion</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return the union of CharSets <var>charSet</var> and <var>otherSet</var>.</li></ol></li><li>Return <var>charSet</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassUnion</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSetOperand</span> <span class=\"nt\">ClassUnion</span><span class=\"mod\">?</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of <emu-nt>ClassSetOperand</emu-nt> with argument <var>regexpRecord</var>.</li><li>If <emu-nt>ClassUnion</emu-nt> is present, then<ol><li>Let <var>otherSet</var> be CompileToCharSet of <emu-nt>ClassUnion</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return the union of CharSets <var>charSet</var> and <var>otherSet</var>.</li></ol></li><li>Return <var>charSet</var>.</li></ol></emu-alg>\n\n        <!-- ClassIntersection -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassIntersection</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSetOperand</span> <span class=\"t\">&amp;&amp;</span> <span class=\"nt\">ClassSetOperand</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of the first <emu-nt>ClassSetOperand</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>otherSet</var> be CompileToCharSet of the second <emu-nt>ClassSetOperand</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return the intersection of CharSets <var>charSet</var> and <var>otherSet</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassIntersection</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassIntersection</span> <span class=\"t\">&amp;&amp;</span> <span class=\"nt\">ClassSetOperand</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of the <emu-nt>ClassIntersection</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>otherSet</var> be CompileToCharSet of the <emu-nt>ClassSetOperand</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return the intersection of CharSets <var>charSet</var> and <var>otherSet</var>.</li></ol></emu-alg>\n\n        <!-- ClassSubtraction -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSubtraction</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSetOperand</span> <span class=\"t\">--</span> <span class=\"nt\">ClassSetOperand</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of the first <emu-nt>ClassSetOperand</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>otherSet</var> be CompileToCharSet of the second <emu-nt>ClassSetOperand</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return the CharSet containing the CharSetElements of <var>charSet</var> which are not also CharSetElements of <var>otherSet</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSubtraction</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSubtraction</span> <span class=\"t\">--</span> <span class=\"nt\">ClassSetOperand</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of the <emu-nt>ClassSubtraction</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>otherSet</var> be CompileToCharSet of the <emu-nt>ClassSetOperand</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return the CharSet containing the CharSetElements of <var>charSet</var> which are not also CharSetElements of <var>otherSet</var>.</li></ol></emu-alg>\n\n        <!-- ClassSetRange -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSetRange</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSetCharacter</span> <span class=\"t\">-</span> <span class=\"nt\">ClassSetCharacter</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of the first <emu-nt>ClassSetCharacter</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>otherSet</var> be CompileToCharSet of the second <emu-nt>ClassSetCharacter</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return MaybeSimpleCaseFolding(<var>regexpRecord</var>, CharacterRange(<var>charSet</var>, <var>otherSet</var>)).</li></ol></emu-alg>\n        <emu-note>\n          <p>The result will often consist of two or more ranges. When UnicodeSets is <b>true</b> and IgnoreCase is <b>true</b>, then MaybeSimpleCaseFolding(<var>regexpRecord</var>, [Ā-č]) will include only the odd-numbered code points of that range.</p>\n        </emu-note>\n\n        <!-- ClassSetOperand -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSetOperand</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSetCharacter</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of <emu-nt>ClassSetCharacter</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return MaybeSimpleCaseFolding(<var>regexpRecord</var>, <var>charSet</var>).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSetOperand</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassStringDisjunction</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of <emu-nt>ClassStringDisjunction</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return MaybeSimpleCaseFolding(<var>regexpRecord</var>, <var>charSet</var>).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSetOperand</span> <span class=\"geq\">::</span> <span class=\"nt\">NestedClass</span></pre>\n        <emu-alg><ol><li>Return CompileToCharSet of <emu-nt>NestedClass</emu-nt> with argument <var>regexpRecord</var>.</li></ol></emu-alg>\n\n        <!-- NestedClass -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">NestedClass</span> <span class=\"geq\">::</span> <span class=\"t\">[</span> <span class=\"nt\">ClassContents</span> <span class=\"t\">]</span></pre>\n        <emu-alg><ol><li>Return CompileToCharSet of <emu-nt>ClassContents</emu-nt> with argument <var>regexpRecord</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">NestedClass</span> <span class=\"geq\">::</span> <span class=\"t\">[^</span> <span class=\"nt\">ClassContents</span> <span class=\"t\">]</span></pre>\n        <emu-alg><ol><li>Let <var>charSet</var> be CompileToCharSet of <emu-nt>ClassContents</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return CharacterComplement(<var>regexpRecord</var>, <var>charSet</var>).</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">NestedClass</span> <span class=\"geq\">::</span> <span class=\"t\">\\</span> <span class=\"nt\">CharacterClassEscape</span></pre>\n        <emu-alg><ol><li>Return CompileToCharSet of <emu-nt>CharacterClassEscape</emu-nt> with argument <var>regexpRecord</var>.</li></ol></emu-alg>\n\n        <!-- ClassStringDisjunction -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassStringDisjunction</span> <span class=\"geq\">::</span> <span class=\"t\">\\q{</span> <span class=\"nt\">ClassStringDisjunctionContents</span> <span class=\"t\">}</span></pre>\n        <emu-alg><ol><li>Return CompileToCharSet of <emu-nt>ClassStringDisjunctionContents</emu-nt> with argument <var>regexpRecord</var>.</li></ol></emu-alg>\n\n        <!-- ClassStringDisjunctionContents -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassStringDisjunctionContents</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassString</span></pre>\n        <emu-alg><ol><li>Let <var>s</var> be CompileClassSetString of <emu-nt>ClassString</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return the CharSet containing the one string <var>s</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassStringDisjunctionContents</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassString</span> <span class=\"t\">|</span> <span class=\"nt\">ClassStringDisjunctionContents</span></pre>\n        <emu-alg><ol><li>Let <var>s</var> be CompileClassSetString of <emu-nt>ClassString</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>charSet</var> be the CharSet containing the one string <var>s</var>.</li><li>Let <var>otherSet</var> be CompileToCharSet of <emu-nt>ClassStringDisjunctionContents</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return the union of CharSets <var>charSet</var> and <var>otherSet</var>.</li></ol></emu-alg>\n\n        <!-- ClassSetCharacter -->\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSetCharacter</span> <span class=\"geq\">::</span>\n  <span class=\"nt\">SourceCharacter</span> but not <span class=\"nt\">ClassSetSyntaxCharacter</span>\n  <span class=\"t\">\\</span> <span class=\"nt\">CharacterEscape</span>\n  <span class=\"t\">\\</span> <span class=\"nt\">ClassSetReservedPunctuator</span></pre>\n        <emu-alg><ol><li>Let <var>cv</var> be the CharacterValue of this <emu-nt>ClassSetCharacter</emu-nt>.</li><li>Let <var>c</var> be the character whose character value is <var>cv</var>.</li><li>Return the CharSet containing the single character <var>c</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassSetCharacter</span> <span class=\"geq\">::</span> <span class=\"t\">\\b</span></pre>\n        <emu-alg><ol><li>Return the CharSet containing the single character U+0008 (BACKSPACE).</li></ol></emu-alg>\n\n        ","2.2.9.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Assert: <var>charSet</var> and <var>otherSet</var> each contain exactly one character.</li><li>Let <var>a</var> be the one character in CharSet <var>charSet</var>.</li><li>Let <var>b</var> be the one character in CharSet <var>otherSet</var>.</li><li>Let <var>i</var> be the character value of character <var>a</var>.</li><li>Let <var>j</var> be the character value of character <var>b</var>.</li><li>Assert: <var>i</var> ≤ <var>j</var>.</li><li>Return the CharSet containing all characters with a character value in the inclusive interval from <var>i</var> to <var>j</var>.</li></ol></emu-alg>\n        ","2.2.9.2":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>regexpRecord</var>.[[Unicode]] is <b>true</b> or <var>regexpRecord</var>.[[UnicodeSets]] is <b>true</b>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n        ","2.2.9.3":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>Returns a CharSet containing the characters considered \"word characters\" for the purposes of <code>\\\\b</code>, <code>\\\\B</code>, <code>\\\\w</code>, and <code>\\\\W</code>.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>basicWordChars</var> be the CharSet containing every character in the ASCII word characters.</li><li>Let <var>extraWordChars</var> be the CharSet containing all characters <var>c</var> such that <var>c</var> is not in <var>basicWordChars</var> but Canonicalize(<var>regexpRecord</var>, <var>c</var>) is in <var>basicWordChars</var>.</li><li>Assert: <var>extraWordChars</var> is empty unless HasEitherUnicodeFlag(<var>regexpRecord</var>) is <b>true</b> and <var>regexpRecord</var>.[[IgnoreCase]] is <b>true</b>.</li><li>Return the union of <var>basicWordChars</var> and <var>extraWordChars</var>.</li></ol></emu-alg>\n        ","2.2.9.4":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>Returns the set of “all characters” according to the regular expression flags.</dd>\n          </dl>\n          <emu-alg><ol><li>If <var>regexpRecord</var>.[[UnicodeSets]] is <b>true</b> and <var>regexpRecord</var>.[[IgnoreCase]] is <b>true</b>, then<ol><li>Return the CharSet containing all Unicode code points <var>c</var> that do not have a <a href=\"https://www.unicode.org/reports/tr44/#Simple_Case_Folding\">Simple Case Folding</a> mapping (that is, scf(<var>c</var>)=<var>c</var>).</li></ol></li><li>If HasEitherUnicodeFlag(<var>regexpRecord</var>) is <b>true</b>, then<ol><li>Return the CharSet containing all code point values.</li></ol></li><li>Return the CharSet containing all code unit values.</li></ol></emu-alg>\n        ","2.2.9.5":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>If <var>regexpRecord</var>.[[UnicodeSets]] is <b>false</b> or <var>regexpRecord</var>.[[IgnoreCase]] is <b>false</b>, it returns <var>charSet</var>. Otherwise, it uses the <a href=\"https://www.unicode.org/reports/tr44/#Simple_Case_Folding\">Simple Case Folding</a> (<emu-eqn id=\"eqn-scf\" aoid=\"scf\">scf(<var>cp</var>)</emu-eqn>) definitions in the file <a href=\"https://unicode.org/Public/UCD/latest/ucd/CaseFolding.txt\"><code>CaseFolding.txt</code></a> of the Unicode Character Database (each of which maps a single code point to another single code point) to map each CharSetElement of <var>charSet</var> character-by-character into a canonical form and returns the resulting CharSet.</dd>\n          </dl>\n          <emu-alg><ol><li>If <var>regexpRecord</var>.[[UnicodeSets]] is <b>false</b> or <var>regexpRecord</var>.[[IgnoreCase]] is <b>false</b>, return <var>charSet</var>.</li><li>Let <var>otherSet</var> be a new empty CharSet.</li><li>For each CharSetElement <var>s</var> of <var>charSet</var>, do<ol><li>Let <var>t</var> be an empty sequence of characters.</li><li>For each single code point <var>cp</var> in <var>s</var>, do<ol><li>Append scf(<var>cp</var>) to <var>t</var>.</li></ol></li><li>Add <var>t</var> to <var>otherSet</var>.</li></ol></li><li>Return <var>otherSet</var>.</li></ol></emu-alg>\n        ","2.2.9.6":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>charSet</var> be AllCharacters(<var>regexpRecord</var>).</li><li>Return the CharSet containing the CharSetElements of <var>charSet</var> which are not also CharSetElements of <var>complement</var>.</li></ol></emu-alg>\n        ","2.2.9.7":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>regexpRecord</var>.[[UnicodeSets]] is <b>true</b> and <var>p</var> is a <emu-not-ref>Unicode property name</emu-not-ref> listed in the “<emu-not-ref>Property name</emu-not-ref>” column of table-binary-unicode-properties-of-strings, then<ol><li>Return the List of Unicode code points <var>p</var>.</li></ol></li><li>Assert: <var>p</var> is a <emu-not-ref>Unicode property name</emu-not-ref> or property alias listed in the “<emu-not-ref>Property name and aliases</emu-not-ref>” column of table-nonbinary-unicode-properties or table-binary-unicode-properties.</li><li>Let <var>c</var> be the canonical <emu-not-ref>property name</emu-not-ref> of <var>p</var> as given in the “<emu-not-ref>Canonical property name</emu-not-ref>” column of the corresponding row.</li><li>Return the List of Unicode code points <var>c</var>.</li></ol></emu-alg>\n          <p>Implementations must support the <emu-not-ref>Unicode property names</emu-not-ref> and aliases listed in table-nonbinary-unicode-properties, table-binary-unicode-properties, and table-binary-unicode-properties-of-strings. To ensure interoperability, implementations must not support any other <emu-not-ref>property names</emu-not-ref> or aliases.</p>\n          <emu-note>\n            <p>For example, <code>Script_Extensions</code> (<emu-not-ref>property name</emu-not-ref>) and <code>scx</code> (property alias) are valid, but <code>script_extensions</code> or <code>Scx</code> aren't.</p>\n          </emu-note>\n          <emu-note>\n            <p>The listed properties form a superset of what <a href=\"https://unicode.org/reports/tr18/#RL1.2\">UTS18 RL1.2</a> requires.</p>\n          </emu-note>\n          <emu-note>\n            <p>The spellings of entries in these tables (including casing) match the spellings used in the file <a href=\"https://unicode.org/Public/UCD/latest/ucd/PropertyAliases.txt\"><code>PropertyAliases.txt</code></a> in the Unicode Character Database. The precise spellings in that file are <a href=\"https://www.unicode.org/policies/stability_policy.html#Alias_Stability\">guaranteed to be stable</a>.</p>\n          </emu-note>\n          <emu-import href=\"table-nonbinary-unicode-properties.html\"></emu-import>\n          <emu-import href=\"table-binary-unicode-properties.html\"></emu-import>\n          <emu-import href=\"table-binary-unicode-properties-of-strings.html\"></emu-import>\n        ","2.2.9.8":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Assert: <var>p</var> is a canonical, unaliased <emu-not-ref>Unicode property name</emu-not-ref> listed in the “<emu-not-ref>Canonical property name</emu-not-ref>” column of table-nonbinary-unicode-properties.</li><li>Assert: <var>v</var> is a property value or property value alias for the Unicode property <var>p</var> listed in <a href=\"https://unicode.org/Public/UCD/latest/ucd/PropertyValueAliases.txt\"><code>PropertyValueAliases.txt</code></a>.</li><li>Let <var>value</var> be the canonical property value of <var>v</var> as given in the “Canonical property value” column of the corresponding row.</li><li>Return the List of Unicode code points <var>value</var>.</li></ol></emu-alg>\n          <p>Implementations must support the Unicode property values and property value aliases listed in <a href=\"https://unicode.org/Public/UCD/latest/ucd/PropertyValueAliases.txt\"><code>PropertyValueAliases.txt</code></a> for the properties listed in table-nonbinary-unicode-properties. To ensure interoperability, implementations must not support any other property values or property value aliases.</p>\n          <emu-note>\n            <p>For example, <code>Xpeo</code> and <code>Old_Persian</code> are valid <code>Script_Extensions</code> values, but <code>xpeo</code> and <code>Old Persian</code> aren't.</p>\n          </emu-note>\n          <emu-note>\n            <p>This algorithm differs from <a href=\"https://unicode.org/reports/tr44/#Matching_Symbolic\">the matching rules for symbolic values listed in UAX44</a>: case, <a href=\"/ecmascript-language-lexical-grammar#sec-white-space\">white space</a>, U+002D (HYPHEN-MINUS), and U+005F (LOW LINE) are not ignored, and the <code>Is</code> prefix is not supported.</p>\n          </emu-note>\n        ","2.2.10":"<dl class=\"header\">\n        </dl>\n\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassString</span> <span class=\"geq\">::</span> <span class=\"p\">[empty]</span></pre>\n        <emu-alg><ol><li>Return an empty sequence of characters.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">ClassString</span> <span class=\"geq\">::</span> <span class=\"nt\">NonEmptyClassString</span></pre>\n        <emu-alg><ol><li>Return CompileClassSetString of <emu-nt>NonEmptyClassString</emu-nt> with argument <var>regexpRecord</var>.</li></ol></emu-alg>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">NonEmptyClassString</span> <span class=\"geq\">::</span> <span class=\"nt\">ClassSetCharacter</span> <span class=\"nt\">NonEmptyClassString</span><span class=\"mod\">?</span></pre>\n        <emu-alg><ol><li>Let <var>cs</var> be CompileToCharSet of <emu-nt>ClassSetCharacter</emu-nt> with argument <var>regexpRecord</var>.</li><li>Let <var>s1</var> be the sequence of characters that is the single CharSetElement of <var>cs</var>.</li><li>If <emu-nt>NonEmptyClassString</emu-nt> is present, then<ol><li>Let <var>s2</var> be CompileClassSetString of <emu-nt>NonEmptyClassString</emu-nt> with argument <var>regexpRecord</var>.</li><li>Return the concatenation of <var>s1</var> and <var>s2</var>.</li></ol></li><li>Return <var>s1</var>.</li></ol></emu-alg>\n      ","2.3":"","2.3.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>obj</var> be ! RegExpAlloc(<emu-intrinsic>%RegExp%</emu-intrinsic>).</li><li>Return ? RegExpInitialize(<var>obj</var>, <var>pattern</var>, <var>flags</var>).</li></ol></emu-alg>\n      ","2.3.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>obj</var> be ? OrdinaryCreateFromConstructor(<var>newTarget</var>, <b>\"<emu-intrinsic>%RegExp.prototype%</emu-intrinsic>\"</b>, « [[OriginalSource]], [[OriginalFlags]], [[RegExpRecord]], [[RegExpMatcher]] »).</li><li>Perform ! DefinePropertyOrThrow(<var>obj</var>, <b>\"lastIndex\"</b>, PropertyDescriptor { [[Writable]]: <b>true</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }).</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","2.3.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If <var>pattern</var> is <b>undefined</b>, set <var>pattern</var> to the empty String.</li><li>Else, set <var>pattern</var> to ? ToString(<var>pattern</var>).</li><li>If <var>flags</var> is <b>undefined</b>, set <var>flags</var> to the empty String.</li><li>Else, set <var>flags</var> to ? ToString(<var>flags</var>).</li><li>If <var>flags</var> contains any code unit other than <b>\"d\"</b>, <b>\"g\"</b>, <b>\"i\"</b>, <b>\"m\"</b>, <b>\"s\"</b>, <b>\"u\"</b>, <b>\"v\"</b>, or <b>\"y\"</b>, throw a <b>SyntaxError</b> exception.</li><li>If <var>flags</var> contains any code unit more than once, throw a <b>SyntaxError</b> exception.</li><li>If <var>flags</var> contains <b>\"i\"</b>, let <var>i</var> be <b>true</b>; else let <var>i</var> be <b>false</b>.</li><li>If <var>flags</var> contains <b>\"m\"</b>, let <var>m</var> be <b>true</b>; else let <var>m</var> be <b>false</b>.</li><li>If <var>flags</var> contains <b>\"s\"</b>, let <var>s</var> be <b>true</b>; else let <var>s</var> be <b>false</b>.</li><li>If <var>flags</var> contains <b>\"u\"</b>, let <var>u</var> be <b>true</b>; else let <var>u</var> be <b>false</b>.</li><li>If <var>flags</var> contains <b>\"v\"</b>, let <var>v</var> be <b>true</b>; else let <var>v</var> be <b>false</b>.</li><li>If <var>u</var> is <b>true</b> or <var>v</var> is <b>true</b>, then<ol><li>Let <var>patternText</var> be StringToCodePoints(<var>pattern</var>).</li></ol></li><li>Else,<ol><li>Let <var>patternText</var> be the result of interpreting each of <var>pattern</var>'s 16-bit elements as a Unicode BMP code point. UTF-16 decoding is not applied to the elements.</li></ol></li><li>Let <var>parseResult</var> be ParsePattern(<var>patternText</var>, <var>u</var>, <var>v</var>).</li><li>If <var>parseResult</var> is a non-empty List of <b>SyntaxError</b> objects, throw a <b>SyntaxError</b> exception.</li><li>Assert: <var>parseResult</var> is a <emu-nt>Pattern</emu-nt> Parse Node.</li><li>Set <var>obj</var>.[[OriginalSource]] to <var>pattern</var>.</li><li>Set <var>obj</var>.[[OriginalFlags]] to <var>flags</var>.</li><li>Let <var>capturingGroupsCount</var> be CountLeftCapturingParensWithin(<var>parseResult</var>).</li><li>Let <var>regexpRecord</var> be the RegExp Record { [[IgnoreCase]]: <var>i</var>, [[Multiline]]: <var>m</var>, [[DotAll]]: <var>s</var>, [[Unicode]]: <var>u</var>, [[UnicodeSets]]: <var>v</var>, [[CapturingGroupsCount]]: <var>capturingGroupsCount</var> }.</li><li>Set <var>obj</var>.[[RegExpRecord]] to <var>regexpRecord</var>.</li><li>Set <var>obj</var>.[[RegExpMatcher]] to CompilePattern of <var>parseResult</var> with argument <var>regexpRecord</var>.</li><li>Perform ? <emu-meta suppress-effects=\"user-code\">Set(<var>obj</var>, <b>\"lastIndex\"</b>, <b>+0</b><sub>𝔽</sub>, <b>true</b>)</emu-meta>.</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","2.3.4":"<dl class=\"header\">\n        </dl>\n        <emu-note>\n          <p>This section is amended in <a href=\"/additional-ecmascript-features-for-web-browsers#sec-parsepattern-annexb\">B.1.2.9</a>.</p>\n        </emu-note>\n        <emu-alg><ol><li>If <var>v</var> is <b>true</b> and <var>u</var> is <b>true</b>, then<ol><li>Let <var>parseResult</var> be a List containing one or more <b>SyntaxError</b> objects.</li></ol></li><li>Else if <var>v</var> is <b>true</b>, then<ol><li>Let <var>parseResult</var> be ParseText(<var>patternText</var>, <emu-nt>Pattern[+UnicodeMode, +UnicodeSetsMode, +NamedCaptureGroups]</emu-nt>).</li></ol></li><li>Else if <var>u</var> is <b>true</b>, then<ol><li>Let <var>parseResult</var> be ParseText(<var>patternText</var>, <emu-nt>Pattern[+UnicodeMode, ~UnicodeSetsMode, +NamedCaptureGroups]</emu-nt>).</li></ol></li><li>Else,<ol><li>Let <var>parseResult</var> be ParseText(<var>patternText</var>, <emu-nt>Pattern[<emu-const>UnicodeMode, </emu-const>UnicodeSetsMode, +NamedCaptureGroups]</emu-nt>).</li></ol></li><li>Return <var>parseResult</var>.</li></ol></emu-alg>\n      ","2.4":"<p>The RegExp constructor:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%RegExp%</emu-intrinsic></dfn>.</li>\n        <li>is the initial value of the <b>\"RegExp\"</b> property of the global object.</li>\n        <li>creates and initializes a new RegExp object when called as a constructor.</li>\n        <li>when called as a function rather than as a constructor, returns either a new RegExp object, or the argument itself if the only argument is a RegExp object.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified RegExp behaviour must include a <code>super</code> call to the RegExp constructor to create and initialize subclass instances with the necessary internal slots.</li>\n      </ul>\n\n      ","2.4.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>patternIsRegExp</var> be ? IsRegExp(<var>patternOrRegexp</var>).</li><li>If NewTarget is <b>undefined</b>, then<ol><li>Let <var>newTarget</var> be the active function object.</li><li>If <var>patternIsRegExp</var> is <b>true</b> and <var>flags</var> is <b>undefined</b>, then<ol><li>Let <var>patternConstructor</var> be ? Get(<var>patternOrRegexp</var>, <b>\"constructor\"</b>).</li><li>If SameValue(<var>newTarget</var>, <var>patternConstructor</var>) is <b>true</b>, return <var>patternOrRegexp</var>.</li></ol></li></ol></li><li>Else,<ol><li>Let <var>newTarget</var> be NewTarget.</li></ol></li><li>If <var>patternOrRegexp</var> is an Object and <var>patternOrRegexp</var> has a [[RegExpMatcher]] internal slot, then<ol><li>Let <var>patternSource</var> be <var>patternOrRegexp</var>.[[OriginalSource]].</li><li>If <var>flags</var> is <b>undefined</b>, set <var>flags</var> to <var>patternOrRegexp</var>.[[OriginalFlags]].</li></ol></li><li>Else if <var>patternIsRegExp</var> is <b>true</b>, then<ol><li>Let <var>patternSource</var> be ? Get(<var>patternOrRegexp</var>, <b>\"source\"</b>).</li><li>If <var>flags</var> is <b>undefined</b>, then<ol><li>Set <var>flags</var> to ? Get(<var>patternOrRegexp</var>, <b>\"flags\"</b>).</li></ol></li></ol></li><li>Else,<ol><li>Let <var>patternSource</var> be <var>patternOrRegexp</var>.</li></ol></li><li>Let <var>obj</var> be ? RegExpAlloc(<var>newTarget</var>).</li><li>Return ? RegExpInitialize(<var>obj</var>, <var>patternSource</var>, <var>flags</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>If pattern is supplied using a <emu-nt>StringLiteral</emu-nt>, the usual escape sequence substitutions are performed before the String is processed by this function. If pattern must contain an escape sequence to be recognized by this function, any U+005C (REVERSE SOLIDUS) code points must be escaped within the <emu-nt>StringLiteral</emu-nt> to prevent them being removed when the contents of the <emu-nt>StringLiteral</emu-nt> are formed.</p>\n        </emu-note>\n      ","2.5":"<p>The RegExp constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Function.prototype%</emu-intrinsic>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","2.5.1":"<p>This function returns a copy of <var>str</var> in which characters that are potentially special in a regular expression <emu-nt>Pattern</emu-nt> have been replaced by equivalent escape sequences.</p>\n        <p>It performs the following steps when called:</p>\n\n        <emu-alg><ol><li>If <var>str</var> is not a String, throw a <b>TypeError</b> exception.</li><li>Let <var>escaped</var> be the empty String.</li><li>Let <var>cpList</var> be StringToCodePoints(<var>str</var>).</li><li>For each code point <var>cp</var> of <var>cpList</var>, do<ol><li>If <var>escaped</var> is the empty String and <var>cp</var> is matched by either <emu-nt>DecimalDigit</emu-nt> or <emu-nt>AsciiLetter</emu-nt>, then<ol><li>NOTE: Escaping a leading digit ensures that output corresponds with pattern text which may be used after a <code>\\0</code> character escape or a <emu-nt>DecimalEscape</emu-nt> such as <code>\\1</code> and still match <var>str</var> rather than be interpreted as an extension of the preceding escape sequence. Escaping a leading ASCII letter does the same for the context after <code>\\c</code>.</li><li>Let <var>numericValue</var> be the numeric value of <var>cp</var>.</li><li>Let <var>hex</var> be Number::toString(𝔽(<var>numericValue</var>), 16).</li><li>Assert: The length of <var>hex</var> is 2.</li><li>Set <var>escaped</var> to the string-concatenation of the code unit 0x005C (REVERSE SOLIDUS), <b>\"x\"</b>, and <var>hex</var>.</li></ol></li><li>Else,<ol><li>Set <var>escaped</var> to the string-concatenation of <var>escaped</var> and EncodeForRegExpEscape(<var>cp</var>).</li></ol></li></ol></li><li>Return <var>escaped</var>.</li></ol></emu-alg>\n\n        <emu-note>\n          <p>Despite having similar names, EscapeRegExpPattern and <code>RegExp.escape</code> do not perform similar actions. The former escapes a pattern for representation as a string, while this function escapes a string for representation inside a pattern.</p>\n        </emu-note>\n\n        ","2.5.1.1":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It returns a String representing a <emu-nt>Pattern</emu-nt> for matching <var>cp</var>. If <var>cp</var> is white space or an ASCII punctuator, the returned value is an escape sequence. Otherwise, the returned value is a String representation of <var>cp</var> itself.</dd>\n          </dl>\n\n          <emu-alg><ol><li>If <var>cp</var> is matched by <emu-nt>SyntaxCharacter</emu-nt> or <var>cp</var> is U+002F (SOLIDUS), then<ol><li>Return the string-concatenation of 0x005C (REVERSE SOLIDUS) and UTF16EncodeCodePoint(<var>cp</var>).</li></ol></li><li>If <var>cp</var> is a code point listed in the “Code Point” column of table-controlescape-code-point-values, then<ol><li>Return the string-concatenation of 0x005C (REVERSE SOLIDUS) and the string in the “ControlEscape” column of the row whose “Code Point” column contains <var>cp</var>.</li></ol></li><li>Let <var>otherPunctuators</var> be the string-concatenation of <b>\",-=&lt;>#&amp;!%:;@~'`\"</b> and the code unit 0x0022 (QUOTATION MARK).</li><li>Let <var>toEscape</var> be StringToCodePoints(<var>otherPunctuators</var>).</li><li>If <var>toEscape</var> contains <var>cp</var>, <var>cp</var> is matched by either <emu-nt>WhiteSpace</emu-nt> or <emu-nt>LineTerminator</emu-nt>, or <var>cp</var> has the same numeric value as a leading surrogate or trailing surrogate, then<ol><li>Let <var>cpNum</var> be the numeric value of <var>cp</var>.</li><li>If <var>cpNum</var> ≤ 0xFF, then<ol><li>Let <var>hex</var> be Number::toString(𝔽(<var>cpNum</var>), 16).</li><li>Return the string-concatenation of the code unit 0x005C (REVERSE SOLIDUS), <b>\"x\"</b>, and StringPad(<var>hex</var>, 2, <b>\"0\"</b>, <emu-const>start</emu-const>).</li></ol></li><li>Let <var>escaped</var> be the empty String.</li><li>Let <var>codeUnits</var> be UTF16EncodeCodePoint(<var>cp</var>).</li><li>For each code unit <var>cu</var> of <var>codeUnits</var>, do<ol><li>Set <var>escaped</var> to the string-concatenation of <var>escaped</var> and UnicodeEscape(<var>cu</var>).</li></ol></li><li>Return <var>escaped</var>.</li></ol></li><li>Return UTF16EncodeCodePoint(<var>cp</var>).</li></ol></emu-alg>\n        ","2.5.2":"<p>The initial value of <code>RegExp.prototype</code> is the RegExp prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","2.5.3":"<p><code>RegExp[%Symbol.species%]</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Return the <b>this</b> value.</li></ol></emu-alg>\n        <p>The value of the <b>\"name\"</b> property of this function is <b>\"get [Symbol.species]\"</b>.</p>\n        <emu-note>\n          <p>RegExp prototype methods normally use their <b>this</b> value's constructor to create a derived object. However, a subclass constructor may over-ride that default behaviour by redefining its <emu-intrinsic>%Symbol.species%</emu-intrinsic> property.</p>\n        </emu-note>\n      ","2.6":"<p>The <dfn>RegExp prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%RegExp.prototype%</emu-intrinsic></dfn>.</li>\n        <li>is an ordinary object.</li>\n        <li>is not a RegExp instance and does not have a [[RegExpMatcher]] internal slot or any of the other internal slots of RegExp instance objects.</li>\n        <li>has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Object.prototype%</emu-intrinsic>.</li>\n      </ul>\n      <emu-note>\n        <p>The RegExp prototype object does not have a <b>\"valueOf\"</b> property of its own; however, it inherits the <b>\"valueOf\"</b> property from the Object prototype object.</p>\n      </emu-note>\n\n      ","2.6.1":"<p>The initial value of <code>RegExp.prototype.constructor</code> is <emu-intrinsic>%RegExp%</emu-intrinsic>.</p>\n      ","2.6.2":"<p>This method searches <var>string</var> for an occurrence of the regular expression pattern and returns an Array containing the results of the match, or <b>null</b> if <var>string</var> did not match.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>regexp</var>, [[RegExpMatcher]]).</li><li>Let <var>str</var> be ? ToString(<var>string</var>).</li><li>Return ? RegExpBuiltinExec(<var>regexp</var>, <var>str</var>).</li></ol></emu-alg>\n      ","2.6.3":"<p><code>RegExp.prototype.dotAll</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>Let <var>cu</var> be the code unit 0x0073 (LATIN SMALL LETTER S).</li><li>Return ? RegExpHasFlag(<var>regexp</var>, <var>cu</var>).</li></ol></emu-alg>\n      ","2.6.4":"<p><code>RegExp.prototype.flags</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>If <var>regexp</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>codeUnits</var> be a new empty List.</li><li>Let <var>hasIndices</var> be ToBoolean(? Get(<var>regexp</var>, <b>\"hasIndices\"</b>)).</li><li>If <var>hasIndices</var> is <b>true</b>, append the code unit 0x0064 (LATIN SMALL LETTER D) to <var>codeUnits</var>.</li><li>Let <var>global</var> be ToBoolean(? Get(<var>regexp</var>, <b>\"global\"</b>)).</li><li>If <var>global</var> is <b>true</b>, append the code unit 0x0067 (LATIN SMALL LETTER G) to <var>codeUnits</var>.</li><li>Let <var>ignoreCase</var> be ToBoolean(? Get(<var>regexp</var>, <b>\"ignoreCase\"</b>)).</li><li>If <var>ignoreCase</var> is <b>true</b>, append the code unit 0x0069 (LATIN SMALL LETTER I) to <var>codeUnits</var>.</li><li>Let <var>multiline</var> be ToBoolean(? Get(<var>regexp</var>, <b>\"multiline\"</b>)).</li><li>If <var>multiline</var> is <b>true</b>, append the code unit 0x006D (LATIN SMALL LETTER M) to <var>codeUnits</var>.</li><li>Let <var>dotAll</var> be ToBoolean(? Get(<var>regexp</var>, <b>\"dotAll\"</b>)).</li><li>If <var>dotAll</var> is <b>true</b>, append the code unit 0x0073 (LATIN SMALL LETTER S) to <var>codeUnits</var>.</li><li>Let <var>unicode</var> be ToBoolean(? Get(<var>regexp</var>, <b>\"unicode\"</b>)).</li><li>If <var>unicode</var> is <b>true</b>, append the code unit 0x0075 (LATIN SMALL LETTER U) to <var>codeUnits</var>.</li><li>Let <var>unicodeSets</var> be ToBoolean(? Get(<var>regexp</var>, <b>\"unicodeSets\"</b>)).</li><li>If <var>unicodeSets</var> is <b>true</b>, append the code unit 0x0076 (LATIN SMALL LETTER V) to <var>codeUnits</var>.</li><li>Let <var>sticky</var> be ToBoolean(? Get(<var>regexp</var>, <b>\"sticky\"</b>)).</li><li>If <var>sticky</var> is <b>true</b>, append the code unit 0x0079 (LATIN SMALL LETTER Y) to <var>codeUnits</var>.</li><li>Return the String value whose code units are the elements of the List <var>codeUnits</var>. If <var>codeUnits</var> has no elements, the empty String is returned.</li></ol></emu-alg>\n\n        ","2.6.4.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>regexp</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>If <var>regexp</var> does not have an [[OriginalFlags]] internal slot, then<ol><li>If SameValue(<var>regexp</var>, <emu-intrinsic>%RegExp.prototype%</emu-intrinsic>) is <b>true</b>, return <b>undefined</b>.</li><li>Throw a <b>TypeError</b> exception.</li></ol></li><li>Let <var>flags</var> be <var>regexp</var>.[[OriginalFlags]].</li><li>If <var>flags</var> contains <var>codeUnit</var>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n        ","2.6.5":"<p><code>RegExp.prototype.global</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>Let <var>cu</var> be the code unit 0x0067 (LATIN SMALL LETTER G).</li><li>Return ? RegExpHasFlag(<var>regexp</var>, <var>cu</var>).</li></ol></emu-alg>\n      ","2.6.6":"<p><code>RegExp.prototype.hasIndices</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>Let <var>cu</var> be the code unit 0x0064 (LATIN SMALL LETTER D).</li><li>Return ? RegExpHasFlag(<var>regexp</var>, <var>cu</var>).</li></ol></emu-alg>\n      ","2.6.7":"<p><code>RegExp.prototype.ignoreCase</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>Let <var>cu</var> be the code unit 0x0069 (LATIN SMALL LETTER I).</li><li>Return ? RegExpHasFlag(<var>regexp</var>, <var>cu</var>).</li></ol></emu-alg>\n      ","2.6.8":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>If <var>regexp</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>str</var> be ? ToString(<var>string</var>).</li><li>Let <var>flags</var> be ? ToString(? Get(<var>regexp</var>, <b>\"flags\"</b>)).</li><li>If <var>flags</var> does not contain <b>\"g\"</b>, return ? RegExpExec(<var>regexp</var>, <var>str</var>).</li><li>If <var>flags</var> contains <b>\"u\"</b> or <var>flags</var> contains <b>\"v\"</b>, let <var>fullUnicode</var> be <b>true</b>; else let <var>fullUnicode</var> be <b>false</b>.</li><li>Perform ? Set(<var>regexp</var>, <b>\"lastIndex\"</b>, <b>+0</b><sub>𝔽</sub>, <b>true</b>).</li><li>Let <var>array</var> be ! ArrayCreate(0).</li><li>Let <var>matchCount</var> be 0.</li><li>Repeat,<ol><li>Let <var>result</var> be ? RegExpExec(<var>regexp</var>, <var>str</var>).</li><li>If <var>result</var> is <b>null</b>, then<ol><li>If <var>matchCount</var> = 0, return <b>null</b>.</li><li>Return <var>array</var>.</li></ol></li><li>Let <var>matchStr</var> be ? ToString(? Get(<var>result</var>, <b>\"0\"</b>)).</li><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, ! ToString(𝔽(<var>matchCount</var>)), <var>matchStr</var>).</li><li>If <var>matchStr</var> is the empty String, then<ol><li>Let <var>thisIndex</var> be ℝ(? ToLength(? Get(<var>regexp</var>, <b>\"lastIndex\"</b>))).</li><li>Let <var>nextIndex</var> be AdvanceStringIndex(<var>str</var>, <var>thisIndex</var>, <var>fullUnicode</var>).</li><li>Perform ? Set(<var>regexp</var>, <b>\"lastIndex\"</b>, 𝔽(<var>nextIndex</var>), <b>true</b>).</li></ol></li><li>Set <var>matchCount</var> to <var>matchCount</var> + 1.</li></ol></li></ol></emu-alg>\n        <p>The value of the <b>\"name\"</b> property of this method is <b>\"[Symbol.match]\"</b>.</p>\n        <emu-note>\n          <p>The <emu-intrinsic>%Symbol.match%</emu-intrinsic> property is used by the IsRegExp abstract operation to identify objects that have the basic behaviour of regular expressions. The absence of a <emu-intrinsic>%Symbol.match%</emu-intrinsic> property or the existence of such a property whose value does not Boolean coerce to <b>true</b> indicates that the object is not intended to be used as a regular expression object.</p>\n        </emu-note>\n      ","2.6.9":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>If <var>regexp</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>str</var> be ? ToString(<var>string</var>).</li><li>Let <var>speciesConstructor</var> be ? SpeciesConstructor(<var>regexp</var>, <emu-intrinsic>%RegExp%</emu-intrinsic>).</li><li>Let <var>flags</var> be ? ToString(? Get(<var>regexp</var>, <b>\"flags\"</b>)).</li><li>Let <var>matcher</var> be ? Construct(<var>speciesConstructor</var>, « <var>regexp</var>, <var>flags</var> »).</li><li>Let <var>lastIndex</var> be ? ToLength(? Get(<var>regexp</var>, <b>\"lastIndex\"</b>)).</li><li>Perform ? Set(<var>matcher</var>, <b>\"lastIndex\"</b>, <var>lastIndex</var>, <b>true</b>).</li><li>If <var>flags</var> contains <b>\"g\"</b>, let <var>global</var> be <b>true</b>.</li><li>Else, let <var>global</var> be <b>false</b>.</li><li>If <var>flags</var> contains <b>\"u\"</b> or <var>flags</var> contains <b>\"v\"</b>, let <var>fullUnicode</var> be <b>true</b>.</li><li>Else, let <var>fullUnicode</var> be <b>false</b>.</li><li>Return CreateRegExpStringIterator(<var>matcher</var>, <var>str</var>, <var>global</var>, <var>fullUnicode</var>).</li></ol></emu-alg>\n        <p>The value of the <b>\"name\"</b> property of this method is <b>\"[Symbol.matchAll]\"</b>.</p>\n      ","2.6.10":"<p><code>RegExp.prototype.multiline</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>Let <var>cu</var> be the code unit 0x006D (LATIN SMALL LETTER M).</li><li>Return ? RegExpHasFlag(<var>regexp</var>, <var>cu</var>).</li></ol></emu-alg>\n      ","2.6.11":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>If <var>regexp</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>str</var> be ? ToString(<var>string</var>).</li><li>Let <var>lengthS</var> be the length of <var>str</var>.</li><li>Let <var>functionalReplace</var> be IsCallable(<var>replaceValue</var>).</li><li>If <var>functionalReplace</var> is <b>false</b>, then<ol><li>Set <var>replaceValue</var> to ? ToString(<var>replaceValue</var>).</li></ol></li><li>Let <var>flags</var> be ? ToString(? Get(<var>regexp</var>, <b>\"flags\"</b>)).</li><li>If <var>flags</var> contains <b>\"g\"</b>, let <var>global</var> be <b>true</b>; else let <var>global</var> be <b>false</b>.</li><li>If <var>global</var> is <b>true</b>, then<ol><li>Perform ? Set(<var>regexp</var>, <b>\"lastIndex\"</b>, <b>+0</b><sub>𝔽</sub>, <b>true</b>).</li></ol></li><li>Let <var>results</var> be a new empty List.</li><li>Let <var>done</var> be <b>false</b>.</li><li>Repeat, while <var>done</var> is <b>false</b>,<ol><li>Let <var>result</var> be ? RegExpExec(<var>regexp</var>, <var>str</var>).</li><li>If <var>result</var> is <b>null</b>, then<ol><li>Set <var>done</var> to <b>true</b>.</li></ol></li><li>Else,<ol><li>Append <var>result</var> to <var>results</var>.</li><li>If <var>global</var> is <b>false</b>, then<ol><li>Set <var>done</var> to <b>true</b>.</li></ol></li><li>Else,<ol><li>Let <var>matchStr</var> be ? ToString(? Get(<var>result</var>, <b>\"0\"</b>)).</li><li>If <var>matchStr</var> is the empty String, then<ol><li>Let <var>thisIndex</var> be ℝ(? ToLength(? Get(<var>regexp</var>, <b>\"lastIndex\"</b>))).</li><li>If <var>flags</var> contains <b>\"u\"</b> or <var>flags</var> contains <b>\"v\"</b>, let <var>fullUnicode</var> be <b>true</b>; else let <var>fullUnicode</var> be <b>false</b>.</li><li>Let <var>nextIndex</var> be AdvanceStringIndex(<var>str</var>, <var>thisIndex</var>, <var>fullUnicode</var>).</li><li>Perform ? Set(<var>regexp</var>, <b>\"lastIndex\"</b>, 𝔽(<var>nextIndex</var>), <b>true</b>).</li></ol></li></ol></li></ol></li></ol></li><li>Let <var>accumulatedResult</var> be the empty String.</li><li>Let <var>nextSourcePosition</var> be 0.</li><li>For each element <var>result</var> of <var>results</var>, do<ol><li>Let <var>resultLength</var> be ? LengthOfArrayLike(<var>result</var>).</li><li>Let <var>nCaptures</var> be max(<var>resultLength</var> - 1, 0).</li><li>Let <var>matched</var> be ? ToString(? Get(<var>result</var>, <b>\"0\"</b>)).</li><li>Let <var>matchLength</var> be the length of <var>matched</var>.</li><li>Let <var>position</var> be ? ToIntegerOrInfinity(? Get(<var>result</var>, <b>\"index\"</b>)).</li><li>Set <var>position</var> to the result of clamping <var>position</var> between 0 and <var>lengthS</var>.</li><li>Let <var>captures</var> be a new empty List.</li><li>Let <var>captureNumber</var> be 1.</li><li>Repeat, while <var>captureNumber</var> ≤ <var>nCaptures</var>,<ol><li>Let <var>capN</var> be ? Get(<var>result</var>, ! ToString(𝔽(<var>captureNumber</var>))).</li><li>If <var>capN</var> is not <b>undefined</b>, then<ol><li>Set <var>capN</var> to ? ToString(<var>capN</var>).</li></ol></li><li>Append <var>capN</var> to <var>captures</var>.</li><li>NOTE: When <var>captureNumber</var> = 1, the preceding step puts the first element into <var>captures</var> (at index 0). More generally, the <var>captureNumber</var><sup>th</sup> capture (the characters captured by the <var>captureNumber</var><sup>th</sup> set of capturing parentheses) is at <var>captures</var>[<var>captureNumber</var> - 1].</li><li>Set <var>captureNumber</var> to <var>captureNumber</var> + 1.</li></ol></li><li>Let <var>namedCaptures</var> be ? Get(<var>result</var>, <b>\"groups\"</b>).</li><li>If <var>functionalReplace</var> is <b>true</b>, then<ol><li>Let <var>replacerArgs</var> be the list-concatenation of « <var>matched</var> », <var>captures</var>, and « 𝔽(<var>position</var>), <var>str</var> ».</li><li>If <var>namedCaptures</var> is not <b>undefined</b>, then<ol><li>Append <var>namedCaptures</var> to <var>replacerArgs</var>.</li></ol></li><li>Let <var>replacementValue</var> be ? Call(<var>replaceValue</var>, <b>undefined</b>, <var>replacerArgs</var>).</li><li>Let <var>replacementString</var> be ? ToString(<var>replacementValue</var>).</li></ol></li><li>Else,<ol><li>If <var>namedCaptures</var> is not <b>undefined</b>, then<ol><li>Set <var>namedCaptures</var> to ? ToObject(<var>namedCaptures</var>).</li></ol></li><li>Let <var>replacementString</var> be ? GetSubstitution(<var>matched</var>, <var>str</var>, <var>position</var>, <var>captures</var>, <var>namedCaptures</var>, <var>replaceValue</var>).</li></ol></li><li>If <var>position</var> ≥ <var>nextSourcePosition</var>, then<ol><li>NOTE: <var>position</var> should not normally move backwards. If it does, it is an indication of an ill-behaving RegExp subclass or use of an access triggered side-effect to change the global flag or other characteristics of <var>regexp</var>. In such cases, the corresponding substitution is ignored.</li><li>Set <var>accumulatedResult</var> to the string-concatenation of <var>accumulatedResult</var>, the substring of <var>str</var> from <var>nextSourcePosition</var> to <var>position</var>, and <var>replacementString</var>.</li><li>Set <var>nextSourcePosition</var> to <var>position</var> + <var>matchLength</var>.</li></ol></li></ol></li><li>If <var>nextSourcePosition</var> ≥ <var>lengthS</var>, return <var>accumulatedResult</var>.</li><li>Return the string-concatenation of <var>accumulatedResult</var> and the substring of <var>str</var> from <var>nextSourcePosition</var>.</li></ol></emu-alg>\n        <p>The value of the <b>\"name\"</b> property of this method is <b>\"[Symbol.replace]\"</b>.</p>\n      ","2.6.12":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>If <var>regexp</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>str</var> be ? ToString(<var>string</var>).</li><li>Let <var>previousLastIndex</var> be ? Get(<var>regexp</var>, <b>\"lastIndex\"</b>).</li><li>If <var>previousLastIndex</var> is not <b>+0</b><sub>𝔽</sub>, then<ol><li>Perform ? Set(<var>regexp</var>, <b>\"lastIndex\"</b>, <b>+0</b><sub>𝔽</sub>, <b>true</b>).</li></ol></li><li>Let <var>result</var> be ? RegExpExec(<var>regexp</var>, <var>str</var>).</li><li>Let <var>currentLastIndex</var> be ? Get(<var>regexp</var>, <b>\"lastIndex\"</b>).</li><li>If SameValue(<var>currentLastIndex</var>, <var>previousLastIndex</var>) is <b>false</b>, then<ol><li>Perform ? Set(<var>regexp</var>, <b>\"lastIndex\"</b>, <var>previousLastIndex</var>, <b>true</b>).</li></ol></li><li>If <var>result</var> is <b>null</b>, return <b>-1</b><sub>𝔽</sub>.</li><li>Return ? Get(<var>result</var>, <b>\"index\"</b>).</li></ol></emu-alg>\n        <p>The value of the <b>\"name\"</b> property of this method is <b>\"[Symbol.search]\"</b>.</p>\n        <emu-note>\n          <p>The <b>\"lastIndex\"</b> and <b>\"global\"</b> properties of this RegExp object are ignored when performing the search. The <b>\"lastIndex\"</b> property is left unchanged.</p>\n        </emu-note>\n      ","2.6.13":"<p><code>RegExp.prototype.source</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>If <var>regexp</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>If <var>regexp</var> does not have an [[OriginalSource]] internal slot, then<ol><li>If SameValue(<var>regexp</var>, <emu-intrinsic>%RegExp.prototype%</emu-intrinsic>) is <b>true</b>, return <b>\"(?:)\"</b>.</li><li>Throw a <b>TypeError</b> exception.</li></ol></li><li>Assert: <var>regexp</var> has an [[OriginalFlags]] internal slot.</li><li>Let <var>src</var> be <var>regexp</var>.[[OriginalSource]].</li><li>Let <var>flags</var> be <var>regexp</var>.[[OriginalFlags]].</li><li>Return EscapeRegExpPattern(<var>src</var>, <var>flags</var>).</li></ol></emu-alg>\n\n        ","2.6.13.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>flags</var> contains <b>\"v\"</b>, then<ol><li>Let <var>patternSymbol</var> be <emu-nt>Pattern[+UnicodeMode, +UnicodeSetsMode]</emu-nt>.</li></ol></li><li>Else if <var>flags</var> contains <b>\"u\"</b>, then<ol><li>Let <var>patternSymbol</var> be <emu-nt>Pattern[+UnicodeMode, ~UnicodeSetsMode]</emu-nt>.</li></ol></li><li>Else,<ol><li>Let <var>patternSymbol</var> be <emu-nt>Pattern[<emu-const>UnicodeMode, </emu-const>UnicodeSetsMode]</emu-nt>.</li></ol></li><li>Let <var>escapedPattern</var> be a String in the form of a <var>patternSymbol</var> equivalent to <var>pattern</var> interpreted as UTF-16 encoded Unicode code points (<a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>), in which certain code points are escaped as described below. <var>escapedPattern</var> may or may not differ from <var>pattern</var>; however, the Abstract Closure that would result from evaluating <var>escapedPattern</var> as a <var>patternSymbol</var> must behave identically to the Abstract Closure given by the constructed object's [[RegExpMatcher]] internal slot. Multiple calls to this abstract operation using the same values for <var>pattern</var> and <var>flags</var> must produce identical results.</li><li>The code points <code>/</code> or any <emu-nt>LineTerminator</emu-nt> occurring in the pattern shall be escaped in <var>escapedPattern</var> as necessary to ensure that the string-concatenation of <b>\"/\"</b>, <var>escapedPattern</var>, <b>\"/\"</b>, and <var>flags</var> can be parsed (in an appropriate lexical context) as a <emu-nt>RegularExpressionLiteral</emu-nt> that behaves identically to the constructed regular expression. For example, if <var>pattern</var> is <b>\"/\"</b>, then <var>escapedPattern</var> could be <b>\"\\\\/\"</b> or <b>\"\\\\u002F\"</b>, among other possibilities, but not <b>\"/\"</b>, because <code>///</code> followed by <var>flags</var> would be parsed as a <emu-nt>SingleLineComment</emu-nt> rather than a <emu-nt>RegularExpressionLiteral</emu-nt>. If <var>pattern</var> is the empty String, this specification can be met by letting <var>escapedPattern</var> be <b>\"(?:)\"</b>.</li><li>Return <var>escapedPattern</var>.</li></ol></emu-alg>\n\n          <emu-note>\n            <p>Despite having similar names, <code>RegExp.escape</code> and EscapeRegExpPattern do not perform similar actions. The former escapes a string for representation inside a pattern, while this function escapes a pattern for representation as a string.</p>\n          </emu-note>\n        ","2.6.14":"<emu-note>\n          <p>This method returns an Array into which substrings of the result of converting <var>string</var> to a String have been stored. The substrings are determined by searching from left to right for matches of the <b>this</b> value regular expression; these occurrences are not part of any String in the returned array, but serve to divide up the String value.</p>\n          <p>The <b>this</b> value may be an empty regular expression or a regular expression that can match an empty String. In this case, the regular expression does not match the empty <emu-not-ref>substring</emu-not-ref> at the beginning or end of the input String, nor does it match the empty <emu-not-ref>substring</emu-not-ref> at the end of the previous separator match. (For example, if the regular expression matches the empty String, the String is split up into individual code unit elements; the length of the result array equals the length of the String, and each <emu-not-ref>substring</emu-not-ref> contains one code unit.) Only the first match at a given index of the String is considered, even if backtracking could yield a non-empty <emu-not-ref>substring</emu-not-ref> match at that index. (For example, <code>/a*?/[Symbol.split](\"ab\")</code> evaluates to the array <code>[\"a\", \"b\"]</code>, while <code>/a*/[Symbol.split](\"ab\")</code> evaluates to the array <code>[\"\",\"b\"]</code>.)</p>\n          <p>If <var>string</var> is (or converts to) the empty String, the result depends on whether the regular expression can match the empty String. If it can, the result array contains no elements. Otherwise, the result array contains one element, which is the empty String.</p>\n          <p>If the regular expression contains capturing parentheses, then each time <var>separator</var> is matched the results (including any <b>undefined</b> results) of the capturing parentheses are spliced into the output array. For example,</p>\n          <pre><code class=\"javascript\">/&lt;(\\/)?([^&lt;&gt;]+)&gt;/[Symbol.split](\"A&lt;B&gt;bold&lt;/B&gt;and&lt;CODE&gt;coded&lt;/CODE&gt;\")</code></pre>\n          <p>evaluates to the array</p>\n          <pre><code class=\"javascript\">[\"A\", undefined, \"B\", \"bold\", \"/\", \"B\", \"and\", undefined, \"CODE\", \"coded\", \"/\", \"CODE\", \"\"]</code></pre>\n          <p>If <var>limit</var> is not <b>undefined</b>, then the output array is truncated so that it contains no more than <var>limit</var> elements.</p>\n        </emu-note>\n        <p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>If <var>regexp</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>str</var> be ? ToString(<var>string</var>).</li><li>Let <var>speciesConstructor</var> be ? SpeciesConstructor(<var>regexp</var>, <emu-intrinsic>%RegExp%</emu-intrinsic>).</li><li>Let <var>flags</var> be ? ToString(? Get(<var>regexp</var>, <b>\"flags\"</b>)).</li><li>If <var>flags</var> contains <b>\"u\"</b> or <var>flags</var> contains <b>\"v\"</b>, let <var>unicodeMatching</var> be <b>true</b>.</li><li>Else, let <var>unicodeMatching</var> be <b>false</b>.</li><li>If <var>flags</var> contains <b>\"y\"</b>, let <var>newFlags</var> be <var>flags</var>.</li><li>Else, let <var>newFlags</var> be the string-concatenation of <var>flags</var> and <b>\"y\"</b>.</li><li>Let <var>splitter</var> be ? Construct(<var>speciesConstructor</var>, « <var>regexp</var>, <var>newFlags</var> »).</li><li>Let <var>array</var> be ! ArrayCreate(0).</li><li>Let <var>lengthA</var> be 0.</li><li>If <var>limit</var> is <b>undefined</b>, let <var>lim</var> be 2<sup>32</sup> - 1; else let <var>lim</var> be ℝ(? ToUint32(<var>limit</var>)).</li><li>If <var>lim</var> = 0, return <var>array</var>.</li><li>If <var>str</var> is the empty String, then<ol><li>Let <var>matchResult</var> be ? RegExpExec(<var>splitter</var>, <var>str</var>).</li><li>If <var>matchResult</var> is not <b>null</b>, return <var>array</var>.</li><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, <b>\"0\"</b>, <var>str</var>).</li><li>Return <var>array</var>.</li></ol></li><li>Let <var>size</var> be the length of <var>str</var>.</li><li>Let <var>lastMatchEnd</var> be 0.</li><li>Let <var>searchIndex</var> be <var>lastMatchEnd</var>.</li><li>Repeat, while <var>searchIndex</var> &lt; <var>size</var>,<ol><li>Perform ? Set(<var>splitter</var>, <b>\"lastIndex\"</b>, 𝔽(<var>searchIndex</var>), <b>true</b>).</li><li>Let <var>matchResult</var> be ? RegExpExec(<var>splitter</var>, <var>str</var>).</li><li>If <var>matchResult</var> is <b>null</b>, then<ol><li>Set <var>searchIndex</var> to AdvanceStringIndex(<var>str</var>, <var>searchIndex</var>, <var>unicodeMatching</var>).</li></ol></li><li>Else,<ol><li>Let <var>matchEnd</var> be ℝ(? ToLength(? Get(<var>splitter</var>, <b>\"lastIndex\"</b>))).</li><li>Set <var>matchEnd</var> to min(<var>matchEnd</var>, <var>size</var>).</li><li>If <var>matchEnd</var> = <var>lastMatchEnd</var>, then<ol><li>Set <var>searchIndex</var> to AdvanceStringIndex(<var>str</var>, <var>searchIndex</var>, <var>unicodeMatching</var>).</li></ol></li><li>Else,<ol><li>Let <var>substring</var> be the substring of <var>str</var> from <var>lastMatchEnd</var> to <var>searchIndex</var>.</li><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, ! ToString(𝔽(<var>lengthA</var>)), <var>substring</var>).</li><li>Set <var>lengthA</var> to <var>lengthA</var> + 1.</li><li>If <var>lengthA</var> = <var>lim</var>, return <var>array</var>.</li><li>Set <var>lastMatchEnd</var> to <var>matchEnd</var>.</li><li>Let <var>numberOfCaptures</var> be ? LengthOfArrayLike(<var>matchResult</var>).</li><li>Set <var>numberOfCaptures</var> to max(<var>numberOfCaptures</var> - 1, 0).</li><li>Let <var>captureIndex</var> be 1.</li><li>Repeat, while <var>captureIndex</var> ≤ <var>numberOfCaptures</var>,<ol><li>Let <var>nextCapture</var> be ? Get(<var>matchResult</var>, ! ToString(𝔽(<var>captureIndex</var>))).</li><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, ! ToString(𝔽(<var>lengthA</var>)), <var>nextCapture</var>).</li><li>Set <var>captureIndex</var> to <var>captureIndex</var> + 1.</li><li>Set <var>lengthA</var> to <var>lengthA</var> + 1.</li><li>If <var>lengthA</var> = <var>lim</var>, return <var>array</var>.</li></ol></li><li>Set <var>searchIndex</var> to <var>lastMatchEnd</var>.</li></ol></li></ol></li></ol></li><li>Let <var>substring</var> be the substring of <var>str</var> from <var>lastMatchEnd</var> to <var>size</var>.</li><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, ! ToString(𝔽(<var>lengthA</var>)), <var>substring</var>).</li><li>Return <var>array</var>.</li></ol></emu-alg>\n        <p>The value of the <b>\"name\"</b> property of this method is <b>\"[Symbol.split]\"</b>.</p>\n        <emu-note>\n          <p>This method ignores the value of the <b>\"global\"</b> and <b>\"sticky\"</b> properties of this RegExp object.</p>\n        </emu-note>\n      ","2.6.15":"<p><code>RegExp.prototype.sticky</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>Let <var>cu</var> be the code unit 0x0079 (LATIN SMALL LETTER Y).</li><li>Return ? RegExpHasFlag(<var>regexp</var>, <var>cu</var>).</li></ol></emu-alg>\n      ","2.6.16":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>If <var>regexp</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>string</var> be ? ToString(<var>str</var>).</li><li>Let <var>match</var> be ? RegExpExec(<var>regexp</var>, <var>string</var>).</li><li>If <var>match</var> is <b>null</b>, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n      ","2.6.17":"<emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>If <var>regexp</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>pattern</var> be ? ToString(? Get(<var>regexp</var>, <b>\"source\"</b>)).</li><li>Let <var>flags</var> be ? ToString(? Get(<var>regexp</var>, <b>\"flags\"</b>)).</li><li>Let <var>result</var> be the string-concatenation of <b>\"/\"</b>, <var>pattern</var>, <b>\"/\"</b>, and <var>flags</var>.</li><li>Return <var>result</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The returned String has the form of a <emu-nt>RegularExpressionLiteral</emu-nt> that evaluates to another RegExp object with the same behaviour as this object.</p>\n        </emu-note>\n      ","2.6.18":"<p><code>RegExp.prototype.unicode</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>Let <var>cu</var> be the code unit 0x0075 (LATIN SMALL LETTER U).</li><li>Return ? RegExpHasFlag(<var>regexp</var>, <var>cu</var>).</li></ol></emu-alg>\n      ","2.6.19":"<p><code>RegExp.prototype.unicodeSets</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>regexp</var> be the <b>this</b> value.</li><li>Let <var>cu</var> be the code unit 0x0076 (LATIN SMALL LETTER V).</li><li>Return ? RegExpHasFlag(<var>regexp</var>, <var>cu</var>).</li></ol></emu-alg>\n      ","2.7":"","2.7.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>exec</var> be ? Get(<var>regexp</var>, <b>\"exec\"</b>).</li><li>If IsCallable(<var>exec</var>) is <b>true</b>, then<ol><li>Let <var>result</var> be ? Call(<var>exec</var>, <var>regexp</var>, « <var>str</var> »).</li><li>If <var>result</var> is not an Object and <var>result</var> is not <b>null</b>, throw a <b>TypeError</b> exception.</li><li>Return <var>result</var>.</li></ol></li><li>Perform ? RequireInternalSlot(<var>regexp</var>, [[RegExpMatcher]]).</li><li>Return ? RegExpBuiltinExec(<var>regexp</var>, <var>str</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>If a callable <b>\"exec\"</b> property is not found this algorithm falls back to attempting to use the built-in RegExp matching algorithm. This provides compatible behaviour for code written for prior editions where most built-in algorithms that use regular expressions did not perform a dynamic property lookup of <b>\"exec\"</b>.</p>\n        </emu-note>\n      ","2.7.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>length</var> be the length of <var>str</var>.</li><li>Let <var>lastIndex</var> be ℝ(? ToLength(! Get(<var>regexp</var>, <b>\"lastIndex\"</b>))).</li><li>Let <var>flags</var> be <var>regexp</var>.[[OriginalFlags]].</li><li>If <var>flags</var> contains <b>\"g\"</b>, let <var>global</var> be <b>true</b>; else let <var>global</var> be <b>false</b>.</li><li>If <var>flags</var> contains <b>\"y\"</b>, let <var>sticky</var> be <b>true</b>; else let <var>sticky</var> be <b>false</b>.</li><li>If <var>flags</var> contains <b>\"d\"</b>, let <var>hasIndices</var> be <b>true</b>; else let <var>hasIndices</var> be <b>false</b>.</li><li>If <var>global</var> is <b>false</b> and <var>sticky</var> is <b>false</b>, set <var>lastIndex</var> to 0.</li><li>Let <var>matcher</var> be <var>regexp</var>.[[RegExpMatcher]].</li><li>If <var>flags</var> contains <b>\"u\"</b> or <var>flags</var> contains <b>\"v\"</b>, let <var>fullUnicode</var> be <b>true</b>; else let <var>fullUnicode</var> be <b>false</b>.</li><li>Let <var>matchSucceeded</var> be <b>false</b>.</li><li>If <var>fullUnicode</var> is <b>true</b>, let <var>input</var> be StringToCodePoints(<var>str</var>); else let <var>input</var> be a List whose elements are the code units that are the elements of <var>str</var>.</li><li>NOTE: Each element of <var>input</var> is considered to be a character.</li><li>Repeat, while <var>matchSucceeded</var> is <b>false</b>,<ol><li>If <var>lastIndex</var> > <var>length</var>, then<ol><li>If <var>global</var> is <b>true</b> or <var>sticky</var> is <b>true</b>, then<ol><li>Perform ? <emu-meta suppress-effects=\"user-code\">Set(<var>regexp</var>, <b>\"lastIndex\"</b>, <b>+0</b><sub>𝔽</sub>, <b>true</b>)</emu-meta>.</li></ol></li><li>Return <b>null</b>.</li></ol></li><li>Let <var>inputIndex</var> be the index into <var>input</var> of the character that was obtained from element <var>lastIndex</var> of <var>str</var>.</li><li>Let <var>r</var> be <var>matcher</var>(<var>input</var>, <var>inputIndex</var>).</li><li>If <var>r</var> is <emu-const>failure</emu-const>, then<ol><li>If <var>sticky</var> is <b>true</b>, then<ol><li>Perform ? <emu-meta suppress-effects=\"user-code\">Set(<var>regexp</var>, <b>\"lastIndex\"</b>, <b>+0</b><sub>𝔽</sub>, <b>true</b>)</emu-meta>.</li><li>Return <b>null</b>.</li></ol></li><li>Set <var>lastIndex</var> to AdvanceStringIndex(<var>str</var>, <var>lastIndex</var>, <var>fullUnicode</var>).</li></ol></li><li>Else,<ol><li>Assert: <var>r</var> is a MatchState.</li><li>Set <var>matchSucceeded</var> to <b>true</b>.</li></ol></li></ol></li><li>Let <var>endIndex</var> be <var>r</var>.[[EndIndex]].</li><li>If <var>fullUnicode</var> is <b>true</b>, set <var>endIndex</var> to GetStringIndex(<var>str</var>, <var>endIndex</var>).</li><li>If <var>global</var> is <b>true</b> or <var>sticky</var> is <b>true</b>, then<ol><li>Perform ? <emu-meta suppress-effects=\"user-code\">Set(<var>regexp</var>, <b>\"lastIndex\"</b>, 𝔽(<var>endIndex</var>), <b>true</b>)</emu-meta>.</li></ol></li><li>Let <var>n</var> be the number of elements in <var>r</var>.[[Captures]].</li><li>Assert: <var>n</var> = <var>regexp</var>.[[RegExpRecord]].[[CapturingGroupsCount]].</li><li>Assert: <var>n</var> &lt; 2<sup>32</sup> - 1.</li><li>Let <var>array</var> be ! ArrayCreate(<var>n</var> + 1).</li><li>Assert: The mathematical value of <var>array</var>'s <b>\"length\"</b> property is <var>n</var> + 1.</li><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, <b>\"index\"</b>, 𝔽(<var>lastIndex</var>)).</li><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, <b>\"input\"</b>, <var>str</var>).</li><li>Let <var>match</var> be the Match Record { [[StartIndex]]: <var>lastIndex</var>, [[EndIndex]]: <var>endIndex</var> }.</li><li>Let <var>indices</var> be a new empty List.</li><li>Let <var>groupNames</var> be a new empty List.</li><li>Append <var>match</var> to <var>indices</var>.</li><li>Let <var>matchedSubstr</var> be GetMatchString(<var>str</var>, <var>match</var>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, <b>\"0\"</b>, <var>matchedSubstr</var>).</li><li>If <var>regexp</var> contains any <emu-nt>GroupName</emu-nt>, then<ol><li>Let <var>groups</var> be OrdinaryObjectCreate(<b>null</b>).</li><li>Let <var>hasGroups</var> be <b>true</b>.</li></ol></li><li>Else,<ol><li>Let <var>groups</var> be <b>undefined</b>.</li><li>Let <var>hasGroups</var> be <b>false</b>.</li></ol></li><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, <b>\"groups\"</b>, <var>groups</var>).</li><li>Let <var>matchedGroupNames</var> be a new empty List.</li><li>For each integer <var>i</var> such that 1 ≤ <var>i</var> ≤ <var>n</var>, in ascending order, do<ol><li>Let <var>captureI</var> be <var>i</var><sup>th</sup> element of <var>r</var>.[[Captures]].</li><li>If <var>captureI</var> is <b>undefined</b>, then<ol><li>Let <var>capturedValue</var> be <b>undefined</b>.</li><li>Append <b>undefined</b> to <var>indices</var>.</li></ol></li><li>Else,<ol><li>Let <var>captureStart</var> be <var>captureI</var>.[[StartIndex]].</li><li>Let <var>captureEnd</var> be <var>captureI</var>.[[EndIndex]].</li><li>If <var>fullUnicode</var> is <b>true</b>, then<ol><li>Set <var>captureStart</var> to GetStringIndex(<var>str</var>, <var>captureStart</var>).</li><li>Set <var>captureEnd</var> to GetStringIndex(<var>str</var>, <var>captureEnd</var>).</li></ol></li><li>Let <var>capture</var> be the Match Record { [[StartIndex]]: <var>captureStart</var>, [[EndIndex]]: <var>captureEnd</var> }.</li><li>Let <var>capturedValue</var> be GetMatchString(<var>str</var>, <var>capture</var>).</li><li>Append <var>capture</var> to <var>indices</var>.</li></ol></li><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, ! ToString(𝔽(<var>i</var>)), <var>capturedValue</var>).</li><li>If the <var>i</var><sup>th</sup> capture of <var>regexp</var> was defined with a <emu-nt>GroupName</emu-nt>, then<ol><li>Let <var>s</var> be the CapturingGroupName of that <emu-nt>GroupName</emu-nt>.</li><li>If <var>matchedGroupNames</var> contains <var>s</var>, then<ol><li>Assert: <var>capturedValue</var> is <b>undefined</b>.</li><li>Append <b>undefined</b> to <var>groupNames</var>.</li></ol></li><li>Else,<ol><li>If <var>capturedValue</var> is not <b>undefined</b>, append <var>s</var> to <var>matchedGroupNames</var>.</li><li>NOTE: If there are multiple groups named <var>s</var>, <var>groups</var> may already have an <var>s</var> property at this point. However, because <var>groups</var> is an ordinary object whose properties are all writable data properties, the call to CreateDataPropertyOrThrow is nevertheless guaranteed to succeed.</li><li>Perform ! CreateDataPropertyOrThrow(<var>groups</var>, <var>s</var>, <var>capturedValue</var>).</li><li>Append <var>s</var> to <var>groupNames</var>.</li></ol></li></ol></li><li>Else,<ol><li>Append <b>undefined</b> to <var>groupNames</var>.</li></ol></li></ol></li><li>If <var>hasIndices</var> is <b>true</b>, then<ol><li>Let <var>indicesArray</var> be MakeMatchIndicesIndexPairArray(<var>str</var>, <var>indices</var>, <var>groupNames</var>, <var>hasGroups</var>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, <b>\"indices\"</b>, <var>indicesArray</var>).</li></ol></li><li>Return <var>array</var>.</li></ol></emu-alg>\n      ","2.7.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: <var>index</var> ≤ 2<sup>53</sup> - 1.</li><li>If <var>unicode</var> is <b>false</b>, return <var>index</var> + 1.</li><li>Let <var>length</var> be the length of <var>str</var>.</li><li>If <var>index</var> + 1 ≥ <var>length</var>, return <var>index</var> + 1.</li><li>Let <var>cp</var> be CodePointAt(<var>str</var>, <var>index</var>).</li><li>Return <var>index</var> + <var>cp</var>.[[CodeUnitCount]].</li></ol></emu-alg>\n      ","2.7.4":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It interprets <var>str</var> as a sequence of UTF-16 encoded code points, as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>, and returns the code unit index corresponding to code point index <var>codePointIndex</var> when such an index exists. Otherwise, it returns the length of <var>str</var>.</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>str</var> is the empty String, return 0.</li><li>Let <var>len</var> be the length of <var>str</var>.</li><li>Let <var>codeUnitCount</var> be 0.</li><li>Let <var>codePointCount</var> be 0.</li><li>Repeat, while <var>codeUnitCount</var> &lt; <var>len</var>,<ol><li>If <var>codePointCount</var> = <var>codePointIndex</var>, return <var>codeUnitCount</var>.</li><li>Let <var>cp</var> be CodePointAt(<var>str</var>, <var>codeUnitCount</var>).</li><li>Set <var>codeUnitCount</var> to <var>codeUnitCount</var> + <var>cp</var>.[[CodeUnitCount]].</li><li>Set <var>codePointCount</var> to <var>codePointCount</var> + 1.</li></ol></li><li>Return <var>len</var>.</li></ol></emu-alg>\n      ","2.7.5":"<p>A <dfn variants=\"Match Records\">Match Record</dfn> is a Record value used to encapsulate the start and end indices of a regular expression match or capture.</p>\n        <p>Match Records have the fields listed in table-match-record.</p>\n        <emu-table id=\"table-match-record\" caption=\"Match Record Fields\">\n          <table>\n            <thead>\n              <tr>\n                <th>Field Name</th>\n                <th>Value</th>\n                <th>Meaning</th>\n              </tr>\n            </thead>\n            <tr>\n              <td>[[StartIndex]]</td>\n              <td>a non-negative integer</td>\n              <td>The number of code units from the start of a string at which the match begins (inclusive).</td>\n            </tr>\n            <tr>\n              <td>[[EndIndex]]</td>\n              <td>an integer ≥ [[StartIndex]]</td>\n              <td>The number of code units from the start of a string at which the match ends (exclusive).</td>\n            </tr>\n          </table>\n        </emu-table>\n      ","2.7.6":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: <var>match</var>.[[StartIndex]] ≤ <var>match</var>.[[EndIndex]] ≤ the length of <var>str</var>.</li><li>Return the substring of <var>str</var> from <var>match</var>.[[StartIndex]] to <var>match</var>.[[EndIndex]].</li></ol></emu-alg>\n      ","2.7.7":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: <var>match</var>.[[StartIndex]] ≤ <var>match</var>.[[EndIndex]] ≤ the length of <var>str</var>.</li><li>Return CreateArrayFromList(« 𝔽(<var>match</var>.[[StartIndex]]), 𝔽(<var>match</var>.[[EndIndex]]) »).</li></ol></emu-alg>\n      ","2.7.8":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>n</var> be the number of elements in <var>indices</var>.</li><li>Assert: <var>n</var> &lt; 2<sup>32</sup> - 1.</li><li>Assert: <var>groupNames</var> has <var>n</var> - 1 elements.</li><li>NOTE: The <var>groupNames</var> List contains elements aligned with the <var>indices</var> List starting at <var>indices</var>[1].</li><li>Let <var>array</var> be ! ArrayCreate(<var>n</var>).</li><li>If <var>hasGroups</var> is <b>true</b>, then<ol><li>Let <var>groups</var> be OrdinaryObjectCreate(<b>null</b>).</li></ol></li><li>Else,<ol><li>Let <var>groups</var> be <b>undefined</b>.</li></ol></li><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, <b>\"groups\"</b>, <var>groups</var>).</li><li>For each integer <var>i</var> such that 0 ≤ <var>i</var> &lt; <var>n</var>, in ascending order, do<ol><li>Let <var>matchIndices</var> be <var>indices</var>[<var>i</var>].</li><li>If <var>matchIndices</var> is not <b>undefined</b>, then<ol><li>Let <var>matchIndexPair</var> be GetMatchIndexPair(<var>str</var>, <var>matchIndices</var>).</li></ol></li><li>Else,<ol><li>Let <var>matchIndexPair</var> be <b>undefined</b>.</li></ol></li><li>Perform ! CreateDataPropertyOrThrow(<var>array</var>, ! ToString(𝔽(<var>i</var>)), <var>matchIndexPair</var>).</li><li>If <var>i</var> > 0, then<ol><li>Let <var>name</var> be <var>groupNames</var>[<var>i</var> - 1].</li><li>If <var>name</var> is not <b>undefined</b>, then<ol><li>Assert: <var>groups</var> is not <b>undefined</b>.</li><li>NOTE: If there are multiple groups named <var>name</var>, <var>groups</var> may already have an <var>name</var> property at this point. However, because <var>groups</var> is an ordinary object whose properties are all writable data properties, the call to CreateDataPropertyOrThrow is nevertheless guaranteed to succeed.</li><li>Perform ! CreateDataPropertyOrThrow(<var>groups</var>, <var>name</var>, <var>matchIndexPair</var>).</li></ol></li></ol></li></ol></li><li>Return <var>array</var>.</li></ol></emu-alg>\n      ","2.8":"<p>RegExp instances are ordinary objects that inherit properties from the RegExp prototype object. RegExp instances have internal slots [[OriginalSource]], [[OriginalFlags]], [[RegExpRecord]], and [[RegExpMatcher]]. The value of the [[RegExpMatcher]] internal slot is an Abstract Closure representation of the <emu-nt>Pattern</emu-nt> of the RegExp object.</p>\n      <emu-note>\n        <p>Prior to ECMAScript 2015, RegExp instances were specified as having the own data properties <b>\"source\"</b>, <b>\"global\"</b>, <b>\"ignoreCase\"</b>, and <b>\"multiline\"</b>. Those properties are now specified as accessor properties of <code>RegExp.prototype</code>.</p>\n      </emu-note>\n      <p>RegExp instances also have the following property:</p>\n\n      ","2.8.1":"<p>The value of the <b>\"lastIndex\"</b> property specifies the String index at which to start the next match. It is coerced to an integral Number when used (see <a href=\"/text-processing#sec-regexpbuiltinexec\">22.2.7.2</a>). This property shall have the attributes { [[Writable]]: <b>true</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","2.9":"<p>A <dfn variants=\"RegExp String Iterators,RegExp String Iterator object,RegExp String Iterator objects\">RegExp String Iterator</dfn> is an object that represents a specific iteration over some specific String instance object, matching against some specific RegExp instance object. There is not a named constructor for RegExp String Iterator objects. Instead, RegExp String Iterator objects are created by calling certain methods of RegExp instance objects.</p>\n\n      ","2.9.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>iterator</var> be OrdinaryObjectCreate(<emu-intrinsic>%RegExpStringIteratorPrototype%</emu-intrinsic>, « [[IteratingRegExp]], [[IteratedString]], [[Global]], [[Unicode]], [[Done]] »).</li><li>Set <var>iterator</var>.[[IteratingRegExp]] to <var>regexp</var>.</li><li>Set <var>iterator</var>.[[IteratedString]] to <var>str</var>.</li><li>Set <var>iterator</var>.[[Global]] to <var>global</var>.</li><li>Set <var>iterator</var>.[[Unicode]] to <var>fullUnicode</var>.</li><li>Set <var>iterator</var>.[[Done]] to <b>false</b>.</li><li>Return <var>iterator</var>.</li></ol></emu-alg>\n      ","2.9.2":"<p>The <dfn><emu-intrinsic>%RegExpStringIteratorPrototype%</emu-intrinsic></dfn> object:</p>\n        <ul>\n          <li>has properties that are inherited by all RegExp String Iterator objects.</li>\n          <li>is an ordinary object.</li>\n          <li>has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Iterator.prototype%</emu-intrinsic>.</li>\n          <li>has the following properties:</li>\n        </ul>\n\n        ","2.9.2.1":"<emu-alg><ol><li>Let <var>iteratorObj</var> be the <b>this</b> value.</li><li>If <var>iteratorObj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>If <var>iteratorObj</var> does not have all of the internal slots of a RegExp String Iterator Object Instance (see <a href=\"/text-processing#sec-properties-of-regexp-string-iterator-instances\">22.2.9.3</a>), throw a <b>TypeError</b> exception.</li><li>If <var>iteratorObj</var>.[[Done]] is <b>true</b>, then<ol><li>Return CreateIteratorResultObject(<b>undefined</b>, <b>true</b>).</li></ol></li><li>Let <var>regexp</var> be <var>iteratorObj</var>.[[IteratingRegExp]].</li><li>Let <var>str</var> be <var>iteratorObj</var>.[[IteratedString]].</li><li>Let <var>global</var> be <var>iteratorObj</var>.[[Global]].</li><li>Let <var>fullUnicode</var> be <var>iteratorObj</var>.[[Unicode]].</li><li>Let <var>match</var> be ? RegExpExec(<var>regexp</var>, <var>str</var>).</li><li>If <var>match</var> is <b>null</b>, then<ol><li>Set <var>iteratorObj</var>.[[Done]] to <b>true</b>.</li><li>Return CreateIteratorResultObject(<b>undefined</b>, <b>true</b>).</li></ol></li><li>If <var>global</var> is <b>false</b>, then<ol><li>Set <var>iteratorObj</var>.[[Done]] to <b>true</b>.</li><li>Return CreateIteratorResultObject(<var>match</var>, <b>false</b>).</li></ol></li><li>Let <var>matchStr</var> be ? ToString(? Get(<var>match</var>, <b>\"0\"</b>)).</li><li>If <var>matchStr</var> is the empty String, then<ol><li>Let <var>thisIndex</var> be ℝ(? ToLength(? Get(<var>regexp</var>, <b>\"lastIndex\"</b>))).</li><li>Let <var>nextIndex</var> be AdvanceStringIndex(<var>str</var>, <var>thisIndex</var>, <var>fullUnicode</var>).</li><li>Perform ? Set(<var>regexp</var>, <b>\"lastIndex\"</b>, 𝔽(<var>nextIndex</var>), <b>true</b>).</li></ol></li><li>Return CreateIteratorResultObject(<var>match</var>, <b>false</b>).</li></ol></emu-alg>\n        ","2.9.2.2":"<p>The initial value of the <emu-intrinsic>%Symbol.toStringTag%</emu-intrinsic> property is the String value <b>\"RegExp String Iterator\"</b>.</p>\n          <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n        ","2.9.3":"<p>RegExp String Iterator instances are ordinary objects that inherit properties from the <emu-intrinsic>%RegExpStringIteratorPrototype%</emu-intrinsic> intrinsic object. RegExp String Iterator instances are initially created with the internal slots listed in table-regexp-string-iterator-instance-slots.</p>\n        <emu-table id=\"table-regexp-string-iterator-instance-slots\" caption=\"Internal Slots of RegExp String Iterator Instances\">\n          <table>\n            <tr>\n              <th>Internal Slot</th>\n              <th>Type</th>\n              <th>Description</th>\n            </tr>\n            <tr>\n              <td>[[IteratingRegExp]]</td>\n              <td>an Object</td>\n              <td>The regular expression used for iteration. IsRegExp([[IteratingRegExp]]) is initially <b>true</b>.</td>\n            </tr>\n            <tr>\n              <td>[[IteratedString]]</td>\n              <td>a String</td>\n              <td>The String value being iterated upon.</td>\n            </tr>\n            <tr>\n              <td>[[Global]]</td>\n              <td>a Boolean</td>\n              <td>Indicates whether the [[IteratingRegExp]] is global or not.</td>\n            </tr>\n            <tr>\n              <td>[[Unicode]]</td>\n              <td>a Boolean</td>\n              <td>Indicates whether the [[IteratingRegExp]] is in Unicode mode or not.</td>\n            </tr>\n            <tr>\n              <td>[[Done]]</td>\n              <td>a Boolean</td>\n              <td>Indicates whether the iteration is complete or not.</td>\n            </tr>\n          </table>\n        </emu-table>\n      "};
const _basePath = process.env.NEXT_PUBLIC_BASE_PATH || '';
const sections = _basePath
  ? Object.fromEntries(
      Object.entries(_sections).map(([k, v]) => [
        k,
        v.replaceAll('href="/', `href="${_basePath}/`),
      ])
    )
  : _sections;
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
