// Generated from ecma262/spec.html — do not edit by hand.
const sections = {"1":"<p>The Reflect object:</p>\n    <ul>\n      <li>is <dfn><code class=\"emu-intrinsic\">%Reflect%</code></dfn>.</li>\n      <li>is the initial value of the <b>\"Reflect\"</b> property of the global object.</li>\n      <li>is an ordinary object.</li>\n      <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n      <li>is not a function object.</li>\n      <li>does not have a [[Construct]] internal method; it cannot be used as a constructor with the <code>new</code> operator.</li>\n      <li>does not have a [[Call]] internal method; it cannot be invoked as a function.</li>\n    </ul>\n\n    ","2":"","3":"<p>A Module Namespace Object is a module namespace exotic object that provides runtime property-based access to a module's exported bindings. There is no constructor function for Module Namespace Objects. Instead, such an object is created for each module that is imported by an <emu-nt>ImportDeclaration</emu-nt> that contains a <emu-nt>NameSpaceImport</emu-nt>.</p>\n    <p>In addition to the properties specified in <a href=\"/ordinary-and-exotic-objects-behaviours#sec-module-namespace-exotic-objects\">10.4.6</a> each Module Namespace Object has the following own property:</p>\n\n    ","":"","1.1":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If IsCallable(<var>target</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>args</var> be ? CreateListFromArrayLike(<var>argumentsList</var>).</li><li>Perform PrepareForTailCall().</li><li>Return ? Call(<var>target</var>, <var>thisArgument</var>, <var>args</var>).</li></ol></emu-alg>\n    ","1.2":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If IsConstructor(<var>target</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>If <var>newTarget</var> is not present, set <var>newTarget</var> to <var>target</var>.</li><li>Else if IsConstructor(<var>newTarget</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>args</var> be ? CreateListFromArrayLike(<var>argumentsList</var>).</li><li>Return ? Construct(<var>target</var>, <var>args</var>, <var>newTarget</var>).</li></ol></emu-alg>\n    ","1.3":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If <var>target</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>key</var> be ? ToPropertyKey(<var>propertyKey</var>).</li><li>Let <var>desc</var> be ? ToPropertyDescriptor(<var>attributes</var>).</li><li>Return ? <emu-meta effects=\"user-code\"><var>target</var>.[[DefineOwnProperty]](<var>key</var>, <var>desc</var>)</emu-meta>.</li></ol></emu-alg>\n    ","1.4":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If <var>target</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>key</var> be ? ToPropertyKey(<var>propertyKey</var>).</li><li>Return ? <emu-meta effects=\"user-code\"><var>target</var>.[[Delete]](<var>key</var>)</emu-meta>.</li></ol></emu-alg>\n    ","1.5":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If <var>target</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>key</var> be ? ToPropertyKey(<var>propertyKey</var>).</li><li>If <var>receiver</var> is not present, then<ol><li>Set <var>receiver</var> to <var>target</var>.</li></ol></li><li>Return ? <emu-meta effects=\"user-code\"><var>target</var>.[[Get]](<var>key</var>, <var>receiver</var>)</emu-meta>.</li></ol></emu-alg>\n    ","1.6":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If <var>target</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>key</var> be ? ToPropertyKey(<var>propertyKey</var>).</li><li>Let <var>desc</var> be ? <emu-meta effects=\"user-code\"><var>target</var>.[[GetOwnProperty]](<var>key</var>)</emu-meta>.</li><li>Return FromPropertyDescriptor(<var>desc</var>).</li></ol></emu-alg>\n    ","1.7":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If <var>target</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Return ? <emu-meta effects=\"user-code\"><var>target</var>.[[GetPrototypeOf]]()</emu-meta>.</li></ol></emu-alg>\n    ","1.8":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If <var>target</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>key</var> be ? ToPropertyKey(<var>propertyKey</var>).</li><li>Return ? <emu-meta effects=\"user-code\"><var>target</var>.[[HasProperty]](<var>key</var>)</emu-meta>.</li></ol></emu-alg>\n    ","1.9":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If <var>target</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Return ? <emu-meta effects=\"user-code\"><var>target</var>.[[IsExtensible]]()</emu-meta>.</li></ol></emu-alg>\n    ","1.10":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If <var>target</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>keys</var> be ? <emu-meta effects=\"user-code\"><var>target</var>.[[OwnPropertyKeys]]()</emu-meta>.</li><li>Return CreateArrayFromList(<var>keys</var>).</li></ol></emu-alg>\n    ","1.11":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If <var>target</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Return ? <emu-meta effects=\"user-code\"><var>target</var>.[[PreventExtensions]]()</emu-meta>.</li></ol></emu-alg>\n    ","1.12":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If <var>target</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>key</var> be ? ToPropertyKey(<var>propertyKey</var>).</li><li>If <var>receiver</var> is not present, then<ol><li>Set <var>receiver</var> to <var>target</var>.</li></ol></li><li>Return ? <emu-meta effects=\"user-code\"><var>target</var>.[[Set]](<var>key</var>, <var>value</var>, <var>receiver</var>)</emu-meta>.</li></ol></emu-alg>\n    ","1.13":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If <var>target</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>If <var>proto</var> is not an Object and <var>proto</var> is not <b>null</b>, throw a <b>TypeError</b> exception.</li><li>Return ? <emu-meta effects=\"user-code\"><var>target</var>.[[SetPrototypeOf]](<var>proto</var>)</emu-meta>.</li></ol></emu-alg>\n    ","1.14":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"Reflect\"</b>.</p>\n      <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n    ","2.1":"<p>The Proxy constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Proxy%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"Proxy\"</b> property of the global object.</li>\n        <li>creates and initializes a new Proxy object when called as a constructor.</li>\n        <li>is not intended to be called as a function and will throw an exception when called in that manner.</li>\n      </ul>\n\n      ","2.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Return ? ProxyCreate(<var>target</var>, <var>handler</var>).</li></ol></emu-alg>\n      ","2.2":"<p>The Proxy constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li>does not have a <b>\"prototype\"</b> property because Proxy objects do not have a [[Prototype]] internal slot that requires initialization.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","2.2.1":"<p>This function creates a revocable Proxy object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>proxy</var> be ? ProxyCreate(<var>target</var>, <var>handler</var>).</li><li>Let <var>revokerClosure</var> be a new Abstract Closure with no parameters that captures nothing and performs the following steps when called:<ol><li>Let <var>activeFunc</var> be the active function object.</li><li>Let <var>p</var> be <var>activeFunc</var>.[[RevocableProxy]].</li><li>If <var>p</var> is <b>null</b>, return NormalCompletion(<b>undefined</b>).</li><li>Set <var>activeFunc</var>.[[RevocableProxy]] to <b>null</b>.</li><li>Assert: <var>p</var> is a Proxy exotic object.</li><li>Set <var>p</var>.[[ProxyTarget]] to <b>null</b>.</li><li>Set <var>p</var>.[[ProxyHandler]] to <b>null</b>.</li><li>Return NormalCompletion(<b>undefined</b>).</li></ol></li><li>Let <var>revoker</var> be CreateBuiltinFunction(<var>revokerClosure</var>, 0, <b>\"\"</b>, « [[RevocableProxy]] »).</li><li>Set <var>revoker</var>.[[RevocableProxy]] to <var>proxy</var>.</li><li>Let <var>result</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Object.prototype%</code>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>result</var>, <b>\"proxy\"</b>, <var>proxy</var>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>result</var>, <b>\"revoke\"</b>, <var>revoker</var>).</li><li>Return <var>result</var>.</li></ol></emu-alg>\n      ","3.1":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"Module\"</b>.</p>\n      <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n    "};
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
