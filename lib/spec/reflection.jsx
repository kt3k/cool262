// Generated from ecma262/spec.html — do not edit by hand.
const sections = {"1":"<p>The Reflect object:</p>\n    <ul>\n      <li>is <dfn>%Reflect%</dfn>.</li>\n      <li>is the initial value of the *\"Reflect\"* property of the global object.</li>\n      <li>is an ordinary object.</li>\n      <li>has a [[Prototype]] internal slot whose value is %Object.prototype%.</li>\n      <li>is not a function object.</li>\n      <li>does not have a [[Construct]] internal method; it cannot be used as a constructor with the `new` operator.</li>\n      <li>does not have a [[Call]] internal method; it cannot be invoked as a function.</li>\n    </ul>\n\n    ","2":"","3":"<p>A Module Namespace Object is a module namespace exotic object that provides runtime property-based access to a module's exported bindings. There is no constructor function for Module Namespace Objects. Instead, such an object is created for each module that is imported by an |ImportDeclaration| that contains a |NameSpaceImport|.</p>\n    <p>In addition to the properties specified in 10.4.6 each Module Namespace Object has the following own property:</p>\n\n    ","":"","1.1":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If IsCallable(_target_) is *false*, throw a *TypeError* exception.</li><li>Let _args_ be ? CreateListFromArrayLike(_argumentsList_).</li><li>Perform PrepareForTailCall().</li><li>Return ? Call(_target_, _thisArgument_, _args_).</li></ol></emu-alg>\n    ","1.2":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If IsConstructor(_target_) is *false*, throw a *TypeError* exception.</li><li>If _newTarget_ is not present, set _newTarget_ to _target_.</li><li>Else if IsConstructor(_newTarget_) is *false*, throw a *TypeError* exception.</li><li>Let _args_ be ? CreateListFromArrayLike(_argumentsList_).</li><li>Return ? Construct(_target_, _args_, _newTarget_).</li></ol></emu-alg>\n    ","1.3":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If _target_ is not an Object, throw a *TypeError* exception.</li><li>Let _key_ be ? ToPropertyKey(_propertyKey_).</li><li>Let _desc_ be ? ToPropertyDescriptor(_attributes_).</li><li>Return ? <emu-meta effects=\"user-code\">_target_.[[DefineOwnProperty]](_key_, _desc_)</emu-meta>.</li></ol></emu-alg>\n    ","1.4":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If _target_ is not an Object, throw a *TypeError* exception.</li><li>Let _key_ be ? ToPropertyKey(_propertyKey_).</li><li>Return ? <emu-meta effects=\"user-code\">_target_.[[Delete]](_key_)</emu-meta>.</li></ol></emu-alg>\n    ","1.5":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If _target_ is not an Object, throw a *TypeError* exception.</li><li>Let _key_ be ? ToPropertyKey(_propertyKey_).</li><li>If _receiver_ is not present, then<ol><li>Set _receiver_ to _target_.</li></ol></li><li>Return ? <emu-meta effects=\"user-code\">_target_.[[Get]](_key_, _receiver_)</emu-meta>.</li></ol></emu-alg>\n    ","1.6":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If _target_ is not an Object, throw a *TypeError* exception.</li><li>Let _key_ be ? ToPropertyKey(_propertyKey_).</li><li>Let _desc_ be ? <emu-meta effects=\"user-code\">_target_.[[GetOwnProperty]](_key_)</emu-meta>.</li><li>Return FromPropertyDescriptor(_desc_).</li></ol></emu-alg>\n    ","1.7":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If _target_ is not an Object, throw a *TypeError* exception.</li><li>Return ? <emu-meta effects=\"user-code\">_target_.[[GetPrototypeOf]]()</emu-meta>.</li></ol></emu-alg>\n    ","1.8":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If _target_ is not an Object, throw a *TypeError* exception.</li><li>Let _key_ be ? ToPropertyKey(_propertyKey_).</li><li>Return ? <emu-meta effects=\"user-code\">_target_.[[HasProperty]](_key_)</emu-meta>.</li></ol></emu-alg>\n    ","1.9":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If _target_ is not an Object, throw a *TypeError* exception.</li><li>Return ? <emu-meta effects=\"user-code\">_target_.[[IsExtensible]]()</emu-meta>.</li></ol></emu-alg>\n    ","1.10":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If _target_ is not an Object, throw a *TypeError* exception.</li><li>Let _keys_ be ? <emu-meta effects=\"user-code\">_target_.[[OwnPropertyKeys]]()</emu-meta>.</li><li>Return CreateArrayFromList(_keys_).</li></ol></emu-alg>\n    ","1.11":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If _target_ is not an Object, throw a *TypeError* exception.</li><li>Return ? <emu-meta effects=\"user-code\">_target_.[[PreventExtensions]]()</emu-meta>.</li></ol></emu-alg>\n    ","1.12":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If _target_ is not an Object, throw a *TypeError* exception.</li><li>Let _key_ be ? ToPropertyKey(_propertyKey_).</li><li>If _receiver_ is not present, then<ol><li>Set _receiver_ to _target_.</li></ol></li><li>Return ? <emu-meta effects=\"user-code\">_target_.[[Set]](_key_, _value_, _receiver_)</emu-meta>.</li></ol></emu-alg>\n    ","1.13":"<p>This function performs the following steps when called:</p>\n      <emu-alg><ol><li>If _target_ is not an Object, throw a *TypeError* exception.</li><li>If _proto_ is not an Object and _proto_ is not *null*, throw a *TypeError* exception.</li><li>Return ? <emu-meta effects=\"user-code\">_target_.[[SetPrototypeOf]](_proto_)</emu-meta>.</li></ol></emu-alg>\n    ","1.14":"<p>The initial value of the %Symbol.toStringTag% property is the String value *\"Reflect\"*.</p>\n      <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *true* }.</p>\n    ","2.1":"<p>The Proxy constructor:</p>\n      <ul>\n        <li>is <dfn>%Proxy%</dfn>.</li>\n        <li>is the initial value of the *\"Proxy\"* property of the global object.</li>\n        <li>creates and initializes a new Proxy object when called as a constructor.</li>\n        <li>is not intended to be called as a function and will throw an exception when called in that manner.</li>\n      </ul>\n\n      ","2.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is *undefined*, throw a *TypeError* exception.</li><li>Return ? ProxyCreate(_target_, _handler_).</li></ol></emu-alg>\n      ","2.2":"<p>The Proxy constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is %Function.prototype%.</li>\n        <li>does not have a *\"prototype\"* property because Proxy objects do not have a [[Prototype]] internal slot that requires initialization.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","2.2.1":"<p>This function creates a revocable Proxy object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _proxy_ be ? ProxyCreate(_target_, _handler_).</li><li>Let _revokerClosure_ be a new Abstract Closure with no parameters that captures nothing and performs the following steps when called:<ol><li>Let _activeFunc_ be the active function object.</li><li>Let _p_ be _activeFunc_.[[RevocableProxy]].</li><li>If _p_ is *null*, return NormalCompletion(*undefined*).</li><li>Set _activeFunc_.[[RevocableProxy]] to *null*.</li><li>Assert: _p_ is a Proxy exotic object.</li><li>Set _p_.[[ProxyTarget]] to *null*.</li><li>Set _p_.[[ProxyHandler]] to *null*.</li><li>Return NormalCompletion(*undefined*).</li></ol></li><li>Let _revoker_ be CreateBuiltinFunction(_revokerClosure_, 0, *\"\"*, « [[RevocableProxy]] »).</li><li>Set _revoker_.[[RevocableProxy]] to _proxy_.</li><li>Let _result_ be OrdinaryObjectCreate(%Object.prototype%).</li><li>Perform ! CreateDataPropertyOrThrow(_result_, *\"proxy\"*, _proxy_).</li><li>Perform ! CreateDataPropertyOrThrow(_result_, *\"revoke\"*, _revoker_).</li><li>Return _result_.</li></ol></emu-alg>\n      ","3.1":"<p>The initial value of the %Symbol.toStringTag% property is the String value *\"Module\"*.</p>\n      <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *false* }.</p>\n    "};
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
