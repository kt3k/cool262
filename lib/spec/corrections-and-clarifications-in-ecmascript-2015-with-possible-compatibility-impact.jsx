// Generated from ecma262/spec.html — do not edit by hand.
const sections = {"":"<p>9.1.1.4.14-9.1.1.4.17 Edition 5 and 5.1 used a property existence test to determine whether a global object property corresponding to a new global declaration already existed. ECMAScript 2015 uses an own property existence test. This corresponds to what has been most commonly implemented by web browsers.</p>\n  <p>10.4.2.1: The 5<sup>th</sup> Edition moved the capture of the current array length prior to the integer conversion of the array index or new length value. However, the captured length value could become invalid if the conversion process has the side-effect of changing the array length. ECMAScript 2015 specifies that the current array length must be captured after the possible occurrence of such side-effects.</p>\n  <p>21.4.1.31: Previous editions permitted the TimeClip abstract operation to return either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub> as the representation of a 0 time value. ECMAScript 2015 specifies that <b>+0</b><sub>𝔽</sub> always returned. This means that for ECMAScript 2015 the time value of a Date is never observably <b>-0</b><sub>𝔽</sub> and methods that return time values never return <b>-0</b><sub>𝔽</sub>.</p>\n  <p>21.4.1.32: If a UTC offset representation is not present, the local time zone is used. Edition 5.1 incorrectly stated that a missing time zone should be interpreted as <b>\"z\"</b>.</p>\n  <p>21.4.4.36: If the year cannot be represented using the Date Time String Format specified in 21.4.1.32 a RangeError exception is thrown. Previous editions did not specify the behaviour for that case.</p>\n  <p>21.4.4.41: Previous editions did not specify the value returned by <code>Date.prototype.toString</code> when the time value is <b>NaN</b>. ECMAScript 2015 specifies the result to be the String value <b>\"Invalid Date\"</b>.</p>\n  <p>22.2.4.1, 22.2.6.13.1: Any LineTerminator code points in the value of the <b>\"source\"</b> property of a RegExp instance must be expressed using an escape sequence. Edition 5.1 only required the escaping of <code>/</code>.</p>\n  <p>22.2.6.8, 22.2.6.11: In previous editions, the specifications for <code>String.prototype.match</code> and <code>String.prototype.replace</code> was incorrect for cases where the pattern argument was a RegExp value whose <code>global</code> flag is set. The previous specifications stated that for each attempt to match the pattern, if <code>lastIndex</code> did not change, it should be incremented by 1. The correct behaviour is that <code>lastIndex</code> should be incremented by 1 only if the pattern matched the empty String.</p>\n  <p>23.1.3.30: Previous editions did not specify how a <b>NaN</b> value returned by a <var>comparator</var> was interpreted by <code>Array.prototype.sort</code>. ECMAScript 2015 specifies that such as value is treated as if <b>+0</b><sub>𝔽</sub> was returned from the <var>comparator</var>. ECMAScript 2015 also specifies that ToNumber is applied to the result returned by a <var>comparator</var>. In previous editions, the effect of a <var>comparator</var> result that is not a Number value was implementation-defined. In practice, implementations call ToNumber.</p>"};
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
