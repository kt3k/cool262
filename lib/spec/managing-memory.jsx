// Generated from ecma262/spec.html — do not edit by hand.
const _sections = {"1":"<p>A WeakRef is an object that is used to refer to a target object or symbol without preserving it from garbage collection. WeakRefs can be dereferenced to allow access to the target value, if the target hasn't been reclaimed by garbage collection.</p>\n\n    ","2":"<p>A FinalizationRegistry is an object that manages registration and unregistration of cleanup operations that are performed when target objects and symbols are garbage collected.</p>\n\n    ","":"","1.1":"<p>The <dfn variants=\"WeakRefs\">WeakRef</dfn> constructor:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%WeakRef%</emu-intrinsic></dfn>.</li>\n        <li>\n          is the initial value of the <b>\"WeakRef\"</b> property of the global object.\n        </li>\n        <li>\n          creates and initializes a new WeakRef when called as a constructor.\n        </li>\n        <li>\n          is not intended to be called as a function and will throw an exception when called in that manner.\n        </li>\n        <li>\n          may be used as the value in an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified <code>WeakRef</code> behaviour must include a <code>super</code> call to the <code>WeakRef</code> constructor to create and initialize the subclass instance with the internal state necessary to support the <code>WeakRef.prototype</code> built-in methods.\n        </li>\n      </ul>\n\n      ","1.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>If CanBeHeldWeakly(<var>target</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>weakRef</var> be ? OrdinaryCreateFromConstructor(NewTarget, <b>\"<emu-intrinsic>%WeakRef.prototype%</emu-intrinsic>\"</b>, « [[WeakRefTarget]] »).</li><li>Perform AddToKeptObjects(<var>target</var>).</li><li>Set <var>weakRef</var>.[[WeakRefTarget]] to <var>target</var>.</li><li>Return <var>weakRef</var>.</li></ol></emu-alg>\n      ","1.2":"<p>The WeakRef constructor:</p>\n      <ul>\n        <li>\n          has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Function.prototype%</emu-intrinsic>.\n        </li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","1.2.1":"<p>The initial value of <code>WeakRef.prototype</code> is the WeakRef prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.3":"<p>The <dfn>WeakRef prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%WeakRef.prototype%</emu-intrinsic></dfn>.</li>\n        <li>\n          has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Object.prototype%</emu-intrinsic>.\n        </li>\n        <li>is an ordinary object.</li>\n        <li>does not have a [[WeakRefTarget]] internal slot.</li>\n      </ul>\n\n      ","1.3.1":"<p>The initial value of <code>WeakRef.prototype.constructor</code> is <emu-intrinsic>%WeakRef%</emu-intrinsic>.</p>\n      ","1.3.2":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>weakRef</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>weakRef</var>, [[WeakRefTarget]]).</li><li>Return WeakRefDeref(<var>weakRef</var>).</li></ol></emu-alg>\n\n        <emu-note>\n          <p>If the WeakRef returns a <var>target</var> value that is not <b>undefined</b>, then this <var>target</var> value should not be garbage collected until the current execution of ECMAScript code has completed. The AddToKeptObjects operation makes sure read consistency is maintained.</p>\n\n          <pre><code class=\"javascript\">\n            let target = { foo() {} };\n            let weakRef = new WeakRef(target);\n\n            // ... later ...\n\n            if (weakRef.deref()) {\n              weakRef.deref().foo();\n            }\n          </code></pre>\n\n          <p>In the above example, if the first deref does not evaluate to <b>undefined</b> then the second deref cannot either.</p>\n        </emu-note>\n      ","1.3.3":"<p>The initial value of the <emu-intrinsic>%Symbol.toStringTag%</emu-intrinsic> property is the String value <b>\"WeakRef\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","1.4":"","1.4.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>target</var> be <var>weakRef</var>.[[WeakRefTarget]].</li><li>If <var>target</var> is not <emu-const>empty</emu-const>, then<ol><li>Perform AddToKeptObjects(<var>target</var>).</li><li>Return <var>target</var>.</li></ol></li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p>This abstract operation is defined separately from WeakRef.prototype.deref strictly to make it possible to succinctly define liveness.</p>\n        </emu-note>\n      ","1.5":"<p>WeakRef instances are ordinary objects that inherit properties from the WeakRef prototype object. WeakRef instances also have a [[WeakRefTarget]] internal slot.</p>\n    ","2.1":"<p>The <dfn variants=\"FinalizationRegistrys\">FinalizationRegistry</dfn> constructor:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%FinalizationRegistry%</emu-intrinsic></dfn>.</li>\n        <li>\n          is the initial value of the <b>\"FinalizationRegistry\"</b> property of the global object.\n        </li>\n        <li>\n          creates and initializes a new FinalizationRegistry when called as a constructor.\n        </li>\n        <li>\n          is not intended to be called as a function and will throw an exception when called in that manner.\n        </li>\n        <li>\n          may be used as the value in an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified <code>FinalizationRegistry</code> behaviour must include a <code>super</code> call to the <code>FinalizationRegistry</code> constructor to create and initialize the subclass instance with the internal state necessary to support the <code>FinalizationRegistry.prototype</code> built-in methods.\n        </li>\n      </ul>\n\n      ","2.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>If IsCallable(<var>cleanupCallback</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>finalizationRegistry</var> be ? OrdinaryCreateFromConstructor(NewTarget, <b>\"<emu-intrinsic>%FinalizationRegistry.prototype%</emu-intrinsic>\"</b>, « [[Realm]], [[CleanupCallback]], [[Cells]] »).</li><li>Let <var>fn</var> be the active function object.</li><li>Set <var>finalizationRegistry</var>.[[Realm]] to <var>fn</var>.[[Realm]].</li><li>Set <var>finalizationRegistry</var>.[[CleanupCallback]] to HostMakeJobCallback(<var>cleanupCallback</var>).</li><li>Set <var>finalizationRegistry</var>.[[Cells]] to a new empty List.</li><li>Return <var>finalizationRegistry</var>.</li></ol></emu-alg>\n      ","2.2":"<p>The FinalizationRegistry constructor:</p>\n      <ul>\n        <li>\n          has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Function.prototype%</emu-intrinsic>.\n        </li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","2.2.1":"<p>The initial value of <code>FinalizationRegistry.prototype</code> is the FinalizationRegistry prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","2.3":"<p>The <dfn>FinalizationRegistry prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%FinalizationRegistry.prototype%</emu-intrinsic></dfn>.</li>\n        <li>\n          has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Object.prototype%</emu-intrinsic>.\n        </li>\n        <li>is an ordinary object.</li>\n        <li>\n          does not have [[Cells]] and [[CleanupCallback]] internal slots.\n        </li>\n      </ul>\n\n      ","2.3.1":"<p>The initial value of <code>FinalizationRegistry.prototype.constructor</code> is <emu-intrinsic>%FinalizationRegistry%</emu-intrinsic>.</p>\n      ","2.3.2":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>finalizationRegistry</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>finalizationRegistry</var>, [[Cells]]).</li><li>If CanBeHeldWeakly(<var>target</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>If SameValue(<var>target</var>, <var>heldValue</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>If CanBeHeldWeakly(<var>unregisterToken</var>) is <b>false</b>, then<ol><li>If <var>unregisterToken</var> is not <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Set <var>unregisterToken</var> to <emu-const>empty</emu-const>.</li></ol></li><li>Let <var>cell</var> be the Record { [[WeakRefTarget]]: <var>target</var>, [[HeldValue]]: <var>heldValue</var>, [[UnregisterToken]]: <var>unregisterToken</var> }.</li><li>Append <var>cell</var> to <var>finalizationRegistry</var>.[[Cells]].</li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n\n        <emu-note>\n          <p>Based on the algorithms and definitions in this specification, <var>cell</var>.[[HeldValue]] is live when <var>finalizationRegistry</var>.[[Cells]] contains <var>cell</var>; however, this does not necessarily mean that <var>cell</var>.[[UnregisterToken]] or <var>cell</var>.[[Target]] are live. For example, registering an object with itself as its unregister token would not keep the object alive forever.</p>\n        </emu-note>\n      ","2.3.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>finalizationRegistry</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>finalizationRegistry</var>, [[Cells]]).</li><li>If CanBeHeldWeakly(<var>unregisterToken</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>removed</var> be <b>false</b>.</li><li>For each Record { [[WeakRefTarget]], [[HeldValue]], [[UnregisterToken]] } <var>cell</var> of <var>finalizationRegistry</var>.[[Cells]], do<ol><li>If <var>cell</var>.[[UnregisterToken]] is not <emu-const>empty</emu-const> and SameValue(<var>cell</var>.[[UnregisterToken]], <var>unregisterToken</var>) is <b>true</b>, then<ol><li>Remove <var>cell</var> from <var>finalizationRegistry</var>.[[Cells]].</li><li>Set <var>removed</var> to <b>true</b>.</li></ol></li></ol></li><li>Return <var>removed</var>.</li></ol></emu-alg>\n      ","2.3.4":"<p>The initial value of the <emu-intrinsic>%Symbol.toStringTag%</emu-intrinsic> property is the String value <b>\"FinalizationRegistry\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","2.4":"<p>FinalizationRegistry instances are ordinary objects that inherit properties from the FinalizationRegistry prototype object. FinalizationRegistry instances also have [[Cells]] and [[CleanupCallback]] internal slots.</p>\n    "};
const _basePath = process.env.NEXT_PUBLIC_BASE_PATH || '';
const sections = _basePath
  ? Object.fromEntries(
      Object.entries(_sections).map(([k, v]) => [
        k,
        v.replaceAll('href="/', `href="${_basePath}/`),
      ])
    )
  : _sections;
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
