// Generated from ecma262/spec.html — do not edit by hand.
const sections = {"1":"<p>A WeakRef is an object that is used to refer to a target object or symbol without preserving it from garbage collection. WeakRefs can be dereferenced to allow access to the target value, if the target hasn't been reclaimed by garbage collection.</p>\n\n    ","2":"<p>A FinalizationRegistry is an object that manages registration and unregistration of cleanup operations that are performed when target objects and symbols are garbage collected.</p>\n\n    ","":"","1.1":"<p>The <dfn variants=\"WeakRefs\">WeakRef</dfn> constructor:</p>\n      <ul>\n        <li>is <dfn>%WeakRef%</dfn>.</li>\n        <li>\n          is the initial value of the *\"WeakRef\"* property of the global object.\n        </li>\n        <li>\n          creates and initializes a new WeakRef when called as a constructor.\n        </li>\n        <li>\n          is not intended to be called as a function and will throw an exception when called in that manner.\n        </li>\n        <li>\n          may be used as the value in an `extends` clause of a class definition. Subclass constructors that intend to inherit the specified `WeakRef` behaviour must include a `super` call to the `WeakRef` constructor to create and initialize the subclass instance with the internal state necessary to support the `WeakRef.prototype` built-in methods.\n        </li>\n      </ul>\n\n      ","1.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is *undefined*, throw a *TypeError* exception.</li><li>If CanBeHeldWeakly(_target_) is *false*, throw a *TypeError* exception.</li><li>Let _weakRef_ be ? OrdinaryCreateFromConstructor(NewTarget, *\"%WeakRef.prototype%\"*, « [[WeakRefTarget]] »).</li><li>Perform AddToKeptObjects(_target_).</li><li>Set _weakRef_.[[WeakRefTarget]] to _target_.</li><li>Return _weakRef_.</li></ol></emu-alg>\n      ","1.2":"<p>The WeakRef constructor:</p>\n      <ul>\n        <li>\n          has a [[Prototype]] internal slot whose value is %Function.prototype%.\n        </li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","1.2.1":"<p>The initial value of `WeakRef.prototype` is the WeakRef prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *false* }.</p>\n      ","1.3":"<p>The <dfn>WeakRef prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn>%WeakRef.prototype%</dfn>.</li>\n        <li>\n          has a [[Prototype]] internal slot whose value is %Object.prototype%.\n        </li>\n        <li>is an ordinary object.</li>\n        <li>does not have a [[WeakRefTarget]] internal slot.</li>\n      </ul>\n\n      ","1.3.1":"<p>The initial value of `WeakRef.prototype.constructor` is %WeakRef%.</p>\n      ","1.3.2":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _weakRef_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_weakRef_, [[WeakRefTarget]]).</li><li>Return WeakRefDeref(_weakRef_).</li></ol></emu-alg>\n\n        <emu-note>\n          <p>If the WeakRef returns a _target_ value that is not *undefined*, then this _target_ value should not be garbage collected until the current execution of ECMAScript code has completed. The AddToKeptObjects operation makes sure read consistency is maintained.</p>\n\n          <pre><code class=\"javascript\">\n            let target = { foo() {} };\n            let weakRef = new WeakRef(target);\n\n            // ... later ...\n\n            if (weakRef.deref()) {\n              weakRef.deref().foo();\n            }\n          </code></pre>\n\n          <p>In the above example, if the first deref does not evaluate to *undefined* then the second deref cannot either.</p>\n        </emu-note>\n      ","1.3.3":"<p>The initial value of the %Symbol.toStringTag% property is the String value *\"WeakRef\"*.</p>\n        <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *true* }.</p>\n      ","1.4":"","1.4.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _target_ be _weakRef_.[[WeakRefTarget]].</li><li>If _target_ is not ~empty~, then<ol><li>Perform AddToKeptObjects(_target_).</li><li>Return _target_.</li></ol></li><li>Return *undefined*.</li></ol></emu-alg>\n        <emu-note>\n          <p>This abstract operation is defined separately from WeakRef.prototype.deref strictly to make it possible to succinctly define liveness.</p>\n        </emu-note>\n      ","1.5":"<p>WeakRef instances are ordinary objects that inherit properties from the WeakRef prototype object. WeakRef instances also have a [[WeakRefTarget]] internal slot.</p>\n    ","2.1":"<p>The <dfn variants=\"FinalizationRegistrys\">FinalizationRegistry</dfn> constructor:</p>\n      <ul>\n        <li>is <dfn>%FinalizationRegistry%</dfn>.</li>\n        <li>\n          is the initial value of the *\"FinalizationRegistry\"* property of the global object.\n        </li>\n        <li>\n          creates and initializes a new FinalizationRegistry when called as a constructor.\n        </li>\n        <li>\n          is not intended to be called as a function and will throw an exception when called in that manner.\n        </li>\n        <li>\n          may be used as the value in an `extends` clause of a class definition. Subclass constructors that intend to inherit the specified `FinalizationRegistry` behaviour must include a `super` call to the `FinalizationRegistry` constructor to create and initialize the subclass instance with the internal state necessary to support the `FinalizationRegistry.prototype` built-in methods.\n        </li>\n      </ul>\n\n      ","2.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is *undefined*, throw a *TypeError* exception.</li><li>If IsCallable(_cleanupCallback_) is *false*, throw a *TypeError* exception.</li><li>Let _finalizationRegistry_ be ? OrdinaryCreateFromConstructor(NewTarget, *\"%FinalizationRegistry.prototype%\"*, « [[Realm]], [[CleanupCallback]], [[Cells]] »).</li><li>Let _fn_ be the active function object.</li><li>Set _finalizationRegistry_.[[Realm]] to _fn_.[[Realm]].</li><li>Set _finalizationRegistry_.[[CleanupCallback]] to HostMakeJobCallback(_cleanupCallback_).</li><li>Set _finalizationRegistry_.[[Cells]] to a new empty List.</li><li>Return _finalizationRegistry_.</li></ol></emu-alg>\n      ","2.2":"<p>The FinalizationRegistry constructor:</p>\n      <ul>\n        <li>\n          has a [[Prototype]] internal slot whose value is %Function.prototype%.\n        </li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","2.2.1":"<p>The initial value of `FinalizationRegistry.prototype` is the FinalizationRegistry prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *false* }.</p>\n      ","2.3":"<p>The <dfn>FinalizationRegistry prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn>%FinalizationRegistry.prototype%</dfn>.</li>\n        <li>\n          has a [[Prototype]] internal slot whose value is %Object.prototype%.\n        </li>\n        <li>is an ordinary object.</li>\n        <li>\n          does not have [[Cells]] and [[CleanupCallback]] internal slots.\n        </li>\n      </ul>\n\n      ","2.3.1":"<p>The initial value of `FinalizationRegistry.prototype.constructor` is %FinalizationRegistry%.</p>\n      ","2.3.2":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _finalizationRegistry_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_finalizationRegistry_, [[Cells]]).</li><li>If CanBeHeldWeakly(_target_) is *false*, throw a *TypeError* exception.</li><li>If SameValue(_target_, _heldValue_) is *true*, throw a *TypeError* exception.</li><li>If CanBeHeldWeakly(_unregisterToken_) is *false*, then<ol><li>If _unregisterToken_ is not *undefined*, throw a *TypeError* exception.</li><li>Set _unregisterToken_ to ~empty~.</li></ol></li><li>Let _cell_ be the Record { [[WeakRefTarget]]: _target_, [[HeldValue]]: _heldValue_, [[UnregisterToken]]: _unregisterToken_ }.</li><li>Append _cell_ to _finalizationRegistry_.[[Cells]].</li><li>Return *undefined*.</li></ol></emu-alg>\n\n        <emu-note>\n          <p>Based on the algorithms and definitions in this specification, _cell_.[[HeldValue]] is live when _finalizationRegistry_.[[Cells]] contains _cell_; however, this does not necessarily mean that _cell_.[[UnregisterToken]] or _cell_.[[Target]] are live. For example, registering an object with itself as its unregister token would not keep the object alive forever.</p>\n        </emu-note>\n      ","2.3.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _finalizationRegistry_ be the *this* value.</li><li>Perform ? RequireInternalSlot(_finalizationRegistry_, [[Cells]]).</li><li>If CanBeHeldWeakly(_unregisterToken_) is *false*, throw a *TypeError* exception.</li><li>Let _removed_ be *false*.</li><li>For each Record { [[WeakRefTarget]], [[HeldValue]], [[UnregisterToken]] } _cell_ of _finalizationRegistry_.[[Cells]], do<ol><li>If _cell_.[[UnregisterToken]] is not ~empty~ and SameValue(_cell_.[[UnregisterToken]], _unregisterToken_) is *true*, then<ol><li>Remove _cell_ from _finalizationRegistry_.[[Cells]].</li><li>Set _removed_ to *true*.</li></ol></li></ol></li><li>Return _removed_.</li></ol></emu-alg>\n      ","2.3.4":"<p>The initial value of the %Symbol.toStringTag% property is the String value *\"FinalizationRegistry\"*.</p>\n        <p>This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *true* }.</p>\n      ","2.4":"<p>FinalizationRegistry instances are ordinary objects that inherit properties from the FinalizationRegistry prototype object. FinalizationRegistry instances also have [[Cells]] and [[CleanupCallback]] internal slots.</p>\n    "};
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
