// Generated from ecma262/spec.html — do not edit by hand.
const sections = {"1":"","2":"<p>A Promise is an object that is used as a placeholder for the eventual results of a deferred (and possibly asynchronous) computation.</p>\n    <p>Any Promise is in one of three mutually exclusive states: <em>fulfilled</em>, <em>rejected</em>, and <em>pending</em>:</p>\n    <ul>\n      <li>\n        A promise <code>p</code> is fulfilled if <code>p.then(f, r)</code> will immediately enqueue a Job to call the function <code>f</code>.\n      </li>\n      <li>\n        A promise <code>p</code> is rejected if <code>p.then(f, r)</code> will immediately enqueue a Job to call the function <code>r</code>.\n      </li>\n      <li>\n        A promise is pending if it is neither fulfilled nor rejected.\n      </li>\n    </ul>\n    <p>A promise is said to be <em>settled</em> if it is not pending, i.e. if it is either fulfilled or rejected.</p>\n    <p>A promise is <em>resolved</em> if it is settled or if it has been “locked in” to match the state of another promise. Attempting to resolve or reject a resolved promise has no effect. A promise is <em>unresolved</em> if it is not resolved. An unresolved promise is always in the pending state. A resolved promise may be pending, fulfilled or rejected.</p>\n\n    ","3":"<p>GeneratorFunctions are functions that are usually created by evaluating <emu-nt>GeneratorDeclaration</emu-nt>s, <emu-nt>GeneratorExpression</emu-nt>s, and <emu-nt>GeneratorMethod</emu-nt>s. They may also be created by calling the <code class=\"emu-intrinsic\">%GeneratorFunction%</code> intrinsic.</p>\n    <emu-figure id=\"figure-2\" caption=\"Generator Objects Relationships\" informative>\n      <img alt=\"A staggering variety of boxes and arrows.\" height=\"700\" src=\"img/figure-2.svg\" width=\"900\">\n    </emu-figure>\n\n    ","4":"<p>AsyncGeneratorFunctions are functions that are usually created by evaluating <emu-nt>AsyncGeneratorDeclaration</emu-nt>, <emu-nt>AsyncGeneratorExpression</emu-nt>, and <emu-nt>AsyncGeneratorMethod</emu-nt> syntactic productions. They may also be created by calling the <code class=\"emu-intrinsic\">%AsyncGeneratorFunction%</code> intrinsic.</p>\n\n    ","5":"<p>A Generator is created by calling a generator function and conforms to both the iterator interface and the iterable interface.</p>\n    <p>Generator instances directly inherit properties from the initial value of the <b>\"prototype\"</b> property of the generator function that created the instance. Generator instances indirectly inherit properties from <code class=\"emu-intrinsic\">%GeneratorPrototype%</code>.</p>\n\n    ","6":"<p>An AsyncGenerator is created by calling an async generator function and conforms to both the async iterator interface and the async iterable interface.</p>\n\n    <p>AsyncGenerator instances directly inherit properties from the initial value of the <b>\"prototype\"</b> property of the async generator function that created the instance. AsyncGenerator instances indirectly inherit properties from <code class=\"emu-intrinsic\">%AsyncGeneratorPrototype%</code>.</p>\n\n    ","7":"<p>AsyncFunctions are functions that are usually created by evaluating <emu-nt>AsyncFunctionDeclaration</emu-nt>s, <emu-nt>AsyncFunctionExpression</emu-nt>s, <emu-nt>AsyncMethod</emu-nt>s, and <emu-nt>AsyncArrowFunction</emu-nt>s. They may also be created by calling the <code class=\"emu-intrinsic\">%AsyncFunction%</code> intrinsic.</p>\n\n    ","":"","1.1":"<p>An interface is a set of property keys whose associated values match a specific specification. Any object that provides all the properties as described by an interface's specification <em>conforms</em> to that interface. An interface is not represented by a distinct object. There may be many separately implemented objects that conform to any interface. An individual object may conform to multiple interfaces.</p>\n\n      ","1.1.1":"<p>The <dfn variants=\"iterable,iterables,iterable object,iterable objects\">iterable interface</dfn> includes the property described in table-iterable-interface-required-properties:</p>\n        <emu-table id=\"table-iterable-interface-required-properties\" caption=\"Iterable Interface Required Properties\" oldids=\"table-52\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Property\n                </th>\n                <th>\n                  Value\n                </th>\n                <th>\n                  Requirements\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                <code><code class=\"emu-intrinsic\">%Symbol.iterator%</code></code>\n              </td>\n              <td>\n                a function that returns an iterator object\n              </td>\n              <td>\n                The returned object must conform to the iterator interface.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n      ","1.1.2":"<p>An object that implements the <dfn variants=\"iterator object,iterator objects,iterator,iterators\">iterator interface</dfn> must include the property in table-iterator-interface-required-properties. Such objects may also implement the properties in table-iterator-interface-optional-properties.</p>\n        <emu-table id=\"table-iterator-interface-required-properties\" caption=\"Iterator Interface Required Properties\" oldids=\"table-53\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Property\n                </th>\n                <th>\n                  Value\n                </th>\n                <th>\n                  Requirements\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                <b>\"next\"</b>\n              </td>\n              <td>\n                a function that returns an IteratorResult object\n              </td>\n              <td>\n                The returned object must conform to the IteratorResult interface. If a previous call to the <code>next</code> method of an iterator has returned an IteratorResult object whose <b>\"done\"</b> property is <b>true</b>, then all subsequent calls to the <code>next</code> method of that object should also return an IteratorResult object whose <b>\"done\"</b> property is <b>true</b>. However, this requirement is not enforced.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n        <emu-note>\n          <p>Arguments may be passed to the <code>next</code> function but their interpretation and validity is dependent upon the target iterator. The for-of statement and other common users of iterators do not pass any arguments, so iterator objects that expect to be used in such a manner must be prepared to deal with being called with no arguments.</p>\n        </emu-note>\n        <emu-table id=\"table-iterator-interface-optional-properties\" caption=\"Iterator Interface Optional Properties\" oldids=\"table-54\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Property\n                </th>\n                <th>\n                  Value\n                </th>\n                <th>\n                  Requirements\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                <b>\"return\"</b>\n              </td>\n              <td>\n                a function that returns an IteratorResult object\n              </td>\n              <td>\n                The returned object must conform to the IteratorResult interface. Invoking this method notifies the iterator object that the caller does not intend to make any more <code>next</code> method calls to the iterator. The returned IteratorResult object will typically have a <b>\"done\"</b> property whose value is <b>true</b>, and a <b>\"value\"</b> property with the value passed as the argument of the <code>return</code> method. However, this requirement is not enforced.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <b>\"throw\"</b>\n              </td>\n              <td>\n                a function that returns an IteratorResult object\n              </td>\n              <td>\n                The returned object must conform to the IteratorResult interface. Invoking this method notifies the iterator object that the caller has detected an error condition. The argument may be used to identify the error condition and typically will be an exception object. A typical response is to <code>throw</code> the value passed as the argument. If the method does not <code>throw</code>, the returned IteratorResult object will typically have a <b>\"done\"</b> property whose value is <b>true</b>.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n        <emu-note>\n          <p>Typically callers of these methods should check for their existence before invoking them. Certain ECMAScript language features including <code>for</code>-<code>of</code>, <code>yield*</code>, and array destructuring call these methods after performing an existence check. Most ECMAScript library functions that accept iterable objects as arguments also conditionally call them.</p>\n        </emu-note>\n      ","1.1.3":"<p>The <dfn variants=\"async iterable,async iterables,async iterable object,async iterable objects\">async iterable interface</dfn> includes the properties described in table-async-iterable:</p>\n        <emu-table id=\"table-async-iterable\" caption=\"Async Iterable Interface Required Properties\">\n          <table>\n            <thead>\n              <tr>\n                <th>Property</th>\n                <th>Value</th>\n                <th>Requirements</th>\n              </tr>\n            </thead>\n            <tr>\n              <td><code><code class=\"emu-intrinsic\">%Symbol.asyncIterator%</code></code></td>\n              <td>a function that returns an async iterator object</td>\n              <td>The returned object must conform to the async iterator interface.</td>\n            </tr>\n          </table>\n        </emu-table>\n      ","1.1.4":"<p>An object that implements the <dfn variants=\"async iterator object,async iterator objects,async iterator,async iterators\">async iterator interface</dfn> must include the properties in table-async-iterator-required. Such objects may also implement the properties in table-async-iterator-optional.</p>\n        <emu-table id=\"table-async-iterator-required\" caption=\"Async Iterator Interface Required Properties\">\n          <table>\n            <thead>\n              <tr>\n                <th>Property</th>\n                <th>Value</th>\n                <th>Requirements</th>\n              </tr>\n            </thead>\n            <tr>\n              <td><b>\"next\"</b></td>\n              <td>a function that returns a promise for an IteratorResult object</td>\n              <td>\n                <p>The returned promise, when fulfilled, must fulfill with an object that conforms to the IteratorResult interface. If a previous call to the <code>next</code> method of an async iterator has returned a promise for an IteratorResult object whose <b>\"done\"</b> property is <b>true</b>, then all subsequent calls to the <code>next</code> method of that object should also return a promise for an IteratorResult object whose <b>\"done\"</b> property is <b>true</b>. However, this requirement is not enforced.</p>\n\n                <p>Additionally, the IteratorResult object that serves as a fulfillment value should have a <b>\"value\"</b> property whose value is not a promise (or \"thenable\"). However, this requirement is also not enforced.</p>\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n        <emu-note>\n          <p>Arguments may be passed to the <code>next</code> function but their interpretation and validity is dependent upon the target async iterator. The <code>for</code>-<code>await</code>-<code>of</code> statement and other common users of async iterators do not pass any arguments, so async iterator objects that expect to be used in such a manner must be prepared to deal with being called with no arguments.</p>\n        </emu-note>\n        <emu-table id=\"table-async-iterator-optional\" caption=\"Async Iterator Interface Optional Properties\">\n          <table>\n            <thead>\n              <tr>\n                <th>Property</th>\n                <th>Value</th>\n                <th>Requirements</th>\n              </tr>\n            </thead>\n            <tr>\n              <td><b>\"return\"</b></td>\n              <td>a function that returns a promise for an IteratorResult object</td>\n              <td>\n                <p>The returned promise, when fulfilled, must fulfill with an object that conforms to the IteratorResult interface. Invoking this method notifies the async iterator object that the caller does not intend to make any more <code>next</code> method calls to the async iterator. The returned promise will fulfill with an IteratorResult object which will typically have a <b>\"done\"</b> property whose value is <b>true</b>, and a <b>\"value\"</b> property with the value passed as the argument of the <code>return</code> method. However, this requirement is not enforced.</p>\n\n                <p>Additionally, the IteratorResult object that serves as a fulfillment value should have a <b>\"value\"</b> property whose value is not a promise (or \"thenable\"). If the argument value is used in the typical manner, then if it is a rejected promise, a promise rejected with the same reason should be returned; if it is a fulfilled promise, then its fulfillment value should be used as the <b>\"value\"</b> property of the returned promise's IteratorResult object fulfillment value. However, these requirements are also not enforced.</p>\n              </td>\n            </tr>\n            <tr>\n              <td><b>\"throw\"</b></td>\n              <td>a function that returns a promise for an IteratorResult object</td>\n              <td>\n                <p>The returned promise, when fulfilled, must fulfill with an object that conforms to the IteratorResult interface. Invoking this method notifies the async iterator object that the caller has detected an error condition. The argument may be used to identify the error condition and typically will be an exception object. A typical response is to return a rejected promise which rejects with the value passed as the argument.</p>\n\n                <p>If the returned promise is fulfilled, the IteratorResult object fulfillment value will typically have a <b>\"done\"</b> property whose value is <b>true</b>. Additionally, it should have a <b>\"value\"</b> property whose value is not a promise (or \"thenable\"), but this requirement is not enforced.</p>\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n        <emu-note>\n          <p>Typically callers of these methods should check for their existence before invoking them. Certain ECMAScript language features including <code>for</code>-<code>await</code>-<code>of</code> and <code>yield*</code> call these methods after performing an existence check.</p>\n        </emu-note>\n      ","1.1.5":"<p>The <dfn variants=\"IteratorResult object,IteratorResult objects\">IteratorResult interface</dfn> includes the properties listed in table-iteratorresult-interface-properties:</p>\n        <emu-table id=\"table-iteratorresult-interface-properties\" caption=\"IteratorResult Interface Properties\" oldids=\"table-55\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Property\n                </th>\n                <th>\n                  Value\n                </th>\n                <th>\n                  Requirements\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                <b>\"done\"</b>\n              </td>\n              <td>\n                a Boolean\n              </td>\n              <td>\n                This is the result status of an iterator <code>next</code> method call. If the end of the iterator was reached <b>\"done\"</b> is <b>true</b>. If the end was not reached <b>\"done\"</b> is <b>false</b> and a value is available. If a <b>\"done\"</b> property (either own or inherited) does not exist, it is considered to have the value <b>false</b>.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <b>\"value\"</b>\n              </td>\n              <td>\n                an ECMAScript language value\n              </td>\n              <td>\n                If done is <b>false</b>, this is the current iteration element value. If done is <b>true</b>, this is the return value of the iterator, if it supplied one. If the iterator does not have a return value, <b>\"value\"</b> is <b>undefined</b>. In that case, the <b>\"value\"</b> property may be absent from the conforming object if it does not inherit an explicit <b>\"value\"</b> property.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n      ","1.2":"<p>An <dfn variants=\"Iterator Helper objects\">Iterator Helper object</dfn> is an ordinary object that represents a lazy transformation of some specific source iterator object. There is not a named constructor for Iterator Helper objects. Instead, Iterator Helper objects are created by calling certain methods of Iterator instance objects.</p>\n\n      ","1.2.1":"<p>The <dfn><code class=\"emu-intrinsic\">%IteratorHelperPrototype%</code></dfn> object:</p>\n        <ul>\n          <li>has properties that are inherited by all Iterator Helper objects.</li>\n          <li>is an ordinary object.</li>\n          <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Iterator.prototype%</code>.</li>\n          <li>has the following properties:</li>\n        </ul>\n\n        ","1.2.1.1":"<emu-alg><ol><li>Return ? GeneratorResume(<b>this</b> value, <b>undefined</b>, <b>\"Iterator Helper\"</b>).</li></ol></emu-alg>\n        ","1.2.1.2":"<emu-alg><ol><li>Let <var>obj</var> be <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[UnderlyingIterators]]).</li><li>Assert: <var>obj</var> has a [[GeneratorState]] internal slot.</li><li>If <var>obj</var>.[[GeneratorState]] is <emu-const>suspended-start</emu-const>, then<ol><li>Set <var>obj</var>.[[GeneratorState]] to <emu-const>completed</emu-const>.</li><li>NOTE: Once a generator enters the completed state it never leaves it and its associated execution context is never resumed. Any execution state associated with <var>obj</var> can be discarded at this point.</li><li>Perform ? IteratorCloseAll(<var>obj</var>.[[UnderlyingIterators]], NormalCompletion(<emu-const>unused</emu-const>)).</li><li>Return CreateIteratorResultObject(<b>undefined</b>, <b>true</b>).</li></ol></li><li>Let <var>completion</var> be ReturnCompletion(<b>undefined</b>).</li><li>Return ? GeneratorResumeAbrupt(<var>obj</var>, <var>completion</var>, <b>\"Iterator Helper\"</b>).</li></ol></emu-alg>\n        ","1.2.1.3":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"Iterator Helper\"</b>.</p>\n          <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n        ","1.3":"","1.3.1":"<p>The <dfn>Iterator</dfn> constructor:</p>\n        <ul>\n          <li>is <dfn><code class=\"emu-intrinsic\">%Iterator%</code></dfn>.</li>\n          <li>is the initial value of the <b>\"Iterator\"</b> property of the global object.</li>\n          <li>is designed to be subclassable. It may be used as the value of an <b>extends</b> clause of a class definition.</li>\n        </ul>\n\n        ","1.3.1.1":"<p>This function performs the following steps when called:</p>\n          <emu-alg><ol><li>If NewTarget is either <b>undefined</b> or the active function object, throw a <b>TypeError</b> exception.</li><li>Return ? OrdinaryCreateFromConstructor(NewTarget, <b>\"<code class=\"emu-intrinsic\">%Iterator.prototype%</code>\"</b>).</li></ol></emu-alg>\n        ","1.3.2":"<p>The Iterator constructor:</p>\n        <ul>\n          <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n          <li>has the following properties:</li>\n        </ul>\n\n        ","1.3.2.1":"<emu-alg><ol><li>Let <var>iterables</var> be a new empty List.</li><li>For each element <var>item</var> of <var>items</var>, do<ol><li>If <var>item</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>method</var> be ? GetMethod(<var>item</var>, <code class=\"emu-intrinsic\">%Symbol.iterator%</code>).</li><li>If <var>method</var> is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Append the Record { [[OpenMethod]]: <var>method</var>, [[Iterable]]: <var>item</var> } to <var>iterables</var>.</li></ol></li><li>Let <var>closure</var> be a new Abstract Closure with no parameters that captures <var>iterables</var> and performs the following steps when called:<ol><li>For each Record <var>iterable</var> of <var>iterables</var>, do<ol><li>Let <var>iter</var> be ? Call(<var>iterable</var>.[[OpenMethod]], <var>iterable</var>.[[Iterable]]).</li><li>If <var>iter</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>iteratorRecord</var> be ? GetIteratorDirect(<var>iter</var>).</li><li>Let <var>innerAlive</var> be <b>true</b>.</li><li>Repeat, while <var>innerAlive</var> is <b>true</b>,<ol><li>Let <var>innerValue</var> be ? IteratorStepValue(<var>iteratorRecord</var>).</li><li>If <var>innerValue</var> is <emu-const>done</emu-const>, then<ol><li>Set <var>innerAlive</var> to <b>false</b>.</li></ol></li><li>Else,<ol><li>Let <var>completion</var> be Completion(Yield(<var>innerValue</var>)).</li><li>If <var>completion</var> is an abrupt completion, then<ol><li>Return ? IteratorClose(<var>iteratorRecord</var>, <var>completion</var>).</li></ol></li></ol></li></ol></li></ol></li><li>Return ReturnCompletion(<b>undefined</b>).</li></ol></li><li>Let <var>gen</var> be CreateIteratorFromClosure(<var>closure</var>, <b>\"Iterator Helper\"</b>, <code class=\"emu-intrinsic\">%IteratorHelperPrototype%</code>, « [[UnderlyingIterators]] »).</li><li>Set <var>gen</var>.[[UnderlyingIterators]] to a new empty List.</li><li>Return <var>gen</var>.</li></ol></emu-alg>\n        ","1.3.2.2":"<emu-alg><ol><li>Let <var>iteratorRecord</var> be ? GetIteratorFlattenable(<var>obj</var>, <emu-const>iterate-string-primitives</emu-const>).</li><li>Let <var>hasInstance</var> be ? OrdinaryHasInstance(<code class=\"emu-intrinsic\">%Iterator%</code>, <var>iteratorRecord</var>.[[Iterator]]).</li><li>If <var>hasInstance</var> is <b>true</b>, then<ol><li>Return <var>iteratorRecord</var>.[[Iterator]].</li></ol></li><li>Let <var>wrapper</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%WrapForValidIteratorPrototype%</code>, « [[Iterated]] »).</li><li>Set <var>wrapper</var>.[[Iterated]] to <var>iteratorRecord</var>.</li><li>Return <var>wrapper</var>.</li></ol></emu-alg>\n\n          ","1.3.2.2.1":"<p>The <dfn><code class=\"emu-intrinsic\">%WrapForValidIteratorPrototype%</code></dfn> object:</p>\n            <ul>\n              <li>is an ordinary object.</li>\n              <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Iterator.prototype%</code>.</li>\n            </ul>\n\n            ","1.3.2.2.1.1":"<emu-alg><ol><li>Let <var>obj</var> be <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[Iterated]]).</li><li>Let <var>iteratorRecord</var> be <var>obj</var>.[[Iterated]].</li><li>Return ? Call(<var>iteratorRecord</var>.[[NextMethod]], <var>iteratorRecord</var>.[[Iterator]]).</li></ol></emu-alg>\n            ","1.3.2.2.1.2":"<emu-alg><ol><li>Let <var>obj</var> be <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>obj</var>, [[Iterated]]).</li><li>Let <var>iterator</var> be <var>obj</var>.[[Iterated]].[[Iterator]].</li><li>Assert: <var>iterator</var> is an Object.</li><li>Let <var>returnMethod</var> be ? GetMethod(<var>iterator</var>, <b>\"return\"</b>).</li><li>If <var>returnMethod</var> is <b>undefined</b>, then<ol><li>Return CreateIteratorResultObject(<b>undefined</b>, <b>true</b>).</li></ol></li><li>Return ? Call(<var>returnMethod</var>, <var>iterator</var>).</li></ol></emu-alg>\n            ","1.3.2.3":"<p>The initial value of Iterator.prototype is the Iterator prototype object.</p>\n          <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n        ","1.3.3":"<p>The <dfn>Iterator prototype object</dfn>:</p>\n        <ul>\n          <li>is <dfn><code class=\"emu-intrinsic\">%Iterator.prototype%</code></dfn>.</li>\n          <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n          <li>is an ordinary object.</li>\n        </ul>\n        <emu-note>\n          <p>All objects defined in this specification that implement the iterator interface also inherit from <code class=\"emu-intrinsic\">%Iterator.prototype%</code>. ECMAScript code may also define objects that inherit from <code class=\"emu-intrinsic\">%Iterator.prototype%</code>. <code class=\"emu-intrinsic\">%Iterator.prototype%</code> provides a place where additional methods that are applicable to all iterator objects may be added.</p>\n          <p>The following expression is one way that ECMAScript code can access the <code class=\"emu-intrinsic\">%Iterator.prototype%</code> object:</p>\n          <pre><code class=\"javascript\">Object.getPrototypeOf(Object.getPrototypeOf([][Symbol.iterator]()))</code></pre>\n        </emu-note>\n\n        ","1.3.3.1":"<p><code>Iterator.prototype.constructor</code> is an accessor property with attributes { [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }. The [[Get]] and [[Set]] attributes are defined as follows:</p>\n\n          ","1.3.3.1.1":"<p>The value of the [[Get]] attribute is a built-in function that requires no arguments. It performs the following steps when called:</p>\n            <emu-alg><ol><li>Return <code class=\"emu-intrinsic\">%Iterator%</code>.</li></ol></emu-alg>\n          ","1.3.3.1.2":"<p>The value of the [[Set]] attribute is a built-in function that takes an argument <var>v</var>. It performs the following steps when called:</p>\n            <emu-alg><ol><li>Perform ? SetterThatIgnoresPrototypeProperties(<b>this</b> value, <code class=\"emu-intrinsic\">%Iterator.prototype%</code>, <b>\"constructor\"</b>, <var>v</var>).</li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n          \n\n          <emu-note>\n            <p>Unlike the <b>\"constructor\"</b> property on most built-in prototypes, for web-compatibility reasons this property must be an accessor.</p>\n          </emu-note>\n        ","1.3.3.2":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>iterated</var> be the Iterator Record { [[Iterator]]: <var>obj</var>, [[NextMethod]]: <b>undefined</b>, [[Done]]: <b>false</b> }.</li><li>Let <var>numLimit</var> be Completion(ToNumber(<var>limit</var>)).</li><li>IfAbruptCloseIterator(<var>numLimit</var>, <var>iterated</var>).</li><li>If <var>numLimit</var> is <b>NaN</b>, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>RangeError</b> object).</li><li>Return ? IteratorClose(<var>iterated</var>, <var>error</var>).</li></ol></li><li>Let <var>integerLimit</var> be ! ToIntegerOrInfinity(<var>numLimit</var>).</li><li>If <var>integerLimit</var> &lt; 0, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>RangeError</b> object).</li><li>Return ? IteratorClose(<var>iterated</var>, <var>error</var>).</li></ol></li><li>Set <var>iterated</var> to ? GetIteratorDirect(<var>obj</var>).</li><li>Let <var>closure</var> be a new Abstract Closure with no parameters that captures <var>iterated</var> and <var>integerLimit</var> and performs the following steps when called:<ol><li>Let <var>remaining</var> be <var>integerLimit</var>.</li><li>Repeat, while <var>remaining</var> > 0,<ol><li>If <var>remaining</var> ≠ +∞, then<ol><li>Set <var>remaining</var> to <var>remaining</var> - 1.</li></ol></li><li>Let <var>next</var> be ? IteratorStep(<var>iterated</var>).</li><li>If <var>next</var> is <emu-const>done</emu-const>, return ReturnCompletion(<b>undefined</b>).</li></ol></li><li>Repeat,<ol><li>Let <var>value</var> be ? IteratorStepValue(<var>iterated</var>).</li><li>If <var>value</var> is <emu-const>done</emu-const>, return ReturnCompletion(<b>undefined</b>).</li><li>Let <var>completion</var> be Completion(Yield(<var>value</var>)).</li><li>IfAbruptCloseIterator(<var>completion</var>, <var>iterated</var>).</li></ol></li></ol></li><li>Let <var>result</var> be CreateIteratorFromClosure(<var>closure</var>, <b>\"Iterator Helper\"</b>, <code class=\"emu-intrinsic\">%IteratorHelperPrototype%</code>, « [[UnderlyingIterators]] »).</li><li>Set <var>result</var>.[[UnderlyingIterators]] to « <var>iterated</var> ».</li><li>Return <var>result</var>.</li></ol></emu-alg>\n        ","1.3.3.3":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>iterated</var> be the Iterator Record { [[Iterator]]: <var>obj</var>, [[NextMethod]]: <b>undefined</b>, [[Done]]: <b>false</b> }.</li><li>If IsCallable(<var>predicate</var>) is <b>false</b>, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>TypeError</b> object).</li><li>Return ? IteratorClose(<var>iterated</var>, <var>error</var>).</li></ol></li><li>Set <var>iterated</var> to ? GetIteratorDirect(<var>obj</var>).</li><li>Let <var>counter</var> be 0.</li><li>Repeat,<ol><li>Let <var>value</var> be ? IteratorStepValue(<var>iterated</var>).</li><li>If <var>value</var> is <emu-const>done</emu-const>, return <b>true</b>.</li><li>Let <var>result</var> be Completion(Call(<var>predicate</var>, <b>undefined</b>, « <var>value</var>, 𝔽(<var>counter</var>) »)).</li><li>IfAbruptCloseIterator(<var>result</var>, <var>iterated</var>).</li><li>If ToBoolean(<var>result</var>) is <b>false</b>, return ? IteratorClose(<var>iterated</var>, NormalCompletion(<b>false</b>)).</li><li>Set <var>counter</var> to <var>counter</var> + 1.</li></ol></li></ol></emu-alg>\n        ","1.3.3.4":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>iterated</var> be the Iterator Record { [[Iterator]]: <var>obj</var>, [[NextMethod]]: <b>undefined</b>, [[Done]]: <b>false</b> }.</li><li>If IsCallable(<var>predicate</var>) is <b>false</b>, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>TypeError</b> object).</li><li>Return ? IteratorClose(<var>iterated</var>, <var>error</var>).</li></ol></li><li>Set <var>iterated</var> to ? GetIteratorDirect(<var>obj</var>).</li><li>Let <var>closure</var> be a new Abstract Closure with no parameters that captures <var>iterated</var> and <var>predicate</var> and performs the following steps when called:<ol><li>Let <var>counter</var> be 0.</li><li>Repeat,<ol><li>Let <var>value</var> be ? IteratorStepValue(<var>iterated</var>).</li><li>If <var>value</var> is <emu-const>done</emu-const>, return ReturnCompletion(<b>undefined</b>).</li><li>Let <var>selected</var> be Completion(Call(<var>predicate</var>, <b>undefined</b>, « <var>value</var>, 𝔽(<var>counter</var>) »)).</li><li>IfAbruptCloseIterator(<var>selected</var>, <var>iterated</var>).</li><li>If ToBoolean(<var>selected</var>) is <b>true</b>, then<ol><li>Let <var>completion</var> be Completion(Yield(<var>value</var>)).</li><li>IfAbruptCloseIterator(<var>completion</var>, <var>iterated</var>).</li></ol></li><li>Set <var>counter</var> to <var>counter</var> + 1.</li></ol></li></ol></li><li>Let <var>result</var> be CreateIteratorFromClosure(<var>closure</var>, <b>\"Iterator Helper\"</b>, <code class=\"emu-intrinsic\">%IteratorHelperPrototype%</code>, « [[UnderlyingIterators]] »).</li><li>Set <var>result</var>.[[UnderlyingIterators]] to « <var>iterated</var> ».</li><li>Return <var>result</var>.</li></ol></emu-alg>\n        ","1.3.3.5":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>iterated</var> be the Iterator Record { [[Iterator]]: <var>obj</var>, [[NextMethod]]: <b>undefined</b>, [[Done]]: <b>false</b> }.</li><li>If IsCallable(<var>predicate</var>) is <b>false</b>, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>TypeError</b> object).</li><li>Return ? IteratorClose(<var>iterated</var>, <var>error</var>).</li></ol></li><li>Set <var>iterated</var> to ? GetIteratorDirect(<var>obj</var>).</li><li>Let <var>counter</var> be 0.</li><li>Repeat,<ol><li>Let <var>value</var> be ? IteratorStepValue(<var>iterated</var>).</li><li>If <var>value</var> is <emu-const>done</emu-const>, return <b>undefined</b>.</li><li>Let <var>result</var> be Completion(Call(<var>predicate</var>, <b>undefined</b>, « <var>value</var>, 𝔽(<var>counter</var>) »)).</li><li>IfAbruptCloseIterator(<var>result</var>, <var>iterated</var>).</li><li>If ToBoolean(<var>result</var>) is <b>true</b>, return ? IteratorClose(<var>iterated</var>, NormalCompletion(<var>value</var>)).</li><li>Set <var>counter</var> to <var>counter</var> + 1.</li></ol></li></ol></emu-alg>\n        ","1.3.3.6":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>iterated</var> be the Iterator Record { [[Iterator]]: <var>obj</var>, [[NextMethod]]: <b>undefined</b>, [[Done]]: <b>false</b> }.</li><li>If IsCallable(<var>mapper</var>) is <b>false</b>, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>TypeError</b> object).</li><li>Return ? IteratorClose(<var>iterated</var>, <var>error</var>).</li></ol></li><li>Set <var>iterated</var> to ? GetIteratorDirect(<var>obj</var>).</li><li>Let <var>closure</var> be a new Abstract Closure with no parameters that captures <var>iterated</var> and <var>mapper</var> and performs the following steps when called:<ol><li>Let <var>counter</var> be 0.</li><li>Repeat,<ol><li>Let <var>value</var> be ? IteratorStepValue(<var>iterated</var>).</li><li>If <var>value</var> is <emu-const>done</emu-const>, return ReturnCompletion(<b>undefined</b>).</li><li>Let <var>mapped</var> be Completion(Call(<var>mapper</var>, <b>undefined</b>, « <var>value</var>, 𝔽(<var>counter</var>) »)).</li><li>IfAbruptCloseIterator(<var>mapped</var>, <var>iterated</var>).</li><li>Let <var>innerIterator</var> be Completion(GetIteratorFlattenable(<var>mapped</var>, <emu-const>reject-primitives</emu-const>)).</li><li>IfAbruptCloseIterator(<var>innerIterator</var>, <var>iterated</var>).</li><li>Let <var>innerAlive</var> be <b>true</b>.</li><li>Repeat, while <var>innerAlive</var> is <b>true</b>,<ol><li>Let <var>innerValue</var> be Completion(IteratorStepValue(<var>innerIterator</var>)).</li><li>IfAbruptCloseIterator(<var>innerValue</var>, <var>iterated</var>).</li><li>If <var>innerValue</var> is <emu-const>done</emu-const>, then<ol><li>Set <var>innerAlive</var> to <b>false</b>.</li></ol></li><li>Else,<ol><li>Let <var>completion</var> be Completion(Yield(<var>innerValue</var>)).</li><li>If <var>completion</var> is an abrupt completion, then<ol><li>Let <var>backupCompletion</var> be Completion(IteratorClose(<var>innerIterator</var>, <var>completion</var>)).</li><li>IfAbruptCloseIterator(<var>backupCompletion</var>, <var>iterated</var>).</li><li>Return ? IteratorClose(<var>iterated</var>, <var>completion</var>).</li></ol></li></ol></li></ol></li><li>Set <var>counter</var> to <var>counter</var> + 1.</li></ol></li></ol></li><li>Let <var>result</var> be CreateIteratorFromClosure(<var>closure</var>, <b>\"Iterator Helper\"</b>, <code class=\"emu-intrinsic\">%IteratorHelperPrototype%</code>, « [[UnderlyingIterators]] »).</li><li>Set <var>result</var>.[[UnderlyingIterators]] to « <var>iterated</var> ».</li><li>Return <var>result</var>.</li></ol></emu-alg>\n        ","1.3.3.7":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>iterated</var> be the Iterator Record { [[Iterator]]: <var>obj</var>, [[NextMethod]]: <b>undefined</b>, [[Done]]: <b>false</b> }.</li><li>If IsCallable(<var>procedure</var>) is <b>false</b>, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>TypeError</b> object).</li><li>Return ? IteratorClose(<var>iterated</var>, <var>error</var>).</li></ol></li><li>Set <var>iterated</var> to ? GetIteratorDirect(<var>obj</var>).</li><li>Let <var>counter</var> be 0.</li><li>Repeat,<ol><li>Let <var>value</var> be ? IteratorStepValue(<var>iterated</var>).</li><li>If <var>value</var> is <emu-const>done</emu-const>, return <b>undefined</b>.</li><li>Let <var>result</var> be Completion(Call(<var>procedure</var>, <b>undefined</b>, « <var>value</var>, 𝔽(<var>counter</var>) »)).</li><li>IfAbruptCloseIterator(<var>result</var>, <var>iterated</var>).</li><li>Set <var>counter</var> to <var>counter</var> + 1.</li></ol></li></ol></emu-alg>\n        ","1.3.3.8":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>iterated</var> be the Iterator Record { [[Iterator]]: <var>obj</var>, [[NextMethod]]: <b>undefined</b>, [[Done]]: <b>false</b> }.</li><li>If IsCallable(<var>mapper</var>) is <b>false</b>, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>TypeError</b> object).</li><li>Return ? IteratorClose(<var>iterated</var>, <var>error</var>).</li></ol></li><li>Set <var>iterated</var> to ? GetIteratorDirect(<var>obj</var>).</li><li>Let <var>closure</var> be a new Abstract Closure with no parameters that captures <var>iterated</var> and <var>mapper</var> and performs the following steps when called:<ol><li>Let <var>counter</var> be 0.</li><li>Repeat,<ol><li>Let <var>value</var> be ? IteratorStepValue(<var>iterated</var>).</li><li>If <var>value</var> is <emu-const>done</emu-const>, return ReturnCompletion(<b>undefined</b>).</li><li>Let <var>mapped</var> be Completion(Call(<var>mapper</var>, <b>undefined</b>, « <var>value</var>, 𝔽(<var>counter</var>) »)).</li><li>IfAbruptCloseIterator(<var>mapped</var>, <var>iterated</var>).</li><li>Let <var>completion</var> be Completion(Yield(<var>mapped</var>)).</li><li>IfAbruptCloseIterator(<var>completion</var>, <var>iterated</var>).</li><li>Set <var>counter</var> to <var>counter</var> + 1.</li></ol></li></ol></li><li>Let <var>result</var> be CreateIteratorFromClosure(<var>closure</var>, <b>\"Iterator Helper\"</b>, <code class=\"emu-intrinsic\">%IteratorHelperPrototype%</code>, « [[UnderlyingIterators]] »).</li><li>Set <var>result</var>.[[UnderlyingIterators]] to « <var>iterated</var> ».</li><li>Return <var>result</var>.</li></ol></emu-alg>\n        ","1.3.3.9":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>iterated</var> be the Iterator Record { [[Iterator]]: <var>obj</var>, [[NextMethod]]: <b>undefined</b>, [[Done]]: <b>false</b> }.</li><li>If IsCallable(<var>reducer</var>) is <b>false</b>, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>TypeError</b> object).</li><li>Return ? IteratorClose(<var>iterated</var>, <var>error</var>).</li></ol></li><li>Set <var>iterated</var> to ? GetIteratorDirect(<var>obj</var>).</li><li>If <var>initialValue</var> is not present, then<ol><li>Let <var>accumulator</var> be ? IteratorStepValue(<var>iterated</var>).</li><li>If <var>accumulator</var> is <emu-const>done</emu-const>, throw a <b>TypeError</b> exception.</li><li>Let <var>counter</var> be 1.</li></ol></li><li>Else,<ol><li>Let <var>accumulator</var> be <var>initialValue</var>.</li><li>Let <var>counter</var> be 0.</li></ol></li><li>Repeat,<ol><li>Let <var>value</var> be ? IteratorStepValue(<var>iterated</var>).</li><li>If <var>value</var> is <emu-const>done</emu-const>, return <var>accumulator</var>.</li><li>Let <var>result</var> be Completion(Call(<var>reducer</var>, <b>undefined</b>, « <var>accumulator</var>, <var>value</var>, 𝔽(<var>counter</var>) »)).</li><li>IfAbruptCloseIterator(<var>result</var>, <var>iterated</var>).</li><li>Set <var>accumulator</var> to <var>result</var>.</li><li>Set <var>counter</var> to <var>counter</var> + 1.</li></ol></li></ol></emu-alg>\n        ","1.3.3.10":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>iterated</var> be the Iterator Record { [[Iterator]]: <var>obj</var>, [[NextMethod]]: <b>undefined</b>, [[Done]]: <b>false</b> }.</li><li>If IsCallable(<var>predicate</var>) is <b>false</b>, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>TypeError</b> object).</li><li>Return ? IteratorClose(<var>iterated</var>, <var>error</var>).</li></ol></li><li>Set <var>iterated</var> to ? GetIteratorDirect(<var>obj</var>).</li><li>Let <var>counter</var> be 0.</li><li>Repeat,<ol><li>Let <var>value</var> be ? IteratorStepValue(<var>iterated</var>).</li><li>If <var>value</var> is <emu-const>done</emu-const>, return <b>false</b>.</li><li>Let <var>result</var> be Completion(Call(<var>predicate</var>, <b>undefined</b>, « <var>value</var>, 𝔽(<var>counter</var>) »)).</li><li>IfAbruptCloseIterator(<var>result</var>, <var>iterated</var>).</li><li>If ToBoolean(<var>result</var>) is <b>true</b>, return ? IteratorClose(<var>iterated</var>, NormalCompletion(<b>true</b>)).</li><li>Set <var>counter</var> to <var>counter</var> + 1.</li></ol></li></ol></emu-alg>\n        ","1.3.3.11":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>iterated</var> be the Iterator Record { [[Iterator]]: <var>obj</var>, [[NextMethod]]: <b>undefined</b>, [[Done]]: <b>false</b> }.</li><li>Let <var>numLimit</var> be Completion(ToNumber(<var>limit</var>)).</li><li>IfAbruptCloseIterator(<var>numLimit</var>, <var>iterated</var>).</li><li>If <var>numLimit</var> is <b>NaN</b>, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>RangeError</b> object).</li><li>Return ? IteratorClose(<var>iterated</var>, <var>error</var>).</li></ol></li><li>Let <var>integerLimit</var> be ! ToIntegerOrInfinity(<var>numLimit</var>).</li><li>If <var>integerLimit</var> &lt; 0, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>RangeError</b> object).</li><li>Return ? IteratorClose(<var>iterated</var>, <var>error</var>).</li></ol></li><li>Set <var>iterated</var> to ? GetIteratorDirect(<var>obj</var>).</li><li>Let <var>closure</var> be a new Abstract Closure with no parameters that captures <var>iterated</var> and <var>integerLimit</var> and performs the following steps when called:<ol><li>Let <var>remaining</var> be <var>integerLimit</var>.</li><li>Repeat,<ol><li>If <var>remaining</var> = 0, then<ol><li>Return ? IteratorClose(<var>iterated</var>, ReturnCompletion(<b>undefined</b>)).</li></ol></li><li>If <var>remaining</var> ≠ +∞, then<ol><li>Set <var>remaining</var> to <var>remaining</var> - 1.</li></ol></li><li>Let <var>value</var> be ? IteratorStepValue(<var>iterated</var>).</li><li>If <var>value</var> is <emu-const>done</emu-const>, return ReturnCompletion(<b>undefined</b>).</li><li>Let <var>completion</var> be Completion(Yield(<var>value</var>)).</li><li>IfAbruptCloseIterator(<var>completion</var>, <var>iterated</var>).</li></ol></li></ol></li><li>Let <var>result</var> be CreateIteratorFromClosure(<var>closure</var>, <b>\"Iterator Helper\"</b>, <code class=\"emu-intrinsic\">%IteratorHelperPrototype%</code>, « [[UnderlyingIterators]] »).</li><li>Set <var>result</var>.[[UnderlyingIterators]] to « <var>iterated</var> ».</li><li>Return <var>result</var>.</li></ol></emu-alg>\n        ","1.3.3.12":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>iterated</var> be ? GetIteratorDirect(<var>obj</var>).</li><li>Let <var>items</var> be a new empty List.</li><li>Repeat,<ol><li>Let <var>value</var> be ? IteratorStepValue(<var>iterated</var>).</li><li>If <var>value</var> is <emu-const>done</emu-const>, return CreateArrayFromList(<var>items</var>).</li><li>Append <var>value</var> to <var>items</var>.</li></ol></li></ol></emu-alg>\n        ","1.3.3.13":"<p>This function performs the following steps when called:</p>\n          <emu-alg><ol><li>Return the <b>this</b> value.</li></ol></emu-alg>\n          <p>The value of the <b>\"name\"</b> property of this function is <b>\"[Symbol.iterator]\"</b>.</p>\n        ","1.3.3.14":"<p><code>Iterator.prototype[<code class=\"emu-intrinsic\">%Symbol.toStringTag%</code>]</code> is an accessor property with attributes { [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }. The [[Get]] and [[Set]] attributes are defined as follows:</p>\n\n          ","1.3.3.14.1":"<p>The value of the [[Get]] attribute is a built-in function that requires no arguments. It performs the following steps when called:</p>\n            <emu-alg><ol><li>Return <b>\"Iterator\"</b>.</li></ol></emu-alg>\n          ","1.3.3.14.2":"<p>The value of the [[Set]] attribute is a built-in function that takes an argument <var>v</var>. It performs the following steps when called:</p>\n            <emu-alg><ol><li>Perform ? SetterThatIgnoresPrototypeProperties(<b>this</b> value, <code class=\"emu-intrinsic\">%Iterator.prototype%</code>, <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code>, <var>v</var>).</li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n          \n\n          <emu-note>\n            <p>Unlike the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property on most built-in prototypes, for web-compatibility reasons this property must be an accessor.</p>\n          </emu-note>\n        ","1.4":"<p>The <dfn><code class=\"emu-intrinsic\">%AsyncIteratorPrototype%</code></dfn> object:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n        <li>is an ordinary object.</li>\n      </ul>\n      <emu-note>\n        <p>All objects defined in this specification that implement the async iterator interface also inherit from <code class=\"emu-intrinsic\">%AsyncIteratorPrototype%</code>. ECMAScript code may also define objects that inherit from <code class=\"emu-intrinsic\">%AsyncIteratorPrototype%</code>. The <code class=\"emu-intrinsic\">%AsyncIteratorPrototype%</code> object provides a place where additional methods that are applicable to all async iterator objects may be added.</p>\n      </emu-note>\n\n      ","1.4.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Return the <b>this</b> value.</li></ol></emu-alg>\n        <p>The value of the <b>\"name\"</b> property of this function is <b>\"[Symbol.asyncIterator]\"</b>.</p>\n      ","1.5":"<p>An <dfn variants=\"Async-from-Sync Iterator objects\">Async-from-Sync Iterator object</dfn> is an async iterator that adapts a specific synchronous iterator. Async-from-Sync Iterator objects are never directly accessible to ECMAScript code. There is not a named constructor for Async-from-Sync Iterator objects. Instead, Async-from-Sync Iterator objects are created by the CreateAsyncFromSyncIterator abstract operation as needed.</p>\n\n      ","1.5.1":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It is used to create an async Iterator Record from a synchronous Iterator Record.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>asyncIterator</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%AsyncFromSyncIteratorPrototype%</code>, « [[SyncIteratorRecord]] »).</li><li>Set <var>asyncIterator</var>.[[SyncIteratorRecord]] to <var>syncIteratorRecord</var>.</li><li>Let <var>nextMethod</var> be ! Get(<var>asyncIterator</var>, <b>\"next\"</b>).</li><li>Let <var>iteratorRecord</var> be the Iterator Record { [[Iterator]]: <var>asyncIterator</var>, [[NextMethod]]: <var>nextMethod</var>, [[Done]]: <b>false</b> }.</li><li>Return <var>iteratorRecord</var>.</li></ol></emu-alg>\n      ","1.5.2":"<p>The <dfn><code class=\"emu-intrinsic\">%AsyncFromSyncIteratorPrototype%</code></dfn> object:</p>\n        <ul>\n          <li>has properties that are inherited by all Async-from-Sync Iterator objects.</li>\n          <li>is an ordinary object.</li>\n          <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%AsyncIteratorPrototype%</code>.</li>\n          <li>is never directly accessible to ECMAScript code.</li>\n          <li>has the following properties:</li>\n        </ul>\n\n        ","1.5.2.1":"<emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Assert: <var>obj</var> is an Object that has a [[SyncIteratorRecord]] internal slot.</li><li>Let <var>promiseCapability</var> be ! NewPromiseCapability(<code class=\"emu-intrinsic\">%Promise%</code>).</li><li>Let <var>syncIteratorRecord</var> be <var>obj</var>.[[SyncIteratorRecord]].</li><li>If <var>value</var> is present, then<ol><li>Let <var>result</var> be Completion(IteratorNext(<var>syncIteratorRecord</var>, <var>value</var>)).</li></ol></li><li>Else,<ol><li>Let <var>result</var> be Completion(IteratorNext(<var>syncIteratorRecord</var>)).</li></ol></li><li>IfAbruptRejectPromise(<var>result</var>, <var>promiseCapability</var>).</li><li>Return AsyncFromSyncIteratorContinuation(<var>result</var>, <var>promiseCapability</var>, <var>syncIteratorRecord</var>, <b>true</b>).</li></ol></emu-alg>\n        ","1.5.2.2":"<emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Assert: <var>obj</var> is an Object that has a [[SyncIteratorRecord]] internal slot.</li><li>Let <var>promiseCapability</var> be ! NewPromiseCapability(<code class=\"emu-intrinsic\">%Promise%</code>).</li><li>Let <var>syncIteratorRecord</var> be <var>obj</var>.[[SyncIteratorRecord]].</li><li>Let <var>syncIterator</var> be <var>syncIteratorRecord</var>.[[Iterator]].</li><li>Let <var>return</var> be Completion(GetMethod(<var>syncIterator</var>, <b>\"return\"</b>)).</li><li>IfAbruptRejectPromise(<var>return</var>, <var>promiseCapability</var>).</li><li>If <var>return</var> is <b>undefined</b>, then<ol><li>Let <var>iteratorResult</var> be CreateIteratorResultObject(<var>value</var>, <b>true</b>).</li><li>Perform ! <emu-meta effects=\"user-code\">Call(<var>promiseCapability</var>.[[Resolve]], <b>undefined</b>, « <var>iteratorResult</var> »)</emu-meta>.</li><li>Return <var>promiseCapability</var>.[[Promise]].</li></ol></li><li>If <var>value</var> is present, then<ol><li>Let <var>result</var> be Completion(Call(<var>return</var>, <var>syncIterator</var>, « <var>value</var> »)).</li></ol></li><li>Else,<ol><li>Let <var>result</var> be Completion(Call(<var>return</var>, <var>syncIterator</var>)).</li></ol></li><li>IfAbruptRejectPromise(<var>result</var>, <var>promiseCapability</var>).</li><li>If <var>result</var> is not an Object, then<ol><li>Perform ! Call(<var>promiseCapability</var>.[[Reject]], <b>undefined</b>, « a newly created <b>TypeError</b> object »).</li><li>Return <var>promiseCapability</var>.[[Promise]].</li></ol></li><li>Return AsyncFromSyncIteratorContinuation(<var>result</var>, <var>promiseCapability</var>, <var>syncIteratorRecord</var>, <b>false</b>).</li></ol></emu-alg>\n        ","1.5.2.3":"<emu-note>In this specification, <var>value</var> is always provided, but is left optional for consistency with 27.1.5.2.2.</emu-note>\n\n          <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>Assert: <var>obj</var> is an Object that has a [[SyncIteratorRecord]] internal slot.</li><li>Let <var>promiseCapability</var> be ! NewPromiseCapability(<code class=\"emu-intrinsic\">%Promise%</code>).</li><li>Let <var>syncIteratorRecord</var> be <var>obj</var>.[[SyncIteratorRecord]].</li><li>Let <var>syncIterator</var> be <var>syncIteratorRecord</var>.[[Iterator]].</li><li>Let <var>throw</var> be Completion(GetMethod(<var>syncIterator</var>, <b>\"throw\"</b>)).</li><li>IfAbruptRejectPromise(<var>throw</var>, <var>promiseCapability</var>).</li><li>If <var>throw</var> is <b>undefined</b>, then<ol><li>NOTE: If <var>syncIterator</var> does not have a <code>throw</code> method, close it to give it a chance to clean up before we reject the capability.</li><li>Let <var>closeCompletion</var> be NormalCompletion(<emu-const>empty</emu-const>).</li><li>Let <var>result</var> be Completion(IteratorClose(<var>syncIteratorRecord</var>, <var>closeCompletion</var>)).</li><li>IfAbruptRejectPromise(<var>result</var>, <var>promiseCapability</var>).</li><li>NOTE: The next step throws a <b>TypeError</b> to indicate that there was a protocol violation: <var>syncIterator</var> does not have a <code>throw</code> method.</li><li>NOTE: If closing <var>syncIterator</var> does not throw then the result of that operation is ignored, even if it yields a rejected promise.</li><li>Perform ! Call(<var>promiseCapability</var>.[[Reject]], <b>undefined</b>, « a newly created <b>TypeError</b> object »).</li><li>Return <var>promiseCapability</var>.[[Promise]].</li></ol></li><li>If <var>value</var> is present, then<ol><li>Let <var>result</var> be Completion(Call(<var>throw</var>, <var>syncIterator</var>, « <var>value</var> »)).</li></ol></li><li>Else,<ol><li>Let <var>result</var> be Completion(Call(<var>throw</var>, <var>syncIterator</var>)).</li></ol></li><li>IfAbruptRejectPromise(<var>result</var>, <var>promiseCapability</var>).</li><li>If <var>result</var> is not an Object, then<ol><li>Perform ! Call(<var>promiseCapability</var>.[[Reject]], <b>undefined</b>, « a newly created <b>TypeError</b> object »).</li><li>Return <var>promiseCapability</var>.[[Promise]].</li></ol></li><li>Return AsyncFromSyncIteratorContinuation(<var>result</var>, <var>promiseCapability</var>, <var>syncIteratorRecord</var>, <b>true</b>).</li></ol></emu-alg>\n        ","1.5.3":"<p>Async-from-Sync Iterator instances are ordinary objects that inherit properties from the <code class=\"emu-intrinsic\">%AsyncFromSyncIteratorPrototype%</code> intrinsic object. Async-from-Sync Iterator instances are initially created with the internal slots listed in table-async-from-sync-iterator-internal-slots.</p>\n        <emu-table id=\"table-async-from-sync-iterator-internal-slots\" caption=\"Internal Slots of Async-from-Sync Iterator Instances\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Internal Slot\n                </th>\n                <th>\n                  Type\n                </th>\n                <th>\n                  Description\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                [[SyncIteratorRecord]]\n              </td>\n              <td>\n                an Iterator Record\n              </td>\n              <td>\n                Represents the original synchronous iterator which is being adapted.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n      ","1.5.4":"<dl class=\"header\">\n        </dl>\n\n        <emu-alg><ol><li>NOTE: Because <var>promiseCapability</var> is derived from the intrinsic <code class=\"emu-intrinsic\">%Promise%</code>, the calls to <var>promiseCapability</var>.[[Reject]] entailed by the use IfAbruptRejectPromise below are guaranteed not to throw.</li><li>Let <var>done</var> be Completion(IteratorComplete(<var>result</var>)).</li><li>IfAbruptRejectPromise(<var>done</var>, <var>promiseCapability</var>).</li><li>Let <var>value</var> be Completion(IteratorValue(<var>result</var>)).</li><li>IfAbruptRejectPromise(<var>value</var>, <var>promiseCapability</var>).</li><li>Let <var>valueWrapper</var> be Completion(PromiseResolve(<code class=\"emu-intrinsic\">%Promise%</code>, <var>value</var>)).</li><li>If <var>valueWrapper</var> is an abrupt completion, <var>done</var> is <b>false</b>, and <var>closeOnRejection</var> is <b>true</b>, then<ol><li>Set <var>valueWrapper</var> to Completion(IteratorClose(<var>syncIteratorRecord</var>, <var>valueWrapper</var>)).</li></ol></li><li>IfAbruptRejectPromise(<var>valueWrapper</var>, <var>promiseCapability</var>).</li><li>Let <var>unwrap</var> be a new Abstract Closure with parameters (<var>v</var>) that captures <var>done</var> and performs the following steps when called:<ol><li>Return CreateIteratorResultObject(<var>v</var>, <var>done</var>).</li></ol></li><li>Let <var>onFulfilled</var> be CreateBuiltinFunction(<var>unwrap</var>, 1, <b>\"\"</b>, « »).</li><li>NOTE: <var>onFulfilled</var> is used when processing the <b>\"value\"</b> property of an IteratorResult object in order to wait for its value if it is a promise and re-package the result in a new \"unwrapped\" IteratorResult object.</li><li>If <var>done</var> is <b>true</b> or <var>closeOnRejection</var> is <b>false</b>, then<ol><li>Let <var>onRejected</var> be <b>undefined</b>.</li></ol></li><li>Else,<ol><li>Let <var>closeIterator</var> be a new Abstract Closure with parameters (<var>error</var>) that captures <var>syncIteratorRecord</var> and performs the following steps when called:<ol><li>Return ? IteratorClose(<var>syncIteratorRecord</var>, ThrowCompletion(<var>error</var>)).</li></ol></li><li>Let <var>onRejected</var> be CreateBuiltinFunction(<var>closeIterator</var>, 1, <b>\"\"</b>, « »).</li><li>NOTE: <var>onRejected</var> is used to close the Iterator when the <b>\"value\"</b> property of an IteratorResult object it yields is a rejected promise.</li></ol></li><li>Perform PerformPromiseThen(<var>valueWrapper</var>, <var>onFulfilled</var>, <var>onRejected</var>, <var>promiseCapability</var>).</li><li>Return <var>promiseCapability</var>.[[Promise]].</li></ol></emu-alg>\n      ","2.1":"","2.1.1":"<p>A <dfn variants=\"PromiseCapability Records\">PromiseCapability Record</dfn> is a Record value used to encapsulate a Promise or promise-like object along with the functions that are capable of resolving or rejecting that promise. PromiseCapability Records are produced by the NewPromiseCapability abstract operation.</p>\n        <p>PromiseCapability Records have the fields listed in table-promisecapability-record-fields.</p>\n        <emu-table id=\"table-promisecapability-record-fields\" caption=\"PromiseCapability Record Fields\" oldids=\"table-57\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Field Name\n                </th>\n                <th>\n                  Value\n                </th>\n                <th>\n                  Meaning\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                [[Promise]]\n              </td>\n              <td>\n                an Object\n              </td>\n              <td>\n                An object that is usable as a promise.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[Resolve]]\n              </td>\n              <td>\n                a function object\n              </td>\n              <td>\n                The function that is used to resolve the given promise.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[Reject]]\n              </td>\n              <td>\n                a function object\n              </td>\n              <td>\n                The function that is used to reject the given promise.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n\n        ","2.1.1.1":"<p>IfAbruptRejectPromise is a shorthand for a sequence of algorithm steps that use a PromiseCapability Record. An algorithm step of the form:</p>\n          <emu-alg><ol><li>IfAbruptRejectPromise(<var>value</var>, <var>capability</var>).</li></ol></emu-alg>\n          <p>means the same thing as:</p>\n          <emu-alg><ol><li>Assert: <var>value</var> is a Completion Record.</li><li>If <var>value</var> is an abrupt completion, then<ol><li>Perform ? Call(<var>capability</var>.[[Reject]], <b>undefined</b>, « <var>value</var>.[[Value]] »).</li><li>Return <var>capability</var>.[[Promise]].</li></ol></li><li>Set <var>value</var> to ! <var>value</var>.</li></ol></emu-alg>\n        ","2.1.2":"<p>A <dfn variants=\"PromiseReaction Records\">PromiseReaction Record</dfn> is a Record value used to store information about how a promise should react when it becomes resolved or rejected with a given value. PromiseReaction Records are created by the PerformPromiseThen abstract operation, and are used by the Abstract Closure returned by NewPromiseReactionJob.</p>\n        <p>PromiseReaction Records have the fields listed in table-promisereaction-record-fields.</p>\n        <emu-table id=\"table-promisereaction-record-fields\" caption=\"PromiseReaction Record Fields\" oldids=\"table-58\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Field Name\n                </th>\n                <th>\n                  Value\n                </th>\n                <th>\n                  Meaning\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                [[Capability]]\n              </td>\n              <td>\n                a PromiseCapability Record or <b>undefined</b>\n              </td>\n              <td>\n                The capabilities of the promise for which this record provides a reaction handler.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[Type]]\n              </td>\n              <td>\n                <emu-const>fulfill</emu-const> or <emu-const>reject</emu-const>\n              </td>\n              <td>\n                The [[Type]] is used when [[Handler]] is <emu-const>empty</emu-const> to allow for behaviour specific to the settlement type.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[Handler]]\n              </td>\n              <td>\n                a JobCallback Record or <emu-const>empty</emu-const>\n              </td>\n              <td>\n                The function that should be applied to the incoming value, and whose return value will govern what happens to the derived promise. If [[Handler]] is <emu-const>empty</emu-const>, a function that depends on the value of [[Type]] will be used instead.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n      ","2.1.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>promiseOrEmpty</var> be the Record { [[Value]]: <var>toResolve</var> }.</li><li>Let <var>resolveSteps</var> be a new Abstract Closure with parameters (<var>resolution</var>) that captures <var>promiseOrEmpty</var> and performs the following steps when called:<ol><li>If <var>promiseOrEmpty</var>.[[Value]] is <emu-const>empty</emu-const>, return <b>undefined</b>.</li><li>Let <var>promise</var> be <var>promiseOrEmpty</var>.[[Value]].</li><li>Set <var>promiseOrEmpty</var>.[[Value]] to <emu-const>empty</emu-const>.</li><li>If SameValue(<var>resolution</var>, <var>promise</var>) is <b>true</b>, then<ol><li>Let <var>selfResolutionError</var> be a newly created <b>TypeError</b> object.</li><li>Perform RejectPromise(<var>promise</var>, <var>selfResolutionError</var>).</li><li>Return <b>undefined</b>.</li></ol></li><li>If <var>resolution</var> is not an Object, then<ol><li>Perform FulfillPromise(<var>promise</var>, <var>resolution</var>).</li><li>Return <b>undefined</b>.</li></ol></li><li>Let <var>then</var> be Completion(Get(<var>resolution</var>, <b>\"then\"</b>)).</li><li>If <var>then</var> is an abrupt completion, then<ol><li>Perform RejectPromise(<var>promise</var>, <var>then</var>.[[Value]]).</li><li>Return <b>undefined</b>.</li></ol></li><li>Let <var>thenAction</var> be <var>then</var>.[[Value]].</li><li>If IsCallable(<var>thenAction</var>) is <b>false</b>, then<ol><li>Perform FulfillPromise(<var>promise</var>, <var>resolution</var>).</li><li>Return <b>undefined</b>.</li></ol></li><li>Let <var>thenJobCallback</var> be HostMakeJobCallback(<var>thenAction</var>).</li><li>Let <var>job</var> be NewPromiseResolveThenableJob(<var>promise</var>, <var>resolution</var>, <var>thenJobCallback</var>).</li><li>Perform HostEnqueuePromiseJob(<var>job</var>.[[Job]], <var>job</var>.[[Realm]]).</li><li>Return <b>undefined</b>.</li></ol></li><li>Let <var>resolve</var> be CreateBuiltinFunction(<var>resolveSteps</var>, 1, <b>\"\"</b>, « »).</li><li>Let <var>rejectSteps</var> be a new Abstract Closure with parameters (<var>reason</var>) that captures <var>promiseOrEmpty</var> and performs the following steps when called:<ol><li>If <var>promiseOrEmpty</var>.[[Value]] is <emu-const>empty</emu-const>, return <b>undefined</b>.</li><li>Let <var>promise</var> be <var>promiseOrEmpty</var>.[[Value]].</li><li>Set <var>promiseOrEmpty</var>.[[Value]] to <emu-const>empty</emu-const>.</li><li>Perform RejectPromise(<var>promise</var>, <var>reason</var>).</li><li>Return <b>undefined</b>.</li></ol></li><li>Let <var>reject</var> be CreateBuiltinFunction(<var>rejectSteps</var>, 1, <b>\"\"</b>, « »).</li><li>Return the Record { [[Resolve]]: <var>resolve</var>, [[Reject]]: <var>reject</var> }.</li></ol></emu-alg>\n      ","2.1.4":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: <var>promise</var>.[[PromiseState]] is <emu-const>pending</emu-const>.</li><li>Let <var>reactions</var> be <var>promise</var>.[[PromiseFulfillReactions]].</li><li>Set <var>promise</var>.[[PromiseResult]] to <var>value</var>.</li><li>Set <var>promise</var>.[[PromiseFulfillReactions]] to <b>undefined</b>.</li><li>Set <var>promise</var>.[[PromiseRejectReactions]] to <b>undefined</b>.</li><li>Set <var>promise</var>.[[PromiseState]] to <emu-const>fulfilled</emu-const>.</li><li>Perform TriggerPromiseReactions(<var>reactions</var>, <var>value</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","2.1.5":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It attempts to use <var>constructor</var> as a constructor in the fashion of the built-in Promise constructor to create a promise and extract its <code>resolve</code> and <code>reject</code> functions. The promise plus the <code>resolve</code> and <code>reject</code> functions are used to initialize a new PromiseCapability Record.</dd>\n        </dl>\n        <emu-alg><ol><li>If IsConstructor(<var>constructor</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>NOTE: <var>constructor</var> is assumed to be a constructor function that supports the parameter conventions of the Promise constructor (see 27.2.3.1).</li><li>Let <var>resolvingFunctions</var> be the Record { [[Resolve]]: <b>undefined</b>, [[Reject]]: <b>undefined</b> }.</li><li>Let <var>executorClosure</var> be a new Abstract Closure with parameters (<var>resolve</var>, <var>reject</var>) that captures <var>resolvingFunctions</var> and performs the following steps when called:<ol><li>If <var>resolvingFunctions</var>.[[Resolve]] is not <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>If <var>resolvingFunctions</var>.[[Reject]] is not <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Set <var>resolvingFunctions</var>.[[Resolve]] to <var>resolve</var>.</li><li>Set <var>resolvingFunctions</var>.[[Reject]] to <var>reject</var>.</li><li>Return NormalCompletion(<b>undefined</b>).</li></ol></li><li>Let <var>executor</var> be CreateBuiltinFunction(<var>executorClosure</var>, 2, <b>\"\"</b>, « »).</li><li>Let <var>promise</var> be ? Construct(<var>constructor</var>, « <var>executor</var> »).</li><li>If IsCallable(<var>resolvingFunctions</var>.[[Resolve]]) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>If IsCallable(<var>resolvingFunctions</var>.[[Reject]]) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return the PromiseCapability Record { [[Promise]]: <var>promise</var>, [[Resolve]]: <var>resolvingFunctions</var>.[[Resolve]], [[Reject]]: <var>resolvingFunctions</var>.[[Reject]] }.</li></ol></emu-alg>\n        <emu-note>\n          <p>This abstract operation supports Promise subclassing, as it is generic on any constructor that calls a passed executor function argument in the same way as the Promise constructor. It is used to generalize static methods of the Promise constructor to any subclass.</p>\n        </emu-note>\n      ","2.1.6":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It checks for the promise brand on an object.</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>value</var> is not an Object, return <b>false</b>.</li><li>If <var>value</var> does not have a [[PromiseState]] internal slot, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n      ","2.1.7":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: <var>promise</var>.[[PromiseState]] is <emu-const>pending</emu-const>.</li><li>Let <var>reactions</var> be <var>promise</var>.[[PromiseRejectReactions]].</li><li>Set <var>promise</var>.[[PromiseResult]] to <var>reason</var>.</li><li>Set <var>promise</var>.[[PromiseFulfillReactions]] to <b>undefined</b>.</li><li>Set <var>promise</var>.[[PromiseRejectReactions]] to <b>undefined</b>.</li><li>Set <var>promise</var>.[[PromiseState]] to <emu-const>rejected</emu-const>.</li><li>If <var>promise</var>.[[PromiseIsHandled]] is <b>false</b>, perform HostPromiseRejectionTracker(<var>promise</var>, <b>\"reject\"</b>).</li><li>Perform TriggerPromiseReactions(<var>reactions</var>, <var>reason</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","2.1.8":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It enqueues a new Job for each record in <var>reactions</var>. Each such Job processes the [[Type]] and [[Handler]] of the PromiseReaction Record, and if the [[Handler]] is not <emu-const>empty</emu-const>, calls it passing the given argument. If the [[Handler]] is <emu-const>empty</emu-const>, the behaviour is determined by the [[Type]].</dd>\n        </dl>\n        <emu-alg><ol><li>For each element <var>reaction</var> of <var>reactions</var>, do<ol><li>Let <var>job</var> be NewPromiseReactionJob(<var>reaction</var>, <var>argument</var>).</li><li>Perform HostEnqueuePromiseJob(<var>job</var>.[[Job]], <var>job</var>.[[Realm]]).</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","2.1.9":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It allows host environments to track promise rejections.</dd>\n        </dl>\n        <p>The default implementation of HostPromiseRejectionTracker is to return <emu-const>unused</emu-const>.</p>\n\n        <emu-note>\n          <p>HostPromiseRejectionTracker is called in two scenarios:</p>\n\n          <ul>\n            <li>When a promise is rejected without any handlers, it is called with its <var>operation</var> argument set to <b>\"reject\"</b>.</li>\n            <li>When a handler is added to a rejected promise for the first time, it is called with its <var>operation</var> argument set to <b>\"handle\"</b>.</li>\n          </ul>\n\n          <p>A typical implementation of HostPromiseRejectionTracker might try to notify developers of unhandled rejections, while also being careful to notify them if such previous notifications are later invalidated by new handlers being attached.</p>\n        </emu-note>\n\n        <emu-note>\n          <p>If <var>operation</var> is <b>\"handle\"</b>, an implementation should not hold a reference to <var>promise</var> in a way that would interfere with garbage collection. An implementation may hold a reference to <var>promise</var> if <var>operation</var> is <b>\"reject\"</b>, since it is expected that rejections will be rare and not on hot code paths.</p>\n        </emu-note>\n      ","2.2":"","2.2.1":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns a new Job Abstract Closure that applies the appropriate handler to the incoming value, and uses the handler's return value to resolve or reject the derived promise associated with that handler.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>job</var> be a new Job Abstract Closure with no parameters that captures <var>reaction</var> and <var>argument</var> and performs the following steps when called:<ol><li>Let <var>promiseCapability</var> be <var>reaction</var>.[[Capability]].</li><li>Let <var>type</var> be <var>reaction</var>.[[Type]].</li><li>Let <var>handler</var> be <var>reaction</var>.[[Handler]].</li><li>If <var>handler</var> is <emu-const>empty</emu-const>, then<ol><li>If <var>type</var> is <emu-const>fulfill</emu-const>, then<ol><li>Let <var>handlerResult</var> be NormalCompletion(<var>argument</var>).</li></ol></li><li>Else,<ol><li>Assert: <var>type</var> is <emu-const>reject</emu-const>.</li><li>Let <var>handlerResult</var> be ThrowCompletion(<var>argument</var>).</li></ol></li></ol></li><li>Else,<ol><li>Let <var>handlerResult</var> be Completion(HostCallJobCallback(<var>handler</var>, <b>undefined</b>, « <var>argument</var> »)).</li></ol></li><li>If <var>promiseCapability</var> is <b>undefined</b>, then<ol><li>Assert: <var>handlerResult</var> is not an abrupt completion.</li><li>Return <emu-const>empty</emu-const>.</li></ol></li><li>Assert: <var>promiseCapability</var> is a PromiseCapability Record.</li><li>If <var>handlerResult</var> is an abrupt completion, then<ol><li>Return ? Call(<var>promiseCapability</var>.[[Reject]], <b>undefined</b>, « <var>handlerResult</var>.[[Value]] »).</li></ol></li><li>Return ? Call(<var>promiseCapability</var>.[[Resolve]], <b>undefined</b>, « <var>handlerResult</var>.[[Value]] »).</li></ol></li><li>Let <var>handlerRealm</var> be <b>null</b>.</li><li>If <var>reaction</var>.[[Handler]] is not <emu-const>empty</emu-const>, then<ol><li>Let <var>getHandlerRealmResult</var> be Completion(GetFunctionRealm(<var>reaction</var>.[[Handler]].[[Callback]])).</li><li>If <var>getHandlerRealmResult</var> is a normal completion, set <var>handlerRealm</var> to <var>getHandlerRealmResult</var>.[[Value]].</li><li>Else, set <var>handlerRealm</var> to the current Realm Record.</li><li>NOTE: <var>handlerRealm</var> is never <b>null</b> unless the handler is <b>undefined</b>. When the handler is a revoked Proxy and no ECMAScript code runs, <var>handlerRealm</var> is used to create error objects.</li></ol></li><li>Return the Record { [[Job]]: <var>job</var>, [[Realm]]: <var>handlerRealm</var> }.</li></ol></emu-alg>\n      ","2.2.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>job</var> be a new Job Abstract Closure with no parameters that captures <var>promiseToResolve</var>, <var>thenable</var>, and <var>then</var> and performs the following steps when called:<ol><li>Let <var>resolvingFunctions</var> be CreateResolvingFunctions(<var>promiseToResolve</var>).</li><li>Let <var>thenCallResult</var> be Completion(HostCallJobCallback(<var>then</var>, <var>thenable</var>, « <var>resolvingFunctions</var>.[[Resolve]], <var>resolvingFunctions</var>.[[Reject]] »)).</li><li>If <var>thenCallResult</var> is an abrupt completion, then<ol><li>Return ? Call(<var>resolvingFunctions</var>.[[Reject]], <b>undefined</b>, « <var>thenCallResult</var>.[[Value]] »).</li></ol></li><li>Return ! <var>thenCallResult</var>.</li></ol></li><li>Let <var>getThenRealmResult</var> be Completion(GetFunctionRealm(<var>then</var>.[[Callback]])).</li><li>If <var>getThenRealmResult</var> is a normal completion, let <var>thenRealm</var> be <var>getThenRealmResult</var>.[[Value]].</li><li>Else, let <var>thenRealm</var> be the current Realm Record.</li><li>NOTE: <var>thenRealm</var> is never <b>null</b>. When <var>then</var>.[[Callback]] is a revoked Proxy and no code runs, <var>thenRealm</var> is used to create error objects.</li><li>Return the Record { [[Job]]: <var>job</var>, [[Realm]]: <var>thenRealm</var> }.</li></ol></emu-alg>\n        <emu-note>\n          <p>This Job uses the supplied thenable and its <code>then</code> method to resolve the given promise. This process must take place as a Job to ensure that the evaluation of the <code>then</code> method occurs after evaluation of any surrounding code has completed.</p>\n        </emu-note>\n      ","2.3":"<p>The Promise constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Promise%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"Promise\"</b> property of the global object.</li>\n        <li>creates and initializes a new Promise when called as a constructor.</li>\n        <li>is not intended to be called as a function and will throw an exception when called in that manner.</li>\n        <li>may be used as the value in an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified Promise behaviour must include a <code>super</code> call to the Promise constructor to create and initialize the subclass instance with the internal state necessary to support the <code>Promise</code> and <code>Promise.prototype</code> built-in methods.</li>\n      </ul>\n\n      ","2.3.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>If IsCallable(<var>executor</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>promise</var> be ? OrdinaryCreateFromConstructor(NewTarget, <b>\"<code class=\"emu-intrinsic\">%Promise.prototype%</code>\"</b>, « [[PromiseState]], [[PromiseResult]], [[PromiseFulfillReactions]], [[PromiseRejectReactions]], [[PromiseIsHandled]] »).</li><li>Set <var>promise</var>.[[PromiseState]] to <emu-const>pending</emu-const>.</li><li>Set <var>promise</var>.[[PromiseResult]] to <emu-const>empty</emu-const>.</li><li>Set <var>promise</var>.[[PromiseFulfillReactions]] to a new empty List.</li><li>Set <var>promise</var>.[[PromiseRejectReactions]] to a new empty List.</li><li>Set <var>promise</var>.[[PromiseIsHandled]] to <b>false</b>.</li><li>Let <var>resolvingFunctions</var> be CreateResolvingFunctions(<var>promise</var>).</li><li>Let <var>completion</var> be Completion(Call(<var>executor</var>, <b>undefined</b>, « <var>resolvingFunctions</var>.[[Resolve]], <var>resolvingFunctions</var>.[[Reject]] »)).</li><li>If <var>completion</var> is an abrupt completion, then<ol><li>Perform ? Call(<var>resolvingFunctions</var>.[[Reject]], <b>undefined</b>, « <var>completion</var>.[[Value]] »).</li></ol></li><li>Return <var>promise</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The <var>executor</var> argument must be a function object. It is called for initiating and reporting completion of the possibly deferred action represented by this Promise. The executor is called with two arguments: <var>resolve</var> and <var>reject</var>. These are functions that may be used by the <var>executor</var> function to report eventual completion or failure of the deferred computation. Returning from the executor function does not mean that the deferred action has been completed but only that the request to eventually perform the deferred action has been accepted.</p>\n          <p>The <var>resolve</var> function that is passed to an <var>executor</var> function accepts a single argument. The <var>executor</var> code may eventually call the <var>resolve</var> function to indicate that it wishes to resolve the associated Promise. The argument passed to the <var>resolve</var> function represents the eventual value of the deferred action and can be either the actual fulfillment value or another promise which will provide the value if it is fulfilled.</p>\n          <p>The <var>reject</var> function that is passed to an <var>executor</var> function accepts a single argument. The <var>executor</var> code may eventually call the <var>reject</var> function to indicate that the associated Promise is rejected and will never be fulfilled. The argument passed to the <var>reject</var> function is used as the rejection value of the promise. Typically it will be an Error object.</p>\n          <p>The resolve and reject functions passed to an <var>executor</var> function by the Promise constructor have the capability to actually resolve and reject the associated promise. Subclasses may have different constructor behaviour that passes in customized values for resolve and reject.</p>\n        </emu-note>\n      ","2.4":"<p>The Promise constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","2.4.1":"<p>This function returns a new promise which is fulfilled with an array of fulfillment values for the passed promises, or rejects with the reason of the first passed promise that rejects. It resolves all elements of the passed iterable to promises as it runs this algorithm.</p>\n        <emu-alg><ol><li>Let <var>constructor</var> be the <b>this</b> value.</li><li>Let <var>promiseCapability</var> be ? NewPromiseCapability(<var>constructor</var>).</li><li>Let <var>promiseResolve</var> be Completion(GetPromiseResolve(<var>constructor</var>)).</li><li>IfAbruptRejectPromise(<var>promiseResolve</var>, <var>promiseCapability</var>).</li><li>Let <var>iteratorRecord</var> be Completion(GetIterator(<var>iterable</var>, <emu-const>sync</emu-const>)).</li><li>IfAbruptRejectPromise(<var>iteratorRecord</var>, <var>promiseCapability</var>).</li><li>Let <var>result</var> be Completion(PerformPromiseAll(<var>iteratorRecord</var>, <var>constructor</var>, <var>promiseCapability</var>, <var>promiseResolve</var>)).</li><li>If <var>result</var> is an abrupt completion, then<ol><li>If <var>iteratorRecord</var>.[[Done]] is <b>false</b>, set <var>result</var> to Completion(IteratorClose(<var>iteratorRecord</var>, <var>result</var>)).</li><li>IfAbruptRejectPromise(<var>result</var>, <var>promiseCapability</var>).</li></ol></li><li>Return ! <var>result</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>This function requires its <b>this</b> value to be a constructor function that supports the parameter conventions of the Promise constructor.</p>\n        </emu-note>\n\n        ","2.4.1.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>promiseResolve</var> be ? Get(<var>promiseConstructor</var>, <b>\"resolve\"</b>).</li><li>If IsCallable(<var>promiseResolve</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return <var>promiseResolve</var>.</li></ol></emu-alg>\n        ","2.4.1.2":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>values</var> be a new empty List.</li><li>NOTE: <var>remainingElementsCount</var> starts at 1 instead of 0 to ensure <var>resultCapability</var>.[[Resolve]] is only called once, even in the presence of a misbehaving <b>\"then\"</b> which calls the passed callback before the input iterator is exhausted.</li><li>Let <var>remainingElementsCount</var> be the Record { [[Value]]: 1 }.</li><li>Let <var>index</var> be 0.</li><li>Repeat,<ol><li>Let <var>next</var> be ? IteratorStepValue(<var>iteratorRecord</var>).</li><li>If <var>next</var> is <emu-const>done</emu-const>, then<ol><li>Set <var>remainingElementsCount</var>.[[Value]] to <var>remainingElementsCount</var>.[[Value]] - 1.</li><li>If <var>remainingElementsCount</var>.[[Value]] = 0, then<ol><li>Let <var>valuesArray</var> be CreateArrayFromList(<var>values</var>).</li><li>Perform ? Call(<var>resultCapability</var>.[[Resolve]], <b>undefined</b>, « <var>valuesArray</var> »).</li></ol></li><li>Return <var>resultCapability</var>.[[Promise]].</li></ol></li><li>Append <b>undefined</b> to <var>values</var>.</li><li>Let <var>nextPromise</var> be ? Call(<var>promiseResolve</var>, <var>constructor</var>, « <var>next</var> »).</li><li>Let <var>fulfilledSteps</var> be a new Abstract Closure with parameters (<var>value</var>) that captures <var>values</var>, <var>resultCapability</var>, and <var>remainingElementsCount</var> and performs the following steps when called:<ol><li>Let <var>activeFunc</var> be the active function object.</li><li>If <var>activeFunc</var>.[[AlreadyCalled]] is <b>true</b>, return <b>undefined</b>.</li><li>Set <var>activeFunc</var>.[[AlreadyCalled]] to <b>true</b>.</li><li>Let <var>thisIndex</var> be <var>activeFunc</var>.[[Index]].</li><li>Set <var>values</var>[<var>thisIndex</var>] to <var>value</var>.</li><li>Set <var>remainingElementsCount</var>.[[Value]] to <var>remainingElementsCount</var>.[[Value]] - 1.</li><li>If <var>remainingElementsCount</var>.[[Value]] = 0, then<ol><li>Let <var>valuesArray</var> be CreateArrayFromList(<var>values</var>).</li><li>Return ? Call(<var>resultCapability</var>.[[Resolve]], <b>undefined</b>, « <var>valuesArray</var> »).</li></ol></li><li>Return <b>undefined</b>.</li></ol></li><li>Let <var>onFulfilled</var> be CreateBuiltinFunction(<var>fulfilledSteps</var>, 1, <b>\"\"</b>, « [[AlreadyCalled]], [[Index]] »).</li><li>Set <var>onFulfilled</var>.[[AlreadyCalled]] to <b>false</b>.</li><li>Set <var>onFulfilled</var>.[[Index]] to <var>index</var>.</li><li>Set <var>index</var> to <var>index</var> + 1.</li><li>Set <var>remainingElementsCount</var>.[[Value]] to <var>remainingElementsCount</var>.[[Value]] + 1.</li><li>Perform ? Invoke(<var>nextPromise</var>, <b>\"then\"</b>, « <var>onFulfilled</var>, <var>resultCapability</var>.[[Reject]] »).</li></ol></li></ol></emu-alg>\n        ","2.4.2":"<p>This function returns a promise that is fulfilled with an array of promise state snapshots, but only after all the original promises have settled, i.e. become either fulfilled or rejected. It resolves all elements of the passed iterable to promises as it runs this algorithm.</p>\n        <emu-alg><ol><li>Let <var>constructor</var> be the <b>this</b> value.</li><li>Let <var>promiseCapability</var> be ? NewPromiseCapability(<var>constructor</var>).</li><li>Let <var>promiseResolve</var> be Completion(GetPromiseResolve(<var>constructor</var>)).</li><li>IfAbruptRejectPromise(<var>promiseResolve</var>, <var>promiseCapability</var>).</li><li>Let <var>iteratorRecord</var> be Completion(GetIterator(<var>iterable</var>, <emu-const>sync</emu-const>)).</li><li>IfAbruptRejectPromise(<var>iteratorRecord</var>, <var>promiseCapability</var>).</li><li>Let <var>result</var> be Completion(PerformPromiseAllSettled(<var>iteratorRecord</var>, <var>constructor</var>, <var>promiseCapability</var>, <var>promiseResolve</var>)).</li><li>If <var>result</var> is an abrupt completion, then<ol><li>If <var>iteratorRecord</var>.[[Done]] is <b>false</b>, set <var>result</var> to Completion(IteratorClose(<var>iteratorRecord</var>, <var>result</var>)).</li><li>IfAbruptRejectPromise(<var>result</var>, <var>promiseCapability</var>).</li></ol></li><li>Return ! <var>result</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>This function requires its <b>this</b> value to be a constructor function that supports the parameter conventions of the Promise constructor.</p>\n        </emu-note>\n\n        ","2.4.2.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>values</var> be a new empty List.</li><li>NOTE: <var>remainingElementsCount</var> starts at 1 instead of 0 to ensure <var>resultCapability</var>.[[Resolve]] is only called once, even in the presence of a misbehaving <b>\"then\"</b> which calls one of the passed callbacks before the input iterator is exhausted.</li><li>Let <var>remainingElementsCount</var> be the Record { [[Value]]: 1 }.</li><li>Let <var>index</var> be 0.</li><li>Repeat,<ol><li>Let <var>next</var> be ? IteratorStepValue(<var>iteratorRecord</var>).</li><li>If <var>next</var> is <emu-const>done</emu-const>, then<ol><li>Set <var>remainingElementsCount</var>.[[Value]] to <var>remainingElementsCount</var>.[[Value]] - 1.</li><li>If <var>remainingElementsCount</var>.[[Value]] = 0, then<ol><li>Let <var>valuesArray</var> be CreateArrayFromList(<var>values</var>).</li><li>Perform ? Call(<var>resultCapability</var>.[[Resolve]], <b>undefined</b>, « <var>valuesArray</var> »).</li></ol></li><li>Return <var>resultCapability</var>.[[Promise]].</li></ol></li><li>Append <b>undefined</b> to <var>values</var>.</li><li>Let <var>nextPromise</var> be ? Call(<var>promiseResolve</var>, <var>constructor</var>, « <var>next</var> »).</li><li>Let <var>alreadyCalled</var> be the Record { [[Value]]: <b>false</b> }.</li><li>Let <var>fulfilledSteps</var> be a new Abstract Closure with parameters (<var>value</var>) that captures <var>values</var>, <var>resultCapability</var>, and <var>remainingElementsCount</var> and performs the following steps when called:<ol><li>Let <var>activeFunc</var> be the active function object.</li><li>If <var>activeFunc</var>.[[AlreadyCalled]].[[Value]] is <b>true</b>, return <b>undefined</b>.</li><li>Set <var>activeFunc</var>.[[AlreadyCalled]].[[Value]] to <b>true</b>.</li><li>Let <var>obj</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Object.prototype%</code>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>obj</var>, <b>\"status\"</b>, <b>\"fulfilled\"</b>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>obj</var>, <b>\"value\"</b>, <var>value</var>).</li><li>Let <var>thisIndex</var> be <var>activeFunc</var>.[[Index]].</li><li>Set <var>values</var>[<var>thisIndex</var>] to <var>obj</var>.</li><li>Set <var>remainingElementsCount</var>.[[Value]] to <var>remainingElementsCount</var>.[[Value]] - 1.</li><li>If <var>remainingElementsCount</var>.[[Value]] = 0, then<ol><li>Let <var>valuesArray</var> be CreateArrayFromList(<var>values</var>).</li><li>Return ? Call(<var>resultCapability</var>.[[Resolve]], <b>undefined</b>, « <var>valuesArray</var> »).</li></ol></li><li>Return <b>undefined</b>.</li></ol></li><li>Let <var>onFulfilled</var> be CreateBuiltinFunction(<var>fulfilledSteps</var>, 1, <b>\"\"</b>, « [[AlreadyCalled]], [[Index]] »).</li><li>Set <var>onFulfilled</var>.[[AlreadyCalled]] to <var>alreadyCalled</var>.</li><li>Set <var>onFulfilled</var>.[[Index]] to <var>index</var>.</li><li>Let <var>rejectedSteps</var> be a new Abstract Closure with parameters (<var>error</var>) that captures <var>values</var>, <var>resultCapability</var>, and <var>remainingElementsCount</var> and performs the following steps when called:<ol><li>Let <var>activeFunc</var> be the active function object.</li><li>If <var>activeFunc</var>.[[AlreadyCalled]].[[Value]] is <b>true</b>, return <b>undefined</b>.</li><li>Set <var>activeFunc</var>.[[AlreadyCalled]].[[Value]] to <b>true</b>.</li><li>Let <var>obj</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Object.prototype%</code>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>obj</var>, <b>\"status\"</b>, <b>\"rejected\"</b>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>obj</var>, <b>\"reason\"</b>, <var>error</var>).</li><li>Let <var>thisIndex</var> be <var>activeFunc</var>.[[Index]].</li><li>Set <var>values</var>[<var>thisIndex</var>] to <var>obj</var>.</li><li>Set <var>remainingElementsCount</var>.[[Value]] to <var>remainingElementsCount</var>.[[Value]] - 1.</li><li>If <var>remainingElementsCount</var>.[[Value]] = 0, then<ol><li>Let <var>valuesArray</var> be CreateArrayFromList(<var>values</var>).</li><li>Return ? Call(<var>resultCapability</var>.[[Resolve]], <b>undefined</b>, « <var>valuesArray</var> »).</li></ol></li><li>Return <b>undefined</b>.</li></ol></li><li>Let <var>onRejected</var> be CreateBuiltinFunction(<var>rejectedSteps</var>, 1, <b>\"\"</b>, « [[AlreadyCalled]], [[Index]] »).</li><li>Set <var>onRejected</var>.[[AlreadyCalled]] to <var>alreadyCalled</var>.</li><li>Set <var>onRejected</var>.[[Index]] to <var>index</var>.</li><li>Set <var>index</var> to <var>index</var> + 1.</li><li>Set <var>remainingElementsCount</var>.[[Value]] to <var>remainingElementsCount</var>.[[Value]] + 1.</li><li>Perform ? Invoke(<var>nextPromise</var>, <b>\"then\"</b>, « <var>onFulfilled</var>, <var>onRejected</var> »).</li></ol></li></ol></emu-alg>\n        ","2.4.3":"<p>This function returns a promise that is fulfilled by the first given promise to be fulfilled, or rejected with an <code>AggregateError</code> holding the rejection reasons if all of the given promises are rejected. It resolves all elements of the passed iterable to promises as it runs this algorithm.</p>\n        <emu-alg><ol><li>Let <var>constructor</var> be the <b>this</b> value.</li><li>Let <var>promiseCapability</var> be ? NewPromiseCapability(<var>constructor</var>).</li><li>Let <var>promiseResolve</var> be Completion(GetPromiseResolve(<var>constructor</var>)).</li><li>IfAbruptRejectPromise(<var>promiseResolve</var>, <var>promiseCapability</var>).</li><li>Let <var>iteratorRecord</var> be Completion(GetIterator(<var>iterable</var>, <emu-const>sync</emu-const>)).</li><li>IfAbruptRejectPromise(<var>iteratorRecord</var>, <var>promiseCapability</var>).</li><li>Let <var>result</var> be Completion(PerformPromiseAny(<var>iteratorRecord</var>, <var>constructor</var>, <var>promiseCapability</var>, <var>promiseResolve</var>)).</li><li>If <var>result</var> is an abrupt completion, then<ol><li>If <var>iteratorRecord</var>.[[Done]] is <b>false</b>, set <var>result</var> to Completion(IteratorClose(<var>iteratorRecord</var>, <var>result</var>)).</li><li>IfAbruptRejectPromise(<var>result</var>, <var>promiseCapability</var>).</li></ol></li><li>Return ! <var>result</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>This function requires its <b>this</b> value to be a constructor function that supports the parameter conventions of the <code>Promise</code> constructor.</p>\n        </emu-note>\n\n        ","2.4.3.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>errors</var> be a new empty List.</li><li>NOTE: <var>remainingElementsCount</var> starts at 1 instead of 0 to ensure <var>resultCapability</var>.[[Reject]] is only called once, even in the presence of a misbehaving <b>\"then\"</b> which calls the passed callback before the input iterator is exhausted.</li><li>Let <var>remainingElementsCount</var> be the Record { [[Value]]: 1 }.</li><li>Let <var>index</var> be 0.</li><li>Repeat,<ol><li>Let <var>next</var> be ? IteratorStepValue(<var>iteratorRecord</var>).</li><li>If <var>next</var> is <emu-const>done</emu-const>, then<ol><li>Set <var>remainingElementsCount</var>.[[Value]] to <var>remainingElementsCount</var>.[[Value]] - 1.</li><li>If <var>remainingElementsCount</var>.[[Value]] = 0, then<ol><li>Let <var>aggregateError</var> be a newly created <b>AggregateError</b> object.</li><li>Perform ! DefinePropertyOrThrow(<var>aggregateError</var>, <b>\"errors\"</b>, PropertyDescriptor { [[Configurable]]: <b>true</b>, [[Enumerable]]: <b>false</b>, [[Writable]]: <b>true</b>, [[Value]]: CreateArrayFromList(<var>errors</var>) }).</li><li>Perform ? Call(<var>resultCapability</var>.[[Reject]], <b>undefined</b>, « <var>aggregateError</var> »).</li></ol></li><li>Return <var>resultCapability</var>.[[Promise]].</li></ol></li><li>Append <b>undefined</b> to <var>errors</var>.</li><li>Let <var>nextPromise</var> be ? Call(<var>promiseResolve</var>, <var>constructor</var>, « <var>next</var> »).</li><li>Let <var>rejectedSteps</var> be a new Abstract Closure with parameters (<var>error</var>) that captures <var>errors</var>, <var>resultCapability</var>, and <var>remainingElementsCount</var> and performs the following steps when called:<ol><li>Let <var>activeFunc</var> be the active function object.</li><li>If <var>activeFunc</var>.[[AlreadyCalled]] is <b>true</b>, return <b>undefined</b>.</li><li>Set <var>activeFunc</var>.[[AlreadyCalled]] to <b>true</b>.</li><li>Let <var>thisIndex</var> be <var>activeFunc</var>.[[Index]].</li><li>Set <var>errors</var>[<var>thisIndex</var>] to <var>error</var>.</li><li>Set <var>remainingElementsCount</var>.[[Value]] to <var>remainingElementsCount</var>.[[Value]] - 1.</li><li>If <var>remainingElementsCount</var>.[[Value]] = 0, then<ol><li>Let <var>aggregateError</var> be a newly created <b>AggregateError</b> object.</li><li>Perform ! DefinePropertyOrThrow(<var>aggregateError</var>, <b>\"errors\"</b>, PropertyDescriptor { [[Configurable]]: <b>true</b>, [[Enumerable]]: <b>false</b>, [[Writable]]: <b>true</b>, [[Value]]: CreateArrayFromList(<var>errors</var>) }).</li><li>Return ? Call(<var>resultCapability</var>.[[Reject]], <b>undefined</b>, « <var>aggregateError</var> »).</li></ol></li><li>Return <b>undefined</b>.</li></ol></li><li>Let <var>onRejected</var> be CreateBuiltinFunction(<var>rejectedSteps</var>, 1, <b>\"\"</b>, « [[AlreadyCalled]], [[Index]] »).</li><li>Set <var>onRejected</var>.[[AlreadyCalled]] to <b>false</b>.</li><li>Set <var>onRejected</var>.[[Index]] to <var>index</var>.</li><li>Set <var>index</var> to <var>index</var> + 1.</li><li>Set <var>remainingElementsCount</var>.[[Value]] to <var>remainingElementsCount</var>.[[Value]] + 1.</li><li>Perform ? Invoke(<var>nextPromise</var>, <b>\"then\"</b>, « <var>resultCapability</var>.[[Resolve]], <var>onRejected</var> »).</li></ol></li></ol></emu-alg>\n        ","2.4.4":"<p>The initial value of <code>Promise.prototype</code> is the Promise prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","2.4.5":"<p>This function returns a new promise which is settled in the same way as the first passed promise to settle. It resolves all elements of the passed <var>iterable</var> to promises as it runs this algorithm.</p>\n        <emu-alg><ol><li>Let <var>constructor</var> be the <b>this</b> value.</li><li>Let <var>promiseCapability</var> be ? NewPromiseCapability(<var>constructor</var>).</li><li>Let <var>promiseResolve</var> be Completion(GetPromiseResolve(<var>constructor</var>)).</li><li>IfAbruptRejectPromise(<var>promiseResolve</var>, <var>promiseCapability</var>).</li><li>Let <var>iteratorRecord</var> be Completion(GetIterator(<var>iterable</var>, <emu-const>sync</emu-const>)).</li><li>IfAbruptRejectPromise(<var>iteratorRecord</var>, <var>promiseCapability</var>).</li><li>Let <var>result</var> be Completion(PerformPromiseRace(<var>iteratorRecord</var>, <var>constructor</var>, <var>promiseCapability</var>, <var>promiseResolve</var>)).</li><li>If <var>result</var> is an abrupt completion, then<ol><li>If <var>iteratorRecord</var>.[[Done]] is <b>false</b>, set <var>result</var> to Completion(IteratorClose(<var>iteratorRecord</var>, <var>result</var>)).</li><li>IfAbruptRejectPromise(<var>result</var>, <var>promiseCapability</var>).</li></ol></li><li>Return ! <var>result</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>If the <var>iterable</var> argument yields no values or if none of the promises yielded by <var>iterable</var> ever settle, then the pending promise returned by this method will never be settled.</p>\n        </emu-note>\n        <emu-note>\n          <p>This function expects its <b>this</b> value to be a constructor function that supports the parameter conventions of the Promise constructor. It also expects that its <b>this</b> value provides a <code>resolve</code> method.</p>\n        </emu-note>\n\n        ","2.4.5.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Repeat,<ol><li>Let <var>next</var> be ? IteratorStepValue(<var>iteratorRecord</var>).</li><li>If <var>next</var> is <emu-const>done</emu-const>, then<ol><li>Return <var>resultCapability</var>.[[Promise]].</li></ol></li><li>Let <var>nextPromise</var> be ? Call(<var>promiseResolve</var>, <var>constructor</var>, « <var>next</var> »).</li><li>Perform ? Invoke(<var>nextPromise</var>, <b>\"then\"</b>, « <var>resultCapability</var>.[[Resolve]], <var>resultCapability</var>.[[Reject]] »).</li></ol></li></ol></emu-alg>\n        ","2.4.6":"<p>This function returns a new promise rejected with the passed argument.</p>\n        <emu-alg><ol><li>Let <var>constructor</var> be the <b>this</b> value.</li><li>Let <var>promiseCapability</var> be ? NewPromiseCapability(<var>constructor</var>).</li><li>Perform ? Call(<var>promiseCapability</var>.[[Reject]], <b>undefined</b>, « <var>reason</var> »).</li><li>Return <var>promiseCapability</var>.[[Promise]].</li></ol></emu-alg>\n        <emu-note>\n          <p>This function expects its <b>this</b> value to be a constructor function that supports the parameter conventions of the Promise constructor.</p>\n        </emu-note>\n      ","2.4.7":"<p>This function returns either a new promise resolved with the passed argument, or the argument itself if the argument is a promise produced by this constructor.</p>\n        <emu-alg><ol><li>Let <var>constructor</var> be the <b>this</b> value.</li><li>If <var>constructor</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Return ? PromiseResolve(<var>constructor</var>, <var>resolution</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>This function expects its <b>this</b> value to be a constructor function that supports the parameter conventions of the Promise constructor.</p>\n        </emu-note>\n\n        ","2.4.7.1":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It returns a new promise resolved with <var>resolution</var>.</dd>\n          </dl>\n          <emu-alg><ol><li>If IsPromise(<var>resolution</var>) is <b>true</b>, then<ol><li>Let <var>xConstructor</var> be ? Get(<var>resolution</var>, <b>\"constructor\"</b>).</li><li>If SameValue(<var>xConstructor</var>, <var>constructor</var>) is <b>true</b>, return <var>resolution</var>.</li></ol></li><li>Let <var>promiseCapability</var> be ? NewPromiseCapability(<var>constructor</var>).</li><li>Perform ? Call(<var>promiseCapability</var>.[[Resolve]], <b>undefined</b>, « <var>resolution</var> »).</li><li>Return <var>promiseCapability</var>.[[Promise]].</li></ol></emu-alg>\n        ","2.4.8":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>constructor</var> be the <b>this</b> value.</li><li>If <var>constructor</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>promiseCapability</var> be ? NewPromiseCapability(<var>constructor</var>).</li><li>Let <var>status</var> be Completion(Call(<var>callback</var>, <b>undefined</b>, <var>args</var>)).</li><li>If <var>status</var> is an abrupt completion, then<ol><li>Perform ? Call(<var>promiseCapability</var>.[[Reject]], <b>undefined</b>, « <var>status</var>.[[Value]] »).</li></ol></li><li>Else,<ol><li>Perform ? Call(<var>promiseCapability</var>.[[Resolve]], <b>undefined</b>, « <var>status</var>.[[Value]] »).</li></ol></li><li>Return <var>promiseCapability</var>.[[Promise]].</li></ol></emu-alg>\n        <emu-note>\n          <p>This function expects its <b>this</b> value to be a constructor function that supports the parameter conventions of the Promise constructor.</p>\n        </emu-note>\n      ","2.4.9":"<p>This function returns an object with three properties: a new promise together with the <code>resolve</code> and <code>reject</code> functions associated with it.</p>\n        <emu-alg><ol><li>Let <var>constructor</var> be the <b>this</b> value.</li><li>Let <var>promiseCapability</var> be ? NewPromiseCapability(<var>constructor</var>).</li><li>Let <var>obj</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Object.prototype%</code>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>obj</var>, <b>\"promise\"</b>, <var>promiseCapability</var>.[[Promise]]).</li><li>Perform ! CreateDataPropertyOrThrow(<var>obj</var>, <b>\"resolve\"</b>, <var>promiseCapability</var>.[[Resolve]]).</li><li>Perform ! CreateDataPropertyOrThrow(<var>obj</var>, <b>\"reject\"</b>, <var>promiseCapability</var>.[[Reject]]).</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","2.4.10":"<p><code>Promise[<code class=\"emu-intrinsic\">%Symbol.species%</code>]</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Return the <b>this</b> value.</li></ol></emu-alg>\n        <p>The value of the <b>\"name\"</b> property of this function is <b>\"get [Symbol.species]\"</b>.</p>\n        <emu-note>\n          <p>Promise prototype methods normally use their <b>this</b> value's constructor to create a derived object. However, a subclass constructor may over-ride that default behaviour by redefining its <code class=\"emu-intrinsic\">%Symbol.species%</code> property.</p>\n        </emu-note>\n      ","2.5":"<p>The <dfn>Promise prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Promise.prototype%</code></dfn>.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n        <li>is an ordinary object.</li>\n        <li>does not have a [[PromiseState]] internal slot or any of the other internal slots of Promise instances.</li>\n      </ul>\n\n      ","2.5.1":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>promise</var> be the <b>this</b> value.</li><li>Return ? Invoke(<var>promise</var>, <b>\"then\"</b>, « <b>undefined</b>, <var>onRejected</var> »).</li></ol></emu-alg>\n      ","2.5.2":"<p>The initial value of <code>Promise.prototype.constructor</code> is <code class=\"emu-intrinsic\">%Promise%</code>.</p>\n      ","2.5.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>promise</var> be the <b>this</b> value.</li><li>If <var>promise</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>constructor</var> be ? SpeciesConstructor(<var>promise</var>, <code class=\"emu-intrinsic\">%Promise%</code>).</li><li>Assert: IsConstructor(<var>constructor</var>) is <b>true</b>.</li><li>If IsCallable(<var>onFinally</var>) is <b>false</b>, then<ol><li>Let <var>thenFinally</var> be <var>onFinally</var>.</li><li>Let <var>catchFinally</var> be <var>onFinally</var>.</li></ol></li><li>Else,<ol><li>Let <var>thenFinallyClosure</var> be a new Abstract Closure with parameters (<var>value</var>) that captures <var>onFinally</var> and <var>constructor</var> and performs the following steps when called:<ol><li>Let <var>result</var> be ? Call(<var>onFinally</var>, <b>undefined</b>).</li><li>Let <var>p</var> be ? PromiseResolve(<var>constructor</var>, <var>result</var>).</li><li>Let <var>returnValue</var> be a new Abstract Closure with no parameters that captures <var>value</var> and performs the following steps when called:<ol><li>Return NormalCompletion(<var>value</var>).</li></ol></li><li>Let <var>valueThunk</var> be CreateBuiltinFunction(<var>returnValue</var>, 0, <b>\"\"</b>, « »).</li><li>Return ? Invoke(<var>p</var>, <b>\"then\"</b>, « <var>valueThunk</var> »).</li></ol></li><li>Let <var>thenFinally</var> be CreateBuiltinFunction(<var>thenFinallyClosure</var>, 1, <b>\"\"</b>, « »).</li><li>Let <var>catchFinallyClosure</var> be a new Abstract Closure with parameters (<var>reason</var>) that captures <var>onFinally</var> and <var>constructor</var> and performs the following steps when called:<ol><li>Let <var>result</var> be ? Call(<var>onFinally</var>, <b>undefined</b>).</li><li>Let <var>p</var> be ? PromiseResolve(<var>constructor</var>, <var>result</var>).</li><li>Let <var>throwReason</var> be a new Abstract Closure with no parameters that captures <var>reason</var> and performs the following steps when called:<ol><li>Throw <var>reason</var>.</li></ol></li><li>Let <var>thrower</var> be CreateBuiltinFunction(<var>throwReason</var>, 0, <b>\"\"</b>, « »).</li><li>Return ? Invoke(<var>p</var>, <b>\"then\"</b>, « <var>thrower</var> »).</li></ol></li><li>Let <var>catchFinally</var> be CreateBuiltinFunction(<var>catchFinallyClosure</var>, 1, <b>\"\"</b>, « »).</li></ol></li><li>Return ? Invoke(<var>promise</var>, <b>\"then\"</b>, « <var>thenFinally</var>, <var>catchFinally</var> »).</li></ol></emu-alg>\n      ","2.5.4":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>promise</var> be the <b>this</b> value.</li><li>If IsPromise(<var>promise</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>constructor</var> be ? SpeciesConstructor(<var>promise</var>, <code class=\"emu-intrinsic\">%Promise%</code>).</li><li>Let <var>resultCapability</var> be ? NewPromiseCapability(<var>constructor</var>).</li><li>Return PerformPromiseThen(<var>promise</var>, <var>onFulfilled</var>, <var>onRejected</var>, <var>resultCapability</var>).</li></ol></emu-alg>\n\n        ","2.5.4.1":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It performs the “then” operation on <var>promise</var> using <var>onFulfilled</var> and <var>onRejected</var> as its settlement actions. If <var>resultCapability</var> is passed, the result is stored by updating <var>resultCapability</var>'s promise. If it is not passed, then PerformPromiseThen is being called by a specification-internal operation where the result does not matter.</dd>\n          </dl>\n          <emu-alg><ol><li>Assert: IsPromise(<var>promise</var>) is <b>true</b>.</li><li>If <var>resultCapability</var> is not present, then<ol><li>Set <var>resultCapability</var> to <b>undefined</b>.</li></ol></li><li>If IsCallable(<var>onFulfilled</var>) is <b>false</b>, then<ol><li>Let <var>onFulfilledJobCallback</var> be <emu-const>empty</emu-const>.</li></ol></li><li>Else,<ol><li>Let <var>onFulfilledJobCallback</var> be HostMakeJobCallback(<var>onFulfilled</var>).</li></ol></li><li>If IsCallable(<var>onRejected</var>) is <b>false</b>, then<ol><li>Let <var>onRejectedJobCallback</var> be <emu-const>empty</emu-const>.</li></ol></li><li>Else,<ol><li>Let <var>onRejectedJobCallback</var> be HostMakeJobCallback(<var>onRejected</var>).</li></ol></li><li>Let <var>fulfillReaction</var> be the PromiseReaction Record { [[Capability]]: <var>resultCapability</var>, [[Type]]: <emu-const>fulfill</emu-const>, [[Handler]]: <var>onFulfilledJobCallback</var> }.</li><li>Let <var>rejectReaction</var> be the PromiseReaction Record { [[Capability]]: <var>resultCapability</var>, [[Type]]: <emu-const>reject</emu-const>, [[Handler]]: <var>onRejectedJobCallback</var> }.</li><li>If <var>promise</var>.[[PromiseState]] is <emu-const>pending</emu-const>, then<ol><li>Append <var>fulfillReaction</var> to <var>promise</var>.[[PromiseFulfillReactions]].</li><li>Append <var>rejectReaction</var> to <var>promise</var>.[[PromiseRejectReactions]].</li></ol></li><li>Else if <var>promise</var>.[[PromiseState]] is <emu-const>fulfilled</emu-const>, then<ol><li>Let <var>value</var> be <var>promise</var>.[[PromiseResult]].</li><li>Let <var>fulfillJob</var> be NewPromiseReactionJob(<var>fulfillReaction</var>, <var>value</var>).</li><li>Perform HostEnqueuePromiseJob(<var>fulfillJob</var>.[[Job]], <var>fulfillJob</var>.[[Realm]]).</li></ol></li><li>Else,<ol><li>Assert: <var>promise</var>.[[PromiseState]] is <emu-const>rejected</emu-const>.</li><li>Let <var>reason</var> be <var>promise</var>.[[PromiseResult]].</li><li>If <var>promise</var>.[[PromiseIsHandled]] is <b>false</b>, perform HostPromiseRejectionTracker(<var>promise</var>, <b>\"handle\"</b>).</li><li>Let <var>rejectJob</var> be NewPromiseReactionJob(<var>rejectReaction</var>, <var>reason</var>).</li><li>Perform HostEnqueuePromiseJob(<var>rejectJob</var>.[[Job]], <var>rejectJob</var>.[[Realm]]).</li></ol></li><li>Set <var>promise</var>.[[PromiseIsHandled]] to <b>true</b>.</li><li>If <var>resultCapability</var> is <b>undefined</b>, return <b>undefined</b>.</li><li>Return <var>resultCapability</var>.[[Promise]].</li></ol></emu-alg>\n        ","2.5.5":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"Promise\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","2.6":"<p>Promise instances are ordinary objects that inherit properties from the Promise prototype object (the intrinsic, <code class=\"emu-intrinsic\">%Promise.prototype%</code>). Promise instances are initially created with the internal slots described in table-internal-slots-of-promise-instances.</p>\n      <emu-table id=\"table-internal-slots-of-promise-instances\" caption=\"Internal Slots of Promise Instances\" oldids=\"table-59\">\n        <table>\n          <thead>\n            <tr>\n              <th>\n                Internal Slot\n              </th>\n              <th>\n                Type\n              </th>\n              <th>\n                Description\n              </th>\n            </tr>\n          </thead>\n          <tr>\n            <td>\n              [[PromiseState]]\n            </td>\n            <td>\n              <emu-const>pending</emu-const>, <emu-const>fulfilled</emu-const>, or <emu-const>rejected</emu-const>\n            </td>\n            <td>\n              Governs how a promise will react to incoming calls to its <code>then</code> method.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[PromiseResult]]\n            </td>\n            <td>\n              an ECMAScript language value or <emu-const>empty</emu-const>\n            </td>\n            <td>\n              The value with which the promise has been fulfilled or rejected, if any. <emu-const>empty</emu-const> if and only if the [[PromiseState]] is <emu-const>pending</emu-const>.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[PromiseFulfillReactions]]\n            </td>\n            <td>\n              a List of PromiseReaction Records\n            </td>\n            <td>\n              Records to be processed when/if the promise transitions from the <emu-const>pending</emu-const> state to the <emu-const>fulfilled</emu-const> state.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[PromiseRejectReactions]]\n            </td>\n            <td>\n              a List of PromiseReaction Records\n            </td>\n            <td>\n              Records to be processed when/if the promise transitions from the <emu-const>pending</emu-const> state to the <emu-const>rejected</emu-const> state.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[PromiseIsHandled]]\n            </td>\n            <td>\n              a Boolean\n            </td>\n            <td>\n              Indicates whether the promise has ever had a fulfillment or rejection handler; used in unhandled rejection tracking.\n            </td>\n          </tr>\n        </table>\n      </emu-table>\n    ","3.1":"<p>The GeneratorFunction constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%GeneratorFunction%</code></dfn>.</li>\n        <li>is a subclass of <code>Function</code>.</li>\n        <li>creates and initializes a new GeneratorFunction when called as a function rather than as a constructor. Thus the function call <code>GeneratorFunction (…)</code> is equivalent to the object creation expression <code>new GeneratorFunction (…)</code> with the same arguments.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified GeneratorFunction behaviour must include a <code>super</code> call to the GeneratorFunction constructor to create and initialize subclass instances with the internal slots necessary for built-in GeneratorFunction behaviour. All ECMAScript syntactic forms for defining generator function objects create direct instances of GeneratorFunction. There is no syntactic means to create instances of GeneratorFunction subclasses.</li>\n      </ul>\n\n      ","3.1.1":"<p>The last argument (if any) specifies the body (executable code) of a generator function; any preceding arguments specify formal parameters.</p>\n        <p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>activeFunc</var> be the active function object.</li><li>If <var>bodyArg</var> is not present, set <var>bodyArg</var> to the empty String.</li><li>Return ? CreateDynamicFunction(<var>activeFunc</var>, NewTarget, <emu-const>generator</emu-const>, <var>parameterArgs</var>, <var>bodyArg</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>See NOTE for 20.2.1.1.</p>\n        </emu-note>\n      ","3.2":"<p>The GeneratorFunction constructor:</p>\n      <ul>\n        <li>is a standard built-in function object that inherits from the Function constructor.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function%</code>.</li>\n        <li oldids=\"sec-generatorfunction.length\">has a <b>\"length\"</b> property whose value is <b>1</b><sub>𝔽</sub>.</li>\n        <li>has a <b>\"name\"</b> property whose value is <b>\"GeneratorFunction\"</b>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","3.2.1":"<p>The initial value of <code>GeneratorFunction.prototype</code> is the GeneratorFunction prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","3.3":"<p>The <dfn>GeneratorFunction prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%GeneratorFunction.prototype%</code></dfn> (see figure-2).</li>\n        <li>is an ordinary object.</li>\n        <li>is not a function object and does not have an [[ECMAScriptCode]] internal slot or any other of the internal slots listed in table-internal-slots-of-ecmascript-function-objects or table-internal-slots-of-generator-instances.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n      </ul>\n\n      ","3.3.1":"<p>The initial value of <code>GeneratorFunction.prototype.constructor</code> is <code class=\"emu-intrinsic\">%GeneratorFunction%</code>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","3.3.2":"<p>The initial value of <code>GeneratorFunction.prototype.prototype</code> is <code class=\"emu-intrinsic\">%GeneratorPrototype%</code>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","3.3.3":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"GeneratorFunction\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","3.4":"<p>Every GeneratorFunction instance is an ECMAScript function object and has the internal slots listed in table-internal-slots-of-ecmascript-function-objects. The value of the [[IsClassConstructor]] internal slot for all such instances is <b>false</b>.</p>\n      <p>Each GeneratorFunction instance has the following own properties:</p>\n\n      ","3.4.1":"<p>The specification for the <b>\"length\"</b> property of Function instances given in 20.2.4.1 also applies to GeneratorFunction instances.</p>\n      ","3.4.2":"<p>The specification for the <b>\"name\"</b> property of Function instances given in 20.2.4.2 also applies to GeneratorFunction instances.</p>\n      ","3.4.3":"<p>Whenever a GeneratorFunction instance is created another ordinary object is also created and is the initial value of the generator function's <b>\"prototype\"</b> property. The value of the prototype property is used to initialize the [[Prototype]] internal slot of a newly created Generator when the generator function object is invoked using [[Call]].</p>\n        <p>This property has the attributes { [[Writable]]: <b>true</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n        <emu-note>\n          <p>Unlike Function instances, the object that is the value of a GeneratorFunction's <b>\"prototype\"</b> property does not have a <b>\"constructor\"</b> property whose value is the GeneratorFunction instance.</p>\n        </emu-note>\n      ","4.1":"<p>The AsyncGeneratorFunction constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%AsyncGeneratorFunction%</code></dfn>.</li>\n        <li>is a subclass of <code>Function</code>.</li>\n        <li>creates and initializes a new AsyncGeneratorFunction when called as a function rather than as a constructor. Thus the function call <code>AsyncGeneratorFunction (...)</code> is equivalent to the object creation expression <code>new AsyncGeneratorFunction (...)</code> with the same arguments.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified AsyncGeneratorFunction behaviour must include a <code>super</code> call to the AsyncGeneratorFunction constructor to create and initialize subclass instances with the internal slots necessary for built-in AsyncGeneratorFunction behaviour. All ECMAScript syntactic forms for defining async generator function objects create direct instances of AsyncGeneratorFunction. There is no syntactic means to create instances of AsyncGeneratorFunction subclasses.</li>\n      </ul>\n\n      ","4.1.1":"<p>The last argument (if any) specifies the body (executable code) of an async generator function; any preceding arguments specify formal parameters.</p>\n        <p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>activeFunc</var> be the active function object.</li><li>If <var>bodyArg</var> is not present, set <var>bodyArg</var> to the empty String.</li><li>Return ? CreateDynamicFunction(<var>activeFunc</var>, NewTarget, <emu-const>async-generator</emu-const>, <var>parameterArgs</var>, <var>bodyArg</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>See NOTE for 20.2.1.1.</p>\n        </emu-note>\n      ","4.2":"<p>The AsyncGeneratorFunction constructor:</p>\n      <ul>\n        <li>is a standard built-in function object that inherits from the Function constructor.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function%</code>.</li>\n        <li oldids=\"sec-asyncgeneratorfunction-length\">has a <b>\"length\"</b> property whose value is <b>1</b><sub>𝔽</sub>.</li>\n        <li>has a <b>\"name\"</b> property whose value is <b>\"AsyncGeneratorFunction\"</b>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","4.2.1":"<p>The initial value of <code>AsyncGeneratorFunction.prototype</code> is the AsyncGeneratorFunction prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.3":"<p>The <dfn>AsyncGeneratorFunction prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%AsyncGeneratorFunction.prototype%</code></dfn>.</li>\n        <li>is an ordinary object.</li>\n        <li>is not a function object and does not have an [[ECMAScriptCode]] internal slot or any other of the internal slots listed in table-internal-slots-of-ecmascript-function-objects or table-internal-slots-of-asyncgenerator-instances.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n      </ul>\n\n      ","4.3.1":"<p>The initial value of <code>AsyncGeneratorFunction.prototype.constructor</code> is <code class=\"emu-intrinsic\">%AsyncGeneratorFunction%</code>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","4.3.2":"<p>The initial value of <code>AsyncGeneratorFunction.prototype.prototype</code> is <code class=\"emu-intrinsic\">%AsyncGeneratorPrototype%</code>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","4.3.3":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"AsyncGeneratorFunction\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","4.4":"<p>Every AsyncGeneratorFunction instance is an ECMAScript function object and has the internal slots listed in table-internal-slots-of-ecmascript-function-objects. The value of the [[IsClassConstructor]] internal slot for all such instances is <b>false</b>.</p>\n      <p>Each AsyncGeneratorFunction instance has the following own properties:</p>\n\n      ","4.4.1":"<p>The value of the <b>\"length\"</b> property is an integral Number that indicates the typical number of arguments expected by the AsyncGeneratorFunction. However, the language permits the function to be invoked with some other number of arguments. The behaviour of an AsyncGeneratorFunction when invoked on a number of arguments other than the number specified by its <b>\"length\"</b> property depends on the function.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","4.4.2":"<p>The specification for the <b>\"name\"</b> property of Function instances given in 20.2.4.2 also applies to AsyncGeneratorFunction instances.</p>\n      ","4.4.3":"<p>Whenever an AsyncGeneratorFunction instance is created, another ordinary object is also created and is the initial value of the async generator function's <b>\"prototype\"</b> property. The value of the prototype property is used to initialize the [[Prototype]] internal slot of a newly created AsyncGenerator when the generator function object is invoked using [[Call]].</p>\n        <p>This property has the attributes { [[Writable]]: <b>true</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n        <emu-note>\n          <p>Unlike function instances, the object that is the value of an AsyncGeneratorFunction's <b>\"prototype\"</b> property does not have a <b>\"constructor\"</b> property whose value is the AsyncGeneratorFunction instance.</p>\n        </emu-note>\n      ","5.1":"<p>The <dfn><code class=\"emu-intrinsic\">%GeneratorPrototype%</code></dfn> object:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%GeneratorFunction.prototype.prototype%</code></dfn>.</li>\n        <li>is an ordinary object.</li>\n        <li>is not a Generator instance and does not have a [[GeneratorState]] internal slot.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Iterator.prototype%</code>.</li>\n        <li>has properties that are indirectly inherited by all Generator instances.</li>\n      </ul>\n\n      ","5.1.1":"<p>The initial value of <code class=\"emu-intrinsic\">%GeneratorPrototype%</code><code>.constructor</code> is <code class=\"emu-intrinsic\">%GeneratorFunction.prototype%</code>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","5.1.2":"<emu-alg><ol><li>Return ? GeneratorResume(<b>this</b> value, <var>value</var>, <emu-const>empty</emu-const>).</li></ol></emu-alg>\n      ","5.1.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>generator</var> be the <b>this</b> value.</li><li>Let <var>completion</var> be ReturnCompletion(<var>value</var>).</li><li>Return ? GeneratorResumeAbrupt(<var>generator</var>, <var>completion</var>, <emu-const>empty</emu-const>).</li></ol></emu-alg>\n      ","5.1.4":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>generator</var> be the <b>this</b> value.</li><li>Let <var>completion</var> be ThrowCompletion(<var>exception</var>).</li><li>Return ? GeneratorResumeAbrupt(<var>generator</var>, <var>completion</var>, <emu-const>empty</emu-const>).</li></ol></emu-alg>\n      ","5.1.5":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"Generator\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","5.2":"<p>Generator instances are initially created with the internal slots described in table-internal-slots-of-generator-instances.</p>\n      <emu-table id=\"table-internal-slots-of-generator-instances\" caption=\"Internal Slots of Generator Instances\" oldids=\"table-56\">\n        <table>\n          <thead>\n            <tr>\n              <th>\n                Internal Slot\n              </th>\n              <th>\n                Type\n              </th>\n              <th>\n                Description\n              </th>\n            </tr>\n          </thead>\n          <tr>\n            <td>\n              [[GeneratorState]]\n            </td>\n            <td>\n              <emu-const>suspended-start</emu-const>, <emu-const>suspended-yield</emu-const>, <emu-const>executing</emu-const>, or <emu-const>completed</emu-const>\n            </td>\n            <td>\n              The current execution state of the generator.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[GeneratorContext]]\n            </td>\n            <td>\n              an execution context\n            </td>\n            <td>\n              The execution context that is used when executing the code of this generator.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[GeneratorBrand]]\n            </td>\n            <td>\n              a String or <emu-const>empty</emu-const>\n            </td>\n            <td>\n              A brand used to distinguish different kinds of generators. The [[GeneratorBrand]] of generators declared by ECMAScript source text is always <emu-const>empty</emu-const>.\n            </td>\n          </tr>\n        </table>\n      </emu-table>\n    ","5.3":"","5.3.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: <var>generator</var>.[[GeneratorState]] is <emu-const>suspended-start</emu-const>.</li><li>Let <var>genContext</var> be the running execution context.</li><li>Set the Generator component of <var>genContext</var> to <var>generator</var>.</li><li>Let <var>closure</var> be a new Abstract Closure with no parameters that captures <var>generatorBody</var> and performs the following steps when called:<ol><li>Let <var>acGenContext</var> be the running execution context.</li><li>Let <var>acGenerator</var> be the Generator component of <var>acGenContext</var>.</li><li>If <var>generatorBody</var> is a Parse Node, then<ol><li>Let <var>result</var> be Completion(Evaluation of <var>generatorBody</var>).</li></ol></li><li>Else,<ol><li>Assert: <var>generatorBody</var> is an Abstract Closure with no parameters.</li><li>Let <var>result</var> be Completion(<var>generatorBody</var>()).</li></ol></li><li>Assert: If we return here, the generator either threw an exception or performed either an implicit or explicit return.</li><li>Remove <var>acGenContext</var> from the execution context stack and restore the execution context that is at the top of the execution context stack as the running execution context.</li><li>Set <var>acGenerator</var>.[[GeneratorState]] to <emu-const>completed</emu-const>.</li><li>NOTE: Once a generator enters the <emu-const>completed</emu-const> state it never leaves it and its associated execution context is never resumed. Any execution state associated with <var>acGenerator</var> can be discarded at this point.</li><li>If <var>result</var> is a normal completion, then<ol><li>Let <var>resultValue</var> be <b>undefined</b>.</li></ol></li><li>Else if <var>result</var> is a return completion, then<ol><li>Let <var>resultValue</var> be <var>result</var>.[[Value]].</li></ol></li><li>Else,<ol><li>Assert: <var>result</var> is a throw completion.</li><li>Return ? <var>result</var>.</li></ol></li><li>Return NormalCompletion(CreateIteratorResultObject(<var>resultValue</var>, <b>true</b>)).</li></ol></li><li>Set the code evaluation state of <var>genContext</var> such that when evaluation is resumed for that execution context, <var>closure</var> will be called with no arguments.</li><li>Set <var>generator</var>.[[GeneratorContext]] to <var>genContext</var>.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","5.3.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Perform ? RequireInternalSlot(<var>generator</var>, [[GeneratorState]]).</li><li>Perform ? RequireInternalSlot(<var>generator</var>, [[GeneratorBrand]]).</li><li>If <var>generator</var>.[[GeneratorBrand]] is not <var>generatorBrand</var>, throw a <b>TypeError</b> exception.</li><li>Assert: <var>generator</var> has a [[GeneratorContext]] internal slot.</li><li>Let <var>state</var> be <var>generator</var>.[[GeneratorState]].</li><li>If <var>state</var> is <emu-const>executing</emu-const>, throw a <b>TypeError</b> exception.</li><li>Return <var>state</var>.</li></ol></emu-alg>\n      ","5.3.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>state</var> be ? GeneratorValidate(<var>generator</var>, <var>generatorBrand</var>).</li><li>If <var>state</var> is <emu-const>completed</emu-const>, return CreateIteratorResultObject(<b>undefined</b>, <b>true</b>).</li><li>Assert: <var>state</var> is either <emu-const>suspended-start</emu-const> or <emu-const>suspended-yield</emu-const>.</li><li>Let <var>genContext</var> be <var>generator</var>.[[GeneratorContext]].</li><li>Set <var>generator</var>.[[GeneratorState]] to <emu-const>executing</emu-const>.</li><li>Return ? RunSuspendedContext(<var>genContext</var>, NormalCompletion(<var>value</var>)).</li></ol></emu-alg>\n      ","5.3.4":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>state</var> be ? GeneratorValidate(<var>generator</var>, <var>generatorBrand</var>).</li><li>If <var>state</var> is <emu-const>suspended-start</emu-const>, then<ol><li>Set <var>generator</var>.[[GeneratorState]] to <emu-const>completed</emu-const>.</li><li>NOTE: Once a generator enters the <emu-const>completed</emu-const> state it never leaves it and its associated execution context is never resumed. Any execution state associated with <var>generator</var> can be discarded at this point.</li><li>Set <var>state</var> to <emu-const>completed</emu-const>.</li></ol></li><li>If <var>state</var> is <emu-const>completed</emu-const>, then<ol><li>If <var>abruptCompletion</var> is a return completion, then<ol><li>Return CreateIteratorResultObject(<var>abruptCompletion</var>.[[Value]], <b>true</b>).</li></ol></li><li>Return ? <var>abruptCompletion</var>.</li></ol></li><li>Assert: <var>state</var> is <emu-const>suspended-yield</emu-const>.</li><li>Let <var>genContext</var> be <var>generator</var>.[[GeneratorContext]].</li><li>Set <var>generator</var>.[[GeneratorState]] to <emu-const>executing</emu-const>.</li><li>Return ? RunSuspendedContext(<var>genContext</var>, <var>abruptCompletion</var>).</li></ol></emu-alg>\n      ","5.3.5":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>genContext</var> be the running execution context.</li><li>If <var>genContext</var> does not have a Generator component, return <emu-const>non-generator</emu-const>.</li><li>Let <var>generator</var> be the Generator component of <var>genContext</var>.</li><li>If <var>generator</var> has an [[AsyncGeneratorState]] internal slot, return <emu-const>async</emu-const>.</li><li>Return <emu-const>sync</emu-const>.</li></ol></emu-alg>\n      ","5.3.6":"<dl class=\"header\">\n          <dt>skip return checks</dt>\n          <dd>true</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>genContext</var> be the running execution context.</li><li>Assert: <var>genContext</var> is the execution context of a generator.</li><li>Let <var>generator</var> be the value of the Generator component of <var>genContext</var>.</li><li>Assert: GetGeneratorKind() is <emu-const>sync</emu-const>.</li><li>Set <var>generator</var>.[[GeneratorState]] to <emu-const>suspended-yield</emu-const>.</li><li>Remove <var>genContext</var> from the execution context stack and restore the execution context that is at the top of the execution context stack as the running execution context.</li><li>Let <var>callerContext</var> be the running execution context.</li><li>Resume <var>callerContext</var> passing NormalCompletion(<var>iteratorResult</var>). If <var>genContext</var> is ever resumed again, let <var>resumptionValue</var> be the Completion Record with which it is resumed.</li><li>Assert: If control reaches here, then <var>genContext</var> is the running execution context again.</li><li>Return <var>resumptionValue</var>.</li></ol></emu-alg>\n      ","5.3.7":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>generatorKind</var> be GetGeneratorKind().</li><li>If <var>generatorKind</var> is <emu-const>async</emu-const>, return ? AsyncGeneratorYield(? Await(<var>value</var>)).</li><li>Return ? GeneratorYield(CreateIteratorResultObject(<var>value</var>, <b>false</b>)).</li></ol></emu-alg>\n      ","5.3.8":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>NOTE: <var>closure</var> can contain uses of the Yield operation to yield an IteratorResult object.</li><li>If <var>extraSlots</var> is not present, set <var>extraSlots</var> to a new empty List.</li><li>Let <var>internalSlotsList</var> be the list-concatenation of <var>extraSlots</var> and « [[GeneratorState]], [[GeneratorContext]], [[GeneratorBrand]] ».</li><li>Let <var>generator</var> be OrdinaryObjectCreate(<var>generatorPrototype</var>, <var>internalSlotsList</var>).</li><li>Set <var>generator</var>.[[GeneratorBrand]] to <var>generatorBrand</var>.</li><li>Set <var>generator</var>.[[GeneratorState]] to <emu-const>suspended-start</emu-const>.</li><li>Let <var>callerContext</var> be the running execution context.</li><li>Let <var>calleeContext</var> be a new execution context.</li><li>Set the Function of <var>calleeContext</var> to <b>null</b>.</li><li>Set the Realm of <var>calleeContext</var> to the current Realm Record.</li><li>Set the ScriptOrModule of <var>calleeContext</var> to <var>callerContext</var>'s ScriptOrModule.</li><li>If <var>callerContext</var> is not already suspended, suspend <var>callerContext</var>.</li><li>Push <var>calleeContext</var> onto the execution context stack; <var>calleeContext</var> is now the running execution context.</li><li>Perform GeneratorStart(<var>generator</var>, <var>closure</var>).</li><li>Remove <var>calleeContext</var> from the execution context stack and restore <var>callerContext</var> as the running execution context.</li><li>Return <var>generator</var>.</li></ol></emu-alg>\n      ","6.1":"<p>The <dfn><code class=\"emu-intrinsic\">%AsyncGeneratorPrototype%</code></dfn> object:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%AsyncGeneratorFunction.prototype.prototype%</code></dfn>.</li>\n        <li>is an ordinary object.</li>\n        <li>is not an AsyncGenerator instance and does not have an [[AsyncGeneratorState]] internal slot.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%AsyncIteratorPrototype%</code>.</li>\n        <li>has properties that are indirectly inherited by all AsyncGenerator instances.</li>\n      </ul>\n\n      ","6.1.1":"<p>The initial value of <code class=\"emu-intrinsic\">%AsyncGeneratorPrototype%</code><code>.constructor</code> is <code class=\"emu-intrinsic\">%AsyncGeneratorFunction.prototype%</code>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","6.1.2":"<emu-alg><ol><li>Let <var>generator</var> be the <b>this</b> value.</li><li>Let <var>promiseCapability</var> be ! NewPromiseCapability(<code class=\"emu-intrinsic\">%Promise%</code>).</li><li>Let <var>result</var> be Completion(AsyncGeneratorValidate(<var>generator</var>, <emu-const>empty</emu-const>)).</li><li>IfAbruptRejectPromise(<var>result</var>, <var>promiseCapability</var>).</li><li>Let <var>state</var> be <var>generator</var>.[[AsyncGeneratorState]].</li><li>If <var>state</var> is <emu-const>completed</emu-const>, then<ol><li>Let <var>iteratorResult</var> be CreateIteratorResultObject(<b>undefined</b>, <b>true</b>).</li><li>Perform ! <emu-meta effects=\"user-code\">Call(<var>promiseCapability</var>.[[Resolve]], <b>undefined</b>, « <var>iteratorResult</var> »)</emu-meta>.</li><li>Return <var>promiseCapability</var>.[[Promise]].</li></ol></li><li>Let <var>completion</var> be NormalCompletion(<var>value</var>).</li><li>Perform AsyncGeneratorEnqueue(<var>generator</var>, <var>completion</var>, <var>promiseCapability</var>).</li><li>If <var>state</var> is either <emu-const>suspended-start</emu-const> or <emu-const>suspended-yield</emu-const>, then<ol><li>Perform AsyncGeneratorResume(<var>generator</var>, <var>completion</var>).</li></ol></li><li>Else,<ol><li>Assert: <var>state</var> is either <emu-const>executing</emu-const> or <emu-const>draining-queue</emu-const>.</li></ol></li><li>Return <var>promiseCapability</var>.[[Promise]].</li></ol></emu-alg>\n      ","6.1.3":"<emu-alg><ol><li>Let <var>generator</var> be the <b>this</b> value.</li><li>Let <var>promiseCapability</var> be ! NewPromiseCapability(<code class=\"emu-intrinsic\">%Promise%</code>).</li><li>Let <var>result</var> be Completion(AsyncGeneratorValidate(<var>generator</var>, <emu-const>empty</emu-const>)).</li><li>IfAbruptRejectPromise(<var>result</var>, <var>promiseCapability</var>).</li><li>Let <var>completion</var> be ReturnCompletion(<var>value</var>).</li><li>Perform AsyncGeneratorEnqueue(<var>generator</var>, <var>completion</var>, <var>promiseCapability</var>).</li><li>Let <var>state</var> be <var>generator</var>.[[AsyncGeneratorState]].</li><li>If <var>state</var> is either <emu-const>suspended-start</emu-const> or <emu-const>completed</emu-const>, then<ol><li>Set <var>generator</var>.[[AsyncGeneratorState]] to <emu-const>draining-queue</emu-const>.</li><li>Perform AsyncGeneratorAwaitReturn(<var>generator</var>).</li></ol></li><li>Else if <var>state</var> is <emu-const>suspended-yield</emu-const>, then<ol><li>Perform AsyncGeneratorResume(<var>generator</var>, <var>completion</var>).</li></ol></li><li>Else,<ol><li>Assert: <var>state</var> is either <emu-const>executing</emu-const> or <emu-const>draining-queue</emu-const>.</li></ol></li><li>Return <var>promiseCapability</var>.[[Promise]].</li></ol></emu-alg>\n      ","6.1.4":"<emu-alg><ol><li>Let <var>generator</var> be the <b>this</b> value.</li><li>Let <var>promiseCapability</var> be ! NewPromiseCapability(<code class=\"emu-intrinsic\">%Promise%</code>).</li><li>Let <var>result</var> be Completion(AsyncGeneratorValidate(<var>generator</var>, <emu-const>empty</emu-const>)).</li><li>IfAbruptRejectPromise(<var>result</var>, <var>promiseCapability</var>).</li><li>Let <var>state</var> be <var>generator</var>.[[AsyncGeneratorState]].</li><li>If <var>state</var> is <emu-const>suspended-start</emu-const>, then<ol><li>Set <var>generator</var>.[[AsyncGeneratorState]] to <emu-const>completed</emu-const>.</li><li>Set <var>state</var> to <emu-const>completed</emu-const>.</li></ol></li><li>If <var>state</var> is <emu-const>completed</emu-const>, then<ol><li>Perform ! Call(<var>promiseCapability</var>.[[Reject]], <b>undefined</b>, « <var>exception</var> »).</li><li>Return <var>promiseCapability</var>.[[Promise]].</li></ol></li><li>Let <var>completion</var> be ThrowCompletion(<var>exception</var>).</li><li>Perform AsyncGeneratorEnqueue(<var>generator</var>, <var>completion</var>, <var>promiseCapability</var>).</li><li>If <var>state</var> is <emu-const>suspended-yield</emu-const>, then<ol><li>Perform AsyncGeneratorResume(<var>generator</var>, <var>completion</var>).</li></ol></li><li>Else,<ol><li>Assert: <var>state</var> is either <emu-const>executing</emu-const> or <emu-const>draining-queue</emu-const>.</li></ol></li><li>Return <var>promiseCapability</var>.[[Promise]].</li></ol></emu-alg>\n      ","6.1.5":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"AsyncGenerator\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","6.2":"<p>AsyncGenerator instances are initially created with the internal slots described below:</p>\n      <emu-table id=\"table-internal-slots-of-asyncgenerator-instances\" caption=\"Internal Slots of AsyncGenerator Instances\">\n        <table>\n          <thead>\n            <tr>\n              <th>Internal Slot</th>\n              <th>Type</th>\n              <th>Description</th>\n            </tr>\n          </thead>\n          <tr>\n            <td>[[AsyncGeneratorState]]</td>\n            <td><emu-const>suspended-start</emu-const>, <emu-const>suspended-yield</emu-const>, <emu-const>executing</emu-const>, <emu-const>draining-queue</emu-const>, or <emu-const>completed</emu-const></td>\n            <td>The current execution state of the async generator.</td>\n          </tr>\n          <tr>\n            <td>[[AsyncGeneratorContext]]</td>\n            <td>an execution context</td>\n            <td>The execution context that is used when executing the code of this async generator.</td>\n          </tr>\n          <tr>\n            <td>[[AsyncGeneratorQueue]]</td>\n            <td>a List of AsyncGeneratorRequest Records</td>\n            <td>Records which represent requests to resume the async generator. Except during state transitions, it is non-empty if and only if [[AsyncGeneratorState]] is either <emu-const>executing</emu-const> or <emu-const>draining-queue</emu-const>.</td>\n          </tr>\n          <tr>\n            <td>[[GeneratorBrand]]</td>\n            <td>a String or <emu-const>empty</emu-const></td>\n            <td>A brand used to distinguish different kinds of async generators. The [[GeneratorBrand]] of async generators declared by ECMAScript source text is always <emu-const>empty</emu-const>.</td>\n          </tr>\n        </table>\n      </emu-table>\n    ","6.3":"","6.3.1":"<p>An <dfn variants=\"AsyncGeneratorRequests\">AsyncGeneratorRequest</dfn> is a Record value used to store information about how an async generator should be resumed and contains capabilities for fulfilling or rejecting the corresponding promise.</p>\n        <p>They have the following fields:</p>\n        <emu-table caption=\"AsyncGeneratorRequest Record Fields\">\n          <table>\n            <thead>\n              <tr>\n                <th>Field Name</th>\n                <th>Value</th>\n                <th>Meaning</th>\n              </tr>\n            </thead>\n            <tr>\n              <td>[[Completion]]</td>\n              <td>a Completion Record</td>\n              <td>The Completion Record which should be used to resume the async generator.</td>\n            </tr>\n            <tr>\n              <td>[[Capability]]</td>\n              <td>a PromiseCapability Record</td>\n              <td>The promise capabilities associated with this request.</td>\n            </tr>\n          </table>\n        </emu-table>\n      ","6.3.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: <var>generator</var>.[[AsyncGeneratorState]] is <emu-const>suspended-start</emu-const>.</li><li>Let <var>genContext</var> be the running execution context.</li><li>Set the Generator component of <var>genContext</var> to <var>generator</var>.</li><li>Let <var>closure</var> be a new Abstract Closure with no parameters that captures <var>generatorBody</var> and performs the following steps when called:<ol><li>Let <var>acGenContext</var> be the running execution context.</li><li>Let <var>acGenerator</var> be the Generator component of <var>acGenContext</var>.</li><li>If <var>generatorBody</var> is a Parse Node, then<ol><li>Let <var>result</var> be Completion(Evaluation of <var>generatorBody</var>).</li></ol></li><li>Else,<ol><li>Assert: <var>generatorBody</var> is an Abstract Closure with no parameters.</li><li>Let <var>result</var> be Completion(<var>generatorBody</var>()).</li></ol></li><li>Assert: If we return here, the async generator either threw an exception or performed either an implicit or explicit return.</li><li>Remove <var>acGenContext</var> from the execution context stack and restore the execution context that is at the top of the execution context stack as the running execution context.</li><li>Set <var>acGenerator</var>.[[AsyncGeneratorState]] to <emu-const>draining-queue</emu-const>.</li><li>If <var>result</var> is a normal completion, set <var>result</var> to NormalCompletion(<b>undefined</b>).</li><li>If <var>result</var> is a return completion, set <var>result</var> to NormalCompletion(<var>result</var>.[[Value]]).</li><li>Perform AsyncGeneratorCompleteStep(<var>acGenerator</var>, <var>result</var>, <b>true</b>).</li><li>Perform AsyncGeneratorDrainQueue(<var>acGenerator</var>).</li><li>Return NormalCompletion(<b>undefined</b>).</li></ol></li><li>Set the code evaluation state of <var>genContext</var> such that when evaluation is resumed for that execution context, <var>closure</var> will be called with no arguments.</li><li>Set <var>generator</var>.[[AsyncGeneratorContext]] to <var>genContext</var>.</li><li>Set <var>generator</var>.[[AsyncGeneratorQueue]] to a new empty List.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","6.3.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Perform ? RequireInternalSlot(<var>generator</var>, [[AsyncGeneratorContext]]).</li><li>Perform ? RequireInternalSlot(<var>generator</var>, [[AsyncGeneratorState]]).</li><li>Perform ? RequireInternalSlot(<var>generator</var>, [[AsyncGeneratorQueue]]).</li><li>If <var>generator</var>.[[GeneratorBrand]] is not <var>generatorBrand</var>, throw a <b>TypeError</b> exception.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","6.3.4":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>request</var> be AsyncGeneratorRequest { [[Completion]]: <var>completion</var>, [[Capability]]: <var>promiseCapability</var> }.</li><li>Append <var>request</var> to <var>generator</var>.[[AsyncGeneratorQueue]].</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","6.3.5":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: <var>generator</var>.[[AsyncGeneratorQueue]] is not empty.</li><li>Let <var>next</var> be the first element of <var>generator</var>.[[AsyncGeneratorQueue]].</li><li>Remove the first element from <var>generator</var>.[[AsyncGeneratorQueue]].</li><li>Let <var>promiseCapability</var> be <var>next</var>.[[Capability]].</li><li>Let <var>value</var> be <var>completion</var>.[[Value]].</li><li>If <var>completion</var> is a throw completion, then<ol><li>Perform ! Call(<var>promiseCapability</var>.[[Reject]], <b>undefined</b>, « <var>value</var> »).</li></ol></li><li>Else,<ol><li>Assert: <var>completion</var> is a normal completion.</li><li>If <var>realm</var> is present, then<ol><li>Let <var>oldRealm</var> be the running execution context's Realm.</li><li>Set the running execution context's Realm to <var>realm</var>.</li><li>Let <var>iteratorResult</var> be CreateIteratorResultObject(<var>value</var>, <var>done</var>).</li><li>Set the running execution context's Realm to <var>oldRealm</var>.</li></ol></li><li>Else,<ol><li>Let <var>iteratorResult</var> be CreateIteratorResultObject(<var>value</var>, <var>done</var>).</li></ol></li><li>Perform ! <emu-meta effects=\"user-code\">Call(<var>promiseCapability</var>.[[Resolve]], <b>undefined</b>, « <var>iteratorResult</var> »)</emu-meta>.</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","6.3.6":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: <var>generator</var>.[[AsyncGeneratorState]] is either <emu-const>suspended-start</emu-const> or <emu-const>suspended-yield</emu-const>.</li><li>Let <var>genContext</var> be <var>generator</var>.[[AsyncGeneratorContext]].</li><li>Set <var>generator</var>.[[AsyncGeneratorState]] to <emu-const>executing</emu-const>.</li><li>Perform ! RunSuspendedContext(<var>genContext</var>, <var>completion</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","6.3.7":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If <var>resumptionValue</var> is not a return completion, return ? <var>resumptionValue</var>.</li><li>Let <var>awaited</var> be Completion(Await(<var>resumptionValue</var>.[[Value]])).</li><li>If <var>awaited</var> is a throw completion, return ? <var>awaited</var>.</li><li>Assert: <var>awaited</var> is a normal completion.</li><li>Return ReturnCompletion(<var>awaited</var>.[[Value]]).</li></ol></emu-alg>\n      ","6.3.8":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>genContext</var> be the running execution context.</li><li>Assert: <var>genContext</var> is the execution context of a generator.</li><li>Let <var>generator</var> be the value of the Generator component of <var>genContext</var>.</li><li>Assert: GetGeneratorKind() is <emu-const>async</emu-const>.</li><li>Let <var>completion</var> be NormalCompletion(<var>value</var>).</li><li>Assert: The execution context stack has at least two elements.</li><li>Let <var>previousContext</var> be the second to top element of the execution context stack.</li><li>Let <var>previousRealm</var> be <var>previousContext</var>'s Realm.</li><li>Perform AsyncGeneratorCompleteStep(<var>generator</var>, <var>completion</var>, <b>false</b>, <var>previousRealm</var>).</li><li>Let <var>queue</var> be <var>generator</var>.[[AsyncGeneratorQueue]].</li><li>If <var>queue</var> is not empty, then<ol><li>NOTE: Execution continues without suspending the generator.</li><li>Let <var>toYield</var> be the first element of <var>queue</var>.</li><li>Let <var>resumptionValue</var> be Completion(<var>toYield</var>.[[Completion]]).</li><li>Return ? AsyncGeneratorUnwrapYieldResumption(<var>resumptionValue</var>).</li></ol></li><li>Set <var>generator</var>.[[AsyncGeneratorState]] to <emu-const>suspended-yield</emu-const>.</li><li>Remove <var>genContext</var> from the execution context stack and restore the execution context that is at the top of the execution context stack as the running execution context.</li><li>Let <var>callerContext</var> be the running execution context.</li><li>Resume <var>callerContext</var> passing <b>undefined</b>. If <var>genContext</var> is ever resumed again, let <var>resumptionValue</var> be the Completion Record with which it is resumed.</li><li>Assert: If control reaches here, then <var>genContext</var> is the running execution context again.</li><li>Return ? AsyncGeneratorUnwrapYieldResumption(<var>resumptionValue</var>).</li></ol></emu-alg>\n      ","6.3.9":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: <var>generator</var>.[[AsyncGeneratorState]] is <emu-const>draining-queue</emu-const>.</li><li>Let <var>queue</var> be <var>generator</var>.[[AsyncGeneratorQueue]].</li><li>Assert: <var>queue</var> is not empty.</li><li>Let <var>next</var> be the first element of <var>queue</var>.</li><li>Let <var>completion</var> be Completion(<var>next</var>.[[Completion]]).</li><li>Assert: <var>completion</var> is a return completion.</li><li>Let <var>promiseCompletion</var> be Completion(PromiseResolve(<code class=\"emu-intrinsic\">%Promise%</code>, <var>completion</var>.[[Value]])).</li><li>If <var>promiseCompletion</var> is an abrupt completion, then<ol><li>Perform AsyncGeneratorCompleteStep(<var>generator</var>, <var>promiseCompletion</var>, <b>true</b>).</li><li>Perform AsyncGeneratorDrainQueue(<var>generator</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></li><li>Assert: <var>promiseCompletion</var> is a normal completion.</li><li>Let <var>promise</var> be <var>promiseCompletion</var>.[[Value]].</li><li>Let <var>fulfilledClosure</var> be a new Abstract Closure with parameters (<var>value</var>) that captures <var>generator</var> and performs the following steps when called:<ol><li>Assert: <var>generator</var>.[[AsyncGeneratorState]] is <emu-const>draining-queue</emu-const>.</li><li>Let <var>result</var> be NormalCompletion(<var>value</var>).</li><li>Perform AsyncGeneratorCompleteStep(<var>generator</var>, <var>result</var>, <b>true</b>).</li><li>Perform AsyncGeneratorDrainQueue(<var>generator</var>).</li><li>Return NormalCompletion(<b>undefined</b>).</li></ol></li><li>Let <var>onFulfilled</var> be CreateBuiltinFunction(<var>fulfilledClosure</var>, 1, <b>\"\"</b>, « »).</li><li>Let <var>rejectedClosure</var> be a new Abstract Closure with parameters (<var>reason</var>) that captures <var>generator</var> and performs the following steps when called:<ol><li>Assert: <var>generator</var>.[[AsyncGeneratorState]] is <emu-const>draining-queue</emu-const>.</li><li>Let <var>result</var> be ThrowCompletion(<var>reason</var>).</li><li>Perform AsyncGeneratorCompleteStep(<var>generator</var>, <var>result</var>, <b>true</b>).</li><li>Perform AsyncGeneratorDrainQueue(<var>generator</var>).</li><li>Return NormalCompletion(<b>undefined</b>).</li></ol></li><li>Let <var>onRejected</var> be CreateBuiltinFunction(<var>rejectedClosure</var>, 1, <b>\"\"</b>, « »).</li><li>Perform PerformPromiseThen(<var>promise</var>, <var>onFulfilled</var>, <var>onRejected</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","6.3.10":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It drains the generator's AsyncGeneratorQueue until it encounters an AsyncGeneratorRequest which holds a return completion.</dd>\n        </dl>\n        <emu-alg><ol><li>Assert: <var>generator</var>.[[AsyncGeneratorState]] is <emu-const>draining-queue</emu-const>.</li><li>Let <var>queue</var> be <var>generator</var>.[[AsyncGeneratorQueue]].</li><li>Repeat, while <var>queue</var> is not empty,<ol><li>Let <var>next</var> be the first element of <var>queue</var>.</li><li>Let <var>completion</var> be Completion(<var>next</var>.[[Completion]]).</li><li>If <var>completion</var> is a return completion, then<ol><li>Perform AsyncGeneratorAwaitReturn(<var>generator</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></li><li>If <var>completion</var> is a normal completion, then<ol><li>Set <var>completion</var> to NormalCompletion(<b>undefined</b>).</li></ol></li><li>Perform AsyncGeneratorCompleteStep(<var>generator</var>, <var>completion</var>, <b>true</b>).</li></ol></li><li>Set <var>generator</var>.[[AsyncGeneratorState]] to <emu-const>completed</emu-const>.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","7.1":"<p>The AsyncFunction constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%AsyncFunction%</code></dfn>.</li>\n        <li>is a subclass of <code>Function</code>.</li>\n        <li>creates and initializes a new AsyncFunction when called as a function rather than as a constructor. Thus the function call <code>AsyncFunction(…)</code> is equivalent to the object creation expression <code>new AsyncFunction(…)</code> with the same arguments.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified AsyncFunction behaviour must include a <code>super</code> call to the AsyncFunction constructor to create and initialize a subclass instance with the internal slots necessary for built-in async function behaviour. All ECMAScript syntactic forms for defining async function objects create direct instances of AsyncFunction. There is no syntactic means to create instances of AsyncFunction subclasses.</li>\n      </ul>\n\n      ","7.1.1":"<p>The last argument (if any) specifies the body (executable code) of an async function. Any preceding arguments specify formal parameters.</p>\n        <p>This function performs the following steps when called:</p>\n\n        <emu-alg><ol><li>Let <var>activeFunc</var> be the active function object.</li><li>If <var>bodyArg</var> is not present, set <var>bodyArg</var> to the empty String.</li><li>Return ? CreateDynamicFunction(<var>activeFunc</var>, NewTarget, <emu-const>async</emu-const>, <var>parameterArgs</var>, <var>bodyArg</var>).</li></ol></emu-alg>\n\n        <emu-note>See NOTE for 20.2.1.1.</emu-note>\n      ","7.2":"<p>The AsyncFunction constructor:</p>\n      <ul>\n        <li>is a standard built-in function object that inherits from the Function constructor.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function%</code>.</li>\n        <li oldids=\"sec-async-function-constructor-length\">has a <b>\"length\"</b> property whose value is <b>1</b><sub>𝔽</sub>.</li>\n        <li>has a <b>\"name\"</b> property whose value is <b>\"AsyncFunction\"</b>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","7.2.1":"<p>The initial value of <code>AsyncFunction.prototype</code> is the AsyncFunction prototype object.</p>\n\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","7.3":"<p>The <dfn>AsyncFunction prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%AsyncFunction.prototype%</code></dfn>.</li>\n        <li>is an ordinary object.</li>\n        <li>is not a function object and does not have an [[ECMAScriptCode]] internal slot or any other of the internal slots listed in table-internal-slots-of-ecmascript-function-objects.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n      </ul>\n\n      ","7.3.1":"<p>The initial value of <code>AsyncFunction.prototype.constructor</code> is <code class=\"emu-intrinsic\">%AsyncFunction%</code>.</p>\n\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","7.3.2":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"AsyncFunction\"</b>.</p>\n\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","7.4":"<p>Every AsyncFunction instance is an ECMAScript function object and has the internal slots listed in table-internal-slots-of-ecmascript-function-objects. The value of the [[IsClassConstructor]] internal slot for all such instances is <b>false</b>. AsyncFunction instances are not constructors and do not have a [[Construct]] internal method. AsyncFunction instances do not have a prototype property as they are not constructable.</p>\n      <p>Each AsyncFunction instance has the following own properties:</p>\n\n      ","7.4.1":"<p>The specification for the <b>\"length\"</b> property of Function instances given in 20.2.4.1 also applies to AsyncFunction instances.</p>\n      ","7.4.2":"<p>The specification for the <b>\"name\"</b> property of Function instances given in 20.2.4.2 also applies to AsyncFunction instances.</p>\n      ","7.5":"","7.5.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>runningContext</var> be the running execution context.</li><li>Let <var>asyncContext</var> be a copy of <var>runningContext</var>.</li><li>NOTE: Copying the execution state is required for AsyncBlockStart to resume its execution. It is ill-defined to resume a currently executing context.</li><li>Perform AsyncBlockStart(<var>promiseCapability</var>, <var>asyncFunctionBody</var>, <var>asyncContext</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","7.5.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>closure</var> be a new Abstract Closure with no parameters that captures <var>promiseCapability</var> and <var>asyncBody</var> and performs the following steps when called:<ol><li>Let <var>acAsyncContext</var> be the running execution context.</li><li>If <var>asyncBody</var> is a Parse Node, then<ol><li>Let <var>result</var> be Completion(Evaluation of <var>asyncBody</var>).</li></ol></li><li>Else,<ol><li>Assert: <var>asyncBody</var> is an Abstract Closure with no parameters.</li><li>Let <var>result</var> be Completion(<var>asyncBody</var>()).</li></ol></li><li>Assert: If we return here, the async function either threw an exception or performed an implicit or explicit return; all awaiting is done.</li><li>Remove <var>acAsyncContext</var> from the execution context stack and restore the execution context that is at the top of the execution context stack as the running execution context.</li><li>If <var>result</var> is a normal completion, then<ol><li>Perform ! Call(<var>promiseCapability</var>.[[Resolve]], <b>undefined</b>, « <b>undefined</b> »).</li></ol></li><li>Else if <var>result</var> is a return completion, then<ol><li>Perform ! <emu-meta effects=\"user-code\">Call(<var>promiseCapability</var>.[[Resolve]], <b>undefined</b>, « <var>result</var>.[[Value]] »)</emu-meta>.</li></ol></li><li>Else,<ol><li>Assert: <var>result</var> is a throw completion.</li><li>Perform ! Call(<var>promiseCapability</var>.[[Reject]], <b>undefined</b>, « <var>result</var>.[[Value]] »).</li></ol></li><li>Return NormalCompletion(<emu-const>unused</emu-const>).</li></ol></li><li>Set the code evaluation state of <var>asyncContext</var> such that when evaluation is resumed for that execution context, <var>closure</var> will be called with no arguments.</li><li>Let <var>result</var> be ! RunSuspendedContext(<var>asyncContext</var>, NormalCompletion(<emu-const>empty</emu-const>)).</li><li>Assert: <var>result</var> is <emu-const>unused</emu-const>.</li><li>NOTE: The possible sources of <var>result</var> values are Await or, if the async function doesn't await anything, step step-asyncblockstart-return-undefined above.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","7.5.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>asyncContext</var> be the running execution context.</li><li>Let <var>promise</var> be ? PromiseResolve(<code class=\"emu-intrinsic\">%Promise%</code>, <var>value</var>).</li><li>Let <var>fulfilledClosure</var> be a new Abstract Closure with parameters (<var>v</var>) that captures <var>asyncContext</var> and performs the following steps when called:<ol><li>Perform Completion(RunSuspendedContext(<var>asyncContext</var>, NormalCompletion(<var>v</var>))).</li><li>NOTE: The Completion Record returned by RunSuspendedContext is intentionally ignored.</li><li>Return NormalCompletion(<b>undefined</b>).</li></ol></li><li>Let <var>onFulfilled</var> be CreateBuiltinFunction(<var>fulfilledClosure</var>, 1, <b>\"\"</b>, « »).</li><li>Let <var>rejectedClosure</var> be a new Abstract Closure with parameters (<var>reason</var>) that captures <var>asyncContext</var> and performs the following steps when called:<ol><li>Perform Completion(RunSuspendedContext(<var>asyncContext</var>, ThrowCompletion(<var>reason</var>))).</li><li>NOTE: The Completion Record returned by RunSuspendedContext is intentionally ignored.</li><li>Return NormalCompletion(<b>undefined</b>).</li></ol></li><li>Let <var>onRejected</var> be CreateBuiltinFunction(<var>rejectedClosure</var>, 1, <b>\"\"</b>, « »).</li><li>Perform PerformPromiseThen(<var>promise</var>, <var>onFulfilled</var>, <var>onRejected</var>).</li><li>Remove <var>asyncContext</var> from the execution context stack and restore the execution context that is at the top of the execution context stack as the running execution context.</li><li>Let <var>callerContext</var> be the running execution context.</li><li>Resume <var>callerContext</var> passing <emu-const>empty</emu-const>. If <var>asyncContext</var> is ever resumed again, let <var>completion</var> be the Completion Record with which it is resumed.</li><li>Assert: If control reaches here, then <var>asyncContext</var> is the running execution context again.</li><li>Return <var>completion</var>.</li></ol></emu-alg>\n      "};
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
