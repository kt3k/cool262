// Generated from ecma262/spec.html — do not edit by hand.
const _sections = {"1":"<p>Maps are collections of key/value pairs where both the keys and values may be arbitrary ECMAScript language values. A distinct key value may only occur in one key/value pair within the Map's collection. Distinct key values are discriminated using the semantics of the SameValueZero comparison algorithm.</p>\n    <p>Maps must be implemented using either hash tables or other mechanisms that, on average, provide access times that are sublinear on the number of elements in the collection. The data structure used in this specification is only intended to describe the required observable semantics of Maps. It is not intended to be a viable implementation model.</p>\n\n    ","2":"<p><dfn variants=\"Set object\">Set objects</dfn> are collections of ECMAScript language values. A Set may contain each distinct value at most once. Distinct values are discriminated using the semantics of the SameValueZero comparison algorithm.</p>\n    <p>Set objects must be implemented using either hash tables or other mechanisms that, on average, provide access times that are sublinear on the number of elements in the collection. The data structure used in this specification is only intended to describe the required observable semantics of Set objects. It is not intended to be a viable implementation model.</p>\n\n    ","3":"<p>WeakMaps are collections of key/value pairs where the keys are objects and/or symbols and values may be arbitrary ECMAScript language values. A WeakMap may be queried to see if it contains a key/value pair with a specific key, but no mechanism is provided for enumerating the values it holds as keys. In certain conditions, values which are not live are removed as WeakMap keys, as described in <a href=\"/executable-code-and-execution-contexts#sec-weakref-execution\">9.9.3</a>.</p>\n    <p>An implementation may impose an arbitrarily determined latency between the time a key/value pair of a WeakMap becomes inaccessible and the time when the key/value pair is removed from the WeakMap. If this latency was observable to ECMAScript program, it would be a source of indeterminacy that could impact program execution. For that reason, an ECMAScript implementation must not provide any means to observe a key of a WeakMap that does not require the observer to present the observed key.</p>\n    <p>WeakMaps must be implemented using either hash tables or other mechanisms that, on average, provide access times that are sublinear on the number of key/value pairs in the collection. The data structure used in this specification is only intended to describe the required observable semantics of WeakMaps. It is not intended to be a viable implementation model.</p>\n    <emu-note>\n      <p>WeakMap and WeakSet are intended to provide mechanisms for dynamically associating state with an object or symbol in a manner that does not “leak” memory resources if, in the absence of the WeakMap or WeakSet instance, the object or symbol otherwise became inaccessible and subject to resource reclamation by the implementation's garbage collection mechanisms. This characteristic can be achieved by using an inverted per-object/symbol mapping of WeakMap or WeakSet instances to keys. Alternatively, each WeakMap or WeakSet instance may internally store its key and value data, but this approach requires coordination between the WeakMap or WeakSet implementation and the garbage collector. The following references describe mechanism that may be useful to implementations of WeakMap and WeakSet:</p>\n      <p>Barry Hayes. 1997. Ephemerons: a new finalization mechanism. In <i>Proceedings of the 12th ACM SIGPLAN conference on Object-oriented programming, systems, languages, and applications (OOPSLA '97)</i>, A. Michael Berman (Ed.). ACM, New York, NY, USA, 176-183, <a href=\"http://doi.acm.org/10.1145/263698.263733\">http://doi.acm.org/10.1145/263698.263733</a>.</p>\n      <p>Alexandra Barros, Roberto Ierusalimschy, Eliminating Cycles in Weak Tables. Journal of Universal Computer Science - J.UCS, vol. 14, no. 21, pp. 3481-3497, 2008, <a href=\"http://www.jucs.org/jucs_14_21/eliminating_cycles_in_weak\">http://www.jucs.org/jucs_14_21/eliminating_cycles_in_weak</a></p>\n    </emu-note>\n\n    ","4":"<p>WeakSets are collections of objects and/or symbols. A distinct object or symbol may only occur once as an element of a WeakSet's collection. A WeakSet may be queried to see if it contains a specific value, but no mechanism is provided for enumerating the values it holds. In certain conditions, values which are not live are removed as WeakSet elements, as described in <a href=\"/executable-code-and-execution-contexts#sec-weakref-execution\">9.9.3</a>.</p>\n    <p>An implementation may impose an arbitrarily determined latency between the time a value contained in a WeakSet becomes inaccessible and the time when the value is removed from the WeakSet. If this latency was observable to ECMAScript program, it would be a source of indeterminacy that could impact program execution. For that reason, an ECMAScript implementation must not provide any means to determine if a WeakSet contains a particular value that does not require the observer to present the observed value.</p>\n    <p>WeakSets must be implemented using either hash tables or other mechanisms that, on average, provide access times that are sublinear on the number of elements in the collection. The data structure used in this specification is only intended to describe the required observable semantics of WeakSets. It is not intended to be a viable implementation model.</p>\n    <emu-note>\n      <p>See the NOTE in <a href=\"/keyed-collections#sec-weakmap-objects\">24.3</a>.</p>\n    </emu-note>\n\n    ","5":"","":"","1.1":"<p>The Map constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Map%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"Map\"</b> property of the global object.</li>\n        <li>creates and initializes a new Map when called as a constructor.</li>\n        <li>is not intended to be called as a function and will throw an exception when called in that manner.</li>\n        <li>may be used as the value in an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified Map behaviour must include a <code>super</code> call to the Map constructor to create and initialize the subclass instance with the internal state necessary to support the <code>Map.prototype</code> built-in methods.</li>\n      </ul>\n\n      ","1.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>map</var> be ? OrdinaryCreateFromConstructor(NewTarget, <b>\"<code class=\"emu-intrinsic\">%Map.prototype%</code>\"</b>, « [[MapData]] »).</li><li>Set <var>map</var>.[[MapData]] to a new empty List.</li><li>If <var>iterable</var> is either <b>undefined</b> or <b>null</b>, return <var>map</var>.</li><li>Let <var>adder</var> be ? Get(<var>map</var>, <b>\"set\"</b>).</li><li>If IsCallable(<var>adder</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return ? AddEntriesFromIterable(<var>map</var>, <var>iterable</var>, <var>adder</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>If the parameter <var>iterable</var> is present, it is expected to be an object that implements a <code class=\"emu-intrinsic\">%Symbol.iterator%</code> method that returns an iterator object that produces a two element array-like object whose first element is a value that will be used as a Map key and whose second element is the value to associate with that key.</p>\n        </emu-note>\n      ","1.1.2":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd><var>adder</var> will be invoked, with <var>target</var> as the receiver.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>iteratorRecord</var> be ? GetIterator(<var>iterable</var>, <emu-const>sync</emu-const>).</li><li>Repeat,<ol><li>Let <var>next</var> be ? IteratorStepValue(<var>iteratorRecord</var>).</li><li>If <var>next</var> is <emu-const>done</emu-const>, return <var>target</var>.</li><li>If <var>next</var> is not an Object, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>TypeError</b> object).</li><li>Return ? IteratorClose(<var>iteratorRecord</var>, <var>error</var>).</li></ol></li><li>Let <var>key</var> be Completion(Get(<var>next</var>, <b>\"0\"</b>)).</li><li>IfAbruptCloseIterator(<var>key</var>, <var>iteratorRecord</var>).</li><li>Let <var>value</var> be Completion(Get(<var>next</var>, <b>\"1\"</b>)).</li><li>IfAbruptCloseIterator(<var>value</var>, <var>iteratorRecord</var>).</li><li>Let <var>status</var> be Completion(Call(<var>adder</var>, <var>target</var>, « <var>key</var>, <var>value</var> »)).</li><li>IfAbruptCloseIterator(<var>status</var>, <var>iteratorRecord</var>).</li></ol></li></ol></emu-alg>\n        <emu-note>\n          <p>The parameter <var>iterable</var> is expected to be an object that implements a <code class=\"emu-intrinsic\">%Symbol.iterator%</code> method that returns an iterator object that produces a two element array-like object whose first element is a value that will be used as a Map key and whose second element is the value to associate with that key.</p>\n        </emu-note>\n      ","1.2":"<p>The Map constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","1.2.1":"<emu-note>\n          <p><var>callback</var> should be a function that accepts two arguments. <code>groupBy</code> calls <var>callback</var> once for each element in <var>items</var>, in ascending order, and constructs a new Map. Each value returned by <var>callback</var> is used as a key in the Map. For each such key, the result Map has an entry whose key is that key and whose value is an array containing all the elements for which <var>callback</var> returned that key.</p>\n          <p><var>callback</var> is called with two arguments: the value of the element and the index of the element.</p>\n          <p>The return value of <code>groupBy</code> is a Map.</p>\n        </emu-note>\n        <p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>groups</var> be ? GroupBy(<var>items</var>, <var>callback</var>, <emu-const>collection</emu-const>).</li><li>Let <var>map</var> be ! Construct(<code class=\"emu-intrinsic\">%Map%</code>).</li><li>For each Record { [[Key]], [[Elements]] } <var>group</var> of <var>groups</var>, do<ol><li>Let <var>elements</var> be CreateArrayFromList(<var>group</var>.[[Elements]]).</li><li>Let <var>entry</var> be the Record { [[Key]]: <var>group</var>.[[Key]], [[Value]]: <var>elements</var> }.</li><li>Append <var>entry</var> to <var>map</var>.[[MapData]].</li></ol></li><li>Return <var>map</var>.</li></ol></emu-alg>\n      ","1.2.2":"<p>The initial value of <code>Map.prototype</code> is the Map prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.2.3":"<p><code>Map[<code class=\"emu-intrinsic\">%Symbol.species%</code>]</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Return the <b>this</b> value.</li></ol></emu-alg>\n        <p>The value of the <b>\"name\"</b> property of this function is <b>\"get [Symbol.species]\"</b>.</p>\n        <emu-note>\n          <p>Methods that create derived collection objects should call <code class=\"emu-intrinsic\">%Symbol.species%</code> to determine the constructor to use to create the derived objects. Subclass constructor may over-ride <code class=\"emu-intrinsic\">%Symbol.species%</code> to change the default constructor assignment.</p>\n        </emu-note>\n      ","1.3":"<p>The <dfn>Map prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Map.prototype%</code></dfn>.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n        <li>is an ordinary object.</li>\n        <li>does not have a [[MapData]] internal slot.</li>\n      </ul>\n\n      ","1.3.1":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>map</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>map</var>, [[MapData]]).</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>map</var>.[[MapData]], do<ol><li>Set <var>entry</var>.[[Key]] to <emu-const>empty</emu-const>.</li><li>Set <var>entry</var>.[[Value]] to <emu-const>empty</emu-const>.</li></ol></li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The existing [[MapData]] List is preserved because there may be existing Map Iterator objects that are suspended midway through iterating over that List.</p>\n        </emu-note>\n      ","1.3.2":"<p>The initial value of <code>Map.prototype.constructor</code> is <code class=\"emu-intrinsic\">%Map%</code>.</p>\n      ","1.3.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>map</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>map</var>, [[MapData]]).</li><li>Set <var>key</var> to CanonicalizeKeyedCollectionKey(<var>key</var>).</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>map</var>.[[MapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, then<ol><li>Set <var>entry</var>.[[Key]] to <emu-const>empty</emu-const>.</li><li>Set <var>entry</var>.[[Value]] to <emu-const>empty</emu-const>.</li><li>Return <b>true</b>.</li></ol></li></ol></li><li>Return <b>false</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The value <emu-const>empty</emu-const> is used as a specification device to indicate that an entry has been deleted. Actual implementations may take other actions such as physically removing the entry from internal data structures.</p>\n        </emu-note>\n      ","1.3.4":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>map</var> be the <b>this</b> value.</li><li>Return ? CreateMapIterator(<var>map</var>, <emu-const>key+value</emu-const>).</li></ol></emu-alg>\n      ","1.3.5":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>map</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>map</var>, [[MapData]]).</li><li>If IsCallable(<var>callback</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>entries</var> be <var>map</var>.[[MapData]].</li><li>Let <var>numEntries</var> be the number of elements in <var>entries</var>.</li><li>Let <var>index</var> be 0.</li><li>Repeat, while <var>index</var> &lt; <var>numEntries</var>,<ol><li>Let <var>e</var> be <var>entries</var>[<var>index</var>].</li><li>Set <var>index</var> to <var>index</var> + 1.</li><li>If <var>e</var>.[[Key]] is not <emu-const>empty</emu-const>, then<ol><li>Perform ? Call(<var>callback</var>, <var>thisArg</var>, « <var>e</var>.[[Value]], <var>e</var>.[[Key]], <var>map</var> »).</li><li>NOTE: The number of elements in <var>entries</var> may have increased during execution of <var>callback</var>.</li><li>Set <var>numEntries</var> to the number of elements in <var>entries</var>.</li></ol></li></ol></li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p><var>callback</var> should be a function that accepts three arguments. <code>forEach</code> calls <var>callback</var> once for each key/value pair present in the Map, in key insertion order. <var>callback</var> is called only for keys of the Map which actually exist; it is not called for keys that have been deleted from the Map.</p>\n          <p>If a <var>thisArg</var> parameter is provided, it will be used as the <b>this</b> value for each invocation of <var>callback</var>. If it is not provided, <b>undefined</b> is used instead.</p>\n          <p><var>callback</var> is called with three arguments: the value of the item, the key of the item, and the Map being traversed.</p>\n          <p><code>forEach</code> does not directly mutate the object on which it is called but the object may be mutated by the calls to <var>callback</var>. Each entry of a map's [[MapData]] is only visited once. New keys added after the call to <code>forEach</code> begins are visited. A key will be revisited if it is deleted after it has been visited and then re-added before the <code>forEach</code> call completes. Keys that are deleted after the call to <code>forEach</code> begins and before being visited are not visited unless the key is added again before the <code>forEach</code> call completes.</p>\n        </emu-note>\n      ","1.3.6":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>map</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>map</var>, [[MapData]]).</li><li>Set <var>key</var> to CanonicalizeKeyedCollectionKey(<var>key</var>).</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>map</var>.[[MapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, return <var>entry</var>.[[Value]].</li></ol></li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n      ","1.3.7":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>map</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>map</var>, [[MapData]]).</li><li>Set <var>key</var> to CanonicalizeKeyedCollectionKey(<var>key</var>).</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>map</var>.[[MapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, return <var>entry</var>.[[Value]].</li></ol></li><li>Let <var>entry</var> be the Record { [[Key]]: <var>key</var>, [[Value]]: <var>value</var> }.</li><li>Append <var>entry</var> to <var>map</var>.[[MapData]].</li><li>Return <var>value</var>.</li></ol></emu-alg>\n      ","1.3.8":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>map</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>map</var>, [[MapData]]).</li><li>If IsCallable(<var>callback</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Set <var>key</var> to CanonicalizeKeyedCollectionKey(<var>key</var>).</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>map</var>.[[MapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, return <var>entry</var>.[[Value]].</li></ol></li><li>Let <var>value</var> be ? Call(<var>callback</var>, <b>undefined</b>, « <var>key</var> »).</li><li>NOTE: The Map may have been modified during execution of <var>callback</var>.</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>map</var>.[[MapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, then<ol><li>Set <var>entry</var>.[[Value]] to <var>value</var>.</li><li>Return <var>value</var>.</li></ol></li></ol></li><li>Let <var>entry</var> be the Record { [[Key]]: <var>key</var>, [[Value]]: <var>value</var> }.</li><li>Append <var>entry</var> to <var>map</var>.[[MapData]].</li><li>Return <var>value</var>.</li></ol></emu-alg>\n      ","1.3.9":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>map</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>map</var>, [[MapData]]).</li><li>Set <var>key</var> to CanonicalizeKeyedCollectionKey(<var>key</var>).</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>map</var>.[[MapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, return <b>true</b>.</li></ol></li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","1.3.10":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>map</var> be the <b>this</b> value.</li><li>Return ? CreateMapIterator(<var>map</var>, <emu-const>key</emu-const>).</li></ol></emu-alg>\n      ","1.3.11":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>map</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>map</var>, [[MapData]]).</li><li>Set <var>key</var> to CanonicalizeKeyedCollectionKey(<var>key</var>).</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>map</var>.[[MapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, then<ol><li>Set <var>entry</var>.[[Value]] to <var>value</var>.</li><li>Return <var>map</var>.</li></ol></li></ol></li><li>Let <var>entry</var> be the Record { [[Key]]: <var>key</var>, [[Value]]: <var>value</var> }.</li><li>Append <var>entry</var> to <var>map</var>.[[MapData]].</li><li>Return <var>map</var>.</li></ol></emu-alg>\n      ","1.3.12":"<p><code>Map.prototype.size</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>map</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>map</var>, [[MapData]]).</li><li>Let <var>count</var> be 0.</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>map</var>.[[MapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const>, set <var>count</var> to <var>count</var> + 1.</li></ol></li><li>Return 𝔽(<var>count</var>).</li></ol></emu-alg>\n      ","1.3.13":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>map</var> be the <b>this</b> value.</li><li>Return ? CreateMapIterator(<var>map</var>, <emu-const>value</emu-const>).</li></ol></emu-alg>\n      ","1.3.14":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.iterator%</code> property is <code class=\"emu-intrinsic\">%Map.prototype.entries%</code>, defined in <a href=\"/keyed-collections#sec-map.prototype.entries\">24.1.3.4</a>.</p>\n      ","1.3.15":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"Map\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","1.4":"<p>Map instances are ordinary objects that inherit properties from the Map prototype object. Map instances also have a [[MapData]] internal slot.</p>\n    ","1.5":"<p>A <dfn variants=\"Map Iterators,Map Iterator object,Map Iterator objects\">Map Iterator</dfn> is an object that represents a specific iteration over some specific Map instance object. There is not a named constructor for Map Iterator objects. Instead, Map Iterator objects are created by calling certain methods of Map instance objects.</p>\n\n      ","1.5.1":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It is used to create iterator objects for Map methods that return such iterators.</dd>\n        </dl>\n        <emu-alg><ol><li>Perform ? RequireInternalSlot(<var>map</var>, [[MapData]]).</li><li>Let <var>closure</var> be a new Abstract Closure with no parameters that captures <var>map</var> and <var>kind</var> and performs the following steps when called:<ol><li>Let <var>entries</var> be <var>map</var>.[[MapData]].</li><li>Let <var>index</var> be 0.</li><li>Let <var>numEntries</var> be the number of elements in <var>entries</var>.</li><li>Repeat, while <var>index</var> &lt; <var>numEntries</var>,<ol><li>Let <var>entry</var> be <var>entries</var>[<var>index</var>].</li><li>Set <var>index</var> to <var>index</var> + 1.</li><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const>, then<ol><li>If <var>kind</var> is <emu-const>key</emu-const>, then<ol><li>Let <var>result</var> be <var>entry</var>.[[Key]].</li></ol></li><li>Else if <var>kind</var> is <emu-const>value</emu-const>, then<ol><li>Let <var>result</var> be <var>entry</var>.[[Value]].</li></ol></li><li>Else,<ol><li>Assert: <var>kind</var> is <emu-const>key+value</emu-const>.</li><li>Let <var>result</var> be CreateArrayFromList(« <var>entry</var>.[[Key]], <var>entry</var>.[[Value]] »).</li></ol></li><li>Perform ? GeneratorYield(CreateIteratorResultObject(<var>result</var>, <b>false</b>)).</li><li>NOTE: The number of elements in <var>entries</var> may have increased while execution of this abstract operation was paused by GeneratorYield.</li><li>Set <var>numEntries</var> to the number of elements in <var>entries</var>.</li></ol></li></ol></li><li>Return NormalCompletion(<emu-const>unused</emu-const>).</li></ol></li><li>Return CreateIteratorFromClosure(<var>closure</var>, <b>\"<code class=\"emu-intrinsic\">%MapIteratorPrototype%</code>\"</b>, <code class=\"emu-intrinsic\">%MapIteratorPrototype%</code>).</li></ol></emu-alg>\n      ","1.5.2":"<p>The <dfn><code class=\"emu-intrinsic\">%MapIteratorPrototype%</code></dfn> object:</p>\n        <ul>\n          <li>has properties that are inherited by all Map Iterator objects.</li>\n          <li>is an ordinary object.</li>\n          <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Iterator.prototype%</code>.</li>\n          <li>has the following properties:</li>\n        </ul>\n\n        ","1.5.2.1":"<emu-alg><ol><li>Return ? <emu-meta suppress-effects=\"user-code\">GeneratorResume(<b>this</b> value, <emu-const>empty</emu-const>, <b>\"<code class=\"emu-intrinsic\">%MapIteratorPrototype%</code>\"</b>)</emu-meta>.</li></ol></emu-alg>\n        ","1.5.2.2":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"Map Iterator\"</b>.</p>\n          <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n        ","2.1":"","2.1.1":"<p>A <dfn variants=\"Set Records\">Set Record</dfn> is a Record value used to encapsulate the interface of a Set or similar object.</p>\n        <p>Set Records have the fields listed in table-set-record-fields.</p>\n        <emu-table id=\"table-set-record-fields\" caption=\"Set Record Fields\">\n          <table>\n            <tr>\n              <th>\n                Field Name\n              </th>\n              <th>\n                Value\n              </th>\n              <th>\n                Meaning\n              </th>\n            </tr>\n            <tr>\n              <td>\n                [[SetObject]]\n              </td>\n              <td>\n                an Object\n              </td>\n              <td>\n                the Set or similar object.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[Size]]\n              </td>\n              <td>\n                a non-negative integer or +∞\n              </td>\n              <td>\n                The reported size of the object.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[Has]]\n              </td>\n              <td>\n                a function object\n              </td>\n              <td>\n                The <code>has</code> method of the object.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[Keys]]\n              </td>\n              <td>\n                a function object\n              </td>\n              <td>\n                The <code>keys</code> method of the object.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n      ","2.1.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>rawSize</var> be ? Get(<var>obj</var>, <b>\"size\"</b>).</li><li>Let <var>numSize</var> be ? ToNumber(<var>rawSize</var>).</li><li>NOTE: If <var>rawSize</var> is <b>undefined</b>, then <var>numSize</var> will be <b>NaN</b>.</li><li>If <var>numSize</var> is <b>NaN</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>intSize</var> be ! ToIntegerOrInfinity(<var>numSize</var>).</li><li>If <var>intSize</var> &lt; 0, throw a <b>RangeError</b> exception.</li><li>Let <var>has</var> be ? Get(<var>obj</var>, <b>\"has\"</b>).</li><li>If IsCallable(<var>has</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>keys</var> be ? Get(<var>obj</var>, <b>\"keys\"</b>).</li><li>If IsCallable(<var>keys</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return a new Set Record { [[SetObject]]: <var>obj</var>, [[Size]]: <var>intSize</var>, [[Has]]: <var>has</var>, [[Keys]]: <var>keys</var> }.</li></ol></emu-alg>\n      ","2.1.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If SetDataIndex(<var>setData</var>, <var>value</var>) is <emu-const>not-found</emu-const>, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n      ","2.1.4":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Set <var>value</var> to CanonicalizeKeyedCollectionKey(<var>value</var>).</li><li>Let <var>size</var> be the number of elements in <var>setData</var>.</li><li>Let <var>index</var> be 0.</li><li>Repeat, while <var>index</var> &lt; <var>size</var>,<ol><li>Let <var>e</var> be <var>setData</var>[<var>index</var>].</li><li>If <var>e</var> is not <emu-const>empty</emu-const> and <var>e</var> is <var>value</var>, then<ol><li>Return <var>index</var>.</li></ol></li><li>Set <var>index</var> to <var>index</var> + 1.</li></ol></li><li>Return <emu-const>not-found</emu-const>.</li></ol></emu-alg>\n      ","2.1.5":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>count</var> be 0.</li><li>For each element <var>e</var> of <var>setData</var>, do<ol><li>If <var>e</var> is not <emu-const>empty</emu-const>, set <var>count</var> to <var>count</var> + 1.</li></ol></li><li>Return <var>count</var>.</li></ol></emu-alg>\n      ","2.2":"<p>The Set constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Set%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"Set\"</b> property of the global object.</li>\n        <li>creates and initializes a new Set object when called as a constructor.</li>\n        <li>is not intended to be called as a function and will throw an exception when called in that manner.</li>\n        <li>may be used as the value in an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified Set behaviour must include a <code>super</code> call to the Set constructor to create and initialize the subclass instance with the internal state necessary to support the <code>Set.prototype</code> built-in methods.</li>\n      </ul>\n\n      ","2.2.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>set</var> be ? OrdinaryCreateFromConstructor(NewTarget, <b>\"<code class=\"emu-intrinsic\">%Set.prototype%</code>\"</b>, « [[SetData]] »).</li><li>Set <var>set</var>.[[SetData]] to a new empty List.</li><li>If <var>iterable</var> is either <b>undefined</b> or <b>null</b>, return <var>set</var>.</li><li>Let <var>adder</var> be ? Get(<var>set</var>, <b>\"add\"</b>).</li><li>If IsCallable(<var>adder</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>iteratorRecord</var> be ? GetIterator(<var>iterable</var>, <emu-const>sync</emu-const>).</li><li>Repeat,<ol><li>Let <var>next</var> be ? IteratorStepValue(<var>iteratorRecord</var>).</li><li>If <var>next</var> is <emu-const>done</emu-const>, return <var>set</var>.</li><li>Let <var>status</var> be Completion(Call(<var>adder</var>, <var>set</var>, « <var>next</var> »)).</li><li>IfAbruptCloseIterator(<var>status</var>, <var>iteratorRecord</var>).</li></ol></li></ol></emu-alg>\n      ","2.3":"<p>The Set constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","2.3.1":"<p>The initial value of <code>Set.prototype</code> is the Set prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","2.3.2":"<p><code>Set[<code class=\"emu-intrinsic\">%Symbol.species%</code>]</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Return the <b>this</b> value.</li></ol></emu-alg>\n        <p>The value of the <b>\"name\"</b> property of this function is <b>\"get [Symbol.species]\"</b>.</p>\n        <emu-note>\n          <p>Methods that create derived collection objects should call <code class=\"emu-intrinsic\">%Symbol.species%</code> to determine the constructor to use to create the derived objects. Subclass constructor may over-ride <code class=\"emu-intrinsic\">%Symbol.species%</code> to change the default constructor assignment.</p>\n        </emu-note>\n      ","2.4":"<p>The <dfn>Set prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Set.prototype%</code></dfn>.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n        <li>is an ordinary object.</li>\n        <li>does not have a [[SetData]] internal slot.</li>\n      </ul>\n\n      ","2.4.1":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>Set <var>value</var> to CanonicalizeKeyedCollectionKey(<var>value</var>).</li><li>For each element <var>entry</var> of <var>set</var>.[[SetData]], do<ol><li>If <var>entry</var> is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>, <var>value</var>) is <b>true</b>, then<ol><li>Return <var>set</var>.</li></ol></li></ol></li><li>Append <var>value</var> to <var>set</var>.[[SetData]].</li><li>Return <var>set</var>.</li></ol></emu-alg>\n      ","2.4.2":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>For each element <var>entry</var> of <var>set</var>.[[SetData]], do<ol><li>Replace the element of <var>set</var>.[[SetData]] whose value is <var>entry</var> with an element whose value is <emu-const>empty</emu-const>.</li></ol></li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The existing [[SetData]] List is preserved because there may be existing Set Iterator objects that are suspended midway through iterating over that List.</p>\n        </emu-note>\n      ","2.4.3":"<p>The initial value of <code>Set.prototype.constructor</code> is <code class=\"emu-intrinsic\">%Set%</code>.</p>\n      ","2.4.4":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>Set <var>value</var> to CanonicalizeKeyedCollectionKey(<var>value</var>).</li><li>For each element <var>entry</var> of <var>set</var>.[[SetData]], do<ol><li>If <var>entry</var> is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>, <var>value</var>) is <b>true</b>, then<ol><li>Replace the element of <var>set</var>.[[SetData]] whose value is <var>entry</var> with an element whose value is <emu-const>empty</emu-const>.</li><li>Return <b>true</b>.</li></ol></li></ol></li><li>Return <b>false</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The value <emu-const>empty</emu-const> is used as a specification device to indicate that an entry has been deleted. Actual implementations may take other actions such as physically removing the entry from internal data structures.</p>\n        </emu-note>\n      ","2.4.5":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>Let <var>otherRec</var> be ? GetSetRecord(<var>other</var>).</li><li>Let <var>resultSetData</var> be a copy of <var>set</var>.[[SetData]].</li><li>If SetDataSize(<var>set</var>.[[SetData]]) ≤ <var>otherRec</var>.[[Size]], then<ol><li>Let <var>thisSize</var> be the number of elements in <var>set</var>.[[SetData]].</li><li>Let <var>index</var> be 0.</li><li>Repeat, while <var>index</var> &lt; <var>thisSize</var>,<ol><li>Let <var>entry</var> be <var>resultSetData</var>[<var>index</var>].</li><li>If <var>entry</var> is not <emu-const>empty</emu-const>, then<ol><li>Let <var>inOther</var> be ToBoolean(? Call(<var>otherRec</var>.[[Has]], <var>otherRec</var>.[[SetObject]], « <var>entry</var> »)).</li><li>If <var>inOther</var> is <b>true</b>, then<ol><li>Set <var>resultSetData</var>[<var>index</var>] to <emu-const>empty</emu-const>.</li></ol></li></ol></li><li>Set <var>index</var> to <var>index</var> + 1.</li></ol></li></ol></li><li>Else,<ol><li>Let <var>keysIter</var> be ? GetIteratorFromMethod(<var>otherRec</var>.[[SetObject]], <var>otherRec</var>.[[Keys]]).</li><li>Let <var>next</var> be <emu-const>not-started</emu-const>.</li><li>Repeat, while <var>next</var> is not <emu-const>done</emu-const>,<ol><li>Set <var>next</var> to ? IteratorStepValue(<var>keysIter</var>).</li><li>If <var>next</var> is not <emu-const>done</emu-const>, then<ol><li>Set <var>next</var> to CanonicalizeKeyedCollectionKey(<var>next</var>).</li><li>Let <var>valueIndex</var> be SetDataIndex(<var>resultSetData</var>, <var>next</var>).</li><li>If <var>valueIndex</var> is not <emu-const>not-found</emu-const>, then<ol><li>Set <var>resultSetData</var>[<var>valueIndex</var>] to <emu-const>empty</emu-const>.</li></ol></li></ol></li></ol></li></ol></li><li>Let <var>result</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Set.prototype%</code>, « [[SetData]] »).</li><li>Set <var>result</var>.[[SetData]] to <var>resultSetData</var>.</li><li>Return <var>result</var>.</li></ol></emu-alg>\n      ","2.4.6":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Return ? CreateSetIterator(<var>set</var>, <emu-const>key+value</emu-const>).</li></ol></emu-alg>\n        <emu-note>\n          <p>For iteration purposes, a Set appears similar to a Map where each entry has the same value for its key and value.</p>\n        </emu-note>\n      ","2.4.7":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>If IsCallable(<var>callback</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>entries</var> be <var>set</var>.[[SetData]].</li><li>Let <var>numEntries</var> be the number of elements in <var>entries</var>.</li><li>Let <var>index</var> be 0.</li><li>Repeat, while <var>index</var> &lt; <var>numEntries</var>,<ol><li>Let <var>entry</var> be <var>entries</var>[<var>index</var>].</li><li>Set <var>index</var> to <var>index</var> + 1.</li><li>If <var>entry</var> is not <emu-const>empty</emu-const>, then<ol><li>Perform ? Call(<var>callback</var>, <var>thisArg</var>, « <var>entry</var>, <var>entry</var>, <var>set</var> »).</li><li>NOTE: The number of elements in <var>entries</var> may have increased during execution of <var>callback</var>.</li><li>Set <var>numEntries</var> to the number of elements in <var>entries</var>.</li></ol></li></ol></li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p><var>callback</var> should be a function that accepts three arguments. <code>forEach</code> calls <var>callback</var> once for each value present in the Set object, in value insertion order. <var>callback</var> is called only for values of the Set which actually exist; it is not called for keys that have been deleted from the set.</p>\n          <p>If a <var>thisArg</var> parameter is provided, it will be used as the <b>this</b> value for each invocation of <var>callback</var>. If it is not provided, <b>undefined</b> is used instead.</p>\n          <p><var>callback</var> is called with three arguments: the first two arguments are a value contained in the Set. The same value is passed for both arguments. The Set object being traversed is passed as the third argument.</p>\n          <p>The <var>callback</var> is called with three arguments to be consistent with the call back functions used by <code>forEach</code> methods for Map and Array. For Sets, each item value is considered to be both the key and the value.</p>\n          <p><code>forEach</code> does not directly mutate the object on which it is called but the object may be mutated by the calls to <var>callback</var>.</p>\n          <p>Each value is normally visited only once. However, a value will be revisited if it is deleted after it has been visited and then re-added before the <code>forEach</code> call completes. Values that are deleted after the call to <code>forEach</code> begins and before being visited are not visited unless the value is added again before the <code>forEach</code> call completes. New values added after the call to <code>forEach</code> begins are visited.</p>\n        </emu-note>\n      ","2.4.8":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>Set <var>value</var> to CanonicalizeKeyedCollectionKey(<var>value</var>).</li><li>For each element <var>entry</var> of <var>set</var>.[[SetData]], do<ol><li>If <var>entry</var> is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>, <var>value</var>) is <b>true</b>, return <b>true</b>.</li></ol></li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","2.4.9":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>Let <var>otherRec</var> be ? GetSetRecord(<var>other</var>).</li><li>Let <var>resultSetData</var> be a new empty List.</li><li>If SetDataSize(<var>set</var>.[[SetData]]) ≤ <var>otherRec</var>.[[Size]], then<ol><li>Let <var>thisSize</var> be the number of elements in <var>set</var>.[[SetData]].</li><li>Let <var>index</var> be 0.</li><li>Repeat, while <var>index</var> &lt; <var>thisSize</var>,<ol><li>Let <var>entry</var> be <var>set</var>.[[SetData]][<var>index</var>].</li><li>Set <var>index</var> to <var>index</var> + 1.</li><li>If <var>entry</var> is not <emu-const>empty</emu-const>, then<ol><li>Let <var>inOther</var> be ToBoolean(? Call(<var>otherRec</var>.[[Has]], <var>otherRec</var>.[[SetObject]], « <var>entry</var> »)).</li><li>If <var>inOther</var> is <b>true</b>, then<ol><li>NOTE: It is possible for earlier calls to <var>otherRec</var>.[[Has]] to remove and re-add an element of <var>set</var>.[[SetData]], which can cause the same element to be visited twice during this iteration.</li><li>If SetDataHas(<var>resultSetData</var>, <var>entry</var>) is <b>false</b>, then<ol><li>Append <var>entry</var> to <var>resultSetData</var>.</li></ol></li></ol></li><li>NOTE: The number of elements in <var>set</var>.[[SetData]] may have increased during execution of <var>otherRec</var>.[[Has]].</li><li>Set <var>thisSize</var> to the number of elements in <var>set</var>.[[SetData]].</li></ol></li></ol></li></ol></li><li>Else,<ol><li>Let <var>keysIter</var> be ? GetIteratorFromMethod(<var>otherRec</var>.[[SetObject]], <var>otherRec</var>.[[Keys]]).</li><li>Let <var>next</var> be <emu-const>not-started</emu-const>.</li><li>Repeat, while <var>next</var> is not <emu-const>done</emu-const>,<ol><li>Set <var>next</var> to ? IteratorStepValue(<var>keysIter</var>).</li><li>If <var>next</var> is not <emu-const>done</emu-const>, then<ol><li>Set <var>next</var> to CanonicalizeKeyedCollectionKey(<var>next</var>).</li><li>Let <var>inThis</var> be SetDataHas(<var>set</var>.[[SetData]], <var>next</var>).</li><li>If <var>inThis</var> is <b>true</b>, then<ol><li>NOTE: Because <var>other</var> is an arbitrary object, it is possible for its <b>\"keys\"</b> iterator to produce the same value more than once.</li><li>If SetDataHas(<var>resultSetData</var>, <var>next</var>) is <b>false</b>, then<ol><li>Append <var>next</var> to <var>resultSetData</var>.</li></ol></li></ol></li></ol></li></ol></li></ol></li><li>Let <var>result</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Set.prototype%</code>, « [[SetData]] »).</li><li>Set <var>result</var>.[[SetData]] to <var>resultSetData</var>.</li><li>Return <var>result</var>.</li></ol></emu-alg>\n      ","2.4.10":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>Let <var>otherRec</var> be ? GetSetRecord(<var>other</var>).</li><li>If SetDataSize(<var>set</var>.[[SetData]]) ≤ <var>otherRec</var>.[[Size]], then<ol><li>Let <var>thisSize</var> be the number of elements in <var>set</var>.[[SetData]].</li><li>Let <var>index</var> be 0.</li><li>Repeat, while <var>index</var> &lt; <var>thisSize</var>,<ol><li>Let <var>entry</var> be <var>set</var>.[[SetData]][<var>index</var>].</li><li>Set <var>index</var> to <var>index</var> + 1.</li><li>If <var>entry</var> is not <emu-const>empty</emu-const>, then<ol><li>Let <var>inOther</var> be ToBoolean(? Call(<var>otherRec</var>.[[Has]], <var>otherRec</var>.[[SetObject]], « <var>entry</var> »)).</li><li>If <var>inOther</var> is <b>true</b>, return <b>false</b>.</li><li>NOTE: The number of elements in <var>set</var>.[[SetData]] may have increased during execution of <var>otherRec</var>.[[Has]].</li><li>Set <var>thisSize</var> to the number of elements in <var>set</var>.[[SetData]].</li></ol></li></ol></li></ol></li><li>Else,<ol><li>Let <var>keysIter</var> be ? GetIteratorFromMethod(<var>otherRec</var>.[[SetObject]], <var>otherRec</var>.[[Keys]]).</li><li>Let <var>next</var> be <emu-const>not-started</emu-const>.</li><li>Repeat, while <var>next</var> is not <emu-const>done</emu-const>,<ol><li>Set <var>next</var> to ? IteratorStepValue(<var>keysIter</var>).</li><li>If <var>next</var> is not <emu-const>done</emu-const>, then<ol><li>If SetDataHas(<var>set</var>.[[SetData]], <var>next</var>) is <b>true</b>, then<ol><li>Perform ? IteratorClose(<var>keysIter</var>, NormalCompletion(<emu-const>unused</emu-const>)).</li><li>Return <b>false</b>.</li></ol></li></ol></li></ol></li></ol></li><li>Return <b>true</b>.</li></ol></emu-alg>\n      ","2.4.11":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>Let <var>otherRec</var> be ? GetSetRecord(<var>other</var>).</li><li>If SetDataSize(<var>set</var>.[[SetData]]) > <var>otherRec</var>.[[Size]], return <b>false</b>.</li><li>Let <var>thisSize</var> be the number of elements in <var>set</var>.[[SetData]].</li><li>Let <var>index</var> be 0.</li><li>Repeat, while <var>index</var> &lt; <var>thisSize</var>,<ol><li>Let <var>entry</var> be <var>set</var>.[[SetData]][<var>index</var>].</li><li>Set <var>index</var> to <var>index</var> + 1.</li><li>If <var>entry</var> is not <emu-const>empty</emu-const>, then<ol><li>Let <var>inOther</var> be ToBoolean(? Call(<var>otherRec</var>.[[Has]], <var>otherRec</var>.[[SetObject]], « <var>entry</var> »)).</li><li>If <var>inOther</var> is <b>false</b>, return <b>false</b>.</li><li>NOTE: The number of elements in <var>set</var>.[[SetData]] may have increased during execution of <var>otherRec</var>.[[Has]].</li><li>Set <var>thisSize</var> to the number of elements in <var>set</var>.[[SetData]].</li></ol></li></ol></li><li>Return <b>true</b>.</li></ol></emu-alg>\n      ","2.4.12":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>Let <var>otherRec</var> be ? GetSetRecord(<var>other</var>).</li><li>If SetDataSize(<var>set</var>.[[SetData]]) &lt; <var>otherRec</var>.[[Size]], return <b>false</b>.</li><li>Let <var>keysIter</var> be ? GetIteratorFromMethod(<var>otherRec</var>.[[SetObject]], <var>otherRec</var>.[[Keys]]).</li><li>Let <var>next</var> be <emu-const>not-started</emu-const>.</li><li>Repeat, while <var>next</var> is not <emu-const>done</emu-const>,<ol><li>Set <var>next</var> to ? IteratorStepValue(<var>keysIter</var>).</li><li>If <var>next</var> is not <emu-const>done</emu-const>, then<ol><li>If SetDataHas(<var>set</var>.[[SetData]], <var>next</var>) is <b>false</b>, then<ol><li>Perform ? IteratorClose(<var>keysIter</var>, NormalCompletion(<emu-const>unused</emu-const>)).</li><li>Return <b>false</b>.</li></ol></li></ol></li></ol></li><li>Return <b>true</b>.</li></ol></emu-alg>\n      ","2.4.13":"<p>The initial value of the <b>\"keys\"</b> property is <code class=\"emu-intrinsic\">%Set.prototype.values%</code>, defined in <a href=\"/keyed-collections#sec-set.prototype.values\">24.2.4.17</a>.</p>\n        <emu-note>\n          <p>For iteration purposes, a Set appears similar to a Map where each entry has the same value for its key and value.</p>\n        </emu-note>\n      ","2.4.14":"<p><code>Set.prototype.size</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>Let <var>size</var> be SetDataSize(<var>set</var>.[[SetData]]).</li><li>Return 𝔽(<var>size</var>).</li></ol></emu-alg>\n      ","2.4.15":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>Let <var>otherRec</var> be ? GetSetRecord(<var>other</var>).</li><li>Let <var>keysIter</var> be ? GetIteratorFromMethod(<var>otherRec</var>.[[SetObject]], <var>otherRec</var>.[[Keys]]).</li><li>Let <var>resultSetData</var> be a copy of <var>set</var>.[[SetData]].</li><li>Let <var>next</var> be <emu-const>not-started</emu-const>.</li><li>Repeat, while <var>next</var> is not <emu-const>done</emu-const>,<ol><li>Set <var>next</var> to ? IteratorStepValue(<var>keysIter</var>).</li><li>If <var>next</var> is not <emu-const>done</emu-const>, then<ol><li>Set <var>next</var> to CanonicalizeKeyedCollectionKey(<var>next</var>).</li><li>Let <var>resultIndex</var> be SetDataIndex(<var>resultSetData</var>, <var>next</var>).</li><li>If <var>resultIndex</var> is <emu-const>not-found</emu-const>, let <var>alreadyInResult</var> be <b>false</b>; else let <var>alreadyInResult</var> be <b>true</b>.</li><li>If SetDataHas(<var>set</var>.[[SetData]], <var>next</var>) is <b>true</b>, then<ol><li>If <var>alreadyInResult</var> is <b>true</b>, set <var>resultSetData</var>[<var>resultIndex</var>] to <emu-const>empty</emu-const>.</li></ol></li><li>Else,<ol><li>If <var>alreadyInResult</var> is <b>false</b>, append <var>next</var> to <var>resultSetData</var>.</li></ol></li></ol></li></ol></li><li>Let <var>result</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Set.prototype%</code>, « [[SetData]] »).</li><li>Set <var>result</var>.[[SetData]] to <var>resultSetData</var>.</li><li>Return <var>result</var>.</li></ol></emu-alg>\n      ","2.4.16":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>Let <var>otherRec</var> be ? GetSetRecord(<var>other</var>).</li><li>Let <var>keysIter</var> be ? GetIteratorFromMethod(<var>otherRec</var>.[[SetObject]], <var>otherRec</var>.[[Keys]]).</li><li>Let <var>resultSetData</var> be a copy of <var>set</var>.[[SetData]].</li><li>Let <var>next</var> be <emu-const>not-started</emu-const>.</li><li>Repeat, while <var>next</var> is not <emu-const>done</emu-const>,<ol><li>Set <var>next</var> to ? IteratorStepValue(<var>keysIter</var>).</li><li>If <var>next</var> is not <emu-const>done</emu-const>, then<ol><li>Set <var>next</var> to CanonicalizeKeyedCollectionKey(<var>next</var>).</li><li>If SetDataHas(<var>resultSetData</var>, <var>next</var>) is <b>false</b>, then<ol><li>Append <var>next</var> to <var>resultSetData</var>.</li></ol></li></ol></li></ol></li><li>Let <var>result</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Set.prototype%</code>, « [[SetData]] »).</li><li>Set <var>result</var>.[[SetData]] to <var>resultSetData</var>.</li><li>Return <var>result</var>.</li></ol></emu-alg>\n      ","2.4.17":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>set</var> be the <b>this</b> value.</li><li>Return ? CreateSetIterator(<var>set</var>, <emu-const>value</emu-const>).</li></ol></emu-alg>\n      ","2.4.18":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.iterator%</code> property is <code class=\"emu-intrinsic\">%Set.prototype.values%</code>, defined in <a href=\"/keyed-collections#sec-set.prototype.values\">24.2.4.17</a>.</p>\n      ","2.4.19":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"Set\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","2.5":"<p>Set instances are ordinary objects that inherit properties from the Set prototype object. Set instances also have a [[SetData]] internal slot.</p>\n    ","2.6":"<p>A <dfn variants=\"Set Iterators,Set Iterator object,Set Iterator objects\">Set Iterator</dfn> is an ordinary object, with the structure defined below, that represents a specific iteration over some specific Set instance object. There is not a named constructor for Set Iterator objects. Instead, Set Iterator objects are created by calling certain methods of Set instance objects.</p>\n\n      ","2.6.1":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It is used to create iterator objects for Set methods that return such iterators.</dd>\n        </dl>\n        <emu-alg><ol><li>Perform ? RequireInternalSlot(<var>set</var>, [[SetData]]).</li><li>Let <var>closure</var> be a new Abstract Closure with no parameters that captures <var>set</var> and <var>kind</var> and performs the following steps when called:<ol><li>Let <var>index</var> be 0.</li><li>Let <var>entries</var> be <var>set</var>.[[SetData]].</li><li>Let <var>numEntries</var> be the number of elements in <var>entries</var>.</li><li>Repeat, while <var>index</var> &lt; <var>numEntries</var>,<ol><li>Let <var>entry</var> be <var>entries</var>[<var>index</var>].</li><li>Set <var>index</var> to <var>index</var> + 1.</li><li>If <var>entry</var> is not <emu-const>empty</emu-const>, then<ol><li>If <var>kind</var> is <emu-const>key+value</emu-const>, then<ol><li>Let <var>result</var> be CreateArrayFromList(« <var>entry</var>, <var>entry</var> »).</li><li>Perform ? GeneratorYield(CreateIteratorResultObject(<var>result</var>, <b>false</b>)).</li></ol></li><li>Else,<ol><li>Assert: <var>kind</var> is <emu-const>value</emu-const>.</li><li>Perform ? GeneratorYield(CreateIteratorResultObject(<var>entry</var>, <b>false</b>)).</li></ol></li><li>NOTE: The number of elements in <var>entries</var> may have increased while execution of this abstract operation was paused by GeneratorYield.</li><li>Set <var>numEntries</var> to the number of elements in <var>entries</var>.</li></ol></li></ol></li><li>Return NormalCompletion(<emu-const>unused</emu-const>).</li></ol></li><li>Return CreateIteratorFromClosure(<var>closure</var>, <b>\"<code class=\"emu-intrinsic\">%SetIteratorPrototype%</code>\"</b>, <code class=\"emu-intrinsic\">%SetIteratorPrototype%</code>).</li></ol></emu-alg>\n      ","2.6.2":"<p>The <dfn><code class=\"emu-intrinsic\">%SetIteratorPrototype%</code></dfn> object:</p>\n        <ul>\n          <li>has properties that are inherited by all Set Iterator objects.</li>\n          <li>is an ordinary object.</li>\n          <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Iterator.prototype%</code>.</li>\n          <li>has the following properties:</li>\n        </ul>\n\n        ","2.6.2.1":"<emu-alg><ol><li>Return ? <emu-meta suppress-effects=\"user-code\">GeneratorResume(<b>this</b> value, <emu-const>empty</emu-const>, <b>\"<code class=\"emu-intrinsic\">%SetIteratorPrototype%</code>\"</b>)</emu-meta>.</li></ol></emu-alg>\n        ","2.6.2.2":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"Set Iterator\"</b>.</p>\n          <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n        ","3.1":"<p>The WeakMap constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%WeakMap%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"WeakMap\"</b> property of the global object.</li>\n        <li>creates and initializes a new WeakMap when called as a constructor.</li>\n        <li>is not intended to be called as a function and will throw an exception when called in that manner.</li>\n        <li>may be used as the value in an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified WeakMap behaviour must include a <code>super</code> call to the WeakMap constructor to create and initialize the subclass instance with the internal state necessary to support the <code>WeakMap.prototype</code> built-in methods.</li>\n      </ul>\n\n      ","3.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>map</var> be ? OrdinaryCreateFromConstructor(NewTarget, <b>\"<code class=\"emu-intrinsic\">%WeakMap.prototype%</code>\"</b>, « [[WeakMapData]] »).</li><li>Set <var>map</var>.[[WeakMapData]] to a new empty List.</li><li>If <var>iterable</var> is either <b>undefined</b> or <b>null</b>, return <var>map</var>.</li><li>Let <var>adder</var> be ? Get(<var>map</var>, <b>\"set\"</b>).</li><li>If IsCallable(<var>adder</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return ? AddEntriesFromIterable(<var>map</var>, <var>iterable</var>, <var>adder</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>If the parameter <var>iterable</var> is present, it is expected to be an object that implements a <code class=\"emu-intrinsic\">%Symbol.iterator%</code> method that returns an iterator object that produces a two element array-like object whose first element is a value that will be used as a WeakMap key and whose second element is the value to associate with that key.</p>\n        </emu-note>\n      ","3.2":"<p>The WeakMap constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","3.2.1":"<p>The initial value of <code>WeakMap.prototype</code> is the WeakMap prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","3.3":"<p>The <dfn>WeakMap prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%WeakMap.prototype%</code></dfn>.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n        <li>is an ordinary object.</li>\n        <li>does not have a [[WeakMapData]] internal slot.</li>\n      </ul>\n\n      ","3.3.1":"<p>The initial value of <code>WeakMap.prototype.constructor</code> is <code class=\"emu-intrinsic\">%WeakMap%</code>.</p>\n      ","3.3.2":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>weakMap</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>weakMap</var>, [[WeakMapData]]).</li><li>If CanBeHeldWeakly(<var>key</var>) is <b>false</b>, return <b>false</b>.</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>weakMap</var>.[[WeakMapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, then<ol><li>Set <var>entry</var>.[[Key]] to <emu-const>empty</emu-const>.</li><li>Set <var>entry</var>.[[Value]] to <emu-const>empty</emu-const>.</li><li>Return <b>true</b>.</li></ol></li></ol></li><li>Return <b>false</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The value <emu-const>empty</emu-const> is used as a specification device to indicate that an entry has been deleted. Actual implementations may take other actions such as physically removing the entry from internal data structures.</p>\n        </emu-note>\n      ","3.3.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>weakMap</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>weakMap</var>, [[WeakMapData]]).</li><li>If CanBeHeldWeakly(<var>key</var>) is <b>false</b>, return <b>undefined</b>.</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>weakMap</var>.[[WeakMapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, return <var>entry</var>.[[Value]].</li></ol></li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n      ","3.3.4":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>weakMap</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>weakMap</var>, [[WeakMapData]]).</li><li>If CanBeHeldWeakly(<var>key</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>weakMap</var>.[[WeakMapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, return <var>entry</var>.[[Value]].</li></ol></li><li>Let <var>entry</var> be the Record { [[Key]]: <var>key</var>, [[Value]]: <var>value</var> }.</li><li>Append <var>entry</var> to <var>weakMap</var>.[[WeakMapData]].</li><li>Return <var>value</var>.</li></ol></emu-alg>\n      ","3.3.5":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>weakMap</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>weakMap</var>, [[WeakMapData]]).</li><li>If CanBeHeldWeakly(<var>key</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>If IsCallable(<var>callback</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>weakMap</var>.[[WeakMapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, return <var>entry</var>.[[Value]].</li></ol></li><li>Let <var>value</var> be ? Call(<var>callback</var>, <b>undefined</b>, « <var>key</var> »).</li><li>NOTE: The WeakMap may have been modified during execution of <var>callback</var>.</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>weakMap</var>.[[WeakMapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, then<ol><li>Set <var>entry</var>.[[Value]] to <var>value</var>.</li><li>Return <var>value</var>.</li></ol></li></ol></li><li>Let <var>entry</var> be the Record { [[Key]]: <var>key</var>, [[Value]]: <var>value</var> }.</li><li>Append <var>entry</var> to <var>weakMap</var>.[[WeakMapData]].</li><li>Return <var>value</var>.</li></ol></emu-alg>\n      ","3.3.6":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>weakMap</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>weakMap</var>, [[WeakMapData]]).</li><li>If CanBeHeldWeakly(<var>key</var>) is <b>false</b>, return <b>false</b>.</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>weakMap</var>.[[WeakMapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, return <b>true</b>.</li></ol></li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","3.3.7":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>weakMap</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>weakMap</var>, [[WeakMapData]]).</li><li>If CanBeHeldWeakly(<var>key</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>For each Record { [[Key]], [[Value]] } <var>entry</var> of <var>weakMap</var>.[[WeakMapData]], do<ol><li>If <var>entry</var>.[[Key]] is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>.[[Key]], <var>key</var>) is <b>true</b>, then<ol><li>Set <var>entry</var>.[[Value]] to <var>value</var>.</li><li>Return <var>weakMap</var>.</li></ol></li></ol></li><li>Let <var>entry</var> be the Record { [[Key]]: <var>key</var>, [[Value]]: <var>value</var> }.</li><li>Append <var>entry</var> to <var>weakMap</var>.[[WeakMapData]].</li><li>Return <var>weakMap</var>.</li></ol></emu-alg>\n      ","3.3.8":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"WeakMap\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","3.4":"<p>WeakMap instances are ordinary objects that inherit properties from the WeakMap prototype object. WeakMap instances also have a [[WeakMapData]] internal slot.</p>\n    ","4.1":"<p>The WeakSet constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%WeakSet%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"WeakSet\"</b> property of the global object.</li>\n        <li>creates and initializes a new WeakSet when called as a constructor.</li>\n        <li>is not intended to be called as a function and will throw an exception when called in that manner.</li>\n        <li>may be used as the value in an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified WeakSet behaviour must include a <code>super</code> call to the WeakSet constructor to create and initialize the subclass instance with the internal state necessary to support the <code>WeakSet.prototype</code> built-in methods.</li>\n      </ul>\n\n      ","4.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>set</var> be ? OrdinaryCreateFromConstructor(NewTarget, <b>\"<code class=\"emu-intrinsic\">%WeakSet.prototype%</code>\"</b>, « [[WeakSetData]] »).</li><li>Set <var>set</var>.[[WeakSetData]] to a new empty List.</li><li>If <var>iterable</var> is either <b>undefined</b> or <b>null</b>, return <var>set</var>.</li><li>Let <var>adder</var> be ? Get(<var>set</var>, <b>\"add\"</b>).</li><li>If IsCallable(<var>adder</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>iteratorRecord</var> be ? GetIterator(<var>iterable</var>, <emu-const>sync</emu-const>).</li><li>Repeat,<ol><li>Let <var>next</var> be ? IteratorStepValue(<var>iteratorRecord</var>).</li><li>If <var>next</var> is <emu-const>done</emu-const>, return <var>set</var>.</li><li>Let <var>status</var> be Completion(Call(<var>adder</var>, <var>set</var>, « <var>next</var> »)).</li><li>IfAbruptCloseIterator(<var>status</var>, <var>iteratorRecord</var>).</li></ol></li></ol></emu-alg>\n      ","4.2":"<p>The WeakSet constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","4.2.1":"<p>The initial value of <code>WeakSet.prototype</code> is the WeakSet prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.3":"<p>The <dfn>WeakSet prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%WeakSet.prototype%</code></dfn>.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n        <li>is an ordinary object.</li>\n        <li>does not have a [[WeakSetData]] internal slot.</li>\n      </ul>\n\n      ","4.3.1":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>weakSet</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>weakSet</var>, [[WeakSetData]]).</li><li>If CanBeHeldWeakly(<var>value</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>For each element <var>entry</var> of <var>weakSet</var>.[[WeakSetData]], do<ol><li>If <var>entry</var> is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>, <var>value</var>) is <b>true</b>, then<ol><li>Return <var>weakSet</var>.</li></ol></li></ol></li><li>Append <var>value</var> to <var>weakSet</var>.[[WeakSetData]].</li><li>Return <var>weakSet</var>.</li></ol></emu-alg>\n      ","4.3.2":"<p>The initial value of <code>WeakSet.prototype.constructor</code> is <code class=\"emu-intrinsic\">%WeakSet%</code>.</p>\n      ","4.3.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>weakSet</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>weakSet</var>, [[WeakSetData]]).</li><li>If CanBeHeldWeakly(<var>value</var>) is <b>false</b>, return <b>false</b>.</li><li>For each element <var>entry</var> of <var>weakSet</var>.[[WeakSetData]], do<ol><li>If <var>entry</var> is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>, <var>value</var>) is <b>true</b>, then<ol><li>Replace the element of <var>weakSet</var>.[[WeakSetData]] whose value is <var>entry</var> with an element whose value is <emu-const>empty</emu-const>.</li><li>Return <b>true</b>.</li></ol></li></ol></li><li>Return <b>false</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The value <emu-const>empty</emu-const> is used as a specification device to indicate that an entry has been deleted. Actual implementations may take other actions such as physically removing the entry from internal data structures.</p>\n        </emu-note>\n      ","4.3.4":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>weakSet</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>weakSet</var>, [[WeakSetData]]).</li><li>If CanBeHeldWeakly(<var>value</var>) is <b>false</b>, return <b>false</b>.</li><li>For each element <var>entry</var> of <var>weakSet</var>.[[WeakSetData]], do<ol><li>If <var>entry</var> is not <emu-const>empty</emu-const> and SameValue(<var>entry</var>, <var>value</var>) is <b>true</b>, return <b>true</b>.</li></ol></li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","4.3.5":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"WeakSet\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","4.4":"<p>WeakSet instances are ordinary objects that inherit properties from the WeakSet prototype object. WeakSet instances also have a [[WeakSetData]] internal slot.</p>\n    ","5.1":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>If <var>key</var> is <b>-0</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>Return <var>key</var>.</li></ol></emu-alg>\n    "};
const _basePath = process.env.NEXT_PUBLIC_BASE_PATH || '';
const sections = _basePath
  ? Object.fromEntries(
      Object.entries(_sections).map(([k, v]) => [
        k,
        v.replaceAll('href="/', `href="${_basePath}/`),
      ])
    )
  : _sections;
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
