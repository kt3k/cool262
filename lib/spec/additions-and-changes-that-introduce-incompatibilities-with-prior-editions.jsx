// Generated from ecma262/spec.html — do not edit by hand.
const _sections = {"":"<p><a href=\"/ecmascript-data-types-and-values#sec-reference-record-specification-type\">6.2.5</a>: In ECMAScript 2015, Function calls are not allowed to return a Reference Record.</p>\n  <p><a href=\"/abstract-operations#sec-tonumber-applied-to-the-string-type\">7.1.4.1</a>: In ECMAScript 2015, ToNumber applied to a String value now recognizes and converts <emu-nt>BinaryIntegerLiteral</emu-nt> and <emu-nt>OctalIntegerLiteral</emu-nt> numeric strings. In previous editions such strings were converted to <b>NaN</b>.</p>\n  <p><a href=\"/executable-code-and-execution-contexts#sec-code-realms\">9.3</a>: In ECMAScript 2018, Template objects are canonicalized based on Parse Node (source location), instead of across all occurrences of that template literal or tagged template in a Realm in previous editions.</p>\n  <p><a href=\"/ecmascript-language-lexical-grammar#sec-white-space\">12.2</a>: In ECMAScript 2016, Unicode 8.0.0 or higher is mandated, as opposed to ECMAScript 2015 which mandated Unicode 5.1. In particular, this caused U+180E MONGOLIAN VOWEL SEPARATOR, which was in the <code>Space_Separator</code> (<code>Zs</code>) category and thus treated as whitespace in ECMAScript 2015, to be moved to the <code>Format</code> (<code>Cf</code>) category (as of Unicode 6.3.0). This causes whitespace-sensitive methods to behave differently. For example, <code>\"\\u180E\".trim().length</code> was <code>0</code> in previous editions, but <code>1</code> in ECMAScript 2016 and later. Additionally, ECMAScript 2017 mandated always using the latest version of the Unicode Standard.</p>\n  <p><a href=\"/ecmascript-language-lexical-grammar#sec-names-and-keywords\">12.7</a>: In ECMAScript 2015, the valid code points for an <emu-nt>IdentifierName</emu-nt> are specified in terms of the Unicode properties “ID_Start” and “ID_Continue”. In previous editions, the valid <emu-nt>IdentifierName</emu-nt> or <emu-nt>Identifier</emu-nt> code points were specified by enumerating various Unicode code point categories.</p>\n  <p><a href=\"/ecmascript-language-lexical-grammar#sec-rules-of-automatic-semicolon-insertion\">12.10.1</a>: In ECMAScript 2015, Automatic Semicolon Insertion adds a semicolon at the end of a do-while statement if the semicolon is missing. This change aligns the specification with the actual behaviour of most existing implementations.</p>\n  <p><a href=\"/ecmascript-language-expressions#sec-object-initializer-static-semantics-early-errors\">13.2.5.1</a>: In ECMAScript 2015, it is no longer an early error to have duplicate property names in Object Initializers.</p>\n  <p><a href=\"/ecmascript-language-expressions#sec-assignment-operators-static-semantics-early-errors\">13.15.1</a>: In ECMAScript 2015, strict mode code containing an assignment to an immutable binding such as the function name of a <emu-nt>FunctionExpression</emu-nt> does not produce an early error. Instead it produces a runtime error.</p>\n  <p><a href=\"/ecmascript-language-statements-and-declarations#sec-block\">14.2</a>: In ECMAScript 2015, a <emu-nt>StatementList</emu-nt> beginning with the token let followed by the input elements <emu-nt>LineTerminator</emu-nt> then <emu-nt>Identifier</emu-nt> is the start of a <emu-nt>LexicalDeclaration</emu-nt>. In previous editions, automatic semicolon insertion would always insert a semicolon before the <emu-nt>Identifier</emu-nt> input element.</p>\n  <p><a href=\"/ecmascript-language-statements-and-declarations#sec-expression-statement\">14.5</a>: In ECMAScript 2015, a <emu-nt>StatementListItem</emu-nt> beginning with the token <code>let</code> followed by the token <code>[</code> is the start of a <emu-nt>LexicalDeclaration</emu-nt>. In previous editions such a sequence would be the start of an <emu-nt>ExpressionStatement</emu-nt>.</p>\n  <p><a href=\"/ecmascript-language-statements-and-declarations#sec-if-statement-runtime-semantics-evaluation\">14.6.2</a>: In ECMAScript 2015, the normal result of an <emu-nt>IfStatement</emu-nt> is never the value <emu-const>empty</emu-const>. If no <emu-nt>Statement</emu-nt> part is evaluated or if the evaluated <emu-nt>Statement</emu-nt> part produces a normal completion containing <emu-const>empty</emu-const>, the result of the <emu-nt>IfStatement</emu-nt> is <b>undefined</b>.</p>\n  <p><a href=\"/ecmascript-language-statements-and-declarations#sec-iteration-statements\">14.7</a>: In ECMAScript 2015, if the <code>(</code> token of a for statement is immediately followed by the token sequence <code>let [</code> then the <code>let</code> is treated as the start of a <emu-nt>LexicalDeclaration</emu-nt>. In previous editions such a token sequence would be the start of an <emu-nt>Expression</emu-nt>.</p>\n  <p><a href=\"/ecmascript-language-statements-and-declarations#sec-iteration-statements\">14.7</a>: In ECMAScript 2015, if the ( token of a for-in statement is immediately followed by the token sequence <code>let [</code> then the <code>let</code> is treated as the start of a <emu-nt>ForDeclaration</emu-nt>. In previous editions such a token sequence would be the start of an <emu-nt>LeftHandSideExpression</emu-nt>.</p>\n  <p><a href=\"/ecmascript-language-statements-and-declarations#sec-iteration-statements\">14.7</a>: Prior to ECMAScript 2015, an initialization expression could appear as part of the <emu-nt>VariableDeclaration</emu-nt> that precedes the <code>in</code> keyword. In ECMAScript 2015, the <emu-nt>ForBinding</emu-nt> in that same position does not allow the occurrence of such an initializer. In ECMAScript 2017, such an initializer is permitted only in non-strict code.</p>\n  <p><a href=\"/ecmascript-language-statements-and-declarations#sec-iteration-statements\">14.7</a>: In ECMAScript 2015, the result of evaluating an <emu-nt>IterationStatement</emu-nt> is never a normal completion whose [[Value]] is <emu-const>empty</emu-const>. If the <emu-nt>Statement</emu-nt> part of an <emu-nt>IterationStatement</emu-nt> is not evaluated or if the final evaluation of the <emu-nt>Statement</emu-nt> part produces a normal completion whose [[Value]] is <emu-const>empty</emu-const>, the result of evaluating the <emu-nt>IterationStatement</emu-nt> is a normal completion whose [[Value]] is <b>undefined</b>.</p>\n  <p><a href=\"/ecmascript-language-statements-and-declarations#sec-with-statement-runtime-semantics-evaluation\">14.11.2</a>: In ECMAScript 2015, the result of evaluating a <emu-nt>WithStatement</emu-nt> is never a normal completion whose [[Value]] is <emu-const>empty</emu-const>. If evaluation of the <emu-nt>Statement</emu-nt> part of a <emu-nt>WithStatement</emu-nt> produces a normal completion whose [[Value]] is <emu-const>empty</emu-const>, the result of evaluating the <emu-nt>WithStatement</emu-nt> is a normal completion whose [[Value]] is <b>undefined</b>.</p>\n  <p><a href=\"/ecmascript-language-statements-and-declarations#sec-switch-statement-runtime-semantics-evaluation\">14.12.4</a>: In ECMAScript 2015, the result of evaluating a <emu-nt>SwitchStatement</emu-nt> is never a normal completion whose [[Value]] is <emu-const>empty</emu-const>. If evaluation of the <emu-nt>CaseBlock</emu-nt> part of a <emu-nt>SwitchStatement</emu-nt> produces a normal completion whose [[Value]] is <emu-const>empty</emu-const>, the result of evaluating the <emu-nt>SwitchStatement</emu-nt> is a normal completion whose [[Value]] is <b>undefined</b>.</p>\n  <p><a href=\"/ecmascript-language-statements-and-declarations#sec-try-statement\">14.15</a>: In ECMAScript 2015, it is an early error for a <emu-nt>Catch</emu-nt> clause to contain a <code>var</code> declaration for the same <emu-nt>Identifier</emu-nt> that appears as the <emu-nt>Catch</emu-nt> clause parameter. In previous editions, such a variable declaration would be instantiated in the enclosing variable environment but the declaration's <emu-nt>Initializer</emu-nt> value would be assigned to the <emu-nt>Catch</emu-nt> parameter.</p>\n  <p><a href=\"/ecmascript-language-statements-and-declarations#sec-try-statement\">14.15</a>, <a href=\"/global-object#sec-evaldeclarationinstantiation\">19.2.1.3</a>: In ECMAScript 2015, a runtime <b>SyntaxError</b> is thrown if a <emu-nt>Catch</emu-nt> clause evaluates a non-strict direct <code>eval</code> whose eval code includes a <code>var</code> or <code>FunctionDeclaration</code> declaration that binds the same <emu-nt>Identifier</emu-nt> that appears as the <emu-nt>Catch</emu-nt> clause parameter.</p>\n  <p><a href=\"/ecmascript-language-statements-and-declarations#sec-try-statement-runtime-semantics-evaluation\">14.15.3</a>: In ECMAScript 2015, the result of a <emu-nt>TryStatement</emu-nt> is never the value <emu-const>empty</emu-const>. If the <emu-nt>Block</emu-nt> part of a <emu-nt>TryStatement</emu-nt> evaluates to a normal completion containing <emu-const>empty</emu-const>, the result of the <emu-nt>TryStatement</emu-nt> is <b>undefined</b>. If the <emu-nt>Block</emu-nt> part of a <emu-nt>TryStatement</emu-nt> evaluates to a throw completion and it has a <emu-nt>Catch</emu-nt> part that evaluates to a normal completion containing <emu-const>empty</emu-const>, the result of the <emu-nt>TryStatement</emu-nt> is <b>undefined</b> if there is no <emu-nt>Finally</emu-nt> clause or if its <emu-nt>Finally</emu-nt> clause evaluates to an <emu-const>empty</emu-const> normal completion.</p>\n  <p><a href=\"/ecmascript-language-functions-and-classes#sec-runtime-semantics-methoddefinitionevaluation\">15.4.5</a> In ECMAScript 2015, the function objects that are created as the values of the [[Get]] or [[Set]] attribute of accessor properties in an <emu-nt>ObjectLiteral</emu-nt> are not constructor functions and they do not have a <b>\"prototype\"</b> own property. In the previous edition, they were constructors and had a <b>\"prototype\"</b> property.</p>\n  <p><a href=\"/fundamental-objects#sec-object.freeze\">20.1.2.6</a>: In ECMAScript 2015, if the argument to <code>Object.freeze</code> is not an object it is treated as if it was a non-extensible ordinary object with no own properties. In the previous edition, a non-object argument always causes a <b>TypeError</b> to be thrown.</p>\n  <p><a href=\"/fundamental-objects#sec-object.getownpropertydescriptor\">20.1.2.8</a>: In ECMAScript 2015, if the argument to <code>Object.getOwnPropertyDescriptor</code> is not an object an attempt is made to coerce the argument using ToObject. If the coercion is successful the result is used in place of the original argument value. In the previous edition, a non-object argument always causes a <b>TypeError</b> to be thrown.</p>\n  <p><a href=\"/fundamental-objects#sec-object.getownpropertynames\">20.1.2.10</a>: In ECMAScript 2015, if the argument to <code>Object.getOwnPropertyNames</code> is not an object an attempt is made to coerce the argument using ToObject. If the coercion is successful the result is used in place of the original argument value. In the previous edition, a non-object argument always causes a <b>TypeError</b> to be thrown.</p>\n  <p><a href=\"/fundamental-objects#sec-object.getprototypeof\">20.1.2.12</a>: In ECMAScript 2015, if the argument to <code>Object.getPrototypeOf</code> is not an object an attempt is made to coerce the argument using ToObject. If the coercion is successful the result is used in place of the original argument value. In the previous edition, a non-object argument always causes a <b>TypeError</b> to be thrown.</p>\n  <p><a href=\"/fundamental-objects#sec-object.isextensible\">20.1.2.16</a>: In ECMAScript 2015, if the argument to <code>Object.isExtensible</code> is not an object it is treated as if it was a non-extensible ordinary object with no own properties. In the previous edition, a non-object argument always causes a <b>TypeError</b> to be thrown.</p>\n  <p><a href=\"/fundamental-objects#sec-object.isfrozen\">20.1.2.17</a>: In ECMAScript 2015, if the argument to <code>Object.isFrozen</code> is not an object it is treated as if it was a non-extensible ordinary object with no own properties. In the previous edition, a non-object argument always causes a <b>TypeError</b> to be thrown.</p>\n  <p><a href=\"/fundamental-objects#sec-object.issealed\">20.1.2.18</a>: In ECMAScript 2015, if the argument to <code>Object.isSealed</code> is not an object it is treated as if it was a non-extensible ordinary object with no own properties. In the previous edition, a non-object argument always causes a <b>TypeError</b> to be thrown.</p>\n  <p><a href=\"/fundamental-objects#sec-object.keys\">20.1.2.19</a>: In ECMAScript 2015, if the argument to <code>Object.keys</code> is not an object an attempt is made to coerce the argument using ToObject. If the coercion is successful the result is used in place of the original argument value. In the previous edition, a non-object argument always causes a <b>TypeError</b> to be thrown.</p>\n  <p><a href=\"/fundamental-objects#sec-object.preventextensions\">20.1.2.20</a>: In ECMAScript 2015, if the argument to <code>Object.preventExtensions</code> is not an object it is treated as if it was a non-extensible ordinary object with no own properties. In the previous edition, a non-object argument always causes a <b>TypeError</b> to be thrown.</p>\n  <p><a href=\"/fundamental-objects#sec-object.seal\">20.1.2.22</a>: In ECMAScript 2015, if the argument to <code>Object.seal</code> is not an object it is treated as if it was a non-extensible ordinary object with no own properties. In the previous edition, a non-object argument always causes a <b>TypeError</b> to be thrown.</p>\n  <p><a href=\"/fundamental-objects#sec-function.prototype.bind\">20.2.3.2</a>: In ECMAScript 2015, the [[Prototype]] internal slot of a bound function is set to the [[GetPrototypeOf]] value of its target function. In the previous edition, [[Prototype]] was always set to <emu-intrinsic>%Function.prototype%</emu-intrinsic>.</p>\n  <p><a href=\"/fundamental-objects#sec-function-instances-length\">20.2.4.1</a>: In ECMAScript 2015, the <b>\"length\"</b> property of function instances is configurable. In previous editions it was non-configurable.</p>\n  <p><a href=\"/fundamental-objects#sec-properties-of-the-nativeerror-constructors\">20.5.6.2</a>: In ECMAScript 2015, the [[Prototype]] internal slot of a <var>NativeError</var> constructor is the Error constructor. In previous editions it was the Function prototype object.</p>\n  <p><a href=\"/numbers-and-dates#sec-properties-of-the-date-prototype-object\">21.4.4</a> In ECMAScript 2015, the Date prototype object is not a Date instance. In previous editions it was a Date instance whose TimeValue was <b>NaN</b>.</p>\n  <p><a href=\"/text-processing#sec-string.prototype.localecompare\">22.1.3.12</a> In ECMAScript 2015, the <code>String.prototype.localeCompare</code> function must treat Strings that are canonically equivalent according to the Unicode Standard as being identical. In previous editions implementations were permitted to ignore canonical equivalence and could instead use a bit-wise comparison.</p>\n  <p><a href=\"/text-processing#sec-string.prototype.tolowercase\">22.1.3.28</a> and <a href=\"/text-processing#sec-string.prototype.touppercase\">22.1.3.30</a> In ECMAScript 2015, lowercase/upper conversion processing operates on code points. In previous editions such the conversion processing was only applied to individual code units. The only affected code points are those in the Deseret block of Unicode.</p>\n  <p><a href=\"/text-processing#sec-string.prototype.trim\">22.1.3.32</a> In ECMAScript 2015, the <code>String.prototype.trim</code> method is defined to recognize white space code points that may exist outside of the Unicode BMP. However, as of Unicode 7 no such code points are defined. In previous editions such code points would not have been recognized as white space.</p>\n  <p><a href=\"/text-processing#sec-regexp-pattern-flags\">22.2.4.1</a> In ECMAScript 2015, If the <var>pattern</var> argument is a RegExp instance and the <var>flags</var> argument is not <b>undefined</b>, a new RegExp instance is created just like <var>pattern</var> except that <var>pattern</var>'s flags are replaced by the argument <var>flags</var>. In previous editions a <b>TypeError</b> exception was thrown when <var>pattern</var> was a RegExp instance and <var>flags</var> was not <b>undefined</b>.</p>\n  <p><a href=\"/text-processing#sec-properties-of-the-regexp-prototype-object\">22.2.6</a> In ECMAScript 2015, the RegExp prototype object is not a RegExp instance. In previous editions it was a RegExp instance whose pattern is the empty String.</p>\n  <p><a href=\"/text-processing#sec-properties-of-the-regexp-prototype-object\">22.2.6</a> In ECMAScript 2015, <b>\"source\"</b>, <b>\"global\"</b>, <b>\"ignoreCase\"</b>, and <b>\"multiline\"</b> are accessor properties defined on the RegExp prototype object. In previous editions they were data properties defined on RegExp instances.</p>\n  <p><a href=\"/structured-data#sec-atomics.notify\">25.4.15</a>: In ECMAScript 2019, <code>Atomics.wake</code> has been renamed to <code>Atomics.notify</code> to prevent confusion with <code>Atomics.wait</code>.</p>\n  <p><a href=\"/control-abstraction-objects#sec-asyncfromsynciteratorcontinuation\">27.1.5.4</a>, <a href=\"/control-abstraction-objects#sec-asyncgeneratorresume\">27.6.3.6</a>: In ECMAScript 2019, the number of Jobs enqueued by <code>await</code> was reduced, which could create an observable difference in resolution order between a <code>then()</code> call and an <code>await</code> expression.</p>"};
const _basePath = process.env.NEXT_PUBLIC_BASE_PATH || '';
const sections = _basePath
  ? Object.fromEntries(
      Object.entries(_sections).map(([k, v]) => [
        k,
        v.replaceAll('href="/', `href="${_basePath}/`),
      ])
    )
  : _sections;
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
