// Generated from ecma262/spec.html — do not edit by hand.
const _sections = {"1":"","2":"","3":"<p>The Math object:</p>\n    <ul>\n      <li>is <dfn><emu-intrinsic>%Math%</emu-intrinsic></dfn>.</li>\n      <li>is the initial value of the <b>\"Math\"</b> property of the global object.</li>\n      <li>is an ordinary object.</li>\n      <li>has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Object.prototype%</emu-intrinsic>.</li>\n      <li>is not a function object.</li>\n      <li>does not have a [[Construct]] internal method; it cannot be used as a constructor with the <code>new</code> operator.</li>\n      <li>does not have a [[Call]] internal method; it cannot be invoked as a function.</li>\n    </ul>\n    <emu-note>\n      <p>In this specification, the phrase “the Number value for <var>x</var>” has a technical meaning defined in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-number-type\">6.1.6.1</a>.</p>\n    </emu-note>\n\n    ","4":"","":"","1.1":"<p>The Number constructor:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%Number%</emu-intrinsic></dfn>.</li>\n        <li>is the initial value of the <b>\"Number\"</b> property of the global object.</li>\n        <li>creates and initializes a new Number object when called as a constructor.</li>\n        <li>performs a type conversion when called as a function rather than as a constructor.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified Number behaviour must include a <code>super</code> call to the Number constructor to create and initialize the subclass instance with a [[NumberData]] internal slot.</li>\n      </ul>\n\n      ","1.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>value</var> is present, then<ol><li>Let <var>prim</var> be ? ToNumeric(<var>value</var>).</li><li>If <var>prim</var> is a BigInt, let <var>n</var> be 𝔽(ℝ(<var>prim</var>)).</li><li>Else, let <var>n</var> be <var>prim</var>.</li></ol></li><li>Else,<ol><li>Let <var>n</var> be <b>+0</b><sub>𝔽</sub>.</li></ol></li><li>If NewTarget is <b>undefined</b>, return <var>n</var>.</li><li>Let <var>obj</var> be ? OrdinaryCreateFromConstructor(NewTarget, <b>\"<emu-intrinsic>%Number.prototype%</emu-intrinsic>\"</b>, « [[NumberData]] »).</li><li>Set <var>obj</var>.[[NumberData]] to <var>n</var>.</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","1.2":"<p>The Number constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Function.prototype%</emu-intrinsic>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","1.2.1":"<p>The value of <code>Number.EPSILON</code> is the Number value for the magnitude of the difference between 1 and the smallest value greater than 1 that is representable as a Number value, which is approximately 2.2204460492503130808472633361816 × 10<sup>-16</sup>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.2.2":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>number</var> is not a Number, return <b>false</b>.</li><li>If <var>number</var> is not finite, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n      ","1.2.3":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>number</var> is an integral Number, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","1.2.4":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>number</var> is not a Number, return <b>false</b>.</li><li>If <var>number</var> is <b>NaN</b>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p>This function differs from the global isNaN function (<a href=\"/global-object#sec-isnan-number\">19.2.3</a>) in that it does not convert its argument to a Number before determining whether it is <b>NaN</b>.</p>\n        </emu-note>\n      ","1.2.5":"<emu-note>\n          <p>An integer <var>n</var> is a \"<dfn id=\"safe-integer\">safe integer</dfn>\" if and only if the Number value for <var>n</var> is not the Number value for any other integer.</p>\n        </emu-note>\n        <p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>number</var> is an integral Number, then<ol><li>If abs(ℝ(<var>number</var>)) ≤ 2<sup>53</sup> - 1, return <b>true</b>.</li></ol></li><li>Return <b>false</b>.</li></ol></emu-alg>\n      ","1.2.6":"<emu-note>\n          <p>Due to rounding behaviour necessitated by precision limitations of IEEE 754-2019, the Number value for every integer greater than <code>Number.MAX_SAFE_INTEGER</code> is shared with at least one other integer. Such large-magnitude integers are therefore not safe, and are not guaranteed to be exactly representable as Number values or even to be distinguishable from each other. For example, both <code>9007199254740992</code> and <code>9007199254740993</code> evaluate to the Number value <b>9007199254740992</b><sub>𝔽</sub>.</p>\n        </emu-note>\n        <p>The value of <code>Number.MAX_SAFE_INTEGER</code> is <b>9007199254740991</b><sub>𝔽</sub> (𝔽(2<sup>53</sup> - 1)).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.2.7":"<p>The value of <code>Number.MAX_VALUE</code> is the largest positive finite value of the Number type, which is approximately <emu-eqn>1.7976931348623157 × 10<sup>308</sup></emu-eqn>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.2.8":"<emu-note>\n          <p>Due to rounding behaviour necessitated by precision limitations of IEEE 754-2019, the Number value for every integer less than <code>Number.MIN_SAFE_INTEGER</code> is shared with at least one other integer. Such large-magnitude integers are therefore not safe, and are not guaranteed to be exactly representable as Number values or even to be distinguishable from each other. For example, both <code>-9007199254740992</code> and <code>-9007199254740993</code> evaluate to the Number value <b>-9007199254740992</b><sub>𝔽</sub>.</p>\n        </emu-note>\n        <p>The value of <code>Number.MIN_SAFE_INTEGER</code> is <b>-9007199254740991</b><sub>𝔽</sub> (𝔽(-(2<sup>53</sup> - 1))).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.2.9":"<p>The value of <code>Number.MIN_VALUE</code> is the smallest positive value of the Number type, which is approximately <emu-eqn>5 × 10<sup>-324</sup></emu-eqn>.</p>\n        <p>In the IEEE 754-2019 double precision binary representation, the smallest possible value is a denormalized number. If an implementation does not support denormalized values, the value of <code>Number.MIN_VALUE</code> must be the smallest non-zero positive value that can actually be represented by the implementation.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.2.10":"<p>The value of <code>Number.NaN</code> is <b>NaN</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.2.11":"<p>The value of <code>Number.NEGATIVE_INFINITY</code> is <b>-∞</b><sub>𝔽</sub>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.2.12":"<p>The initial value of the <b>\"parseFloat\"</b> property is <emu-intrinsic>%parseFloat%</emu-intrinsic>.</p>\n      ","1.2.13":"<p>The initial value of the <b>\"parseInt\"</b> property is <emu-intrinsic>%parseInt%</emu-intrinsic>.</p>\n      ","1.2.14":"<p>The value of <code>Number.POSITIVE_INFINITY</code> is <b>+∞</b><sub>𝔽</sub>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.2.15":"<p>The initial value of <code>Number.prototype</code> is the Number prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.3":"<p>The <dfn>Number prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%Number.prototype%</emu-intrinsic></dfn>.</li>\n        <li>is an ordinary object.</li>\n        <li>is itself a Number object; it has a [[NumberData]] internal slot with the value <b>+0</b><sub>𝔽</sub>.</li>\n        <li>has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Object.prototype%</emu-intrinsic>.</li>\n      </ul>\n      <p>Unless explicitly stated otherwise, the methods of the Number prototype object defined below are not generic and the <b>this</b> value passed to them must be either a Number value or an object that has a [[NumberData]] internal slot that has been initialized to a Number value.</p>\n      <p>The phrase “this Number value” within the specification of a method refers to the result returned by calling the abstract operation ThisNumberValue with the <b>this</b> value of the method invocation passed as the argument.</p>\n\n      ","1.3.1":"<p>The initial value of <code>Number.prototype.constructor</code> is <emu-intrinsic>%Number%</emu-intrinsic>.</p>\n      ","1.3.2":"<p>This method returns a String containing this Number value represented in decimal exponential notation with one digit before the significand's decimal point and <var>fractionDigits</var> digits after the significand's decimal point. If <var>fractionDigits</var> is <b>undefined</b>, it includes as many significand digits as necessary to uniquely specify the Number (just like in ToString except that in this case the Number is always output in exponential notation).</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>numberValue</var> be ? ThisNumberValue(<b>this</b> value).</li><li>Let <var>fractionCount</var> be ? ToIntegerOrInfinity(<var>fractionDigits</var>).</li><li>Assert: If <var>fractionDigits</var> is <b>undefined</b>, then <var>fractionCount</var> is 0.</li><li>If <var>numberValue</var> is not finite, return Number::toString(<var>numberValue</var>, 10).</li><li>If <var>fractionCount</var> &lt; 0 or <var>fractionCount</var> > 100, throw a <b>RangeError</b> exception.</li><li>Set <var>numberValue</var> to ℝ(<var>numberValue</var>).</li><li>Let <var>sign</var> be the empty String.</li><li>If <var>numberValue</var> &lt; 0, then<ol><li>Set <var>sign</var> to <b>\"-\"</b>.</li><li>Set <var>numberValue</var> to -<var>numberValue</var>.</li></ol></li><li>If <var>numberValue</var> = 0, then<ol><li>Let <var>significand</var> be the String value consisting of <var>fractionCount</var> + 1 occurrences of the code unit 0x0030 (DIGIT ZERO).</li><li>Let <var>exponent</var> be 0.</li></ol></li><li>Else,<ol><li>If <var>fractionDigits</var> is not <b>undefined</b>, then<ol><li>Let <var>exponent</var> and <var>intSignificand</var> be integers such that 10<sup><var>fractionCount</var></sup> ≤ <var>intSignificand</var> &lt; 10<sup><var>fractionCount</var> + 1</sup> and for which <var>intSignificand</var> × 10<sup><var>exponent</var> - <var>fractionCount</var></sup> - <var>numberValue</var> is as close to zero as possible. If there are two such sets of <var>exponent</var> and <var>intSignificand</var>, pick the <var>exponent</var> and <var>intSignificand</var> for which <var>intSignificand</var> × 10<sup><var>exponent</var> - <var>fractionCount</var></sup> is larger.</li></ol></li><li>Else,<ol><li>Let <var>exponent</var>, <var>intSignificand</var>, and <var>ff</var> be integers such that <var>ff</var> ≥ 0, 10<sup><var>ff</var></sup> ≤ <var>intSignificand</var> &lt; 10<sup><var>ff</var> + 1</sup>, 𝔽(<var>intSignificand</var> × 10<sup><var>exponent</var> - <var>ff</var></sup>) is 𝔽(<var>numberValue</var>), and <var>ff</var> is as small as possible. Note that the decimal representation of <var>intSignificand</var> has <var>ff</var> + 1 digits, <var>intSignificand</var> is not divisible by 10, and the least significant digit of <var>intSignificand</var> is not necessarily uniquely determined by these criteria.</li><li>Set <var>fractionCount</var> to <var>ff</var>.</li></ol></li><li>Let <var>significand</var> be the String value consisting of the digits of the decimal representation of <var>intSignificand</var> (in order, with no leading zeroes).</li></ol></li><li>If <var>fractionCount</var> ≠ 0, then<ol><li>Let <var>integerPart</var> be the first code unit of <var>significand</var>.</li><li>Let <var>fractionalPart</var> be the other <var>fractionCount</var> code units of <var>significand</var>.</li><li>Set <var>significand</var> to the string-concatenation of <var>integerPart</var>, <b>\".\"</b>, and <var>fractionalPart</var>.</li></ol></li><li>If <var>exponent</var> = 0, then<ol><li>Let <var>exponentSign</var> be <b>\"+\"</b>.</li><li>Let <var>exponentDigits</var> be <b>\"0\"</b>.</li></ol></li><li>Else,<ol><li>If <var>exponent</var> > 0, then<ol><li>Let <var>exponentSign</var> be <b>\"+\"</b>.</li></ol></li><li>Else,<ol><li>Assert: <var>exponent</var> &lt; 0.</li><li>Let <var>exponentSign</var> be <b>\"-\"</b>.</li><li>Set <var>exponent</var> to -<var>exponent</var>.</li></ol></li><li>Let <var>exponentDigits</var> be the String value consisting of the digits of the decimal representation of <var>exponent</var> (in order, with no leading zeroes).</li></ol></li><li>Set <var>significand</var> to the string-concatenation of <var>significand</var>, <b>\"e\"</b>, <var>exponentSign</var>, and <var>exponentDigits</var>.</li><li>Return the string-concatenation of <var>sign</var> and <var>significand</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>For implementations that provide more accurate conversions than required by the rules above, it is recommended that the following alternative version of step step-number-proto-toexponential-intermediate-values be used as a guideline:</p>\n          <emu-alg replaces-step=\"step-number-proto-toexponential-intermediate-values\"><ol><li>Let <var>exponent</var>, <var>intSignificand</var>, and <var>ff</var> be integers such that <var>ff</var> ≥ 0, 10<sup><var>ff</var></sup> ≤ <var>intSignificand</var> &lt; 10<sup><var>ff</var> + 1</sup>, 𝔽(<var>intSignificand</var> × 10<sup><var>exponent</var> - <var>ff</var></sup>) is 𝔽(<var>numberValue</var>), and <var>ff</var> is as small as possible. If there are multiple possibilities for <var>intSignificand</var>, choose <var>intSignificand</var> such that 𝔽(<var>intSignificand</var> × 10<sup><var>exponent</var> - <var>ff</var></sup>) is closest to 𝔽(<var>numberValue</var>). If there are two such possible values of <var>intSignificand</var>, choose the one that is even.</li></ol></emu-alg>\n        </emu-note>\n      ","1.3.3":"<emu-note>\n          <p>This method returns a String containing this Number value represented in decimal fixed-point notation with <var>fractionDigits</var> digits after the decimal point. If <var>fractionDigits</var> is <b>undefined</b>, 0 is assumed.</p>\n        </emu-note>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>numberValue</var> be ? ThisNumberValue(<b>this</b> value).</li><li>Let <var>fractionCount</var> be ? ToIntegerOrInfinity(<var>fractionDigits</var>).</li><li>Assert: If <var>fractionDigits</var> is <b>undefined</b>, then <var>fractionCount</var> is 0.</li><li>If <var>fractionCount</var> is not finite, throw a <b>RangeError</b> exception.</li><li>If <var>fractionCount</var> &lt; 0 or <var>fractionCount</var> > 100, throw a <b>RangeError</b> exception.</li><li>If <var>numberValue</var> is not finite, return Number::toString(<var>numberValue</var>, 10).</li><li>Set <var>numberValue</var> to ℝ(<var>numberValue</var>).</li><li>Let <var>sign</var> be the empty String.</li><li>If <var>numberValue</var> &lt; 0, then<ol><li>Set <var>sign</var> to <b>\"-\"</b>.</li><li>Set <var>numberValue</var> to -<var>numberValue</var>.</li></ol></li><li>If <var>numberValue</var> ≥ 10<sup>21</sup>, then<ol><li>Let <var>digitString</var> be ! ToString(𝔽(<var>numberValue</var>)).</li></ol></li><li>Else,<ol><li>Let <var>intValue</var> be an integer for which <var>intValue</var> / 10<sup><var>fractionCount</var></sup> - <var>numberValue</var> is as close to zero as possible. If there are two such <var>intValue</var>, pick the larger <var>intValue</var>.</li><li>If <var>intValue</var> = 0, let <var>digitString</var> be <b>\"0\"</b>; else let <var>digitString</var> be the String value consisting of the digits of the decimal representation of <var>intValue</var> (in order, with no leading zeroes).</li><li>If <var>fractionCount</var> ≠ 0, then<ol><li>Let <var>digitCount</var> be the length of <var>digitString</var>.</li><li>If <var>digitCount</var> ≤ <var>fractionCount</var>, then<ol><li>Let <var>zeroPad</var> be the String value consisting of <var>fractionCount</var> + 1 - <var>digitCount</var> occurrences of the code unit 0x0030 (DIGIT ZERO).</li><li>Set <var>digitString</var> to the string-concatenation of <var>zeroPad</var> and <var>digitString</var>.</li><li>Set <var>digitCount</var> to <var>fractionCount</var> + 1.</li></ol></li><li>Let <var>integerPart</var> be the first <var>digitCount</var> - <var>fractionCount</var> code units of <var>digitString</var>.</li><li>Let <var>fractionalPart</var> be the other <var>fractionCount</var> code units of <var>digitString</var>.</li><li>Set <var>digitString</var> to the string-concatenation of <var>integerPart</var>, <b>\".\"</b>, and <var>fractionalPart</var>.</li></ol></li></ol></li><li>Return the string-concatenation of <var>sign</var> and <var>digitString</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The output of <code>toFixed</code> may be more precise than <code>toString</code> for some values because toString only prints enough significant digits to distinguish the number from adjacent Number values. For example,</p>\n          <p>\n            <code>(1000000000000000128).toString()</code> returns <b>\"1000000000000000100\"</b>, while<br>\n            <code>(1000000000000000128).toFixed(0)</code> returns <b>\"1000000000000000128\"</b>.\n          </p>\n        </emu-note>\n      ","1.3.4":"<p>An ECMAScript implementation that includes the ECMA-402 Internationalization API must implement this method as specified in the ECMA-402 specification. If an ECMAScript implementation does not include the ECMA-402 API the following specification of this method is used:</p>\n        <p>This method produces a String value that represents this Number value formatted according to the conventions of the host environment's current locale. This method is implementation-defined, and it is permissible, but not encouraged, for it to return the same thing as <code>toString</code>.</p>\n        <p>The meanings of the optional parameters to this method are defined in the ECMA-402 specification; implementations that do not include ECMA-402 support must not use those parameter positions for anything else.</p>\n      ","1.3.5":"<p>This method returns a String containing this Number value represented either in decimal exponential notation with one digit before the significand's decimal point and <emu-eqn><var>precision</var> - 1</emu-eqn> digits after the significand's decimal point or in decimal fixed notation with <var>precision</var> significant digits. If <var>precision</var> is <b>undefined</b>, it calls ToString instead.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>numberValue</var> be ? ThisNumberValue(<b>this</b> value).</li><li>If <var>precision</var> is <b>undefined</b>, return ! ToString(<var>numberValue</var>).</li><li>Let <var>precisionCount</var> be ? ToIntegerOrInfinity(<var>precision</var>).</li><li>If <var>numberValue</var> is not finite, return Number::toString(<var>numberValue</var>, 10).</li><li>If <var>precisionCount</var> &lt; 1 or <var>precisionCount</var> > 100, throw a <b>RangeError</b> exception.</li><li>Set <var>numberValue</var> to ℝ(<var>numberValue</var>).</li><li>Let <var>sign</var> be the empty String.</li><li>If <var>numberValue</var> &lt; 0, then<ol><li>Set <var>sign</var> to the code unit 0x002D (HYPHEN-MINUS).</li><li>Set <var>numberValue</var> to -<var>numberValue</var>.</li></ol></li><li>If <var>numberValue</var> = 0, then<ol><li>Let <var>significand</var> be the String value consisting of <var>precisionCount</var> occurrences of the code unit 0x0030 (DIGIT ZERO).</li><li>Let <var>exponent</var> be 0.</li></ol></li><li>Else,<ol><li>Let <var>exponent</var> and <var>intSignificand</var> be integers such that 10<sup><var>precisionCount</var> - 1</sup> ≤ <var>intSignificand</var> &lt; 10<sup><var>precisionCount</var></sup> and for which <var>intSignificand</var> × 10<sup><var>exponent</var> - <var>precisionCount</var> + 1</sup> - <var>numberValue</var> is as close to zero as possible. If there are two such sets of <var>exponent</var> and <var>intSignificand</var>, pick the <var>exponent</var> and <var>intSignificand</var> for which <var>intSignificand</var> × 10<sup><var>exponent</var> - <var>precisionCount</var> + 1</sup> is larger.</li><li>Let <var>significand</var> be the String value consisting of the digits of the decimal representation of <var>intSignificand</var> (in order, with no leading zeroes).</li><li>If <var>exponent</var> &lt; -6 or <var>exponent</var> ≥ <var>precisionCount</var>, then<ol><li>Assert: <var>exponent</var> ≠ 0.</li><li>If <var>precisionCount</var> ≠ 1, then<ol><li>Let <var>integerPart</var> be the first code unit of <var>significand</var>.</li><li>Let <var>fractionalPart</var> be the other <var>precisionCount</var> - 1 code units of <var>significand</var>.</li><li>Set <var>significand</var> to the string-concatenation of <var>integerPart</var>, <b>\".\"</b>, and <var>fractionalPart</var>.</li></ol></li><li>If <var>exponent</var> > 0, then<ol><li>Let <var>exponentSign</var> be the code unit 0x002B (PLUS SIGN).</li></ol></li><li>Else,<ol><li>Assert: <var>exponent</var> &lt; 0.</li><li>Let <var>exponentSign</var> be the code unit 0x002D (HYPHEN-MINUS).</li><li>Set <var>exponent</var> to -<var>exponent</var>.</li></ol></li><li>Let <var>exponentDigits</var> be the String value consisting of the digits of the decimal representation of <var>exponent</var> (in order, with no leading zeroes).</li><li>Return the string-concatenation of <var>sign</var>, <var>significand</var>, the code unit 0x0065 (LATIN SMALL LETTER E), <var>exponentSign</var>, and <var>exponentDigits</var>.</li></ol></li></ol></li><li>If <var>exponent</var> = <var>precisionCount</var> - 1, return the string-concatenation of <var>sign</var> and <var>significand</var>.</li><li>If <var>exponent</var> ≥ 0, then<ol><li>Set <var>significand</var> to the string-concatenation of the first <var>exponent</var> + 1 code units of <var>significand</var>, the code unit 0x002E (FULL STOP), and the remaining <var>precisionCount</var> - (<var>exponent</var> + 1) code units of <var>significand</var>.</li></ol></li><li>Else,<ol><li>Set <var>significand</var> to the string-concatenation of the code unit 0x0030 (DIGIT ZERO), the code unit 0x002E (FULL STOP), -(<var>exponent</var> + 1) occurrences of the code unit 0x0030 (DIGIT ZERO), and the String <var>significand</var>.</li></ol></li><li>Return the string-concatenation of <var>sign</var> and <var>significand</var>.</li></ol></emu-alg>\n      ","1.3.6":"<emu-note>\n          <p>The optional <var>radix</var> should be an integral Number value in the inclusive interval from <b>2</b><sub>𝔽</sub> to <b>36</b><sub>𝔽</sub>. If <var>radix</var> is <b>undefined</b> then <b>10</b><sub>𝔽</sub> is used as the value of <var>radix</var>.</p>\n        </emu-note>\n        <p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>x</var> be ? ThisNumberValue(<b>this</b> value).</li><li>If <var>radix</var> is <b>undefined</b>, let <var>radixMV</var> be 10.</li><li>Else, let <var>radixMV</var> be ? ToIntegerOrInfinity(<var>radix</var>).</li><li>If <var>radixMV</var> is not in the inclusive interval from 2 to 36, throw a <b>RangeError</b> exception.</li><li>Return Number::toString(<var>x</var>, <var>radixMV</var>).</li></ol></emu-alg>\n        <p>This method is not generic; it throws a <b>TypeError</b> exception if its <b>this</b> value is not a Number or a Number object. Therefore, it cannot be transferred to other kinds of objects for use as a method.</p>\n        <p>The <b>\"length\"</b> property of this method is <b>1</b><sub>𝔽</sub>.</p>\n      ","1.3.7":"<emu-alg><ol><li>Return ? ThisNumberValue(<b>this</b> value).</li></ol></emu-alg>\n\n        ","1.3.7.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>value</var> is a Number, return <var>value</var>.</li><li>If <var>value</var> is an Object and <var>value</var> has a [[NumberData]] internal slot, then<ol><li>Let <var>n</var> be <var>value</var>.[[NumberData]].</li><li>Assert: <var>n</var> is a Number.</li><li>Return <var>n</var>.</li></ol></li><li>Throw a <b>TypeError</b> exception.</li></ol></emu-alg>\n        ","1.4":"<p>Number instances are ordinary objects that inherit properties from the Number prototype object. Number instances also have a [[NumberData]] internal slot. The [[NumberData]] internal slot is the Number value represented by this Number object.</p>\n    ","2.1":"<p>The BigInt constructor:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%BigInt%</emu-intrinsic></dfn>.</li>\n        <li>is the initial value of the <b>\"BigInt\"</b> property of the global object.</li>\n        <li>performs a type conversion when called as a function rather than as a constructor.</li>\n        <li>is not intended to be used with the <code>new</code> operator or to be subclassed. It may be used as the value of an <code>extends</code> clause of a class definition but a <code>super</code> call to the BigInt constructor will cause an exception.</li>\n      </ul>\n\n      ","2.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is not <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>prim</var> be ? ToPrimitive(<var>value</var>, <emu-const>number</emu-const>).</li><li>If <var>prim</var> is a Number, return ? NumberToBigInt(<var>prim</var>).</li><li>Return ? <emu-meta suppress-effects=\"user-code\">ToBigInt(<var>prim</var>)</emu-meta>.</li></ol></emu-alg>\n\n        ","2.1.1.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>number</var> is not an integral Number, throw a <b>RangeError</b> exception.</li><li>Return ℤ(ℝ(<var>number</var>)).</li></ol></emu-alg>\n        ","2.2":"<p>The BigInt constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Function.prototype%</emu-intrinsic>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","2.2.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Set <var>bits</var> to ? ToIndex(<var>bits</var>).</li><li>Set <var>bigint</var> to ? ToBigInt(<var>bigint</var>).</li><li>Let <var>mod</var> be ℝ(<var>bigint</var>) modulo 2<sup><var>bits</var></sup>.</li><li>If <var>mod</var> ≥ 2<sup><var>bits</var> - 1</sup>, return ℤ(<var>mod</var> - 2<sup><var>bits</var></sup>).</li><li>Return ℤ(<var>mod</var>).</li></ol></emu-alg>\n      ","2.2.2":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Set <var>bits</var> to ? ToIndex(<var>bits</var>).</li><li>Set <var>bigint</var> to ? ToBigInt(<var>bigint</var>).</li><li>Return ℤ(ℝ(<var>bigint</var>) modulo 2<sup><var>bits</var></sup>).</li></ol></emu-alg>\n      ","2.2.3":"<p>The initial value of <code>BigInt.prototype</code> is the BigInt prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","2.3":"<p>The <dfn>BigInt prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%BigInt.prototype%</emu-intrinsic></dfn>.</li>\n        <li>is an ordinary object.</li>\n        <li>is not a BigInt object; it does not have a [[BigIntData]] internal slot.</li>\n        <li>has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Object.prototype%</emu-intrinsic>.</li>\n      </ul>\n      <p>The phrase “this BigInt value” within the specification of a method refers to the result returned by calling the abstract operation ThisBigIntValue with the <b>this</b> value of the method invocation passed as the argument.</p>\n\n      ","2.3.1":"<p>The initial value of <code>BigInt.prototype.constructor</code> is <emu-intrinsic>%BigInt%</emu-intrinsic>.</p>\n      ","2.3.2":"<p>An ECMAScript implementation that includes the ECMA-402 Internationalization API must implement this method as specified in the ECMA-402 specification. If an ECMAScript implementation does not include the ECMA-402 API the following specification of this method is used:</p>\n        <p>This method produces a String value that represents this BigInt value formatted according to the conventions of the host environment's current locale. This method is implementation-defined, and it is permissible, but not encouraged, for it to return the same thing as <code>toString</code>.</p>\n        <p>The meanings of the optional parameters to this method are defined in the ECMA-402 specification; implementations that do not include ECMA-402 support must not use those parameter positions for anything else.</p>\n      ","2.3.3":"<emu-note>\n          <p>The optional <var>radix</var> should be an integral Number value in the inclusive interval from <b>2</b><sub>𝔽</sub> to <b>36</b><sub>𝔽</sub>. If <var>radix</var> is <b>undefined</b> then <b>10</b><sub>𝔽</sub> is used as the value of <var>radix</var>.</p>\n        </emu-note>\n        <p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>x</var> be ? ThisBigIntValue(<b>this</b> value).</li><li>If <var>radix</var> is <b>undefined</b>, let <var>radixMV</var> be 10.</li><li>Else, let <var>radixMV</var> be ? ToIntegerOrInfinity(<var>radix</var>).</li><li>If <var>radixMV</var> is not in the inclusive interval from 2 to 36, throw a <b>RangeError</b> exception.</li><li>Return BigInt::toString(<var>x</var>, <var>radixMV</var>).</li></ol></emu-alg>\n        <p>This method is not generic; it throws a <b>TypeError</b> exception if its <b>this</b> value is not a BigInt or a BigInt object. Therefore, it cannot be transferred to other kinds of objects for use as a method.</p>\n      ","2.3.4":"<emu-alg><ol><li>Return ? ThisBigIntValue(<b>this</b> value).</li></ol></emu-alg>\n\n        ","2.3.4.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>value</var> is a BigInt, return <var>value</var>.</li><li>If <var>value</var> is an Object and <var>value</var> has a [[BigIntData]] internal slot, then<ol><li>Assert: <var>value</var>.[[BigIntData]] is a BigInt.</li><li>Return <var>value</var>.[[BigIntData]].</li></ol></li><li>Throw a <b>TypeError</b> exception.</li></ol></emu-alg>\n        ","2.3.5":"<p>The initial value of the <emu-intrinsic>%Symbol.toStringTag%</emu-intrinsic> property is the String value <b>\"BigInt\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","2.4":"<p>BigInt instances are ordinary objects that inherit properties from the BigInt prototype object. BigInt instances also have a [[BigIntData]] internal slot. The [[BigIntData]] internal slot is the BigInt value represented by this BigInt object.</p>\n    ","3.1":"","3.1.1":"<p>The Number value for <i>e</i>, the base of the natural logarithms, which is approximately 2.7182818284590452354.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","3.1.2":"<p>The Number value for the natural logarithm of 10, which is approximately 2.302585092994046.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","3.1.3":"<p>The Number value for the natural logarithm of 2, which is approximately 0.6931471805599453.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","3.1.4":"<p>The Number value for the base-10 logarithm of <i>e</i>, the base of the natural logarithms; this value is approximately 0.4342944819032518.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n        <emu-note>\n          <p>The value of <code>Math.LOG10E</code> is approximately the reciprocal of the value of <code>Math.LN10</code>.</p>\n        </emu-note>\n      ","3.1.5":"<p>The Number value for the base-2 logarithm of <i>e</i>, the base of the natural logarithms; this value is approximately 1.4426950408889634.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n        <emu-note>\n          <p>The value of <code>Math.LOG2E</code> is approximately the reciprocal of the value of <code>Math.LN2</code>.</p>\n        </emu-note>\n      ","3.1.6":"<p>The Number value for π, the ratio of the circumference of a circle to its diameter, which is approximately 3.1415926535897932.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","3.1.7":"<p>The Number value for the square root of ½, which is approximately 0.7071067811865476.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n        <emu-note>\n          <p>The value of <code>Math.SQRT1_2</code> is approximately the reciprocal of the value of <code>Math.SQRT2</code>.</p>\n        </emu-note>\n      ","3.1.8":"<p>The Number value for the square root of 2, which is approximately 1.4142135623730951.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","3.1.9":"<p>The initial value of the <emu-intrinsic>%Symbol.toStringTag%</emu-intrinsic> property is the String value <b>\"Math\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","3.2":"<emu-note>\n        <p>The behaviour of the functions <code>acos</code>, <code>acosh</code>, <code>asin</code>, <code>asinh</code>, <code>atan</code>, <code>atanh</code>, <code>atan2</code>, <code>cbrt</code>, <code>cos</code>, <code>cosh</code>, <code>exp</code>, <code>expm1</code>, <code>hypot</code>, <code>log</code>, <code>log1p</code>, <code>log2</code>, <code>log10</code>, <code>pow</code>, <code>random</code>, <code>sin</code>, <code>sinh</code>, <code>tan</code>, and <code>tanh</code> is not precisely specified here except to require specific results for certain argument values that represent boundary cases of interest. For other argument values, these functions are intended to compute approximations to the results of familiar mathematical functions, but some latitude is allowed in the choice of approximation algorithms. The general intent is that an implementer should be able to use the same mathematical library for ECMAScript on a given hardware platform that is available to C programmers on that platform.</p>\n        <p>Although the choice of algorithms is left to the implementation, it is recommended (but not specified by this standard) that implementations use the approximation algorithms for IEEE 754-2019 arithmetic contained in <code>fdlibm</code>, the freely distributable mathematical library from Sun Microsystems (<a href=\"http://www.netlib.org/fdlibm\">http://www.netlib.org/fdlibm</a>).</p>\n      </emu-note>\n\n      ","3.2.1":"<p>This function returns the absolute value of <var>x</var>; the result has the same magnitude as <var>x</var> but has positive sign.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>If <var>n</var> is <b>-0</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>If <var>n</var> is <b>-∞</b><sub>𝔽</sub>, return <b>+∞</b><sub>𝔽</sub>.</li><li>If <var>n</var> &lt; <b>-0</b><sub>𝔽</sub>, return -<var>n</var>.</li><li>Return <var>n</var>.</li></ol></emu-alg>\n      ","3.2.2":"<p>This function returns the inverse cosine of <var>x</var>. The result is expressed in radians and is in the inclusive interval from <b>+0</b><sub>𝔽</sub> to 𝔽(π).</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is <b>NaN</b>, <var>n</var> > <b>1</b><sub>𝔽</sub>, or <var>n</var> &lt; <b>-1</b><sub>𝔽</sub>, return <b>NaN</b>.</li><li>If <var>n</var> is <b>1</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>Return an implementation-approximated Number value representing the inverse cosine of ℝ(<var>n</var>).</li></ol></emu-alg>\n      ","3.2.3":"<p>This function returns the inverse hyperbolic cosine of <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is either <b>NaN</b> or <b>+∞</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> is <b>1</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>If <var>n</var> &lt; <b>1</b><sub>𝔽</sub>, return <b>NaN</b>.</li><li>Return an implementation-approximated Number value representing the inverse hyperbolic cosine of ℝ(<var>n</var>).</li></ol></emu-alg>\n      ","3.2.4":"<p>This function returns the inverse sine of <var>x</var>. The result is expressed in radians and is in the inclusive interval from 𝔽(-π / 2) to 𝔽(π / 2).</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is one of <b>NaN</b>, <b>+0</b><sub>𝔽</sub>, or <b>-0</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> > <b>1</b><sub>𝔽</sub> or <var>n</var> &lt; <b>-1</b><sub>𝔽</sub>, return <b>NaN</b>.</li><li>Return an implementation-approximated Number value representing the inverse sine of ℝ(<var>n</var>).</li></ol></emu-alg>\n      ","3.2.5":"<p>This function returns the inverse hyperbolic sine of <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is not finite or <var>n</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return <var>n</var>.</li><li>Return an implementation-approximated Number value representing the inverse hyperbolic sine of ℝ(<var>n</var>).</li></ol></emu-alg>\n      ","3.2.6":"<p>This function returns the inverse tangent of <var>x</var>. The result is expressed in radians and is in the inclusive interval from 𝔽(-π / 2) to 𝔽(π / 2).</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is one of <b>NaN</b>, <b>+0</b><sub>𝔽</sub>, or <b>-0</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> is <b>+∞</b><sub>𝔽</sub>, return an implementation-approximated Number value representing π / 2.</li><li>If <var>n</var> is <b>-∞</b><sub>𝔽</sub>, return an implementation-approximated Number value representing -π / 2.</li><li>Return an implementation-approximated Number value representing the inverse tangent of ℝ(<var>n</var>).</li></ol></emu-alg>\n      ","3.2.7":"<p>This function returns the inverse hyperbolic tangent of <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is one of <b>NaN</b>, <b>+0</b><sub>𝔽</sub>, or <b>-0</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> > <b>1</b><sub>𝔽</sub> or <var>n</var> &lt; <b>-1</b><sub>𝔽</sub>, return <b>NaN</b>.</li><li>If <var>n</var> is <b>1</b><sub>𝔽</sub>, return <b>+∞</b><sub>𝔽</sub>.</li><li>If <var>n</var> is <b>-1</b><sub>𝔽</sub>, return <b>-∞</b><sub>𝔽</sub>.</li><li>Return an implementation-approximated Number value representing the inverse hyperbolic tangent of ℝ(<var>n</var>).</li></ol></emu-alg>\n      ","3.2.8":"<p>This function returns the inverse tangent of the quotient <emu-eqn><var>y</var> / <var>x</var></emu-eqn> of the arguments <var>y</var> and <var>x</var>, where the signs of <var>y</var> and <var>x</var> are used to determine the quadrant of the result. Note that it is intentional and traditional for the two-argument inverse tangent function that the argument named <var>y</var> be first and the argument named <var>x</var> be second. The result is expressed in radians and is in the inclusive interval from -π to +π.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>ny</var> be ? ToNumber(<var>y</var>).</li><li>Let <var>nx</var> be ? ToNumber(<var>x</var>).</li><li>If <var>ny</var> is <b>NaN</b> or <var>nx</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>If <var>ny</var> is <b>+∞</b><sub>𝔽</sub>, then<ol><li>If <var>nx</var> is <b>+∞</b><sub>𝔽</sub>, return an implementation-approximated Number value representing π / 4.</li><li>If <var>nx</var> is <b>-∞</b><sub>𝔽</sub>, return an implementation-approximated Number value representing 3π / 4.</li><li>Return an implementation-approximated Number value representing π / 2.</li></ol></li><li>If <var>ny</var> is <b>-∞</b><sub>𝔽</sub>, then<ol><li>If <var>nx</var> is <b>+∞</b><sub>𝔽</sub>, return an implementation-approximated Number value representing -π / 4.</li><li>If <var>nx</var> is <b>-∞</b><sub>𝔽</sub>, return an implementation-approximated Number value representing -3π / 4.</li><li>Return an implementation-approximated Number value representing -π / 2.</li></ol></li><li>If <var>ny</var> is <b>+0</b><sub>𝔽</sub>, then<ol><li>If <var>nx</var> > <b>+0</b><sub>𝔽</sub> or <var>nx</var> is <b>+0</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>Return an implementation-approximated Number value representing π.</li></ol></li><li>If <var>ny</var> is <b>-0</b><sub>𝔽</sub>, then<ol><li>If <var>nx</var> > <b>+0</b><sub>𝔽</sub> or <var>nx</var> is <b>+0</b><sub>𝔽</sub>, return <b>-0</b><sub>𝔽</sub>.</li><li>Return an implementation-approximated Number value representing -π.</li></ol></li><li>Assert: <var>ny</var> is finite and is neither <b>+0</b><sub>𝔽</sub> nor <b>-0</b><sub>𝔽</sub>.</li><li>If <var>ny</var> > <b>+0</b><sub>𝔽</sub>, then<ol><li>If <var>nx</var> is <b>+∞</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>If <var>nx</var> is <b>-∞</b><sub>𝔽</sub>, return an implementation-approximated Number value representing π.</li><li>If <var>nx</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return an implementation-approximated Number value representing π / 2.</li></ol></li><li>If <var>ny</var> &lt; <b>-0</b><sub>𝔽</sub>, then<ol><li>If <var>nx</var> is <b>+∞</b><sub>𝔽</sub>, return <b>-0</b><sub>𝔽</sub>.</li><li>If <var>nx</var> is <b>-∞</b><sub>𝔽</sub>, return an implementation-approximated Number value representing -π.</li><li>If <var>nx</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return an implementation-approximated Number value representing -π / 2.</li></ol></li><li>Assert: <var>nx</var> is finite and is neither <b>+0</b><sub>𝔽</sub> nor <b>-0</b><sub>𝔽</sub>.</li><li>Let <var>r</var> be the inverse tangent of abs(ℝ(<var>ny</var>) / ℝ(<var>nx</var>)).</li><li>If <var>nx</var> &lt; <b>-0</b><sub>𝔽</sub>, then<ol><li>If <var>ny</var> > <b>+0</b><sub>𝔽</sub>, set <var>r</var> to π - <var>r</var>.</li><li>Else, set <var>r</var> to -π + <var>r</var>.</li></ol></li><li>Else,<ol><li>If <var>ny</var> &lt; <b>-0</b><sub>𝔽</sub>, set <var>r</var> to -<var>r</var>.</li></ol></li><li>Return an implementation-approximated Number value representing <var>r</var>.</li></ol></emu-alg>\n      ","3.2.9":"<p>This function returns the cube root of <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is not finite or <var>n</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return <var>n</var>.</li><li>Return an implementation-approximated Number value representing the cube root of ℝ(<var>n</var>).</li></ol></emu-alg>\n      ","3.2.10":"<p>This function returns the smallest (closest to -∞) integral Number value that is not less than <var>x</var>. If <var>x</var> is already an integral Number, the result is <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is not finite or <var>n</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> &lt; <b>-0</b><sub>𝔽</sub> and <var>n</var> > <b>-1</b><sub>𝔽</sub>, return <b>-0</b><sub>𝔽</sub>.</li><li>If <var>n</var> is an integral Number, return <var>n</var>.</li><li>Return the smallest (closest to -∞) integral Number value that is not less than <var>n</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The value of <code>Math.ceil(x)</code> is the same as the value of <code>-Math.floor(-x)</code>.</p>\n        </emu-note>\n      ","3.2.11":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToUint32(<var>x</var>).</li><li>Let <var>p</var> be the number of leading zero bits in the unsigned 32-bit binary representation of <var>n</var>.</li><li>Return 𝔽(<var>p</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>If <var>n</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, this method returns <b>32</b><sub>𝔽</sub>. If the most significant bit of the 32-bit binary encoding of <var>n</var> is 1, this method returns <b>+0</b><sub>𝔽</sub>.</p>\n        </emu-note>\n      ","3.2.12":"<p>This function returns the cosine of <var>x</var>. The argument is expressed in radians.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is not finite, return <b>NaN</b>.</li><li>If <var>n</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return <b>1</b><sub>𝔽</sub>.</li><li>Return an implementation-approximated Number value representing the cosine of ℝ(<var>n</var>).</li></ol></emu-alg>\n      ","3.2.13":"<p>This function returns the hyperbolic cosine of <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>If <var>n</var> is either <b>+∞</b><sub>𝔽</sub> or <b>-∞</b><sub>𝔽</sub>, return <b>+∞</b><sub>𝔽</sub>.</li><li>If <var>n</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return <b>1</b><sub>𝔽</sub>.</li><li>Return an implementation-approximated Number value representing the hyperbolic cosine of ℝ(<var>n</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>The value of <code>Math.cosh(x)</code> is the same as the value of <code>(Math.exp(x) + Math.exp(-x)) / 2</code>.</p>\n        </emu-note>\n      ","3.2.14":"<p>This function returns the exponential function of <var>x</var> (<var>e</var> raised to the power of <var>x</var>, where <var>e</var> is the base of the natural logarithms).</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is either <b>NaN</b> or <b>+∞</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return <b>1</b><sub>𝔽</sub>.</li><li>If <var>n</var> is <b>-∞</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>Return an implementation-approximated Number value representing the exponential function of ℝ(<var>n</var>).</li></ol></emu-alg>\n      ","3.2.15":"<p>This function returns the result of subtracting 1 from the exponential function of <var>x</var> (<var>e</var> raised to the power of <var>x</var>, where <var>e</var> is the base of the natural logarithms). The result is computed in a way that is accurate even when the value of <var>x</var> is close to 0.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is one of <b>NaN</b>, <b>+0</b><sub>𝔽</sub>, <b>-0</b><sub>𝔽</sub>, or <b>+∞</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> is <b>-∞</b><sub>𝔽</sub>, return <b>-1</b><sub>𝔽</sub>.</li><li>Let <var>exp</var> be the exponential function of ℝ(<var>n</var>).</li><li>Return an implementation-approximated Number value representing <var>exp</var> - 1.</li></ol></emu-alg>\n      ","3.2.16":"<p>This function returns the greatest (closest to +∞) integral Number value that is not greater than <var>x</var>. If <var>x</var> is already an integral Number, the result is <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is not finite or <var>n</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> &lt; <b>1</b><sub>𝔽</sub> and <var>n</var> > <b>+0</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>If <var>n</var> is an integral Number, return <var>n</var>.</li><li>Return the greatest (closest to +∞) integral Number value that is not greater than <var>n</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The value of <code>Math.floor(x)</code> is the same as the value of <code>-Math.ceil(-x)</code>.</p>\n        </emu-note>\n      ","3.2.17":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>If <var>n</var> is one of <b>+0</b><sub>𝔽</sub>, <b>-0</b><sub>𝔽</sub>, <b>+∞</b><sub>𝔽</sub>, or <b>-∞</b><sub>𝔽</sub>, return <var>n</var>.</li><li>Let <var>n32</var> be the result of converting <var>n</var> to IEEE 754-2019 binary32 format using roundTiesToEven mode.</li><li>Let <var>n64</var> be the result of converting <var>n32</var> to IEEE 754-2019 binary64 format.</li><li>Return the ECMAScript Number value corresponding to <var>n64</var>.</li></ol></emu-alg>\n      ","3.2.18":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>If <var>n</var> is one of <b>+0</b><sub>𝔽</sub>, <b>-0</b><sub>𝔽</sub>, <b>+∞</b><sub>𝔽</sub>, or <b>-∞</b><sub>𝔽</sub>, return <var>n</var>.</li><li>Let <var>n16</var> be the result of converting <var>n</var> to IEEE 754-2019 binary16 format using roundTiesToEven mode.</li><li>Let <var>n64</var> be the result of converting <var>n16</var> to IEEE 754-2019 binary64 format.</li><li>Return the ECMAScript Number value corresponding to <var>n64</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>This operation is not the same as casting to binary32 and then to binary16 because of the possibility of double-rounding: consider the number <var>k</var> = <b>1.00048828125000022204</b><sub>𝔽</sub>, for example, for which Math.f16round(<var>k</var>) is <b>1.0009765625</b><sub>𝔽</sub>, but Math.f16round(Math.fround(<var>k</var>)) is <b>1</b><sub>𝔽</sub>.</p>\n          <p>Not all platforms provide native support for casting from binary64 to binary16. There are various libraries which can provide this, including the MIT-licensed <a href=\"https://half.sourceforge.net/\">half</a> library. Alternatively, it is possible to first cast from binary64 to binary32 under roundTiesToEven and then check whether the result could lead to incorrect double-rounding. Such cases can be handled explicitly by adjusting the mantissa of the binary32 value so that it is the value which would be produced by performing the initial cast under roundTiesToOdd. Casting the adjusted value to binary16 under roundTiesToEven then produces the correct value.</p>\n        </emu-note>\n      ","3.2.19":"<p>Given zero or more arguments, this function returns the square root of the sum of squares of its arguments.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>coerced</var> be a new empty List.</li><li>For each element <var>arg</var> of <var>args</var>, do<ol><li>Let <var>n</var> be ? ToNumber(<var>arg</var>).</li><li>Append <var>n</var> to <var>coerced</var>.</li></ol></li><li>For each element <var>number</var> of <var>coerced</var>, do<ol><li>If <var>number</var> is either <b>+∞</b><sub>𝔽</sub> or <b>-∞</b><sub>𝔽</sub>, return <b>+∞</b><sub>𝔽</sub>.</li></ol></li><li>Let <var>onlyZero</var> be <b>true</b>.</li><li>For each element <var>number</var> of <var>coerced</var>, do<ol><li>If <var>number</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>If <var>number</var> is neither <b>+0</b><sub>𝔽</sub> nor <b>-0</b><sub>𝔽</sub>, set <var>onlyZero</var> to <b>false</b>.</li></ol></li><li>If <var>onlyZero</var> is <b>true</b>, return <b>+0</b><sub>𝔽</sub>.</li><li>Return an implementation-approximated Number value representing the square root of the sum of squares of the mathematical values of the elements of <var>coerced</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this function is <b>2</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>Implementations should take care to avoid the loss of precision from overflows and underflows that are prone to occur in naive implementations when this function is called with two or more arguments.</p>\n        </emu-note>\n      ","3.2.20":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>a</var> be ℝ(? ToUint32(<var>x</var>)).</li><li>Let <var>b</var> be ℝ(? ToUint32(<var>y</var>)).</li><li>Let <var>product</var> be (<var>a</var> × <var>b</var>) modulo 2<sup>32</sup>.</li><li>If <var>product</var> ≥ 2<sup>31</sup>, return 𝔽(<var>product</var> - 2<sup>32</sup>).</li><li>Return 𝔽(<var>product</var>).</li></ol></emu-alg>\n      ","3.2.21":"<p>This function returns the natural logarithm of <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is either <b>NaN</b> or <b>+∞</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> is <b>1</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>If <var>n</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return <b>-∞</b><sub>𝔽</sub>.</li><li>If <var>n</var> &lt; <b>-0</b><sub>𝔽</sub>, return <b>NaN</b>.</li><li>Return an implementation-approximated Number value representing ln(ℝ(<var>n</var>)).</li></ol></emu-alg>\n      ","3.2.22":"<p>This function returns the natural logarithm of 1 + <var>x</var>. The result is computed in a way that is accurate even when the value of x is close to zero.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is one of <b>NaN</b>, <b>+0</b><sub>𝔽</sub>, <b>-0</b><sub>𝔽</sub>, or <b>+∞</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> is <b>-1</b><sub>𝔽</sub>, return <b>-∞</b><sub>𝔽</sub>.</li><li>If <var>n</var> &lt; <b>-1</b><sub>𝔽</sub>, return <b>NaN</b>.</li><li>Return an implementation-approximated Number value representing ln(1 + ℝ(<var>n</var>)).</li></ol></emu-alg>\n      ","3.2.23":"<p>This function returns the base 10 logarithm of <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is either <b>NaN</b> or <b>+∞</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> is <b>1</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>If <var>n</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return <b>-∞</b><sub>𝔽</sub>.</li><li>If <var>n</var> &lt; <b>-0</b><sub>𝔽</sub>, return <b>NaN</b>.</li><li>Return an implementation-approximated Number value representing log10(ℝ(<var>n</var>)).</li></ol></emu-alg>\n      ","3.2.24":"<p>This function returns the base 2 logarithm of <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is either <b>NaN</b> or <b>+∞</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> is <b>1</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>If <var>n</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return <b>-∞</b><sub>𝔽</sub>.</li><li>If <var>n</var> &lt; <b>-0</b><sub>𝔽</sub>, return <b>NaN</b>.</li><li>Return an implementation-approximated Number value representing log2(ℝ(<var>n</var>)).</li></ol></emu-alg>\n      ","3.2.25":"<p>Given zero or more arguments, this function calls ToNumber on each of the arguments and returns the largest of the resulting values.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>coerced</var> be a new empty List.</li><li>For each element <var>arg</var> of <var>args</var>, do<ol><li>Let <var>n</var> be ? ToNumber(<var>arg</var>).</li><li>Append <var>n</var> to <var>coerced</var>.</li></ol></li><li>Let <var>highest</var> be <b>-∞</b><sub>𝔽</sub>.</li><li>For each element <var>number</var> of <var>coerced</var>, do<ol><li>If <var>number</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>If <var>number</var> is <b>+0</b><sub>𝔽</sub> and <var>highest</var> is <b>-0</b><sub>𝔽</sub>, set <var>highest</var> to <b>+0</b><sub>𝔽</sub>.</li><li>If <var>number</var> > <var>highest</var>, set <var>highest</var> to <var>number</var>.</li></ol></li><li>Return <var>highest</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The comparison of values to determine the largest value is done using the IsLessThan algorithm except that <b>+0</b><sub>𝔽</sub> is considered to be larger than <b>-0</b><sub>𝔽</sub>.</p>\n        </emu-note>\n        <p>The <b>\"length\"</b> property of this function is <b>2</b><sub>𝔽</sub>.</p>\n      ","3.2.26":"<p>Given zero or more arguments, this function calls ToNumber on each of the arguments and returns the smallest of the resulting values.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>coerced</var> be a new empty List.</li><li>For each element <var>arg</var> of <var>args</var>, do<ol><li>Let <var>n</var> be ? ToNumber(<var>arg</var>).</li><li>Append <var>n</var> to <var>coerced</var>.</li></ol></li><li>Let <var>lowest</var> be <b>+∞</b><sub>𝔽</sub>.</li><li>For each element <var>number</var> of <var>coerced</var>, do<ol><li>If <var>number</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>If <var>number</var> is <b>-0</b><sub>𝔽</sub> and <var>lowest</var> is <b>+0</b><sub>𝔽</sub>, set <var>lowest</var> to <b>-0</b><sub>𝔽</sub>.</li><li>If <var>number</var> &lt; <var>lowest</var>, set <var>lowest</var> to <var>number</var>.</li></ol></li><li>Return <var>lowest</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The comparison of values to determine the smallest value is done using the IsLessThan algorithm except that <b>+0</b><sub>𝔽</sub> is considered to be larger than <b>-0</b><sub>𝔽</sub>.</p>\n        </emu-note>\n        <p>The <b>\"length\"</b> property of this function is <b>2</b><sub>𝔽</sub>.</p>\n      ","3.2.27":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Set <var>base</var> to ? ToNumber(<var>base</var>).</li><li>Set <var>exponent</var> to ? ToNumber(<var>exponent</var>).</li><li>Return Number::exponentiate(<var>base</var>, <var>exponent</var>).</li></ol></emu-alg>\n      ","3.2.28":"<p>This function returns a Number value with positive sign, greater than or equal to <b>+0</b><sub>𝔽</sub> but strictly less than <b>1</b><sub>𝔽</sub>, chosen randomly or pseudo randomly with approximately uniform distribution over that range, using an implementation-defined algorithm or strategy.</p>\n        <p>Each <code>Math.random</code> function created for distinct realms must produce a distinct sequence of values from successive calls.</p>\n      ","3.2.29":"<p>This function returns the Number value that is closest to <var>x</var> and is integral. If two integral Numbers are equally close to <var>x</var>, then the result is the Number value that is closer to +∞. If <var>x</var> is already integral, the result is <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is not finite or <var>n</var> is an integral Number, return <var>n</var>.</li><li>If <var>n</var> &lt; <b>0.5</b><sub>𝔽</sub> and <var>n</var> > <b>+0</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>If <var>n</var> &lt; <b>-0</b><sub>𝔽</sub> and <var>n</var> ≥ <b>-0.5</b><sub>𝔽</sub>, return <b>-0</b><sub>𝔽</sub>.</li><li>Return the integral Number closest to <var>n</var>, preferring the Number closer to +∞ in the case of a tie.</li></ol></emu-alg>\n        <emu-note>\n          <p><code>Math.round(3.5)</code> returns 4, but <code>Math.round(-3.5)</code> returns -3.</p>\n        </emu-note>\n        <emu-note>\n          <p>The value of <code>Math.round(x)</code> is not always the same as the value of <code>Math.floor(x + 0.5)</code>. When <code>x</code> is <b>-0</b><sub>𝔽</sub> or <code>x</code> is less than <b>-0</b><sub>𝔽</sub> but greater than or equal to <b>-0.5</b><sub>𝔽</sub>, <code>Math.round(x)</code> returns <b>-0</b><sub>𝔽</sub>, but <code>Math.floor(x + 0.5)</code> returns <b>+0</b><sub>𝔽</sub>. <code>Math.round(x)</code> may also differ from the value of <code>Math.floor(x + 0.5)</code>because of internal rounding when computing <code>x + 0.5</code>.</p>\n        </emu-note>\n      ","3.2.30":"<p>This function returns the sign of <var>x</var>, indicating whether <var>x</var> is positive, negative, or zero.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is one of <b>NaN</b>, <b>+0</b><sub>𝔽</sub>, or <b>-0</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> &lt; <b>-0</b><sub>𝔽</sub>, return <b>-1</b><sub>𝔽</sub>.</li><li>Return <b>1</b><sub>𝔽</sub>.</li></ol></emu-alg>\n      ","3.2.31":"<p>This function returns the sine of <var>x</var>. The argument is expressed in radians.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is one of <b>NaN</b>, <b>+0</b><sub>𝔽</sub>, or <b>-0</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> is either <b>+∞</b><sub>𝔽</sub> or <b>-∞</b><sub>𝔽</sub>, return <b>NaN</b>.</li><li>Return an implementation-approximated Number value representing the sine of ℝ(<var>n</var>).</li></ol></emu-alg>\n      ","3.2.32":"<p>This function returns the hyperbolic sine of <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is not finite or <var>n</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return <var>n</var>.</li><li>Return an implementation-approximated Number value representing the hyperbolic sine of ℝ(<var>n</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>The value of <code>Math.sinh(x)</code> is the same as the value of <code>(Math.exp(x) - Math.exp(-x)) / 2</code>.</p>\n        </emu-note>\n      ","3.2.33":"<p>This function returns the square root of <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is one of <b>NaN</b>, <b>+0</b><sub>𝔽</sub>, <b>-0</b><sub>𝔽</sub>, or <b>+∞</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> &lt; <b>-0</b><sub>𝔽</sub>, return <b>NaN</b>.</li><li>Return 𝔽(the square root of ℝ(<var>n</var>)).</li></ol></emu-alg>\n      ","3.2.34":"<p>Given an iterable of Numbers, this function sums each value in the iterable and returns their sum. If any value is not a Number it throws a <b>TypeError</b> exception.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Perform ? RequireObjectCoercible(<var>items</var>).</li><li>Let <var>iteratorRecord</var> be ? GetIterator(<var>items</var>, <emu-const>sync</emu-const>).</li><li>Let <var>state</var> be <emu-const>minus-zero</emu-const>.</li><li>Let <var>sum</var> be 0.</li><li>Let <var>count</var> be 0.</li><li>Let <var>next</var> be <emu-const>not-started</emu-const>.</li><li>Repeat, while <var>next</var> is not <emu-const>done</emu-const>,<ol><li>Set <var>next</var> to ? IteratorStepValue(<var>iteratorRecord</var>).</li><li>If <var>next</var> is not <emu-const>done</emu-const>, then<ol><li>If <var>count</var> ≥ 2<sup>53</sup> - 1, then<ol><li>NOTE: This step is not expected to be reached in practice and is included only so that implementations may rely on inputs being \"reasonably sized\" without violating this specification.</li><li>Let <var>error</var> be ThrowCompletion(a newly created <b>RangeError</b> object).</li><li>Return ? IteratorClose(<var>iteratorRecord</var>, <var>error</var>).</li></ol></li><li>If <var>next</var> is not a Number, then<ol><li>Let <var>error</var> be ThrowCompletion(a newly created <b>TypeError</b> object).</li><li>Return ? IteratorClose(<var>iteratorRecord</var>, <var>error</var>).</li></ol></li><li>Let <var>n</var> be <var>next</var>.</li><li>If <var>state</var> is not <emu-const>not-a-number</emu-const>, then<ol><li>If <var>n</var> is <b>NaN</b>, then<ol><li>Set <var>state</var> to <emu-const>not-a-number</emu-const>.</li></ol></li><li>Else if <var>n</var> is <b>+∞</b><sub>𝔽</sub>, then<ol><li>If <var>state</var> is <emu-const>minus-infinity</emu-const>, set <var>state</var> to <emu-const>not-a-number</emu-const>.</li><li>Else, set <var>state</var> to <emu-const>plus-infinity</emu-const>.</li></ol></li><li>Else if <var>n</var> is <b>-∞</b><sub>𝔽</sub>, then<ol><li>If <var>state</var> is <emu-const>plus-infinity</emu-const>, set <var>state</var> to <emu-const>not-a-number</emu-const>.</li><li>Else, set <var>state</var> to <emu-const>minus-infinity</emu-const>.</li></ol></li><li>Else if <var>n</var> is not <b>-0</b><sub>𝔽</sub> and <var>state</var> is either <emu-const>minus-zero</emu-const> or <emu-const>finite</emu-const>, then<ol><li>Set <var>state</var> to <emu-const>finite</emu-const>.</li><li>Set <var>sum</var> to <var>sum</var> + ℝ(<var>n</var>).</li></ol></li></ol></li><li>Set <var>count</var> to <var>count</var> + 1.</li></ol></li></ol></li><li>If <var>state</var> is <emu-const>not-a-number</emu-const>, return <b>NaN</b>.</li><li>If <var>state</var> is <emu-const>plus-infinity</emu-const>, return <b>+∞</b><sub>𝔽</sub>.</li><li>If <var>state</var> is <emu-const>minus-infinity</emu-const>, return <b>-∞</b><sub>𝔽</sub>.</li><li>If <var>state</var> is <emu-const>minus-zero</emu-const>, return <b>-0</b><sub>𝔽</sub>.</li><li>Return 𝔽(<var>sum</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>The value of <var>sum</var> can be computed without arbitrary-precision arithmetic by a variety of algorithms. One such is the \"Grow-Expansion\" algorithm given in <i>Adaptive Precision Floating-Point Arithmetic and Fast Robust Geometric Predicates</i> by Jonathan Richard Shewchuk. A more recent algorithm is given in \"<a href=\"https://arxiv.org/abs/1505.05571\">Fast exact summation using small and large superaccumulators</a>\", code for which is available at <a href=\"https://gitlab.com/radfordneal/xsum\">https://gitlab.com/radfordneal/xsum</a>.</p>\n        </emu-note>\n      ","3.2.35":"<p>This function returns the tangent of <var>x</var>. The argument is expressed in radians.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is one of <b>NaN</b>, <b>+0</b><sub>𝔽</sub>, or <b>-0</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> is either <b>+∞</b><sub>𝔽</sub> or <b>-∞</b><sub>𝔽</sub>, return <b>NaN</b>.</li><li>Return an implementation-approximated Number value representing the tangent of ℝ(<var>n</var>).</li></ol></emu-alg>\n      ","3.2.36":"<p>This function returns the hyperbolic tangent of <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is one of <b>NaN</b>, <b>+0</b><sub>𝔽</sub>, or <b>-0</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> is <b>+∞</b><sub>𝔽</sub>, return <b>1</b><sub>𝔽</sub>.</li><li>If <var>n</var> is <b>-∞</b><sub>𝔽</sub>, return <b>-1</b><sub>𝔽</sub>.</li><li>Return an implementation-approximated Number value representing the hyperbolic tangent of ℝ(<var>n</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>The value of <code>Math.tanh(x)</code> is the same as the value of <code>(Math.exp(x) - Math.exp(-x)) / (Math.exp(x) + Math.exp(-x))</code>.</p>\n        </emu-note>\n      ","3.2.37":"<p>This function returns the integral part of the number <var>x</var>, removing any fractional digits. If <var>x</var> is already integral, the result is <var>x</var>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>n</var> be ? ToNumber(<var>x</var>).</li><li>If <var>n</var> is not finite or <var>n</var> is either <b>+0</b><sub>𝔽</sub> or <b>-0</b><sub>𝔽</sub>, return <var>n</var>.</li><li>If <var>n</var> &lt; <b>1</b><sub>𝔽</sub> and <var>n</var> > <b>+0</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>If <var>n</var> &lt; <b>-0</b><sub>𝔽</sub> and <var>n</var> > <b>-1</b><sub>𝔽</sub>, return <b>-0</b><sub>𝔽</sub>.</li><li>Return the integral Number nearest <var>n</var> in the direction of <b>+0</b><sub>𝔽</sub>.</li></ol></emu-alg>\n      ","4.1":"<p>The following abstract operations operate on time values (defined in <a href=\"/numbers-and-dates#sec-time-values-and-time-range\">21.4.1.1</a>). Note that, in every case, if any argument to one of these functions is <b>NaN</b>, the result will be <b>NaN</b>.</p>\n\n      ","4.1.1":"<p>Time measurement in ECMAScript is analogous to time measurement in POSIX, in particular sharing definition in terms of the proleptic Gregorian calendar, an <dfn id=\"epoch\">epoch</dfn> of midnight at the beginning of 1 January 1970 UTC, and an accounting of every day as comprising exactly 86,400 seconds (each of which is 1000 milliseconds long).</p>\n        <p>An ECMAScript <dfn variants=\"time values\">time value</dfn> is a Number, either a finite integral Number representing an instant in time to millisecond precision or <b>NaN</b> representing no specific instant. A time value that is a multiple of <emu-eqn>24 × 60 × 60 × 1000 = 86,400,000</emu-eqn> (i.e., is 86,400,000 × <var>d</var> for some integer <var>d</var>) represents the instant at the start of the UTC day that follows the epoch by <var>d</var> whole UTC days (preceding the epoch for negative <var>d</var>). Every other finite time value <var>t</var> is defined relative to the greatest preceding time value <var>s</var> that is such a multiple, and represents the instant that occurs within the same UTC day as <var>s</var> but follows it by (<var>t</var> - <var>s</var>) milliseconds.</p>\n        <p>Time values do not account for UTC leap seconds—there are no time values representing instants within positive leap seconds, and there are time values representing instants removed from the UTC timeline by negative leap seconds. However, the definition of time values nonetheless yields piecewise alignment with UTC, with discontinuities only at leap second boundaries and zero difference outside of leap seconds.</p>\n        <p>A Number can exactly represent all integers from -9,007,199,254,740,992 to 9,007,199,254,740,992 (<a href=\"/numbers-and-dates#sec-number.min_safe_integer\">21.1.2.8</a> and <a href=\"/numbers-and-dates#sec-number.max_safe_integer\">21.1.2.6</a>). A time value supports a slightly smaller range of -8,640,000,000,000,000 to 8,640,000,000,000,000 milliseconds. This yields a supported time value range of exactly -100,000,000 days to 100,000,000 days relative to midnight at the beginning of 1 January 1970 UTC.</p>\n        <p>The exact moment of midnight at the beginning of 1 January 1970 UTC is represented by the time value <b>+0</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>In the proleptic Gregorian calendar, leap years are precisely those which are both divisible by 4 and either divisible by 400 or not divisible by 100.</p>\n          <p>The 400 year cycle of the proleptic Gregorian calendar contains 97 leap years. This yields an average of 365.2425 days per year, which is 31,556,952,000 milliseconds. Therefore, the maximum range a Number could represent exactly with millisecond precision is approximately -285,426 to 285,426 years relative to 1970. The smaller range supported by a time value as specified in this section is approximately -273,790 to 273,790 years relative to 1970.</p>\n        </emu-note>\n      ","4.1.2":"<p>These constants are referenced by algorithms in the following sections.</p>\n        <emu-eqn id=\"eqn-HoursPerDay\" aoid=\"HoursPerDay\">HoursPerDay = 24</emu-eqn>\n        <emu-eqn id=\"eqn-MinutesPerHour\" aoid=\"MinutesPerHour\">MinutesPerHour = 60</emu-eqn>\n        <emu-eqn id=\"eqn-SecondsPerMinute\" aoid=\"SecondsPerMinute\">SecondsPerMinute = 60</emu-eqn>\n        <emu-eqn id=\"eqn-msPerSecond\" aoid=\"msPerSecond\">msPerSecond = <b>1000</b><sub>𝔽</sub></emu-eqn>\n        <emu-eqn id=\"eqn-msPerMinute\" aoid=\"msPerMinute\">msPerMinute = <b>60000</b><sub>𝔽</sub> = msPerSecond × 𝔽(SecondsPerMinute)</emu-eqn>\n        <emu-eqn id=\"eqn-msPerHour\" aoid=\"msPerHour\">msPerHour = <b>3600000</b><sub>𝔽</sub> = msPerMinute × 𝔽(MinutesPerHour)</emu-eqn>\n        <emu-eqn id=\"eqn-msPerDay\" aoid=\"msPerDay\">msPerDay = <b>86400000</b><sub>𝔽</sub> = msPerHour × 𝔽(HoursPerDay)</emu-eqn>\n      ","4.1.3":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the day number of the day in which <var>t</var> falls.</dd>\n        </dl>\n        <emu-alg><ol><li>Return 𝔽(floor(ℝ(<var>t</var> / msPerDay))).</li></ol></emu-alg>\n      ","4.1.4":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the number of milliseconds since the start of the day in which <var>t</var> falls.</dd>\n        </dl>\n        <emu-alg><ol><li>Return 𝔽(ℝ(<var>t</var>) modulo ℝ(msPerDay)).</li></ol></emu-alg>\n      ","4.1.5":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the number of days in year <var>y</var>. Leap years have 366 days; all other years have 365.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>ry</var> be ℝ(<var>y</var>).</li><li>If (<var>ry</var> modulo 400) = 0, return <b>366</b><sub>𝔽</sub>.</li><li>If (<var>ry</var> modulo 100) = 0, return <b>365</b><sub>𝔽</sub>.</li><li>If (<var>ry</var> modulo 4) = 0, return <b>366</b><sub>𝔽</sub>.</li><li>Return <b>365</b><sub>𝔽</sub>.</li></ol></emu-alg>\n      ","4.1.6":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the day number of the first day of year <var>y</var>.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>ry</var> be ℝ(<var>y</var>).</li><li>NOTE: In the following steps, <var>numYears1</var>, <var>numYears4</var>, <var>numYears100</var>, and <var>numYears400</var> represent the number of years divisible by 1, 4, 100, and 400, respectively, that occur between the epoch and the start of year <var>y</var>. The number is negative if <var>y</var> is before the epoch.</li><li>Let <var>numYears1</var> be (<var>ry</var> - 1970).</li><li>Let <var>numYears4</var> be floor((<var>ry</var> - 1969) / 4).</li><li>Let <var>numYears100</var> be floor((<var>ry</var> - 1901) / 100).</li><li>Let <var>numYears400</var> be floor((<var>ry</var> - 1601) / 400).</li><li>Return 𝔽(365 × <var>numYears1</var> + <var>numYears4</var> - <var>numYears100</var> + <var>numYears400</var>).</li></ol></emu-alg>\n      ","4.1.7":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the time value of the start of year <var>y</var>.</dd>\n        </dl>\n        <emu-alg><ol><li>Return msPerDay × DayFromYear(<var>y</var>).</li></ol></emu-alg>\n      ","4.1.8":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the year in which <var>t</var> falls.</dd>\n        </dl>\n        <emu-alg><ol><li>Return the largest integral Number <var>y</var> (closest to +∞) such that TimeFromYear(<var>y</var>) ≤ <var>t</var>.</li></ol></emu-alg>\n      ","4.1.9":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Return Day(<var>t</var>) - DayFromYear(YearFromTime(<var>t</var>)).</li></ol></emu-alg>\n      ","4.1.10":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns <b>1</b><sub>𝔽</sub> if <var>t</var> is within a leap year and <b>+0</b><sub>𝔽</sub> otherwise.</dd>\n        </dl>\n        <emu-alg><ol><li>If DaysInYear(YearFromTime(<var>t</var>)) is <b>366</b><sub>𝔽</sub>, return <b>1</b><sub>𝔽</sub>.</li><li>Return <b>+0</b><sub>𝔽</sub>.</li></ol></emu-alg>\n      ","4.1.11":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns a Number identifying the month in which <var>t</var> falls. A month value of <b>+0</b><sub>𝔽</sub> specifies January; <b>1</b><sub>𝔽</sub> specifies February; <b>2</b><sub>𝔽</sub> specifies March; <b>3</b><sub>𝔽</sub> specifies April; <b>4</b><sub>𝔽</sub> specifies May; <b>5</b><sub>𝔽</sub> specifies June; <b>6</b><sub>𝔽</sub> specifies July; <b>7</b><sub>𝔽</sub> specifies August; <b>8</b><sub>𝔽</sub> specifies September; <b>9</b><sub>𝔽</sub> specifies October; <b>10</b><sub>𝔽</sub> specifies November; and <b>11</b><sub>𝔽</sub> specifies December. Note that <emu-eqn>MonthFromTime(<b>+0</b><sub>𝔽</sub>) = <b>+0</b><sub>𝔽</sub></emu-eqn>, corresponding to Thursday, 1 January 1970.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>inLeapYear</var> be InLeapYear(<var>t</var>).</li><li>Let <var>dayWithinYear</var> be DayWithinYear(<var>t</var>).</li><li>If <var>dayWithinYear</var> &lt; <b>31</b><sub>𝔽</sub>, return <b>+0</b><sub>𝔽</sub>.</li><li>If <var>dayWithinYear</var> &lt; <b>59</b><sub>𝔽</sub> + <var>inLeapYear</var>, return <b>1</b><sub>𝔽</sub>.</li><li>If <var>dayWithinYear</var> &lt; <b>90</b><sub>𝔽</sub> + <var>inLeapYear</var>, return <b>2</b><sub>𝔽</sub>.</li><li>If <var>dayWithinYear</var> &lt; <b>120</b><sub>𝔽</sub> + <var>inLeapYear</var>, return <b>3</b><sub>𝔽</sub>.</li><li>If <var>dayWithinYear</var> &lt; <b>151</b><sub>𝔽</sub> + <var>inLeapYear</var>, return <b>4</b><sub>𝔽</sub>.</li><li>If <var>dayWithinYear</var> &lt; <b>181</b><sub>𝔽</sub> + <var>inLeapYear</var>, return <b>5</b><sub>𝔽</sub>.</li><li>If <var>dayWithinYear</var> &lt; <b>212</b><sub>𝔽</sub> + <var>inLeapYear</var>, return <b>6</b><sub>𝔽</sub>.</li><li>If <var>dayWithinYear</var> &lt; <b>243</b><sub>𝔽</sub> + <var>inLeapYear</var>, return <b>7</b><sub>𝔽</sub>.</li><li>If <var>dayWithinYear</var> &lt; <b>273</b><sub>𝔽</sub> + <var>inLeapYear</var>, return <b>8</b><sub>𝔽</sub>.</li><li>If <var>dayWithinYear</var> &lt; <b>304</b><sub>𝔽</sub> + <var>inLeapYear</var>, return <b>9</b><sub>𝔽</sub>.</li><li>If <var>dayWithinYear</var> &lt; <b>334</b><sub>𝔽</sub> + <var>inLeapYear</var>, return <b>10</b><sub>𝔽</sub>.</li><li>Assert: <var>dayWithinYear</var> &lt; <b>365</b><sub>𝔽</sub> + <var>inLeapYear</var>.</li><li>Return <b>11</b><sub>𝔽</sub>.</li></ol></emu-alg>\n      ","4.1.12":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the day of the month in which <var>t</var> falls.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>inLeapYear</var> be InLeapYear(<var>t</var>).</li><li>Let <var>dayWithinYear</var> be DayWithinYear(<var>t</var>).</li><li>Let <var>month</var> be MonthFromTime(<var>t</var>).</li><li>If <var>month</var> is <b>+0</b><sub>𝔽</sub>, return <var>dayWithinYear</var> + <b>1</b><sub>𝔽</sub>.</li><li>If <var>month</var> is <b>1</b><sub>𝔽</sub>, return <var>dayWithinYear</var> - <b>30</b><sub>𝔽</sub>.</li><li>If <var>month</var> is <b>2</b><sub>𝔽</sub>, return <var>dayWithinYear</var> - <b>58</b><sub>𝔽</sub> - <var>inLeapYear</var>.</li><li>If <var>month</var> is <b>3</b><sub>𝔽</sub>, return <var>dayWithinYear</var> - <b>89</b><sub>𝔽</sub> - <var>inLeapYear</var>.</li><li>If <var>month</var> is <b>4</b><sub>𝔽</sub>, return <var>dayWithinYear</var> - <b>119</b><sub>𝔽</sub> - <var>inLeapYear</var>.</li><li>If <var>month</var> is <b>5</b><sub>𝔽</sub>, return <var>dayWithinYear</var> - <b>150</b><sub>𝔽</sub> - <var>inLeapYear</var>.</li><li>If <var>month</var> is <b>6</b><sub>𝔽</sub>, return <var>dayWithinYear</var> - <b>180</b><sub>𝔽</sub> - <var>inLeapYear</var>.</li><li>If <var>month</var> is <b>7</b><sub>𝔽</sub>, return <var>dayWithinYear</var> - <b>211</b><sub>𝔽</sub> - <var>inLeapYear</var>.</li><li>If <var>month</var> is <b>8</b><sub>𝔽</sub>, return <var>dayWithinYear</var> - <b>242</b><sub>𝔽</sub> - <var>inLeapYear</var>.</li><li>If <var>month</var> is <b>9</b><sub>𝔽</sub>, return <var>dayWithinYear</var> - <b>272</b><sub>𝔽</sub> - <var>inLeapYear</var>.</li><li>If <var>month</var> is <b>10</b><sub>𝔽</sub>, return <var>dayWithinYear</var> - <b>303</b><sub>𝔽</sub> - <var>inLeapYear</var>.</li><li>Assert: <var>month</var> is <b>11</b><sub>𝔽</sub>.</li><li>Return <var>dayWithinYear</var> - <b>333</b><sub>𝔽</sub> - <var>inLeapYear</var>.</li></ol></emu-alg>\n      ","4.1.13":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns a Number identifying the day of the week in which <var>t</var> falls. A weekday value of <b>+0</b><sub>𝔽</sub> specifies Sunday; <b>1</b><sub>𝔽</sub> specifies Monday; <b>2</b><sub>𝔽</sub> specifies Tuesday; <b>3</b><sub>𝔽</sub> specifies Wednesday; <b>4</b><sub>𝔽</sub> specifies Thursday; <b>5</b><sub>𝔽</sub> specifies Friday; and <b>6</b><sub>𝔽</sub> specifies Saturday. Note that <emu-eqn>WeekDay(<b>+0</b><sub>𝔽</sub>) = <b>4</b><sub>𝔽</sub></emu-eqn>, corresponding to Thursday, 1 January 1970.</dd>\n        </dl>\n        <emu-alg><ol><li>Return 𝔽(ℝ(Day(<var>t</var>) + <b>4</b><sub>𝔽</sub>) modulo 7).</li></ol></emu-alg>\n      ","4.1.14":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the hour of the day in which <var>t</var> falls.</dd>\n        </dl>\n        <emu-alg><ol><li>Return 𝔽(floor(ℝ(<var>t</var> / msPerHour)) modulo HoursPerDay).</li></ol></emu-alg>\n      ","4.1.15":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the minute of the hour in which <var>t</var> falls.</dd>\n        </dl>\n        <emu-alg><ol><li>Return 𝔽(floor(ℝ(<var>t</var> / msPerMinute)) modulo MinutesPerHour).</li></ol></emu-alg>\n      ","4.1.16":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the second of the minute in which <var>t</var> falls.</dd>\n        </dl>\n        <emu-alg><ol><li>Return 𝔽(floor(ℝ(<var>t</var> / msPerSecond)) modulo SecondsPerMinute).</li></ol></emu-alg>\n      ","4.1.17":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the millisecond of the second in which <var>t</var> falls.</dd>\n        </dl>\n        <emu-alg><ol><li>Return 𝔽(ℝ(<var>t</var>) modulo ℝ(msPerSecond)).</li></ol></emu-alg>\n      ","4.1.18":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>The returned value represents a number of nanoseconds since the epoch that corresponds to the given ISO 8601 calendar date and wall-clock time in UTC.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>date</var> be MakeDay(𝔽(<var>year</var>), 𝔽(<var>month</var> - 1), 𝔽(<var>day</var>)).</li><li>Let <var>time</var> be MakeTime(𝔽(<var>hour</var>), 𝔽(<var>minute</var>), 𝔽(<var>second</var>), 𝔽(<var>millisecond</var>)).</li><li>Let <var>ms</var> be MakeDate(<var>date</var>, <var>time</var>).</li><li>Assert: <var>ms</var> is an integral Number.</li><li>Return ℤ(ℝ(<var>ms</var>) × 10<sup>6</sup> + <var>microsecond</var> × 10<sup>3</sup> + <var>nanosecond</var>).</li></ol></emu-alg>\n      ","4.1.19":"<p>\n          Time zones in ECMAScript are represented by <dfn variants=\"time zone identifier\">time zone identifiers</dfn>, which are Strings composed entirely of code units in the inclusive interval from 0x0000 to 0x007F.\n          Time zones supported by an ECMAScript implementation may be <dfn variants=\"available named time zone\">available named time zones</dfn>, represented by the [[Identifier]] field of the Time Zone Identifier Records returned by AvailableNamedTimeZoneIdentifiers, or <dfn variants=\"offset time zone\">offset time zones</dfn>, represented by Strings for which IsTimeZoneOffsetString returns <b>true</b>.\n        </p>\n        <p>\n          A <dfn variants=\"primary time zone identifiers\">primary time zone identifier</dfn> is the preferred identifier for an available named time zone.\n          A <dfn variants=\"non-primary time zone identifiers\">non-primary time zone identifier</dfn> is an identifier for an available named time zone that is not a primary time zone identifier.\n          An <dfn variants=\"available named time zone identifiers\">available named time zone identifier</dfn> is either a primary time zone identifier or a non-primary time zone identifier.\n          Each available named time zone identifier is associated with exactly one available named time zone.\n          Each available named time zone is associated with exactly one primary time zone identifier and zero or more non-primary time zone identifiers.\n        </p>\n        <p>\n          ECMAScript implementations must support an available named time zone with the identifier <b>\"UTC\"</b>, which must be the primary time zone identifier for the UTC time zone.\n          In addition, implementations may support any number of other available named time zones.\n        </p>\n        <p>\n          Implementations that follow the requirements for time zones as described in the ECMA-402 Internationalization API specification are called <dfn>time zone aware</dfn>.\n          Time zone aware implementations must support available named time zones corresponding to the Zone and Link names of the IANA Time Zone Database, and only such names.\n          In time zone aware implementations, a primary time zone identifier is a Zone name, and a non-primary time zone identifier is a Link name, respectively, in the IANA Time Zone Database except as specifically overridden by AvailableNamedTimeZoneIdentifiers as specified in the ECMA-402 specification.\n          Implementations that do not support the entire IANA Time Zone Database are still recommended to use IANA Time Zone Database names as identifiers to represent time zones.\n        </p>\n      ","4.1.20":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>\n            Each value in the returned List represents a number of nanoseconds since the epoch that corresponds to the given ISO 8601 calendar date and wall-clock time in the named time zone identified by <var>timeZoneIdentifier</var>.\n          </dd>\n        </dl>\n        <p>\n          When the input represents a local time occurring more than once because of a negative time zone transition (e.g. when daylight saving time ends or the time zone offset is decreased due to a time zone rule change), the returned List will have more than one element and will be sorted by ascending numerical value.\n          When the input represents a local time skipped because of a positive time zone transition (e.g. when daylight saving time begins or the time zone offset is increased due to a time zone rule change), the returned List will be empty.\n          Otherwise, the returned List will have one element.\n        </p>\n        <p>The default implementation of GetNamedTimeZoneEpochNanoseconds, to be used for ECMAScript implementations that do not include local political rules for any time zones, performs the following steps when called:</p>\n        <emu-alg><ol><li>Assert: <var>timeZoneIdentifier</var> is <b>\"UTC\"</b>.</li><li>Let <var>epochNanoseconds</var> be GetUTCEpochNanoseconds(<var>year</var>, <var>month</var>, <var>day</var>, <var>hour</var>, <var>minute</var>, <var>second</var>, <var>millisecond</var>, <var>microsecond</var>, <var>nanosecond</var>).</li><li>Return « <var>epochNanoseconds</var> ».</li></ol></emu-alg>\n        <emu-note>\n          <p>It is required for time zone aware implementations (and recommended for all others) to use the time zone information of the IANA Time Zone Database <a href=\"https://www.iana.org/time-zones/\">https://www.iana.org/time-zones/</a>.</p>\n          <p>1:30 AM on 5 November 2017 in America/New_York is repeated twice, so GetNamedTimeZoneEpochNanoseconds(<b>\"America/New_York\"</b>, 2017, 11, 5, 1, 30, 0, 0, 0, 0) would return a List of length 2 in which the first element represents 05:30 UTC (corresponding with 01:30 US Eastern Daylight Time at UTC offset -04:00) and the second element represents 06:30 UTC (corresponding with 01:30 US Eastern Standard Time at UTC offset -05:00).</p>\n          <p>2:30 AM on 12 March 2017 in America/New_York does not exist, so GetNamedTimeZoneEpochNanoseconds(<b>\"America/New_York\"</b>, 2017, 3, 12, 2, 30, 0, 0, 0, 0) would return an empty List.</p>\n        </emu-note>\n      ","4.1.21":"<dl class=\"header\">\n        </dl>\n        <p>The returned integer represents the offset from UTC of the named time zone identified by <var>timeZoneIdentifier</var>, at the instant corresponding with <var>epochNanoseconds</var> relative to the epoch, both in nanoseconds.</p>\n        <p>The default implementation of GetNamedTimeZoneOffsetNanoseconds, to be used for ECMAScript implementations that do not include local political rules for any time zones, performs the following steps when called:</p>\n        <emu-alg><ol><li>Assert: <var>timeZoneIdentifier</var> is <b>\"UTC\"</b>.</li><li>Return 0.</li></ol></emu-alg>\n        <emu-note>\n          <p>Time zone offset values may be positive or negative.</p>\n        </emu-note>\n      ","4.1.22":"<p>A <dfn variants=\"Time Zone Identifier Records\">Time Zone Identifier Record</dfn> is a Record used to describe an available named time zone identifier and its corresponding primary time zone identifier.</p>\n        <p>Time Zone Identifier Records have the fields listed in table-time-zone-identifier-record-fields.</p>\n        <emu-table id=\"table-time-zone-identifier-record-fields\" caption=\"Time Zone Identifier Record Fields\">\n          <table>\n            <thead>\n              <tr>\n                <th>Field Name</th>\n                <th>Value</th>\n                <th>Meaning</th>\n              </tr>\n            </thead>\n            <tr>\n              <td>[[Identifier]]</td>\n              <td>a String</td>\n              <td>An available named time zone identifier that is supported by the implementation.</td>\n            </tr>\n            <tr>\n              <td>[[PrimaryIdentifier]]</td>\n              <td>a String</td>\n              <td>The primary time zone identifier that [[Identifier]] resolves to.</td>\n            </tr>\n          </table>\n        </emu-table>\n        <emu-note>\n          <p>If [[Identifier]] is a primary time zone identifier, then [[Identifier]] is [[PrimaryIdentifier]].</p>\n        </emu-note>\n      ","4.1.23":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>\n            Its result describes all available named time zone identifiers in this implementation, as well as the primary time zone identifier corresponding to each available named time zone identifier.\n            The List is ordered according to the [[Identifier]] field of each Time Zone Identifier Record.\n          </dd>\n        </dl>\n        <p>\n          Time zone aware implementations, including all implementations that implement the ECMA-402 Internationalization API, must implement the AvailableNamedTimeZoneIdentifiers abstract operation as specified in the ECMA-402 specification.\n          For implementations that are not time zone aware, AvailableNamedTimeZoneIdentifiers performs the following steps when called:\n        </p>\n        <emu-alg><ol><li>If the implementation does not include local political rules for any time zones, then<ol><li>Return « the Time Zone Identifier Record { [[Identifier]]: <b>\"UTC\"</b>, [[PrimaryIdentifier]]: <b>\"UTC\"</b> } ».</li></ol></li><li>Let <var>identifiers</var> be the List of unique available named time zone identifiers, sorted according to lexicographic code unit order.</li><li>Let <var>result</var> be a new empty List.</li><li>For each element <var>identifier</var> of <var>identifiers</var>, do<ol><li>Let <var>primary</var> be <var>identifier</var>.</li><li>If <var>identifier</var> is a non-primary time zone identifier in this implementation and <var>identifier</var> is not <b>\"UTC\"</b>, then<ol><li>Set <var>primary</var> to the primary time zone identifier associated with <var>identifier</var>.</li><li>NOTE: An implementation may need to resolve <var>identifier</var> iteratively to obtain the primary time zone identifier.</li></ol></li><li>Let <var>record</var> be the Time Zone Identifier Record { [[Identifier]]: <var>identifier</var>, [[PrimaryIdentifier]]: <var>primary</var> }.</li><li>Append <var>record</var> to <var>result</var>.</li></ol></li><li>Assert: <var>result</var> contains a Time Zone Identifier Record <var>r</var> such that <var>r</var>.[[Identifier]] is <b>\"UTC\"</b> and <var>r</var>.[[PrimaryIdentifier]] is <b>\"UTC\"</b>.</li><li>Return <var>result</var>.</li></ol></emu-alg>\n      ","4.1.24":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>\n            It returns a String representing the host environment's current time zone, which is either a String representing a UTC offset for which IsTimeZoneOffsetString returns <b>true</b>, or a primary time zone identifier.\n          </dd>\n        </dl>\n\n        <emu-alg><ol><li>If the implementation only supports the UTC time zone, return <b>\"UTC\"</b>.</li><li>Let <var>systemTimeZoneString</var> be the String representing the host environment's current time zone, either a primary time zone identifier or an offset time zone identifier.</li><li>Return <var>systemTimeZoneString</var>.</li></ol></emu-alg>\n\n        <emu-note>\n          <p>\n            To ensure the level of functionality that implementations commonly provide in the methods of the Date object, it is recommended that SystemTimeZoneIdentifier return an IANA time zone name corresponding to the host environment's time zone setting, if such a thing exists.\n            GetNamedTimeZoneEpochNanoseconds and GetNamedTimeZoneOffsetNanoseconds must reflect the local political rules for standard time and daylight saving time in that time zone, if such rules exist.\n          </p>\n          <p>For example, if the host environment is a browser on a system where the user has chosen US Eastern Time as their time zone, SystemTimeZoneIdentifier returns <b>\"America/New_York\"</b>.</p>\n        </emu-note>\n      ","4.1.25":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>\n            It converts <var>t</var> from UTC to local time.\n            The local political rules for standard time and daylight saving time in effect at <var>t</var> should be used to determine the result in the way specified in this section.\n          </dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>systemTimeZoneIdentifier</var> be SystemTimeZoneIdentifier().</li><li>If IsTimeZoneOffsetString(<var>systemTimeZoneIdentifier</var>) is <b>true</b>, then<ol><li>Let <var>offsetNs</var> be ParseTimeZoneOffsetString(<var>systemTimeZoneIdentifier</var>).</li></ol></li><li>Else,<ol><li>Let <var>offsetNs</var> be GetNamedTimeZoneOffsetNanoseconds(<var>systemTimeZoneIdentifier</var>, ℤ(ℝ(<var>t</var>) × 10<sup>6</sup>)).</li></ol></li><li>Let <var>offsetMs</var> be truncate(<var>offsetNs</var> / 10<sup>6</sup>).</li><li>Return <var>t</var> + 𝔽(<var>offsetMs</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>If political rules for the local time <var>t</var> are not available within the implementation, the result is <var>t</var> because SystemTimeZoneIdentifier returns <b>\"UTC\"</b> and GetNamedTimeZoneOffsetNanoseconds returns 0.</p>\n        </emu-note>\n        <emu-note>\n          <p>It is required for time zone aware implementations (and recommended for all others) to use the time zone information of the IANA Time Zone Database <a href=\"https://www.iana.org/time-zones/\">https://www.iana.org/time-zones/</a>.</p>\n        </emu-note>\n        <emu-note>\n          <p>Two different input time values <emu-eqn><var>t</var><sub>UTC</sub></emu-eqn> are converted to the same local time <emu-eqn>t<sub>local</sub></emu-eqn> at a negative time zone transition when there are repeated times (e.g. the daylight saving time ends or the time zone adjustment is decreased.).</p>\n          <p><emu-eqn>LocalTime(UTC(<var>t</var><sub>local</sub>))</emu-eqn> is not necessarily always equal to <emu-eqn><var>t</var><sub>local</sub></emu-eqn>. Correspondingly, <emu-eqn>UTC(LocalTime(<var>t</var><sub>UTC</sub>))</emu-eqn> is not necessarily always equal to <emu-eqn><var>t</var><sub>UTC</sub></emu-eqn>.</p>\n        </emu-note>\n      ","4.1.26":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>\n            It converts <var>t</var> from local time to a UTC time value.\n            The local political rules for standard time and daylight saving time in effect at <var>t</var> should be used to determine the result in the way specified in this section.\n          </dd>\n        </dl>\n        <emu-alg><ol><li>If <var>t</var> is not finite, return <b>NaN</b>.</li><li>Let <var>systemTimeZoneIdentifier</var> be SystemTimeZoneIdentifier().</li><li>If IsTimeZoneOffsetString(<var>systemTimeZoneIdentifier</var>) is <b>true</b>, then<ol><li>Let <var>offsetNs</var> be ParseTimeZoneOffsetString(<var>systemTimeZoneIdentifier</var>).</li></ol></li><li>Else,<ol><li>Let <var>possibleInstants</var> be GetNamedTimeZoneEpochNanoseconds(<var>systemTimeZoneIdentifier</var>, ℝ(YearFromTime(<var>t</var>)), ℝ(MonthFromTime(<var>t</var>)) + 1, ℝ(DateFromTime(<var>t</var>)), ℝ(HourFromTime(<var>t</var>)), ℝ(MinFromTime(<var>t</var>)), ℝ(SecFromTime(<var>t</var>)), ℝ(msFromTime(<var>t</var>)), 0, 0).</li><li>NOTE: The following steps ensure that when <var>t</var> represents local time repeating multiple times at a negative time zone transition (e.g. when the daylight saving time ends or the time zone offset is decreased due to a time zone rule change) or skipped local time at a positive time zone transition (e.g. when the daylight saving time starts or the time zone offset is increased due to a time zone rule change), <var>t</var> is interpreted using the time zone offset before the transition.</li><li>If <var>possibleInstants</var> is not empty, then<ol><li>Let <var>disambiguatedInstant</var> be <var>possibleInstants</var>[0].</li></ol></li><li>Else,<ol><li>NOTE: <var>t</var> represents a local time skipped at a positive time zone transition (e.g. due to daylight saving time starting or a time zone rule change increasing the UTC offset).</li><li>Let <var>possibleInstantsBefore</var> be GetNamedTimeZoneEpochNanoseconds(<var>systemTimeZoneIdentifier</var>, ℝ(YearFromTime(<var>tBefore</var>)), ℝ(MonthFromTime(<var>tBefore</var>)) + 1, ℝ(DateFromTime(<var>tBefore</var>)), ℝ(HourFromTime(<var>tBefore</var>)), ℝ(MinFromTime(<var>tBefore</var>)), ℝ(SecFromTime(<var>tBefore</var>)), ℝ(msFromTime(<var>tBefore</var>)), 0, 0), where <var>tBefore</var> is the largest integral Number &lt; <var>t</var> for which <var>possibleInstantsBefore</var> is not empty (i.e., <var>tBefore</var> represents the last local time before the transition).</li><li>Let <var>disambiguatedInstant</var> be the last element of <var>possibleInstantsBefore</var>.</li></ol></li><li>Let <var>offsetNs</var> be GetNamedTimeZoneOffsetNanoseconds(<var>systemTimeZoneIdentifier</var>, <var>disambiguatedInstant</var>).</li></ol></li><li>Let <var>offsetMs</var> be truncate(<var>offsetNs</var> / 10<sup>6</sup>).</li><li>Return <var>t</var> - 𝔽(<var>offsetMs</var>).</li></ol></emu-alg>\n        <p>\n          Input <var>t</var> is nominally a time value but may be any Number value.\n          The algorithm must not limit <var>t</var> to the time value range, so that inputs corresponding with a boundary of the time value range can be supported regardless of local UTC offset.\n          For example, the maximum time value is 8.64 × 10<sup>15</sup>, corresponding with <b>\"+275760-09-13T00:00:00Z\"</b>.\n          In an environment where the local time zone offset is ahead of UTC by 1 hour at that instant, it is represented by the larger input of 8.64 × 10<sup>15</sup> + 3.6 × 10<sup>6</sup>, corresponding with <b>\"+275760-09-13T01:00:00+01:00\"</b>.\n        </p>\n        <p>If political rules for the local time <var>t</var> are not available within the implementation, the result is <var>t</var> because SystemTimeZoneIdentifier returns <b>\"UTC\"</b> and GetNamedTimeZoneOffsetNanoseconds returns 0.</p>\n        <emu-note>\n          <p>It is required for time zone aware implementations (and recommended for all others) to use the time zone information of the IANA Time Zone Database <a href=\"https://www.iana.org/time-zones/\">https://www.iana.org/time-zones/</a>.</p>\n          <p>\n            1:30 AM on 5 November 2017 in America/New_York is repeated twice (fall backward), but it must be interpreted as 1:30 AM UTC-04 instead of 1:30 AM UTC-05.\n            In UTC(TimeClip(MakeDate(MakeDay(2017, 10, 5), MakeTime(1, 30, 0, 0)))), the value of <var>offsetMs</var> is <emu-eqn>-4 × msPerHour</emu-eqn>.\n          </p>\n          <p>\n            2:30 AM on 12 March 2017 in America/New_York does not exist, but it must be interpreted as 2:30 AM UTC-05 (equivalent to 3:30 AM UTC-04).\n            In UTC(TimeClip(MakeDate(MakeDay(2017, 2, 12), MakeTime(2, 30, 0, 0)))), the value of <var>offsetMs</var> is <emu-eqn>-5 × msPerHour</emu-eqn>.\n          </p>\n        </emu-note>\n        <emu-note>\n          <p><emu-eqn>UTC(LocalTime(<var>t</var><sub>UTC</sub>))</emu-eqn> is not necessarily always equal to <emu-eqn><var>t</var><sub>UTC</sub></emu-eqn>. Correspondingly, <emu-eqn>LocalTime(UTC(<var>t</var><sub>local</sub>))</emu-eqn> is not necessarily always equal to <emu-eqn><var>t</var><sub>local</sub></emu-eqn>.</p>\n        </emu-note>\n      ","4.1.27":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It calculates a number of milliseconds.</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>hour</var> is not finite, <var>min</var> is not finite, <var>sec</var> is not finite, or <var>ms</var> is not finite, return <b>NaN</b>.</li><li>Let <var>h</var> be 𝔽(! ToIntegerOrInfinity(<var>hour</var>)).</li><li>Let <var>m</var> be 𝔽(! ToIntegerOrInfinity(<var>min</var>)).</li><li>Let <var>s</var> be 𝔽(! ToIntegerOrInfinity(<var>sec</var>)).</li><li>Let <var>milli</var> be 𝔽(! ToIntegerOrInfinity(<var>ms</var>)).</li><li>Return ((<var>h</var> × msPerHour + <var>m</var> × msPerMinute) + <var>s</var> × msPerSecond) + <var>milli</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The arithmetic in MakeTime is floating-point arithmetic, which is not associative, so the operations must be performed in the correct order.</p>\n        </emu-note>\n      ","4.1.28":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It calculates a number of days.</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>year</var> is not finite, <var>month</var> is not finite, or <var>date</var> is not finite, return <b>NaN</b>.</li><li>Let <var>y</var> be 𝔽(! ToIntegerOrInfinity(<var>year</var>)).</li><li>Let <var>m</var> be 𝔽(! ToIntegerOrInfinity(<var>month</var>)).</li><li>Let <var>dt</var> be 𝔽(! ToIntegerOrInfinity(<var>date</var>)).</li><li>Let <var>ym</var> be <var>y</var> + 𝔽(floor(ℝ(<var>m</var>) / 12)).</li><li>If <var>ym</var> is not finite, return <b>NaN</b>.</li><li>Let <var>mn</var> be 𝔽(ℝ(<var>m</var>) modulo 12).</li><li>Find a finite time value <var>t</var> such that YearFromTime(<var>t</var>) is <var>ym</var>, MonthFromTime(<var>t</var>) is <var>mn</var>, and DateFromTime(<var>t</var>) is <b>1</b><sub>𝔽</sub>; but if this is not possible (because some argument is out of range), return <b>NaN</b>.</li><li>Return Day(<var>t</var>) + <var>dt</var> - <b>1</b><sub>𝔽</sub>.</li></ol></emu-alg>\n      ","4.1.29":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It calculates a number of milliseconds.</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>day</var> is not finite or <var>time</var> is not finite, return <b>NaN</b>.</li><li>Let <var>tv</var> be <var>day</var> × msPerDay + <var>time</var>.</li><li>If <var>tv</var> is not finite, return <b>NaN</b>.</li><li>Return <var>tv</var>.</li></ol></emu-alg>\n      ","4.1.30":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It returns the full year associated with the integer part of <var>year</var>, interpreting any value in the inclusive interval from 0 to 99 as a count of years since the start of 1900. For alignment with the proleptic Gregorian calendar, \"full year\" is defined as the signed count of complete years since the start of year 0 (1 B.C.).</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>year</var> is one of <b>NaN</b>, <b>+∞</b><sub>𝔽</sub>, or <b>-∞</b><sub>𝔽</sub>, return <b>NaN</b>.</li><li>Let <var>truncated</var> be ! ToIntegerOrInfinity(<var>year</var>).</li><li>If <var>truncated</var> is in the inclusive interval from 0 to 99, return <b>1900</b><sub>𝔽</sub> + 𝔽(<var>truncated</var>).</li><li>Return 𝔽(<var>truncated</var>).</li></ol></emu-alg>\n      ","4.1.31":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It calculates a number of milliseconds.</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>time</var> is not finite, return <b>NaN</b>.</li><li>If abs(ℝ(<var>time</var>)) > 8.64 × 10<sup>15</sup>, return <b>NaN</b>.</li><li>Return 𝔽(! ToIntegerOrInfinity(<var>time</var>)).</li></ol></emu-alg>\n      ","4.1.32":"<p>ECMAScript defines a string interchange format for date-times based upon a simplification of the ISO 8601 calendar date extended format. The format is as follows: <code>YYYY-MM-DDTHH:mm:ss.sssZ</code></p>\n        <p>Where the elements are as follows:</p>\n        <figure>\n          <table class=\"lightweight-table\">\n            <tr>\n              <td>\n                <code>YYYY</code>\n              </td>\n              <td>\n                is the year in the proleptic Gregorian calendar as four decimal digits from 0000 to 9999, or as an <a href=\"/numbers-and-dates#sec-expanded-years\">expanded year</a> of <b>\"+\"</b> or <b>\"-\"</b> followed by six decimal digits.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>-</code>\n              </td>\n              <td>\n                <b>\"-\"</b> (hyphen) appears literally twice in the string.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>MM</code>\n              </td>\n              <td>\n                is the month of the year as two decimal digits from 01 (January) to 12 (December).\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>DD</code>\n              </td>\n              <td>\n                is the day of the month as two decimal digits from 01 to 31.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>T</code>\n              </td>\n              <td>\n                <b>\"T\"</b> appears literally in the string, to indicate the beginning of the time element.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>HH</code>\n              </td>\n              <td>\n                is the number of complete hours that have passed since midnight as two decimal digits from 00 to 24.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>:</code>\n              </td>\n              <td>\n                <b>\":\"</b> (colon) appears literally twice in the string.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>mm</code>\n              </td>\n              <td>\n                is the number of complete minutes since the start of the hour as two decimal digits from 00 to 59.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>ss</code>\n              </td>\n              <td>\n                is the number of complete seconds since the start of the minute as two decimal digits from 00 to 59.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>.</code>\n              </td>\n              <td>\n                <b>\".\"</b> (dot) appears literally in the string.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>sss</code>\n              </td>\n              <td>\n                is the number of complete milliseconds since the start of the second as three decimal digits.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                <code>Z</code>\n              </td>\n              <td>\n                is the UTC offset representation specified as <b>\"Z\"</b> (for UTC with no offset) or as either <b>\"+\"</b> or <b>\"-\"</b> followed by a time expression <code>HH:mm</code> (a subset of the <a href=\"/numbers-and-dates#sec-time-zone-offset-strings\">time zone offset string format</a> for indicating local time ahead of or behind UTC, respectively)\n              </td>\n            </tr>\n          </table>\n        </figure>\n        <p>This format includes date-only forms:</p>\n        <pre>\nYYYY\nYYYY-MM\nYYYY-MM-DD\n        </pre>\n        <p>It also includes “date-time” forms that consist of one of the above date-only forms immediately followed by one of the following time forms with an optional UTC offset representation appended:</p>\n        <pre>\nTHH:mm\nTHH:mm:ss\nTHH:mm:ss.sss\n        </pre>\n        <p>A string containing out-of-bounds or nonconforming elements is not a valid instance of this format.</p>\n        <emu-note>\n          <p>As every day both starts and ends with midnight, the two notations <code>00:00</code> and <code>24:00</code> are available to distinguish the two midnights that can be associated with one date. This means that the following two notations refer to exactly the same point in time: <code>1995-02-04T24:00</code> and <code>1995-02-05T00:00</code>. This interpretation of the latter form as \"end of a calendar day\" is consistent with ISO 8601, even though that specification reserves it for describing time intervals and does not permit it within representations of single points in time.</p>\n        </emu-note>\n        <emu-note>\n          <p>There exists no international standard that specifies abbreviations for civil time zones like CET, EST, etc. and sometimes the same abbreviation is even used for two very different time zones. For this reason, both ISO 8601 and this format specify numeric representations of time zone offsets.</p>\n        </emu-note>\n\n        ","4.1.32.1":"<p>Covering the full time value range of approximately 273,790 years forward or backward from 1 January 1970 (<a href=\"/numbers-and-dates#sec-time-values-and-time-range\">21.4.1.1</a>) requires representing years before 0 or after 9999. ISO 8601 permits expansion of the year representation, but only by mutual agreement of the partners in information interchange. In the simplified ECMAScript format, such an expanded year representation shall have 6 digits and is always prefixed with a + or - sign. The year 0 is considered positive and must be prefixed with a + sign. The representation of the year 0 as -000000 is invalid. Strings matching the <a href=\"/numbers-and-dates#sec-date-time-string-format\">Date Time String Format</a> with expanded years representing instants in time outside the range of a time value are treated as unrecognizable by <a href=\"/numbers-and-dates#sec-date.parse\"><code>Date.parse</code></a> and cause that function to return <b>NaN</b> without falling back to implementation-specific behaviour or heuristics.</p>\n          <emu-note>\n            <p>Examples of date-<emu-not-ref>time values</emu-not-ref> with expanded years:</p>\n            <figure>\n              <table class=\"lightweight-table\">\n                <tr>\n                  <td>-271821-04-20T00:00:00Z</td>\n                  <td>271822 B.C.</td>\n                </tr>\n                <tr>\n                  <td>-000001-01-01T00:00:00Z</td>\n                  <td>2 B.C.</td>\n                </tr>\n                <tr>\n                  <td>+000000-01-01T00:00:00Z</td>\n                  <td>1 B.C.</td>\n                </tr>\n                <tr>\n                  <td>+000001-01-01T00:00:00Z</td>\n                  <td>1 A.D.</td>\n                </tr>\n                <tr>\n                  <td>+001970-01-01T00:00:00Z</td>\n                  <td>1970 A.D.</td>\n                </tr>\n                <tr>\n                  <td>+002009-12-15T00:00:00Z</td>\n                  <td>2009 A.D.</td>\n                </tr>\n                <tr>\n                  <td>+275760-09-13T00:00:00Z</td>\n                  <td>275760 A.D.</td>\n                </tr>\n              </table>\n            </figure>\n          </emu-note>\n        ","4.1.33":"<p>\n          ECMAScript defines a string interchange format for UTC offsets, derived from ISO 8601.\n          The format is described by the following grammar.\n        </p>\n\n        <h2>Syntax</h2>\n        <pre class=\"emu-grammar\"><span class=\"nt lhs\">UTCOffset</span> <span class=\"geq\">:::</span>\n  <span class=\"nt\">ASCIISign</span> <span class=\"nt\">Hour</span>\n  <span class=\"nt\">ASCIISign</span> <span class=\"nt\">Hour</span> <span class=\"nt\">HourSubcomponents</span><span class=\"p\">[+Extended]</span>\n  <span class=\"nt\">ASCIISign</span> <span class=\"nt\">Hour</span> <span class=\"nt\">HourSubcomponents</span><span class=\"p\">[~Extended]</span>\n\n<span class=\"nt lhs\">ASCIISign</span> <span class=\"geq\">:::</span> <span class=\"oneof\">one of</span>\n  <span class=\"t\">+</span> <span class=\"t\">-</span>\n\n<span class=\"nt lhs\">Hour</span> <span class=\"geq\">:::</span>\n  <span class=\"t\">0</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"t\">1</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"t\">20</span>\n  <span class=\"t\">21</span>\n  <span class=\"t\">22</span>\n  <span class=\"t\">23</span>\n\n<span class=\"nt lhs\">HourSubcomponents</span><span class=\"p\">[Extended]</span> <span class=\"geq\">:::</span>\n  <span class=\"nt\">TimeSeparator</span><span class=\"p\">[?Extended]</span> <span class=\"nt\">MinuteSecond</span>\n  <span class=\"nt\">TimeSeparator</span><span class=\"p\">[?Extended]</span> <span class=\"nt\">MinuteSecond</span> <span class=\"nt\">TimeSeparator</span><span class=\"p\">[?Extended]</span> <span class=\"nt\">MinuteSecond</span> <span class=\"nt\">TemporalDecimalFraction</span><span class=\"mod\">?</span>\n\n<span class=\"nt lhs\">TimeSeparator</span><span class=\"p\">[Extended]</span> <span class=\"geq\">:::</span>\n  <span class=\"p\">[+Extended]</span> <span class=\"t\">:</span>\n  <span class=\"p\">[~Extended]</span> <span class=\"p\">[empty]</span>\n\n<span class=\"nt lhs\">MinuteSecond</span> <span class=\"geq\">:::</span>\n  <span class=\"t\">0</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"t\">1</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"t\">2</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"t\">3</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"t\">4</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"t\">5</span> <span class=\"nt\">DecimalDigit</span>\n\n<span class=\"nt lhs\">TemporalDecimalFraction</span> <span class=\"geq\">:::</span>\n  <span class=\"nt\">TemporalDecimalSeparator</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"nt\">TemporalDecimalSeparator</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"nt\">TemporalDecimalSeparator</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"nt\">TemporalDecimalSeparator</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"nt\">TemporalDecimalSeparator</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"nt\">TemporalDecimalSeparator</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"nt\">TemporalDecimalSeparator</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"nt\">TemporalDecimalSeparator</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span>\n  <span class=\"nt\">TemporalDecimalSeparator</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span> <span class=\"nt\">DecimalDigit</span>\n\n<span class=\"nt lhs\">TemporalDecimalSeparator</span> <span class=\"geq\">:::</span> <span class=\"oneof\">one of</span>\n  <span class=\"t\">.</span> <span class=\"t\">,</span></pre>\n\n        ","4.1.33.1":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>The return value indicates whether <var>offsetString</var> conforms to the grammar given by <emu-nt>UTCOffset</emu-nt>.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>parseResult</var> be ParseText(<var>offsetString</var>, <emu-nt>UTCOffset</emu-nt>).</li><li>If <var>parseResult</var> is a List of errors, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n        ","4.1.33.2":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>The return value is the UTC offset, as a number of nanoseconds, that corresponds to the String <var>offsetString</var>.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>parseResult</var> be ParseText(<var>offsetString</var>, <emu-nt>UTCOffset</emu-nt>).</li><li>Assert: <var>parseResult</var> is not a List of errors.</li><li>Assert: <var>parseResult</var> contains a <emu-nt>ASCIISign</emu-nt> Parse Node.</li><li>Let <var>parsedSign</var> be the source text matched by the <emu-nt>ASCIISign</emu-nt> Parse Node contained within <var>parseResult</var>.</li><li>If <var>parsedSign</var> is the single code point U+002D (HYPHEN-MINUS), then<ol><li>Let <var>sign</var> be -1.</li></ol></li><li>Else,<ol><li>Let <var>sign</var> be 1.</li></ol></li><li>NOTE: Applications of StringToNumber below do not lose precision, since each of the parsed values is guaranteed to be a sufficiently short string of decimal digits.</li><li>Assert: <var>parseResult</var> contains an <emu-nt>Hour</emu-nt> Parse Node.</li><li>Let <var>parsedHours</var> be the source text matched by the <emu-nt>Hour</emu-nt> Parse Node contained within <var>parseResult</var>.</li><li>Let <var>hours</var> be ℝ(StringToNumber(CodePointsToString(<var>parsedHours</var>))).</li><li>If <var>parseResult</var> does not contain a <emu-nt>MinuteSecond</emu-nt> Parse Node, then<ol><li>Let <var>minutes</var> be 0.</li></ol></li><li>Else,<ol><li>Let <var>parsedMinutes</var> be the source text matched by the first <emu-nt>MinuteSecond</emu-nt> Parse Node contained within <var>parseResult</var>.</li><li>Let <var>minutes</var> be ℝ(StringToNumber(CodePointsToString(<var>parsedMinutes</var>))).</li></ol></li><li>If <var>parseResult</var> does not contain two <emu-nt>MinuteSecond</emu-nt> Parse Nodes, then<ol><li>Let <var>seconds</var> be 0.</li></ol></li><li>Else,<ol><li>Let <var>parsedSeconds</var> be the source text matched by the second <emu-nt>MinuteSecond</emu-nt> Parse Node contained within <var>parseResult</var>.</li><li>Let <var>seconds</var> be ℝ(StringToNumber(CodePointsToString(<var>parsedSeconds</var>))).</li></ol></li><li>If <var>parseResult</var> does not contain a <emu-nt>TemporalDecimalFraction</emu-nt> Parse Node, then<ol><li>Let <var>nanoseconds</var> be 0.</li></ol></li><li>Else,<ol><li>Let <var>parsedFraction</var> be the source text matched by the <emu-nt>TemporalDecimalFraction</emu-nt> Parse Node contained within <var>parseResult</var>.</li><li>Let <var>fraction</var> be the string-concatenation of CodePointsToString(<var>parsedFraction</var>) and <b>\"000000000\"</b>.</li><li>Let <var>nanosecondsString</var> be the substring of <var>fraction</var> from 1 to 10.</li><li>Let <var>nanoseconds</var> be ℝ(StringToNumber(<var>nanosecondsString</var>)).</li></ol></li><li>Return <var>sign</var> × (((<var>hours</var> × 60 + <var>minutes</var>) × 60 + <var>seconds</var>) × 10<sup>9</sup> + <var>nanoseconds</var>).</li></ol></emu-alg>\n        ","4.2":"<p>The Date constructor:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%Date%</emu-intrinsic></dfn>.</li>\n        <li>is the initial value of the <b>\"Date\"</b> property of the global object.</li>\n        <li>creates and initializes a new Date when called as a constructor.</li>\n        <li>returns a String representing the current time (UTC) when called as a function rather than as a constructor.</li>\n        <li>is a function whose behaviour differs based upon the number and types of its arguments.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified Date behaviour must include a <code>super</code> call to the Date constructor to create and initialize the subclass instance with a [[DateValue]] internal slot.</li>\n      </ul>\n\n      ","4.2.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is <b>undefined</b>, then<ol><li>Let <var>now</var> be the time value (UTC) identifying the current time.</li><li>Return ToDateString(<var>now</var>).</li></ol></li><li>Let <var>numberOfArgs</var> be the number of elements in <var>values</var>.</li><li>If <var>numberOfArgs</var> = 0, then<ol><li>Let <var>dv</var> be the time value (UTC) identifying the current time.</li></ol></li><li>Else if <var>numberOfArgs</var> = 1, then<ol><li>Let <var>value</var> be <var>values</var>[0].</li><li>If <var>value</var> is an Object and <var>value</var> has a [[DateValue]] internal slot, then<ol><li>Let <var>tv</var> be <var>value</var>.[[DateValue]].</li></ol></li><li>Else,<ol><li>Let <var>v</var> be ? ToPrimitive(<var>value</var>).</li><li>If <var>v</var> is a String, then<ol><li>Assert: The next step never returns an abrupt completion because <var>v</var> is a String.</li><li>Let <var>tv</var> be the result of parsing <var>v</var> as a date, in exactly the same manner as for the <code>parse</code> method (<a href=\"/numbers-and-dates#sec-date.parse\">21.4.3.2</a>).</li></ol></li><li>Else,<ol><li>Let <var>tv</var> be ? ToNumber(<var>v</var>).</li></ol></li></ol></li><li>Let <var>dv</var> be TimeClip(<var>tv</var>).</li></ol></li><li>Else,<ol><li>Assert: <var>numberOfArgs</var> ≥ 2.</li><li>Let <var>y</var> be ? ToNumber(<var>values</var>[0]).</li><li>Let <var>m</var> be ? ToNumber(<var>values</var>[1]).</li><li>If <var>numberOfArgs</var> > 2, let <var>dt</var> be ? ToNumber(<var>values</var>[2]); else let <var>dt</var> be <b>1</b><sub>𝔽</sub>.</li><li>If <var>numberOfArgs</var> > 3, let <var>h</var> be ? ToNumber(<var>values</var>[3]); else let <var>h</var> be <b>+0</b><sub>𝔽</sub>.</li><li>If <var>numberOfArgs</var> > 4, let <var>min</var> be ? ToNumber(<var>values</var>[4]); else let <var>min</var> be <b>+0</b><sub>𝔽</sub>.</li><li>If <var>numberOfArgs</var> > 5, let <var>s</var> be ? ToNumber(<var>values</var>[5]); else let <var>s</var> be <b>+0</b><sub>𝔽</sub>.</li><li>If <var>numberOfArgs</var> > 6, let <var>milli</var> be ? ToNumber(<var>values</var>[6]); else let <var>milli</var> be <b>+0</b><sub>𝔽</sub>.</li><li>Let <var>yr</var> be MakeFullYear(<var>y</var>).</li><li>Let <var>finalDate</var> be MakeDate(MakeDay(<var>yr</var>, <var>m</var>, <var>dt</var>), MakeTime(<var>h</var>, <var>min</var>, <var>s</var>, <var>milli</var>)).</li><li>Let <var>dv</var> be TimeClip(UTC(<var>finalDate</var>)).</li></ol></li><li>Let <var>obj</var> be ? OrdinaryCreateFromConstructor(NewTarget, <b>\"<emu-intrinsic>%Date.prototype%</emu-intrinsic>\"</b>, « [[DateValue]] »).</li><li>Set <var>obj</var>.[[DateValue]] to <var>dv</var>.</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","4.3":"<p>The Date constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Function.prototype%</emu-intrinsic>.</li>\n        <li>has a <b>\"length\"</b> property whose value is <b>7</b><sub>𝔽</sub>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","4.3.1":"<p>This function returns the time value designating the UTC date and time of the occurrence of the call to it.</p>\n      ","4.3.2":"<p>This function applies the ToString operator to its argument. If ToString results in an abrupt completion the Completion Record is immediately returned. Otherwise, this function interprets the resulting String as a date and time; it returns a Number, the UTC time value corresponding to the date and time. The String may be interpreted as a local time, a UTC time, or a time in some other time zone, depending on the contents of the String. The function first attempts to parse the String according to the format described in Date Time String Format (<a href=\"/numbers-and-dates#sec-date-time-string-format\">21.4.1.32</a>), including expanded years. If the String does not conform to that format the function may fall back to any implementation-specific heuristics or implementation-specific date formats. Strings that are unrecognizable or contain out-of-bounds format element values shall cause this function to return <b>NaN</b>.</p>\n        <p>If the String conforms to the <a href=\"/numbers-and-dates#sec-date-time-string-format\">Date Time String Format</a>, substitute values take the place of absent format elements. When the <code>MM</code> or <code>DD</code> elements are absent, <b>\"01\"</b> is used. When the <code>HH</code>, <code>mm</code>, or <code>ss</code> elements are absent, <b>\"00\"</b> is used. When the <code>sss</code> element is absent, <b>\"000\"</b> is used. When the UTC offset representation is absent, date-only forms are interpreted as a UTC time and date-time forms are interpreted as a local time.</p>\n        <p>If <code>x</code> is any Date whose milliseconds amount is zero within a particular implementation of ECMAScript, then all of the following expressions should produce the same numeric value in that implementation, if all the properties referenced have their initial values:</p>\n        <pre><code class=\"javascript\">\n          x.valueOf()\n          Date.parse(x.toString())\n          Date.parse(x.toUTCString())\n          Date.parse(x.toISOString())\n        </code></pre>\n        <p>However, the expression</p>\n        <pre><code class=\"javascript\">\n          Date.parse(x.toLocaleString())\n        </code></pre>\n        <p>is not required to produce the same Number value as the preceding three expressions and, in general, the value produced by this function is implementation-defined when given any String value that does not conform to the Date Time String Format (<a href=\"/numbers-and-dates#sec-date-time-string-format\">21.4.1.32</a>) and that could not be produced in that implementation by the <code>toString</code> or <code>toUTCString</code> method.</p>\n      ","4.3.3":"<p>The initial value of <code>Date.prototype</code> is the Date prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.3.4":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>y</var> be ? ToNumber(<var>year</var>).</li><li>If <var>month</var> is present, let <var>m</var> be ? ToNumber(<var>month</var>); else let <var>m</var> be <b>+0</b><sub>𝔽</sub>.</li><li>If <var>date</var> is present, let <var>dt</var> be ? ToNumber(<var>date</var>); else let <var>dt</var> be <b>1</b><sub>𝔽</sub>.</li><li>If <var>hours</var> is present, let <var>h</var> be ? ToNumber(<var>hours</var>); else let <var>h</var> be <b>+0</b><sub>𝔽</sub>.</li><li>If <var>minutes</var> is present, let <var>min</var> be ? ToNumber(<var>minutes</var>); else let <var>min</var> be <b>+0</b><sub>𝔽</sub>.</li><li>If <var>seconds</var> is present, let <var>s</var> be ? ToNumber(<var>seconds</var>); else let <var>s</var> be <b>+0</b><sub>𝔽</sub>.</li><li>If <var>ms</var> is present, let <var>milli</var> be ? ToNumber(<var>ms</var>); else let <var>milli</var> be <b>+0</b><sub>𝔽</sub>.</li><li>Let <var>yr</var> be MakeFullYear(<var>y</var>).</li><li>Return TimeClip(MakeDate(MakeDay(<var>yr</var>, <var>m</var>, <var>dt</var>), MakeTime(<var>h</var>, <var>min</var>, <var>s</var>, <var>milli</var>))).</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this function is <b>7</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>This function differs from the Date constructor in two ways: it returns a time value as a Number, rather than creating a Date, and it interprets the arguments in UTC rather than as local time.</p>\n        </emu-note>\n      ","4.4":"<p>The <dfn>Date prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><emu-intrinsic>%Date.prototype%</emu-intrinsic></dfn>.</li>\n        <li>is itself an ordinary object.</li>\n        <li>is not a Date instance and does not have a [[DateValue]] internal slot.</li>\n        <li>has a [[Prototype]] internal slot whose value is <emu-intrinsic>%Object.prototype%</emu-intrinsic>.</li>\n      </ul>\n      <p>Unless explicitly defined otherwise, the methods of the Date prototype object defined below are not generic and the <b>this</b> value passed to them must be an object that has a [[DateValue]] internal slot that has been initialized to a time value.</p>\n\n      ","4.4.1":"<p>The initial value of <code>Date.prototype.constructor</code> is <emu-intrinsic>%Date%</emu-intrinsic>.</p>\n      ","4.4.2":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return DateFromTime(LocalTime(<var>t</var>)).</li></ol></emu-alg>\n      ","4.4.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return WeekDay(LocalTime(<var>t</var>)).</li></ol></emu-alg>\n      ","4.4.4":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return YearFromTime(LocalTime(<var>t</var>)).</li></ol></emu-alg>\n      ","4.4.5":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return HourFromTime(LocalTime(<var>t</var>)).</li></ol></emu-alg>\n      ","4.4.6":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return msFromTime(LocalTime(<var>t</var>)).</li></ol></emu-alg>\n      ","4.4.7":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return MinFromTime(LocalTime(<var>t</var>)).</li></ol></emu-alg>\n      ","4.4.8":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return MonthFromTime(LocalTime(<var>t</var>)).</li></ol></emu-alg>\n      ","4.4.9":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return SecFromTime(LocalTime(<var>t</var>)).</li></ol></emu-alg>\n      ","4.4.10":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Return <var>dateObject</var>.[[DateValue]].</li></ol></emu-alg>\n      ","4.4.11":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return (<var>t</var> - LocalTime(<var>t</var>)) / msPerMinute.</li></ol></emu-alg>\n      ","4.4.12":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return DateFromTime(<var>t</var>).</li></ol></emu-alg>\n      ","4.4.13":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return WeekDay(<var>t</var>).</li></ol></emu-alg>\n      ","4.4.14":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return YearFromTime(<var>t</var>).</li></ol></emu-alg>\n      ","4.4.15":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return HourFromTime(<var>t</var>).</li></ol></emu-alg>\n      ","4.4.16":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return msFromTime(<var>t</var>).</li></ol></emu-alg>\n      ","4.4.17":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return MinFromTime(<var>t</var>).</li></ol></emu-alg>\n      ","4.4.18":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return MonthFromTime(<var>t</var>).</li></ol></emu-alg>\n      ","4.4.19":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Return SecFromTime(<var>t</var>).</li></ol></emu-alg>\n      ","4.4.20":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>Let <var>dt</var> be ? ToNumber(<var>date</var>).</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Set <var>t</var> to LocalTime(<var>t</var>).</li><li>Let <var>newDate</var> be MakeDate(MakeDay(YearFromTime(<var>t</var>), MonthFromTime(<var>t</var>), <var>dt</var>), TimeWithinDay(<var>t</var>)).</li><li>Let <var>u</var> be TimeClip(UTC(<var>newDate</var>)).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>u</var>.</li><li>Return <var>u</var>.</li></ol></emu-alg>\n      ","4.4.21":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>Let <var>y</var> be ? ToNumber(<var>year</var>).</li><li>If <var>t</var> is <b>NaN</b>, set <var>t</var> to <b>+0</b><sub>𝔽</sub>; else set <var>t</var> to LocalTime(<var>t</var>).</li><li>If <var>month</var> is present, let <var>m</var> be ? ToNumber(<var>month</var>); else let <var>m</var> be MonthFromTime(<var>t</var>).</li><li>If <var>date</var> is present, let <var>dt</var> be ? ToNumber(<var>date</var>); else let <var>dt</var> be DateFromTime(<var>t</var>).</li><li>Let <var>newDate</var> be MakeDate(MakeDay(<var>y</var>, <var>m</var>, <var>dt</var>), TimeWithinDay(<var>t</var>)).</li><li>Let <var>u</var> be TimeClip(UTC(<var>newDate</var>)).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>u</var>.</li><li>Return <var>u</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this method is <b>3</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>If <var>month</var> is not present, this method behaves as if <var>month</var> was present with the value <code>getMonth()</code>. If <var>date</var> is not present, it behaves as if <var>date</var> was present with the value <code>getDate()</code>.</p>\n        </emu-note>\n      ","4.4.22":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>Let <var>h</var> be ? ToNumber(<var>hour</var>).</li><li>If <var>min</var> is present, let <var>m</var> be ? ToNumber(<var>min</var>).</li><li>If <var>sec</var> is present, let <var>s</var> be ? ToNumber(<var>sec</var>).</li><li>If <var>ms</var> is present, let <var>milli</var> be ? ToNumber(<var>ms</var>).</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Set <var>t</var> to LocalTime(<var>t</var>).</li><li>If <var>min</var> is not present, let <var>m</var> be MinFromTime(<var>t</var>).</li><li>If <var>sec</var> is not present, let <var>s</var> be SecFromTime(<var>t</var>).</li><li>If <var>ms</var> is not present, let <var>milli</var> be msFromTime(<var>t</var>).</li><li>Let <var>date</var> be MakeDate(Day(<var>t</var>), MakeTime(<var>h</var>, <var>m</var>, <var>s</var>, <var>milli</var>)).</li><li>Let <var>u</var> be TimeClip(UTC(<var>date</var>)).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>u</var>.</li><li>Return <var>u</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this method is <b>4</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>If <var>min</var> is not present, this method behaves as if <var>min</var> was present with the value <code>getMinutes()</code>. If <var>sec</var> is not present, it behaves as if <var>sec</var> was present with the value <code>getSeconds()</code>. If <var>ms</var> is not present, it behaves as if <var>ms</var> was present with the value <code>getMilliseconds()</code>.</p>\n        </emu-note>\n      ","4.4.23":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>Set <var>ms</var> to ? ToNumber(<var>ms</var>).</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Set <var>t</var> to LocalTime(<var>t</var>).</li><li>Let <var>time</var> be MakeTime(HourFromTime(<var>t</var>), MinFromTime(<var>t</var>), SecFromTime(<var>t</var>), <var>ms</var>).</li><li>Let <var>u</var> be TimeClip(UTC(MakeDate(Day(<var>t</var>), <var>time</var>))).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>u</var>.</li><li>Return <var>u</var>.</li></ol></emu-alg>\n      ","4.4.24":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>Let <var>m</var> be ? ToNumber(<var>min</var>).</li><li>If <var>sec</var> is present, let <var>s</var> be ? ToNumber(<var>sec</var>).</li><li>If <var>ms</var> is present, let <var>milli</var> be ? ToNumber(<var>ms</var>).</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Set <var>t</var> to LocalTime(<var>t</var>).</li><li>If <var>sec</var> is not present, let <var>s</var> be SecFromTime(<var>t</var>).</li><li>If <var>ms</var> is not present, let <var>milli</var> be msFromTime(<var>t</var>).</li><li>Let <var>date</var> be MakeDate(Day(<var>t</var>), MakeTime(HourFromTime(<var>t</var>), <var>m</var>, <var>s</var>, <var>milli</var>)).</li><li>Let <var>u</var> be TimeClip(UTC(<var>date</var>)).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>u</var>.</li><li>Return <var>u</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this method is <b>3</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>If <var>sec</var> is not present, this method behaves as if <var>sec</var> was present with the value <code>getSeconds()</code>. If <var>ms</var> is not present, this behaves as if <var>ms</var> was present with the value <code>getMilliseconds()</code>.</p>\n        </emu-note>\n      ","4.4.25":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>Let <var>m</var> be ? ToNumber(<var>month</var>).</li><li>If <var>date</var> is present, let <var>dt</var> be ? ToNumber(<var>date</var>).</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Set <var>t</var> to LocalTime(<var>t</var>).</li><li>If <var>date</var> is not present, let <var>dt</var> be DateFromTime(<var>t</var>).</li><li>Let <var>newDate</var> be MakeDate(MakeDay(YearFromTime(<var>t</var>), <var>m</var>, <var>dt</var>), TimeWithinDay(<var>t</var>)).</li><li>Let <var>u</var> be TimeClip(UTC(<var>newDate</var>)).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>u</var>.</li><li>Return <var>u</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this method is <b>2</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>If <var>date</var> is not present, this method behaves as if <var>date</var> was present with the value <code>getDate()</code>.</p>\n        </emu-note>\n      ","4.4.26":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>Let <var>s</var> be ? ToNumber(<var>sec</var>).</li><li>If <var>ms</var> is present, let <var>milli</var> be ? ToNumber(<var>ms</var>).</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Set <var>t</var> to LocalTime(<var>t</var>).</li><li>If <var>ms</var> is not present, let <var>milli</var> be msFromTime(<var>t</var>).</li><li>Let <var>date</var> be MakeDate(Day(<var>t</var>), MakeTime(HourFromTime(<var>t</var>), MinFromTime(<var>t</var>), <var>s</var>, <var>milli</var>)).</li><li>Let <var>u</var> be TimeClip(UTC(<var>date</var>)).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>u</var>.</li><li>Return <var>u</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this method is <b>2</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>If <var>ms</var> is not present, this method behaves as if <var>ms</var> was present with the value <code>getMilliseconds()</code>.</p>\n        </emu-note>\n      ","4.4.27":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be ? ToNumber(<var>time</var>).</li><li>Let <var>v</var> be TimeClip(<var>t</var>).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>v</var>.</li><li>Return <var>v</var>.</li></ol></emu-alg>\n      ","4.4.28":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>Let <var>dt</var> be ? ToNumber(<var>date</var>).</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Let <var>newDate</var> be MakeDate(MakeDay(YearFromTime(<var>t</var>), MonthFromTime(<var>t</var>), <var>dt</var>), TimeWithinDay(<var>t</var>)).</li><li>Let <var>v</var> be TimeClip(<var>newDate</var>).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>v</var>.</li><li>Return <var>v</var>.</li></ol></emu-alg>\n      ","4.4.29":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>t</var> is <b>NaN</b>, set <var>t</var> to <b>+0</b><sub>𝔽</sub>.</li><li>Let <var>y</var> be ? ToNumber(<var>year</var>).</li><li>If <var>month</var> is present, let <var>m</var> be ? ToNumber(<var>month</var>); else let <var>m</var> be MonthFromTime(<var>t</var>).</li><li>If <var>date</var> is present, let <var>dt</var> be ? ToNumber(<var>date</var>); else let <var>dt</var> be DateFromTime(<var>t</var>).</li><li>Let <var>newDate</var> be MakeDate(MakeDay(<var>y</var>, <var>m</var>, <var>dt</var>), TimeWithinDay(<var>t</var>)).</li><li>Let <var>v</var> be TimeClip(<var>newDate</var>).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>v</var>.</li><li>Return <var>v</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this method is <b>3</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>If <var>month</var> is not present, this method behaves as if <var>month</var> was present with the value <code>getUTCMonth()</code>. If <var>date</var> is not present, it behaves as if <var>date</var> was present with the value <code>getUTCDate()</code>.</p>\n        </emu-note>\n      ","4.4.30":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>Let <var>h</var> be ? ToNumber(<var>hour</var>).</li><li>If <var>min</var> is present, let <var>m</var> be ? ToNumber(<var>min</var>).</li><li>If <var>sec</var> is present, let <var>s</var> be ? ToNumber(<var>sec</var>).</li><li>If <var>ms</var> is present, let <var>milli</var> be ? ToNumber(<var>ms</var>).</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>If <var>min</var> is not present, let <var>m</var> be MinFromTime(<var>t</var>).</li><li>If <var>sec</var> is not present, let <var>s</var> be SecFromTime(<var>t</var>).</li><li>If <var>ms</var> is not present, let <var>milli</var> be msFromTime(<var>t</var>).</li><li>Let <var>date</var> be MakeDate(Day(<var>t</var>), MakeTime(<var>h</var>, <var>m</var>, <var>s</var>, <var>milli</var>)).</li><li>Let <var>v</var> be TimeClip(<var>date</var>).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>v</var>.</li><li>Return <var>v</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this method is <b>4</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>If <var>min</var> is not present, this method behaves as if <var>min</var> was present with the value <code>getUTCMinutes()</code>. If <var>sec</var> is not present, it behaves as if <var>sec</var> was present with the value <code>getUTCSeconds()</code>. If <var>ms</var> is not present, it behaves as if <var>ms</var> was present with the value <code>getUTCMilliseconds()</code>.</p>\n        </emu-note>\n      ","4.4.31":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>Set <var>ms</var> to ? ToNumber(<var>ms</var>).</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>Let <var>time</var> be MakeTime(HourFromTime(<var>t</var>), MinFromTime(<var>t</var>), SecFromTime(<var>t</var>), <var>ms</var>).</li><li>Let <var>v</var> be TimeClip(MakeDate(Day(<var>t</var>), <var>time</var>)).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>v</var>.</li><li>Return <var>v</var>.</li></ol></emu-alg>\n      ","4.4.32":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>Let <var>m</var> be ? ToNumber(<var>min</var>).</li><li>If <var>sec</var> is present, let <var>s</var> be ? ToNumber(<var>sec</var>).</li><li>If <var>ms</var> is present, let <var>milli</var> be ? ToNumber(<var>ms</var>).</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>If <var>sec</var> is not present, let <var>s</var> be SecFromTime(<var>t</var>).</li><li>If <var>ms</var> is not present, let <var>milli</var> be msFromTime(<var>t</var>).</li><li>Let <var>date</var> be MakeDate(Day(<var>t</var>), MakeTime(HourFromTime(<var>t</var>), <var>m</var>, <var>s</var>, <var>milli</var>)).</li><li>Let <var>v</var> be TimeClip(<var>date</var>).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>v</var>.</li><li>Return <var>v</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this method is <b>3</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>If <var>sec</var> is not present, this method behaves as if <var>sec</var> was present with the value <code>getUTCSeconds()</code>. If <var>ms</var> is not present, it behaves as if <var>ms</var> was present with the value return by <code>getUTCMilliseconds()</code>.</p>\n        </emu-note>\n      ","4.4.33":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>Let <var>m</var> be ? ToNumber(<var>month</var>).</li><li>If <var>date</var> is present, let <var>dt</var> be ? ToNumber(<var>date</var>).</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>If <var>date</var> is not present, let <var>dt</var> be DateFromTime(<var>t</var>).</li><li>Let <var>newDate</var> be MakeDate(MakeDay(YearFromTime(<var>t</var>), <var>m</var>, <var>dt</var>), TimeWithinDay(<var>t</var>)).</li><li>Let <var>v</var> be TimeClip(<var>newDate</var>).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>v</var>.</li><li>Return <var>v</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this method is <b>2</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>If <var>date</var> is not present, this method behaves as if <var>date</var> was present with the value <code>getUTCDate()</code>.</p>\n        </emu-note>\n      ","4.4.34":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>t</var> be <var>dateObject</var>.[[DateValue]].</li><li>Let <var>s</var> be ? ToNumber(<var>sec</var>).</li><li>If <var>ms</var> is present, let <var>milli</var> be ? ToNumber(<var>ms</var>).</li><li>If <var>t</var> is <b>NaN</b>, return <b>NaN</b>.</li><li>If <var>ms</var> is not present, let <var>milli</var> be msFromTime(<var>t</var>).</li><li>Let <var>date</var> be MakeDate(Day(<var>t</var>), MakeTime(HourFromTime(<var>t</var>), MinFromTime(<var>t</var>), <var>s</var>, <var>milli</var>)).</li><li>Let <var>v</var> be TimeClip(<var>date</var>).</li><li>Set <var>dateObject</var>.[[DateValue]] to <var>v</var>.</li><li>Return <var>v</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this method is <b>2</b><sub>𝔽</sub>.</p>\n        <emu-note>\n          <p>If <var>ms</var> is not present, this method behaves as if <var>ms</var> was present with the value <code>getUTCMilliseconds()</code>.</p>\n        </emu-note>\n      ","4.4.35":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>tv</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>tv</var> is <b>NaN</b>, return <b>\"Invalid Date\"</b>.</li><li>Let <var>t</var> be LocalTime(<var>tv</var>).</li><li>Return DateString(<var>t</var>).</li></ol></emu-alg>\n      ","4.4.36":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>tv</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>tv</var> is <b>NaN</b>, throw a <b>RangeError</b> exception.</li><li>Assert: <var>tv</var> is an integral Number.</li><li>If <var>tv</var> corresponds with a year that cannot be represented in the <a href=\"/numbers-and-dates#sec-date-time-string-format\">Date Time String Format</a>, throw a <b>RangeError</b> exception.</li><li>Return a String representation of <var>tv</var> in the <a href=\"/numbers-and-dates#sec-date-time-string-format\">Date Time String Format</a> on the UTC time scale, including all format elements and the UTC offset representation <b>\"Z\"</b>.</li></ol></emu-alg>\n      ","4.4.37":"<p>This method provides a String representation of a Date for use by <code>JSON.stringify</code> (<a href=\"/structured-data#sec-json.stringify\">25.5.4</a>).</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be ? ToObject(<b>this</b> value).</li><li>Let <var>tv</var> be ? ToPrimitive(<var>obj</var>, <emu-const>number</emu-const>).</li><li>If <var>tv</var> is a Number and <var>tv</var> is not finite, return <b>null</b>.</li><li>Return ? Invoke(<var>obj</var>, <b>\"toISOString\"</b>).</li></ol></emu-alg>\n        <emu-note>\n          <p>The argument is ignored.</p>\n        </emu-note>\n        <emu-note>\n          <p>This method is intentionally generic; it does not require that its <b>this</b> value be a Date. Therefore, it can be transferred to other kinds of objects for use as a method. However, it does require that any such object have a <code>toISOString</code> method.</p>\n        </emu-note>\n      ","4.4.38":"<p>An ECMAScript implementation that includes the ECMA-402 Internationalization API must implement this method as specified in the ECMA-402 specification. If an ECMAScript implementation does not include the ECMA-402 API the following specification of this method is used:</p>\n        <p>This method returns a String value. The contents of the String are implementation-defined, but are intended to represent the “date” portion of the Date in the current time zone in a convenient, human-readable form that corresponds to the conventions of the host environment's current locale.</p>\n        <p>The meaning of the optional parameters to this method are defined in the ECMA-402 specification; implementations that do not include ECMA-402 support must not use those parameter positions for anything else.</p>\n      ","4.4.39":"<p>An ECMAScript implementation that includes the ECMA-402 Internationalization API must implement this method as specified in the ECMA-402 specification. If an ECMAScript implementation does not include the ECMA-402 API the following specification of this method is used:</p>\n        <p>This method returns a String value. The contents of the String are implementation-defined, but are intended to represent the Date in the current time zone in a convenient, human-readable form that corresponds to the conventions of the host environment's current locale.</p>\n        <p>The meaning of the optional parameters to this method are defined in the ECMA-402 specification; implementations that do not include ECMA-402 support must not use those parameter positions for anything else.</p>\n      ","4.4.40":"<p>An ECMAScript implementation that includes the ECMA-402 Internationalization API must implement this method as specified in the ECMA-402 specification. If an ECMAScript implementation does not include the ECMA-402 API the following specification of this method is used:</p>\n        <p>This method returns a String value. The contents of the String are implementation-defined, but are intended to represent the “time” portion of the Date in the current time zone in a convenient, human-readable form that corresponds to the conventions of the host environment's current locale.</p>\n        <p>The meaning of the optional parameters to this method are defined in the ECMA-402 specification; implementations that do not include ECMA-402 support must not use those parameter positions for anything else.</p>\n      ","4.4.41":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>tv</var> be <var>dateObject</var>.[[DateValue]].</li><li>Return ToDateString(<var>tv</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>For any Date <code>d</code> such that <code>d.[[DateValue]]</code> is evenly divisible by 1000, the result of <code>Date.parse(d.toString())</code> = <code>d.valueOf()</code>. See <a href=\"/numbers-and-dates#sec-date.parse\">21.4.3.2</a>.</p>\n        </emu-note>\n        <emu-note>\n          <p>This method is not generic; it throws a <b>TypeError</b> exception if its <b>this</b> value is not a Date. Therefore, it cannot be transferred to other kinds of objects for use as a method.</p>\n        </emu-note>\n\n        ","4.4.41.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>hour</var> be ToZeroPaddedDecimalString(ℝ(HourFromTime(<var>tv</var>)), 2).</li><li>Let <var>minute</var> be ToZeroPaddedDecimalString(ℝ(MinFromTime(<var>tv</var>)), 2).</li><li>Let <var>second</var> be ToZeroPaddedDecimalString(ℝ(SecFromTime(<var>tv</var>)), 2).</li><li>Return the string-concatenation of <var>hour</var>, <b>\":\"</b>, <var>minute</var>, <b>\":\"</b>, <var>second</var>, the code unit 0x0020 (SPACE), and <b>\"GMT\"</b>.</li></ol></emu-alg>\n        ","4.4.41.2":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>weekday</var> be the Name of the entry in sec-todatestring-day-names with the Number WeekDay(<var>tv</var>).</li><li>Let <var>month</var> be the Name of the entry in sec-todatestring-month-names with the Number MonthFromTime(<var>tv</var>).</li><li>Let <var>day</var> be ToZeroPaddedDecimalString(ℝ(DateFromTime(<var>tv</var>)), 2).</li><li>Let <var>yv</var> be YearFromTime(<var>tv</var>).</li><li>If <var>yv</var> is <b>+0</b><sub>𝔽</sub> or <var>yv</var> > <b>+0</b><sub>𝔽</sub>, let <var>yearSign</var> be the empty String; else let <var>yearSign</var> be <b>\"-\"</b>.</li><li>Let <var>paddedYear</var> be ToZeroPaddedDecimalString(abs(ℝ(<var>yv</var>)), 4).</li><li>Return the string-concatenation of <var>weekday</var>, the code unit 0x0020 (SPACE), <var>month</var>, the code unit 0x0020 (SPACE), <var>day</var>, the code unit 0x0020 (SPACE), <var>yearSign</var>, and <var>paddedYear</var>.</li></ol></emu-alg>\n          <emu-table id=\"sec-todatestring-day-names\" caption=\"Names of days of the week\">\n            <table>\n              <thead>\n                <tr>\n                  <th>\n                    Number\n                  </th>\n                  <th>\n                    Name\n                  </th>\n                </tr>\n              </thead>\n              <tr>\n                <td>\n                  <b>+0</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Sun\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>1</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Mon\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>2</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Tue\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>3</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Wed\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>4</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Thu\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>5</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Fri\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>6</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Sat\"</b>\n                </td>\n              </tr>\n            </table>\n          </emu-table>\n          <emu-table id=\"sec-todatestring-month-names\" caption=\"Names of months of the year\">\n            <table>\n              <thead>\n                <tr>\n                  <th>\n                    Number\n                  </th>\n                  <th>\n                    Name\n                  </th>\n                </tr>\n              </thead>\n              <tr>\n                <td>\n                  <b>+0</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Jan\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>1</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Feb\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>2</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Mar\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>3</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Apr\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>4</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"May\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>5</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Jun\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>6</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Jul\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>7</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Aug\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>8</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Sep\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>9</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Oct\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>10</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Nov\"</b>\n                </td>\n              </tr>\n              <tr>\n                <td>\n                  <b>11</b><sub>𝔽</sub>\n                </td>\n                <td>\n                  <b>\"Dec\"</b>\n                </td>\n              </tr>\n            </table>\n          </emu-table>\n        ","4.4.41.3":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>systemTimeZoneIdentifier</var> be SystemTimeZoneIdentifier().</li><li>If IsTimeZoneOffsetString(<var>systemTimeZoneIdentifier</var>) is <b>true</b>, then<ol><li>Let <var>offsetNs</var> be ParseTimeZoneOffsetString(<var>systemTimeZoneIdentifier</var>).</li></ol></li><li>Else,<ol><li>Let <var>offsetNs</var> be GetNamedTimeZoneOffsetNanoseconds(<var>systemTimeZoneIdentifier</var>, ℤ(ℝ(<var>tv</var>) × 10<sup>6</sup>)).</li></ol></li><li>Let <var>offset</var> be 𝔽(truncate(<var>offsetNs</var> / 10<sup>6</sup>)).</li><li>If <var>offset</var> is <b>+0</b><sub>𝔽</sub> or <var>offset</var> > <b>+0</b><sub>𝔽</sub>, then<ol><li>Let <var>offsetSign</var> be <b>\"+\"</b>.</li><li>Let <var>absOffset</var> be <var>offset</var>.</li></ol></li><li>Else,<ol><li>Let <var>offsetSign</var> be <b>\"-\"</b>.</li><li>Let <var>absOffset</var> be -<var>offset</var>.</li></ol></li><li>Let <var>offsetMin</var> be ToZeroPaddedDecimalString(ℝ(MinFromTime(<var>absOffset</var>)), 2).</li><li>Let <var>offsetHour</var> be ToZeroPaddedDecimalString(ℝ(HourFromTime(<var>absOffset</var>)), 2).</li><li>Let <var>tzName</var> be an implementation-defined string that is either the empty String or the string-concatenation of the code unit 0x0020 (SPACE), the code unit 0x0028 (LEFT PARENTHESIS), an implementation-defined timezone name, and the code unit 0x0029 (RIGHT PARENTHESIS).</li><li>Return the string-concatenation of <var>offsetSign</var>, <var>offsetHour</var>, <var>offsetMin</var>, and <var>tzName</var>.</li></ol></emu-alg>\n        ","4.4.41.4":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>tv</var> is <b>NaN</b>, return <b>\"Invalid Date\"</b>.</li><li>Let <var>t</var> be LocalTime(<var>tv</var>).</li><li>Return the string-concatenation of DateString(<var>t</var>), the code unit 0x0020 (SPACE), TimeString(<var>t</var>), and TimeZoneString(<var>tv</var>).</li></ol></emu-alg>\n        ","4.4.42":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>tv</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>tv</var> is <b>NaN</b>, return <b>\"Invalid Date\"</b>.</li><li>Let <var>t</var> be LocalTime(<var>tv</var>).</li><li>Return the string-concatenation of TimeString(<var>t</var>) and TimeZoneString(<var>tv</var>).</li></ol></emu-alg>\n      ","4.4.43":"<p>This method returns a String value representing the instant in time corresponding to the <b>this</b> value. The format of the String is based upon \"HTTP-date\" from RFC 7231, generalized to support the full range of times supported by ECMAScript Dates.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Let <var>tv</var> be <var>dateObject</var>.[[DateValue]].</li><li>If <var>tv</var> is <b>NaN</b>, return <b>\"Invalid Date\"</b>.</li><li>Let <var>weekday</var> be the Name of the entry in sec-todatestring-day-names with the Number WeekDay(<var>tv</var>).</li><li>Let <var>month</var> be the Name of the entry in sec-todatestring-month-names with the Number MonthFromTime(<var>tv</var>).</li><li>Let <var>day</var> be ToZeroPaddedDecimalString(ℝ(DateFromTime(<var>tv</var>)), 2).</li><li>Let <var>yv</var> be YearFromTime(<var>tv</var>).</li><li>If <var>yv</var> is <b>+0</b><sub>𝔽</sub> or <var>yv</var> > <b>+0</b><sub>𝔽</sub>, let <var>yearSign</var> be the empty String; else let <var>yearSign</var> be <b>\"-\"</b>.</li><li>Let <var>paddedYear</var> be ToZeroPaddedDecimalString(abs(ℝ(<var>yv</var>)), 4).</li><li>Return the string-concatenation of <var>weekday</var>, <b>\",\"</b>, the code unit 0x0020 (SPACE), <var>day</var>, the code unit 0x0020 (SPACE), <var>month</var>, the code unit 0x0020 (SPACE), <var>yearSign</var>, <var>paddedYear</var>, the code unit 0x0020 (SPACE), and TimeString(<var>tv</var>).</li></ol></emu-alg>\n      ","4.4.44":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>dateObject</var> be the <b>this</b> value.</li><li>Perform ? RequireInternalSlot(<var>dateObject</var>, [[DateValue]]).</li><li>Return <var>dateObject</var>.[[DateValue]].</li></ol></emu-alg>\n      ","4.4.45":"<p>This method is called by ECMAScript language operators to convert a Date to a primitive value. The allowed values for <var>hint</var> are <b>\"default\"</b>, <b>\"number\"</b>, and <b>\"string\"</b>. Dates are unique among built-in ECMAScript object in that they treat <b>\"default\"</b> as being equivalent to <b>\"string\"</b>, All other built-in ECMAScript objects treat <b>\"default\"</b> as being equivalent to <b>\"number\"</b>.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>If <var>hint</var> is either <b>\"string\"</b> or <b>\"default\"</b>, then<ol><li>Let <var>tryFirst</var> be <emu-const>string</emu-const>.</li></ol></li><li>Else if <var>hint</var> is <b>\"number\"</b>, then<ol><li>Let <var>tryFirst</var> be <emu-const>number</emu-const>.</li></ol></li><li>Else,<ol><li>Throw a <b>TypeError</b> exception.</li></ol></li><li>Return ? OrdinaryToPrimitive(<var>obj</var>, <var>tryFirst</var>).</li></ol></emu-alg>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n        <p>The value of the <b>\"name\"</b> property of this method is <b>\"[Symbol.toPrimitive]\"</b>.</p>\n      ","4.5":"<p>Date instances are ordinary objects that inherit properties from the Date prototype object. Date instances also have a [[DateValue]] internal slot. The [[DateValue]] internal slot is the time value represented by this Date.</p>\n    "};
const _basePath = process.env.NEXT_PUBLIC_BASE_PATH || '';
const sections = _basePath
  ? Object.fromEntries(
      Object.entries(_sections).map(([k, v]) => [
        k,
        v.replaceAll('href="/', `href="${_basePath}/`),
      ])
    )
  : _sections;
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
