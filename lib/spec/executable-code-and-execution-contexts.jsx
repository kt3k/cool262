// Generated from ecma262/spec.html — do not edit by hand.
const sections = {"1":"<p><dfn variants=\"Environment Records\">Environment Record</dfn> is a specification type used to define the association of <emu-nt>Identifier</emu-nt>s to specific variables and functions, based upon the lexical nesting structure of ECMAScript code. Usually an Environment Record is associated with some specific syntactic structure of ECMAScript code such as a <emu-nt>FunctionDeclaration</emu-nt>, a <emu-nt>BlockStatement</emu-nt>, or a <emu-nt>Catch</emu-nt> clause of a <emu-nt>TryStatement</emu-nt>. Each time such code is evaluated, a new Environment Record is created to record the identifier bindings that are created by that code.</p>\n    <p>Every Environment Record has an [[OuterEnv]] field, which is either <b>null</b> or a reference to an outer Environment Record. This is used to model the logical nesting of Environment Record values. The outer reference of an (inner) Environment Record is a reference to the Environment Record that logically surrounds the inner Environment Record. An outer Environment Record may, of course, have its own outer Environment Record. An Environment Record may serve as the outer environment for multiple inner Environment Records. For example, if a <emu-nt>FunctionDeclaration</emu-nt> contains two nested <emu-nt>FunctionDeclaration</emu-nt>s then the Environment Records of each of the nested functions will have as their outer Environment Record the Environment Record of the current evaluation of the surrounding function.</p>\n    <p>Environment Records are purely specification mechanisms and need not correspond to any specific artefact of an ECMAScript implementation. It is impossible for an ECMAScript program to directly access or manipulate such values.</p>\n\n    ","2":"<p>A <dfn id=\"privateenvironment-record\" variants=\"PrivateEnvironment Records\">PrivateEnvironment Record</dfn> is a specification mechanism used to track Private Names based upon the lexical nesting structure of <emu-nt>ClassDeclaration</emu-nt>s and <emu-nt>ClassExpression</emu-nt>s in ECMAScript code. They are similar to, but distinct from, Environment Records. Each PrivateEnvironment Record is associated with a <emu-nt>ClassDeclaration</emu-nt> or <emu-nt>ClassExpression</emu-nt>. Each time such a class is evaluated, a new PrivateEnvironment Record is created to record the Private Names declared by that class.</p>\n    <p>Each PrivateEnvironment Record has the fields defined in table-privateenvironment-records.</p>\n    <emu-table id=\"table-privateenvironment-records\" caption=\"PrivateEnvironment Record Fields\">\n      <table>\n        <thead>\n          <tr>\n            <th>\n              Field Name\n            </th>\n            <th>\n              Value Type\n            </th>\n            <th>\n              Meaning\n            </th>\n          </tr>\n        </thead>\n        <tr>\n          <td>\n            [[OuterPrivateEnvironment]]\n          </td>\n          <td>\n            a PrivateEnvironment Record or <b>null</b>\n          </td>\n          <td>\n            The PrivateEnvironment Record of the nearest containing class. <b>null</b> if the class with which this PrivateEnvironment Record is associated is not contained in any other class.\n          </td>\n        </tr>\n        <tr>\n          <td>\n            [[Names]]\n          </td>\n          <td>\n            a List of Private Names\n          </td>\n          <td>\n            The Private Names declared by this class.\n          </td>\n        </tr>\n      </table>\n    </emu-table>\n\n    ","3":"<p>Before it is evaluated, all ECMAScript code must be associated with a <dfn id=\"realm\" variants=\"realms\">realm</dfn>. Conceptually, a realm consists of a set of intrinsic objects, an ECMAScript global environment, all of the ECMAScript code that is loaded within the scope of that global environment, and other associated state and resources.</p>\n    <p>A realm is represented in this specification as a <dfn id=\"realm-record\" variants=\"Realm Records\">Realm Record</dfn> with the fields specified in table-realm-record-fields:</p>\n    <emu-table id=\"table-realm-record-fields\" caption=\"Realm Record Fields\" oldids=\"table-21\">\n      <table>\n        <thead>\n          <tr>\n            <th>\n              Field Name\n            </th>\n            <th>\n              Value\n            </th>\n            <th>\n              Meaning\n            </th>\n          </tr>\n        </thead>\n        <tr>\n          <td>\n            [[AgentSignifier]]\n          </td>\n          <td>\n            an agent signifier\n          </td>\n          <td>\n            The agent that owns this realm\n          </td>\n        </tr>\n        <tr>\n          <td>\n            [[Intrinsics]]\n          </td>\n          <td>\n            a Record whose field names are intrinsic keys and whose values are objects\n          </td>\n          <td>\n            The intrinsic values used by code associated with this realm\n          </td>\n        </tr>\n        <tr>\n          <td>\n            [[GlobalObject]]\n          </td>\n          <td>\n            an Object\n          </td>\n          <td>\n            The global object for this realm\n          </td>\n        </tr>\n        <tr>\n          <td>\n            [[GlobalEnv]]\n          </td>\n          <td>\n            a Global Environment Record\n          </td>\n          <td>\n            The global environment for this realm\n          </td>\n        </tr>\n        <tr>\n          <td>\n            [[TemplateMap]]\n          </td>\n          <td>\n            a List of Records with fields [[Site]] (a <emu-nt>TemplateLiteral</emu-nt> Parse Node) and [[Array]] (an Array)\n          </td>\n          <td>\n            <p>Template objects are canonicalized separately for each realm using its Realm Record's [[TemplateMap]]. Each [[Site]] value is a Parse Node that is a <emu-nt>TemplateLiteral</emu-nt>. The associated [[Array]] value is the corresponding template object that is passed to a tag function.</p>\n            <emu-note>Once a Parse Node becomes unreachable, the corresponding [[Array]] is also unreachable, and it would be unobservable if an implementation removed the pair from the [[TemplateMap]] list.</emu-note>\n          </td>\n        </tr>\n        <tr>\n          <td>\n            [[LoadedModules]]\n          </td>\n          <td>\n            a List of LoadedModuleRequest Records\n          </td>\n          <td>\n            <p>A map from the specifier strings imported by this realm to the resolved Module Record. The list does not contain two different Records <var>r1</var> and <var>r2</var> such that ModuleRequestsEqual(<var>r1</var>, <var>r2</var>) is <b>true</b>.</p>\n            <emu-note>\n              As mentioned in HostLoadImportedModule (note-HostLoadImportedModule-referrer-Realm-Record), [[LoadedModules]] in Realm Records is only used when running an <code>import()</code> expression in a context where there is no active script or module.\n            </emu-note>\n          </td>\n        </tr>\n        <tr>\n          <td>\n            [[HostDefined]]\n          </td>\n          <td>\n            anything (default value is <b>undefined</b>)\n          </td>\n          <td>\n            Field reserved for use by hosts that need to associate additional information with a Realm Record.\n          </td>\n        </tr>\n      </table>\n    </emu-table>\n\n    ","4":"<p>An <dfn variants=\"execution contexts\">execution context</dfn> is a specification device that is used to track the runtime evaluation of code by an ECMAScript implementation. At any point in time, there is at most one execution context per agent that is actually executing code. This is known as the agent's <dfn id=\"running-execution-context\" variants=\"running execution contexts\">running execution context</dfn>. All references to the running execution context in this specification denote the running execution context of the surrounding agent.</p>\n    <p>The <dfn id=\"execution-context-stack\" variants=\"execution context stacks\">execution context stack</dfn> is used to track execution contexts. The running execution context is always the top element of this stack. A new execution context is created whenever control is transferred from the executable code associated with the currently running execution context to executable code that is not associated with that execution context. The newly created execution context is pushed onto the stack and becomes the running execution context.</p>\n    <p>An execution context contains whatever implementation specific state is necessary to track the execution progress of its associated code. Each execution context has at least the state components listed in table-state-components-for-all-execution-contexts.</p>\n    <emu-table id=\"table-state-components-for-all-execution-contexts\" caption=\"State Components for All Execution Contexts\" oldids=\"table-22\">\n      <table>\n        <thead>\n          <tr>\n            <th>\n              Component\n            </th>\n            <th>\n              Purpose\n            </th>\n          </tr>\n        </thead>\n        <tr>\n          <td>\n            code evaluation state\n          </td>\n          <td>\n            Any state needed to perform, suspend, and resume evaluation of the code associated with this execution context.\n          </td>\n        </tr>\n        <tr>\n          <td>\n            Function\n          </td>\n          <td>\n            If this execution context is evaluating the code of a function object, then the value of this component is that function object. If the context is evaluating the code of a <emu-nt>Script</emu-nt> or <emu-nt>Module</emu-nt>, the value is <b>null</b>.\n          </td>\n        </tr>\n        <tr>\n          <td>\n            Realm\n          </td>\n          <td>\n            The Realm Record from which associated code accesses ECMAScript resources.\n          </td>\n        </tr>\n        <tr>\n          <td>\n            ScriptOrModule\n          </td>\n          <td>\n            The Module Record or Script Record from which associated code originates. If there is no originating script or module, as is the case for the original execution context created in InitializeHostDefinedRealm, the value is <b>null</b>.\n          </td>\n        </tr>\n      </table>\n    </emu-table>\n    <p>Evaluation of code by the running execution context may be suspended at various points defined within this specification. Once the running execution context has been suspended a different execution context may become the running execution context and commence evaluating its code. At some later time a suspended execution context may again become the running execution context and continue evaluating its code at the point where it had previously been suspended. Transition of the running execution context status among execution contexts usually occurs in stack-like last-in/first-out manner. However, some ECMAScript features require non-LIFO transitions of the running execution context.</p>\n    <p>The value of the Realm component of the running execution context is also called <dfn id=\"current-realm\">the current Realm Record</dfn>. The value of the Function component of the running execution context is also called the <dfn id=\"active-function-object\">active function object</dfn>.</p>\n    <p><dfn id=\"ecmascript-code-execution-context\" variants=\"ECMAScript code execution context\">ECMAScript code execution contexts</dfn> have the additional state components listed in table-additional-state-components-for-ecmascript-code-execution-contexts.</p>\n    <emu-table id=\"table-additional-state-components-for-ecmascript-code-execution-contexts\" caption=\"Additional State Components for ECMAScript Code Execution Contexts\" oldids=\"table-23\">\n      <table>\n        <thead>\n          <tr>\n            <th>\n              Component\n            </th>\n            <th>\n              Purpose\n            </th>\n          </tr>\n        </thead>\n        <tr>\n          <td>\n            LexicalEnvironment\n          </td>\n          <td>\n            Identifies the Environment Record used to resolve identifier references made by code within this execution context.\n          </td>\n        </tr>\n        <tr>\n          <td>\n            VariableEnvironment\n          </td>\n          <td>\n            Identifies the Environment Record that holds bindings created by <emu-nt>VariableStatement</emu-nt>s within this execution context.\n          </td>\n        </tr>\n        <tr>\n          <td>\n            PrivateEnvironment\n          </td>\n          <td>\n            Identifies the PrivateEnvironment Record that holds Private Names created by <emu-nt>ClassElement</emu-nt>s in the nearest containing class. <b>null</b> if there is no containing class.\n          </td>\n        </tr>\n      </table>\n    </emu-table>\n    <p>The LexicalEnvironment and VariableEnvironment components of an execution context are always Environment Records.</p>\n    <p>Execution contexts representing the evaluation of Generators have the additional state components listed in table-additional-state-components-for-generator-execution-contexts.</p>\n    <emu-table id=\"table-additional-state-components-for-generator-execution-contexts\" caption=\"Additional State Components for Generator Execution Contexts\" oldids=\"table-24\">\n      <table>\n        <thead>\n          <tr>\n            <th>\n              Component\n            </th>\n            <th>\n              Purpose\n            </th>\n          </tr>\n        </thead>\n        <tr>\n          <td>\n            Generator\n          </td>\n          <td>\n            The Generator that this execution context is evaluating.\n          </td>\n        </tr>\n      </table>\n    </emu-table>\n    <p>In most situations only the running execution context (the top of the execution context stack) is directly manipulated by algorithms within this specification. Hence when the terms “LexicalEnvironment”, and “VariableEnvironment” are used without qualification they are in reference to those components of the running execution context.</p>\n    <p>An execution context is purely a specification mechanism and need not correspond to any particular artefact of an ECMAScript implementation. It is impossible for ECMAScript code to directly access or observe an execution context.</p>\n\n    ","5":"<p>A <dfn id=\"job\" variants=\"Jobs\">Job</dfn> is an Abstract Closure with no parameters that initiates an ECMAScript computation when no other ECMAScript computation is currently in progress.</p>\n    <p>Jobs are scheduled for execution by ECMAScript host environments in a particular agent. This specification describes the host hooks HostEnqueueGenericJob, HostEnqueueFinalizationRegistryCleanupJob, HostEnqueuePromiseJob, and HostEnqueueTimeoutJob to schedule jobs. The host hooks in this specification are organized by the additional constraints imposed on the scheduling of jobs. Hosts may define additional abstract operations which schedule jobs. Such operations accept a Job Abstract Closure and a realm (a Realm Record or <b>null</b>) as parameters. If a Realm Record is provided, these operations schedule the job to be performed at some future time in the provided realm, in the agent that owns the realm. If <b>null</b> is provided instead for the realm, then the job does not evaluate ECMAScript code. Their implementations must conform to the following requirements:</p>\n\n    <ul>\n      <li>At some future point in time, when there is no running context in the agent for which the job is scheduled and that agent's execution context stack is empty, the implementation must:\n        <ol>\n          <li>Perform any host-defined preparation steps.</li>\n          <li><emu-not-ref>Invoke</emu-not-ref> the Job Abstract Closure.</li>\n          <li>Perform any host-defined cleanup steps, after which the execution context stack must be empty.</li>\n        </ol>\n      </li>\n      <li>Only one Job may be actively undergoing evaluation at any point in time in an agent.</li>\n      <li>Once evaluation of a Job starts, it must run to completion before evaluation of any other Job starts in an agent.</li>\n      <li>The Abstract Closure must return a normal completion, implementing its own handling of errors.</li>\n    </ul>\n\n    <emu-note>\n      Host environments are not required to treat Jobs uniformly with respect to scheduling. For example, web browsers and Node.js treat Promise-handling Jobs as a higher priority than other work; future features may add Jobs that are not treated at such a high priority.\n    </emu-note>\n\n    <p>At any particular time, <var>scriptOrModule</var> (a Script Record, a Module Record, or <b>null</b>) is the <dfn id=\"job-activescriptormodule\">active script or module</dfn> if all of the following conditions are true:</p>\n    <ul>\n      <li>GetActiveScriptOrModule() is <var>scriptOrModule</var>.</li>\n      <li>If <var>scriptOrModule</var> is a Script Record or Module Record, let <var>ec</var> be the topmost execution context on the execution context stack whose ScriptOrModule component is <var>scriptOrModule</var>. The Realm component of <var>ec</var> is <var>scriptOrModule</var>.[[Realm]].</li>\n    </ul>\n\n    <p>At any particular time, an execution is <dfn id=\"job-preparedtoevaluatecode\">prepared to evaluate ECMAScript code</dfn> if all of the following conditions are true:</p>\n    <ul>\n      <li>The execution context stack is not empty.</li>\n      <li>The Realm component of the topmost execution context on the execution context stack is a Realm Record.</li>\n    </ul>\n\n    <emu-note>\n      <p>Host environments may prepare an execution to evaluate code by pushing execution contexts onto the execution context stack. The specific steps are implementation-defined.</p>\n      <p>The specific choice of Realm is up to the host environment. This initial execution context and Realm is only in use before any callback function is invoked. When a callback function related to a Job, like a Promise handler, is invoked, the invocation pushes its own execution context and Realm.</p>\n    </emu-note>\n\n    <p>Particular kinds of Jobs have additional conformance requirements.</p>\n\n    ","6":"<p>An <dfn id=\"agent\" variants=\"agents\">agent</dfn> comprises a set of ECMAScript execution contexts, an execution context stack, a running execution context, an <dfn id=\"agent-record\" variants=\"Agent Records\">Agent Record</dfn>, and an <dfn id=\"executing-thread\" variants=\"executing threads\">executing thread</dfn>. Except for the executing thread, the constituents of an agent belong exclusively to that agent.</p>\n    <p>An agent's executing thread executes algorithmic steps on the agent's execution contexts independently of other agents, except that an executing thread may be used as the executing thread by multiple agents, provided none of the agents sharing the thread have an Agent Record whose [[CanBlock]] field is <b>true</b>.</p>\n    <emu-note>\n      <p>Some web browsers share a single executing thread across multiple unrelated tabs of a browser window, for example.</p>\n    </emu-note>\n    <p>While an agent's executing thread is executing algorithmic steps, the agent is the <dfn id=\"surrounding-agent\" variants=\"surrounding agents\">surrounding agent</dfn> for those steps. The steps use the surrounding agent to access the specification-level execution objects held within the agent: the running execution context, the execution context stack, and the Agent Record's fields.</p>\n    <p>An <dfn variants=\"agent signifiers\">agent signifier</dfn> is a globally-unique opaque value used to identify an Agent.</p>\n    <emu-table id=\"table-agent-record\" caption=\"Agent Record Fields\">\n      <table>\n        <thead>\n          <tr>\n            <th>Field Name</th>\n            <th>Value</th>\n            <th>Meaning</th>\n          </tr>\n        </thead>\n        <tr>\n          <td>[[LittleEndian]]</td>\n          <td>a Boolean</td>\n          <td>The default value computed for the <em>isLittleEndian</em> parameter when it is needed by the algorithms GetValueFromBuffer and SetValueInBuffer. The choice is implementation-defined and should be the alternative that is most efficient for the implementation.</td>\n        </tr>\n        <tr>\n          <td>[[CanBlock]]</td>\n          <td>a Boolean</td>\n          <td>Determines whether the agent can block or not.</td>\n        </tr>\n        <tr>\n          <td>[[Signifier]]</td>\n          <td>an agent signifier</td>\n          <td>Uniquely identifies the agent within its agent cluster.</td>\n        </tr>\n        <tr>\n          <td>[[IsLockFree1]]</td>\n          <td>a Boolean</td>\n          <td><b>true</b> if atomic operations on one-<emu-not-ref>byte values</emu-not-ref> are lock-free, <b>false</b> otherwise.</td>\n        </tr>\n        <tr>\n          <td>[[IsLockFree2]]</td>\n          <td>a Boolean</td>\n          <td><b>true</b> if atomic operations on two-<emu-not-ref>byte values</emu-not-ref> are lock-free, <b>false</b> otherwise.</td>\n        </tr>\n        <tr>\n          <td>[[IsLockFree8]]</td>\n          <td>a Boolean</td>\n          <td><b>true</b> if atomic operations on eight-<emu-not-ref>byte values</emu-not-ref> are lock-free, <b>false</b> otherwise.</td>\n        </tr>\n        <tr>\n          <td>[[CandidateExecution]]</td>\n          <td>a candidate execution Record</td>\n          <td>See the memory model.</td>\n        </tr>\n        <tr>\n          <td>[[KeptAlive]]</td>\n          <td>a List of either Objects or Symbols</td>\n          <td>Initially a new empty List, representing the list of objects and/or symbols to be kept alive until the end of the current Job</td>\n        </tr>\n        <tr>\n          <td>[[ModuleAsyncEvaluationCount]]</td>\n          <td>an integer</td>\n          <td>Initially 0, used to assign unique incrementing values to the [[AsyncEvaluationOrder]] field of modules that are asynchronous or have asynchronous dependencies.</td>\n        </tr>\n      </table>\n    </emu-table>\n\n    <p>The values of [[LittleEndian]], [[Signifier]], [[IsLockFree1]], [[IsLockFree2]], and [[IsLockFree8]] cannot change.</p>\n\n    <emu-note>\n      <p>The values of [[IsLockFree1]], [[IsLockFree2]], and [[IsLockFree8]] are not necessarily determined by the hardware, but may also reflect implementation choices that can vary over time and between ECMAScript implementations.</p>\n\n      <p>There is no [[IsLockFree4]] field: 4-byte atomic operations are always lock-free.</p>\n\n      <p>In practice, if an atomic operation is implemented with any type of lock the operation is not lock-free. Lock-free does not imply wait-free: there is no upper bound on how many machine steps may be required to complete a lock-free atomic operation.</p>\n\n      <p>That an atomic access of size <em>n</em> is lock-free does not imply anything about the (perceived) atomicity of non-atomic accesses of size <em>n</em>, specifically, non-atomic accesses may still be performed as a sequence of several separate memory accesses. See ReadSharedMemory and WriteSharedMemory for details.</p>\n    </emu-note>\n\n    <emu-note>\n      <p>An agent is a specification mechanism and need not correspond to any particular artefact of an ECMAScript implementation.</p>\n    </emu-note>\n\n    ","7":"<p>An <dfn variants=\"agent clusters\">agent cluster</dfn> is a maximal set of agents that can communicate by operating on shared memory.</p>\n\n    <emu-note>\n      <p>Programs within different agents may share memory by unspecified means. At a minimum, the backing memory for SharedArrayBuffers can be shared among the agents in the cluster.</p>\n\n      <p>There may be agents that can communicate by message passing that cannot share memory; they are never in the same agent cluster.</p>\n    </emu-note>\n\n    <p>Every agent belongs to exactly one agent cluster.</p>\n\n    <emu-note>\n      <p>The agents in a cluster need not all be alive at some particular point in time. If agent <b>A</b> creates another agent <b>B</b>, after which <b>A</b> terminates and <b>B</b> creates agent <b>C</b>, the three agents are in the same cluster if <b>A</b> could share some memory with <b>B</b> and <b>B</b> could share some memory with <b>C</b>.</p>\n    </emu-note>\n\n    <p>All agents within a cluster must have the same value for the [[LittleEndian]] field in their respective Agent Records.</p>\n\n    <emu-note>\n      <p>If different agents within an agent cluster have different values of [[LittleEndian]] it becomes hard to use shared memory for multi-byte data.</p>\n    </emu-note>\n\n    <p>All agents within a cluster must have the same values for the [[IsLockFree1]] field in their respective Agent Records; similarly for the [[IsLockFree2]] and [[IsLockFree8]] fields.</p>\n\n    <p>All agents within a cluster must have different values for the [[Signifier]] field in their respective Agent Records.</p>\n\n    <p>An embedding may deactivate (stop forward progress) or activate (resume forward progress) an agent without the agent's knowledge or cooperation. If the embedding does so, it must not leave some agents in the cluster active while other agents in the cluster are deactivated indefinitely.</p>\n\n    <emu-note>\n      <p>The purpose of the preceding restriction is to avoid a situation where an agent deadlocks or starves because another agent has been deactivated. For example, if an HTML shared worker that has a lifetime independent of documents in any windows were allowed to share memory with the dedicated worker of such an independent document, and the document and its dedicated worker were to be deactivated while the dedicated worker holds a lock (say, the document is pushed into its window's history), and the shared worker then tries to acquire the lock, then the shared worker will be blocked until the dedicated worker is activated again, if ever. Meanwhile other workers trying to access the shared worker from other windows will starve.</p>\n\n      <p>The implication of the restriction is that it will not be possible to share memory between agents that don't belong to the same suspend/wake collective within the embedding.</p>\n    </emu-note>\n\n    <p>An embedding may terminate an agent without any of the agent's cluster's other agents' prior knowledge or cooperation. If an agent is terminated not by programmatic action of its own or of another agent in the cluster but by forces external to the cluster, then the embedding must choose one of two strategies: Either terminate all the agents in the cluster, or provide reliable APIs that allow the agents in the cluster to coordinate so that at least one remaining member of the cluster will be able to detect the termination, with the termination data containing enough information to identify the agent that was terminated.</p>\n\n    <emu-note>\n      <p>Examples of that type of termination are: operating systems or users terminating agents that are running in separate processes; the embedding itself terminating an agent that is running in-process with the other agents when per-agent resource accounting indicates that the agent is runaway.</p>\n    </emu-note>\n\n    <p>Each of the following specification values, and values transitively reachable from them, belong to exactly one agent cluster.</p>\n    <ul>\n      <li>candidate execution Record</li>\n      <li>Shared Data Block</li>\n      <li>WaiterList Record</li>\n    </ul>\n\n    <p>Prior to any evaluation of any ECMAScript code by any agent in a cluster, the [[CandidateExecution]] field of the Agent Record for all agents in the cluster is set to the initial candidate execution. The initial candidate execution is an empty candidate execution whose [[EventsRecords]] field is a List containing, for each agent, an Agent Events Record whose [[AgentSignifier]] field is that agent's agent signifier, and whose [[EventList]] and [[AgentSynchronizesWith]] fields are empty Lists.</p>\n\n    <emu-note>\n      <p>All agents in an agent cluster share the same candidate execution in its Agent Record's [[CandidateExecution]] field. The candidate execution is a specification mechanism used by the memory model.</p>\n    </emu-note>\n\n    <emu-note>\n      <p>An agent cluster is a specification mechanism and need not correspond to any particular artefact of an ECMAScript implementation.</p>\n    </emu-note>\n  ","8":"<p>For an agent to <em>make forward progress</em> is for it to perform an evaluation step according to this specification.</p>\n    <p>An agent becomes <em>blocked</em> when its running execution context waits synchronously and indefinitely for an external event. Only agents whose Agent Record's [[CanBlock]] field is <b>true</b> can become blocked in this sense. An <em>unblocked</em> agent is one that is not blocked.</p>\n\n    <p>Implementations must ensure that:</p>\n    <ul>\n      <li>every unblocked agent with a dedicated executing thread eventually makes forward progress</li>\n      <li>in a set of agents that share an executing thread, one agent eventually makes forward progress</li>\n      <li>an agent does not cause another agent to become blocked except via explicit APIs that provide blocking.</li>\n    </ul>\n\n    <emu-note>\n      <p>This, along with the liveness guarantee in the memory model, ensures that all <emu-const>seq-cst</emu-const> writes eventually become observable to all agents.</p>\n    </emu-note>\n  ","9":"","10":"<dl class=\"header\">\n      <dt>description</dt>\n      <dd>ECMAScript implementations are expected to call ClearKeptObjects when a synchronous sequence of ECMAScript executions completes.</dd>\n    </dl>\n    <emu-alg><ol><li>Let <var>agentRecord</var> be the surrounding agent's Agent Record.</li><li>Set <var>agentRecord</var>.[[KeptAlive]] to a new empty List.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n  ","11":"<dl class=\"header\">\n    </dl>\n    <emu-alg><ol><li>Let <var>agentRecord</var> be the surrounding agent's Agent Record.</li><li>Append <var>value</var> to <var>agentRecord</var>.[[KeptAlive]].</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    <emu-note>\n      When the abstract operation AddToKeptObjects is called with a target object or symbol, it adds the target to a list that will point strongly at the target until ClearKeptObjects is called.\n    </emu-note>\n  ","12":"<dl class=\"header\">\n    </dl>\n    <emu-alg><ol><li>Assert: <var>finalizationRegistry</var> has [[Cells]] and [[CleanupCallback]] internal slots.</li><li>Let <var>callback</var> be <var>finalizationRegistry</var>.[[CleanupCallback]].</li><li>While <var>finalizationRegistry</var>.[[Cells]] contains a Record <var>cell</var> such that <var>cell</var>.[[WeakRefTarget]] is <emu-const>empty</emu-const>, an implementation may perform the following steps:<ol><li>Choose any such <var>cell</var>.</li><li>Remove <var>cell</var> from <var>finalizationRegistry</var>.[[Cells]].</li><li>Perform ? HostCallJobCallback(<var>callback</var>, <b>undefined</b>, « <var>cell</var>.[[HeldValue]] »).</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n  ","13":"<dl class=\"header\">\n      <dt>description</dt>\n      <dd>It returns <b>true</b> if and only if <var>v</var> is suitable for use as a weak reference. Only values that are suitable for use as a weak reference may be a key of a WeakMap, an element of a WeakSet, the target of a WeakRef, or one of the targets of a FinalizationRegistry.</dd>\n    </dl>\n    <emu-alg><ol><li>If <var>v</var> is an Object, return <b>true</b>.</li><li>If <var>v</var> is a Symbol and KeyForSymbol(<var>v</var>) is <b>undefined</b>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n    <emu-note>\n      <p>A language value without <emu-xref href=\"#sec-identity\">language identity</emu-xref> can be manifested without prior reference and is unsuitable for use as a weak reference. A Symbol value produced by <emu-xref href=\"#sec-symbol.for\">Symbol.for</emu-xref>, unlike other Symbol values, does not have language identity and is unsuitable for use as a weak reference. <emu-xref href=\"#sec-well-known-symbols\">Well-known symbols</emu-xref> are likely to never be collected, but are nonetheless treated as suitable for use as a weak reference because they are limited in number and therefore manageable by a variety of implementation approaches. However, any value associated to a well-known symbol in a live WeakMap is unlikely to be collected and could “leak” memory resources in implementations.</p>\n    </emu-note>\n  ","":"","1.1":"<p>Environment Records can be thought of as existing in a simple object-oriented hierarchy where Environment Record is an abstract class with three concrete subclasses: Declarative Environment Record, Object Environment Record, and Global Environment Record. Function Environment Records and Module Environment Records are subclasses of Declarative Environment Record.</p>\n      <ul>\n        <li>\n          <p>Environment Record (abstract)</p>\n          <ul>\n            <li>\n              <p>A <em>Declarative Environment Record</em> is used to define the effect of ECMAScript language syntactic elements such as <emu-nt>FunctionDeclaration</emu-nt>s, <emu-nt>VariableDeclaration</emu-nt>s, and <emu-nt>Catch</emu-nt> clauses that directly associate identifier bindings with ECMAScript language values.</p>\n              <ul>\n                <li>\n                  <p>A <em>Function Environment Record</em> corresponds to the invocation of an ECMAScript function object, and contains bindings for the top-level declarations within that function. It may establish a new <code>this</code> binding. It also captures the state necessary to support <code>super</code> method invocations.</p>\n                </li>\n                <li>\n                  <p>A <em>Module Environment Record</em> contains the bindings for the top-level declarations of a <emu-nt>Module</emu-nt>. It also contains the bindings that are explicitly imported by the <emu-nt>Module</emu-nt>. Its [[OuterEnv]] is a Global Environment Record.</p>\n                </li>\n              </ul>\n            </li>\n            <li>\n              <p>An <em>Object Environment Record</em> is used to define the effect of ECMAScript elements such as <emu-nt>WithStatement</emu-nt> that associate identifier bindings with the properties of some object.</p>\n            </li>\n            <li>\n              <p>A <em>Global Environment Record</em> is used for <emu-nt>Script</emu-nt> global declarations. It does not have an outer environment; its [[OuterEnv]] is <b>null</b>. It may be prepopulated with identifier bindings and it includes an associated global object whose properties provide some of the global environment's identifier bindings. As ECMAScript code is executed, additional properties may be added to the global object and the initial properties may be modified.</p>\n            </li>\n          </ul>\n        </li>\n      </ul>\n\n      <p>The Environment Record abstract class includes the abstract specification methods defined in table-abstract-methods-of-environment-records. These abstract methods have distinct concrete algorithms for each of the concrete subclasses.</p>\n      <emu-table id=\"table-abstract-methods-of-environment-records\" type=\"abstract methods\" of=\"Environment Records\" oldids=\"table-15,table-17,table-19,table-20,table-additional-methods-of-function-environment-records,table-additional-methods-of-global-environment-records,table-additional-methods-of-module-environment-records\">\n        <table>\n          <thead>\n            <tr>\n              <th>\n                Method\n              </th>\n              <th>\n                Purpose\n              </th>\n              <th>\n                Definitions\n              </th>\n            </tr>\n          </thead>\n          <tr id=\"abstract-hasbinding\">\n            <td>\n              HasBinding (\n                <var>name</var>: a String,\n              ): either a normal completion containing a Boolean or a throw completion\n            </td>\n            <td>\n              It determines if an Environment Record has a binding for <var>name</var>.\n            </td>\n            <td>\n              It has concrete definitions in the following types:\n              <emu-concrete-method-dfns for=\"HasBinding\"></emu-concrete-method-dfns>\n            </td>\n          </tr>\n          <tr id=\"abstract-createmutablebinding\">\n            <td>\n              CreateMutableBinding (\n                <var>name</var>: a String,\n                <var>deletable</var>: a Boolean,\n              ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion\n            </td>\n            <td>\n              It creates a new but uninitialized mutable binding in an Environment Record. <var>name</var> is the text of the bound name. If <var>deletable</var> is <b>true</b> the binding may be subsequently deleted.\n            </td>\n            <td>\n              It has concrete definitions in the following types:\n              <emu-concrete-method-dfns for=\"CreateMutableBinding\"></emu-concrete-method-dfns>\n            </td>\n          </tr>\n          <tr id=\"abstract-createimmutablebinding\">\n            <td>\n              CreateImmutableBinding (\n                <var>name</var>: a String,\n                <var>strict</var>: a Boolean,\n              ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion\n            </td>\n            <td>\n              It creates a new but uninitialized immutable binding in an Environment Record. <var>name</var> is the text of the bound name. If <var>strict</var> is <b>true</b> then attempts to set it after it has been initialized will always throw an exception, regardless of the strict mode setting of operations that reference that binding.\n            </td>\n            <td>\n              It has concrete definitions in the following types:\n              <emu-concrete-method-dfns for=\"CreateImmutableBinding\"></emu-concrete-method-dfns>\n            </td>\n          </tr>\n          <tr id=\"abstract-initializebinding\">\n            <td>\n              InitializeBinding (\n                <var>name</var>: a String,\n                <var>value</var>: an ECMAScript language value,\n              ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion\n            </td>\n            <td>\n              It sets the value of an already existing but uninitialized binding in an Environment Record. <var>name</var> is the text of the bound name. <var>value</var> is the value for the binding.\n            </td>\n            <td>\n              It has concrete definitions in the following types:\n              <emu-concrete-method-dfns for=\"InitializeBinding\"></emu-concrete-method-dfns>\n            </td>\n          </tr>\n          <tr id=\"abstract-setmutablebinding\">\n            <td>\n              SetMutableBinding (\n                <var>name</var>: a String,\n                <var>value</var>: an ECMAScript language value,\n                <var>strict</var>: a Boolean,\n              ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion\n            </td>\n            <td>\n              It sets the value of an already existing mutable binding in an Environment Record. <var>name</var> is the text of the bound name. <var>value</var> is the value for the binding. If <var>strict</var> is <b>true</b> and the binding cannot be set, this will throw a <b>TypeError</b> exception.\n            </td>\n            <td>\n              It has concrete definitions in the following types:\n              <emu-concrete-method-dfns for=\"SetMutableBinding\"></emu-concrete-method-dfns>\n            </td>\n          </tr>\n          <tr id=\"abstract-getbindingvalue\">\n            <td>\n              GetBindingValue (\n                <var>name</var>: a String,\n                <var>strict</var>: a Boolean,\n              ): either a normal completion containing an ECMAScript language value or a throw completion\n            </td>\n            <td>\n              It returns the value of an already existing binding from an Environment Record. <var>name</var> is the text of the bound name. <var>strict</var> is used to identify references originating in strict mode code or that otherwise require strict mode reference semantics. If <var>strict</var> is <b>true</b> and the binding does not exist, this will throw a <b>ReferenceError</b> exception. If the binding exists but is uninitialized a <b>ReferenceError</b> is thrown, regardless of the value of <var>strict</var>.\n            </td>\n            <td>\n              It has concrete definitions in the following types:\n              <emu-concrete-method-dfns for=\"GetBindingValue\"></emu-concrete-method-dfns>\n            </td>\n          </tr>\n          <tr id=\"abstract-deletebinding\">\n            <td>\n              DeleteBinding (\n                <var>name</var>: a String,\n              ): either a normal completion containing a Boolean or a throw completion\n            </td>\n            <td>\n              It deletes a binding from an Environment Record. <var>name</var> is the text of the bound name. If a binding for <var>name</var> exists, this removes the binding and returns <b>true</b>. If the binding exists but cannot be removed, this returns <b>false</b>. If the binding does not exist, this returns <b>true</b>.\n            </td>\n            <td>\n              It has concrete definitions in the following types:\n              <emu-concrete-method-dfns for=\"DeleteBinding\"></emu-concrete-method-dfns>\n            </td>\n          </tr>\n          <tr id=\"abstract-hasthisbinding\">\n            <td>HasThisBinding ( ): a Boolean</td>\n            <td>\n              It determines if an Environment Record establishes a <code>this</code> binding. This returns <b>true</b> if it does and <b>false</b> if it does not.\n            </td>\n            <td>\n              It has concrete definitions in the following types:\n              <emu-concrete-method-dfns for=\"HasThisBinding\"></emu-concrete-method-dfns>\n            </td>\n          </tr>\n          <tr id=\"abstract-getthisbinding\">\n            <td>GetThisBinding ( ): either a normal completion containing an ECMAScript language value or a throw completion</td>\n            <td>\n              It returns the value of this Environment Record's <code>this</code> binding. It throws a <b>ReferenceError</b> if the <code>this</code> binding has not been initialized.\n            </td>\n            <td>\n              It has concrete definitions in the following types:\n              <emu-concrete-method-dfns for=\"GetThisBinding\"></emu-concrete-method-dfns>\n            </td>\n          </tr>\n          <tr id=\"abstract-hassuperbinding\">\n            <td>HasSuperBinding ( ): a Boolean</td>\n            <td>\n              It determines if an Environment Record establishes a <code>super</code> method binding. This returns <b>true</b> if it does and <b>false</b> if it does not. If it returns <b>true</b> it implies that the Environment Record is a Function Environment Record, although the reverse implication does not hold.\n            </td>\n            <td>\n              It has concrete definitions in the following types:\n              <emu-concrete-method-dfns for=\"HasSuperBinding\"></emu-concrete-method-dfns>\n            </td>\n          </tr>\n          <tr id=\"abstract-withbaseobject\">\n            <td>WithBaseObject ( ): an Object or <b>undefined</b></td>\n            <td>\n              If this Environment Record is associated with a <code>with</code> statement, it returns the with object. Otherwise, it returns <b>undefined</b>.\n            </td>\n            <td>\n              It has concrete definitions in the following types:\n              <emu-concrete-method-dfns for=\"WithBaseObject\"></emu-concrete-method-dfns>\n            </td>\n          </tr>\n        </table>\n      </emu-table>\n\n      ","1.1.1":"<p>Each <dfn variants=\"Declarative Environment Records\">Declarative Environment Record</dfn> is associated with an ECMAScript program scope containing variable, constant, let, class, module, import, and/or function declarations. A Declarative Environment Record binds the set of identifiers defined by the declarations contained within its scope.</p>\n\n        ","1.1.1.1":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Declarative Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It determines if the argument identifier is one of the identifiers bound by the record.</dd>\n          </dl>\n          <emu-alg><ol><li>If <var>envRec</var> has a binding for <var>name</var>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n        ","1.1.1.2":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Declarative Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It creates a new mutable binding for the name <var>name</var> that is uninitialized. A binding must not already exist in this Environment Record for <var>name</var>. If <var>deletable</var> is <b>true</b>, the new binding is marked as being subject to deletion.</dd>\n          </dl>\n          <emu-alg><ol><li>Assert: <var>envRec</var> does not already have a binding for <var>name</var>.</li><li>Create a mutable binding in <var>envRec</var> for <var>name</var> and record that it is uninitialized. If <var>deletable</var> is <b>true</b>, record that the newly created binding may be deleted by a subsequent DeleteBinding call.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n        ","1.1.1.3":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Declarative Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It creates a new immutable binding for the name <var>name</var> that is uninitialized. A binding must not already exist in this Environment Record for <var>name</var>. If <var>strict</var> is <b>true</b>, the new binding is marked as a strict binding.</dd>\n          </dl>\n          <emu-alg><ol><li>Assert: <var>envRec</var> does not already have a binding for <var>name</var>.</li><li>Create an immutable binding in <var>envRec</var> for <var>name</var> and record that it is uninitialized. If <var>strict</var> is <b>true</b>, record that the newly created binding is a strict binding.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n        ","1.1.1.4":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Declarative Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It is used to set the bound value of the current binding of the identifier whose name is <var>name</var> to the value <var>value</var>. An uninitialized binding for <var>name</var> must already exist.</dd>\n          </dl>\n          <emu-alg><ol><li>Assert: <var>envRec</var> must have an uninitialized binding for <var>name</var>.</li><li>Set the bound value for <var>name</var> in <var>envRec</var> to <var>value</var>.</li><li><emu-not-ref>Record</emu-not-ref> that the binding for <var>name</var> in <var>envRec</var> has been initialized.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n        ","1.1.1.5":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Declarative Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It attempts to change the bound value of the current binding of the identifier whose name is <var>name</var> to the value <var>value</var>. A binding for <var>name</var> normally already exists, but in rare cases it may not. If the binding is an immutable binding, a <b>TypeError</b> is thrown if <var>strict</var> is <b>true</b>.</dd>\n          </dl>\n          <emu-alg><ol><li>If <var>envRec</var> does not have a binding for <var>name</var>, then<ol><li>If <var>strict</var> is <b>true</b>, throw a <b>ReferenceError</b> exception.</li><li>Perform ! <var>envRec</var>.CreateMutableBinding(<var>name</var>, <b>true</b>).</li><li>Perform ! <var>envRec</var>.InitializeBinding(<var>name</var>, <var>value</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></li><li>If the binding for <var>name</var> in <var>envRec</var> is a strict binding, set <var>strict</var> to <b>true</b>.</li><li>If the binding for <var>name</var> in <var>envRec</var> has not yet been initialized, then<ol><li>Throw a <b>ReferenceError</b> exception.</li></ol></li><li>Else if the binding for <var>name</var> in <var>envRec</var> is a mutable binding, then<ol><li>Change its bound value to <var>value</var>.</li></ol></li><li>Else,<ol><li>Assert: This is an attempt to change the value of an immutable binding.</li><li>If <var>strict</var> is <b>true</b>, throw a <b>TypeError</b> exception.</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n          <emu-note>\n            <p>An example of ECMAScript code that results in a missing binding at step step-setmutablebinding-missing-binding is:</p>\n            <pre><code class=\"javascript\">function f() { eval(\"var x; x = (delete x, 0);\"); }</code></pre>\n          </emu-note>\n        ","1.1.1.6":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Declarative Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It returns the value of its bound identifier whose name is <var>name</var>. If the binding exists but is uninitialized a <b>ReferenceError</b> is thrown, regardless of the value of <var>strict</var>.</dd>\n          </dl>\n          <emu-alg><ol><li>Assert: <var>envRec</var> has a binding for <var>name</var>.</li><li>If the binding for <var>name</var> in <var>envRec</var> is an uninitialized binding, throw a <b>ReferenceError</b> exception.</li><li>Return the value currently bound to <var>name</var> in <var>envRec</var>.</li></ol></emu-alg>\n        ","1.1.1.7":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Declarative Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It can only delete bindings that have been explicitly designated as being subject to deletion.</dd>\n          </dl>\n          <emu-alg><ol><li>Assert: <var>envRec</var> has a binding for <var>name</var>.</li><li>If the binding for <var>name</var> in <var>envRec</var> cannot be deleted, return <b>false</b>.</li><li>Remove the binding for <var>name</var> from <var>envRec</var>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n        ","1.1.1.8":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Declarative Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>Return <b>false</b>.</li></ol></emu-alg>\n          <emu-note>\n            <p>A regular Declarative Environment Record (i.e., one that is neither a Function Environment Record nor a Module Environment Record) does not provide a <code>this</code> binding.</p>\n          </emu-note>\n        ","1.1.1.9":"<p>The GetThisBinding concrete method of a Declarative Environment Record is never used within this specification.</p>\n        ","1.1.1.10":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Declarative Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>Return <b>false</b>.</li></ol></emu-alg>\n          <emu-note>\n            <p>A regular Declarative Environment Record (i.e., one that is neither a Function Environment Record nor a Module Environment Record) does not provide a <code>super</code> binding.</p>\n          </emu-note>\n        ","1.1.1.11":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Declarative Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>Return <b>undefined</b>.</li></ol></emu-alg>\n        ","1.1.2":"<p>Each <dfn variants=\"Object Environment Records\">Object Environment Record</dfn> is associated with an object called its <em>binding object</em>. An Object Environment Record binds the set of string identifier names that directly correspond to the property names of its binding object. Property keys that are not strings in the form of an <emu-nt>IdentifierName</emu-nt> are not included in the set of bound identifiers. Both own and inherited properties are included in the set regardless of the setting of their [[Enumerable]] attribute. Because properties can be dynamically added and deleted from objects, the set of identifiers bound by an Object Environment Record may potentially change as a side-effect of any operation that adds or deletes properties. Any bindings that are created as a result of such a side-effect are considered to be a mutable binding even if the Writable attribute of the corresponding property is <b>false</b>. Immutable bindings do not exist for Object Environment Records.</p>\n        <p>Object Environment Records created for <code>with</code> statements (14.11) can provide their binding object as an implicit <b>this</b> value for use in function calls. The capability is controlled by a Boolean [[IsWithEnvironment]] field.</p>\n        <p>Object Environment Records have the additional state fields listed in table-additional-fields-of-object-environment-records.</p>\n        <emu-table id=\"table-additional-fields-of-object-environment-records\" caption=\"Additional Fields of Object Environment Records\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Field Name\n                </th>\n                <th>\n                  Value\n                </th>\n                <th>\n                  Meaning\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                [[BindingObject]]\n              </td>\n              <td>\n                an Object\n              </td>\n              <td>\n                The binding object of this Environment Record.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[IsWithEnvironment]]\n              </td>\n              <td>\n                a Boolean\n              </td>\n              <td>\n                Indicates whether this Environment Record is created for a <code>with</code> statement.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n\n        ","1.1.2.1":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>an Object Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It determines if its associated binding object has a property whose name is <var>name</var>.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>bindingObject</var> be <var>envRec</var>.[[BindingObject]].</li><li>Let <var>foundBinding</var> be ? HasProperty(<var>bindingObject</var>, <var>name</var>).</li><li>If <var>foundBinding</var> is <b>false</b>, return <b>false</b>.</li><li>If <var>envRec</var>.[[IsWithEnvironment]] is <b>false</b>, return <b>true</b>.</li><li>Let <var>unscopables</var> be ? Get(<var>bindingObject</var>, <code class=\"emu-intrinsic\">%Symbol.unscopables%</code>).</li><li>If <var>unscopables</var> is an Object, then<ol><li>Let <var>blocked</var> be ToBoolean(? Get(<var>unscopables</var>, <var>name</var>)).</li><li>If <var>blocked</var> is <b>true</b>, return <b>false</b>.</li></ol></li><li>Return <b>true</b>.</li></ol></emu-alg>\n        ","1.1.2.2":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>an Object Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It creates in an Environment Record's associated binding object a property whose name is <var>name</var> and initializes it to the value <b>undefined</b>. If <var>deletable</var> is <b>true</b>, the new property's [[Configurable]] attribute is set to <b>true</b>; otherwise it is set to <b>false</b>.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>bindingObject</var> be <var>envRec</var>.[[BindingObject]].</li><li>Perform ? DefinePropertyOrThrow(<var>bindingObject</var>, <var>name</var>, PropertyDescriptor { [[Value]]: <b>undefined</b>, [[Writable]]: <b>true</b>, [[Enumerable]]: <b>true</b>, [[Configurable]]: <var>deletable</var> }).</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n          <emu-note>\n            <p>Normally <var>envRec</var> will not have a binding for <var>name</var> but if it does, the semantics of DefinePropertyOrThrow may result in an existing binding being replaced or shadowed or cause an abrupt completion to be returned.</p>\n          </emu-note>\n        ","1.1.2.3":"<p>The CreateImmutableBinding concrete method of an Object Environment Record is never used within this specification.</p>\n        ","1.1.2.4":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>an Object Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It is used to set the bound value of the current binding of the identifier whose name is <var>name</var> to the value <var>value</var>.</dd>\n          </dl>\n          <emu-alg><ol><li>Perform ? <emu-meta effects=\"user-code\"><var>envRec</var>.SetMutableBinding(<var>name</var>, <var>value</var>, <b>false</b>)</emu-meta>.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n          <emu-note>\n            <p>In this specification, all uses of CreateMutableBinding for Object Environment Records are immediately followed by a call to InitializeBinding for the same name. Hence, this specification does not explicitly track the initialization state of bindings in Object Environment Records.</p>\n          </emu-note>\n        ","1.1.2.5":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>an Object Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It attempts to set the value of the Environment Record's associated binding object's property whose name is <var>name</var> to the value <var>value</var>. A property named <var>name</var> normally already exists but if it does not or is not currently writable, error handling is determined by <var>strict</var>.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>bindingObject</var> be <var>envRec</var>.[[BindingObject]].</li><li>Let <var>stillExists</var> be ? HasProperty(<var>bindingObject</var>, <var>name</var>).</li><li>If <var>stillExists</var> is <b>false</b> and <var>strict</var> is <b>true</b>, throw a <b>ReferenceError</b> exception.</li><li>Perform ? Set(<var>bindingObject</var>, <var>name</var>, <var>value</var>, <var>strict</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n        ","1.1.2.6":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>an Object Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It returns the value of its associated binding object's property whose name is <var>name</var>. The property should already exist but if it does not the result depends upon <var>strict</var>.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>bindingObject</var> be <var>envRec</var>.[[BindingObject]].</li><li>Let <var>value</var> be ? HasProperty(<var>bindingObject</var>, <var>name</var>).</li><li>If <var>value</var> is <b>false</b>, then<ol><li>If <var>strict</var> is <b>false</b>, return <b>undefined</b>.</li><li>Throw a <b>ReferenceError</b> exception.</li></ol></li><li>Return ? Get(<var>bindingObject</var>, <var>name</var>).</li></ol></emu-alg>\n        ","1.1.2.7":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>an Object Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It can only delete bindings that correspond to properties of the environment object whose [[Configurable]] attribute have the value <b>true</b>.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>bindingObject</var> be <var>envRec</var>.[[BindingObject]].</li><li>Return ? <emu-meta effects=\"user-code\"><var>bindingObject</var>.[[Delete]](<var>name</var>)</emu-meta>.</li></ol></emu-alg>\n        ","1.1.2.8":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>an Object Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>Return <b>false</b>.</li></ol></emu-alg>\n          <emu-note>\n            <p>Object Environment Records do not provide a <code>this</code> binding.</p>\n          </emu-note>\n        ","1.1.2.9":"<p>The GetThisBinding concrete method of an Object Environment Record is never used within this specification.</p>\n        ","1.1.2.10":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>an Object Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>Return <b>false</b>.</li></ol></emu-alg>\n          <emu-note>\n            <p>Object Environment Records do not provide a <code>super</code> binding.</p>\n          </emu-note>\n        ","1.1.2.11":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>an Object Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>If <var>envRec</var>.[[IsWithEnvironment]] is <b>true</b>, return <var>envRec</var>.[[BindingObject]].</li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n        ","1.1.3":"<p>A <dfn variants=\"Function Environment Records\">Function Environment Record</dfn> is a Declarative Environment Record that is used to represent the top-level scope of a function and, if the function is not an <emu-nt>ArrowFunction</emu-nt>, provides a <code>this</code> binding. If a function is not an <emu-nt>ArrowFunction</emu-nt> function and references <code>super</code>, its Function Environment Record also contains the state that is used to perform <code>super</code> method invocations from within the function.</p>\n        <p>Function Environment Records have the additional state fields listed in table-additional-fields-of-function-environment-records.</p>\n        <emu-table id=\"table-additional-fields-of-function-environment-records\" caption=\"Additional Fields of Function Environment Records\" oldids=\"table-16\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Field Name\n                </th>\n                <th>\n                  Value\n                </th>\n                <th>\n                  Meaning\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                [[ThisValue]]\n              </td>\n              <td>\n                an ECMAScript language value\n              </td>\n              <td>\n                This is the <b>this</b> value used for this invocation of the function.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[ThisBindingStatus]]\n              </td>\n              <td>\n                <emu-const>lexical</emu-const>, <emu-const>initialized</emu-const>, or <emu-const>uninitialized</emu-const>\n              </td>\n              <td>\n                If the value is <emu-const>lexical</emu-const>, this is an <emu-nt>ArrowFunction</emu-nt> and does not have a local <b>this</b> value.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[FunctionObject]]\n              </td>\n              <td>\n                an ECMAScript function object\n              </td>\n              <td>\n                The function object whose invocation caused this Environment Record to be created.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[NewTarget]]\n              </td>\n              <td>\n                a constructor or <b>undefined</b>\n              </td>\n              <td>\n                If this Environment Record was created by the [[Construct]] internal method, [[NewTarget]] is the value of the [[Construct]] <var>newTarget</var> parameter. Otherwise, its value is <b>undefined</b>.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n        <p>Function Environment Records support all of the Declarative Environment Record methods listed in table-abstract-methods-of-environment-records and share the same specifications for all of those methods except for HasThisBinding, GetThisBinding, and HasSuperBinding.</p>\n\n        ","1.1.3.1":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It sets the <var>envRec</var>.[[ThisValue]] and records that it has been initialized.</dd>\n          </dl>\n          <emu-alg><ol><li>Assert: <var>envRec</var>.[[ThisBindingStatus]] is not <emu-const>lexical</emu-const>.</li><li>If <var>envRec</var>.[[ThisBindingStatus]] is <emu-const>initialized</emu-const>, throw a <b>ReferenceError</b> exception.</li><li>Set <var>envRec</var>.[[ThisValue]] to <var>value</var>.</li><li>Set <var>envRec</var>.[[ThisBindingStatus]] to <emu-const>initialized</emu-const>.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n        ","1.1.3.2":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Function Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>If <var>envRec</var>.[[ThisBindingStatus]] is <emu-const>lexical</emu-const>, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n        ","1.1.3.3":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Function Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>Assert: <var>envRec</var>.[[ThisBindingStatus]] is not <emu-const>lexical</emu-const>.</li><li>If <var>envRec</var>.[[ThisBindingStatus]] is <emu-const>uninitialized</emu-const>, throw a <b>ReferenceError</b> exception.</li><li>Return <var>envRec</var>.[[ThisValue]].</li></ol></emu-alg>\n        ","1.1.3.4":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Function Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>If <var>envRec</var>.[[ThisBindingStatus]] is <emu-const>lexical</emu-const>, return <b>false</b>.</li><li>If <var>envRec</var>.[[FunctionObject]].[[HomeObject]] is <b>undefined</b>, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n        ","1.1.3.5":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It returns the object that is the base for <code>super</code> property accesses bound in <var>envRec</var>. The value <b>undefined</b> indicates that such accesses will produce runtime errors.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>home</var> be <var>envRec</var>.[[FunctionObject]].[[HomeObject]].</li><li>If <var>home</var> is <b>undefined</b>, return <b>undefined</b>.</li><li>Assert: <var>home</var> is an ordinary object.</li><li>Return ! <var>home</var>.[[GetPrototypeOf]]().</li></ol></emu-alg>\n        ","1.1.4":"<p>A <dfn variants=\"Global Environment Records\">Global Environment Record</dfn> is used to represent the outer most scope that is shared by all of the ECMAScript <emu-nt>Script</emu-nt> elements that are processed in a common realm. A Global Environment Record provides the bindings for built-in globals (clause 19), properties of the global object, and for all top-level declarations (8.2.9, 8.2.11) that occur within a <emu-nt>Script</emu-nt>.</p>\n        <p>A Global Environment Record is logically a single record but it is specified as a composite encapsulating an Object Environment Record and a Declarative Environment Record. The Object Environment Record has as its base object the global object of the associated Realm Record. This global object is the value returned by the Global Environment Record's GetThisBinding concrete method. The Object Environment Record component of a Global Environment Record contains the bindings for all built-in globals (clause 19) and all bindings introduced by a <emu-nt>FunctionDeclaration</emu-nt>, <emu-nt>GeneratorDeclaration</emu-nt>, <emu-nt>AsyncFunctionDeclaration</emu-nt>, <emu-nt>AsyncGeneratorDeclaration</emu-nt>, or <emu-nt>VariableStatement</emu-nt> contained in global code. The bindings for all other ECMAScript declarations in global code are contained in the Declarative Environment Record component of the Global Environment Record.</p>\n        <p>Properties may be created directly on a global object. Hence, the Object Environment Record component of a Global Environment Record may contain both bindings created explicitly by <emu-nt>FunctionDeclaration</emu-nt>, <emu-nt>GeneratorDeclaration</emu-nt>, <emu-nt>AsyncFunctionDeclaration</emu-nt>, <emu-nt>AsyncGeneratorDeclaration</emu-nt>, or <emu-nt>VariableDeclaration</emu-nt> declarations and bindings created implicitly as properties of the global object.</p>\n        <p>Global Environment Records have the additional fields listed in table-additional-fields-of-global-environment-records.</p>\n        <emu-table id=\"table-additional-fields-of-global-environment-records\" caption=\"Additional Fields of Global Environment Records\" oldids=\"table-18\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Field Name\n                </th>\n                <th>\n                  Value\n                </th>\n                <th>\n                  Meaning\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                [[ObjectRecord]]\n              </td>\n              <td>\n                an Object Environment Record\n              </td>\n              <td>\n                Binding object is the global object. It contains global built-in bindings as well as <emu-nt>FunctionDeclaration</emu-nt>, <emu-nt>GeneratorDeclaration</emu-nt>, <emu-nt>AsyncFunctionDeclaration</emu-nt>, <emu-nt>AsyncGeneratorDeclaration</emu-nt>, and <emu-nt>VariableDeclaration</emu-nt> bindings in global code for the associated realm.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[GlobalThisValue]]\n              </td>\n              <td>\n                an Object\n              </td>\n              <td>\n                The value returned by <code>this</code> in global scope. Hosts may provide any ECMAScript Object value.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[DeclarativeRecord]]\n              </td>\n              <td>\n                a Declarative Environment Record\n              </td>\n              <td>\n                <emu-not-ref>Contains</emu-not-ref> bindings for all declarations in global code for the associated realm code except for <emu-nt>FunctionDeclaration</emu-nt>, <emu-nt>GeneratorDeclaration</emu-nt>, <emu-nt>AsyncFunctionDeclaration</emu-nt>, <emu-nt>AsyncGeneratorDeclaration</emu-nt>, and <emu-nt>VariableDeclaration</emu-nt> bindings.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n\n        ","1.1.4.1":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Global Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It determines if the argument identifier is one of the identifiers bound by the record.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>declarativeRecord</var> be <var>envRec</var>.[[DeclarativeRecord]].</li><li>If ! <var>declarativeRecord</var>.HasBinding(<var>name</var>) is <b>true</b>, return <b>true</b>.</li><li>Let <var>objectRecord</var> be <var>envRec</var>.[[ObjectRecord]].</li><li>Return ? <emu-meta effects=\"user-code\"><var>objectRecord</var>.HasBinding(<var>name</var>)</emu-meta>.</li></ol></emu-alg>\n        ","1.1.4.2":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Global Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It creates a new mutable binding for the name <var>name</var> that is uninitialized. The binding is created in the associated DeclarativeRecord. A binding for <var>name</var> must not already exist in the DeclarativeRecord. If <var>deletable</var> is <b>true</b>, the new binding is marked as being subject to deletion.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>declarativeRecord</var> be <var>envRec</var>.[[DeclarativeRecord]].</li><li>If ! <var>declarativeRecord</var>.HasBinding(<var>name</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Return ! <var>declarativeRecord</var>.CreateMutableBinding(<var>name</var>, <var>deletable</var>).</li></ol></emu-alg>\n        ","1.1.4.3":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Global Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It creates a new immutable binding for the name <var>name</var> that is uninitialized. A binding must not already exist in this Environment Record for <var>name</var>. If <var>strict</var> is <b>true</b>, the new binding is marked as a strict binding.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>declarativeRecord</var> be <var>envRec</var>.[[DeclarativeRecord]].</li><li>If ! <var>declarativeRecord</var>.HasBinding(<var>name</var>) is <b>true</b>, throw a <b>TypeError</b> exception.</li><li>Return ! <var>declarativeRecord</var>.CreateImmutableBinding(<var>name</var>, <var>strict</var>).</li></ol></emu-alg>\n        ","1.1.4.4":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Global Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It is used to set the bound value of the current binding of the identifier whose name is <var>name</var> to the value <var>value</var>. An uninitialized binding for <var>name</var> must already exist.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>declarativeRecord</var> be <var>envRec</var>.[[DeclarativeRecord]].</li><li>If ! <var>declarativeRecord</var>.HasBinding(<var>name</var>) is <b>true</b>, then<ol><li>Return ! <var>declarativeRecord</var>.InitializeBinding(<var>name</var>, <var>value</var>).</li></ol></li><li>Assert: If the binding exists, it must be in the Object Environment Record.</li><li>Let <var>objectRecord</var> be <var>envRec</var>.[[ObjectRecord]].</li><li>Return ? <emu-meta effects=\"user-code\"><var>objectRecord</var>.InitializeBinding(<var>name</var>, <var>value</var>)</emu-meta>.</li></ol></emu-alg>\n        ","1.1.4.5":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Global Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It attempts to change the bound value of the current binding of the identifier whose name is <var>name</var> to the value <var>value</var>. If the binding is an immutable binding and <var>strict</var> is <b>true</b>, a <b>TypeError</b> is thrown. A binding named <var>name</var> normally already exists but if it does not or is not currently writable, error handling is determined by <var>strict</var>.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>declarativeRecord</var> be <var>envRec</var>.[[DeclarativeRecord]].</li><li>If ! <var>declarativeRecord</var>.HasBinding(<var>name</var>) is <b>true</b>, then<ol><li>Return ? <var>declarativeRecord</var>.SetMutableBinding(<var>name</var>, <var>value</var>, <var>strict</var>).</li></ol></li><li>Let <var>objectRecord</var> be <var>envRec</var>.[[ObjectRecord]].</li><li>Return ? <emu-meta effects=\"user-code\"><var>objectRecord</var>.SetMutableBinding(<var>name</var>, <var>value</var>, <var>strict</var>)</emu-meta>.</li></ol></emu-alg>\n        ","1.1.4.6":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Global Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It returns the value of its bound identifier whose name is <var>name</var>. If the binding is an uninitialized binding, it will throw a <b>ReferenceError</b> exception. A binding named <var>name</var> normally already exists but if it does not or is not currently writable, error handling is determined by <var>strict</var>.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>declarativeRecord</var> be <var>envRec</var>.[[DeclarativeRecord]].</li><li>If ! <var>declarativeRecord</var>.HasBinding(<var>name</var>) is <b>true</b>, then<ol><li>Return ? <var>declarativeRecord</var>.GetBindingValue(<var>name</var>, <var>strict</var>).</li></ol></li><li>Let <var>objectRecord</var> be <var>envRec</var>.[[ObjectRecord]].</li><li>Return ? <emu-meta effects=\"user-code\"><var>objectRecord</var>.GetBindingValue(<var>name</var>, <var>strict</var>)</emu-meta>.</li></ol></emu-alg>\n        ","1.1.4.7":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Global Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It can only delete bindings that have been explicitly designated as being subject to deletion.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>declarativeRecord</var> be <var>envRec</var>.[[DeclarativeRecord]].</li><li>If ! <var>declarativeRecord</var>.HasBinding(<var>name</var>) is <b>true</b>, then<ol><li>Return ! <var>declarativeRecord</var>.DeleteBinding(<var>name</var>).</li></ol></li><li>Let <var>objectRecord</var> be <var>envRec</var>.[[ObjectRecord]].</li><li>Let <var>globalObject</var> be <var>objectRecord</var>.[[BindingObject]].</li><li>Let <var>existingProp</var> be ? HasOwnProperty(<var>globalObject</var>, <var>name</var>).</li><li>If <var>existingProp</var> is <b>true</b>, then<ol><li>Return ? <emu-meta effects=\"user-code\"><var>objectRecord</var>.DeleteBinding(<var>name</var>)</emu-meta>.</li></ol></li><li>Return <b>true</b>.</li></ol></emu-alg>\n        ","1.1.4.8":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Global Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>Return <b>true</b>.</li></ol></emu-alg>\n          <emu-note>\n            <p>Global Environment Records always provide a <code>this</code> binding.</p>\n          </emu-note>\n        ","1.1.4.9":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Global Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>Return <var>envRec</var>.[[GlobalThisValue]].</li></ol></emu-alg>\n        ","1.1.4.10":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Global Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>Return <b>false</b>.</li></ol></emu-alg>\n          <emu-note>\n            <p>Global Environment Records do not provide a <code>super</code> binding.</p>\n          </emu-note>\n        ","1.1.4.11":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Global Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>Return <b>undefined</b>.</li></ol></emu-alg>\n        ","1.1.4.12":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It determines if the argument identifier has a binding in <var>envRec</var> that was created using a lexical declaration such as a <emu-nt>LexicalDeclaration</emu-nt> or a <emu-nt>ClassDeclaration</emu-nt>.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>declarativeRecord</var> be <var>envRec</var>.[[DeclarativeRecord]].</li><li>Return ! <var>declarativeRecord</var>.HasBinding(<var>name</var>).</li></ol></emu-alg>\n        ","1.1.4.13":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It determines if the argument identifier is the name of a property of the global object that must not be shadowed by a global lexical binding.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>objectRecord</var> be <var>envRec</var>.[[ObjectRecord]].</li><li>Let <var>globalObject</var> be <var>objectRecord</var>.[[BindingObject]].</li><li>Let <var>existingProp</var> be ? <emu-meta effects=\"user-code\"><var>globalObject</var>.[[GetOwnProperty]](<var>name</var>)</emu-meta>.</li><li>If <var>existingProp</var> is <b>undefined</b>, return <b>false</b>.</li><li>If <var>existingProp</var>.[[Configurable]] is <b>true</b>, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n          <emu-note>\n            <p>Properties may exist upon a global object that were directly created rather than being declared using a var or function declaration. A global lexical binding may not be created that has the same name as a non-configurable property of the global object. The global property <b>\"undefined\"</b> is an example of such a property.</p>\n          </emu-note>\n        ","1.1.4.14":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It determines if a corresponding CreateGlobalVarBinding call would succeed if called for the same argument <var>name</var>. Redundant var declarations and var declarations for pre-existing global object properties are allowed.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>objectRecord</var> be <var>envRec</var>.[[ObjectRecord]].</li><li>Let <var>globalObject</var> be <var>objectRecord</var>.[[BindingObject]].</li><li>Let <var>hasProperty</var> be ? HasOwnProperty(<var>globalObject</var>, <var>name</var>).</li><li>If <var>hasProperty</var> is <b>true</b>, return <b>true</b>.</li><li>Return ? IsExtensible(<var>globalObject</var>).</li></ol></emu-alg>\n        ","1.1.4.15":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It determines if a corresponding CreateGlobalFunctionBinding call would succeed if called for the same argument <var>name</var>.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>objectRecord</var> be <var>envRec</var>.[[ObjectRecord]].</li><li>Let <var>globalObject</var> be <var>objectRecord</var>.[[BindingObject]].</li><li>Let <var>existingProp</var> be ? <emu-meta effects=\"user-code\"><var>globalObject</var>.[[GetOwnProperty]](<var>name</var>)</emu-meta>.</li><li>If <var>existingProp</var> is <b>undefined</b>, return ? IsExtensible(<var>globalObject</var>).</li><li>If <var>existingProp</var>.[[Configurable]] is <b>true</b>, return <b>true</b>.</li><li>If IsDataDescriptor(<var>existingProp</var>) is <b>true</b> and <var>existingProp</var> has attribute values { [[Writable]]: <b>true</b>, [[Enumerable]]: <b>true</b> }, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n        ","1.1.4.16":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It creates and initializes a mutable binding in the associated Object Environment Record. If a binding already exists, it is reused and assumed to be initialized.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>objectRecord</var> be <var>envRec</var>.[[ObjectRecord]].</li><li>Let <var>globalObject</var> be <var>objectRecord</var>.[[BindingObject]].</li><li>Let <var>hasProperty</var> be ? HasOwnProperty(<var>globalObject</var>, <var>name</var>).</li><li>Let <var>extensible</var> be ? IsExtensible(<var>globalObject</var>).</li><li>If <var>hasProperty</var> is <b>false</b> and <var>extensible</var> is <b>true</b>, then<ol><li>Perform ? <emu-meta effects=\"user-code\"><var>objectRecord</var>.CreateMutableBinding(<var>name</var>, <var>deletable</var>)</emu-meta>.</li><li>Perform ? <emu-meta effects=\"user-code\"><var>objectRecord</var>.InitializeBinding(<var>name</var>, <b>undefined</b>)</emu-meta>.</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n        ","1.1.4.17":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It creates and initializes a mutable binding in the associated Object Environment Record. If a binding already exists, it is replaced.</dd>\n          </dl>\n          <emu-alg><ol><li>Let <var>objectRecord</var> be <var>envRec</var>.[[ObjectRecord]].</li><li>Let <var>globalObject</var> be <var>objectRecord</var>.[[BindingObject]].</li><li>Let <var>existingProp</var> be ? <emu-meta effects=\"user-code\"><var>globalObject</var>.[[GetOwnProperty]](<var>name</var>)</emu-meta>.</li><li>If <var>existingProp</var> is <b>undefined</b> or <var>existingProp</var>.[[Configurable]] is <b>true</b>, then<ol><li>Let <var>desc</var> be the PropertyDescriptor { [[Value]]: <var>value</var>, [[Writable]]: <b>true</b>, [[Enumerable]]: <b>true</b>, [[Configurable]]: <var>deletable</var> }.</li></ol></li><li>Else,<ol><li>Let <var>desc</var> be the PropertyDescriptor { [[Value]]: <var>value</var> }.</li></ol></li><li>Perform ? DefinePropertyOrThrow(<var>globalObject</var>, <var>name</var>, <var>desc</var>).</li><li>Perform ? Set(<var>globalObject</var>, <var>name</var>, <var>value</var>, <b>false</b>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n          <emu-note>\n            <p>Global function declarations are always represented as own properties of the global object. If possible, an existing own property is reconfigured to have a standard set of attribute values. Step step-createglobalfunctionbinding-set is equivalent to what calling the InitializeBinding concrete method would do and if <var>globalObject</var> is a Proxy will produce the same sequence of Proxy trap calls.</p>\n          </emu-note>\n        ","1.1.5":"<p>A <dfn variants=\"Module Environment Records\">Module Environment Record</dfn> is a Declarative Environment Record that is used to represent the outer scope of an ECMAScript <emu-nt>Module</emu-nt>. In additional to normal mutable and immutable bindings, Module Environment Records also provide immutable import bindings which are bindings that provide indirect access to a target binding that exists in another Environment Record.</p>\n        <p>Module Environment Records support all of the Declarative Environment Record methods listed in table-abstract-methods-of-environment-records and share the same specifications for all of those methods except for GetBindingValue, DeleteBinding, HasThisBinding, and GetThisBinding.</p>\n\n        ","1.1.5.1":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Module Environment Record <var>envRec</var></dd>\n\n            <dt>description</dt>\n            <dd>It returns the value of its bound identifier whose name is <var>name</var>. However, if the binding is an indirect binding the value of the target binding is returned. If the binding exists but is uninitialized a <b>ReferenceError</b> is thrown.</dd>\n          </dl>\n          <emu-alg><ol><li>Assert: <var>strict</var> is <b>true</b>.</li><li>Assert: <var>envRec</var> has a binding for <var>name</var>.</li><li>If the binding for <var>name</var> is an indirect binding, then<ol><li>Let <var>module</var> and <var>targetName</var> be the indirection values provided when this binding for <var>name</var> was created.</li><li>Let <var>targetEnv</var> be <var>module</var>.[[Environment]].</li><li>If <var>targetEnv</var> is <emu-const>empty</emu-const>, throw a <b>ReferenceError</b> exception.</li><li>Return ? <emu-meta effects=\"user-code\"><var>targetEnv</var>.GetBindingValue(<var>targetName</var>, <b>true</b>)</emu-meta>.</li></ol></li><li>If the binding for <var>name</var> in <var>envRec</var> is an uninitialized binding, throw a <b>ReferenceError</b> exception.</li><li>Return the value currently bound to <var>name</var> in <var>envRec</var>.</li></ol></emu-alg>\n          <emu-note>\n            <p><var>strict</var> will always be <b>true</b> because a <emu-nt>Module</emu-nt> is always strict mode code.</p>\n          </emu-note>\n        ","1.1.5.2":"<p>The DeleteBinding concrete method of a Module Environment Record is never used within this specification.</p>\n          <emu-note>\n            <p>Module Environment Records are only used within strict code and an early error rule prevents the delete operator, in strict code, from being applied to a Reference Record that would resolve to a Module Environment Record binding. See 13.5.1.1.</p>\n          </emu-note>\n        ","1.1.5.3":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Module Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>Return <b>true</b>.</li></ol></emu-alg>\n          <emu-note>\n            <p>Module Environment Records always provide a <code>this</code> binding.</p>\n          </emu-note>\n        ","1.1.5.4":"<dl class=\"header\">\n            <dt>for</dt>\n            <dd>a Module Environment Record <var>envRec</var></dd>\n          </dl>\n          <emu-alg><ol><li>Return <b>undefined</b>.</li></ol></emu-alg>\n        ","1.1.5.5":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd>It creates a new initialized immutable indirect binding for the name <var>name</var>. A binding must not already exist in <var>envRec</var> for <var>name</var>. <var>targetName</var> is the name of a binding that exists in <var>targetModule</var>'s Module Environment Record. Accesses to the value of the new binding will indirectly access the bound value of the target binding.</dd>\n          </dl>\n          <emu-alg><ol><li>Assert: <var>envRec</var> does not already have a binding for <var>name</var>.</li><li>Assert: When <var>targetModule</var>.[[Environment]] is instantiated, it will have a direct binding for <var>targetName</var>.</li><li>Create an immutable indirect binding in <var>envRec</var> for <var>name</var> that references <var>targetModule</var> and <var>targetName</var> as its target binding and record that the binding is initialized.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n        ","1.2":"<p>The following abstract operations are used in this specification to operate upon Environment Records:</p>\n\n      ","1.2.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>If <var>env</var> is <b>null</b>, then<ol><li>Return the Reference Record { [[Base]]: <emu-const>unresolvable</emu-const>, [[ReferencedName]]: <var>name</var>, [[Strict]]: <var>strict</var>, [[ThisValue]]: <emu-const>empty</emu-const> }.</li></ol></li><li>Let <var>exists</var> be ? <emu-meta effects=\"user-code\"><var>env</var>.HasBinding(<var>name</var>)</emu-meta>.</li><li>If <var>exists</var> is <b>true</b>, then<ol><li>Return the Reference Record { [[Base]]: <var>env</var>, [[ReferencedName]]: <var>name</var>, [[Strict]]: <var>strict</var>, [[ThisValue]]: <emu-const>empty</emu-const> }.</li></ol></li><li>Let <var>outer</var> be <var>env</var>.[[OuterEnv]].</li><li>Return ? GetIdentifierReference(<var>outer</var>, <var>name</var>, <var>strict</var>).</li></ol></emu-alg>\n      ","1.2.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>env</var> be a new Declarative Environment Record containing no bindings.</li><li>Set <var>env</var>.[[OuterEnv]] to <var>outerEnv</var>.</li><li>Return <var>env</var>.</li></ol></emu-alg>\n      ","1.2.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>env</var> be a new Object Environment Record.</li><li>Set <var>env</var>.[[BindingObject]] to <var>obj</var>.</li><li>Set <var>env</var>.[[IsWithEnvironment]] to <var>isWithEnvironment</var>.</li><li>Set <var>env</var>.[[OuterEnv]] to <var>outerEnv</var>.</li><li>Return <var>env</var>.</li></ol></emu-alg>\n      ","1.2.4":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>env</var> be a new Function Environment Record containing no bindings.</li><li>Set <var>env</var>.[[FunctionObject]] to <var>func</var>.</li><li>If <var>func</var>.[[ThisMode]] is <emu-const>lexical</emu-const>, set <var>env</var>.[[ThisBindingStatus]] to <emu-const>lexical</emu-const>.</li><li>Else, set <var>env</var>.[[ThisBindingStatus]] to <emu-const>uninitialized</emu-const>.</li><li>Set <var>env</var>.[[NewTarget]] to <var>newTarget</var>.</li><li>Set <var>env</var>.[[OuterEnv]] to <var>func</var>.[[Environment]].</li><li>Return <var>env</var>.</li></ol></emu-alg>\n      ","1.2.5":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>objRec</var> be NewObjectEnvironment(<var>obj</var>, <b>false</b>, <b>null</b>).</li><li>Let <var>dclRec</var> be NewDeclarativeEnvironment(<b>null</b>).</li><li>Let <var>env</var> be a new Global Environment Record.</li><li>Set <var>env</var>.[[ObjectRecord]] to <var>objRec</var>.</li><li>Set <var>env</var>.[[GlobalThisValue]] to <var>thisValue</var>.</li><li>Set <var>env</var>.[[DeclarativeRecord]] to <var>dclRec</var>.</li><li>Set <var>env</var>.[[OuterEnv]] to <b>null</b>.</li><li>Return <var>env</var>.</li></ol></emu-alg>\n      ","1.2.6":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>env</var> be a new Module Environment Record containing no bindings.</li><li>Set <var>env</var>.[[OuterEnv]] to <var>outerEnv</var>.</li><li>Return <var>env</var>.</li></ol></emu-alg>\n      ","2.1":"<p>The following abstract operations are used in this specification to operate upon PrivateEnvironment Records:</p>\n\n      ","2.1.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>names</var> be a new empty List.</li><li>Return the PrivateEnvironment Record { [[OuterPrivateEnvironment]]: <var>outerPrivateEnv</var>, [[Names]]: <var>names</var> }.</li></ol></emu-alg>\n      ","2.1.2":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>names</var> be <var>privateEnv</var>.[[Names]].</li><li>For each Private Name <var>pn</var> of <var>names</var>, do<ol><li>If <var>pn</var>.[[Description]] is <var>identifier</var>, then<ol><li>Return <var>pn</var>.</li></ol></li></ol></li><li>Let <var>outerPrivateEnv</var> be <var>privateEnv</var>.[[OuterPrivateEnvironment]].</li><li>Assert: <var>outerPrivateEnv</var> is not <b>null</b>.</li><li>Return ResolvePrivateIdentifier(<var>outerPrivateEnv</var>, <var>identifier</var>).</li></ol></emu-alg>\n      ","3.1":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>realm</var> be a new Realm Record.</li><li>Perform CreateIntrinsics(<var>realm</var>).</li><li>Set <var>realm</var>.[[AgentSignifier]] to AgentSignifier().</li><li>Set <var>realm</var>.[[TemplateMap]] to a new empty List.</li><li>Let <var>newContext</var> be a new execution context.</li><li>Set the Function of <var>newContext</var> to <b>null</b>.</li><li>Set the Realm of <var>newContext</var> to <var>realm</var>.</li><li>Set the ScriptOrModule of <var>newContext</var> to <b>null</b>.</li><li>Push <var>newContext</var> onto the execution context stack; <var>newContext</var> is now the running execution context.</li><li>If the host requires use of a specific object to serve as <var>realm</var>'s global object, then<ol><li>Let <var>global</var> be such an object created in a host-defined manner.</li></ol></li><li>Else,<ol><li>Let <var>global</var> be OrdinaryObjectCreate(<var>realm</var>.[[Intrinsics]].[[<code class=\"emu-intrinsic\">%Object.prototype%</code>]]).</li></ol></li><li>If the host requires that the <code>this</code> binding in <var>realm</var>'s global scope return an object other than the global object, then<ol><li>Let <var>thisValue</var> be such an object created in a host-defined manner.</li></ol></li><li>Else,<ol><li>Let <var>thisValue</var> be <var>global</var>.</li></ol></li><li>Set <var>realm</var>.[[GlobalObject]] to <var>global</var>.</li><li>Set <var>realm</var>.[[GlobalEnv]] to NewGlobalEnvironment(<var>global</var>, <var>thisValue</var>).</li><li>Perform ? SetDefaultGlobalBindings(<var>realm</var>).</li><li>Create any host-defined global object properties on <var>global</var>.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    ","3.2":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Set <var>realmRec</var>.[[Intrinsics]] to a new Record.</li><li>Set fields of <var>realmRec</var>.[[Intrinsics]] with the values listed in table-well-known-intrinsic-objects. The field names are the names listed in the “Intrinsic Name” column of the table. The value of each field is a new object value fully and recursively populated with property values as defined by the specification of each object in clauses 19 through 28. All object property values are newly created object values. All values that are built-in function objects are created by performing CreateBuiltinFunction(<var>steps</var>, <var>length</var>, <var>name</var>, <var>slots</var>, <var>realmRec</var>, <var>prototype</var>, <var>async</var>) where <var>steps</var> is the definition of that function provided by this specification, <var>name</var> is the initial value of the function's <b>\"name\"</b> property, <var>length</var> is the initial value of the function's <b>\"length\"</b> property, <var>slots</var> is a list of the names, if any, of the function's specified internal slots, <var>prototype</var> is the specified value of the function's [[Prototype]] internal slot, and <var>async</var> is <b>true</b> if the function is described as “async” and <b>false</b> otherwise. The creation of the intrinsics and their properties must be ordered to avoid any dependencies upon objects that have not yet been created.</li><li>Perform AddRestrictedFunctionProperties(<var>realmRec</var>.[[Intrinsics]].[[<code class=\"emu-intrinsic\">%Function.prototype%</code>]], <var>realmRec</var>).</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    ","3.3":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>global</var> be <var>realmRec</var>.[[GlobalObject]].</li><li>For each property of the Global Object specified in clause 19, do<ol><li>Let <var>name</var> be the String value of the property name.</li><li>Let <var>desc</var> be the fully populated data Property Descriptor for the property, containing the specified attributes for the property. For properties listed in 19.2, 19.3, or 19.4 the value of the [[Value]] attribute is the corresponding intrinsic object from <var>realmRec</var>.</li><li>Perform ? DefinePropertyOrThrow(<var>global</var>, <var>name</var>, <var>desc</var>).</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n    ","4.1":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to determine the running script or module, based on the running execution context.</dd>\n      </dl>\n\n      <emu-alg><ol><li>If the execution context stack is empty, return <b>null</b>.</li><li>Let <var>ec</var> be the topmost execution context on the execution context stack whose ScriptOrModule component is not <b>null</b>.</li><li>If no such execution context exists, return <b>null</b>.</li><li>Return <var>ec</var>'s ScriptOrModule.</li></ol></emu-alg>\n    ","4.2":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It is used to determine the binding of <var>name</var>. <var>env</var> can be used to explicitly provide the Environment Record that is to be searched for the binding.</dd>\n      </dl>\n      <emu-alg><ol><li>If <var>env</var> is not present or <var>env</var> is <b>undefined</b>, then<ol><li>Set <var>env</var> to the running execution context's LexicalEnvironment.</li></ol></li><li>Assert: <var>env</var> is an Environment Record.</li><li>Let <var>strict</var> be IsStrict(the syntactic production that is being evaluated).</li><li>Return ? GetIdentifierReference(<var>env</var>, <var>name</var>, <var>strict</var>).</li></ol></emu-alg>\n      <emu-note>\n        <p>The result of ResolveBinding is always a Reference Record whose [[ReferencedName]] field is <var>name</var>.</p>\n      </emu-note>\n    ","4.3":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It finds the Environment Record that currently supplies the binding of the keyword <code>this</code>.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>env</var> be the running execution context's LexicalEnvironment.</li><li>Repeat,<ol><li>Let <var>exists</var> be <var>env</var>.HasThisBinding().</li><li>If <var>exists</var> is <b>true</b>, return <var>env</var>.</li><li>Let <var>outer</var> be <var>env</var>.[[OuterEnv]].</li><li>Assert: <var>outer</var> is not <b>null</b>.</li><li>Set <var>env</var> to <var>outer</var>.</li></ol></li></ol></emu-alg>\n      <emu-note>\n        <p>The loop in step step-getthisenvironment-loop will always terminate because the list of environments always ends with the global environment which has a <code>this</code> binding.</p>\n      </emu-note>\n    ","4.4":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It determines the binding of the keyword <code>this</code> using the LexicalEnvironment of the running execution context.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>envRec</var> be GetThisEnvironment().</li><li>Return ? <var>envRec</var>.GetThisBinding().</li></ol></emu-alg>\n    ","4.5":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It determines the NewTarget value using the LexicalEnvironment of the running execution context.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>envRec</var> be GetThisEnvironment().</li><li>Assert: <var>envRec</var> has a [[NewTarget]] field.</li><li>Return <var>envRec</var>.[[NewTarget]].</li></ol></emu-alg>\n    ","4.6":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It returns the global object used by the currently running execution context.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>currentRealm</var> be the current Realm Record.</li><li>Return <var>currentRealm</var>.[[GlobalObject]].</li></ol></emu-alg>\n    ","4.7":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It resumes <var>context</var> (sending it <var>completionRecord</var> as the resumption value), and waits for the result.</dd>\n      </dl>\n      <emu-alg><ol><li>Let <var>callerContext</var> be the running execution context.</li><li>Suspend <var>callerContext</var>.</li><li>Push <var>context</var> onto the execution context stack; <var>context</var> is now the running execution context.</li><li><emu-meta effects=\"user-code\">Resume the suspended evaluation of <var>context</var></emu-meta> using <var>completionRecord</var> as the result of the operation that suspended it. Let <var>result</var> be the Completion Record returned by the resumed computation.</li><li>Assert: When we reach this step, <var>context</var> has already been removed from the execution context stack and <var>callerContext</var> is the running execution context again.</li><li>Return Completion(<var>result</var>).</li></ol></emu-alg>\n    ","5.1":"<p>A <dfn variants=\"JobCallback Records\">JobCallback Record</dfn> is a Record value used to store a function object and a host-defined value. Function objects that are invoked via a Job enqueued by the host may have additional host-defined context. To propagate the state, Job Abstract Closures should not capture and call function objects directly. Instead, use HostMakeJobCallback and HostCallJobCallback.</p>\n      <emu-note>\n        <p>The WHATWG HTML specification (<a href=\"https://html.spec.whatwg.org/\">https://html.spec.whatwg.org/</a>), for example, uses the host-defined value to propagate the incumbent settings object for Promise callbacks.</p>\n      </emu-note>\n      <p>JobCallback Records have the fields listed in table-jobcallback-records.</p>\n      <emu-table id=\"table-jobcallback-records\" caption=\"JobCallback Record Fields\">\n        <table>\n          <thead>\n            <tr>\n              <th>\n                Field Name\n              </th>\n              <th>\n                Value\n              </th>\n              <th>\n                Meaning\n              </th>\n            </tr>\n          </thead>\n          <tr>\n            <td>\n              [[Callback]]\n            </td>\n            <td>\n              a function object\n            </td>\n            <td>\n              The function to invoke when the Job is invoked.\n            </td>\n          </tr>\n          <tr>\n            <td>\n              [[HostDefined]]\n            </td>\n            <td>\n              anything (default value is <emu-const>empty</emu-const>)\n            </td>\n            <td>\n              Field reserved for use by hosts.\n            </td>\n          </tr>\n        </table>\n      </emu-table>\n    ","5.2":"<dl class=\"header\">\n      </dl>\n      <p>An implementation of HostMakeJobCallback must conform to the following requirements:</p>\n      <ul>\n        <li>It must return a JobCallback Record whose [[Callback]] field is <var>callback</var>.</li>\n      </ul>\n      <p>The default implementation of HostMakeJobCallback performs the following steps when called:</p>\n      <emu-alg><ol><li>Return the JobCallback Record { [[Callback]]: <var>callback</var>, [[HostDefined]]: <emu-const>empty</emu-const> }.</li></ol></emu-alg>\n      <p>ECMAScript hosts that are not web browsers must use the default implementation of HostMakeJobCallback.</p>\n      <emu-note>\n        <p>This is called at the time that the callback is passed to the function that is responsible for its being eventually scheduled and run. For example, <code>promise.then(thenAction)</code> calls MakeJobCallback on <code>thenAction</code> at the time of invoking <code>Promise.prototype.then</code>, not at the time of scheduling the reaction Job.</p>\n      </emu-note>\n    ","5.3":"<dl class=\"header\">\n      </dl>\n      <p>An implementation of HostCallJobCallback must conform to the following requirements:</p>\n      <ul>\n        <li>It must perform and return the result of Call(<var>jobCallback</var>.[[Callback]], <var>value</var>, <var>argumentsList</var>).</li>\n      </ul>\n      <emu-note>\n        <p>This requirement means that hosts cannot change the [[Call]] behaviour of function objects defined in this specification.</p>\n      </emu-note>\n      <p>The default implementation of HostCallJobCallback performs the following steps when called:</p>\n      <emu-alg><ol><li>Assert: IsCallable(<var>jobCallback</var>.[[Callback]]) is <b>true</b>.</li><li>Return ? Call(<var>jobCallback</var>.[[Callback]], <var>value</var>, <var>argumentsList</var>).</li></ol></emu-alg>\n      <p>ECMAScript hosts that are not web browsers must use the default implementation of HostCallJobCallback.</p>\n    ","5.4":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It schedules <var>job</var> in the realm <var>realm</var> in the agent signified by <var>realm</var>.[[AgentSignifier]] to be performed at some future time. The Abstract Closures used with this algorithm are intended to be scheduled without additional constraints, such as priority and ordering.</dd>\n      </dl>\n      <p>An implementation of HostEnqueueGenericJob must conform to the requirements in 9.5.</p>\n    ","5.5":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It schedules <var>job</var> to be performed at some future time. The Abstract Closures used with this algorithm are intended to be related to the handling of Promises, or otherwise, to be scheduled with equal priority to Promise handling operations.</dd>\n      </dl>\n\n      <p>An implementation of HostEnqueuePromiseJob must conform to the requirements in 9.5 as well as the following:</p>\n      <ul>\n        <li>If <var>realm</var> is not <b>null</b>, each time <var>job</var> is invoked the implementation must perform implementation-defined steps such that execution is prepared to evaluate ECMAScript code at the time of <var>job</var>'s invocation.</li>\n        <li>Let <var>scriptOrModule</var> be GetActiveScriptOrModule() at the time HostEnqueuePromiseJob is invoked. If <var>realm</var> is not <b>null</b>, each time <var>job</var> is invoked the implementation must perform implementation-defined steps such that <var>scriptOrModule</var> is the active script or module at the time of <var>job</var>'s invocation.</li>\n        <li>Jobs must run in the same order as the HostEnqueuePromiseJob invocations that scheduled them.</li>\n      </ul>\n\n      <emu-note>\n        <p>The <var>realm</var> for Jobs returned by NewPromiseResolveThenableJob is usually the result of calling GetFunctionRealm on the <var>then</var> function object. The <var>realm</var> for Jobs returned by NewPromiseReactionJob is usually the result of calling GetFunctionRealm on the handler if the handler is not <b>undefined</b>. If the handler is <b>undefined</b>, <var>realm</var> is <b>null</b>. For both kinds of Jobs, when GetFunctionRealm completes abnormally (i.e. called on a revoked Proxy), <var>realm</var> is the current Realm Record at the time of the GetFunctionRealm call. When the <var>realm</var> is <b>null</b>, no user ECMAScript code will be evaluated and no new ECMAScript objects (e.g. Error objects) will be created. The WHATWG HTML specification (<a href=\"https://html.spec.whatwg.org/\">https://html.spec.whatwg.org/</a>), for example, uses <var>realm</var> to check for the ability to run script and for the <a href=\"https://html.spec.whatwg.org/#entry\">entry</a> concept.</p>\n      </emu-note>\n    ","5.6":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It schedules <var>timeoutJob</var> in the realm <var>realm</var> in the agent signified by <var>realm</var>.[[AgentSignifier]] to be performed after at least <var>milliseconds</var> milliseconds.</dd>\n      </dl>\n      <p>An implementation of HostEnqueueTimeoutJob must conform to the requirements in 9.5.</p>\n    ","6.1":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Return <var>agentRecord</var>.[[Signifier]].</li></ol></emu-alg>\n    ","6.2":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Return <var>agentRecord</var>.[[CanBlock]].</li></ol></emu-alg>\n      <emu-note>\n        <p>In some environments it may not be reasonable for a given agent to suspend. For example, in a web browser environment, it may be reasonable to disallow suspending a document's main event handling thread, while still allowing workers' event handling threads to suspend.</p>\n      </emu-note>\n    ","6.3":"<dl class=\"header\">\n      </dl>\n      <emu-alg><ol><li>Let <var>agentRecord</var> be the Agent Record of the surrounding agent.</li><li>Let <var>count</var> be <var>agentRecord</var>.[[ModuleAsyncEvaluationCount]].</li><li>Set <var>agentRecord</var>.[[ModuleAsyncEvaluationCount]] to <var>count</var> + 1.</li><li>Return <var>count</var>.</li></ol></emu-alg>\n      <emu-note>\n        <p>This value is only used to keep track of the relative evaluation order between pending modules. An implementation may unobservably reset [[ModuleAsyncEvaluationCount]] to 0 whenever there are no pending modules.</p>\n      </emu-note>\n    ","9.1":"<p>This specification does not make any guarantees that any object or symbol will be garbage collected. Objects or symbols which are not live may be released after long periods of time, or never at all. For this reason, this specification uses the term \"may\" when describing behaviour triggered by garbage collection.</p>\n\n      <p>The semantics of WeakRefs and FinalizationRegistrys is based on two operations which happen at particular points in time:</p>\n\n      <ul>\n        <li>\n          When <code>WeakRef.prototype.deref</code> is called, the referent (if <b>undefined</b> is not returned) is kept alive so that subsequent, synchronous accesses also return the same value. This list is reset when synchronous work is done using the ClearKeptObjects abstract operation.\n        </li>\n\n        <li>\n          When an object or symbol which is registered with a FinalizationRegistry becomes unreachable, a call of the FinalizationRegistry's cleanup callback may eventually be made, after synchronous ECMAScript execution completes. The FinalizationRegistry cleanup is performed with the CleanupFinalizationRegistry abstract operation.\n        </li>\n      </ul>\n\n      <p>Neither of these actions (ClearKeptObjects or CleanupFinalizationRegistry) may interrupt synchronous ECMAScript execution. Because hosts may assemble longer, synchronous ECMAScript execution runs, this specification defers the scheduling of ClearKeptObjects and CleanupFinalizationRegistry to the host environment.</p>\n\n      <p>Some ECMAScript implementations include garbage collector implementations which run in the background, including when ECMAScript is idle. Letting the host environment schedule CleanupFinalizationRegistry allows it to resume ECMAScript execution in order to run finalizer work, which may free up held values, reducing overall memory usage.</p>\n    ","9.2":"<p>For some set of objects and/or symbols <var>objectSet</var> a <dfn>hypothetical WeakRef-oblivious</dfn> execution with respect to <var>objectSet</var> is an execution whereby the abstract operation WeakRefDeref of a WeakRef whose referent is an element of <var>objectSet</var> always returns <b>undefined</b>.</p>\n\n      <emu-note>\n        WeakRef-obliviousness, together with liveness, capture two notions. One, that a WeakRef itself does not keep its referent alive. Two, that cycles in liveness does not imply that a value is live. To be concrete, if determining <var>v</var>'s liveness depends on determining the liveness of a WeakRef referent, <var>r</var>, <var>r</var>'s liveness cannot assume <var>v</var>'s liveness, which would be circular reasoning.\n      </emu-note>\n      <emu-note>\n        WeakRef-obliviousness is defined on sets of objects or symbols instead of individual values to account for cycles. If it were defined on individual values, then a WeakRef referent in a cycle will be considered live even though its identity is only observed via other WeakRef referents in the cycle.\n      </emu-note>\n      <emu-note>\n        Colloquially, we say that an individual object or symbol is live if every set containing it is live.\n      </emu-note>\n\n      <p>At any point during evaluation, a set of objects and/or symbols <var>objectSet</var> is considered <dfn>live</dfn> if either of the following conditions is met:</p>\n\n      <ul>\n        <li>\n          Any element in <var>objectSet</var> is included in any agent's [[KeptAlive]] List.\n        </li>\n        <li>\n          There exists a valid future hypothetical WeakRef-oblivious execution with respect to <var>objectSet</var> that observes the identity of any value in <var>objectSet</var>.\n        </li>\n      </ul>\n      <emu-note>\n        The second condition above intends to capture the intuition that a value is live if its identity is observable via non-WeakRef means. A value's identity may be observed by observing a strict equality comparison or observing the value being used as key in a Map.\n      </emu-note>\n      <emu-note>\n        <p>Presence of an object or a symbol in a field, an internal slot, or a property does not imply that the value is live. For example if the value in question is never passed back to the program, then it cannot be observed.</p>\n\n        <p>This is the case for keys in a WeakMap, members of a WeakSet, as well as the [[WeakRefTarget]] and [[UnregisterToken]] fields of a FinalizationRegistry Cell record.</p>\n\n        <p>The above definition implies that, if a key in a WeakMap is not live, then its corresponding value is not necessarily live either.</p>\n      </emu-note>\n      <emu-note>\n        Liveness is the lower bound for guaranteeing which WeakRefs engines must not empty. Liveness as defined here is undecidable. In practice, engines use conservative approximations such as reachability. There is expected to be significant implementation leeway.\n      </emu-note>\n    ","9.3":"<p>At any time, if a set of objects and/or symbols <var>objectSet</var> is not live, an ECMAScript implementation may perform the following steps atomically:</p>\n\n      <emu-alg><ol><li>For each element <var>value</var> of <var>objectSet</var>, do<ol><li>For each WeakRef <var>ref</var> such that <var>ref</var>.[[WeakRefTarget]] is <var>value</var>, do<ol><li>Set <var>ref</var>.[[WeakRefTarget]] to <emu-const>empty</emu-const>.</li></ol></li><li>For each FinalizationRegistry <var>fg</var> such that <var>fg</var>.[[Cells]] contains a Record <var>cell</var> such that <var>cell</var>.[[WeakRefTarget]] is <var>value</var>, do<ol><li>Set <var>cell</var>.[[WeakRefTarget]] to <emu-const>empty</emu-const>.</li><li>Optionally, perform HostEnqueueFinalizationRegistryCleanupJob(<var>fg</var>).</li></ol></li><li>For each WeakMap <var>map</var> such that <var>map</var>.[[WeakMapData]] contains a Record <var>r</var> such that <var>r</var>.[[Key]] is <var>value</var>, do<ol><li>Set <var>r</var>.[[Key]] to <emu-const>empty</emu-const>.</li><li>Set <var>r</var>.[[Value]] to <emu-const>empty</emu-const>.</li></ol></li><li>For each WeakSet <var>set</var> such that <var>set</var>.[[WeakSetData]] contains <var>value</var>, do<ol><li>Replace the element of <var>set</var>.[[WeakSetData]] whose value is <var>value</var> with an element whose value is <emu-const>empty</emu-const>.</li></ol></li></ol></li></ol></emu-alg>\n\n      <emu-note>\n        <p>Together with the definition of liveness, this clause prescribes optimizations that an implementation may apply regarding WeakRefs.</p>\n\n        <p>It is possible to access an object without observing its identity. Optimizations such as dead variable elimination and scalar replacement on properties of non-escaping objects whose identity is not observed are allowed. These optimizations are thus allowed to observably empty WeakRefs that point to such objects.</p>\n\n        <p>On the other hand, if an object's identity is observable, and that object is in the [[WeakRefTarget]] internal slot of a WeakRef, optimizations such as rematerialization that observably empty the WeakRef are prohibited.</p>\n\n        <p>Because calling HostEnqueueFinalizationRegistryCleanupJob is optional, registered objects in a FinalizationRegistry do not necessarily hold that FinalizationRegistry live. Implementations may omit FinalizationRegistry callbacks for any reason, e.g., if the FinalizationRegistry itself becomes dead, or if the application is shutting down.</p>\n      </emu-note>\n      <emu-note>\n        <p>Implementations are not obligated to empty WeakRefs for maximal sets of non-live objects or symbols.</p>\n        <p>If an implementation chooses a non-live set <var>objectSet</var> in which to empty WeakRefs, this definition requires that it empties WeakRefs for all values in <var>objectSet</var> simultaneously. In other words, it is not conformant for an implementation to empty a WeakRef pointing to a value <var>v</var> without emptying out other WeakRefs that, if not emptied, could result in an execution that observes the value of <var>v</var>.</p>\n      </emu-note>\n    ","9.4":"","9.4.1":"<dl class=\"header\">\n        </dl>\n        <p>Let <var>cleanupJob</var> be a new Job Abstract Closure with no parameters that captures <var>finalizationRegistry</var> and performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>cleanupResult</var> be Completion(CleanupFinalizationRegistry(<var>finalizationRegistry</var>)).</li><li>If <var>cleanupResult</var> is an abrupt completion, perform any host-defined steps for reporting the error.</li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n        <p>An implementation of HostEnqueueFinalizationRegistryCleanupJob schedules <var>cleanupJob</var> to be performed at some future time, if possible. It must also conform to the requirements in 9.5.</p>\n      "};
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
