// Generated from ecma262/spec.html — do not edit by hand.
const sections = {"1":"","2":"","3":"","4":"","5":"<p>Instances of Error objects are thrown as exceptions when runtime errors occur. The Error objects may also serve as base objects for user-defined exception classes.</p>\n    <p>When an ECMAScript implementation detects a runtime error, it throws a new instance of one of the <var>NativeError</var> objects defined in <a href=\"/fundamental-objects#sec-native-error-types-used-in-this-standard\">20.5.5</a> or a new instance of the AggregateError object defined in <a href=\"/fundamental-objects#sec-aggregate-error-objects\">20.5.7</a>.</p>\n\n    ","":"","1.1":"<p>The Object constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Object%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"Object\"</b> property of the global object.</li>\n        <li>creates a new ordinary object when called as a constructor.</li>\n        <li>performs a type conversion when called as a function rather than as a constructor.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition.</li>\n      </ul>\n\n      ","1.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is neither <b>undefined</b> nor the active function object, then<ol><li>Return ? OrdinaryCreateFromConstructor(NewTarget, <b>\"<code class=\"emu-intrinsic\">%Object.prototype%</code>\"</b>).</li></ol></li><li>If <var>value</var> is either <b>undefined</b> or <b>null</b>, return OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Object.prototype%</code>).</li><li>Return ! ToObject(<var>value</var>).</li></ol></emu-alg>\n      ","1.2":"<p>The Object constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li>has the following additional properties:</li>\n      </ul>\n\n      ","1.2.1":"<p>This function copies the values of all of the enumerable own properties from one or more source objects to a <var>target</var> object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>to</var> be ? ToObject(<var>target</var>).</li><li>If only one argument was passed, return <var>to</var>.</li><li>For each element <var>nextSource</var> of <var>sources</var>, do<ol><li>If <var>nextSource</var> is neither <b>undefined</b> nor <b>null</b>, then<ol><li>Let <var>from</var> be ! ToObject(<var>nextSource</var>).</li><li>Let <var>keys</var> be ? <emu-meta effects=\"user-code\"><var>from</var>.[[OwnPropertyKeys]]()</emu-meta>.</li><li>For each element <var>nextKey</var> of <var>keys</var>, do<ol><li>Let <var>desc</var> be ? <emu-meta effects=\"user-code\"><var>from</var>.[[GetOwnProperty]](<var>nextKey</var>)</emu-meta>.</li><li>If <var>desc</var> is not <b>undefined</b> and <var>desc</var>.[[Enumerable]] is <b>true</b>, then<ol><li>Let <var>propValue</var> be ? Get(<var>from</var>, <var>nextKey</var>).</li><li>Perform ? Set(<var>to</var>, <var>nextKey</var>, <var>propValue</var>, <b>true</b>).</li></ol></li></ol></li></ol></li></ol></li><li>Return <var>to</var>.</li></ol></emu-alg>\n        <p>The <b>\"length\"</b> property of this function is <b>2</b><sub>𝔽</sub>.</p>\n      ","1.2.2":"<p>This function creates a new object with a specified prototype.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>proto</var> is not an Object and <var>proto</var> is not <b>null</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>obj</var> be OrdinaryObjectCreate(<var>proto</var>).</li><li>If <var>properties</var> is not <b>undefined</b>, then<ol><li>Return ? ObjectDefineProperties(<var>obj</var>, <var>properties</var>).</li></ol></li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","1.2.3":"<p>This function adds own properties and/or updates the attributes of existing own properties of an object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Return ? ObjectDefineProperties(<var>obj</var>, <var>properties</var>).</li></ol></emu-alg>\n\n        ","1.2.3.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>props</var> be ? ToObject(<var>properties</var>).</li><li>Let <var>keys</var> be ? <emu-meta effects=\"user-code\"><var>props</var>.[[OwnPropertyKeys]]()</emu-meta>.</li><li>Let <var>descriptors</var> be a new empty List.</li><li>For each element <var>nextKey</var> of <var>keys</var>, do<ol><li>Let <var>propDesc</var> be ? <emu-meta effects=\"user-code\"><var>props</var>.[[GetOwnProperty]](<var>nextKey</var>)</emu-meta>.</li><li>If <var>propDesc</var> is not <b>undefined</b> and <var>propDesc</var>.[[Enumerable]] is <b>true</b>, then<ol><li>Let <var>descObj</var> be ? Get(<var>props</var>, <var>nextKey</var>).</li><li>Let <var>desc</var> be ? ToPropertyDescriptor(<var>descObj</var>).</li><li>Append the Record { [[Key]]: <var>nextKey</var>, [[Descriptor]]: <var>desc</var> } to <var>descriptors</var>.</li></ol></li></ol></li><li>For each element <var>property</var> of <var>descriptors</var>, do<ol><li>Perform ? DefinePropertyOrThrow(<var>obj</var>, <var>property</var>.[[Key]], <var>property</var>.[[Descriptor]]).</li></ol></li><li>Return <var>obj</var>.</li></ol></emu-alg>\n        ","1.2.4":"<p>This function adds an own property and/or updates the attributes of an existing own property of an object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>key</var> be ? ToPropertyKey(<var>propertyKey</var>).</li><li>Let <var>desc</var> be ? ToPropertyDescriptor(<var>attributes</var>).</li><li>Perform ? DefinePropertyOrThrow(<var>obj</var>, <var>key</var>, <var>desc</var>).</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","1.2.5":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>coerced</var> be ? ToObject(<var>obj</var>).</li><li>Let <var>entryList</var> be ? EnumerableOwnProperties(<var>coerced</var>, <emu-const>key+value</emu-const>).</li><li>Return CreateArrayFromList(<var>entryList</var>).</li></ol></emu-alg>\n      ","1.2.6":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>obj</var> is not an Object, return <var>obj</var>.</li><li>Let <var>status</var> be ? SetIntegrityLevel(<var>obj</var>, <emu-const>frozen</emu-const>).</li><li>If <var>status</var> is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","1.2.7":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Perform ? RequireObjectCoercible(<var>iterable</var>).</li><li>Let <var>obj</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Object.prototype%</code>).</li><li>Assert: <var>obj</var> is an extensible ordinary object with no own properties.</li><li>Let <var>closure</var> be a new Abstract Closure with parameters (<var>key</var>, <var>value</var>) that captures <var>obj</var> and performs the following steps when called:<ol><li>Let <var>propertyKey</var> be ? ToPropertyKey(<var>key</var>).</li><li>Perform ! CreateDataPropertyOrThrow(<var>obj</var>, <var>propertyKey</var>, <var>value</var>).</li><li>Return NormalCompletion(<b>undefined</b>).</li></ol></li><li>Let <var>adder</var> be CreateBuiltinFunction(<var>closure</var>, 2, <b>\"\"</b>, « »).</li><li>Return ? AddEntriesFromIterable(<var>obj</var>, <var>iterable</var>, <var>adder</var>).</li></ol></emu-alg>\n        <emu-note>\n          The function created for <var>adder</var> is never directly accessible to ECMAScript code.\n        </emu-note>\n      ","1.2.8":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>coerced</var> be ? ToObject(<var>obj</var>).</li><li>Let <var>key</var> be ? ToPropertyKey(<var>propertyKey</var>).</li><li>Let <var>desc</var> be ? <emu-meta effects=\"user-code\"><var>coerced</var>.[[GetOwnProperty]](<var>key</var>)</emu-meta>.</li><li>Return FromPropertyDescriptor(<var>desc</var>).</li></ol></emu-alg>\n      ","1.2.9":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>coerced</var> be ? ToObject(<var>obj</var>).</li><li>Let <var>ownKeys</var> be ? <emu-meta effects=\"user-code\"><var>coerced</var>.[[OwnPropertyKeys]]()</emu-meta>.</li><li>Let <var>descriptors</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%Object.prototype%</code>).</li><li>For each element <var>key</var> of <var>ownKeys</var>, do<ol><li>Let <var>desc</var> be ? <emu-meta effects=\"user-code\"><var>coerced</var>.[[GetOwnProperty]](<var>key</var>)</emu-meta>.</li><li>Let <var>descriptor</var> be FromPropertyDescriptor(<var>desc</var>).</li><li>If <var>descriptor</var> is not <b>undefined</b>, perform ! CreateDataPropertyOrThrow(<var>descriptors</var>, <var>key</var>, <var>descriptor</var>).</li></ol></li><li>Return <var>descriptors</var>.</li></ol></emu-alg>\n      ","1.2.10":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Return CreateArrayFromList(? GetOwnPropertyKeys(<var>obj</var>, <emu-const>string</emu-const>)).</li></ol></emu-alg>\n      ","1.2.11":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Return CreateArrayFromList(? GetOwnPropertyKeys(<var>obj</var>, <emu-const>symbol</emu-const>)).</li></ol></emu-alg>\n\n        ","1.2.11.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>obj</var> be ? ToObject(<var>value</var>).</li><li>Let <var>keys</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[OwnPropertyKeys]]()</emu-meta>.</li><li>Let <var>nameList</var> be a new empty List.</li><li>For each element <var>nextKey</var> of <var>keys</var>, do<ol><li>If <var>nextKey</var> is a Symbol and <var>type</var> is <emu-const>symbol</emu-const>, or if <var>nextKey</var> is a String and <var>type</var> is <emu-const>string</emu-const>, then<ol><li>Append <var>nextKey</var> to <var>nameList</var>.</li></ol></li></ol></li><li>Return <var>nameList</var>.</li></ol></emu-alg>\n        ","1.2.12":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>coerced</var> be ? ToObject(<var>obj</var>).</li><li>Return ? <emu-meta effects=\"user-code\"><var>coerced</var>.[[GetPrototypeOf]]()</emu-meta>.</li></ol></emu-alg>\n      ","1.2.13":"<emu-note>\n          <p><var>callback</var> should be a function that accepts two arguments. <code>groupBy</code> calls <var>callback</var> once for each element in <var>items</var>, in ascending order, and constructs a new object. Each value returned by <var>callback</var> is coerced to a property key. For each such property key, the result object has a property whose key is that property key and whose value is an array containing all the elements for which the <var>callback</var> return value coerced to that key.</p>\n          <p><var>callback</var> is called with two arguments: the value of the element and the index of the element.</p>\n          <p>The return value of <code>groupBy</code> is an object that does not inherit from <code class=\"emu-intrinsic\">%Object.prototype%</code>.</p>\n        </emu-note>\n        <p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>groups</var> be ? GroupBy(<var>items</var>, <var>callback</var>, <emu-const>property</emu-const>).</li><li>Let <var>obj</var> be OrdinaryObjectCreate(<b>null</b>).</li><li>For each Record { [[Key]], [[Elements]] } <var>g</var> of <var>groups</var>, do<ol><li>Let <var>elements</var> be CreateArrayFromList(<var>g</var>.[[Elements]]).</li><li>Perform ! CreateDataPropertyOrThrow(<var>obj</var>, <var>g</var>.[[Key]], <var>elements</var>).</li></ol></li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","1.2.14":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>coerced</var> be ? ToObject(<var>obj</var>).</li><li>Let <var>key</var> be ? ToPropertyKey(<var>propertyKey</var>).</li><li>Return ? HasOwnProperty(<var>coerced</var>, <var>key</var>).</li></ol></emu-alg>\n      ","1.2.15":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Return SameValue(<var>value1</var>, <var>value2</var>).</li></ol></emu-alg>\n      ","1.2.16":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>obj</var> is not an Object, return <b>false</b>.</li><li>Return ? IsExtensible(<var>obj</var>).</li></ol></emu-alg>\n      ","1.2.17":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>obj</var> is not an Object, return <b>true</b>.</li><li>Return ? TestIntegrityLevel(<var>obj</var>, <emu-const>frozen</emu-const>).</li></ol></emu-alg>\n      ","1.2.18":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>obj</var> is not an Object, return <b>true</b>.</li><li>Return ? TestIntegrityLevel(<var>obj</var>, <emu-const>sealed</emu-const>).</li></ol></emu-alg>\n      ","1.2.19":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>coerced</var> be ? ToObject(<var>obj</var>).</li><li>Let <var>keyList</var> be ? EnumerableOwnProperties(<var>coerced</var>, <emu-const>key</emu-const>).</li><li>Return CreateArrayFromList(<var>keyList</var>).</li></ol></emu-alg>\n      ","1.2.20":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>obj</var> is not an Object, return <var>obj</var>.</li><li>Let <var>status</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[PreventExtensions]]()</emu-meta>.</li><li>If <var>status</var> is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","1.2.21":"<p>The initial value of <code>Object.prototype</code> is the Object prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","1.2.22":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>obj</var> is not an Object, return <var>obj</var>.</li><li>Let <var>status</var> be ? SetIntegrityLevel(<var>obj</var>, <emu-const>sealed</emu-const>).</li><li>If <var>status</var> is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","1.2.23":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Perform ? RequireObjectCoercible(<var>obj</var>).</li><li>If <var>proto</var> is not an Object and <var>proto</var> is not <b>null</b>, throw a <b>TypeError</b> exception.</li><li>If <var>obj</var> is not an Object, return <var>obj</var>.</li><li>Let <var>status</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[SetPrototypeOf]](<var>proto</var>)</emu-meta>.</li><li>If <var>status</var> is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","1.2.24":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>coerced</var> be ? ToObject(<var>obj</var>).</li><li>Let <var>valueList</var> be ? EnumerableOwnProperties(<var>coerced</var>, <emu-const>value</emu-const>).</li><li>Return CreateArrayFromList(<var>valueList</var>).</li></ol></emu-alg>\n      ","1.3":"<p>The <dfn>Object prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Object.prototype%</code></dfn>.</li>\n        <li>has an [[Extensible]] internal slot whose value is <b>true</b>.</li>\n        <li>has the internal methods defined for ordinary objects, except for the [[SetPrototypeOf]] method, which is as defined in <a href=\"/ordinary-and-exotic-objects-behaviours#sec-immutable-prototype-exotic-objects-setprototypeof-v\">10.4.7.1</a>. (Thus, it is an immutable prototype exotic object.)</li>\n        <li>has a [[Prototype]] internal slot whose value is <b>null</b>.</li>\n      </ul>\n\n      ","1.3.1":"<p>The initial value of <code>Object.prototype.constructor</code> is <code class=\"emu-intrinsic\">%Object%</code>.</p>\n      ","1.3.2":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>propertyKey</var> be ? ToPropertyKey(<var>value</var>).</li><li>Let <var>obj</var> be ? ToObject(<b>this</b> value).</li><li>Return ? HasOwnProperty(<var>obj</var>, <var>propertyKey</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>The ordering of steps step-hasownproperty-topropertykey and step-hasownproperty-toobject is chosen to ensure that any exception that would have been thrown by step step-hasownproperty-topropertykey in previous editions of this specification will continue to be thrown even if the <b>this</b> value is <b>undefined</b> or <b>null</b>.</p>\n        </emu-note>\n      ","1.3.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>value</var> is not an Object, return <b>false</b>.</li><li>Let <var>obj</var> be ? ToObject(<b>this</b> value).</li><li>Repeat,<ol><li>Set <var>value</var> to ? <emu-meta effects=\"user-code\"><var>value</var>.[[GetPrototypeOf]]()</emu-meta>.</li><li>If <var>value</var> is <b>null</b>, return <b>false</b>.</li><li>If SameValue(<var>obj</var>, <var>value</var>) is <b>true</b>, return <b>true</b>.</li></ol></li></ol></emu-alg>\n        <emu-note>\n          <p>The ordering of steps step-isprototypeof-check-object and step-isprototypeof-toobject preserves the behaviour specified by previous editions of this specification for the case where <var>value</var> is not an object and the <b>this</b> value is <b>undefined</b> or <b>null</b>.</p>\n        </emu-note>\n      ","1.3.4":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>propertyKey</var> be ? ToPropertyKey(<var>value</var>).</li><li>Let <var>obj</var> be ? ToObject(<b>this</b> value).</li><li>Let <var>desc</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[GetOwnProperty]](<var>propertyKey</var>)</emu-meta>.</li><li>If <var>desc</var> is <b>undefined</b>, return <b>false</b>.</li><li>Return <var>desc</var>.[[Enumerable]].</li></ol></emu-alg>\n        <emu-note>\n          <p>This method does not consider objects in the prototype chain.</p>\n        </emu-note>\n        <emu-note>\n          <p>The ordering of steps step-propertyisenumerable-topropertykey and step-propertyisenumerable-toobject is chosen to ensure that any exception that would have been thrown by step step-propertyisenumerable-topropertykey in previous editions of this specification will continue to be thrown even if the <b>this</b> value is <b>undefined</b> or <b>null</b>.</p>\n        </emu-note>\n      ","1.3.5":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Return ? Invoke(<var>thisValue</var>, <b>\"toString\"</b>).</li></ol></emu-alg>\n        <p>The optional parameters to this method are not used but are intended to correspond to the parameter pattern used by ECMA-402 <code>toLocaleString</code> methods. Implementations that do not include ECMA-402 support must not use those parameter positions for other purposes.</p>\n        <emu-note>\n          <p>This method provides a generic <code>toLocaleString</code> implementation for objects that have no locale-sensitive <code>toString</code> behaviour. <code>Array</code>, <code>Number</code>, <code>Date</code>, and <code class=\"emu-intrinsic\">%TypedArray%</code> provide their own locale-sensitive <code>toLocaleString</code> methods.</p>\n        </emu-note>\n        <emu-note>\n          <p>ECMA-402 intentionally does not provide an alternative to this default implementation.</p>\n        </emu-note>\n      ","1.3.6":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>If the <b>this</b> value is <b>undefined</b>, return <b>\"[object Undefined]\"</b>.</li><li>If the <b>this</b> value is <b>null</b>, return <b>\"[object Null]\"</b>.</li><li>Let <var>obj</var> be ! ToObject(<b>this</b> value).</li><li>Let <var>isArray</var> be ? IsArray(<var>obj</var>).</li><li>If <var>isArray</var> is <b>true</b>, let <var>builtinTag</var> be <b>\"Array\"</b>.</li><li>Else if <var>obj</var> has a [[ParameterMap]] internal slot, let <var>builtinTag</var> be <b>\"Arguments\"</b>.</li><li>Else if <var>obj</var> has a [[Call]] internal method, let <var>builtinTag</var> be <b>\"Function\"</b>.</li><li>Else if <var>obj</var> has an [[ErrorData]] internal slot, let <var>builtinTag</var> be <b>\"Error\"</b>.</li><li>Else if <var>obj</var> has a [[BooleanData]] internal slot, let <var>builtinTag</var> be <b>\"Boolean\"</b>.</li><li>Else if <var>obj</var> has a [[NumberData]] internal slot, let <var>builtinTag</var> be <b>\"Number\"</b>.</li><li>Else if <var>obj</var> has a [[StringData]] internal slot, let <var>builtinTag</var> be <b>\"String\"</b>.</li><li>Else if <var>obj</var> has a [[DateValue]] internal slot, let <var>builtinTag</var> be <b>\"Date\"</b>.</li><li>Else if <var>obj</var> has a [[RegExpMatcher]] internal slot, let <var>builtinTag</var> be <b>\"RegExp\"</b>.</li><li>Else, let <var>builtinTag</var> be <b>\"Object\"</b>.</li><li>Let <var>tag</var> be ? Get(<var>obj</var>, <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code>).</li><li>If <var>tag</var> is not a String, set <var>tag</var> to <var>builtinTag</var>.</li><li>Return the string-concatenation of <b>\"[object \"</b>, <var>tag</var>, and <b>\"]\"</b>.</li></ol></emu-alg>\n        <emu-note>\n          <p>Historically, this method was occasionally used to access the String value of the [[Class]] internal slot that was used in previous editions of this specification as a nominal type tag for various built-in objects. The above definition of <code>toString</code> preserves compatibility for legacy code that uses <code>toString</code> as a test for those specific kinds of built-in objects. It does not provide a reliable type testing mechanism for other kinds of built-in or program defined objects. In addition, programs can use <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> in ways that will invalidate the reliability of such legacy type tests.</p>\n        </emu-note>\n      ","1.3.7":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Return ? ToObject(<b>this</b> value).</li></ol></emu-alg>\n      ","1.3.8":"<p><code>Object.prototype.__proto__</code> is an accessor property with attributes { [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }. The [[Get]] and [[Set]] attributes are defined as follows:</p>\n\n        ","1.3.8.1":"<p>The value of the [[Get]] attribute is a built-in function that requires no arguments. It performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be ? ToObject(<b>this</b> value).</li><li>Return ? <emu-meta effects=\"user-code\"><var>obj</var>.[[GetPrototypeOf]]()</emu-meta>.</li></ol></emu-alg>\n        ","1.3.8.2":"<p>The value of the [[Set]] attribute is a built-in function that takes an argument <var>proto</var>. It performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Perform ? RequireObjectCoercible(<var>thisValue</var>).</li><li>If <var>proto</var> is not an Object and <var>proto</var> is not <b>null</b>, return <b>undefined</b>.</li><li>If <var>thisValue</var> is not an Object, return <b>undefined</b>.</li><li>Let <var>status</var> be ? <emu-meta effects=\"user-code\"><var>thisValue</var>.[[SetPrototypeOf]](<var>proto</var>)</emu-meta>.</li><li>If <var>status</var> is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n        ","1.3.9":"","1.3.9.1":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be ? ToObject(<b>this</b> value).</li><li>If IsCallable(<var>getter</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>desc</var> be PropertyDescriptor { [[Get]]: <var>getter</var>, [[Enumerable]]: <b>true</b>, [[Configurable]]: <b>true</b> }.</li><li>Let <var>key</var> be ? ToPropertyKey(<var>propertyKey</var>).</li><li>Perform ? DefinePropertyOrThrow(<var>obj</var>, <var>key</var>, <var>desc</var>).</li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n        ","1.3.9.2":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be ? ToObject(<b>this</b> value).</li><li>If IsCallable(<var>setter</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>desc</var> be PropertyDescriptor { [[Set]]: <var>setter</var>, [[Enumerable]]: <b>true</b>, [[Configurable]]: <b>true</b> }.</li><li>Let <var>key</var> be ? ToPropertyKey(<var>propertyKey</var>).</li><li>Perform ? DefinePropertyOrThrow(<var>obj</var>, <var>key</var>, <var>desc</var>).</li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n        ","1.3.9.3":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be ? ToObject(<b>this</b> value).</li><li>Let <var>key</var> be ? ToPropertyKey(<var>propertyKey</var>).</li><li>Repeat,<ol><li>Let <var>desc</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[GetOwnProperty]](<var>key</var>)</emu-meta>.</li><li>If <var>desc</var> is not <b>undefined</b>, then<ol><li>If IsAccessorDescriptor(<var>desc</var>) is <b>true</b>, return <var>desc</var>.[[Get]].</li><li>Return <b>undefined</b>.</li></ol></li><li>Set <var>obj</var> to ? <emu-meta effects=\"user-code\"><var>obj</var>.[[GetPrototypeOf]]()</emu-meta>.</li><li>If <var>obj</var> is <b>null</b>, return <b>undefined</b>.</li></ol></li></ol></emu-alg>\n        ","1.3.9.4":"<p>This method performs the following steps when called:</p>\n          <emu-alg><ol><li>Let <var>obj</var> be ? ToObject(<b>this</b> value).</li><li>Let <var>key</var> be ? ToPropertyKey(<var>propertyKey</var>).</li><li>Repeat,<ol><li>Let <var>desc</var> be ? <emu-meta effects=\"user-code\"><var>obj</var>.[[GetOwnProperty]](<var>key</var>)</emu-meta>.</li><li>If <var>desc</var> is not <b>undefined</b>, then<ol><li>If IsAccessorDescriptor(<var>desc</var>) is <b>true</b>, return <var>desc</var>.[[Set]].</li><li>Return <b>undefined</b>.</li></ol></li><li>Set <var>obj</var> to ? <emu-meta effects=\"user-code\"><var>obj</var>.[[GetPrototypeOf]]()</emu-meta>.</li><li>If <var>obj</var> is <b>null</b>, return <b>undefined</b>.</li></ol></li></ol></emu-alg>\n        ","1.4":"<p>Object instances have no special properties beyond those inherited from the Object prototype object.</p>\n    ","2.1":"<p>The Function constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Function%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"Function\"</b> property of the global object.</li>\n        <li>creates and initializes a new function object when called as a function rather than as a constructor. Thus the function call <code>Function(…)</code> is equivalent to the object creation expression <code>new Function(…)</code> with the same arguments.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified Function behaviour must include a <code>super</code> call to the Function constructor to create and initialize a subclass instance with the internal slots necessary for built-in function behaviour. All ECMAScript syntactic forms for defining function objects create instances of Function. There is no syntactic means to create instances of Function subclasses except for the built-in GeneratorFunction, AsyncFunction, and AsyncGeneratorFunction subclasses.</li>\n      </ul>\n\n      ","2.1.1":"<p>The last argument (if any) specifies the body (executable code) of a function; any preceding arguments specify formal parameters.</p>\n        <p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>constructor</var> be the active function object.</li><li>If <var>bodyArg</var> is not present, set <var>bodyArg</var> to the empty String.</li><li>Return ? CreateDynamicFunction(<var>constructor</var>, NewTarget, <emu-const>normal</emu-const>, <var>parameterArgs</var>, <var>bodyArg</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>It is permissible but not necessary to have one argument for each formal parameter to be specified. For example, all three of the following expressions produce the same result:</p>\n          <pre><code class=\"javascript\">\n            new Function(\"a\", \"b\", \"c\", \"return a+b+c\")\n            new Function(\"a, b, c\", \"return a+b+c\")\n            new Function(\"a,b\", \"c\", \"return a+b+c\")\n          </code></pre>\n        </emu-note>\n\n        ","2.1.1.1":"<dl class=\"header\">\n            <dt>description</dt>\n            <dd><var>constructor</var> is the constructor function that is performing this action. <var>newTarget</var> is the constructor that <code>new</code> was initially applied to. <var>parameterArgs</var> and <var>bodyArg</var> reflect the argument values that were passed to <var>constructor</var>.</dd>\n          </dl>\n          <emu-alg><ol><li>If <var>newTarget</var> is <b>undefined</b>, set <var>newTarget</var> to <var>constructor</var>.</li><li>If <var>kind</var> is <emu-const>normal</emu-const>, then<ol><li>Let <var>prefix</var> be <b>\"function\"</b>.</li><li>Let <var>exprSym</var> be the grammar symbol <emu-nt>FunctionExpression</emu-nt>.</li><li>Let <var>bodySym</var> be the grammar symbol <emu-nt>FunctionBody[<emu-const>Yield, </emu-const>Await]</emu-nt>.</li><li>Let <var>parameterSym</var> be the grammar symbol <emu-nt>FormalParameters[<emu-const>Yield, </emu-const>Await]</emu-nt>.</li><li>Let <var>fallbackProto</var> be <b>\"<code class=\"emu-intrinsic\">%Function.prototype%</code>\"</b>.</li></ol></li><li>Else if <var>kind</var> is <emu-const>generator</emu-const>, then<ol><li>Let <var>prefix</var> be <b>\"function\\</b>\"*.</li><li>Let <var>exprSym</var> be the grammar symbol <emu-nt>GeneratorExpression</emu-nt>.</li><li>Let <var>bodySym</var> be the grammar symbol <emu-nt>GeneratorBody</emu-nt>.</li><li>Let <var>parameterSym</var> be the grammar symbol <emu-nt>FormalParameters[+Yield, ~Await]</emu-nt>.</li><li>Let <var>fallbackProto</var> be <b>\"<code class=\"emu-intrinsic\">%GeneratorFunction.prototype%</code>\"</b>.</li></ol></li><li>Else if <var>kind</var> is <emu-const>async</emu-const>, then<ol><li>Let <var>prefix</var> be <b>\"async function\"</b>.</li><li>Let <var>exprSym</var> be the grammar symbol <emu-nt>AsyncFunctionExpression</emu-nt>.</li><li>Let <var>bodySym</var> be the grammar symbol <emu-nt>AsyncFunctionBody</emu-nt>.</li><li>Let <var>parameterSym</var> be the grammar symbol <emu-nt>FormalParameters[~Yield, +Await]</emu-nt>.</li><li>Let <var>fallbackProto</var> be <b>\"<code class=\"emu-intrinsic\">%AsyncFunction.prototype%</code>\"</b>.</li></ol></li><li>Else,<ol><li>Assert: <var>kind</var> is <emu-const>async-generator</emu-const>.</li><li>Let <var>prefix</var> be <b>\"async function\\</b>\"*.</li><li>Let <var>exprSym</var> be the grammar symbol <emu-nt>AsyncGeneratorExpression</emu-nt>.</li><li>Let <var>bodySym</var> be the grammar symbol <emu-nt>AsyncGeneratorBody</emu-nt>.</li><li>Let <var>parameterSym</var> be the grammar symbol <emu-nt>FormalParameters[+Yield, +Await]</emu-nt>.</li><li>Let <var>fallbackProto</var> be <b>\"<code class=\"emu-intrinsic\">%AsyncGeneratorFunction.prototype%</code>\"</b>.</li></ol></li><li>Let <var>argCount</var> be the number of elements in <var>parameterArgs</var>.</li><li>Let <var>parameterStrings</var> be a new empty List.</li><li>For each element <var>arg</var> of <var>parameterArgs</var>, do<ol><li>Append ? ToString(<var>arg</var>) to <var>parameterStrings</var>.</li></ol></li><li>Let <var>bodyString</var> be ? ToString(<var>bodyArg</var>).</li><li>Let <var>currentRealm</var> be the current Realm Record.</li><li>Perform ? HostEnsureCanCompileStrings(<var>currentRealm</var>, <var>parameterStrings</var>, <var>bodyString</var>, <b>false</b>).</li><li>Let <var>parameterString</var> be the empty String.</li><li>If <var>argCount</var> > 0, then<ol><li>Set <var>parameterString</var> to <var>parameterStrings</var>[0].</li><li>Let <var>k</var> be 1.</li><li>Repeat, while <var>k</var> &lt; <var>argCount</var>,<ol><li>Let <var>nextArgString</var> be <var>parameterStrings</var>[<var>k</var>].</li><li>Set <var>parameterString</var> to the string-concatenation of <var>parameterString</var>, <b>\",\"</b> (a comma), and <var>nextArgString</var>.</li><li>Set <var>k</var> to <var>k</var> + 1.</li></ol></li></ol></li><li>Let <var>bodyParseString</var> be the string-concatenation of 0x000A (LINE FEED), <var>bodyString</var>, and 0x000A (LINE FEED).</li><li>Let <var>sourceString</var> be the string-concatenation of <var>prefix</var>, <b>\" anonymous(\"</b>, <var>parameterString</var>, 0x000A (LINE FEED), <b>\") {\"</b>, <var>bodyParseString</var>, and <b>\"}\"</b>.</li><li>Let <var>sourceText</var> be StringToCodePoints(<var>sourceString</var>).</li><li>Let <var>parameters</var> be ParseText(<var>parameterString</var>, <var>parameterSym</var>).</li><li>If <var>parameters</var> is a List of errors, throw a <b>SyntaxError</b> exception.</li><li>Let <var>body</var> be ParseText(<var>bodyParseString</var>, <var>bodySym</var>).</li><li>If <var>body</var> is a List of errors, throw a <b>SyntaxError</b> exception.</li><li>NOTE: The parameters and body are parsed separately to ensure that each is valid alone. For example, <code>new Function(\"/<b>\", \"</b>/ ) {\")</code> does not evaluate to a function.</li><li>NOTE: If this step is reached, <var>sourceText</var> must have the syntax of <var>exprSym</var> (although the reverse implication does not hold). The purpose of the next two steps is to enforce any Early Error rules which apply to <var>exprSym</var> directly.</li><li>Let <var>expr</var> be ParseText(<var>sourceText</var>, <var>exprSym</var>).</li><li>If <var>expr</var> is a List of errors, throw a <b>SyntaxError</b> exception.</li><li>Let <var>proto</var> be ? GetPrototypeFromConstructor(<var>newTarget</var>, <var>fallbackProto</var>).</li><li>Let <var>env</var> be <var>currentRealm</var>.[[GlobalEnv]].</li><li>Let <var>privateEnv</var> be <b>null</b>.</li><li>Let <var>func</var> be OrdinaryFunctionCreate(<var>proto</var>, <var>sourceText</var>, <var>parameters</var>, <var>body</var>, <emu-const>non-lexical-this</emu-const>, <var>env</var>, <var>privateEnv</var>).</li><li>Perform SetFunctionName(<var>func</var>, <b>\"anonymous\"</b>).</li><li>If <var>kind</var> is <emu-const>generator</emu-const>, then<ol><li>Let <var>prototype</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%GeneratorPrototype%</code>).</li><li>Perform ! DefinePropertyOrThrow(<var>func</var>, <b>\"prototype\"</b>, PropertyDescriptor { [[Value]]: <var>prototype</var>, [[Writable]]: <b>true</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }).</li></ol></li><li>Else if <var>kind</var> is <emu-const>async-generator</emu-const>, then<ol><li>Let <var>prototype</var> be OrdinaryObjectCreate(<code class=\"emu-intrinsic\">%AsyncGeneratorPrototype%</code>).</li><li>Perform ! DefinePropertyOrThrow(<var>func</var>, <b>\"prototype\"</b>, PropertyDescriptor { [[Value]]: <var>prototype</var>, [[Writable]]: <b>true</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }).</li></ol></li><li>Else if <var>kind</var> is <emu-const>normal</emu-const>, then<ol><li>Perform MakeConstructor(<var>func</var>).</li></ol></li><li>NOTE: Functions whose <var>kind</var> is <emu-const>async</emu-const> are not constructable and do not have a [[Construct]] internal method or a <b>\"prototype\"</b> property.</li><li>Return <var>func</var>.</li></ol></emu-alg>\n          <emu-note>\n            <p>CreateDynamicFunction defines a <b>\"prototype\"</b> property on any function it creates whose <var>kind</var> is not <emu-const>async</emu-const> to provide for the possibility that the function will be used as a constructor.</p>\n          </emu-note>\n        ","2.2":"<p>The Function constructor:</p>\n      <ul>\n        <li>is itself a built-in function object.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li oldids=\"sec-function.length\">has a <b>\"length\"</b> property whose value is <b>1</b><sub>𝔽</sub>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","2.2.1":"<p>The value of <code>Function.prototype</code> is the Function prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","2.3":"<p>The <dfn>Function prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Function.prototype%</code></dfn>.</li>\n        <li>is itself a built-in function object.</li>\n        <li>accepts any arguments and returns <b>undefined</b> when invoked.</li>\n        <li>does not have a [[Construct]] internal method; it cannot be used as a constructor with the <code>new</code> operator.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n        <li>does not have a <b>\"prototype\"</b> property.</li>\n        <li>has a <b>\"length\"</b> property whose value is <b>+0</b><sub>𝔽</sub>.</li>\n        <li>has a <b>\"name\"</b> property whose value is the empty String.</li>\n      </ul>\n      <emu-note>\n        <p>The Function prototype object is specified to be a function object to ensure compatibility with ECMAScript code that was created prior to the ECMAScript 2015 specification.</p>\n      </emu-note>\n\n      ","2.3.1":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>func</var> be the <b>this</b> value.</li><li>If IsCallable(<var>func</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>If <var>argArray</var> is either <b>undefined</b> or <b>null</b>, then<ol><li>Perform PrepareForTailCall().</li><li>Return ? Call(<var>func</var>, <var>thisArg</var>).</li></ol></li><li>Let <var>argList</var> be ? CreateListFromArrayLike(<var>argArray</var>).</li><li>Perform PrepareForTailCall().</li><li>Return ? Call(<var>func</var>, <var>thisArg</var>, <var>argList</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>The <var>thisArg</var> value is passed without modification as the <b>this</b> value. This is a change from Edition 3, where an <b>undefined</b> or <b>null</b> <var>thisArg</var> is replaced with the global object and ToObject is applied to all other values and that result is passed as the <b>this</b> value. Even though the <var>thisArg</var> is passed without modification, non-strict functions still perform these transformations upon entry to the function.</p>\n        </emu-note>\n        <emu-note>\n          <p>If <var>func</var> is either an arrow function or a bound function exotic object, then the <var>thisArg</var> will be ignored by the function [[Call]] in step step-function-proto-apply-call.</p>\n        </emu-note>\n      ","2.3.2":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>target</var> be the <b>this</b> value.</li><li>If IsCallable(<var>target</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Let <var>boundFunc</var> be ? BoundFunctionCreate(<var>target</var>, <var>thisArg</var>, <var>args</var>).</li><li>Let <var>length</var> be 0.</li><li>Let <var>targetHasLength</var> be ? HasOwnProperty(<var>target</var>, <b>\"length\"</b>).</li><li>If <var>targetHasLength</var> is <b>true</b>, then<ol><li>Let <var>targetLen</var> be ? Get(<var>target</var>, <b>\"length\"</b>).</li><li>If <var>targetLen</var> is a Number, then<ol><li>If <var>targetLen</var> is <b>+∞</b><sub>𝔽</sub>, then<ol><li>Set <var>length</var> to +∞.</li></ol></li><li>Else if <var>targetLen</var> is <b>-∞</b><sub>𝔽</sub>, then<ol><li>Set <var>length</var> to 0.</li></ol></li><li>Else,<ol><li>Let <var>targetLenAsInt</var> be ! ToIntegerOrInfinity(<var>targetLen</var>).</li><li>Assert: <var>targetLenAsInt</var> is finite.</li><li>Let <var>argCount</var> be the number of elements in <var>args</var>.</li><li>Set <var>length</var> to max(<var>targetLenAsInt</var> - <var>argCount</var>, 0).</li></ol></li></ol></li></ol></li><li>Perform SetFunctionLength(<var>boundFunc</var>, <var>length</var>).</li><li>Let <var>targetName</var> be ? Get(<var>target</var>, <b>\"name\"</b>).</li><li>If <var>targetName</var> is not a String, set <var>targetName</var> to the empty String.</li><li>Perform SetFunctionName(<var>boundFunc</var>, <var>targetName</var>, <b>\"bound\"</b>).</li><li>Return <var>boundFunc</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>Function objects created using <code>Function.prototype.bind</code> are exotic objects. They also do not have a <b>\"prototype\"</b> property.</p>\n        </emu-note>\n        <emu-note>\n          <p>If <var>target</var> is either an arrow function or a bound function exotic object, then the <var>thisArg</var> passed to this method will not be used by subsequent calls to <var>func</var>.</p>\n        </emu-note>\n      ","2.3.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>func</var> be the <b>this</b> value.</li><li>If IsCallable(<var>func</var>) is <b>false</b>, throw a <b>TypeError</b> exception.</li><li>Perform PrepareForTailCall().</li><li>Return ? Call(<var>func</var>, <var>thisArg</var>, <var>args</var>).</li></ol></emu-alg>\n        <emu-note>\n          <p>The <var>thisArg</var> value is passed without modification as the <b>this</b> value. This is a change from Edition 3, where an <b>undefined</b> or <b>null</b> <var>thisArg</var> is replaced with the global object and ToObject is applied to all other values and that result is passed as the <b>this</b> value. Even though the <var>thisArg</var> is passed without modification, non-strict functions still perform these transformations upon entry to the function.</p>\n        </emu-note>\n        <emu-note>\n          <p>If <var>func</var> is either an arrow function or a bound function exotic object, then the <var>thisArg</var> will be ignored by the function [[Call]] in step step-function-proto-call-call.</p>\n        </emu-note>\n      ","2.3.4":"<p>The initial value of <code>Function.prototype.constructor</code> is <code class=\"emu-intrinsic\">%Function%</code>.</p>\n      ","2.3.5":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>func</var> be the <b>this</b> value.</li><li>If <var>func</var> is an Object, <var>func</var> has a [[SourceText]] internal slot, <var>func</var>.[[SourceText]] is a sequence of Unicode code points, and HostHasSourceTextAvailable(<var>func</var>) is <b>true</b>, then<ol><li>Return CodePointsToString(<var>func</var>.[[SourceText]]).</li></ol></li><li>If <var>func</var> is a <a href=\"/ordinary-and-exotic-objects-behaviours#sec-built-in-function-objects\">built-in function object</a>, return an implementation-defined String source code representation of <var>func</var>. The representation must have the syntax of a <emu-nt>NativeFunction</emu-nt>. Additionally, if <var>func</var> has an [[InitialName]] internal slot and <var>func</var>.[[InitialName]] is a String, the portion of the returned String that would be matched by <emu-nt>NativeFunctionAccessor?</emu-nt> <emu-nt>PropertyName</emu-nt> must be <var>func</var>.[[InitialName]].</li><li>If <var>func</var> is an Object and IsCallable(<var>func</var>) is <b>true</b>, return an implementation-defined String source code representation of <var>func</var>. The representation must have the syntax of a <emu-nt>NativeFunction</emu-nt>.</li><li>Throw a <b>TypeError</b> exception.</li></ol></emu-alg>\n\n        <pre class=\"emu-grammar\">NativeFunction :\n  `function` NativeFunctionAccessor? PropertyName[~Yield, ~Await]? `(` FormalParameters[~Yield, ~Await] `)` `{` `[` `native` `code` `]` `}`\n\nNativeFunctionAccessor :\n  `get`\n  `set`</pre>\n      ","2.3.6":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>thisValue</var> be the <b>this</b> value.</li><li>Return ? OrdinaryHasInstance(<var>thisValue</var>, <var>value</var>).</li></ol></emu-alg>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n        <emu-note>\n          <p>This is the default implementation of <code><code class=\"emu-intrinsic\">%Symbol.hasInstance%</code></code> that most functions inherit. <code><code class=\"emu-intrinsic\">%Symbol.hasInstance%</code></code> is called by the <code>instanceof</code> operator to determine whether a value is an instance of a specific constructor. An expression such as</p>\n          <pre><code class=\"javascript\">\n            v instanceof F\n          </code></pre>\n          <p>evaluates as</p>\n          <pre><code class=\"javascript\">\n            F[%Symbol.hasInstance%](v)\n          </code></pre>\n          <p>A constructor function can control which objects are recognized as its instances by <code>instanceof</code> by exposing a different <code><code class=\"emu-intrinsic\">%Symbol.hasInstance%</code></code> method on the function.</p>\n        </emu-note>\n        <p>This property is non-writable and non-configurable to prevent tampering that could be used to globally expose the target function of a bound function.</p>\n        <p>The value of the <b>\"name\"</b> property of this method is <b>\"[Symbol.hasInstance]\"</b>.</p>\n      ","2.4":"<p>Every Function instance is an ECMAScript function object and has the internal slots listed in table-internal-slots-of-ecmascript-function-objects. Function objects created using the <code>Function.prototype.bind</code> method (<a href=\"/fundamental-objects#sec-function.prototype.bind\">20.2.3.2</a>) have the internal slots listed in table-internal-slots-of-bound-function-exotic-objects.</p>\n      <p>Function instances have the following properties:</p>\n\n      ","2.4.1":"<p>The value of the <b>\"length\"</b> property is an integral Number that indicates the typical number of arguments expected by the function. However, the language permits the function to be invoked with some other number of arguments. The behaviour of a function when invoked on a number of arguments other than the number specified by its <b>\"length\"</b> property depends on the function. This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","2.4.2":"<p>The value of the <b>\"name\"</b> property is a String that is descriptive of the function. The name has no semantic significance but is typically a variable or property name that is used to refer to the function at its point of definition in ECMAScript source text. This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n        <p>Anonymous function objects that do not have a contextual name associated with them by this specification use the empty String as the value of the <b>\"name\"</b> property.</p>\n      ","2.4.3":"<p>Function instances that can be used as a constructor have a <b>\"prototype\"</b> property. Whenever such a Function instance is created another ordinary object is also created and is the initial value of the function's <b>\"prototype\"</b> property. Unless otherwise specified, the value of the <b>\"prototype\"</b> property is used to initialize the [[Prototype]] internal slot of the object created when that function is invoked as a constructor.</p>\n        <p>This property has the attributes { [[Writable]]: <b>true</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n        <emu-note>\n          <p>Function objects created using <code>Function.prototype.bind</code>, or by evaluating a <emu-nt>MethodDefinition</emu-nt> (that is not a <emu-nt>GeneratorMethod</emu-nt> or <emu-nt>AsyncGeneratorMethod</emu-nt>) or an <emu-nt>ArrowFunction</emu-nt> do not have a <b>\"prototype\"</b> property.</p>\n        </emu-note>\n      ","2.5":"<dl class=\"header\">\n        <dt>description</dt>\n        <dd>It allows host environments to prevent the source text from being provided for <var>func</var>.</dd>\n      </dl>\n      <p>An implementation of HostHasSourceTextAvailable must conform to the following requirements:</p>\n      <ul>\n        <li>It must be deterministic with respect to its parameters. Each time it is called with a specific <var>func</var> as its argument, it must return the same result.</li>\n      </ul>\n      <p>The default implementation of HostHasSourceTextAvailable is to return <b>true</b>.</p>\n    ","3.1":"<p>The Boolean constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Boolean%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"Boolean\"</b> property of the global object.</li>\n        <li>creates and initializes a new Boolean object when called as a constructor.</li>\n        <li>performs a type conversion when called as a function rather than as a constructor.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified Boolean behaviour must include a <code>super</code> call to the Boolean constructor to create and initialize the subclass instance with a [[BooleanData]] internal slot.</li>\n      </ul>\n\n      ","3.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>b</var> be ToBoolean(<var>value</var>).</li><li>If NewTarget is <b>undefined</b>, return <var>b</var>.</li><li>Let <var>obj</var> be ? OrdinaryCreateFromConstructor(NewTarget, <b>\"<code class=\"emu-intrinsic\">%Boolean.prototype%</code>\"</b>, « [[BooleanData]] »).</li><li>Set <var>obj</var>.[[BooleanData]] to <var>b</var>.</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","3.2":"<p>The Boolean constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","3.2.1":"<p>The initial value of <code>Boolean.prototype</code> is the Boolean prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","3.3":"<p>The <dfn>Boolean prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Boolean.prototype%</code></dfn>.</li>\n        <li>is an ordinary object.</li>\n        <li>is itself a Boolean object; it has a [[BooleanData]] internal slot with the value <b>false</b>.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n      </ul>\n\n      ","3.3.1":"<p>The initial value of <code>Boolean.prototype.constructor</code> is <code class=\"emu-intrinsic\">%Boolean%</code>.</p>\n      ","3.3.2":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>b</var> be ? ThisBooleanValue(<b>this</b> value).</li><li>If <var>b</var> is <b>true</b>, return <b>\"true\"</b>.</li><li>Return <b>\"false\"</b>.</li></ol></emu-alg>\n      ","3.3.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Return ? ThisBooleanValue(<b>this</b> value).</li></ol></emu-alg>\n\n        ","3.3.3.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>value</var> is a Boolean, return <var>value</var>.</li><li>If <var>value</var> is an Object and <var>value</var> has a [[BooleanData]] internal slot, then<ol><li>Let <var>b</var> be <var>value</var>.[[BooleanData]].</li><li>Assert: <var>b</var> is a Boolean.</li><li>Return <var>b</var>.</li></ol></li><li>Throw a <b>TypeError</b> exception.</li></ol></emu-alg>\n        ","3.4":"<p>Boolean instances are ordinary objects that inherit properties from the Boolean prototype object. Boolean instances have a [[BooleanData]] internal slot. The [[BooleanData]] internal slot is the Boolean value represented by this Boolean object.</p>\n    ","4.1":"<p>The Symbol constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Symbol%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"Symbol\"</b> property of the global object.</li>\n        <li>returns a new Symbol value when called as a function.</li>\n        <li>is not intended to be used with the <code>new</code> operator.</li>\n        <li>is not intended to be subclassed.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition but a <code>super</code> call to it will cause an exception.</li>\n      </ul>\n\n      ","4.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is not <b>undefined</b>, throw a <b>TypeError</b> exception.</li><li>If <var>description</var> is <b>undefined</b>, let <var>descString</var> be <b>undefined</b>.</li><li>Else, let <var>descString</var> be ? ToString(<var>description</var>).</li><li>Return a new Symbol whose [[Description]] is <var>descString</var>.</li></ol></emu-alg>\n      ","4.2":"<p>The Symbol constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","4.2.1":"<p>The initial value of <code>Symbol.asyncIterator</code> is the well-known symbol <code class=\"emu-intrinsic\">%Symbol.asyncIterator%</code> (table-well-known-symbols).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.2.2":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>stringKey</var> be ? ToString(<var>key</var>).</li><li>For each element <var>e</var> of the GlobalSymbolRegistry List, do<ol><li>If <var>e</var>.[[Key]] is <var>stringKey</var>, return <var>e</var>.[[Symbol]].</li></ol></li><li>Assert: The GlobalSymbolRegistry List does not currently contain an entry for <var>stringKey</var>.</li><li>Let <var>newSymbol</var> be a new Symbol whose [[Description]] is <var>stringKey</var>.</li><li>Append the GlobalSymbolRegistry Record { [[Key]]: <var>stringKey</var>, [[Symbol]]: <var>newSymbol</var> } to the GlobalSymbolRegistry List.</li><li>Return <var>newSymbol</var>.</li></ol></emu-alg>\n        <p>The <dfn>GlobalSymbolRegistry List</dfn> is an append-only List that is globally available. It is shared by all realms. Prior to the evaluation of any ECMAScript code, it is initialized as a new empty List. Elements of the GlobalSymbolRegistry List are Records with the structure defined in table-globalsymbolregistry-record-fields.</p>\n        <emu-table id=\"table-globalsymbolregistry-record-fields\" caption=\"GlobalSymbolRegistry Record Fields\" oldids=\"table-44\">\n          <table>\n            <thead>\n              <tr>\n                <th>\n                  Field Name\n                </th>\n                <th>\n                  Value\n                </th>\n                <th>\n                  Usage\n                </th>\n              </tr>\n            </thead>\n            <tr>\n              <td>\n                [[Key]]\n              </td>\n              <td>\n                a String\n              </td>\n              <td>\n                A string key used to globally identify a Symbol.\n              </td>\n            </tr>\n            <tr>\n              <td>\n                [[Symbol]]\n              </td>\n              <td>\n                a Symbol\n              </td>\n              <td>\n                A symbol that can be retrieved from any realm.\n              </td>\n            </tr>\n          </table>\n        </emu-table>\n      ","4.2.3":"<p>The initial value of <code>Symbol.hasInstance</code> is the well-known symbol <code class=\"emu-intrinsic\">%Symbol.hasInstance%</code> (table-well-known-symbols).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.2.4":"<p>The initial value of <code>Symbol.isConcatSpreadable</code> is the well-known symbol <code class=\"emu-intrinsic\">%Symbol.isConcatSpreadable%</code> (table-well-known-symbols).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.2.5":"<p>The initial value of <code>Symbol.iterator</code> is the well-known symbol <code class=\"emu-intrinsic\">%Symbol.iterator%</code> (table-well-known-symbols).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.2.6":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>sym</var> is not a Symbol, throw a <b>TypeError</b> exception.</li><li>Return KeyForSymbol(<var>sym</var>).</li></ol></emu-alg>\n      ","4.2.7":"<p>The initial value of <code>Symbol.match</code> is the well-known symbol <code class=\"emu-intrinsic\">%Symbol.match%</code> (table-well-known-symbols).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.2.8":"<p>The initial value of <code>Symbol.matchAll</code> is the well-known symbol <code class=\"emu-intrinsic\">%Symbol.matchAll%</code> (table-well-known-symbols).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.2.9":"<p>The initial value of <code>Symbol.prototype</code> is the Symbol prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.2.10":"<p>The initial value of <code>Symbol.replace</code> is the well-known symbol <code class=\"emu-intrinsic\">%Symbol.replace%</code> (table-well-known-symbols).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.2.11":"<p>The initial value of <code>Symbol.search</code> is the well-known symbol <code class=\"emu-intrinsic\">%Symbol.search%</code> (table-well-known-symbols).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.2.12":"<p>The initial value of <code>Symbol.species</code> is the well-known symbol <code class=\"emu-intrinsic\">%Symbol.species%</code> (table-well-known-symbols).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.2.13":"<p>The initial value of <code>Symbol.split</code> is the well-known symbol <code class=\"emu-intrinsic\">%Symbol.split%</code> (table-well-known-symbols).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.2.14":"<p>The initial value of <code>Symbol.toPrimitive</code> is the well-known symbol <code class=\"emu-intrinsic\">%Symbol.toPrimitive%</code> (table-well-known-symbols).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.2.15":"<p>The initial value of <code>Symbol.toStringTag</code> is the well-known symbol <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> (table-well-known-symbols).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.2.16":"<p>The initial value of <code>Symbol.unscopables</code> is the well-known symbol <code class=\"emu-intrinsic\">%Symbol.unscopables%</code> (table-well-known-symbols).</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","4.3":"<p>The <dfn>Symbol prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Symbol.prototype%</code></dfn>.</li>\n        <li>is an ordinary object.</li>\n        <li>is not a Symbol instance and does not have a [[SymbolData]] internal slot.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n      </ul>\n\n      ","4.3.1":"<p>The initial value of <code>Symbol.prototype.constructor</code> is <code class=\"emu-intrinsic\">%Symbol%</code>.</p>\n      ","4.3.2":"<p><code>Symbol.prototype.description</code> is an accessor property whose set accessor function is <b>undefined</b>. Its get accessor function performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>s</var> be the <b>this</b> value.</li><li>Let <var>sym</var> be ? ThisSymbolValue(<var>s</var>).</li><li>Return <var>sym</var>.[[Description]].</li></ol></emu-alg>\n      ","4.3.3":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>sym</var> be ? ThisSymbolValue(<b>this</b> value).</li><li>Return SymbolDescriptiveString(<var>sym</var>).</li></ol></emu-alg>\n\n        ","4.3.3.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>Let <var>desc</var> be <var>sym</var>.[[Description]].</li><li>If <var>desc</var> is <b>undefined</b>, set <var>desc</var> to the empty String.</li><li>Assert: <var>desc</var> is a String.</li><li>Return the string-concatenation of <b>\"Symbol(\"</b>, <var>desc</var>, and <b>\")\"</b>.</li></ol></emu-alg>\n        ","4.3.4":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Return ? ThisSymbolValue(<b>this</b> value).</li></ol></emu-alg>\n\n        ","4.3.4.1":"<dl class=\"header\">\n          </dl>\n          <emu-alg><ol><li>If <var>value</var> is a Symbol, return <var>value</var>.</li><li>If <var>value</var> is an Object and <var>value</var> has a [[SymbolData]] internal slot, then<ol><li>Let <var>s</var> be <var>value</var>.[[SymbolData]].</li><li>Assert: <var>s</var> is a Symbol.</li><li>Return <var>s</var>.</li></ol></li><li>Throw a <b>TypeError</b> exception.</li></ol></emu-alg>\n        ","4.3.5":"<p>This method is called by ECMAScript language operators to convert a Symbol object to a primitive value.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Return ? ThisSymbolValue(<b>this</b> value).</li></ol></emu-alg>\n        <emu-note>\n          <p>The argument is ignored.</p>\n        </emu-note>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n        <p>The value of the <b>\"name\"</b> property of this method is <b>\"[Symbol.toPrimitive]\"</b>.</p>\n      ","4.3.6":"<p>The initial value of the <code class=\"emu-intrinsic\">%Symbol.toStringTag%</code> property is the String value <b>\"Symbol\"</b>.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n      ","4.4":"<p>Symbol instances are ordinary objects that inherit properties from the Symbol prototype object. Symbol instances have a [[SymbolData]] internal slot. The [[SymbolData]] internal slot is the Symbol value represented by this Symbol object.</p>\n    ","4.5":"","4.5.1":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>If <var>sym</var> is in the GlobalSymbolRegistry List, the String used to register <var>sym</var> will be returned.</dd>\n        </dl>\n        <emu-alg><ol><li>For each element <var>e</var> of the GlobalSymbolRegistry List, do<ol><li>If SameValue(<var>e</var>.[[Symbol]], <var>sym</var>) is <b>true</b>, return <var>e</var>.[[Key]].</li></ol></li><li>Assert: The GlobalSymbolRegistry List does not currently contain an entry for <var>sym</var>.</li><li>Return <b>undefined</b>.</li></ol></emu-alg>\n      ","5.1":"<p>The Error constructor:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Error%</code></dfn>.</li>\n        <li>is the initial value of the <b>\"Error\"</b> property of the global object.</li>\n        <li>creates and initializes a new Error object when called as a function rather than as a constructor. Thus the function call <code>Error(…)</code> is equivalent to the object creation expression <code>new Error(…)</code> with the same arguments.</li>\n        <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified Error behaviour must include a <code>super</code> call to the Error constructor to create and initialize subclass instances with an [[ErrorData]] internal slot.</li>\n      </ul>\n\n      ","5.1.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If NewTarget is <b>undefined</b>, let <var>newTarget</var> be the active function object; else let <var>newTarget</var> be NewTarget.</li><li>Let <var>obj</var> be ? OrdinaryCreateFromConstructor(<var>newTarget</var>, <b>\"<code class=\"emu-intrinsic\">%Error.prototype%</code>\"</b>, « [[ErrorData]] »).</li><li>If <var>message</var> is not <b>undefined</b>, then<ol><li>Let <var>msg</var> be ? ToString(<var>message</var>).</li><li>Perform CreateNonEnumerableDataPropertyOrThrow(<var>obj</var>, <b>\"message\"</b>, <var>msg</var>).</li></ol></li><li>Perform ? InstallErrorCause(<var>obj</var>, <var>options</var>).</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n      ","5.2":"<p>The Error constructor:</p>\n      <ul>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Function.prototype%</code>.</li>\n        <li>has the following properties:</li>\n      </ul>\n\n      ","5.2.1":"<p>This function performs the following steps when called:</p>\n        <emu-alg><ol><li>If <var>arg</var> is not an Object, return <b>false</b>.</li><li>If <var>arg</var> does not have an [[ErrorData]] internal slot, return <b>false</b>.</li><li>Return <b>true</b>.</li></ol></emu-alg>\n      ","5.2.2":"<p>The initial value of <code>Error.prototype</code> is the Error prototype object.</p>\n        <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n      ","5.3":"<p>The <dfn>Error prototype object</dfn>:</p>\n      <ul>\n        <li>is <dfn><code class=\"emu-intrinsic\">%Error.prototype%</code></dfn>.</li>\n        <li>is an ordinary object.</li>\n        <li>is not an Error instance and does not have an [[ErrorData]] internal slot.</li>\n        <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Object.prototype%</code>.</li>\n      </ul>\n\n      ","5.3.1":"<p>The initial value of <code>Error.prototype.constructor</code> is <code class=\"emu-intrinsic\">%Error%</code>.</p>\n      ","5.3.2":"<p>The initial value of <code>Error.prototype.message</code> is the empty String.</p>\n      ","5.3.3":"<p>The initial value of <code>Error.prototype.name</code> is <b>\"Error\"</b>.</p>\n      ","5.3.4":"<p>This method performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>obj</var> be the <b>this</b> value.</li><li>If <var>obj</var> is not an Object, throw a <b>TypeError</b> exception.</li><li>Let <var>name</var> be ? Get(<var>obj</var>, <b>\"name\"</b>).</li><li>If <var>name</var> is <b>undefined</b>, set <var>name</var> to <b>\"Error\"</b>; else set <var>name</var> to ? ToString(<var>name</var>).</li><li>Let <var>msg</var> be ? Get(<var>obj</var>, <b>\"message\"</b>).</li><li>If <var>msg</var> is <b>undefined</b>, set <var>msg</var> to the empty String; else set <var>msg</var> to ? ToString(<var>msg</var>).</li><li>If <var>name</var> is the empty String, return <var>msg</var>.</li><li>If <var>msg</var> is the empty String, return <var>name</var>.</li><li>Return the string-concatenation of <var>name</var>, the code unit 0x003A (COLON), the code unit 0x0020 (SPACE), and <var>msg</var>.</li></ol></emu-alg>\n      ","5.4":"<p>Error instances are ordinary objects that inherit properties from the Error prototype object and have an [[ErrorData]] internal slot whose value is <b>undefined</b>. The only specified use of [[ErrorData]] is to identify Error, AggregateError, and <var>NativeError</var> instances as Error objects within <code>Object.prototype.toString</code> and <code>Error.isError</code>.</p>\n    ","5.5":"<p>A new instance of one of the <var>NativeError</var> objects below or of the AggregateError object is thrown when a runtime error is detected. All <var>NativeError</var> objects share the same structure, as described in <a href=\"/fundamental-objects#sec-nativeerror-object-structure\">20.5.6</a>.</p>\n\n      ","5.5.1":"<p>The EvalError constructor is <dfn><code class=\"emu-intrinsic\">%EvalError%</code></dfn>.</p>\n        <p>This exception is not currently used within this specification. This object remains for compatibility with previous editions of this specification.</p>\n      ","5.5.2":"<p>The RangeError constructor is <dfn><code class=\"emu-intrinsic\">%RangeError%</code></dfn>.</p>\n        <p>Indicates a value that is not in the set or range of allowable values.</p>\n      ","5.5.3":"<p>The ReferenceError constructor is <dfn><code class=\"emu-intrinsic\">%ReferenceError%</code></dfn>.</p>\n        <p>Indicate that an invalid reference has been detected.</p>\n      ","5.5.4":"<p>The SyntaxError constructor is <dfn><code class=\"emu-intrinsic\">%SyntaxError%</code></dfn>.</p>\n        <p>Indicates that a parsing error has occurred.</p>\n      ","5.5.5":"<p>The TypeError constructor is <dfn><code class=\"emu-intrinsic\">%TypeError%</code></dfn>.</p>\n        <p>TypeError is used to indicate an unsuccessful operation when none of the other <var>NativeError</var> objects are an appropriate indication of the failure cause.</p>\n      ","5.5.6":"<p>The URIError constructor is <dfn><code class=\"emu-intrinsic\">%URIError%</code></dfn>.</p>\n        <p>Indicates that one of the global URI handling functions was used in a way that is incompatible with its definition.</p>\n      ","5.6":"<p>Each of these objects has the structure described below, differing only in the name used as the constructor name and in the <b>\"name\"</b> property of the prototype object.</p>\n      <p>For each error object, references to <var>NativeError</var> in the definition should be replaced with the appropriate error object name from <a href=\"/fundamental-objects#sec-native-error-types-used-in-this-standard\">20.5.5</a>.</p>\n\n      ","5.6.1":"<p>Each <var>NativeError</var> constructor:</p>\n        <ul>\n          <li>creates and initializes a new <var>NativeError</var> object when called as a function rather than as a constructor. A call of the object as a function is equivalent to calling it as a constructor with the same arguments. Thus the function call <code><var>NativeError</var>(&hellip;)</code> is equivalent to the object creation expression <code>new <var>NativeError</var>(&hellip;)</code> with the same arguments.</li>\n          <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified <var>NativeError</var> behaviour must include a <code>super</code> call to the <var>NativeError</var> constructor to create and initialize subclass instances with an [[ErrorData]] internal slot.</li>\n        </ul>\n\n        ","5.6.1.1":"<p>Each <var>NativeError</var> function performs the following steps when called:</p>\n          <emu-alg><ol><li>If NewTarget is <b>undefined</b>, let <var>newTarget</var> be the active function object; else let <var>newTarget</var> be NewTarget.</li><li>Let <var>obj</var> be ? OrdinaryCreateFromConstructor(<var>newTarget</var>, <code>\"%<var>NativeError</var>.prototype%\"</code>, « [[ErrorData]] »).</li><li>If <var>message</var> is not <b>undefined</b>, then<ol><li>Let <var>msg</var> be ? ToString(<var>message</var>).</li><li>Perform CreateNonEnumerableDataPropertyOrThrow(<var>obj</var>, <b>\"message\"</b>, <var>msg</var>).</li></ol></li><li>Perform ? InstallErrorCause(<var>obj</var>, <var>options</var>).</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n          <p>The actual value of the string passed in step step-nativeerror-ordinarycreatefromconstructor is either <b>\"<code class=\"emu-intrinsic\">%EvalError.prototype%</code>\"</b>, <b>\"<code class=\"emu-intrinsic\">%RangeError.prototype%</code>\"</b>, <b>\"<code class=\"emu-intrinsic\">%ReferenceError.prototype%</code>\"</b>, <b>\"<code class=\"emu-intrinsic\">%SyntaxError.prototype%</code>\"</b>, <b>\"<code class=\"emu-intrinsic\">%TypeError.prototype%</code>\"</b>, or <b>\"<code class=\"emu-intrinsic\">%URIError.prototype%</code>\"</b> corresponding to which <var>NativeError</var> constructor is being defined.</p>\n        ","5.6.2":"<p>Each <var>NativeError</var> constructor:</p>\n        <ul>\n          <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Error%</code>.</li>\n          <li>has a <b>\"name\"</b> property whose value is the String value <emu-val>\"<var>NativeError</var>\"</emu-val>.</li>\n          <li>has the following properties:</li>\n        </ul>\n\n        ","5.6.2.1":"<p>The initial value of <code><var>NativeError</var>.prototype</code> is a <var>NativeError</var> prototype object (<a href=\"/fundamental-objects#sec-properties-of-the-nativeerror-prototype-objects\">20.5.6.3</a>). Each <var>NativeError</var> constructor has a distinct prototype object.</p>\n          <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n        ","5.6.3":"<p>Each <dfn><var>NativeError</var> prototype object</dfn>:</p>\n        <ul>\n          <li>is an ordinary object.</li>\n          <li>is not an Error instance and does not have an [[ErrorData]] internal slot.</li>\n          <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Error.prototype%</code>.</li>\n        </ul>\n\n        ","5.6.3.1":"<p>The initial value of the <b>\"constructor\"</b> property of the prototype for a given <var>NativeError</var> constructor is the constructor itself.</p>\n        ","5.6.3.2":"<p>The initial value of the <b>\"message\"</b> property of the prototype for a given <var>NativeError</var> constructor is the empty String.</p>\n        ","5.6.3.3":"<p>The initial value of the <b>\"name\"</b> property of the prototype for a given <var>NativeError</var> constructor is the String value consisting of the name of the constructor (the name used instead of <var>NativeError</var>).</p>\n        ","5.6.4":"<p><var>NativeError</var> instances are ordinary objects that inherit properties from their <var>NativeError</var> prototype object and have an [[ErrorData]] internal slot whose value is <b>undefined</b>. The only specified use of [[ErrorData]] is by <code>Object.prototype.toString</code> (<a href=\"/fundamental-objects#sec-object.prototype.tostring\">20.1.3.6</a>) and <code>Error.isError</code> (<a href=\"/fundamental-objects#sec-error.iserror\">20.5.2.1</a>) to identify Error, AggregateError, or <var>NativeError</var> instances.</p>\n      ","5.7":"","5.7.1":"<p>The AggregateError constructor:</p>\n        <ul>\n          <li>is <dfn><code class=\"emu-intrinsic\">%AggregateError%</code></dfn>.</li>\n          <li>is the initial value of the <b>\"AggregateError\"</b> property of the global object.</li>\n          <li>creates and initializes a new AggregateError object when called as a function rather than as a constructor. Thus the function call <code>AggregateError(…)</code> is equivalent to the object creation expression <code>new AggregateError(…)</code> with the same arguments.</li>\n          <li>may be used as the value of an <code>extends</code> clause of a class definition. Subclass constructors that intend to inherit the specified AggregateError behaviour must include a <code>super</code> call to the AggregateError constructor to create and initialize subclass instances with an [[ErrorData]] internal slot.</li>\n        </ul>\n\n        ","5.7.1.1":"<p>This function performs the following steps when called:</p>\n          <emu-alg><ol><li>If NewTarget is <b>undefined</b>, let <var>newTarget</var> be the active function object; else let <var>newTarget</var> be NewTarget.</li><li>Let <var>obj</var> be ? OrdinaryCreateFromConstructor(<var>newTarget</var>, <b>\"<code class=\"emu-intrinsic\">%AggregateError.prototype%</code>\"</b>, « [[ErrorData]] »).</li><li>If <var>message</var> is not <b>undefined</b>, then<ol><li>Let <var>msg</var> be ? ToString(<var>message</var>).</li><li>Perform CreateNonEnumerableDataPropertyOrThrow(<var>obj</var>, <b>\"message\"</b>, <var>msg</var>).</li></ol></li><li>Perform ? InstallErrorCause(<var>obj</var>, <var>options</var>).</li><li>Let <var>errorsList</var> be ? IteratorToList(? GetIterator(<var>errors</var>, <emu-const>sync</emu-const>)).</li><li>Perform ! DefinePropertyOrThrow(<var>obj</var>, <b>\"errors\"</b>, PropertyDescriptor { [[Configurable]]: <b>true</b>, [[Enumerable]]: <b>false</b>, [[Writable]]: <b>true</b>, [[Value]]: CreateArrayFromList(<var>errorsList</var>) }).</li><li>Return <var>obj</var>.</li></ol></emu-alg>\n        ","5.7.2":"<p>The AggregateError constructor:</p>\n        <ul>\n          <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Error%</code>.</li>\n          <li>has the following properties:</li>\n        </ul>\n\n        ","5.7.2.1":"<p>The initial value of <code>AggregateError.prototype</code> is <code class=\"emu-intrinsic\">%AggregateError.prototype%</code>.</p>\n          <p>This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n        ","5.7.3":"<p>The <dfn>AggregateError prototype object</dfn>:</p>\n        <ul>\n          <li>is <dfn><code class=\"emu-intrinsic\">%AggregateError.prototype%</code></dfn>.</li>\n          <li>is an ordinary object.</li>\n          <li>is not an Error instance or an AggregateError instance and does not have an [[ErrorData]] internal slot.</li>\n          <li>has a [[Prototype]] internal slot whose value is <code class=\"emu-intrinsic\">%Error.prototype%</code>.</li>\n        </ul>\n\n        ","5.7.3.1":"<p>The initial value of <code>AggregateError.prototype.constructor</code> is <code class=\"emu-intrinsic\">%AggregateError%</code>.</p>\n        ","5.7.3.2":"<p>The initial value of <code>AggregateError.prototype.message</code> is the empty String.</p>\n        ","5.7.3.3":"<p>The initial value of <code>AggregateError.prototype.name</code> is <b>\"AggregateError\"</b>.</p>\n        ","5.7.4":"<p>AggregateError instances are ordinary objects that inherit properties from their AggregateError prototype object and have an [[ErrorData]] internal slot whose value is <b>undefined</b>. The only specified use of [[ErrorData]] is by <code>Object.prototype.toString</code> (<a href=\"/fundamental-objects#sec-object.prototype.tostring\">20.1.3.6</a>) and <code>Error.isError</code> (<a href=\"/fundamental-objects#sec-error.iserror\">20.5.2.1</a>) to identify Error, AggregateError, or <var>NativeError</var> instances.</p>\n      ","5.8":"","5.8.1":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It is used to create a <b>\"cause\"</b> property on <var>obj</var> when a <b>\"cause\"</b> property is present on <var>options</var>.</dd>\n        </dl>\n        <emu-alg><ol><li>If <var>options</var> is an Object and ? HasProperty(<var>options</var>, <b>\"cause\"</b>) is <b>true</b>, then<ol><li>Let <var>cause</var> be ? Get(<var>options</var>, <b>\"cause\"</b>).</li><li>Perform CreateNonEnumerableDataPropertyOrThrow(<var>obj</var>, <b>\"cause\"</b>, <var>cause</var>).</li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      "};
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
