// Generated from ecma262/spec.html — do not edit by hand.
const sections = {"1":"","2":"","3":"","4":"","":"<p>The <dfn variants=\"global objects\">global object</dfn>:</p>\n  <ul>\n    <li>is created before control enters any execution context.</li>\n    <li>does not have a [[Construct]] internal method; it cannot be used as a constructor with the <code>new</code> operator.</li>\n    <li>does not have a [[Call]] internal method; it cannot be invoked as a function.</li>\n    <li>has a [[Prototype]] internal slot whose value is host-defined.</li>\n    <li>may have host-defined properties in addition to the properties defined in this specification. This may include a property whose value is the global object itself.</li>\n  </ul>\n\n  ","1.1":"<p>The initial value of the <b>\"globalThis\"</b> property of the global object in a Realm Record <var>realm</var> is <var>realm</var>.[[GlobalEnv]].[[GlobalThisValue]].</p>\n      <p>This property has the attributes { [[Writable]]: <b>true</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>true</b> }.</p>\n    ","1.2":"<p>The value of <code>Infinity</code> is <b>+∞</b><sub>𝔽</sub> (see <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-number-type\">6.1.6.1</a>). This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n    ","1.3":"<p>The value of <code>NaN</code> is <b>NaN</b> (see <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-number-type\">6.1.6.1</a>). This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n    ","1.4":"<p>The value of <code>undefined</code> is <b>undefined</b> (see <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-undefined-type\">6.1.1</a>). This property has the attributes { [[Writable]]: <b>false</b>, [[Enumerable]]: <b>false</b>, [[Configurable]]: <b>false</b> }.</p>\n    ","2.1":"<p>This function is the <dfn><code class=\"emu-intrinsic\">%eval%</code></dfn> intrinsic object.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Return ? PerformEval(<var>source</var>, <b>false</b>, <b>false</b>).</li></ol></emu-alg>\n\n      ","2.1.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: If <var>direct</var> is <b>false</b>, then <var>strictCaller</var> is also <b>false</b>.</li><li>If <var>source</var> is not a String, return <var>source</var>.</li><li>Let <var>evalRealm</var> be the current Realm Record.</li><li>NOTE: In the case of a direct eval, <var>evalRealm</var> is the realm of both the caller of <code>eval</code> and of the <code>eval</code> function itself.</li><li>Perform ? HostEnsureCanCompileStrings(<var>evalRealm</var>, « », <var>source</var>, <var>direct</var>).</li><li>Let <var>inFunction</var> be <b>false</b>.</li><li>Let <var>inMethod</var> be <b>false</b>.</li><li>Let <var>inDerivedConstructor</var> be <b>false</b>.</li><li>Let <var>inClassFieldInitializer</var> be <b>false</b>.</li><li>If <var>direct</var> is <b>true</b>, then<ol><li>Let <var>thisEnvRec</var> be GetThisEnvironment().</li><li>If <var>thisEnvRec</var> is a Function Environment Record, then<ol><li>Let <var>func</var> be <var>thisEnvRec</var>.[[FunctionObject]].</li><li>Set <var>inFunction</var> to <b>true</b>.</li><li>Set <var>inMethod</var> to <var>thisEnvRec</var>.HasSuperBinding().</li><li>If <var>func</var>.[[ConstructorKind]] is <emu-const>derived</emu-const>, set <var>inDerivedConstructor</var> to <b>true</b>.</li><li>Let <var>classFieldInitializerName</var> be <var>func</var>.[[ClassFieldInitializerName]].</li><li>If <var>classFieldInitializerName</var> is not <emu-const>empty</emu-const>, set <var>inClassFieldInitializer</var> to <b>true</b>.</li></ol></li></ol></li><li>Perform the following substeps in an implementation-defined order, possibly interleaving parsing and error detection:<ol><li>Let <var>script</var> be ParseText(<var>source</var>, <emu-nt>Script</emu-nt>).</li><li>If <var>script</var> is a List of errors, throw a <b>SyntaxError</b> exception.</li><li>If <var>script</var> Contains <emu-nt>ScriptBody</emu-nt> is <b>false</b>, return <b>undefined</b>.</li><li>Let <var>body</var> be the <emu-nt>ScriptBody</emu-nt> of <var>script</var>.</li><li>If <var>inFunction</var> is <b>false</b> and <var>body</var> Contains <emu-nt>NewTarget</emu-nt>, throw a <b>SyntaxError</b> exception.</li><li>If <var>inMethod</var> is <b>false</b> and <var>body</var> Contains <emu-nt>SuperProperty</emu-nt>, throw a <b>SyntaxError</b> exception.</li><li>If <var>inDerivedConstructor</var> is <b>false</b> and <var>body</var> Contains <emu-nt>SuperCall</emu-nt>, throw a <b>SyntaxError</b> exception.</li><li>If <var>inClassFieldInitializer</var> is <b>true</b> and ContainsArguments of <var>body</var> is <b>true</b>, throw a <b>SyntaxError</b> exception.</li></ol></li><li>If <var>strictCaller</var> is <b>true</b>, let <var>strictEval</var> be <b>true</b>.</li><li>Else, let <var>strictEval</var> be ScriptIsStrict of <var>script</var>.</li><li>Let <var>runningContext</var> be the running execution context.</li><li>NOTE: If <var>direct</var> is <b>true</b>, <var>runningContext</var> will be the execution context that performed the direct eval. If <var>direct</var> is <b>false</b>, <var>runningContext</var> will be the execution context for the invocation of the <code>eval</code> function.</li><li>If <var>direct</var> is <b>true</b>, then<ol><li>Let <var>lexEnv</var> be NewDeclarativeEnvironment(<var>runningContext</var>'s LexicalEnvironment).</li><li>Let <var>varEnv</var> be <var>runningContext</var>'s VariableEnvironment.</li><li>Let <var>privateEnv</var> be <var>runningContext</var>'s PrivateEnvironment.</li></ol></li><li>Else,<ol><li>Let <var>lexEnv</var> be NewDeclarativeEnvironment(<var>evalRealm</var>.[[GlobalEnv]]).</li><li>Let <var>varEnv</var> be <var>evalRealm</var>.[[GlobalEnv]].</li><li>Let <var>privateEnv</var> be <b>null</b>.</li></ol></li><li>If <var>strictEval</var> is <b>true</b>, set <var>varEnv</var> to <var>lexEnv</var>.</li><li>If <var>runningContext</var> is not already suspended, suspend <var>runningContext</var>.</li><li>Let <var>evalContext</var> be a new ECMAScript code execution context.</li><li>Set <var>evalContext</var>'s Function to <b>null</b>.</li><li>Set <var>evalContext</var>'s Realm to <var>evalRealm</var>.</li><li>Set <var>evalContext</var>'s ScriptOrModule to <var>runningContext</var>'s ScriptOrModule.</li><li>Set <var>evalContext</var>'s VariableEnvironment to <var>varEnv</var>.</li><li>Set <var>evalContext</var>'s LexicalEnvironment to <var>lexEnv</var>.</li><li>Set <var>evalContext</var>'s PrivateEnvironment to <var>privateEnv</var>.</li><li>Push <var>evalContext</var> onto the execution context stack; <var>evalContext</var> is now the running execution context.</li><li>Let <var>result</var> be Completion(EvalDeclarationInstantiation(<var>body</var>, <var>varEnv</var>, <var>lexEnv</var>, <var>privateEnv</var>, <var>strictEval</var>)).</li><li>If <var>result</var> is a normal completion, then<ol><li>Set <var>result</var> to Completion(Evaluation of <var>body</var>).</li></ol></li><li>If <var>result</var> is a normal completion and <var>result</var>.[[Value]] is <emu-const>empty</emu-const>, then<ol><li>Set <var>result</var> to NormalCompletion(<b>undefined</b>).</li></ol></li><li>Suspend <var>evalContext</var> and remove it from the execution context stack.</li><li>Resume the context that is now on the top of the execution context stack as the running execution context.</li><li>Return ? <var>result</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>The eval code cannot instantiate variable or function bindings in the variable environment of the calling context that invoked the eval if either the code of the calling context or the eval code is strict mode code. Instead such bindings are instantiated in a new VariableEnvironment that is only accessible to the eval code. Bindings introduced by <code>let</code>, <code>const</code>, or <code>class</code> declarations are always instantiated in a new LexicalEnvironment.</p>\n        </emu-note>\n      ","2.1.2":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It allows host environments to block certain ECMAScript functions which allow developers to interpret and evaluate strings as ECMAScript code.</dd>\n        </dl>\n        <p>\n          <var>parameterStrings</var> represents the strings that, when using one of the function constructors, will be concatenated together to build the parameters list. <var>bodyString</var> represents the function body or the string passed to an <code>eval</code> call.\n          <var>direct</var> signifies whether the evaluation is a direct eval.\n        </p>\n        <p>The default implementation of HostEnsureCanCompileStrings is to return NormalCompletion(<emu-const>unused</emu-const>).</p>\n      ","2.1.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let <var>varNames</var> be the VarDeclaredNames of <var>body</var>.</li><li>Let <var>varDeclarations</var> be the VarScopedDeclarations of <var>body</var>.</li><li>If <var>strict</var> is <b>false</b>, then<ol><li>If <var>varEnv</var> is a Global Environment Record, then<ol><li>For each element <var>name</var> of <var>varNames</var>, do<ol><li>If HasLexicalDeclaration(<var>varEnv</var>, <var>name</var>) is <b>true</b>, throw a <b>SyntaxError</b> exception.</li><li>NOTE: <code>eval</code> will not create a global var declaration that would be shadowed by a global lexical declaration.</li></ol></li></ol></li><li>Let <var>thisEnv</var> be <var>lexEnv</var>.</li><li>Assert: The following loop will terminate.</li><li>Repeat, while <var>thisEnv</var> and <var>varEnv</var> are not the same Environment Record,<ol><li>If <var>thisEnv</var> is not an Object Environment Record, then<ol><li>NOTE: The environment of with statements cannot contain any lexical declaration so it doesn't need to be checked for var/let hoisting conflicts.</li><li>For each element <var>name</var> of <var>varNames</var>, do<ol><li>If ! <var>thisEnv</var>.HasBinding(<var>name</var>) is <b>true</b>, then<ol><li>If the host is a web browser or otherwise supports <a href=\"/additional-ecmascript-features-for-web-browsers#sec-variablestatements-in-catch-blocks\">B.3.4</a>, then<ol><li>If <var>thisEnv</var> is not the Environment Record for a <emu-nt>Catch</emu-nt> clause, throw a <b>SyntaxError</b> exception.</li></ol></li><li>Else,<ol><li>Throw a <b>SyntaxError</b> exception.</li></ol></li></ol></li><li>NOTE: A direct eval will not hoist var declaration over a like-named lexical declaration.</li></ol></li></ol></li><li>Set <var>thisEnv</var> to <var>thisEnv</var>.[[OuterEnv]].</li></ol></li></ol></li><li>Let <var>privateIdentifiers</var> be a new empty List.</li><li>Let <var>pointer</var> be <var>privateEnv</var>.</li><li>Repeat, while <var>pointer</var> is not <b>null</b>,<ol><li>For each Private Name <var>binding</var> of <var>pointer</var>.[[Names]], do<ol><li>If <var>privateIdentifiers</var> does not contain <var>binding</var>.[[Description]], append <var>binding</var>.[[Description]] to <var>privateIdentifiers</var>.</li></ol></li><li>Set <var>pointer</var> to <var>pointer</var>.[[OuterPrivateEnvironment]].</li></ol></li><li>If AllPrivateIdentifiersValid of <var>body</var> with argument <var>privateIdentifiers</var> is <b>false</b>, throw a <b>SyntaxError</b> exception.</li><li>Let <var>functionsToInitialize</var> be a new empty List.</li><li>Let <var>declaredFunctionNames</var> be a new empty List.</li><li>For each element <var>d</var> of <var>varDeclarations</var>, in reverse List order, do<ol><li>If <var>d</var> is not either a <emu-nt>VariableDeclaration</emu-nt>, a <emu-nt>ForBinding</emu-nt>, or a <emu-nt>BindingIdentifier</emu-nt>, then<ol><li>Assert: <var>d</var> is either a <emu-nt>FunctionDeclaration</emu-nt>, a <emu-nt>GeneratorDeclaration</emu-nt>, an <emu-nt>AsyncFunctionDeclaration</emu-nt>, or an <emu-nt>AsyncGeneratorDeclaration</emu-nt>.</li><li>NOTE: If there are multiple function declarations for the same name, the last declaration is used.</li><li>Let <var>fn</var> be the sole element of the BoundNames of <var>d</var>.</li><li>If <var>declaredFunctionNames</var> does not contain <var>fn</var>, then<ol><li>If <var>varEnv</var> is a Global Environment Record, then<ol><li>Let <var>fnDefinable</var> be ? CanDeclareGlobalFunction(<var>varEnv</var>, <var>fn</var>).</li><li>If <var>fnDefinable</var> is <b>false</b>, throw a <b>TypeError</b> exception.</li></ol></li><li>Append <var>fn</var> to <var>declaredFunctionNames</var>.</li><li>Insert <var>d</var> as the first element of <var>functionsToInitialize</var>.</li></ol></li></ol></li></ol></li><li>Let <var>declaredVarNames</var> be a new empty List.</li><li>For each element <var>d</var> of <var>varDeclarations</var>, do<ol><li>If <var>d</var> is either a <emu-nt>VariableDeclaration</emu-nt>, a <emu-nt>ForBinding</emu-nt>, or a <emu-nt>BindingIdentifier</emu-nt>, then<ol><li>For each String <var>vn</var> of the BoundNames of <var>d</var>, do<ol><li>If <var>declaredFunctionNames</var> does not contain <var>vn</var>, then<ol><li>If <var>varEnv</var> is a Global Environment Record, then<ol><li>Let <var>vnDefinable</var> be ? CanDeclareGlobalVar(<var>varEnv</var>, <var>vn</var>).</li><li>If <var>vnDefinable</var> is <b>false</b>, throw a <b>TypeError</b> exception.</li></ol></li><li>If <var>declaredVarNames</var> does not contain <var>vn</var>, then<ol><li>Append <var>vn</var> to <var>declaredVarNames</var>.</li></ol></li></ol></li></ol></li></ol></li></ol></li><li>If <var>strict</var> is <b>false</b> and the host is a web browser or otherwise supports <a href=\"/additional-ecmascript-features-for-web-browsers#sec-block-level-function-declarations-web-legacy-compatibility-semantics\">B.3.2</a>, then<ol><li>Let <var>declaredFunctionOrVarNames</var> be the list-concatenation of <var>declaredFunctionNames</var> and <var>declaredVarNames</var>.</li><li>For each <emu-nt>FunctionDeclaration</emu-nt> <var>f</var> that is directly contained in the <emu-nt>StatementList</emu-nt> of any <emu-nt>Block</emu-nt>, <emu-nt>CaseClause</emu-nt>, or <emu-nt>DefaultClause</emu-nt> <var>x</var> such that <var>body</var> Contains <var>x</var> is <b>true</b>, do<ol><li>Let <var>funcName</var> be the StringValue of the <emu-nt>BindingIdentifier</emu-nt> of <var>f</var>.</li><li>If replacing the <emu-nt>FunctionDeclaration</emu-nt> <var>f</var> with a <emu-nt>VariableStatement</emu-nt> that has <var>funcName</var> as a <emu-nt>BindingIdentifier</emu-nt> would not produce any Early Errors for <var>body</var>, then<ol><li>Let <var>bindingExists</var> be <b>false</b>.</li><li>Set <var>thisEnv</var> to <var>lexEnv</var>.</li><li>Assert: The following loop will terminate.</li><li>Repeat, while <var>thisEnv</var> is not <var>varEnv</var>,<ol><li>If <var>thisEnv</var> is not an Object Environment Record, then<ol><li>If ! <var>thisEnv</var>.HasBinding(<var>funcName</var>) is <b>true</b>, then<ol><li>If the host is a web browser or otherwise supports <a href=\"/additional-ecmascript-features-for-web-browsers#sec-variablestatements-in-catch-blocks\">B.3.4</a>, then<ol><li>If <var>thisEnv</var> is not the Environment Record for a <emu-nt>Catch</emu-nt> clause, set <var>bindingExists</var> to <b>true</b>.</li></ol></li><li>Else,<ol><li>Set <var>bindingExists</var> to <b>true</b>.</li></ol></li></ol></li></ol></li><li>Set <var>thisEnv</var> to <var>thisEnv</var>.[[OuterEnv]].</li></ol></li><li>If <var>bindingExists</var> is <b>false</b> and <var>varEnv</var> is a Global Environment Record, then<ol><li>If HasLexicalDeclaration(<var>varEnv</var>, <var>funcName</var>) is <b>false</b>, then<ol><li>Let <var>fnDefinable</var> be ? CanDeclareGlobalVar(<var>varEnv</var>, <var>funcName</var>).</li></ol></li><li>Else,<ol><li>Let <var>fnDefinable</var> be <b>false</b>.</li></ol></li></ol></li><li>Else,<ol><li>Let <var>fnDefinable</var> be <b>true</b>.</li></ol></li><li>If <var>bindingExists</var> is <b>false</b> and <var>fnDefinable</var> is <b>true</b>, then<ol><li>If <var>declaredFunctionOrVarNames</var> does not contain <var>funcName</var>, then<ol><li>If <var>varEnv</var> is a Global Environment Record, then<ol><li>Perform ? CreateGlobalVarBinding(<var>varEnv</var>, <var>funcName</var>, <b>true</b>).</li></ol></li><li>Else,<ol><li>Set <var>bindingExists</var> to ! <var>varEnv</var>.HasBinding(<var>funcName</var>).</li><li>If <var>bindingExists</var> is <b>false</b>, then<ol><li>Perform ! <var>varEnv</var>.CreateMutableBinding(<var>funcName</var>, <b>true</b>).</li><li>Perform ! <var>varEnv</var>.InitializeBinding(<var>funcName</var>, <b>undefined</b>).</li></ol></li></ol></li><li>Append <var>funcName</var> to <var>declaredFunctionOrVarNames</var>.</li></ol></li><li>When the <emu-nt>FunctionDeclaration</emu-nt> <var>f</var> is evaluated, perform the following steps in place of the <emu-nt>FunctionDeclaration</emu-nt> Evaluation algorithm provided in <a href=\"/ecmascript-language-functions-and-classes#sec-function-definitions-runtime-semantics-evaluation\">15.2.6</a>:<ol><li>Let <var>gEnv</var> be the running execution context's VariableEnvironment.</li><li>Let <var>bEnv</var> be the running execution context's LexicalEnvironment.</li><li>Let <var>fObj</var> be ! <var>bEnv</var>.GetBindingValue(<var>funcName</var>, <b>false</b>).</li><li>Perform ? <emu-meta effects=\"user-code\"><var>gEnv</var>.SetMutableBinding(<var>funcName</var>, <var>fObj</var>, <b>false</b>)</emu-meta>.</li><li>Return <emu-const>unused</emu-const>.</li></ol></li></ol></li></ol></li></ol></li></ol></li><li>NOTE: No abnormal terminations occur after this algorithm step unless <var>varEnv</var> is a Global Environment Record and the global object is a Proxy exotic object.</li><li>Let <var>lexDeclarations</var> be the LexicallyScopedDeclarations of <var>body</var>.</li><li>For each element <var>d</var> of <var>lexDeclarations</var>, do<ol><li>NOTE: Lexically declared names are only instantiated here but not initialized.</li><li>For each element <var>dn</var> of the BoundNames of <var>d</var>, do<ol><li>If IsConstantDeclaration of <var>d</var> is <b>true</b>, then<ol><li>Perform ? <var>lexEnv</var>.CreateImmutableBinding(<var>dn</var>, <b>true</b>).</li></ol></li><li>Else,<ol><li>Perform ? <var>lexEnv</var>.CreateMutableBinding(<var>dn</var>, <b>false</b>).</li></ol></li></ol></li></ol></li><li>For each Parse Node <var>f</var> of <var>functionsToInitialize</var>, do<ol><li>Let <var>fn</var> be the sole element of the BoundNames of <var>f</var>.</li><li>Let <var>fo</var> be InstantiateFunctionObject of <var>f</var> with arguments <var>lexEnv</var> and <var>privateEnv</var>.</li><li>If <var>varEnv</var> is a Global Environment Record, then<ol><li>Perform ? CreateGlobalFunctionBinding(<var>varEnv</var>, <var>fn</var>, <var>fo</var>, <b>true</b>).</li></ol></li><li>Else,<ol><li>Let <var>bindingExists</var> be ! <var>varEnv</var>.HasBinding(<var>fn</var>).</li><li>If <var>bindingExists</var> is <b>false</b>, then<ol><li>NOTE: The following invocation cannot return an abrupt completion because of the validation preceding step step-evaldeclarationinstantiation-post-validation.</li><li>Perform ! <var>varEnv</var>.CreateMutableBinding(<var>fn</var>, <b>true</b>).</li><li>Perform ! <var>varEnv</var>.InitializeBinding(<var>fn</var>, <var>fo</var>).</li></ol></li><li>Else,<ol><li>Perform ! <var>varEnv</var>.SetMutableBinding(<var>fn</var>, <var>fo</var>, <b>false</b>).</li></ol></li></ol></li></ol></li><li>For each String <var>vn</var> of <var>declaredVarNames</var>, do<ol><li>If <var>varEnv</var> is a Global Environment Record, then<ol><li>Perform ? CreateGlobalVarBinding(<var>varEnv</var>, <var>vn</var>, <b>true</b>).</li></ol></li><li>Else,<ol><li>Let <var>bindingExists</var> be ! <var>varEnv</var>.HasBinding(<var>vn</var>).</li><li>If <var>bindingExists</var> is <b>false</b>, then<ol><li>NOTE: The following invocation cannot return an abrupt completion because of the validation preceding step step-evaldeclarationinstantiation-post-validation.</li><li>Perform ! <var>varEnv</var>.CreateMutableBinding(<var>vn</var>, <b>true</b>).</li><li>Perform ! <var>varEnv</var>.InitializeBinding(<var>vn</var>, <b>undefined</b>).</li></ol></li></ol></li></ol></li><li>Return <emu-const>unused</emu-const>.</li></ol></emu-alg>\n      ","2.2":"<p>This function is the <dfn><code class=\"emu-intrinsic\">%isFinite%</code></dfn> intrinsic object.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>num</var> be ? ToNumber(<var>number</var>).</li><li>If <var>num</var> is finite, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n    ","2.3":"<p>This function is the <dfn><code class=\"emu-intrinsic\">%isNaN%</code></dfn> intrinsic object.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>num</var> be ? ToNumber(<var>number</var>).</li><li>If <var>num</var> is <b>NaN</b>, return <b>true</b>.</li><li>Return <b>false</b>.</li></ol></emu-alg>\n      <emu-note>\n        <p>A reliable way for ECMAScript code to test if a value <code>X</code> is <b>NaN</b> is an expression of the form <code>X !== X</code>. The result will be <b>true</b> if and only if <code>X</code> is <b>NaN</b>.</p>\n      </emu-note>\n    ","2.4":"<p>This function produces a Number value dictated by interpretation of the contents of the <var>string</var> argument as a decimal literal.</p>\n      <p>It is the <dfn><code class=\"emu-intrinsic\">%parseFloat%</code></dfn> intrinsic object.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>inputString</var> be ? ToString(<var>string</var>).</li><li>Let <var>trimmedString</var> be ! TrimString(<var>inputString</var>, <emu-const>start</emu-const>).</li><li>Let <var>trimmed</var> be StringToCodePoints(<var>trimmedString</var>).</li><li>Let <var>trimmedPrefix</var> be the longest prefix of <var>trimmed</var> that satisfies the syntax of a <emu-nt>StrDecimalLiteral</emu-nt>, which might be <var>trimmed</var> itself. If there is no such prefix, return <b>NaN</b>.</li><li>Let <var>parsedNumber</var> be ParseText(<var>trimmedPrefix</var>, <emu-nt>StrDecimalLiteral</emu-nt>).</li><li>Assert: <var>parsedNumber</var> is a Parse Node.</li><li>Return the StringNumericValue of <var>parsedNumber</var>.</li></ol></emu-alg>\n      <emu-note>\n        <p>This function may interpret only a leading portion of <var>string</var> as a Number value; it ignores any code units that cannot be interpreted as part of the notation of a decimal literal, and no indication is given that any such code units were ignored.</p>\n      </emu-note>\n    ","2.5":"<p>This function produces an integral Number dictated by interpretation of the contents of <var>string</var> according to the specified <var>radix</var>. Leading white space in <var>string</var> is ignored. If <var>radix</var> coerces to 0 (such as when it is <b>undefined</b>), it is assumed to be 10 except when the number representation begins with <b>\"0x\"</b> or <b>\"0X\"</b>, in which case it is assumed to be 16. If <var>radix</var> is 16, the number representation may optionally begin with <b>\"0x\"</b> or <b>\"0X\"</b>.</p>\n      <p>It is the <dfn><code class=\"emu-intrinsic\">%parseInt%</code></dfn> intrinsic object.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Let <var>inputString</var> be ? ToString(<var>string</var>).</li><li>Let <var>trimmedString</var> be ! TrimString(<var>inputString</var>, <emu-const>start</emu-const>).</li><li>Let <var>sign</var> be 1.</li><li>If <var>trimmedString</var> is not empty and the first code unit of <var>trimmedString</var> is the code unit 0x002D (HYPHEN-MINUS), set <var>sign</var> to -1.</li><li>If <var>trimmedString</var> is not empty and the first code unit of <var>trimmedString</var> is either the code unit 0x002B (PLUS SIGN) or the code unit 0x002D (HYPHEN-MINUS), set <var>trimmedString</var> to the substring of <var>trimmedString</var> from index 1.</li><li>Let <var>radixMV</var> be ℝ(? ToInt32(<var>radix</var>)).</li><li>Let <var>stripPrefix</var> be <b>true</b>.</li><li>If <var>radixMV</var> ≠ 0, then<ol><li>If <var>radixMV</var> &lt; 2 or <var>radixMV</var> > 36, return <b>NaN</b>.</li><li>If <var>radixMV</var> ≠ 16, set <var>stripPrefix</var> to <b>false</b>.</li></ol></li><li>Else,<ol><li>Set <var>radixMV</var> to 10.</li></ol></li><li>If <var>stripPrefix</var> is <b>true</b>, then<ol><li>If the length of <var>trimmedString</var> ≥ 2 and the first two code units of <var>trimmedString</var> are either <b>\"0x\"</b> or <b>\"0X\"</b>, then<ol><li>Set <var>trimmedString</var> to the substring of <var>trimmedString</var> from index 2.</li><li>Set <var>radixMV</var> to 16.</li></ol></li></ol></li><li>If <var>trimmedString</var> contains a code unit that is not a radix-<var>radixMV</var> digit, let <var>end</var> be the index within <var>trimmedString</var> of the first such code unit; else let <var>end</var> be the length of <var>trimmedString</var>.</li><li>Let <var>numberString</var> be the substring of <var>trimmedString</var> from 0 to <var>end</var>.</li><li>If <var>numberString</var> is empty, return <b>NaN</b>.</li><li>Let <var>mathInt</var> be the integer value that is represented by <var>numberString</var> in radix-<var>radixMV</var> notation, using the letters <b>A</b> through <b>Z</b> and <b>a</b> through <b>z</b> for digits with values 10 through 35. (However, if <var>radixMV</var> = 10 and <var>numberString</var> contains more than 20 significant digits, every significant digit after the 20th may be replaced by a 0 digit, at the option of the implementation; and if <var>radixMV</var> is not one of 2, 4, 8, 10, 16, or 32, then <var>mathInt</var> may be an implementation-approximated integer representing the integer value denoted by <var>numberString</var> in radix-<var>radixMV</var> notation.)</li><li>If <var>mathInt</var> = 0, then<ol><li>If <var>sign</var> = -1, return <b>-0</b><sub>𝔽</sub>.</li><li>Return <b>+0</b><sub>𝔽</sub>.</li></ol></li><li>Return 𝔽(<var>sign</var> × <var>mathInt</var>).</li></ol></emu-alg>\n      <emu-note>\n        <p>This function may interpret only a leading portion of <var>string</var> as an integer value; it ignores any code units that cannot be interpreted as part of the notation of an integer, and no indication is given that any such code units were ignored.</p>\n      </emu-note>\n    ","2.6":"<p>Uniform Resource Identifiers, or URIs, are Strings that identify resources (e.g. web pages or files) and transport protocols by which to access them (e.g. HTTP or FTP) on the Internet. The ECMAScript language itself does not provide any support for using URIs except for functions that encode and decode URIs as described in this section. <code>encodeURI</code> and <code>decodeURI</code> are intended to work with complete URIs; they assume that any reserved characters are intended to have special meaning (e.g., as delimiters) and so are not encoded. <code>encodeURIComponent</code> and <code>decodeURIComponent</code> are intended to work with the individual components of a URI; they assume that any reserved characters represent text and must be encoded to avoid special meaning when the component is part of a complete URI.</p>\n      <emu-note>\n        <p>The set of reserved characters is based upon RFC 2396 and does not reflect changes introduced by the more recent RFC 3986.</p>\n      </emu-note>\n      <emu-note>\n        <p>Many implementations of ECMAScript provide additional functions and methods that manipulate web pages; these functions are beyond the scope of this standard.</p>\n      </emu-note>\n\n      ","2.6.1":"<p>This function computes a new version of a URI in which each escape sequence and UTF-8 encoding of the sort that might be introduced by the <code>encodeURI</code> function is replaced with the UTF-16 encoding of the code point that it represents. Escape sequences that could not have been introduced by <code>encodeURI</code> are not replaced.</p>\n        <p>It is the <dfn><code class=\"emu-intrinsic\">%decodeURI%</code></dfn> intrinsic object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>uriString</var> be ? ToString(<var>encodedURI</var>).</li><li>Let <var>preserveEscapeSet</var> be <b>\";/?:@&=+$,#\"</b>.</li><li>Return ? Decode(<var>uriString</var>, <var>preserveEscapeSet</var>).</li></ol></emu-alg>\n      ","2.6.2":"<p>This function computes a new version of a URI in which each escape sequence and UTF-8 encoding of the sort that might be introduced by the <code>encodeURIComponent</code> function is replaced with the UTF-16 encoding of the code point that it represents.</p>\n        <p>It is the <dfn><code class=\"emu-intrinsic\">%decodeURIComponent%</code></dfn> intrinsic object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>componentString</var> be ? ToString(<var>encodedURIComponent</var>).</li><li>Let <var>preserveEscapeSet</var> be the empty String.</li><li>Return ? Decode(<var>componentString</var>, <var>preserveEscapeSet</var>).</li></ol></emu-alg>\n      ","2.6.3":"<p>This function computes a new version of a UTF-16 encoded (<a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>) URI in which each instance of certain code points is replaced by one, two, three, or four escape sequences representing the UTF-8 encoding of the code point.</p>\n        <p>It is the <dfn><code class=\"emu-intrinsic\">%encodeURI%</code></dfn> intrinsic object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>uriString</var> be ? ToString(<var>uri</var>).</li><li>Let <var>extraUnescaped</var> be <b>\";/?:@&=+$,#\"</b>.</li><li>Return ? Encode(<var>uriString</var>, <var>extraUnescaped</var>).</li></ol></emu-alg>\n      ","2.6.4":"<p>This function computes a new version of a UTF-16 encoded (<a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>) URI in which each instance of certain code points is replaced by one, two, three, or four escape sequences representing the UTF-8 encoding of the code point.</p>\n        <p>It is the <dfn><code class=\"emu-intrinsic\">%encodeURIComponent%</code></dfn> intrinsic object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let <var>componentString</var> be ? ToString(<var>uriComponent</var>).</li><li>Let <var>extraUnescaped</var> be the empty String.</li><li>Return ? Encode(<var>componentString</var>, <var>extraUnescaped</var>).</li></ol></emu-alg>\n      ","2.6.5":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It performs URI encoding and escaping, interpreting <var>string</var> as a sequence of UTF-16 encoded code points as described in <a href=\"/ecmascript-data-types-and-values#sec-ecmascript-language-types-string-type\">6.1.4</a>. If a character is identified as unreserved in RFC 2396 or appears in <var>extraUnescaped</var>, it is not escaped.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>len</var> be the length of <var>string</var>.</li><li>Let <var>result</var> be the empty String.</li><li>Let <var>alwaysUnescaped</var> be the string-concatenation of the ASCII word characters and <b>\"-.!~\\</b>'()\"*.</li><li>Let <var>unescapedSet</var> be the string-concatenation of <var>alwaysUnescaped</var> and <var>extraUnescaped</var>.</li><li>Let <var>k</var> be 0.</li><li>Repeat, while <var>k</var> &lt; <var>len</var>,<ol><li>Let <var>codeUnit</var> be the code unit at index <var>k</var> within <var>string</var>.</li><li>If <var>unescapedSet</var> contains <var>codeUnit</var>, then<ol><li>Set <var>k</var> to <var>k</var> + 1.</li><li>Set <var>result</var> to the string-concatenation of <var>result</var> and <var>codeUnit</var>.</li></ol></li><li>Else,<ol><li>Let <var>cp</var> be CodePointAt(<var>string</var>, <var>k</var>).</li><li>If <var>cp</var>.[[IsUnpairedSurrogate]] is <b>true</b>, throw a <b>URIError</b> exception.</li><li>Set <var>k</var> to <var>k</var> + <var>cp</var>.[[CodeUnitCount]].</li><li>Let <var>octets</var> be the List of octets resulting by applying the UTF-8 transformation to <var>cp</var>.[[CodePoint]].</li><li>For each element <var>octet</var> of <var>octets</var>, do<ol><li>Let <var>hex</var> be the String representation of <var>octet</var>, formatted as an uppercase hexadecimal number.</li><li>Set <var>result</var> to the string-concatenation of <var>result</var>, <b>\"%\"</b>, and StringPad(<var>hex</var>, 2, <b>\"0\"</b>, <emu-const>start</emu-const>).</li></ol></li></ol></li></ol></li><li>Return <var>result</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>Because percent-encoding is used to represent individual octets, a single code point may be expressed as multiple consecutive escape sequences (one for each of its 8-bit UTF-8 code units).</p>\n        </emu-note>\n      ","2.6.6":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It performs URI unescaping and decoding, preserving any escape sequences that correspond to Basic Latin characters in <var>preserveEscapeSet</var>.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>len</var> be the length of <var>string</var>.</li><li>Let <var>result</var> be the empty String.</li><li>Let <var>k</var> be 0.</li><li>Repeat, while <var>k</var> &lt; <var>len</var>,<ol><li>Let <var>codeUnit</var> be the code unit at index <var>k</var> within <var>string</var>.</li><li>Let <var>segment</var> be <var>codeUnit</var>.</li><li>If <var>codeUnit</var> is the code unit 0x0025 (PERCENT SIGN), then<ol><li>If <var>k</var> + 3 > <var>len</var>, throw a <b>URIError</b> exception.</li><li>Let <var>escape</var> be the substring of <var>string</var> from <var>k</var> to <var>k</var> + 3.</li><li>Let <var>firstOctet</var> be ParseHexOctet(<var>string</var>, <var>k</var> + 1).</li><li>If <var>firstOctet</var> is not an integer, throw a <b>URIError</b> exception.</li><li>Set <var>k</var> to <var>k</var> + 2.</li><li>Let <var>n</var> be the number of leading 1 bits in <var>firstOctet</var>.</li><li>If <var>n</var> = 0, then<ol><li>Let <var>asciiChar</var> be the code unit whose numeric value is <var>firstOctet</var>.</li><li>If <var>preserveEscapeSet</var> contains <var>asciiChar</var>, set <var>segment</var> to <var>escape</var>; else set <var>segment</var> to <var>asciiChar</var>.</li></ol></li><li>Else,<ol><li>If <var>n</var> = 1 or <var>n</var> > 4, throw a <b>URIError</b> exception.</li><li>Let <var>octets</var> be « <var>firstOctet</var> ».</li><li>Let <var>j</var> be 1.</li><li>Repeat, while <var>j</var> &lt; <var>n</var>,<ol><li>Set <var>k</var> to <var>k</var> + 1.</li><li>If <var>k</var> + 3 > <var>len</var>, throw a <b>URIError</b> exception.</li><li>If the code unit at index <var>k</var> within <var>string</var> is not the code unit 0x0025 (PERCENT SIGN), throw a <b>URIError</b> exception.</li><li>Let <var>continuationByte</var> be ParseHexOctet(<var>string</var>, <var>k</var> + 1).</li><li>If <var>continuationByte</var> is not an integer, throw a <b>URIError</b> exception.</li><li>Append <var>continuationByte</var> to <var>octets</var>.</li><li>Set <var>k</var> to <var>k</var> + 2.</li><li>Set <var>j</var> to <var>j</var> + 1.</li></ol></li><li>Assert: The length of <var>octets</var> is <var>n</var>.</li><li>If <var>octets</var> does not contain a valid UTF-8 encoding of a Unicode code point, throw a <b>URIError</b> exception.</li><li>Let <var>codePoint</var> be the code point obtained by applying the UTF-8 transformation to <var>octets</var>, that is, from a List of octets into a 21-bit value.</li><li>Set <var>segment</var> to UTF16EncodeCodePoint(<var>codePoint</var>).</li></ol></li></ol></li><li>Set <var>result</var> to the string-concatenation of <var>result</var> and <var>segment</var>.</li><li>Set <var>k</var> to <var>k</var> + 1.</li></ol></li><li>Return <var>result</var>.</li></ol></emu-alg>\n        <emu-note>\n          <p>RFC 3629 prohibits the decoding of invalid UTF-8 octet sequences. For example, the invalid sequence 0xC0 0x80 must not decode into the code unit 0x0000. Implementations of the Decode algorithm are required to throw a <b>URIError</b> when encountering such invalid sequences.</p>\n        </emu-note>\n      ","2.6.7":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It parses a sequence of two hexadecimal characters at the specified <var>position</var> in <var>string</var> into an unsigned 8-bit integer.</dd>\n        </dl>\n        <emu-alg><ol><li>Let <var>len</var> be the length of <var>string</var>.</li><li>Assert: <var>position</var> + 2 ≤ <var>len</var>.</li><li>Let <var>hexDigits</var> be the substring of <var>string</var> from <var>position</var> to <var>position</var> + 2.</li><li>Let <var>parseResult</var> be ParseText(<var>hexDigits</var>, <emu-nt>HexDigits[~Sep]</emu-nt>).</li><li>If <var>parseResult</var> is not a Parse Node, return <var>parseResult</var>.</li><li>Let <var>n</var> be the MV of <var>parseResult</var>.</li><li>Assert: <var>n</var> is in the inclusive interval from 0 to 255.</li><li>Return <var>n</var>.</li></ol></emu-alg>\n      ","3.1":"<p>See <a href=\"/fundamental-objects#sec-aggregate-error-constructor\">20.5.7.1</a>.</p>\n    ","3.2":"<p>See <a href=\"/indexed-collections#sec-array-constructor\">23.1.1</a>.</p>\n    ","3.3":"<p>See <a href=\"/structured-data#sec-arraybuffer-constructor\">25.1.4</a>.</p>\n    ","3.4":"<p>See <a href=\"/numbers-and-dates#sec-bigint-constructor\">21.2.1</a>.</p>\n    ","3.5":"<p>See <a href=\"/indexed-collections#sec-typedarray-constructors\">23.2.5</a>.</p>\n    ","3.6":"<p>See <a href=\"/indexed-collections#sec-typedarray-constructors\">23.2.5</a>.</p>\n    ","3.7":"<p>See <a href=\"/fundamental-objects#sec-boolean-constructor\">20.3.1</a>.</p>\n    ","3.8":"<p>See <a href=\"/structured-data#sec-dataview-constructor\">25.3.2</a>.</p>\n    ","3.9":"<p>See <a href=\"/numbers-and-dates#sec-date-constructor\">21.4.2</a>.</p>\n    ","3.10":"<p>See <a href=\"/fundamental-objects#sec-error-constructor\">20.5.1</a>.</p>\n    ","3.11":"<p>See <a href=\"/fundamental-objects#sec-native-error-types-used-in-this-standard-evalerror\">20.5.5.1</a>.</p>\n    ","3.12":"<p>See <a href=\"/managing-memory#sec-finalization-registry-constructor\">26.2.1</a>.</p>\n    ","3.13":"<p>See <a href=\"/indexed-collections#sec-typedarray-constructors\">23.2.5</a>.</p>\n    ","3.14":"<p>See <a href=\"/indexed-collections#sec-typedarray-constructors\">23.2.5</a>.</p>\n    ","3.15":"<p>See <a href=\"/indexed-collections#sec-typedarray-constructors\">23.2.5</a>.</p>\n    ","3.16":"<p>See <a href=\"/fundamental-objects#sec-function-constructor\">20.2.1</a>.</p>\n    ","3.17":"<p>See <a href=\"/indexed-collections#sec-typedarray-constructors\">23.2.5</a>.</p>\n    ","3.18":"<p>See <a href=\"/indexed-collections#sec-typedarray-constructors\">23.2.5</a>.</p>\n    ","3.19":"<p>See <a href=\"/indexed-collections#sec-typedarray-constructors\">23.2.5</a>.</p>\n    ","3.20":"<p>See <a href=\"/control-abstraction-objects#sec-iterator-constructor\">27.1.3.1</a>.</p>\n    ","3.21":"<p>See <a href=\"/keyed-collections#sec-map-constructor\">24.1.1</a>.</p>\n    ","3.22":"<p>See <a href=\"/numbers-and-dates#sec-number-constructor\">21.1.1</a>.</p>\n    ","3.23":"<p>See <a href=\"/fundamental-objects#sec-object-constructor\">20.1.1</a>.</p>\n    ","3.24":"<p>See <a href=\"/control-abstraction-objects#sec-promise-constructor\">27.2.3</a>.</p>\n    ","3.25":"<p>See <a href=\"/reflection#sec-proxy-constructor\">28.2.1</a>.</p>\n    ","3.26":"<p>See <a href=\"/fundamental-objects#sec-native-error-types-used-in-this-standard-rangeerror\">20.5.5.2</a>.</p>\n    ","3.27":"<p>See <a href=\"/fundamental-objects#sec-native-error-types-used-in-this-standard-referenceerror\">20.5.5.3</a>.</p>\n    ","3.28":"<p>See <a href=\"/text-processing#sec-regexp-constructor\">22.2.4</a>.</p>\n    ","3.29":"<p>See <a href=\"/keyed-collections#sec-set-constructor\">24.2.2</a>.</p>\n    ","3.30":"<p>See <a href=\"/structured-data#sec-sharedarraybuffer-constructor\">25.2.3</a>.</p>\n    ","3.31":"<p>See <a href=\"/text-processing#sec-string-constructor\">22.1.1</a>.</p>\n    ","3.32":"<p>See <a href=\"/fundamental-objects#sec-symbol-constructor\">20.4.1</a>.</p>\n    ","3.33":"<p>See <a href=\"/fundamental-objects#sec-native-error-types-used-in-this-standard-syntaxerror\">20.5.5.4</a>.</p>\n    ","3.34":"<p>See <a href=\"/fundamental-objects#sec-native-error-types-used-in-this-standard-typeerror\">20.5.5.5</a>.</p>\n    ","3.35":"<p>See <a href=\"/indexed-collections#sec-typedarray-constructors\">23.2.5</a>.</p>\n    ","3.36":"<p>See <a href=\"/indexed-collections#sec-typedarray-constructors\">23.2.5</a>.</p>\n    ","3.37":"<p>See <a href=\"/indexed-collections#sec-typedarray-constructors\">23.2.5</a>.</p>\n    ","3.38":"<p>See <a href=\"/indexed-collections#sec-typedarray-constructors\">23.2.5</a>.</p>\n    ","3.39":"<p>See <a href=\"/fundamental-objects#sec-native-error-types-used-in-this-standard-urierror\">20.5.5.6</a>.</p>\n    ","3.40":"<p>See <a href=\"/keyed-collections#sec-weakmap-constructor\">24.3.1</a>.</p>\n    ","3.41":"<p>See <a href=\"/managing-memory#sec-weak-ref-constructor\">26.1.1</a>.</p>\n    ","3.42":"<p>See <a href=\"/keyed-collections#sec-weakset-objects\">24.4</a>.</p>\n    ","4.1":"<p>See <a href=\"/structured-data#sec-atomics-object\">25.4</a>.</p>\n    ","4.2":"<p>See <a href=\"/structured-data#sec-json-object\">25.5</a>.</p>\n    ","4.3":"<p>See <a href=\"/numbers-and-dates#sec-math-object\">21.3</a>.</p>\n    ","4.4":"<p>See <a href=\"/reflection#sec-reflect-object\">28.1</a>.</p>\n    "};
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
