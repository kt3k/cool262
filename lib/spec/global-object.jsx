// Generated from ecma262/spec.html — do not edit by hand.
const sections = {"1":"","2":"","3":"","4":"","":"<p>The <dfn variants=\"global objects\">global object</dfn>:</p>\n  <ul>\n    <li>is created before control enters any execution context.</li>\n    <li>does not have a [[Construct]] internal method; it cannot be used as a constructor with the `new` operator.</li>\n    <li>does not have a [[Call]] internal method; it cannot be invoked as a function.</li>\n    <li>has a [[Prototype]] internal slot whose value is host-defined.</li>\n    <li>may have host-defined properties in addition to the properties defined in this specification. This may include a property whose value is the global object itself.</li>\n  </ul>\n\n  ","1.1":"<p>The initial value of the *\"globalThis\"* property of the global object in a Realm Record _realm_ is _realm_.[[GlobalEnv]].[[GlobalThisValue]].</p>\n      <p>This property has the attributes { [[Writable]]: *true*, [[Enumerable]]: *false*, [[Configurable]]: *true* }.</p>\n    ","1.2":"<p>The value of `Infinity` is *+∞*<sub>𝔽</sub> (see 6.1.6.1). This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *false* }.</p>\n    ","1.3":"<p>The value of `NaN` is *NaN* (see 6.1.6.1). This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *false* }.</p>\n    ","1.4":"<p>The value of `undefined` is *undefined* (see 6.1.1). This property has the attributes { [[Writable]]: *false*, [[Enumerable]]: *false*, [[Configurable]]: *false* }.</p>\n    ","2.1":"<p>This function is the <dfn>%eval%</dfn> intrinsic object.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Return ? PerformEval(_source_, *false*, *false*).</li></ol></emu-alg>\n\n      ","2.1.1":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Assert: If _direct_ is *false*, then _strictCaller_ is also *false*.</li><li>If _source_ is not a String, return _source_.</li><li>Let _evalRealm_ be the current Realm Record.</li><li>NOTE: In the case of a direct eval, _evalRealm_ is the realm of both the caller of `eval` and of the `eval` function itself.</li><li>Perform ? HostEnsureCanCompileStrings(_evalRealm_, « », _source_, _direct_).</li><li>Let _inFunction_ be *false*.</li><li>Let _inMethod_ be *false*.</li><li>Let _inDerivedConstructor_ be *false*.</li><li>Let _inClassFieldInitializer_ be *false*.</li><li>If _direct_ is *true*, then<ol><li>Let _thisEnvRec_ be GetThisEnvironment().</li><li>If _thisEnvRec_ is a Function Environment Record, then<ol><li>Let _func_ be _thisEnvRec_.[[FunctionObject]].</li><li>Set _inFunction_ to *true*.</li><li>Set _inMethod_ to _thisEnvRec_.HasSuperBinding().</li><li>If _func_.[[ConstructorKind]] is ~derived~, set _inDerivedConstructor_ to *true*.</li><li>Let _classFieldInitializerName_ be _func_.[[ClassFieldInitializerName]].</li><li>If _classFieldInitializerName_ is not ~empty~, set _inClassFieldInitializer_ to *true*.</li></ol></li></ol></li><li>Perform the following substeps in an implementation-defined order, possibly interleaving parsing and error detection:<ol><li>Let _script_ be ParseText(_source_, |Script|).</li><li>If _script_ is a List of errors, throw a *SyntaxError* exception.</li><li>If _script_ Contains |ScriptBody| is *false*, return *undefined*.</li><li>Let _body_ be the |ScriptBody| of _script_.</li><li>If _inFunction_ is *false* and _body_ Contains |NewTarget|, throw a *SyntaxError* exception.</li><li>If _inMethod_ is *false* and _body_ Contains |SuperProperty|, throw a *SyntaxError* exception.</li><li>If _inDerivedConstructor_ is *false* and _body_ Contains |SuperCall|, throw a *SyntaxError* exception.</li><li>If _inClassFieldInitializer_ is *true* and ContainsArguments of _body_ is *true*, throw a *SyntaxError* exception.</li></ol></li><li>If _strictCaller_ is *true*, let _strictEval_ be *true*.</li><li>Else, let _strictEval_ be ScriptIsStrict of _script_.</li><li>Let _runningContext_ be the running execution context.</li><li>NOTE: If _direct_ is *true*, _runningContext_ will be the execution context that performed the direct eval. If _direct_ is *false*, _runningContext_ will be the execution context for the invocation of the `eval` function.</li><li>If _direct_ is *true*, then<ol><li>Let _lexEnv_ be NewDeclarativeEnvironment(_runningContext_'s LexicalEnvironment).</li><li>Let _varEnv_ be _runningContext_'s VariableEnvironment.</li><li>Let _privateEnv_ be _runningContext_'s PrivateEnvironment.</li></ol></li><li>Else,<ol><li>Let _lexEnv_ be NewDeclarativeEnvironment(_evalRealm_.[[GlobalEnv]]).</li><li>Let _varEnv_ be _evalRealm_.[[GlobalEnv]].</li><li>Let _privateEnv_ be *null*.</li></ol></li><li>If _strictEval_ is *true*, set _varEnv_ to _lexEnv_.</li><li>If _runningContext_ is not already suspended, suspend _runningContext_.</li><li>Let _evalContext_ be a new ECMAScript code execution context.</li><li>Set _evalContext_'s Function to *null*.</li><li>Set _evalContext_'s Realm to _evalRealm_.</li><li>Set _evalContext_'s ScriptOrModule to _runningContext_'s ScriptOrModule.</li><li>Set _evalContext_'s VariableEnvironment to _varEnv_.</li><li>Set _evalContext_'s LexicalEnvironment to _lexEnv_.</li><li>Set _evalContext_'s PrivateEnvironment to _privateEnv_.</li><li>Push _evalContext_ onto the execution context stack; _evalContext_ is now the running execution context.</li><li>Let _result_ be Completion(EvalDeclarationInstantiation(_body_, _varEnv_, _lexEnv_, _privateEnv_, _strictEval_)).</li><li>If _result_ is a normal completion, then<ol><li>Set _result_ to Completion(Evaluation of _body_).</li></ol></li><li>If _result_ is a normal completion and _result_.[[Value]] is ~empty~, then<ol><li>Set _result_ to NormalCompletion(*undefined*).</li></ol></li><li>Suspend _evalContext_ and remove it from the execution context stack.</li><li>Resume the context that is now on the top of the execution context stack as the running execution context.</li><li>Return ? _result_.</li></ol></emu-alg>\n        <emu-note>\n          <p>The eval code cannot instantiate variable or function bindings in the variable environment of the calling context that invoked the eval if either the code of the calling context or the eval code is strict mode code. Instead such bindings are instantiated in a new VariableEnvironment that is only accessible to the eval code. Bindings introduced by `let`, `const`, or `class` declarations are always instantiated in a new LexicalEnvironment.</p>\n        </emu-note>\n      ","2.1.2":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It allows host environments to block certain ECMAScript functions which allow developers to interpret and evaluate strings as ECMAScript code.</dd>\n        </dl>\n        <p>\n          _parameterStrings_ represents the strings that, when using one of the function constructors, will be concatenated together to build the parameters list. _bodyString_ represents the function body or the string passed to an `eval` call.\n          _direct_ signifies whether the evaluation is a direct eval.\n        </p>\n        <p>The default implementation of HostEnsureCanCompileStrings is to return NormalCompletion(~unused~).</p>\n      ","2.1.3":"<dl class=\"header\">\n        </dl>\n        <emu-alg><ol><li>Let _varNames_ be the VarDeclaredNames of _body_.</li><li>Let _varDeclarations_ be the VarScopedDeclarations of _body_.</li><li>If _strict_ is *false*, then<ol><li>If _varEnv_ is a Global Environment Record, then<ol><li>For each element _name_ of _varNames_, do<ol><li>If HasLexicalDeclaration(_varEnv_, _name_) is *true*, throw a *SyntaxError* exception.</li><li>NOTE: `eval` will not create a global var declaration that would be shadowed by a global lexical declaration.</li></ol></li></ol></li><li>Let _thisEnv_ be _lexEnv_.</li><li>Assert: The following loop will terminate.</li><li>Repeat, while _thisEnv_ and _varEnv_ are not the same Environment Record,<ol><li>If _thisEnv_ is not an Object Environment Record, then<ol><li>NOTE: The environment of with statements cannot contain any lexical declaration so it doesn't need to be checked for var/let hoisting conflicts.</li><li>For each element _name_ of _varNames_, do<ol><li>If ! _thisEnv_.HasBinding(_name_) is *true*, then<ol><li>If the host is a web browser or otherwise supports B.3.4, then<ol><li>If _thisEnv_ is not the Environment Record for a |Catch| clause, throw a *SyntaxError* exception.</li></ol></li><li>Else,<ol><li>Throw a *SyntaxError* exception.</li></ol></li></ol></li><li>NOTE: A direct eval will not hoist var declaration over a like-named lexical declaration.</li></ol></li></ol></li><li>Set _thisEnv_ to _thisEnv_.[[OuterEnv]].</li></ol></li></ol></li><li>Let _privateIdentifiers_ be a new empty List.</li><li>Let _pointer_ be _privateEnv_.</li><li>Repeat, while _pointer_ is not *null*,<ol><li>For each Private Name _binding_ of _pointer_.[[Names]], do<ol><li>If _privateIdentifiers_ does not contain _binding_.[[Description]], append _binding_.[[Description]] to _privateIdentifiers_.</li></ol></li><li>Set _pointer_ to _pointer_.[[OuterPrivateEnvironment]].</li></ol></li><li>If AllPrivateIdentifiersValid of _body_ with argument _privateIdentifiers_ is *false*, throw a *SyntaxError* exception.</li><li>Let _functionsToInitialize_ be a new empty List.</li><li>Let _declaredFunctionNames_ be a new empty List.</li><li>For each element _d_ of _varDeclarations_, in reverse List order, do<ol><li>If _d_ is not either a |VariableDeclaration|, a |ForBinding|, or a |BindingIdentifier|, then<ol><li>Assert: _d_ is either a |FunctionDeclaration|, a |GeneratorDeclaration|, an |AsyncFunctionDeclaration|, or an |AsyncGeneratorDeclaration|.</li><li>NOTE: If there are multiple function declarations for the same name, the last declaration is used.</li><li>Let _fn_ be the sole element of the BoundNames of _d_.</li><li>If _declaredFunctionNames_ does not contain _fn_, then<ol><li>If _varEnv_ is a Global Environment Record, then<ol><li>Let _fnDefinable_ be ? CanDeclareGlobalFunction(_varEnv_, _fn_).</li><li>If _fnDefinable_ is *false*, throw a *TypeError* exception.</li></ol></li><li>Append _fn_ to _declaredFunctionNames_.</li><li>Insert _d_ as the first element of _functionsToInitialize_.</li></ol></li></ol></li></ol></li><li>Let _declaredVarNames_ be a new empty List.</li><li>For each element _d_ of _varDeclarations_, do<ol><li>If _d_ is either a |VariableDeclaration|, a |ForBinding|, or a |BindingIdentifier|, then<ol><li>For each String _vn_ of the BoundNames of _d_, do<ol><li>If _declaredFunctionNames_ does not contain _vn_, then<ol><li>If _varEnv_ is a Global Environment Record, then<ol><li>Let _vnDefinable_ be ? CanDeclareGlobalVar(_varEnv_, _vn_).</li><li>If _vnDefinable_ is *false*, throw a *TypeError* exception.</li></ol></li><li>If _declaredVarNames_ does not contain _vn_, then<ol><li>Append _vn_ to _declaredVarNames_.</li></ol></li></ol></li></ol></li></ol></li></ol></li><li>If _strict_ is *false* and the host is a web browser or otherwise supports B.3.2, then<ol><li>Let _declaredFunctionOrVarNames_ be the list-concatenation of _declaredFunctionNames_ and _declaredVarNames_.</li><li>For each |FunctionDeclaration| _f_ that is directly contained in the |StatementList| of any |Block|, |CaseClause|, or |DefaultClause| _x_ such that _body_ Contains _x_ is *true*, do<ol><li>Let _funcName_ be the StringValue of the |BindingIdentifier| of _f_.</li><li>If replacing the |FunctionDeclaration| _f_ with a |VariableStatement| that has _funcName_ as a |BindingIdentifier| would not produce any Early Errors for _body_, then<ol><li>Let _bindingExists_ be *false*.</li><li>Set _thisEnv_ to _lexEnv_.</li><li>Assert: The following loop will terminate.</li><li>Repeat, while _thisEnv_ is not _varEnv_,<ol><li>If _thisEnv_ is not an Object Environment Record, then<ol><li>If ! _thisEnv_.HasBinding(_funcName_) is *true*, then<ol><li>If the host is a web browser or otherwise supports B.3.4, then<ol><li>If _thisEnv_ is not the Environment Record for a |Catch| clause, set _bindingExists_ to *true*.</li></ol></li><li>Else,<ol><li>Set _bindingExists_ to *true*.</li></ol></li></ol></li></ol></li><li>Set _thisEnv_ to _thisEnv_.[[OuterEnv]].</li></ol></li><li>If _bindingExists_ is *false* and _varEnv_ is a Global Environment Record, then<ol><li>If HasLexicalDeclaration(_varEnv_, _funcName_) is *false*, then<ol><li>Let _fnDefinable_ be ? CanDeclareGlobalVar(_varEnv_, _funcName_).</li></ol></li><li>Else,<ol><li>Let _fnDefinable_ be *false*.</li></ol></li></ol></li><li>Else,<ol><li>Let _fnDefinable_ be *true*.</li></ol></li><li>If _bindingExists_ is *false* and _fnDefinable_ is *true*, then<ol><li>If _declaredFunctionOrVarNames_ does not contain _funcName_, then<ol><li>If _varEnv_ is a Global Environment Record, then<ol><li>Perform ? CreateGlobalVarBinding(_varEnv_, _funcName_, *true*).</li></ol></li><li>Else,<ol><li>Set _bindingExists_ to ! _varEnv_.HasBinding(_funcName_).</li><li>If _bindingExists_ is *false*, then<ol><li>Perform ! _varEnv_.CreateMutableBinding(_funcName_, *true*).</li><li>Perform ! _varEnv_.InitializeBinding(_funcName_, *undefined*).</li></ol></li></ol></li><li>Append _funcName_ to _declaredFunctionOrVarNames_.</li></ol></li><li>When the |FunctionDeclaration| _f_ is evaluated, perform the following steps in place of the |FunctionDeclaration| Evaluation algorithm provided in 15.2.6:<ol><li>Let _gEnv_ be the running execution context's VariableEnvironment.</li><li>Let _bEnv_ be the running execution context's LexicalEnvironment.</li><li>Let _fObj_ be ! _bEnv_.GetBindingValue(_funcName_, *false*).</li><li>Perform ? <emu-meta effects=\"user-code\">_gEnv_.SetMutableBinding(_funcName_, _fObj_, *false*)</emu-meta>.</li><li>Return ~unused~.</li></ol></li></ol></li></ol></li></ol></li></ol></li><li>NOTE: No abnormal terminations occur after this algorithm step unless _varEnv_ is a Global Environment Record and the global object is a Proxy exotic object.</li><li>Let _lexDeclarations_ be the LexicallyScopedDeclarations of _body_.</li><li>For each element _d_ of _lexDeclarations_, do<ol><li>NOTE: Lexically declared names are only instantiated here but not initialized.</li><li>For each element _dn_ of the BoundNames of _d_, do<ol><li>If IsConstantDeclaration of _d_ is *true*, then<ol><li>Perform ? _lexEnv_.CreateImmutableBinding(_dn_, *true*).</li></ol></li><li>Else,<ol><li>Perform ? _lexEnv_.CreateMutableBinding(_dn_, *false*).</li></ol></li></ol></li></ol></li><li>For each Parse Node _f_ of _functionsToInitialize_, do<ol><li>Let _fn_ be the sole element of the BoundNames of _f_.</li><li>Let _fo_ be InstantiateFunctionObject of _f_ with arguments _lexEnv_ and _privateEnv_.</li><li>If _varEnv_ is a Global Environment Record, then<ol><li>Perform ? CreateGlobalFunctionBinding(_varEnv_, _fn_, _fo_, *true*).</li></ol></li><li>Else,<ol><li>Let _bindingExists_ be ! _varEnv_.HasBinding(_fn_).</li><li>If _bindingExists_ is *false*, then<ol><li>NOTE: The following invocation cannot return an abrupt completion because of the validation preceding step step-evaldeclarationinstantiation-post-validation.</li><li>Perform ! _varEnv_.CreateMutableBinding(_fn_, *true*).</li><li>Perform ! _varEnv_.InitializeBinding(_fn_, _fo_).</li></ol></li><li>Else,<ol><li>Perform ! _varEnv_.SetMutableBinding(_fn_, _fo_, *false*).</li></ol></li></ol></li></ol></li><li>For each String _vn_ of _declaredVarNames_, do<ol><li>If _varEnv_ is a Global Environment Record, then<ol><li>Perform ? CreateGlobalVarBinding(_varEnv_, _vn_, *true*).</li></ol></li><li>Else,<ol><li>Let _bindingExists_ be ! _varEnv_.HasBinding(_vn_).</li><li>If _bindingExists_ is *false*, then<ol><li>NOTE: The following invocation cannot return an abrupt completion because of the validation preceding step step-evaldeclarationinstantiation-post-validation.</li><li>Perform ! _varEnv_.CreateMutableBinding(_vn_, *true*).</li><li>Perform ! _varEnv_.InitializeBinding(_vn_, *undefined*).</li></ol></li></ol></li></ol></li><li>Return ~unused~.</li></ol></emu-alg>\n      ","2.2":"<p>This function is the <dfn>%isFinite%</dfn> intrinsic object.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _num_ be ? ToNumber(_number_).</li><li>If _num_ is finite, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n    ","2.3":"<p>This function is the <dfn>%isNaN%</dfn> intrinsic object.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _num_ be ? ToNumber(_number_).</li><li>If _num_ is *NaN*, return *true*.</li><li>Return *false*.</li></ol></emu-alg>\n      <emu-note>\n        <p>A reliable way for ECMAScript code to test if a value `X` is *NaN* is an expression of the form `X !== X`. The result will be *true* if and only if `X` is *NaN*.</p>\n      </emu-note>\n    ","2.4":"<p>This function produces a Number value dictated by interpretation of the contents of the _string_ argument as a decimal literal.</p>\n      <p>It is the <dfn>%parseFloat%</dfn> intrinsic object.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _inputString_ be ? ToString(_string_).</li><li>Let _trimmedString_ be ! TrimString(_inputString_, ~start~).</li><li>Let _trimmed_ be StringToCodePoints(_trimmedString_).</li><li>Let _trimmedPrefix_ be the longest prefix of _trimmed_ that satisfies the syntax of a |StrDecimalLiteral|, which might be _trimmed_ itself. If there is no such prefix, return *NaN*.</li><li>Let _parsedNumber_ be ParseText(_trimmedPrefix_, |StrDecimalLiteral|).</li><li>Assert: _parsedNumber_ is a Parse Node.</li><li>Return the StringNumericValue of _parsedNumber_.</li></ol></emu-alg>\n      <emu-note>\n        <p>This function may interpret only a leading portion of _string_ as a Number value; it ignores any code units that cannot be interpreted as part of the notation of a decimal literal, and no indication is given that any such code units were ignored.</p>\n      </emu-note>\n    ","2.5":"<p>This function produces an integral Number dictated by interpretation of the contents of _string_ according to the specified _radix_. Leading white space in _string_ is ignored. If _radix_ coerces to 0 (such as when it is *undefined*), it is assumed to be 10 except when the number representation begins with *\"0x\"* or *\"0X\"*, in which case it is assumed to be 16. If _radix_ is 16, the number representation may optionally begin with *\"0x\"* or *\"0X\"*.</p>\n      <p>It is the <dfn>%parseInt%</dfn> intrinsic object.</p>\n      <p>It performs the following steps when called:</p>\n      <emu-alg><ol><li>Let _inputString_ be ? ToString(_string_).</li><li>Let _trimmedString_ be ! TrimString(_inputString_, ~start~).</li><li>Let _sign_ be 1.</li><li>If _trimmedString_ is not empty and the first code unit of _trimmedString_ is the code unit 0x002D (HYPHEN-MINUS), set _sign_ to -1.</li><li>If _trimmedString_ is not empty and the first code unit of _trimmedString_ is either the code unit 0x002B (PLUS SIGN) or the code unit 0x002D (HYPHEN-MINUS), set _trimmedString_ to the substring of _trimmedString_ from index 1.</li><li>Let _radixMV_ be ℝ(? ToInt32(_radix_)).</li><li>Let _stripPrefix_ be *true*.</li><li>If _radixMV_ ≠ 0, then<ol><li>If _radixMV_ &lt; 2 or _radixMV_ > 36, return *NaN*.</li><li>If _radixMV_ ≠ 16, set _stripPrefix_ to *false*.</li></ol></li><li>Else,<ol><li>Set _radixMV_ to 10.</li></ol></li><li>If _stripPrefix_ is *true*, then<ol><li>If the length of _trimmedString_ ≥ 2 and the first two code units of _trimmedString_ are either *\"0x\"* or *\"0X\"*, then<ol><li>Set _trimmedString_ to the substring of _trimmedString_ from index 2.</li><li>Set _radixMV_ to 16.</li></ol></li></ol></li><li>If _trimmedString_ contains a code unit that is not a radix-_radixMV_ digit, let _end_ be the index within _trimmedString_ of the first such code unit; else let _end_ be the length of _trimmedString_.</li><li>Let _numberString_ be the substring of _trimmedString_ from 0 to _end_.</li><li>If _numberString_ is empty, return *NaN*.</li><li>Let _mathInt_ be the integer value that is represented by _numberString_ in radix-_radixMV_ notation, using the letters <b>A</b> through <b>Z</b> and <b>a</b> through <b>z</b> for digits with values 10 through 35. (However, if _radixMV_ = 10 and _numberString_ contains more than 20 significant digits, every significant digit after the 20th may be replaced by a 0 digit, at the option of the implementation; and if _radixMV_ is not one of 2, 4, 8, 10, 16, or 32, then _mathInt_ may be an implementation-approximated integer representing the integer value denoted by _numberString_ in radix-_radixMV_ notation.)</li><li>If _mathInt_ = 0, then<ol><li>If _sign_ = -1, return *-0*<sub>𝔽</sub>.</li><li>Return *+0*<sub>𝔽</sub>.</li></ol></li><li>Return 𝔽(_sign_ × _mathInt_).</li></ol></emu-alg>\n      <emu-note>\n        <p>This function may interpret only a leading portion of _string_ as an integer value; it ignores any code units that cannot be interpreted as part of the notation of an integer, and no indication is given that any such code units were ignored.</p>\n      </emu-note>\n    ","2.6":"<p>Uniform Resource Identifiers, or URIs, are Strings that identify resources (e.g. web pages or files) and transport protocols by which to access them (e.g. HTTP or FTP) on the Internet. The ECMAScript language itself does not provide any support for using URIs except for functions that encode and decode URIs as described in this section. `encodeURI` and `decodeURI` are intended to work with complete URIs; they assume that any reserved characters are intended to have special meaning (e.g., as delimiters) and so are not encoded. `encodeURIComponent` and `decodeURIComponent` are intended to work with the individual components of a URI; they assume that any reserved characters represent text and must be encoded to avoid special meaning when the component is part of a complete URI.</p>\n      <emu-note>\n        <p>The set of reserved characters is based upon RFC 2396 and does not reflect changes introduced by the more recent RFC 3986.</p>\n      </emu-note>\n      <emu-note>\n        <p>Many implementations of ECMAScript provide additional functions and methods that manipulate web pages; these functions are beyond the scope of this standard.</p>\n      </emu-note>\n\n      ","2.6.1":"<p>This function computes a new version of a URI in which each escape sequence and UTF-8 encoding of the sort that might be introduced by the `encodeURI` function is replaced with the UTF-16 encoding of the code point that it represents. Escape sequences that could not have been introduced by `encodeURI` are not replaced.</p>\n        <p>It is the <dfn>%decodeURI%</dfn> intrinsic object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _uriString_ be ? ToString(_encodedURI_).</li><li>Let _preserveEscapeSet_ be *\";/?:@&=+$,#\"*.</li><li>Return ? Decode(_uriString_, _preserveEscapeSet_).</li></ol></emu-alg>\n      ","2.6.2":"<p>This function computes a new version of a URI in which each escape sequence and UTF-8 encoding of the sort that might be introduced by the `encodeURIComponent` function is replaced with the UTF-16 encoding of the code point that it represents.</p>\n        <p>It is the <dfn>%decodeURIComponent%</dfn> intrinsic object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _componentString_ be ? ToString(_encodedURIComponent_).</li><li>Let _preserveEscapeSet_ be the empty String.</li><li>Return ? Decode(_componentString_, _preserveEscapeSet_).</li></ol></emu-alg>\n      ","2.6.3":"<p>This function computes a new version of a UTF-16 encoded (6.1.4) URI in which each instance of certain code points is replaced by one, two, three, or four escape sequences representing the UTF-8 encoding of the code point.</p>\n        <p>It is the <dfn>%encodeURI%</dfn> intrinsic object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _uriString_ be ? ToString(_uri_).</li><li>Let _extraUnescaped_ be *\";/?:@&=+$,#\"*.</li><li>Return ? Encode(_uriString_, _extraUnescaped_).</li></ol></emu-alg>\n      ","2.6.4":"<p>This function computes a new version of a UTF-16 encoded (6.1.4) URI in which each instance of certain code points is replaced by one, two, three, or four escape sequences representing the UTF-8 encoding of the code point.</p>\n        <p>It is the <dfn>%encodeURIComponent%</dfn> intrinsic object.</p>\n        <p>It performs the following steps when called:</p>\n        <emu-alg><ol><li>Let _componentString_ be ? ToString(_uriComponent_).</li><li>Let _extraUnescaped_ be the empty String.</li><li>Return ? Encode(_componentString_, _extraUnescaped_).</li></ol></emu-alg>\n      ","2.6.5":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It performs URI encoding and escaping, interpreting _string_ as a sequence of UTF-16 encoded code points as described in 6.1.4. If a character is identified as unreserved in RFC 2396 or appears in _extraUnescaped_, it is not escaped.</dd>\n        </dl>\n        <emu-alg><ol><li>Let _len_ be the length of _string_.</li><li>Let _result_ be the empty String.</li><li>Let _alwaysUnescaped_ be the string-concatenation of the ASCII word characters and *\"-.!~\\*'()\"*.</li><li>Let _unescapedSet_ be the string-concatenation of _alwaysUnescaped_ and _extraUnescaped_.</li><li>Let _k_ be 0.</li><li>Repeat, while _k_ &lt; _len_,<ol><li>Let _codeUnit_ be the code unit at index _k_ within _string_.</li><li>If _unescapedSet_ contains _codeUnit_, then<ol><li>Set _k_ to _k_ + 1.</li><li>Set _result_ to the string-concatenation of _result_ and _codeUnit_.</li></ol></li><li>Else,<ol><li>Let _cp_ be CodePointAt(_string_, _k_).</li><li>If _cp_.[[IsUnpairedSurrogate]] is *true*, throw a *URIError* exception.</li><li>Set _k_ to _k_ + _cp_.[[CodeUnitCount]].</li><li>Let _octets_ be the List of octets resulting by applying the UTF-8 transformation to _cp_.[[CodePoint]].</li><li>For each element _octet_ of _octets_, do<ol><li>Let _hex_ be the String representation of _octet_, formatted as an uppercase hexadecimal number.</li><li>Set _result_ to the string-concatenation of _result_, *\"%\"*, and StringPad(_hex_, 2, *\"0\"*, ~start~).</li></ol></li></ol></li></ol></li><li>Return _result_.</li></ol></emu-alg>\n        <emu-note>\n          <p>Because percent-encoding is used to represent individual octets, a single code point may be expressed as multiple consecutive escape sequences (one for each of its 8-bit UTF-8 code units).</p>\n        </emu-note>\n      ","2.6.6":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It performs URI unescaping and decoding, preserving any escape sequences that correspond to Basic Latin characters in _preserveEscapeSet_.</dd>\n        </dl>\n        <emu-alg><ol><li>Let _len_ be the length of _string_.</li><li>Let _result_ be the empty String.</li><li>Let _k_ be 0.</li><li>Repeat, while _k_ &lt; _len_,<ol><li>Let _codeUnit_ be the code unit at index _k_ within _string_.</li><li>Let _segment_ be _codeUnit_.</li><li>If _codeUnit_ is the code unit 0x0025 (PERCENT SIGN), then<ol><li>If _k_ + 3 > _len_, throw a *URIError* exception.</li><li>Let _escape_ be the substring of _string_ from _k_ to _k_ + 3.</li><li>Let _firstOctet_ be ParseHexOctet(_string_, _k_ + 1).</li><li>If _firstOctet_ is not an integer, throw a *URIError* exception.</li><li>Set _k_ to _k_ + 2.</li><li>Let _n_ be the number of leading 1 bits in _firstOctet_.</li><li>If _n_ = 0, then<ol><li>Let _asciiChar_ be the code unit whose numeric value is _firstOctet_.</li><li>If _preserveEscapeSet_ contains _asciiChar_, set _segment_ to _escape_; else set _segment_ to _asciiChar_.</li></ol></li><li>Else,<ol><li>If _n_ = 1 or _n_ > 4, throw a *URIError* exception.</li><li>Let _octets_ be « _firstOctet_ ».</li><li>Let _j_ be 1.</li><li>Repeat, while _j_ &lt; _n_,<ol><li>Set _k_ to _k_ + 1.</li><li>If _k_ + 3 > _len_, throw a *URIError* exception.</li><li>If the code unit at index _k_ within _string_ is not the code unit 0x0025 (PERCENT SIGN), throw a *URIError* exception.</li><li>Let _continuationByte_ be ParseHexOctet(_string_, _k_ + 1).</li><li>If _continuationByte_ is not an integer, throw a *URIError* exception.</li><li>Append _continuationByte_ to _octets_.</li><li>Set _k_ to _k_ + 2.</li><li>Set _j_ to _j_ + 1.</li></ol></li><li>Assert: The length of _octets_ is _n_.</li><li>If _octets_ does not contain a valid UTF-8 encoding of a Unicode code point, throw a *URIError* exception.</li><li>Let _codePoint_ be the code point obtained by applying the UTF-8 transformation to _octets_, that is, from a List of octets into a 21-bit value.</li><li>Set _segment_ to UTF16EncodeCodePoint(_codePoint_).</li></ol></li></ol></li><li>Set _result_ to the string-concatenation of _result_ and _segment_.</li><li>Set _k_ to _k_ + 1.</li></ol></li><li>Return _result_.</li></ol></emu-alg>\n        <emu-note>\n          <p>RFC 3629 prohibits the decoding of invalid UTF-8 octet sequences. For example, the invalid sequence 0xC0 0x80 must not decode into the code unit 0x0000. Implementations of the Decode algorithm are required to throw a *URIError* when encountering such invalid sequences.</p>\n        </emu-note>\n      ","2.6.7":"<dl class=\"header\">\n          <dt>description</dt>\n          <dd>It parses a sequence of two hexadecimal characters at the specified _position_ in _string_ into an unsigned 8-bit integer.</dd>\n        </dl>\n        <emu-alg><ol><li>Let _len_ be the length of _string_.</li><li>Assert: _position_ + 2 ≤ _len_.</li><li>Let _hexDigits_ be the substring of _string_ from _position_ to _position_ + 2.</li><li>Let _parseResult_ be ParseText(_hexDigits_, |HexDigits[~Sep]|).</li><li>If _parseResult_ is not a Parse Node, return _parseResult_.</li><li>Let _n_ be the MV of _parseResult_.</li><li>Assert: _n_ is in the inclusive interval from 0 to 255.</li><li>Return _n_.</li></ol></emu-alg>\n      ","3.1":"<p>See 20.5.7.1.</p>\n    ","3.2":"<p>See 23.1.1.</p>\n    ","3.3":"<p>See 25.1.4.</p>\n    ","3.4":"<p>See 21.2.1.</p>\n    ","3.5":"<p>See 23.2.5.</p>\n    ","3.6":"<p>See 23.2.5.</p>\n    ","3.7":"<p>See 20.3.1.</p>\n    ","3.8":"<p>See 25.3.2.</p>\n    ","3.9":"<p>See 21.4.2.</p>\n    ","3.10":"<p>See 20.5.1.</p>\n    ","3.11":"<p>See 20.5.5.1.</p>\n    ","3.12":"<p>See 26.2.1.</p>\n    ","3.13":"<p>See 23.2.5.</p>\n    ","3.14":"<p>See 23.2.5.</p>\n    ","3.15":"<p>See 23.2.5.</p>\n    ","3.16":"<p>See 20.2.1.</p>\n    ","3.17":"<p>See 23.2.5.</p>\n    ","3.18":"<p>See 23.2.5.</p>\n    ","3.19":"<p>See 23.2.5.</p>\n    ","3.20":"<p>See 27.1.3.1.</p>\n    ","3.21":"<p>See 24.1.1.</p>\n    ","3.22":"<p>See 21.1.1.</p>\n    ","3.23":"<p>See 20.1.1.</p>\n    ","3.24":"<p>See 27.2.3.</p>\n    ","3.25":"<p>See 28.2.1.</p>\n    ","3.26":"<p>See 20.5.5.2.</p>\n    ","3.27":"<p>See 20.5.5.3.</p>\n    ","3.28":"<p>See 22.2.4.</p>\n    ","3.29":"<p>See 24.2.2.</p>\n    ","3.30":"<p>See 25.2.3.</p>\n    ","3.31":"<p>See 22.1.1.</p>\n    ","3.32":"<p>See 20.4.1.</p>\n    ","3.33":"<p>See 20.5.5.4.</p>\n    ","3.34":"<p>See 20.5.5.5.</p>\n    ","3.35":"<p>See 23.2.5.</p>\n    ","3.36":"<p>See 23.2.5.</p>\n    ","3.37":"<p>See 23.2.5.</p>\n    ","3.38":"<p>See 23.2.5.</p>\n    ","3.39":"<p>See 20.5.5.6.</p>\n    ","3.40":"<p>See 24.3.1.</p>\n    ","3.41":"<p>See 26.1.1.</p>\n    ","3.42":"<p>See 24.4.</p>\n    ","4.1":"<p>See 25.4.</p>\n    ","4.2":"<p>See 25.5.</p>\n    ","4.3":"<p>See 21.3.</p>\n    ","4.4":"<p>See 28.1.</p>\n    "};
export function Sec({ id }) {
  const html = sections[id] ?? '';
  return <div className="ecma-spec" dangerouslySetInnerHTML={{ __html: html }} />;
}
