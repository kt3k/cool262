import { Sec } from '../lib/spec/abstract-operations'

# <span id="sec-abstract-operations" /> 7 Abstract Operations

<Sec id="" />

## <span id="sec-type-conversion" /> 7.1 Type Conversion

<Sec id="1" />

### <span id="sec-toprimitive" /> 7.1.1 ToPrimitive ( <var>input</var>: an ECMAScript language value, optional <var>preferredType</var>: <emu-const>string</emu-const> or <emu-const>number</emu-const>, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="1.1" />

#### <span id="sec-ordinarytoprimitive" /> 7.1.1.1 OrdinaryToPrimitive ( <var>obj</var>: an Object, <var>hint</var>: <emu-const>string</emu-const> or <emu-const>number</emu-const>, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="1.1.1" />

### <span id="sec-toboolean" /> 7.1.2 ToBoolean ( <var>argument</var>: an ECMAScript language value, ): a Boolean

<Sec id="1.2" />

### <span id="sec-tonumeric" /> 7.1.3 ToNumeric ( <var>value</var>: an ECMAScript language value, ): either a normal completion containing either a Number or a BigInt, or a throw completion

<Sec id="1.3" />

### <span id="sec-tonumber" /> 7.1.4 ToNumber ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing a Number or a throw completion

<Sec id="1.4" />

#### <span id="sec-tonumber-applied-to-the-string-type" /> 7.1.4.1 ToNumber Applied to the String Type

<Sec id="1.4.1" />

##### <span id="sec-stringtonumber" /> 7.1.4.1.1 StringToNumber ( <var>str</var>: a String, ): a Number

<Sec id="1.4.1.1" />

##### <span id="sec-runtime-semantics-stringnumericvalue" /> 7.1.4.1.2 Runtime Semantics: StringNumericValue ( ): a Number

<Sec id="1.4.1.2" />

##### <span id="sec-roundmvresult" /> 7.1.4.1.3 RoundMVResult ( <var>n</var>: a mathematical value, ): a Number

<Sec id="1.4.1.3" />

### <span id="sec-tointegerorinfinity" /> 7.1.5 ToIntegerOrInfinity ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing either an integer, +&infin;, or -&infin;, or a throw completion

<Sec id="1.5" />

### <span id="sec-tofixedsizeinteger" /> 7.1.6 ToFixedSizeInteger ( <var>int</var>: an integer, +∞, or -∞, <var>signed</var>: <emu-const>unsigned</emu-const> or <emu-const>signed</emu-const>, <var>bitWidth</var>: a positive integer, ): an integer

<Sec id="1.6" />

### <span id="sec-toint32" /> 7.1.7 ToInt32 ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.7" />

### <span id="sec-touint32" /> 7.1.8 ToUint32 ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.8" />

### <span id="sec-toint16" /> 7.1.9 ToInt16 ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.9" />

### <span id="sec-touint16" /> 7.1.10 ToUint16 ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.10" />

### <span id="sec-toint8" /> 7.1.11 ToInt8 ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.11" />

### <span id="sec-touint8" /> 7.1.12 ToUint8 ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.12" />

### <span id="sec-touint8clamp" /> 7.1.13 ToUint8Clamp ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.13" />

### <span id="sec-tobigint" /> 7.1.14 ToBigInt ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing a BigInt or a throw completion

<Sec id="1.14" />

### <span id="sec-stringtobigint" /> 7.1.15 StringToBigInt ( <var>str</var>: a String, ): a BigInt or <b>undefined</b>

<Sec id="1.15" />

#### <span id="sec-stringintegerliteral-grammar" /> 7.1.15.1 StringIntegerLiteral Grammar

<Sec id="1.15.1" />

#### <span id="sec-runtime-semantics-mv-for-stringintegerliteral" /> 7.1.15.2 Runtime Semantics: MV

<Sec id="1.15.2" />

### <span id="sec-tobigint64" /> 7.1.16 ToBigInt64 ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing a BigInt or a throw completion

<Sec id="1.16" />

### <span id="sec-tobiguint64" /> 7.1.17 ToBigUint64 ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing a BigInt or a throw completion

<Sec id="1.17" />

### <span id="sec-tostring" /> 7.1.18 ToString ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing a String or a throw completion

<Sec id="1.18" />

### <span id="sec-toobject" /> 7.1.19 ToObject ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing an Object or a throw completion

<Sec id="1.19" />

### <span id="sec-topropertykey" /> 7.1.20 ToPropertyKey ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing a property key or a throw completion

<Sec id="1.20" />

### <span id="sec-tolength" /> 7.1.21 ToLength ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing a non-negative integral Number or a throw completion

<Sec id="1.21" />

### <span id="sec-canonicalnumericindexstring" /> 7.1.22 CanonicalNumericIndexString ( <var>argument</var>: a String, ): a Number or <b>undefined</b>

<Sec id="1.22" />

### <span id="sec-toindex" /> 7.1.23 ToIndex ( <var>value</var>: an ECMAScript language value, ): either a normal completion containing a non-negative integer or a throw completion

<Sec id="1.23" />

## <span id="sec-testing-and-comparison-operations" /> 7.2 Testing and Comparison Operations

### <span id="sec-requireobjectcoercible" /> 7.2.1 RequireObjectCoercible ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion

<Sec id="2.1" />

### <span id="sec-isarray" /> 7.2.2 IsArray ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing a Boolean or a throw completion

<Sec id="2.2" />

### <span id="sec-iscallable" /> 7.2.3 IsCallable ( <var>argument</var>: an ECMAScript language value, ): a Boolean

<Sec id="2.3" />

### <span id="sec-isconstructor" /> 7.2.4 IsConstructor ( <var>argument</var>: an ECMAScript language value, ): a Boolean

<Sec id="2.4" />

### <span id="sec-isextensible-o" /> 7.2.5 IsExtensible ( <var>obj</var>: an Object, ): either a normal completion containing a Boolean or a throw completion

<Sec id="2.5" />

### <span id="sec-isregexp" /> 7.2.6 IsRegExp ( <var>argument</var>: an ECMAScript language value, ): either a normal completion containing a Boolean or a throw completion

<Sec id="2.6" />

### <span id="sec-isstringwellformedunicode" /> 7.2.7 Static Semantics: IsStringWellFormedUnicode ( <var>string</var>: a String, ): a Boolean

<Sec id="2.7" />

### <span id="sec-sametype" /> 7.2.8 SameType ( <var>x</var>: an ECMAScript language value, <var>y</var>: an ECMAScript language value, ): a Boolean

<Sec id="2.8" />

### <span id="sec-samevalue" /> 7.2.9 SameValue ( <var>x</var>: an ECMAScript language value, <var>y</var>: an ECMAScript language value, ): a Boolean

<Sec id="2.9" />

### <span id="sec-samevaluezero" /> 7.2.10 SameValueZero ( <var>x</var>: an ECMAScript language value, <var>y</var>: an ECMAScript language value, ): a Boolean

<Sec id="2.10" />

### <span id="sec-samevaluenonnumber" /> 7.2.11 SameValueNonNumber ( <var>x</var>: an ECMAScript language value, but not a Number, <var>y</var>: an ECMAScript language value, but not a Number, ): a Boolean

<Sec id="2.11" />

### <span id="sec-islessthan" /> 7.2.12 IsLessThan ( <var>x</var>: an ECMAScript language value, <var>y</var>: an ECMAScript language value, <var>leftFirst</var>: a Boolean, ): either a normal completion containing either a Boolean or <b>undefined</b>, or a throw completion

<Sec id="2.12" />

### <span id="sec-islooselyequal" /> 7.2.13 IsLooselyEqual ( <var>x</var>: an ECMAScript language value, <var>y</var>: an ECMAScript language value, ): either a normal completion containing a Boolean or a throw completion

<Sec id="2.13" />

### <span id="sec-isstrictlyequal" /> 7.2.14 IsStrictlyEqual ( <var>x</var>: an ECMAScript language value, <var>y</var>: an ECMAScript language value, ): a Boolean

<Sec id="2.14" />

## <span id="sec-operations-on-objects" /> 7.3 Operations on Objects

### <span id="sec-makebasicobject" /> 7.3.1 MakeBasicObject ( <var>internalSlotsList</var>: a List of internal slot names, ): an Object

<Sec id="3.1" />

### <span id="sec-get-o-p" /> 7.3.2 Get ( <var>obj</var>: an Object, <var>propertyKey</var>: a property key, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="3.2" />

### <span id="sec-getv" /> 7.3.3 GetV ( <var>value</var>: an ECMAScript language value, <var>propertyKey</var>: a property key, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="3.3" />

### <span id="sec-set-o-p-v-throw" /> 7.3.4 Set ( <var>obj</var>: an Object, <var>propertyKey</var>: a property key, <var>value</var>: an ECMAScript language value, <var>throw</var>: a Boolean, ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion

<Sec id="3.4" />

### <span id="sec-createdataproperty" /> 7.3.5 CreateDataProperty ( <var>obj</var>: an Object, <var>propertyKey</var>: a property key, <var>value</var>: an ECMAScript language value, ): either a normal completion containing a Boolean or a throw completion

<Sec id="3.5" />

### <span id="sec-createdatapropertyorthrow" /> 7.3.6 CreateDataPropertyOrThrow ( <var>obj</var>: an Object, <var>propertyKey</var>: a property key, <var>value</var>: an ECMAScript language value, ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion

<Sec id="3.6" />

### <span id="sec-createnonenumerabledatapropertyorthrow" /> 7.3.7 CreateNonEnumerableDataPropertyOrThrow ( <var>obj</var>: an Object, <var>propertyKey</var>: a property key, <var>value</var>: an ECMAScript language value, ): <emu-const>unused</emu-const>

<Sec id="3.7" />

### <span id="sec-definepropertyorthrow" /> 7.3.8 DefinePropertyOrThrow ( <var>obj</var>: an Object, <var>propertyKey</var>: a property key, <var>desc</var>: a Property Descriptor, ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion

<Sec id="3.8" />

### <span id="sec-deletepropertyorthrow" /> 7.3.9 DeletePropertyOrThrow ( <var>obj</var>: an Object, <var>propertyKey</var>: a property key, ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion

<Sec id="3.9" />

### <span id="sec-getmethod" /> 7.3.10 GetMethod ( <var>value</var>: an ECMAScript language value, <var>propertyKey</var>: a property key, ): either a normal completion containing either a function object or <b>undefined</b>, or a throw completion

<Sec id="3.10" />

### <span id="sec-hasproperty" /> 7.3.11 HasProperty ( <var>obj</var>: an Object, <var>propertyKey</var>: a property key, ): either a normal completion containing a Boolean or a throw completion

<Sec id="3.11" />

### <span id="sec-hasownproperty" /> 7.3.12 HasOwnProperty ( <var>obj</var>: an Object, <var>propertyKey</var>: a property key, ): either a normal completion containing a Boolean or a throw completion

<Sec id="3.12" />

### <span id="sec-call" /> 7.3.13 Call ( <var>func</var>: an ECMAScript language value, <var>thisValue</var>: an ECMAScript language value, optional <var>argumentsList</var>: a List of ECMAScript language values, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="3.13" />

### <span id="sec-construct" /> 7.3.14 Construct ( <var>constructor</var>: a constructor, optional <var>argumentsList</var>: a List of ECMAScript language values, optional <var>newTarget</var>: a constructor, ): either a normal completion containing an Object or a throw completion

<Sec id="3.14" />

### <span id="sec-setintegritylevel" /> 7.3.15 SetIntegrityLevel ( <var>obj</var>: an Object, <var>level</var>: <emu-const>sealed</emu-const> or <emu-const>frozen</emu-const>, ): either a normal completion containing a Boolean or a throw completion

<Sec id="3.15" />

### <span id="sec-testintegritylevel" /> 7.3.16 TestIntegrityLevel ( <var>obj</var>: an Object, <var>level</var>: <emu-const>sealed</emu-const> or <emu-const>frozen</emu-const>, ): either a normal completion containing a Boolean or a throw completion

<Sec id="3.16" />

### <span id="sec-createarrayfromlist" /> 7.3.17 CreateArrayFromList ( <var>elements</var>: a List of ECMAScript language values, ): an Array

<Sec id="3.17" />

### <span id="sec-lengthofarraylike" /> 7.3.18 LengthOfArrayLike ( <var>obj</var>: an Object, ): either a normal completion containing a non-negative integer or a throw completion

<Sec id="3.18" />

### <span id="sec-createlistfromarraylike" /> 7.3.19 CreateListFromArrayLike ( <var>obj</var>: an ECMAScript language value, optional <var>validElementTypes</var>: <emu-const>all</emu-const> or <emu-const>property-key</emu-const>, ): either a normal completion containing a List of ECMAScript language values or a throw completion

<Sec id="3.19" />

### <span id="sec-invoke" /> 7.3.20 Invoke ( <var>value</var>: an ECMAScript language value, <var>propertyKey</var>: a property key, optional <var>argumentsList</var>: a List of ECMAScript language values, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="3.20" />

### <span id="sec-ordinaryhasinstance" /> 7.3.21 OrdinaryHasInstance ( <var>constructor</var>: an ECMAScript language value, <var>instance</var>: an ECMAScript language value, ): either a normal completion containing a Boolean or a throw completion

<Sec id="3.21" />

### <span id="sec-speciesconstructor" /> 7.3.22 SpeciesConstructor ( <var>obj</var>: an Object, <var>defaultConstructor</var>: a constructor, ): either a normal completion containing a constructor or a throw completion

<Sec id="3.22" />

### <span id="sec-enumerableownproperties" /> 7.3.23 EnumerableOwnProperties ( <var>obj</var>: an Object, <var>kind</var>: <emu-const>key</emu-const>, <emu-const>value</emu-const>, or <emu-const>key+value</emu-const>, ): either a normal completion containing a List of ECMAScript language values or a throw completion

<Sec id="3.23" />

### <span id="sec-getfunctionrealm" /> 7.3.24 GetFunctionRealm ( <var>func</var>: a function object, ): either a normal completion containing a Realm Record or a throw completion

<Sec id="3.24" />

### <span id="sec-copydataproperties" /> 7.3.25 CopyDataProperties ( <var>target</var>: an Object, <var>source</var>: an ECMAScript language value, <var>excludedItems</var>: a List of property keys, ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion

<Sec id="3.25" />

### <span id="sec-privateelementfind" /> 7.3.26 PrivateElementFind ( <var>obj</var>: an Object, <var>privateName</var>: a Private Name, ): a PrivateElement or <emu-const>empty</emu-const>

<Sec id="3.26" />

### <span id="sec-privatefieldadd" /> 7.3.27 PrivateFieldAdd ( <var>obj</var>: an Object, <var>privateName</var>: a Private Name, <var>value</var>: an ECMAScript language value, ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion

<Sec id="3.27" />

### <span id="sec-privatemethodoraccessoradd" /> 7.3.28 PrivateMethodOrAccessorAdd ( <var>obj</var>: an Object, <var>method</var>: a PrivateElement, ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion

<Sec id="3.28" />

### <span id="sec-hostensurecanaddprivateelement" /> 7.3.29 HostEnsureCanAddPrivateElement ( <var>obj</var>: an Object, ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion

<Sec id="3.29" />

### <span id="sec-privateget" /> 7.3.30 PrivateGet ( <var>obj</var>: an Object, <var>privateName</var>: a Private Name, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="3.30" />

### <span id="sec-privateset" /> 7.3.31 PrivateSet ( <var>obj</var>: an Object, <var>privateName</var>: a Private Name, <var>value</var>: an ECMAScript language value, ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion

<Sec id="3.31" />

### <span id="sec-definefield" /> 7.3.32 DefineField ( <var>receiver</var>: an Object, <var>fieldRecord</var>: a ClassFieldDefinition Record, ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion

<Sec id="3.32" />

### <span id="sec-initializeinstanceelements" /> 7.3.33 InitializeInstanceElements ( <var>obj</var>: an Object, <var>constructor</var>: an ECMAScript function object or a built-in function object, ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion

<Sec id="3.33" />

### <span id="sec-add-value-to-keyed-group" /> 7.3.34 AddValueToKeyedGroup ( <var>groups</var>: a List of Records with fields [[Key]] (an ECMAScript language value) and [[Elements]] (a List of ECMAScript language values), <var>key</var>: an ECMAScript language value, <var>value</var>: an ECMAScript language value, ): <emu-const>unused</emu-const>

<Sec id="3.34" />

### <span id="sec-groupby" /> 7.3.35 GroupBy ( <var>items</var>: an ECMAScript language value, <var>callback</var>: an ECMAScript language value, <var>keyCoercion</var>: <emu-const>property</emu-const> or <emu-const>collection</emu-const>, ): either a normal completion containing a List of Records with fields [[Key]] (an ECMAScript language value) and [[Elements]] (a List of ECMAScript language values), or a throw completion

<Sec id="3.35" />

### <span id="sec-getoptionsobject" /> 7.3.36 GetOptionsObject ( <var>options</var>: an ECMAScript language value, ): either a normal completion containing an Object or a throw completion

<Sec id="3.36" />

### <span id="sec-SetterThatIgnoresPrototypeProperties" /> 7.3.37 SetterThatIgnoresPrototypeProperties ( <var>thisValue</var>: an ECMAScript language value, <var>home</var>: an Object, <var>propertyKey</var>: a property key, <var>value</var>: an ECMAScript language value, ): either a normal completion containing <emu-const>unused</emu-const> or a throw completion

<Sec id="3.37" />

## <span id="sec-operations-on-iterator-objects" /> 7.4 Operations on Iterator Objects

<Sec id="4" />

### <span id="sec-iterator-records" /> 7.4.1 Iterator Records

<Sec id="4.1" />

### <span id="sec-getiteratordirect" /> 7.4.2 GetIteratorDirect ( <var>obj</var>: an Object, ): either a normal completion containing an Iterator Record or a throw completion

<Sec id="4.2" />

### <span id="sec-getiteratorfrommethod" /> 7.4.3 GetIteratorFromMethod ( <var>obj</var>: an ECMAScript language value, <var>method</var>: a function object, ): either a normal completion containing an Iterator Record or a throw completion

<Sec id="4.3" />

### <span id="sec-getiterator" /> 7.4.4 GetIterator ( <var>obj</var>: an ECMAScript language value, <var>kind</var>: <emu-const>sync</emu-const> or <emu-const>async</emu-const>, ): either a normal completion containing an Iterator Record or a throw completion

<Sec id="4.4" />

### <span id="sec-getiteratorflattenable" /> 7.4.5 GetIteratorFlattenable ( <var>obj</var>: an ECMAScript language value, <var>primitiveHandling</var>: <emu-const>iterate-string-primitives</emu-const> or <emu-const>reject-primitives</emu-const>, ): either a normal completion containing an Iterator Record or a throw completion

<Sec id="4.5" />

### <span id="sec-iteratornext" /> 7.4.6 IteratorNext ( <var>iteratorRecord</var>: an Iterator Record, optional <var>value</var>: an ECMAScript language value, ): either a normal completion containing an Object or a throw completion

<Sec id="4.6" />

### <span id="sec-iteratorcomplete" /> 7.4.7 IteratorComplete ( <var>iteratorResult</var>: an Object, ): either a normal completion containing a Boolean or a throw completion

<Sec id="4.7" />

### <span id="sec-iteratorvalue" /> 7.4.8 IteratorValue ( <var>iteratorResult</var>: an Object, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="4.8" />

### <span id="sec-iteratorstep" /> 7.4.9 IteratorStep ( <var>iteratorRecord</var>: an Iterator Record, ): either a normal completion containing either an Object or <emu-const>done</emu-const>, or a throw completion

<Sec id="4.9" />

### <span id="sec-iteratorstepvalue" /> 7.4.10 IteratorStepValue ( <var>iteratorRecord</var>: an Iterator Record, ): either a normal completion containing either an ECMAScript language value or <emu-const>done</emu-const>, or a throw completion

<Sec id="4.10" />

### <span id="sec-iteratorclose" /> 7.4.11 IteratorClose ( <var>iteratorRecord</var>: an Iterator Record, <var>completion</var>: a Completion Record, ): a Completion Record

<Sec id="4.11" />

### <span id="sec-iteratorcloseall" /> 7.4.12 IteratorCloseAll ( <var>iters</var>: a List of Iterator Records, <var>completion</var>: a Completion Record, ): a Completion Record

<Sec id="4.12" />

### <span id="sec-ifabruptcloseiterator" /> 7.4.13 IfAbruptCloseIterator ( <var>value</var>, <var>iteratorRecord</var> )

<Sec id="4.13" />

### <span id="sec-asynciteratorclose" /> 7.4.14 AsyncIteratorClose ( <var>iteratorRecord</var>: an Iterator Record, <var>completion</var>: a Completion Record, ): a Completion Record

<Sec id="4.14" />

### <span id="sec-ifabruptcloseasynciterator" /> 7.4.15 IfAbruptCloseAsyncIterator ( <var>value</var>, <var>iteratorRecord</var> )

<Sec id="4.15" />

### <span id="sec-createiterresultobject" /> 7.4.16 CreateIteratorResultObject ( <var>value</var>: an ECMAScript language value, <var>done</var>: a Boolean, ): an Object that conforms to the IteratorResult interface

<Sec id="4.16" />

### <span id="sec-createlistiteratorRecord" /> 7.4.17 CreateListIteratorRecord ( <var>list</var>: a List of ECMAScript language values, ): an Iterator Record

<Sec id="4.17" />

### <span id="sec-iteratortolist" /> 7.4.18 IteratorToList ( <var>iteratorRecord</var>: an Iterator Record, ): either a normal completion containing a List of ECMAScript language values or a throw completion

<Sec id="4.18" />
