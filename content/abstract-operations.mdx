import { Sec } from '../lib/spec/abstract-operations'

# 7 Abstract Operations

<Sec id="" />

## 7.1 Type Conversion

<Sec id="1" />

### 7.1.1 ToPrimitive ( _input_: an ECMAScript language value, optional _preferredType_: ~string~ or ~number~, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="1.1" />

#### 7.1.1.1 OrdinaryToPrimitive ( _obj_: an Object, _hint_: ~string~ or ~number~, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="1.1.1" />

### 7.1.2 ToBoolean ( _argument_: an ECMAScript language value, ): a Boolean

<Sec id="1.2" />

### 7.1.3 ToNumeric ( _value_: an ECMAScript language value, ): either a normal completion containing either a Number or a BigInt, or a throw completion

<Sec id="1.3" />

### 7.1.4 ToNumber ( _argument_: an ECMAScript language value, ): either a normal completion containing a Number or a throw completion

<Sec id="1.4" />

#### 7.1.4.1 ToNumber Applied to the String Type

<Sec id="1.4.1" />

##### 7.1.4.1.1 StringToNumber ( _str_: a String, ): a Number

<Sec id="1.4.1.1" />

##### 7.1.4.1.2 Runtime Semantics: StringNumericValue ( ): a Number

<Sec id="1.4.1.2" />

##### 7.1.4.1.3 RoundMVResult ( _n_: a mathematical value, ): a Number

<Sec id="1.4.1.3" />

### 7.1.5 ToIntegerOrInfinity ( _argument_: an ECMAScript language value, ): either a normal completion containing either an integer, +&infin;, or -&infin;, or a throw completion

<Sec id="1.5" />

### 7.1.6 ToFixedSizeInteger ( _int_: an integer, +∞, or -∞, _signed_: ~unsigned~ or ~signed~, _bitWidth_: a positive integer, ): an integer

<Sec id="1.6" />

### 7.1.7 ToInt32 ( _argument_: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.7" />

### 7.1.8 ToUint32 ( _argument_: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.8" />

### 7.1.9 ToInt16 ( _argument_: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.9" />

### 7.1.10 ToUint16 ( _argument_: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.10" />

### 7.1.11 ToInt8 ( _argument_: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.11" />

### 7.1.12 ToUint8 ( _argument_: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.12" />

### 7.1.13 ToUint8Clamp ( _argument_: an ECMAScript language value, ): either a normal completion containing an integral Number or a throw completion

<Sec id="1.13" />

### 7.1.14 ToBigInt ( _argument_: an ECMAScript language value, ): either a normal completion containing a BigInt or a throw completion

<Sec id="1.14" />

### 7.1.15 StringToBigInt ( _str_: a String, ): a BigInt or *undefined*

<Sec id="1.15" />

#### 7.1.15.1 StringIntegerLiteral Grammar

<Sec id="1.15.1" />

#### 7.1.15.2 Runtime Semantics: MV

<Sec id="1.15.2" />

### 7.1.16 ToBigInt64 ( _argument_: an ECMAScript language value, ): either a normal completion containing a BigInt or a throw completion

<Sec id="1.16" />

### 7.1.17 ToBigUint64 ( _argument_: an ECMAScript language value, ): either a normal completion containing a BigInt or a throw completion

<Sec id="1.17" />

### 7.1.18 ToString ( _argument_: an ECMAScript language value, ): either a normal completion containing a String or a throw completion

<Sec id="1.18" />

### 7.1.19 ToObject ( _argument_: an ECMAScript language value, ): either a normal completion containing an Object or a throw completion

<Sec id="1.19" />

### 7.1.20 ToPropertyKey ( _argument_: an ECMAScript language value, ): either a normal completion containing a property key or a throw completion

<Sec id="1.20" />

### 7.1.21 ToLength ( _argument_: an ECMAScript language value, ): either a normal completion containing a non-negative integral Number or a throw completion

<Sec id="1.21" />

### 7.1.22 CanonicalNumericIndexString ( _argument_: a String, ): a Number or *undefined*

<Sec id="1.22" />

### 7.1.23 ToIndex ( _value_: an ECMAScript language value, ): either a normal completion containing a non-negative integer or a throw completion

<Sec id="1.23" />

## 7.2 Testing and Comparison Operations

### 7.2.1 RequireObjectCoercible ( _argument_: an ECMAScript language value, ): either a normal completion containing ~unused~ or a throw completion

<Sec id="2.1" />

### 7.2.2 IsArray ( _argument_: an ECMAScript language value, ): either a normal completion containing a Boolean or a throw completion

<Sec id="2.2" />

### 7.2.3 IsCallable ( _argument_: an ECMAScript language value, ): a Boolean

<Sec id="2.3" />

### 7.2.4 IsConstructor ( _argument_: an ECMAScript language value, ): a Boolean

<Sec id="2.4" />

### 7.2.5 IsExtensible ( _obj_: an Object, ): either a normal completion containing a Boolean or a throw completion

<Sec id="2.5" />

### 7.2.6 IsRegExp ( _argument_: an ECMAScript language value, ): either a normal completion containing a Boolean or a throw completion

<Sec id="2.6" />

### 7.2.7 Static Semantics: IsStringWellFormedUnicode ( _string_: a String, ): a Boolean

<Sec id="2.7" />

### 7.2.8 SameType ( _x_: an ECMAScript language value, _y_: an ECMAScript language value, ): a Boolean

<Sec id="2.8" />

### 7.2.9 SameValue ( _x_: an ECMAScript language value, _y_: an ECMAScript language value, ): a Boolean

<Sec id="2.9" />

### 7.2.10 SameValueZero ( _x_: an ECMAScript language value, _y_: an ECMAScript language value, ): a Boolean

<Sec id="2.10" />

### 7.2.11 SameValueNonNumber ( _x_: an ECMAScript language value, but not a Number, _y_: an ECMAScript language value, but not a Number, ): a Boolean

<Sec id="2.11" />

### 7.2.12 IsLessThan ( _x_: an ECMAScript language value, _y_: an ECMAScript language value, _leftFirst_: a Boolean, ): either a normal completion containing either a Boolean or *undefined*, or a throw completion

<Sec id="2.12" />

### 7.2.13 IsLooselyEqual ( _x_: an ECMAScript language value, _y_: an ECMAScript language value, ): either a normal completion containing a Boolean or a throw completion

<Sec id="2.13" />

### 7.2.14 IsStrictlyEqual ( _x_: an ECMAScript language value, _y_: an ECMAScript language value, ): a Boolean

<Sec id="2.14" />

## 7.3 Operations on Objects

### 7.3.1 MakeBasicObject ( _internalSlotsList_: a List of internal slot names, ): an Object

<Sec id="3.1" />

### 7.3.2 Get ( _obj_: an Object, _propertyKey_: a property key, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="3.2" />

### 7.3.3 GetV ( _value_: an ECMAScript language value, _propertyKey_: a property key, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="3.3" />

### 7.3.4 Set ( _obj_: an Object, _propertyKey_: a property key, _value_: an ECMAScript language value, _throw_: a Boolean, ): either a normal completion containing ~unused~ or a throw completion

<Sec id="3.4" />

### 7.3.5 CreateDataProperty ( _obj_: an Object, _propertyKey_: a property key, _value_: an ECMAScript language value, ): either a normal completion containing a Boolean or a throw completion

<Sec id="3.5" />

### 7.3.6 CreateDataPropertyOrThrow ( _obj_: an Object, _propertyKey_: a property key, _value_: an ECMAScript language value, ): either a normal completion containing ~unused~ or a throw completion

<Sec id="3.6" />

### 7.3.7 CreateNonEnumerableDataPropertyOrThrow ( _obj_: an Object, _propertyKey_: a property key, _value_: an ECMAScript language value, ): ~unused~

<Sec id="3.7" />

### 7.3.8 DefinePropertyOrThrow ( _obj_: an Object, _propertyKey_: a property key, _desc_: a Property Descriptor, ): either a normal completion containing ~unused~ or a throw completion

<Sec id="3.8" />

### 7.3.9 DeletePropertyOrThrow ( _obj_: an Object, _propertyKey_: a property key, ): either a normal completion containing ~unused~ or a throw completion

<Sec id="3.9" />

### 7.3.10 GetMethod ( _value_: an ECMAScript language value, _propertyKey_: a property key, ): either a normal completion containing either a function object or *undefined*, or a throw completion

<Sec id="3.10" />

### 7.3.11 HasProperty ( _obj_: an Object, _propertyKey_: a property key, ): either a normal completion containing a Boolean or a throw completion

<Sec id="3.11" />

### 7.3.12 HasOwnProperty ( _obj_: an Object, _propertyKey_: a property key, ): either a normal completion containing a Boolean or a throw completion

<Sec id="3.12" />

### 7.3.13 Call ( _func_: an ECMAScript language value, _thisValue_: an ECMAScript language value, optional _argumentsList_: a List of ECMAScript language values, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="3.13" />

### 7.3.14 Construct ( _constructor_: a constructor, optional _argumentsList_: a List of ECMAScript language values, optional _newTarget_: a constructor, ): either a normal completion containing an Object or a throw completion

<Sec id="3.14" />

### 7.3.15 SetIntegrityLevel ( _obj_: an Object, _level_: ~sealed~ or ~frozen~, ): either a normal completion containing a Boolean or a throw completion

<Sec id="3.15" />

### 7.3.16 TestIntegrityLevel ( _obj_: an Object, _level_: ~sealed~ or ~frozen~, ): either a normal completion containing a Boolean or a throw completion

<Sec id="3.16" />

### 7.3.17 CreateArrayFromList ( _elements_: a List of ECMAScript language values, ): an Array

<Sec id="3.17" />

### 7.3.18 LengthOfArrayLike ( _obj_: an Object, ): either a normal completion containing a non-negative integer or a throw completion

<Sec id="3.18" />

### 7.3.19 CreateListFromArrayLike ( _obj_: an ECMAScript language value, optional _validElementTypes_: ~all~ or ~property-key~, ): either a normal completion containing a List of ECMAScript language values or a throw completion

<Sec id="3.19" />

### 7.3.20 Invoke ( _value_: an ECMAScript language value, _propertyKey_: a property key, optional _argumentsList_: a List of ECMAScript language values, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="3.20" />

### 7.3.21 OrdinaryHasInstance ( _constructor_: an ECMAScript language value, _instance_: an ECMAScript language value, ): either a normal completion containing a Boolean or a throw completion

<Sec id="3.21" />

### 7.3.22 SpeciesConstructor ( _obj_: an Object, _defaultConstructor_: a constructor, ): either a normal completion containing a constructor or a throw completion

<Sec id="3.22" />

### 7.3.23 EnumerableOwnProperties ( _obj_: an Object, _kind_: ~key~, ~value~, or ~key+value~, ): either a normal completion containing a List of ECMAScript language values or a throw completion

<Sec id="3.23" />

### 7.3.24 GetFunctionRealm ( _func_: a function object, ): either a normal completion containing a Realm Record or a throw completion

<Sec id="3.24" />

### 7.3.25 CopyDataProperties ( _target_: an Object, _source_: an ECMAScript language value, _excludedItems_: a List of property keys, ): either a normal completion containing ~unused~ or a throw completion

<Sec id="3.25" />

### 7.3.26 PrivateElementFind ( _obj_: an Object, _privateName_: a Private Name, ): a PrivateElement or ~empty~

<Sec id="3.26" />

### 7.3.27 PrivateFieldAdd ( _obj_: an Object, _privateName_: a Private Name, _value_: an ECMAScript language value, ): either a normal completion containing ~unused~ or a throw completion

<Sec id="3.27" />

### 7.3.28 PrivateMethodOrAccessorAdd ( _obj_: an Object, _method_: a PrivateElement, ): either a normal completion containing ~unused~ or a throw completion

<Sec id="3.28" />

### 7.3.29 HostEnsureCanAddPrivateElement ( _obj_: an Object, ): either a normal completion containing ~unused~ or a throw completion

<Sec id="3.29" />

### 7.3.30 PrivateGet ( _obj_: an Object, _privateName_: a Private Name, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="3.30" />

### 7.3.31 PrivateSet ( _obj_: an Object, _privateName_: a Private Name, _value_: an ECMAScript language value, ): either a normal completion containing ~unused~ or a throw completion

<Sec id="3.31" />

### 7.3.32 DefineField ( _receiver_: an Object, _fieldRecord_: a ClassFieldDefinition Record, ): either a normal completion containing ~unused~ or a throw completion

<Sec id="3.32" />

### 7.3.33 InitializeInstanceElements ( _obj_: an Object, _constructor_: an ECMAScript function object or a built-in function object, ): either a normal completion containing ~unused~ or a throw completion

<Sec id="3.33" />

### 7.3.34 AddValueToKeyedGroup ( _groups_: a List of Records with fields [[Key]] (an ECMAScript language value) and [[Elements]] (a List of ECMAScript language values), _key_: an ECMAScript language value, _value_: an ECMAScript language value, ): ~unused~

<Sec id="3.34" />

### 7.3.35 GroupBy ( _items_: an ECMAScript language value, _callback_: an ECMAScript language value, _keyCoercion_: ~property~ or ~collection~, ): either a normal completion containing a List of Records with fields [[Key]] (an ECMAScript language value) and [[Elements]] (a List of ECMAScript language values), or a throw completion

<Sec id="3.35" />

### 7.3.36 GetOptionsObject ( _options_: an ECMAScript language value, ): either a normal completion containing an Object or a throw completion

<Sec id="3.36" />

### 7.3.37 SetterThatIgnoresPrototypeProperties ( _thisValue_: an ECMAScript language value, _home_: an Object, _propertyKey_: a property key, _value_: an ECMAScript language value, ): either a normal completion containing ~unused~ or a throw completion

<Sec id="3.37" />

## 7.4 Operations on Iterator Objects

<Sec id="4" />

### 7.4.1 Iterator Records

<Sec id="4.1" />

### 7.4.2 GetIteratorDirect ( _obj_: an Object, ): either a normal completion containing an Iterator Record or a throw completion

<Sec id="4.2" />

### 7.4.3 GetIteratorFromMethod ( _obj_: an ECMAScript language value, _method_: a function object, ): either a normal completion containing an Iterator Record or a throw completion

<Sec id="4.3" />

### 7.4.4 GetIterator ( _obj_: an ECMAScript language value, _kind_: ~sync~ or ~async~, ): either a normal completion containing an Iterator Record or a throw completion

<Sec id="4.4" />

### 7.4.5 GetIteratorFlattenable ( _obj_: an ECMAScript language value, _primitiveHandling_: ~iterate-string-primitives~ or ~reject-primitives~, ): either a normal completion containing an Iterator Record or a throw completion

<Sec id="4.5" />

### 7.4.6 IteratorNext ( _iteratorRecord_: an Iterator Record, optional _value_: an ECMAScript language value, ): either a normal completion containing an Object or a throw completion

<Sec id="4.6" />

### 7.4.7 IteratorComplete ( _iteratorResult_: an Object, ): either a normal completion containing a Boolean or a throw completion

<Sec id="4.7" />

### 7.4.8 IteratorValue ( _iteratorResult_: an Object, ): either a normal completion containing an ECMAScript language value or a throw completion

<Sec id="4.8" />

### 7.4.9 IteratorStep ( _iteratorRecord_: an Iterator Record, ): either a normal completion containing either an Object or ~done~, or a throw completion

<Sec id="4.9" />

### 7.4.10 IteratorStepValue ( _iteratorRecord_: an Iterator Record, ): either a normal completion containing either an ECMAScript language value or ~done~, or a throw completion

<Sec id="4.10" />

### 7.4.11 IteratorClose ( _iteratorRecord_: an Iterator Record, _completion_: a Completion Record, ): a Completion Record

<Sec id="4.11" />

### 7.4.12 IteratorCloseAll ( _iters_: a List of Iterator Records, _completion_: a Completion Record, ): a Completion Record

<Sec id="4.12" />

### 7.4.13 IfAbruptCloseIterator ( _value_, _iteratorRecord_ )

<Sec id="4.13" />

### 7.4.14 AsyncIteratorClose ( _iteratorRecord_: an Iterator Record, _completion_: a Completion Record, ): a Completion Record

<Sec id="4.14" />

### 7.4.15 IfAbruptCloseAsyncIterator ( _value_, _iteratorRecord_ )

<Sec id="4.15" />

### 7.4.16 CreateIteratorResultObject ( _value_: an ECMAScript language value, _done_: a Boolean, ): an Object that conforms to the IteratorResult interface

<Sec id="4.16" />

### 7.4.17 CreateListIteratorRecord ( _list_: a List of ECMAScript language values, ): an Iterator Record

<Sec id="4.17" />

### 7.4.18 IteratorToList ( _iteratorRecord_: an Iterator Record, ): either a normal completion containing a List of ECMAScript language values or a throw completion

<Sec id="4.18" />
